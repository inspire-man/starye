## 1. Formula

- [x] 1.1 新增 `watchlist-environment-v1` 纯函数；完成标准：覆盖覆盖率、上涨占比、有效信号、风险提示、样本不足和待同步排除。
- [x] 1.2 补充纯函数单测；完成标准：正向、分化/防守和不足样本边界通过。

## 2. Workbench

- [x] 2.1 在 Quant 总览展示观察池环境摘要；完成标准：显示状态、结论、四个比例、口径和提醒。
- [x] 2.2 增加桌面/窄屏响应式样式；完成标准：指标单列/双列切换，文本和进度条不溢出。

## 3. Verification

- [x] 3.1 运行 Quant 测试、类型检查、lint、build、OpenSpec strict 和 GitNexus detect_changes；完成标准：全部通过且只影响预期前端文件。
- [x] 3.2 通过 Gateway 浏览器验证桌面和移动视口；完成标准：总览能看到环境面板，样本不足/正常状态文案可读。
