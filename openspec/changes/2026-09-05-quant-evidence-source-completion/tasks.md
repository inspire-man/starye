## 1. 因子健康状态

- [x] 1.1 将原始值覆盖、阈值失败和来源健康拆开；完成标准：有限 `fail` 字段计入覆盖，`null` 字段进入缺口，回退链不再标为不可用。
- [x] 1.2 调整因子健康刷新动作；完成标准：失败但有值的 evidence 不触发重试，缺失/不可用/过期/回退仍有动作。
- [x] 1.3 补充纯函数回归测试；完成标准：完整、阈值失败、真实缺失、回退和不可用场景全部通过。

## 2. provider 来源补齐

- [x] 2.1 扩展 Eastmoney 估值明细字段级补充；完成标准：主接口 PEG 缺失时能读取同代码 `PEG_CAR`，已有字段不被覆盖，明细代码错配被拒绝。
- [x] 2.2 解析现金流合法空响应；完成标准：601318 类空日期响应返回空历史而不是 provider error，异常 JSON 仍严格失败。
- [x] 2.3 补充 provider 与路由测试；完成标准：PEG、空现金流、错误分类和其他股东回报字段保持独立。
- [x] 2.4 增加银行/保险行业字段与适用性边界；完成标准：行业专用指标可展示，通用不适用字段不进入缺失/刷新集合，负 PEG 不被推算。
- [x] 2.5 扩展财报来源链；完成标准：Eastmoney 默认主源支持 Tushare `fina_indicator` 同期字段补充，主源失败时可回退，并保留 provider、补充 provider 和安全错误码。
- [x] 2.6 修正金融机构通用现金流口径；完成标准：银行/保险的通用现金流、利息覆盖和支付率不再作为缺失项，行业专用字段和股东回报原始事实继续展示。
- [x] 2.7 扩展现金流备用来源；完成标准：Eastmoney 空历史或核心字段缺失时可使用 Tushare `cashflow`，并保留实际 provider、回退原因和未映射字段的 null 边界。
- [x] 2.8 同步现金流来源元数据到研究报告和详情页；完成标准：Tushare 回退/补充可见，两端空数据仍显示历史不足，不伪造股利、利息或债务。

## 3. 验证与交付

- [x] 3.1 运行定向测试、API/Quant type-check、Quant build 和 OpenSpec strict；完成标准：全部通过。
- [x] 3.2 运行 GitNexus detect_changes 并复核受影响流程；完成标准：只包含本 change 预期 symbols 和 Quant 流程。
- [x] 3.3 通过 Gateway 做认证态桌面/移动回归并记录结果；完成标准：行业专用指标、PEG 不可比、空现金流和刷新动作可见，无浏览器 error/warn 或横向溢出。2026-09-06 认证态 Gateway 已验证 601318 保险指标与刷新、000001 银行指标与行业不适用口径、601857/000001 负 PEG 原始值、现金流来源边界；1920px 与 390x844 均无横向溢出，浏览器 error/warn 为 0。
