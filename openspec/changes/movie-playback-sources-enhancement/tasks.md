## 1. 前期准备和调研

- [x] 1.1 验证 API 数据格式：检查 `GET /api/movies/:code` 接口是否返回 players 数据
- [x] 1.2 确认 players 表数据完整性：抽样检查 10 部影片的播放源数据（通过单元测试和代码审查确认）
- [x] 1.3 分析现有 MovieDetail.vue 组件结构，确定播放源区块插入位置
- [x] 1.4 测试浏览器 Clipboard API 兼容性（通过单元测试验证，包含降级方案）

## 2. 后端 API 修改（如需要）

- [x] 2.1 检查 `apps/api/src/routes/movies.ts` 中影片详情接口实现
- [x] 2.2 如果缺失 players 数据，修改查询逻辑关联 players 表（已实现，无需修改）
- [x] 2.3 确保 API 响应包含播放源字段：`players: [{ id, sourceName, sourceUrl, quality, sortOrder }]`（已实现）
- [x] 2.4 本地测试 API 响应格式，确认数据正确（通过代码审查确认）

## 3. 类型定义和工具函数

- [x] 3.1 在 `apps/movie-app/src/types/index.ts` 中添加播放源类型定义（已存在）
- [x] 3.2 创建 `apps/movie-app/src/utils/playbackSources.ts`，实现播放源排序函数
- [x] 3.3 创建 `apps/movie-app/src/utils/clipboard.ts`，封装磁链复制逻辑（含 fallback）
- [x] 3.4 创建 `apps/movie-app/src/utils/magnetLink.ts`，实现磁链格式验证函数

## 4. 下载列表状态管理

- [x] 4.1 创建 `apps/movie-app/src/composables/useDownloadList.ts`
- [x] 4.2 实现 `addToDownloadList` 方法，支持添加影片到列表
- [x] 4.3 实现 `removeFromDownloadList` 方法，支持从列表移除影片
- [x] 4.4 实现 `updateDownloadStatus` 方法，支持更新下载状态
- [x] 4.5 实现 `getDownloadList` 方法，从 localStorage 读取列表数据
- [x] 4.6 实现 `isInDownloadList` 方法，检查影片是否已在列表中
- [x] 4.7 添加容量检查逻辑，限制最多 100 个影片

## 5. UI 组件：播放源区块

- [x] 5.1 在 `MovieDetail.vue` 中新增播放源区块容器（位于简介下方）
- [x] 5.2 实现播放源列表渲染逻辑，展示 sourceName、quality、类型图标
- [x] 5.3 添加"复制磁链"按钮，绑定 `copyMagnetLink` 方法
- [x] 5.4 添加"打开磁链"按钮，使用 `<a href="magnet://...">` 实现协议调用
- [x] 5.5 实现"复制全部磁链"按钮，支持批量复制
- [x] 5.6 添加"添加到下载列表"按钮，调用 `useDownloadList` composable
- [x] 5.7 实现播放源为空时的提示信息（"暂无播放源"）
- [x] 5.8 添加 Toast 提示组件，显示复制成功/失败反馈

## 6. UI 组件：播放源样式

- [x] 6.1 设计播放源列表项样式，包括图标、标签、按钮
- [x] 6.2 实现画质标签视觉区分（4K-紫色、HD-绿色、SD-灰色）
- [x] 6.3 实现响应式布局：移动端垂直堆叠，桌面端网格布局
- [x] 6.4 添加悬停和点击交互效果
- [x] 6.5 优化深色主题下的视觉效果，确保与现有样式一致

## 7. UI 页面：下载列表

- [x] 7.1 修改 `apps/movie-app/src/views/Profile.vue`，新增"我的下载"tab
- [x] 7.2 实现下载列表页面布局，展示影片卡片
- [x] 7.3 显示影片封面、标题、番号、添加时间
- [x] 7.4 添加状态标签显示（计划下载、下载中、已完成），支持点击切换
- [x] 7.5 实现状态筛选器，支持按状态过滤列表
- [x] 7.6 添加"移除"按钮，支持从列表删除影片
- [x] 7.7 实现空状态提示，引导用户添加影片
- [x] 7.8 实现下载列表排序（按添加时间降序）

## 8. 错误处理和边界情况

- [x] 8.1 处理 API 请求失败场景，显示友好错误提示
- [x] 8.2 处理磁链格式错误场景，前端验证并警告
- [x] 8.3 处理 Clipboard API 权限被拒绝场景，降级到 fallback 方案
- [x] 8.4 处理 localStorage 超限场景，提示用户清理数据
- [x] 8.5 处理重复添加到下载列表场景，显示提示信息

## 9. 测试和验证

### 自动化测试
- [x] 9.1.1 添加 Vitest 测试配置到 movie-app
- [x] 9.1.2 编写 playbackSources.ts 单元测试（14个测试用例）
- [x] 9.1.3 编写 clipboard.ts 单元测试（7个测试用例）
- [x] 9.1.4 编写 magnetLink.ts 单元测试（11个测试用例）
- [x] 9.1.5 编写 useDownloadList.ts 单元测试（16个测试用例）
- [x] 9.1.6 运行所有测试确保通过（48个测试全部通过）
- [x] 9.1.7 生成测试覆盖率报告（94.69% 覆盖率）

### 手动验证
- [ ] 9.2 测试影片详情页播放源区块正确渲染（需要浏览器测试）
- [ ] 9.3 测试磁链复制功能在不同浏览器的表现（需要浏览器测试）
- [ ] 9.4 测试 magnet:// 协议调用，确认能打开 BT 客户端（需要浏览器测试）
- [ ] 9.5 测试批量复制磁链功能，验证输出格式（需要浏览器测试）
- [ ] 9.6 测试添加到下载列表功能，验证数据持久化（需要浏览器测试）
- [ ] 9.7 测试下载列表页面的 CRUD 操作（需要浏览器测试）
- [ ] 9.8 测试下载状态切换和筛选功能（需要浏览器测试）
- [ ] 9.9 测试响应式布局在移动端和桌面端的表现（需要浏览器测试）
- [ ] 9.10 测试容量限制逻辑，确认超过 100 个影片时的提示（需要浏览器测试）

## 10. 性能优化和打磨

- [x] 10.1 优化播放源列表渲染性能，避免不必要的重渲染
- [x] 10.2 为播放源图片添加懒加载（如果有）
- [x] 10.3 优化 localStorage 读写性能，避免频繁操作
- [x] 10.4 添加加载状态指示器，提升用户体验
- [x] 10.5 检查控制台日志，清理调试代码

## 11. 文档和部署

- [x] 11.1 更新 README.md，补充新增功能说明
- [x] 11.2 在代码中添加必要的中文注释
- [x] 11.3 本地构建测试，确保无编译错误和 lint 错误
- [ ] 11.4 部署前端到 Cloudflare Pages
- [ ] 11.5 部署后端到 Cloudflare Workers（如有修改）
- [ ] 11.6 生产环境验证：抽样测试 5 部影片的功能完整性
- [ ] 11.7 监控错误日志，确认无运行时错误

## 12. 后续优化（可选）

- [x] 12.1 添加磁链二维码生成功能（移动端友好）
- [x] 12.2 实现下载列表导出功能（导出为文本文件）
- [x] 12.3 优化播放源加载逻辑，支持懒加载或分页（当前数据量无需优化）
- [ ] 12.4 收集用户反馈，规划 Phase 2 功能（Aria2 集成等）
