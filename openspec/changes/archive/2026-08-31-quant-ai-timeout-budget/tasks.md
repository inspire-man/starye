## 1. 超时契约

- [x] 1.1 增加统一 AI 生成超时常量和 resolver，覆盖默认值、上限、非正数和非有限输入。
- [x] 1.2 将完整 AI 生成器和路由配置接入统一 resolver，连接测试保留短预算。

## 2. API 链路

- [x] 2.1 让完整 AI POST 路由绕过普通 API 30 秒超时，保留同步、行情和其他 API 的现有边界。
- [x] 2.2 补齐环境类型、超时路径测试和现有错误/降级回归测试。

## 3. 验证

- [x] 3.1 运行 API/Quant 测试、类型检查、lint、构建和 OpenSpec strict。
- [x] 3.2 经 Gateway 触发配置测试与完整 AI 入口，复核等待状态、结果完整性、错误日志和持久化行为。
