## 1. 领域契约和持久化

- [x] 1.1 定义决策助手请求、确定性快照、可信度、AI 复核和最终动作类型，并补纯函数测试。
- [x] 1.2 实现 AI 场景 prompt、JSON 响应校验、引用校验、门槛合并和 AI 失败降级测试。
- [x] 1.3 增加 Drizzle schema、D1 migration、快照读回校验、用户/股票索引和 migration 测试。

## 2. API 和客户端

- [x] 2.1 增加 Valibot 请求 schema 和认证 API：创建评估、读取评估历史。
- [x] 2.2 接入 Quant client 的解析器、类型和创建/历史方法，拒绝损坏响应。
- [x] 2.3 增加 API 集成测试，覆盖用户隔离、研究 run 绑定、AI 故障保留确定性结果和持久化读回。

## 3. Quant UI 和验证

- [x] 3.1 在研究详情加入买入/持有场景输入与结果展示，显示价格条件、可信度、来源、AI 纳入状态和持有计算。
- [x] 3.2 增加组件测试，覆盖默认价格、两种模式、校验错误、AI 失败和 390px 长文本换行。
- [x] 3.3 运行 GitNexus detect_changes、API/DB/Quant 类型检查、定向测试、构建、OpenSpec strict。
- [x] 3.4 经 `http://localhost:8080/quant/#candidates` 验证已登录详情流程、快照读回和窄屏无溢出。

## 4. 服务端行情与控件修复

- [x] 4.1 增加 Eastmoney 实时行情 provider，服务端创建评估时自动读取价格、涨跌幅、观察时间，并在失败时明确回退到本地最新收盘；实时行情使用独立 `EASTMONEY_QUOTE_BASE_URL`，默认指向 `push2.eastmoney.com`。
- [x] 4.2 移除 `current_price` 创建请求和前端输入，扩展快照读回字段；保留历史 `user-input` 快照的审计兼容，并补充严格请求拒绝回归。
- [x] 4.3 修复 AI 交叉核对复选框被通用数字输入样式拉伸的问题，补齐自动行情、回退和控件回归测试。
- [x] 4.4 使用真实行情和已配置 AI 完成紫金矿业场景验证，读回 D1 快照并核对来源、时间、AI 状态和最终判断；实际评估返回实时行情，AI 上游失败时保留确定性最终判断并完成 D1 一致性读回。
