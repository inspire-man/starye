## Why
影片详情和管理端仍把 players 行数量当成可播放，用户无法区分未验证、验证失败、仅磁力、以及最近一次真实播放验证。

## What Changes
- 详情页展示播放源可用性状态，而不是播放源数量。
- 播放失败回写来源级原因和最近验证时间。
- 管理端支持按未验证、验证失败、过期筛选。
- 最近可播放列表只使用验证通过的结果。

## Non-goals
不重写播放器内核，不把磁力成功伪装成直接播放。

## Impact
涉及 API、D1 播放证据、Movie 详情页、Dashboard 筛选。必须保留现有 R18 和 repair 任务边界。
