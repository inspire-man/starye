# 任务

## 1. 组件与提示模板

- [x] 1.1 新增对比核对项纯函数模板，保留固定后缀并限制 500 字符；完成标准：空值、长值和正常值测试通过。
- [x] 1.2 暴露候选简报已有问题填充/聚焦动作，并在对比结果核对项增加同级快捷按钮；完成标准：先填充/聚焦，再关闭抽屉，不自动提交。
- [x] 1.3 增加桌面/390px 快捷按钮布局和禁用状态；完成标准：按钮可访问、长文本不溢出。

## 2. 测试与验证

- [x] 2.1 补充纯函数和组件测试；完成标准：模板边界、按钮层级、输入更新、焦点、禁用和手动提交边界通过。
- [x] 2.2 运行 Quant 测试、type-check、lint、build、OpenSpec strict 与 Gateway/browser 验收；完成标准：所有相关检查通过并记录结果。

## 3. 交付

- [x] 3.1 运行 GitNexus `detect_changes` 并检查目标范围；完成标准：仅包含本 change 与 Quant 目标文件。
- [ ] 3.2 提交、推送、创建 PR，Actions 通过后合并；完成标准：合并提交 Actions 通过并清理本地/远端分支。
