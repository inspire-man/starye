## 1. 比较数据

- [x] 1.1 在比较抽屉建立按候选代码隔离的日线读取状态、generation 和历史摘要派生。
- [x] 1.2 复用 520 根日线上限和 `buildTimingHistory`，不新增 API/D1 合同。

## 2. 对比界面

- [x] 2.1 在比较表增加历史时机回看分组，展示基准、样本等级和 lift。
- [x] 2.2 补充加载、数据不足、来源失败和小样本状态，保持窄屏布局。

## 3. 验证与交付

- [x] 3.1 补充组件/生命周期测试，运行 Quant 全量测试、type-check、build、root lint 和 OpenSpec strict。
- [ ] 3.2 Gateway 认证态验证特变电工与紫金矿业并排对照，提交并检查 Actions 后合并。
