# 任务

## 1. 规格与组件实现

- [x] 1.1 补齐重点候选快捷提问的 proposal、design 和 delta spec；完成标准：规格明确同级控件、500 字符边界、手动提交和禁用状态。
- [x] 1.2 在 `QuantAiCandidateBriefing` 中增加候选问题格式化函数、同级快捷按钮和响应式布局；完成标准：详情行为保持不变，快捷按钮只填充并聚焦追问框。

## 2. 测试与验证

- [x] 2.1 增加组件测试覆盖名称/代码提示、输入聚焦、未自动提交、禁用状态和无嵌套按钮；完成标准：Quant 组件测试通过。
- [x] 2.2 运行 Quant 测试、type-check、lint、build、OpenSpec strict 与 Gateway/browser 验收；完成标准：所有相关检查通过并记录证据。

## 3. 交付

- [ ] 3.1 运行 GitNexus `detect_changes` 并检查目标变更范围；完成标准：仅包含本 change 和目标组件/测试文件。
- [ ] 3.2 提交、推送、创建 PR，Actions 通过后合并；完成标准：合并提交 Actions 通过，并删除本地与远端功能分支。
- [ ] 3.3 从最新 `main` 创建下一步开发分支；完成标准：工作树位于新的 `codex/` 分支且不带入用户无关修改。
