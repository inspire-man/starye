## 1. 服务端响应

- [x] 1.1 在决策助手 POST/GET 视图中派生 `factorImpact`，复用研究摘要的已接受因子规则；完成标准：完整、缺口、历史快照均返回一致的审计字段，确定性结果不变。
- [x] 1.2 补充决策助手 API 回归测试；完成标准：验证 AI 纳入权重、未纳入权重、历史回读和用户隔离。

## 2. Quant 客户端与界面

- [x] 2.1 扩展决策助手类型和响应解析，并兼容缺少派生字段的旧响应。
- [x] 2.2 在决策助手结果区展示因子影响审计；完成标准：用户能区分确定性贡献、已纳入、未纳入和未复核，390px 无横向溢出。

## 3. 验证

- [x] 3.1 运行 API/Quant 全量测试、类型检查、lint、构建、OpenSpec strict 和 Gateway 页面回归；完成标准：全部通过且 GitNexus 变更范围符合预期。
