---
phase: 14-test-and-operations-hardening
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - RUNBOOK.md
  - packages/config/src/deployment-target/__tests__/runbook-contract.test.ts
autonomous: true
requirements: [TEST-06]
must_haves:
  truths:
    - "D-05: RUNBOOK presents explicit target selection, local projection/preflight, deploy/migration/crawl, mode/target/run-id smoke, then rollback/recovery in that order."
    - "D-06: Required-secret guidance is a profile-metadata-derived name/consumer/location/preflight matrix with no target resource values or secret values."
    - "D-07/D-08: `passed` is the only completed smoke result; `failed` and `checkpoint` stop subsequent mutation, retain evidence, and route recovery through bounded local/target/provider handling."
  artifacts:
    - path: RUNBOOK.md
      provides: Stable target-first operations procedure
    - path: packages/config/src/deployment-target/__tests__/runbook-contract.test.ts
      provides: Static RUNBOOK ownership, ordering, secret-boundary, and terminal-state regression test
  key_links:
    - from: RUNBOOK.md
      to: packages/config/src/deployment-target/target-profiles.ts
      via: Required secret names/consumers/locations referenced from profile metadata
      pattern: requiredSecrets
  flagged_assumptions:
    - "FLAGGED unresolved edge TEST-06/unclassified: an empty required-secret metadata set must be represented as an explicit no-required-secrets row by the contract rather than silently dropping the stage."
  prohibitions:
    - "secret values"
    - "target resource values"
    - "phase evidence matrix"
    - "provider execution"
---

<objective>
Rewrite the stable operations owner as a target-first, metadata-bound procedure with explicit terminal smoke and recovery behavior, then test it as documentation contract.

Purpose: Fulfill TEST-06 without turning RUNBOOK into a duplicate target registry or a record of transient Phase 13/14 evidence.
Output: A canonical RUNBOOK section and a local static contract test.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-test-and-operations-hardening/14-CONTEXT.md
@.planning/phases/14-test-and-operations-hardening/14-RESEARCH.md
@.planning/phases/14-test-and-operations-hardening/14-PATTERNS.md
@RUNBOOK.md
@docs/documentation-ownership.md
@packages/config/src/deployment-target/target-profiles.ts
@packages/config/src/deployment-target/preflight.ts
@scripts/verify-data-chain-smoke.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the target-first RUNBOOK documentation contract</name>
  <files>packages/config/src/deployment-target/__tests__/runbook-contract.test.ts</files>
  <read_first>RUNBOOK.md; docs/documentation-ownership.md; packages/config/src/deployment-target/target-profiles.ts; packages/config/src/deployment-target/preflight.ts; scripts/verify-data-chain-smoke.ts</read_first>
  <behavior>
    - The operation stages appear in the required target-first order, and local browser evidence names the canonical `http://localhost:8080/...` Gateway entry.
    - The required-secret matrix is reconciled against `TargetProfile.requiredSecrets` names/consumers/local files/CI environment metadata and contains no values.
    - `checkpoint` and `failed` direct an immediate stop/evidence-preservation path; only terminal `passed` is described as a completed smoke result.
  </behavior>
  <action>Create the failing static documentation contract first. Read `RUNBOOK.md` only as stable-owner prose and compare its required-secret metadata cells to `trackedTargetProfiles`; do not parse or log values. Assert the ordered stages from D-05, the smoke terminal-state branches from D-07, bounded D-08 recovery, existing D1/Pages rollback links, no 30-row evidence matrix, and no copied target resource/secret value. The empty-metadata test must require an explicit no-required-secrets representation. Do not invoke a provider, credential, deploy, migration, crawler, rollback, smoke, or browser command.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/runbook-contract.test.ts</automated></verify>
  <acceptance_criteria>The test fails for the old default-domain sequence or for secret/resource/evidence duplication, and expresses the full stable owner contract.</acceptance_criteria>
  <done>A precise local test defines the required RUNBOOK behavior before prose is changed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the canonical operations procedure and preserve owner boundaries</name>
  <files>RUNBOOK.md</files>
  <read_first>RUNBOOK.md; docs/documentation-ownership.md; .planning/phases/14-test-and-operations-hardening/14-CONTEXT.md; packages/config/src/deployment-target/__tests__/runbook-contract.test.ts; .github/workflows/rollback.yml</read_first>
  <action>Rewrite only the stable switching/deploy/smoke/rollback/recovery portions of `RUNBOOK.md` into D-05 order: explicit `TargetProfile` selection, local projection and preflight, operator-triggered deploy/migration/crawl, explicit mode/target/run-id smoke, then rollback/recovery. Generate the required-secret matrix from profile metadata fields only (name, consumer, local file, CI environment, validation/preflight entry); do not copy secret values, account IDs, resource IDs, bucket names, or canonical domain values. Mark `passed` as terminal/completed and make `failed`/`checkpoint` stop, preserve evidence, classify local/target/provider recovery, then require a new validation run after recovery, per D-07/D-08. Link existing D1 safety and manual Pages rollback sections rather than duplicate them, retain Gateway canonical local URLs, and keep the 30-row matrix/history under Phase 14 artifacts. Before commit, run `npx gitnexus detect-changes --repo starye --scope all` and confirm this documentation/test plan did not alter application execution flows.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/runbook-contract.test.ts && git diff --check</automated></verify>
  <acceptance_criteria>RUNBOOK contains one profile-metadata-bound procedure with correct terminal-state and recovery semantics, no secret/resource value copy, and no phase-evidence ownership drift.</acceptance_criteria>
  <done>TEST-06 is documented and mechanically guarded without executing an operational command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TargetProfile metadata -> RUNBOOK table | Stable prose references non-secret names and operator locations. |
| RUNBOOK outcome wording -> operator action | Documentation controls whether an operator may proceed after a smoke result. |
| phase evidence -> stable runbook | Historical statuses must remain in `.planning` rather than becoming operational facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06-01 | Information disclosure | required-secret matrix | high | mitigate | Task 1 compares only metadata fields; Task 2 forbids values and target resource identity. |
| T-14-06-02 | Tampering | operation ordering | high | mitigate | Tasks 1 and 2 enforce explicit target/preflight before remote operator steps. |
| T-14-06-03 | Repudiation | smoke status language | high | mitigate | Contract requires checkpoint/failed stop plus evidence preservation and treats only terminal passed as complete. |
| T-14-06-04 | Elevation of privilege | recovery wording | medium | mitigate | D-08 bounded checklist requires re-preflight and new validation; no automated recovery is introduced. |
</threat_model>

<verification>
Run the RUNBOOK contract test, `git diff --check`, and before commit `npx gitnexus detect-changes --repo starye --scope all`. No provider or credentialed procedure is run.
</verification>

<success_criteria>
- TEST-06 has a target-first stable owner with secrets/resource values kept out of prose.
- Smoke procedures truthfully stop on failed/checkpoint and preserve evidence for recovery.
- RUNBOOK contains no current phase matrix, no production-status inflation, and no duplicated target registry.
</success_criteria>

<output>
Create `.planning/phases/14-test-and-operations-hardening/14-06-SUMMARY.md` when done.
</output>
