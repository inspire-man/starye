---
phase: 14-test-and-operations-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/config/src/deployment-target/target-profile.schema.ts
  - packages/config/src/deployment-target/target-profiles.ts
  - packages/config/src/deployment-target/pages-redirects.ts
  - packages/config/src/deployment-target/index.ts
  - packages/config/src/deployment-target/__tests__/pages-redirects.test.ts
autonomous: true
requirements: [TEST-01]
assumption_delta:
  decision: promote
  canonical_domain_owner: TargetProfile.pages[*].directOrigin and TargetProfile.urls
must_haves:
  truths:
    - "D-01/D-02: Pages redirect origins and destinations are derived from one resolved TargetProfile, never from an app-local default domain."
    - "Each closed Pages surface preserves its current direct-origin, route, and SPA-fallback semantics when rendered for the selected target."
    - "The promoted TargetProfile invariant rejects an incomplete or inconsistent Pages origin map before any materialization consumer can use it."
  artifacts:
    - path: packages/config/src/deployment-target/pages-redirects.ts
      provides: Pure strict selected-target Pages redirect renderer and validator
    - path: packages/config/src/deployment-target/__tests__/pages-redirects.test.ts
      provides: Renderer, profile-invariant, malformed-input, and all-surface contract tests
  key_links:
    - from: packages/config/src/deployment-target/target-profiles.ts
      to: packages/config/src/deployment-target/pages-redirects.ts
      via: Resolved Pages direct-origin metadata and selected canonical URLs
      pattern: directOrigin
  flagged_assumptions:
    - "FLAGGED unresolved edge TEST-01/unclassified: exact retained default-domain fragments must be enumerated by the later tracked-file audit rather than inferred from renderer output."
  prohibitions:
    - "provider commands"
    - "caller-supplied domain argv"
    - "tracked public redirect writes"
---

<objective>
Promote selected TargetProfile Pages metadata into the sole runtime authority for Pages redirect origins and canonical destinations, then establish a pure validated renderer before build integration.

Purpose: Remove the remaining parallel source of Pages domain truth without deploying or contacting Cloudflare.
Output: Strict profile metadata, a typed all-surface renderer, and contract tests.
</objective>

<assumption_delta_decision>
Promote the selected TargetProfile to the canonical runtime source for Pages direct-origin and canonical-domain metadata. Per-app redirect files and hard-coded default domains must not remain parallel runtime sources; the invariant test is the enforcement point.
</assumption_delta_decision>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-test-and-operations-hardening/14-CONTEXT.md
@.planning/phases/14-test-and-operations-hardening/14-RESEARCH.md
@.planning/phases/14-test-and-operations-hardening/14-PATTERNS.md
@.planning/phases/12-cloudflare-config-switching/12-VERIFICATION.md
@packages/config/src/deployment-target/target-profile.schema.ts
@packages/config/src/deployment-target/target-profiles.ts
@packages/config/src/deployment-target/deploy-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical typed Pages origin metadata and its invariant</name>
  <files>packages/config/src/deployment-target/target-profile.schema.ts, packages/config/src/deployment-target/target-profiles.ts, packages/config/src/deployment-target/__tests__/pages-redirects.test.ts</files>
  <read_first>packages/config/src/deployment-target/target-profile.schema.ts; packages/config/src/deployment-target/target-profiles.ts; packages/config/src/deployment-target/__tests__/target-profile.schema.test.ts; .planning/phases/14-test-and-operations-hardening/14-CONTEXT.md</read_first>
  <behavior>
    - A resolved profile supplies a non-secret direct Pages origin for dashboard, auth, blog, movie, and comic, alongside the existing canonical URL and project.
    - A missing, non-HTTPS, or cross-surface direct origin makes TargetProfile parsing fail before materialization.
    - The selected `starye-org` fixture keeps Blog's direct Pages host explicit instead of deriving it from its project name.
  </behavior>
  <action>Before modifying existing symbols, run GitNexus impact analysis for `parseTargetProfile`, `targetProfileSchema`, and any changed profile helper; surface HIGH or CRITICAL output before editing. Extend the strict Pages record and `trackedTargetProfiles` with a typed non-secret `directOrigin` for every closed Pages surface, per D-02 and the promoted assumption-delta decision. Preserve `requiredSecrets` as metadata only. Add the profile-invariant tests first, including missing/invalid origin and the non-derivable Blog origin, then implement the schema/profile changes required to make them pass. Do not add default resolution, legacy aliases, secret values, or application-local domain fields.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/pages-redirects.test.ts src/deployment-target/__tests__/target-profile.schema.test.ts</automated></verify>
  <acceptance_criteria>Every selected Pages surface has validated profile-owned origin metadata; malformed or guessed metadata fails closed.</acceptance_criteria>
  <done>TargetProfile is the only selected-target source needed to render Pages redirect origins and destinations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the strict pure Pages redirect renderer</name>
  <files>packages/config/src/deployment-target/pages-redirects.ts, packages/config/src/deployment-target/index.ts, packages/config/src/deployment-target/__tests__/pages-redirects.test.ts</files>
  <read_first>packages/config/src/deployment-target/deploy-config.ts; packages/config/src/deployment-target/target-projections.ts; apps/auth/public/_redirects; apps/blog/public/_redirects; apps/dashboard/public/_redirects; apps/movie-app/public/_redirects; apps/comic-app/public/_redirects</read_first>
  <behavior>
    - Each of the five closed surfaces renders its current direct-origin redirect, selected canonical path, and SPA fallback in the existing ordering.
    - Unknown surface, unresolved placeholder, unsafe newline, non-profile origin, or template/surface mismatch is rejected.
    - Rendering uses only a resolved profile and a closed surface enum; no arbitrary hostname or path input is accepted.
  </behavior>
  <action>Before modifying exported symbols, run GitNexus impact analysis for the affected deployment-target exports and report any HIGH or CRITICAL result. Create `pages-redirects.ts` as a pure renderer/validator with a closed `TargetPagesSurface` map that preserves each current route and SPA fallback semantics while sourcing origins and canonical destinations from the resolved profile, per D-01/D-02. Reject missing or unknown placeholders, unresolved token syntax, unsafe line breaks, unsupported origins, and surface/template mismatches. Export only the typed API through `index.ts`; write renderer tests before implementation and cover every surface plus hostile input. Do not read process environment, filesystem paths, credentials, or call build/deploy commands.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/pages-redirects.test.ts</automated></verify>
  <acceptance_criteria>All five redirect contracts render from resolved profile data, and malformed render requests fail before producing text.</acceptance_criteria>
  <done>A pure, tested renderer is ready for run-scoped build materialization without creating a second domain source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected TargetProfile -> redirect renderer | Non-secret identity metadata determines externally visible redirect hosts and paths. |
| renderer input -> generated redirect text | Template and surface values must not introduce arbitrary hosts, paths, or control characters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01-01 | Tampering | TargetProfile Pages metadata | high | mitigate | Task 1 adds strict per-surface metadata and a parse-time canonical-link invariant. |
| T-14-01-02 | Tampering | pages-redirects renderer | high | mitigate | Task 2 accepts only resolved profiles and closed surfaces, then rejects unknown placeholders and unsafe output. |
| T-14-01-03 | Information disclosure | renderer/profile boundary | medium | mitigate | Tasks retain secret names and values outside redirect inputs and assert renderer parameters have no environment or credential channel. |
| T-14-01-04 | Elevation of privilege | caller-selected host | high | mitigate | Task 2 prohibits caller-provided domains and tests surface/origin mismatch rejection. |
</threat_model>

<verification>
Run the two targeted Vitest suites and `pnpm --filter @starye/config type-check`. Before this plan's commit, run `npx gitnexus detect-changes --repo starye --scope all`; retain only the expected deployment-target symbols and report any unexpected HIGH or CRITICAL flow.
</verification>

<success_criteria>
- D-01/D-02 are enforced by one promoted TargetProfile authority and strict renderer tests.
- Every Pages surface has a deterministic render contract without tracked default-domain redirect output.
- No provider, credential, deployment, migration, crawler, or smoke command is executed.
</success_criteria>

<output>
Create `.planning/phases/14-test-and-operations-hardening/14-01-SUMMARY.md` when done.
</output>
