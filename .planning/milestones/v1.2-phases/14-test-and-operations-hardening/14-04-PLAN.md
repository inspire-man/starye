---
phase: 14-test-and-operations-hardening
plan: "04"
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - packages/config/src/deployment-target/legacy-domain-audit.ts
  - packages/config/src/deployment-target/__tests__/legacy-domain-audit.test.ts
  - scripts/audit-legacy-domain.ts
  - package.json
  - apps/api/.dev.vars.example
  - apps/auth/typecheck.pages-build.env
  - apps/gateway/src/index.ts
  - apps/blog/e2e/session.spec.ts
  - apps/dashboard/e2e/auth-crosspath.spec.ts
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "D-01/D-03: The static audit enumerates only Git-tracked active source, configuration, and test files, using a fixed `starye.org` string comparison."
    - "D-01/D-04: Every retained occurrence requires exact path plus fragment/category and a reason; the first green run allows zero unclassified active matches."
    - "Active configuration examples, stale source comments, and unrelated email test identities no longer carry an unclassified production-domain literal."
  artifacts:
    - path: packages/config/src/deployment-target/legacy-domain-audit.ts
      provides: Pure strict fixed-literal audit and exact allowance model
    - path: scripts/audit-legacy-domain.ts
      provides: Tracked-file-only CLI adapter with no mutation or remote behavior
  key_links:
    - from: scripts/audit-legacy-domain.ts
      to: packages/config/src/deployment-target/legacy-domain-audit.ts
      via: git ls-files NUL-delimited active input enumeration and injected reads
      pattern: "git ls-files"
  flagged_assumptions:
    - "FLAGGED unresolved edge TEST-01/unclassified: source classification remains explicit-only; a newly tracked active file containing the fixed legacy domain is a failing unknown until a path-and-fragment allowance or migration is added."
  prohibitions:
    - "directory allowlists"
    - "glob allowlists"
    - "baseline counts"
    - "broad regular expressions"
    - "provider commands"
---

<objective>
Create the strict tracked-file legacy-domain gate and migrate the non-redirect active configuration/source/fixture surfaces it identifies.

Purpose: Make TEST-01 fail closed on first green rather than preserving an undocumented old-domain baseline.
Output: Pure audit, read-only CLI, package check command, and migrated non-redirect active literals.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-test-and-operations-hardening/14-CONTEXT.md
@.planning/phases/14-test-and-operations-hardening/14-RESEARCH.md
@.planning/phases/14-test-and-operations-hardening/14-PATTERNS.md
@.planning/phases/14-test-and-operations-hardening/14-03-SUMMARY.md
@packages/config/src/deployment-target/preflight.ts
@packages/config/src/deployment-target/target-profiles.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the fixed-literal, exact-allowance audit</name>
  <files>packages/config/src/deployment-target/legacy-domain-audit.ts, packages/config/src/deployment-target/__tests__/legacy-domain-audit.test.ts, scripts/audit-legacy-domain.ts, package.json</files>
  <read_first>packages/config/src/deployment-target/preflight.ts; packages/config/src/deployment-target/target-profiles.ts; packages/config/src/deployment-target/__tests__/preflight.test.ts; .planning/phases/14-test-and-operations-hardening/14-RESEARCH.md</read_first>
  <behavior>
    - Only a fixed occurrence of `starye.org` in a Git-tracked active source/config/test file is considered; `starye-org` is not a match.
    - Every retained match is matched to exact path plus fragment/category and an explicit reason; directories, globs, broad regexes, and count baselines are rejected by the model.
    - Docs, `.planning`, ignored local env, generated output, and untracked Phase 13 evidence are excluded before file reads; the CLI has no write, spawn, credential, or network behavior beyond tracked-file enumeration.
  </behavior>
  <action>Before modifying existing deployment-target symbols, run GitNexus impact analysis for any imported public helper; report HIGH or CRITICAL output before changes. Create a pure audit with injected tracked paths and file reads, deterministic `path:line:fragment` issues, exact allowance records, and fixed-string matching, per D-01/D-03/D-04. Write failure-first tests for an unclassified match, an exact allowed default profile fragment, a legacy-alias deny-list fragment, a named fixture fragment, and the non-match hyphenated target ID. Add a thin read-only CLI that obtains tracked paths with NUL-safe Git output, filters active extensions/path classes before reading, and expose it through a root `check:legacy-domain` command. Do not shell out to provider tools or implement a report-only/baseline mode.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/legacy-domain-audit.test.ts && pnpm check:legacy-domain</automated></verify>
  <acceptance_criteria>The audit is deterministic, strict, tracked-file-only, and rejects every form of broad allowance or accidental `starye-org` match.</acceptance_criteria>
  <done>A reusable local TEST-01 gate exists and initially identifies only explicit remaining migration work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate active configuration, comment, and email fixtures</name>
  <files>apps/api/.dev.vars.example, apps/auth/typecheck.pages-build.env, apps/gateway/src/index.ts, apps/blog/e2e/session.spec.ts, apps/dashboard/e2e/auth-crosspath.spec.ts</files>
  <read_first>apps/api/.dev.vars.example; apps/auth/typecheck.pages-build.env; apps/gateway/src/index.ts; apps/blog/e2e/session.spec.ts; apps/dashboard/e2e/auth-crosspath.spec.ts; packages/config/src/deployment-target/legacy-domain-audit.ts</read_first>
  <behavior>
    - Active env example and typecheck input use target-neutral placeholders or typed generated fixtures, not default target URLs.
    - Gateway text becomes target-neutral without changing runtime behavior.
    - Blog and Dashboard E2E test identities use dedicated `.test` addresses and retain the same test behavior.
  </behavior>
  <action>Before modifying `apps/gateway/src/index.ts` or its surrounding symbols, run GitNexus impact analysis and report HIGH or CRITICAL output before edit. Use the Task 1 audit as the test oracle, then remove the active R2 public URL default from the API example, put the Auth typecheck build values behind an explicitly named test/generated fixture boundary, replace the stale Gateway comment without changing runtime logic, and replace only E2E email identities with `.test` values. Add or adjust focused tests before implementation where behavior changes. Do not add a new app-local target source, change application URLs, alter Gateway routing, or allowlist these migrated occurrences.</action>
  <verify><automated>pnpm check:legacy-domain && pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/legacy-domain-audit.test.ts && pnpm --filter blog exec playwright test e2e/session.spec.ts --list && pnpm --filter dashboard exec playwright test e2e/auth-crosspath.spec.ts --list</automated></verify>
  <acceptance_criteria>Those five active non-redirect surfaces no longer produce unclassified fixed-literal audit records and retain their intended fixture/runtime behavior.</acceptance_criteria>
  <done>The non-redirect application/config matches have been migrated instead of grandfathered.</done>
</task>

<task type="auto">
  <name>Task 3: Make the local audit a required static test gate</name>
  <files>package.json</files>
  <read_first>package.json; scripts/audit-legacy-domain.ts; packages/config/src/deployment-target/__tests__/legacy-domain-audit.test.ts; AGENTS.md</read_first>
  <action>Keep the root command narrowly scoped to the read-only audit and make its failure status propagate to local and CI callers, per D-04. Verify the command accepts no target, credential, provider, deploy, migration, crawler, rollback, or smoke option. Before this plan's commit, run `npx gitnexus detect-changes --repo starye --scope all`; report any changed application symbol or execution flow outside the named config/comment/fixture scope.</action>
  <verify><automated>pnpm check:legacy-domain</automated></verify>
  <acceptance_criteria>The repository exposes one deterministic static command that fails on the first unclassified tracked active legacy-domain match.</acceptance_criteria>
  <done>TEST-01 has a repeatable no-network gate instead of a manual inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git tracked-file list -> audit reader | Repository filenames select files whose text will be evaluated. |
| exact allowance -> audit verdict | An allowlist controls whether a legacy-domain occurrence can remain active. |
| test/config source -> static gate | Source literals can accidentally reintroduce old target identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04-01 | Tampering | allowance model | high | mitigate | Task 1 requires exact path/fragment/category/reason and rejects broad/baseline constructs. |
| T-14-04-02 | Tampering | file enumeration | medium | mitigate | Task 1 restricts to NUL-safe Git-tracked active inputs and tests exclusions before reads. |
| T-14-04-03 | Information disclosure | audit CLI | medium | mitigate | Task 1 limits diagnostics to paths/fragments and has no credential/provider execution channel. |
| T-14-04-04 | Repudiation | legacy literal migration | medium | mitigate | Tasks 2 and 3 turn every active occurrence into migration or an explicit auditable allowance. |
</threat_model>

<verification>
Run the audit unit suite, `pnpm check:legacy-domain`, the two E2E discovery commands, and `git diff --check`. Before commit run `npx gitnexus detect-changes --repo starye --scope all`.
</verification>

<success_criteria>
- D-01/D-03/D-04 produce a strict, local, repeatable fixed-literal gate.
- Non-redirect source/config/fixture literals are migrated rather than hidden by a broad exception.
- The gate performs no remote, credentialed, provider, or runtime smoke operation.
</success_criteria>

<output>
Create `.planning/phases/14-test-and-operations-hardening/14-04-SUMMARY.md` when done.
</output>
