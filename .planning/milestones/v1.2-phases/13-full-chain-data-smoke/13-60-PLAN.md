---
phase: 13-full-chain-data-smoke
plan: "60"
type: execute
wave: 54
depends_on: [13-56, 13-57]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-60-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - TEST-05
user_setup:
  - service: Signed-in local Gateway Dashboard session
    why: p13-57 froze at dashboard_auth_unavailable after unauthenticated default observe. A signed-in local session (Codex IAB tab or untracked cookie file) must exist BEFORE allocation and must be the only observe path.
    env_vars:
      - name: STARYE_DATA_CHAIN_SESSION_COOKIE_FILE
        source: untracked local cookie file path only (optional if IAB observeSurface is used)
      - name: STARYE_DATA_CHAIN_SESSION_COOKIE
        source: untracked inline cookie only if file path is unavailable (never commit)
must_haves:
  truths:
    - "13-56 nested+standalone remote preflight remains green history; p13-57 remains immutable dashboard_auth_unavailable and is never reopened."
    - "Operator proves local Dashboard is signed-in at http://localhost:8080/dashboard before any p13-60 run id is allocated (IAB session and/or non-secret get-session proof; no secret values in Summary)."
    - "Exactly one local handoff produces one unobserved pending pair for a new p13-60-<32 hex> run id; p13-57/p13-55/p13-52/p13-50/p13-49/p13-41 are never reused."
    - "Observation uses only a signed-session adapter: Codex IAB observeSurface bridge and/or cookie-backed observeSurfaceDefault. Unauthenticated default observe is forbidden."
    - "Ordered Dashboard then Viewer on Gateway 8080 reaches terminal_passed with provesExternalChain true, or freezes an honest post-allocation checkpoint without retry."
    - "All p13-60 evidence remains untracked; no remote or production claim is made."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-60-RUN-ID.txt
      provides: sole allocated p13-60 identifier when pre-allocation session gates pass
    - path: .planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
      provides: non-secret local terminal or honest checkpoint closeout
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-60-run-id}/local.json
      provides: untracked tuple-bound local evidence
  key_links:
    - from: signed-in local Dashboard proof
      to: pnpm check:services dual readiness
      via: session gate before allocation
      pattern: "local_dashboard_session_ready"
    - from: pnpm smoke:data-chain:handoff
      to: observeDataChainSurfaces with signed IAB/cookie observeSurface only
      via: one pending local pair observed Dashboard-before-Viewer
      pattern: "resolved_pending_observation"
---

<objective>
Create one fresh local-only terminal data-chain proof after the 13-57 dashboard-auth freeze, with a hard pre-allocation signed-session gate so unauthenticated observe cannot re-freeze the carrier.

Purpose: 13-57 is immutable at `dashboard_auth_unavailable` because default puppeteer observe ran without a signed session. This plan requires operator-signed local Dashboard proof first, allocates `p13-60-<32 hex>`, runs one local handoff, and observes via IAB and/or cookie-backed adapter only.

Output: `13-60-RUN-ID.txt` only if allocated, untracked local evidence, and `13-60-SUMMARY.md`. Local proof only.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
</context>

## Artifacts this phase produces
- `13-60-RUN-ID.txt` (conditional)
- untracked `evidence/starye-org/p13-60-*/local.{attempt,json,md}`
- `13-60-SUMMARY.md`

## Non-goals
- No reuse/reopen of p13-57 / p13-55 / p13-52 / p13-50 / p13-49 / p13-41
- No remote handoff, production browser, provider deploy, or migration
- No unauthenticated `pnpm smoke:data-chain-observe` / default puppeteer observe
- No second handoff/observer/verifier after a post-allocation non-success
- No cookie/secret logging or tracked session material

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Prove signed-in local Dashboard session before allocation</name>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
  </read_first>
  <action>
Treat p13-57 as immutable checkpoint history. Require operator to sign in at http://localhost:8080/dashboard (Codex IAB or browser). Accept one of: (a) operator confirmation that IAB is signed-in and will be used as observeSurface adapter, or (b) untracked cookie material via STARYE_DATA_CHAIN_SESSION_COOKIE_FILE / cookie file under .untracked-session (never commit secrets). Optionally prove GET http://localhost:8080/api/auth/get-session returns user without logging cookie values. If session is not ready, write pre-allocation Summary blocked_without_local_session and stop with no run id.
  </action>
  <done>Local Dashboard signed-session path is ready, or honest pre-allocation block is recorded with no p13-60 id.</done>
</task>

<task type="auto">
  <name>Task 2: Dual Gateway readiness and allocate one p13-60 handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-60-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
Only after Task 1 session gate: confirm 13-60-RUN-ID.txt and evidence/starye-org/p13-60-* are absent. Run two consecutive pnpm check:services with robots/auth/authSlash accepted. Allocate exactly one run id p13-60- + 32 lowercase hex into 13-60-RUN-ID.txt. Run one local handoff:

`pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <p13-60>`

Then one pending exact local verifier. On non-success, freeze honest Summary and stop; no second handoff. Never write remote artifacts. Never call unauthenticated observe in this task.
  </action>
  <acceptance_criteria>
    - Session gate completed before run id exists.
    - Exactly one p13-60 run id allocated.
    - Local handoff produces unobserved pending local evidence under that run only.
    - Historical run trees remain untouched.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-60-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>One unobserved local pending pair exists for p13-60, or post-allocation work is honestly frozen before observe.</done>
</task>

<task type="auto">
  <name>Task 3: Signed-session ordered Dashboard→Viewer observation and dual terminal verify</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
  </read_first>
  <action>
Observe Gateway local surfaces in order using ONLY a signed-session adapter: Codex IAB observeSurface bridge (preferred; 13-55 pattern) and/or cookie-backed observeSurfaceDefault with STARYE_DATA_CHAIN_SESSION_COOKIE_FILE. Forbidden: bare `pnpm smoke:data-chain-observe` without cookie/IAB. Paths: Dashboard `http://localhost:8080/dashboard/movies` then Viewer `http://localhost:8080/movie/<item-code>`. Run dual exact local verifiers expecting terminal_passed / provesExternalChain true. On dashboard_auth_unavailable or viewer failure, freeze honest local checkpoint without retry. Write non-secret 13-60-SUMMARY.md. Do not authorize or run remote handoff here.
  </action>
  <acceptance_criteria>
    - Observe adapter is IAB and/or cookie-backed only.
    - Observation order is Dashboard then Viewer on Gateway 8080 only.
    - Dual local verify is terminal_passed / provesExternalChain true, or honest local checkpoint is recorded.
    - No remote mode commands executed; p13-57/p13-55 untouched.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-60-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>p13-60 local terminal proof exists and unlocks human-gated remote handoff planning, or local work is honestly blocked.</done>
</task>
</tasks>

## Verification
- Session gate before allocation
- New p13-60 local carrier only
- Gateway-first URLs only
- p13-57/p13-55 immutable
- Evidence untracked

## Self-check (plan_checker disabled)
- Directly closes 13-57 root cause (unsigned observe)
- Supersedes executable intent of 13-58/13-59 carrier binding to p13-57
- Remote auth deferred to 13-61
