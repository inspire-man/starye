---
phase: 13-full-chain-data-smoke
plan: "72"
type: tdd
wave: 66
depends_on: [13-69]
files_modified:
  - scripts/data-chain-surface-observation.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "A root IAB signed-in Dashboard tab and the observer that writes Dashboard-to-Viewer receipts are the same in-memory owner, so an isolated Puppeteer profile cannot turn a root-session proof into dashboard_auth_unavailable."
    - "The root-IAB readiness probe and ordered observation callback retain only canonical route, status, code, and itemId facts; cookie values, storage, browser profiles, headers, and raw provider responses are outside the adapter and evidence contracts, per D-05 and D-06."
    - "A standalone observation CLI without a supplied root-IAB adapter stops before evidence mutation, carrier allocation, local verification, or remote work."
    - "The existing p13-69 checkpoint remains immutable diagnosis; this repair does not inspect, retry, verify, or modify any prior carrier or untracked evidence tree."
  artifacts:
    - path: scripts/data-chain-surface-observation.ts
      provides: A typed root-IAB-only probe and observation port plus a core that rejects the independent-session fallback before reading or writing evidence.
    - path: packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
      provides: Root-IAB local canonical-route, ordering, no-fallback, and first-checkpoint regression coverage.
    - path: packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
      provides: Selected-target canonical remote root-IAB adapter regression coverage.
    - path: .planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md
      provides: Non-secret record of the observer-ownership repair and focused validation only.
  key_links:
    - from: RootIabSurfaceObserver probe
      to: fresh local carrier allocation
      via: A root-owned canonical Dashboard readiness result is required before Plan 13-73 may create its run id.
      pattern: root_iab and ready
    - from: observeDataChainSurfaces
      to: Dashboard and Viewer receipts
      via: The core accepts only the in-memory root-IAB port and retains its existing exact tuple and Dashboard-first validation.
      pattern: dashboard then viewer
    - from: standalone observer CLI
      to: evidence persistence
      via: Missing root-IAB ownership fails before the pending pair is loaded or rewritten.
      pattern: root_iab_adapter_required
---

<objective>
Eliminate the observer-session split diagnosed by immutable p13-69: only a root-owned IAB adapter may supply Dashboard and Viewer observations, and an independent Puppeteer/cookie/profile observer may not act as a fallback.

Purpose: p13-69 proved that the root IAB can display an authenticated canonical local Dashboard while the repository observer independently reported `dashboard_auth_unavailable`. The missing link is observer ownership, not another local fixture, run id, or retry. This plan closes that code-level gap before any fresh carrier is considered.

Output: Tested root-IAB adapter and pre-evidence refusal contract in the observation script, targeted tests, and `13-72-SUMMARY.md`. It performs no local handoff, browser navigation, evidence operation, provider command, or production activity.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-69-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
@scripts/data-chain-surface-observation.ts
@packages/config/src/deployment-target/data-chain-evidence.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
</context>

<interfaces>
- Replace the optional generic `observeSurface` fallback with an explicit `RootIabSurfaceObserver` port that has a closed `owner: 'root_iab'` marker, `probeDashboard`, and `observeSurface` callbacks. The port receives only the controlled mode, target, canonical base, path, code, and itemId inputs and returns only allowlisted status/code/itemId result fields.
- Export a `verifyRootIabObservationReadiness` helper that resolves the canonical base through the existing target resolver, probes only the Dashboard route, validates the closed result shape, and has no evidence read/write dependency.
- `observeDataChainSurfaces` consumes the same root-IAB port for receipt capture. Its CLI entry must fail before reading a pending pair because a separate process cannot possess the root in-app Browser handle.
- Keep `appendBrowserObservation`, receipt integrity, target resolution, route derivation, and Dashboard-before-Viewer evidence grammar repository-owned. The adapter never writes evidence and never supplies caller-selected code, itemId, origin, or path.
</interfaces>

## Artifacts This Phase Produces

| Artifact | Owner | Produced state |
| --- | --- | --- |
| `scripts/data-chain-surface-observation.ts` | Root observation boundary | Root-IAB probe/observation port and fail-closed absence behavior. |
| Focused local and remote tests | Regression boundary | Tests prove canonical ownership, no independent-session fallback, exact tuple order, and checkpoint stop. |
| `13-72-SUMMARY.md` | Phase 13 | Non-secret repair and validation record; no carrier identifier or live evidence. |

## Source Coverage Audit

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 13 | Fresh local then selected-production canonical data-chain proof | 13-72 to 13-75 | COVERED | 13-72 removes the root-session choke point; 13-73 through 13-75 execute the complete new-carrier chain conditionally. |
| REQ | DATA-01 | Local Gateway-only API/auth/Dashboard/content path | 13-73 | COVERED | Fresh local work uses `http://localhost:8080` only. |
| REQ | DATA-02 | Local D1 and minimal-data readiness before ingest | 13-73 | COVERED | The repository handoff remains after canonical readiness gates. |
| REQ | DATA-03 | One deterministic controlled fixture | 13-73, 13-74 | COVERED | One local and one authorized remote handoff retain the count-one contract. |
| REQ | DATA-04 | D1/API/admin tuple correlation | 13-74, 13-75 | COVERED | Remote pending eligibility and production observation require the exact code/itemId tuple. |
| REQ | DATA-05 | Authenticated Dashboard validation | 13-72, 13-73, 13-75 | COVERED | Root-IAB ownership is explicit for local and selected-production Dashboard observations. |
| REQ | DATA-06 | Canonical Viewer validation | 13-72, 13-73, 13-75 | COVERED | Core preserves Dashboard-before-Viewer route and tuple checks. |
| REQ | DATA-07 | Non-secret local and production evidence | 13-72 to 13-75 | COVERED | Ports and summaries remain allowlisted; fresh evidence is frozen on first non-success. |
| REQ | TEST-05 | Repeatable terminal/checkpoint smoke output | 13-72 to 13-75 | COVERED | Adapter tests precede the fresh run; focused handoff and evidence tests gate later plans. |
| RESEARCH | R-01 | Reuse closed target, handoff, receipt, and Gateway-first seams; add no package | 13-72 to 13-75 | COVERED | The repair alters only the observation seam and preserves the existing target/evidence core. |
| RESEARCH | R-02 | First checkpoint stops downstream execution for its carrier | 13-72 to 13-75 | COVERED | Every live plan has an explicit first-non-success freeze branch. |
| RESEARCH | R-03 | No raw cookie, profile, endpoint, or prepared context in evidence | 13-72 to 13-75 | COVERED | The independent cookie/profile observer is removed from the carrier path. |
| CONTEXT | D-01 | Exactly one non-R18 item and one player | 13-73, 13-74 | COVERED | Only existing count-one handoff surfaces are used. |
| CONTEXT | D-02 | Controlled fixture or targeted adapter only | 13-73, 13-74 | COVERED | No broad crawler or parallel write path is introduced. |
| CONTEXT | D-03 | Local-first and canonical Gateway | 13-72, 13-73 | COVERED | Adapter qualification happens before fresh local allocation and local observation. |
| CONTEXT | D-04 | Exact target/run/code and mode-owned itemId | 13-72 to 13-75 | COVERED | Root adapter receives derived tuple only and returns it for strict correlation. |
| CONTEXT | D-05 | Explicit target, preflight, provider fail-closed | 13-72, 13-74, 13-75 | COVERED | Local adapter carries no provider capability; P74 retains the explicit selected-target gate. |
| CONTEXT | D-06 | Allowlisted non-secret evidence | 13-72 to 13-75 | COVERED | Adapter result shape and summaries exclude session and provider material. |
| CONTEXT | D-07 | Real Phase 13 execution, no Phase 14 substitution | 13-73 to 13-75 | COVERED | Fresh carrier evidence and live verification remain Phase 13-owned. |

<tasks>

<task type="auto">
  <name>Task 1: Write failing root-IAB ownership tests for the observer seam</name>
  <files>packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
  </read_first>
  <action>
Before changing `observeDataChainSurfaces`, `runDataChainSurfaceObservationCli`, or the default observation implementation, run GitNexus upstream impact analysis for each symbol with the file-path hint. Record direct callers and affected flows in the execution summary. If either report is HIGH or CRITICAL, present that blast-radius warning before editing and wait for user acknowledgement; the currently observed LOW scope is not a substitute for this fresh check. Preserve the pre-existing user whitespace edits in both files.

Add RED tests for a closed in-memory `RootIabSurfaceObserver` port. Cover a local canonical Dashboard readiness probe, the exact Dashboard-then-Viewer tuple passed through the root-owned callback, refusal when the port is absent or has the wrong owner marker, and a Dashboard non-success that permits no Viewer callback. Assert that a standalone CLI path has no independent browser/profile/session fallback and fails before any evidence reader or writer is invoked. Add the matching remote test that supplies the same root port and proves its inputs use the selected TargetProfile HTTPS canonical origin and the pending remote tuple, never a local origin. The test doubles may return only status, itemCode, and itemId facts; they must not accept or assert credentials, browser storage, headers, or raw endpoints. These tests implement D-03, D-04, D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new ownership tests fail against the current independent-session fallback.
    - Tests cover both canonical local and selected-production bases, exact tuple correlation, Dashboard-first ordering, and the first-checkpoint stop.
    - No test fixture introduces a cookie, profile directory, provider response, or caller-chosen route into the observation contract.
  </acceptance_criteria>
  <done>Focused tests precisely specify a root-IAB-only observation contract before the production implementation changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the root-IAB-only observation port and remove the split-session fallback</name>
  <files>scripts/data-chain-surface-observation.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts, .planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
  </read_first>
  <behavior>
    - A root-owned adapter probes the canonical Dashboard without reading or writing evidence and returns only ready/unavailable.
    - Pending evidence is observed only through that same root-owned adapter; exact Dashboard then Viewer receipts remain core-owned.
    - Missing, malformed, or non-root adapters fail before independent browser launch, session lookup, evidence mutation, or a carrier-side action.
    - The standalone observer CLI cannot impersonate the root in-app Browser and exits nonzero before it touches the pending pair.
  </behavior>
  <action>
Implement the tested `RootIabSurfaceObserver` and `verifyRootIabObservationReadiness` in `scripts/data-chain-surface-observation.ts`. The port must use a closed root owner marker, a Dashboard readiness probe, and an observation callback that each receive only canonical derived inputs and yield only allowlisted result fields. Make `observeDataChainSurfaces` require this port before loading evidence; preserve existing target resolution, evidence tuple derivation, receipt creation, JSON/Markdown parity, and Dashboard-before-Viewer first-checkpoint behavior.

Remove the automatic independent Puppeteer session path from the fresh-carrier execution route, including cookie environment/file resolution, browser-profile construction, and implicit default observer selection. Retain no route that lets the CLI create a substitute browser session; make the CLI report the missing root-IAB adapter before it reads or writes a pending pair. Keep changes limited to this observation seam and its two focused test files; leave `data-chain-evidence.ts`, handoff code, API transport, historical summaries, and untracked evidence untouched. Update `13-72-SUMMARY.md` with only impacted symbols, GitNexus risk result, focused command outcomes, and the fact that no carrier or external operation ran. This task implements D-03 through D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts; pnpm --filter @starye/config type-check</automated>
  </verify>
  <acceptance_criteria>
    - The default observer can no longer launch a separate session or consume session material for a carrier observation.
    - Root-IAB readiness and receipt observation use the same in-memory owner port, while the repository core still owns target/base resolution, tuple validation, evidence serialization, and ordering.
    - A missing or invalid port fails before evidence mutation; all focused tests and the config type-check pass.
    - The summary records no run id, no local/remote evidence action, and no production navigation.
  </acceptance_criteria>
  <done>The root-IAB ownership boundary is executable and regression-tested, so Plan 13-73 can gate a new local carrier on an actual shared observer session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Root IAB to observation core | A privileged signed browser supplies only visible route/status/tuple facts through an in-memory port. |
| Observation core to evidence pair | Repository code validates canonical paths, exact tuple, receipt integrity, and first-checkpoint lifecycle before persistence. |
| Standalone process to root IAB | A separate CLI process has no authority to reuse or recreate the root browser session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-72-01 | Spoofing | Observer ownership | high | mitigate | Require the closed root owner marker and readiness probe before any evidence observation can start. |
| T-13-72-02 | Information disclosure | Session boundary | critical | mitigate | Remove independent cookie/profile resolution from the carrier observer and restrict callback output to allowlisted facts. |
| T-13-72-03 | Tampering | Pending evidence | high | mitigate | Reject absent or malformed adapters before pair load/write; retain core-owned tuple, receipt, and ordering validation. |
| T-13-72-04 | Elevation of privilege | Dashboard access | high | mitigate | Preserve actual Dashboard auth/role behavior; a root probe or tuple failure is non-success and never a fabricated pass. |
| T-13-72-05 | Repudiation | Repair record | medium | mitigate | Record GitNexus impact result and focused test outcomes in the new summary without session or provider material. |
</threat_model>

<verification>
- Run GitNexus upstream impact analysis before symbol edits and report any HIGH or CRITICAL result before proceeding.
- Run the focused local and remote observer suites followed by `pnpm --filter @starye/config type-check`.
- Confirm the code path without a root-IAB port exits before pending-evidence reads/writes and that no carrier, historical evidence, or remote command was touched.
</verification>

<success_criteria>
- Root IAB signed-session proof and receipt-producing observation have one explicit in-memory owner rather than two browser sessions.
- Independent cookie/profile observer behavior cannot allocate, observe, or mutate a fresh carrier evidence pair.
- The repair is fully covered by focused local/remote tests and is ready to gate a new carrier, while p13-69 remains frozen history.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md` when done.
</output>
