---
phase: 13-full-chain-data-smoke
plan: "52"
type: execute
wave: 47
depends_on: [13-51]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-52-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-52-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - TEST-05
must_haves:
  truths:
    - "Only after 13-51 observer repair and two accepted Gateway readiness records may a new p13-52 local run be allocated."
    - "Exactly one local handoff produces one unobserved pending pair for a new p13-52 run id; p13-49 and p13-50 are never reused."
    - "Ordered Dashboard then Viewer observation on the same local target/run/code/itemId reaches terminal_passed with provesExternalChain true, or freezes an honest post-allocation checkpoint without retry."
    - "All p13-52 evidence remains untracked; no remote or production claim is made."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-52-RUN-ID.txt
      provides: sole allocated p13-52 identifier when pre-allocation gates pass
    - path: .planning/phases/13-full-chain-data-smoke/13-52-SUMMARY.md
      provides: non-secret local terminal or honest checkpoint closeout
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-52-run-id}/local.json
      provides: untracked tuple-bound local evidence
  key_links:
    - from: pnpm check:services
      to: http://localhost:8080/robots.txt,/auth,/auth/
      via: two consecutive accepted readiness records before allocation
      pattern: "starye-gateway-readiness"
    - from: pnpm smoke:data-chain:handoff
      to: observeDataChainSurfaces
      via: one pending local pair observed Dashboard-before-Viewer
      pattern: "resolved_pending_observation"
---

<objective>
Create one fresh local-only terminal data-chain proof after the 13-51 observer repair.

Purpose: p13-49 and p13-50 are immutable Dashboard auth checkpoints. This plan allocates a new `p13-52-<32 hex>` boundary, runs one local handoff, one ordered Dashboard→Viewer observation using the repaired observer contract, and one post-observation exact verifier.

Output: `13-52-RUN-ID.txt` only if allocated, untracked local evidence, and `13-52-SUMMARY.md`. Local proof only.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-full-chain-data-smoke/13-51-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
</context>

## Artifacts this phase produces
- `13-52-RUN-ID.txt` (conditional)
- untracked `evidence/starye-org/p13-52-*/local.{attempt,json,md}`
- `13-52-SUMMARY.md`

## Non-goals
- No reuse of p13-49 / p13-50 / p13-45 / p13-41 run ids
- No remote handoff, production browser, provider deploy, or migration
- No second handoff/observer/verifier after a post-allocation non-success

<tasks>
<task type="auto">
  <name>Task 1: Confirm 13-51 repair and pre-allocation local gates</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-52-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-51-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md
  </read_first>
  <action>
Require `13-51-SUMMARY.md` to record observer repair complete with focused regressions green. Confirm `13-52-RUN-ID.txt` and `evidence/starye-org/p13-52-*` are absent. Run root launcher regression for data-chain CLI if still required by current source, then `pnpm target-profile project-local --target starye-org --check`, `pnpm target-profile preflight --target starye-org --scope local --command smoke --live --wrangler-profile starye-org`, and two consecutive `pnpm check:services` with `robots`/`auth`/`authSlash` all accepted on `http://localhost:8080`. If any gate fails, write honest pre-allocation Summary and stop without creating a run id.
  </action>
  <acceptance_criteria>
    - 13-51 repair Summary exists and is treated as the release gate into this plan.
    - Two service checks accept all three Gateway probes before any p13-52 id exists.
    - Pre-allocation failure leaves no p13-52 run id or evidence tree.
  </acceptance_criteria>
  <verify>
    <automated>pnpm target-profile project-local --target starye-org --check</automated>
    <automated>pnpm check:services</automated>
  </verify>
  <done>Fresh local gates authorize exactly one p13-52 allocation, or only an honest pre-allocation Summary exists.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate one p13-52 handoff and pending verifier</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-52-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-52-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
Generate exactly one `p13-52-` + 32 lowercase hex id, write once to `13-52-RUN-ID.txt`, ensure its evidence directory is absent, then invoke once: `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <run-id>`. Require exit 0, `outcome: pending`, `handoffReady: true`, `runnerInvocations: 1`, non-empty itemId. Run exact local verifier once expecting `resolved_pending_observation`, `pending`, `provesExternalChain: false` for the same tuple. On any post-allocation failure, freeze this run in Summary; no retry or replacement id.
  </action>
  <acceptance_criteria>
    - One new p13-52 id and one pending local pair exist, or the allocated run is frozen honestly.
    - Historical p13-49/p13-50 trees are untouched.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-52-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>One unobserved pending local pair is ready for ordered observation, or the run is permanently stopped.</done>
</task>

<task type="auto">
  <name>Task 3: Ordered Dashboard→Viewer observation and terminal verify</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-52-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - .planning/phases/13-full-chain-data-smoke/13-52-RUN-ID.txt
  </read_first>
  <action>
Invoke repository-owned `observeDataChainSurfaces` exactly once for mode local / target starye-org / the sole p13-52 run id, using the repaired observer path from 13-51 (default authenticated profile or controlled adapter limited to status/itemCode/itemId). Require Dashboard before Viewer. On success, run post-observation exact verifier once expecting `terminal_passed` and `provesExternalChain: true`. On Dashboard/Viewer checkpoint, freeze the run, do not call a second observer or replacement handoff, and record the exact checkpoint code. Keep evidence untracked. Write `13-52-SUMMARY.md` with tuple, exits, and hashes. Make no remote/production claim.
  </action>
  <acceptance_criteria>
    - Success path: ordered Dashboard+Viewer passed and post-observation verifier terminal_passed.
    - Failure path: single honest checkpoint, run frozen, no retry.
    - Evidence untracked; no remote commands.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-52-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>p13-52 is either local terminal proof authorizing remote planning, or an immutable honest local checkpoint.</done>
</task>
</tasks>
