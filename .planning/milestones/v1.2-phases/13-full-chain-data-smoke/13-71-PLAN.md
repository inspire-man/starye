---
phase: 13-full-chain-data-smoke
plan: "71"
type: execute
wave: 65
depends_on: [13-70]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-71-SUMMARY.md
  - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
autonomous: false
gap_closure: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Root in-app Browser selected-production Dashboard session
    why: The production observation can begin only when the root IAB already shows the authorized Movies surface at the selected TargetProfile canonical Dashboard route.
    dashboard_config:
      - task: Sign in interactively in the root IAB if the selected-production Dashboard session is absent; do not export, copy, or persist session material.
must_haves:
  truths:
    - "Production observation begins only from the new p13-69 remote pending pair when its repository-owned handoff reports passed nested preflight, one runner invocation, and a non-empty remote itemId, per D-04 and D-05."
    - "The root IAB proves its selected TargetProfile canonical production Movies surface before it observes the exact p13-69 remote tuple in Dashboard then Viewer order, per D-04, D-05, D-06, and D-07."
    - "A terminal remote result requires both receipt-backed selected-production observations plus exactly one post-success remote verifier reporting terminal_passed and provesExternalChain true; the first observer or verifier non-success permanently freezes p13-69 with no retry, second observer, separate preflight, handoff, or remote verifier."
    - "13-VERIFICATION.md is refreshed only from p13-69 live facts and preserves p13-66 and all older carriers as frozen context; it never treats localhost evidence as production proof."
    - "The absent historical original-creation manifests remain behavior_unverified. No manifest is fabricated, reconstructed from the current tree, or made a prerequisite for the fresh remote Viewer result; any future attempt to cover that behavior requires a separate human checkpoint plan."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-71-SUMMARY.md
      provides: Non-secret production root-IAB readiness, ordered Dashboard/Viewer result, exact verifier branch, and frozen-checkpoint facts for p13-69.
    - path: .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
      provides: Live-aligned Phase 13 status based solely on p13-69 fresh-carrier evidence while retaining historical behavior_unverified truth.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-69-run-id}/remote.json
      provides: Untracked selected-target remote terminal receipts or a frozen first observation/verifier checkpoint.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-69-run-id}/remote.md
      provides: Untracked Markdown rendering that must match the selected-target remote JSON evidence.
  key_links:
    - from: 13-70 sole remote handoff
      to: root IAB production readiness
      via: The exact p13-69 remote pending tuple has passed nested preflight and a non-empty remote itemId.
      pattern: handoffReady true and preflightStatus passed
    - from: root IAB production Dashboard and Viewer receipts
      to: exact remote verifier
      via: `observeDataChainSurfaces` returns success only after Dashboard-before-Viewer exact tuple correlation.
      pattern: Dashboard then Viewer
    - from: exact remote verifier
      to: Phase 13 verification status
      via: Only a receipt-backed terminal_passed result with provesExternalChain true can close the selected-production Viewer gap.
      pattern: terminal_passed and provesExternalChain true
---

<objective>
Observe the fresh p13-69 selected-production pending tuple through root IAB Dashboard then Viewer routes, apply the exact verifier branch once, and refresh the Phase 13 report without altering frozen history.

Purpose: `13-VERIFICATION.md` records a single remaining live requirement: a fresh selected-production pending tuple must obtain terminal canonical Viewer evidence after Dashboard. p13-66 was permanently frozen at `canonical_viewer_unavailable`; its receipts cannot be retried or supplemented. The missing original-creation manifests are a separate historical behavior-unverified condition, not a condition for current remote execution.

Output: `13-71-SUMMARY.md`, updated Phase 13 verification truth, and conditional untracked p13-69 remote terminal or checkpoint evidence. Phase 14 documentation, runbook, literal cleanup, and final requirement matrix stay deferred per D-07.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-66-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-67-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-68-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-69-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-70-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-70-SUMMARY.md
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
@packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
</context>

<interfaces>
- `observeDataChainSurfaces` accepts only `mode`, `target`, and `runId`, resolves remote base from the selected TargetProfile canonical HTTPS Gateway, writes Dashboard before Viewer, and stops at the first checkpoint.
- The root-IAB adapter may return only `status`, `itemCode`, and `itemId`; it must obtain all three from actual root IAB navigation and must not call the default cookie/profile observer.
- `inspectDataChainSmokeVerification` returns terminal proof only for a receipt-backed `resolved` and `passed` pair. It must run once only after both production observations pass; it is skipped after an observer checkpoint.
</interfaces>

## Artifacts This Phase Produces

| Artifact | Owner | Produced state |
| --- | --- | --- |
| `13-71-SUMMARY.md` | Phase 13 tracked planning artifact | Root-IAB production readiness, Dashboard-first Viewer branch result, verifier branch, and immutable p13-69 outcome. |
| `13-VERIFICATION.md` | Phase 13 tracked verification owner | Fresh p13-69 terminal/checkpoint truth plus retained historical behavior-unverified status. |
| `evidence/starye-org/{p13-69-run-id}/remote.json` and `remote.md` | Untracked execution evidence | One ordered production receipt pair or its first terminal checkpoint. |

<tasks>
<task type="auto">
  <name>Task 1: Gate root production observation on the p13-69 remote pending tuple</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-71-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-70-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
    - scripts/data-chain-surface-observation.ts
  </read_first>
  <action>
Read the p13-70 handoff outcome and its p13-69 remote evidence without invoking a remote verifier. Continue only when the sole handoff reports passed nested preflight, one runner invocation, the deterministic p13-69 code, and a non-empty remote itemId in a pending tuple. Then the root agent claims the existing IAB and establishes only non-sensitive readiness facts at the selected TargetProfile canonical production Dashboard: canonical-origin class, signed-in/authorized state, and Movies-surface availability. Never treat localhost session state as production proof and never inspect, export, persist, or transfer cookies, storage, tokens, headers, browser profiles, raw provider data, or complete endpoints. If either remote pending eligibility or root production Dashboard readiness is absent, record the matching p13-69 checkpoint in the summary and stop without Viewer navigation, observation-core invocation, standalone preflight, handoff, or verifier. This task implements D-04, D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>$runId = (Get-Content -Raw -LiteralPath .planning/phases/13-full-chain-data-smoke/13-69-RUN-ID.txt).Trim(); $remote = Join-Path '.planning/phases/13-full-chain-data-smoke/evidence/starye-org' "$runId/remote.json"; if (-not (Test-Path -LiteralPath $remote)) { throw 'p13-69 remote evidence is required' }; $evidence = Get-Content -Raw -LiteralPath $remote | ConvertFrom-Json; if ([string]::IsNullOrWhiteSpace([string]$evidence.itemId)) { throw 'p13-69 remote itemId is required' }; if ($evidence.aggregate -ne 'pending') { throw 'p13-69 must be pending before production observation' }</automated>
  </verify>
  <acceptance_criteria>
    - The only production-observation input is a new p13-69 pending remote tuple with a non-empty remote itemId.
    - Root IAB readiness proves the selected canonical production Dashboard independently of local browser state.
    - Any ineligible condition freezes p13-69 without a Viewer route, observation, preflight, handoff, or verifier.
  </acceptance_criteria>
  <done>The fresh p13-69 remote pair and root production IAB are both eligible for ordered observation, or the plan has stopped honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Root-observe selected-production Dashboard then Viewer and apply the verifier branch</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-71-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>
Only the root agent holding the signed selected-production IAB may invoke `observeDataChainSurfaces` with an in-memory root-IAB adapter. The adapter returns only actual navigation `status`, `itemCode`, and `itemId`; the existing core owns selected TargetProfile base resolution, tuple derivation, exact receipt construction, Dashboard-before-Viewer ordering, validation, and evidence writes. The root must first observe the canonical production Dashboard Movies row and confirm the p13-69 code plus remote itemId, then observe the canonical Viewer route for that same code and confirm the same tuple through allowed non-secret metadata. Do not call the default observer, direct app origins, a cookie-backed profile, or a second observer. If the observation core returns checkpoint exit 2 at Dashboard or Viewer, record that first non-success and permanently freeze p13-69 without any remote verifier, retry, second navigation, preflight, handoff, allocation, or historical carrier use. Only when both receipts pass may the root run `pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id <actual-p13-69-run-id>` exactly once. Only `terminal_passed` with `provesExternalChain: true` is remote success; every verifier non-success is the permanent end of p13-69. This task implements D-01, D-04, D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-evidence.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The selected canonical production Dashboard receipt precedes the Viewer receipt and both use the exact p13-69 code/itemId tuple.
    - An observer checkpoint skips the remote verifier; a successful observer pair permits exactly one remote verifier.
    - Terminal remote proof requires receipt-backed Dashboard and Viewer surfaces plus both terminal verifier fields, while any non-success freezes p13-69.
  </acceptance_criteria>
  <done>p13-69 has terminal selected-production Dashboard-to-Viewer proof or one immutable first non-success outcome.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh live Phase 13 verification while retaining historical behavior-unverified truth</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-71-SUMMARY.md, .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-70-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-71-SUMMARY.md
  </read_first>
  <action>
Update only the current Phase 13 verification frontmatter, re-verification record, live-gap table, observable-truth evidence, required-artifact rows, and DATA-04 through DATA-07 plus TEST-05 coverage rows that p13-69 changes. Derive every current status strictly from the new local terminal, sole remote handoff, root IAB ordered receipts, and the permitted exact verifier result. Mark the selected-production Viewer truth passed only after the non-empty p13-69 remote itemId, terminal Dashboard then Viewer pair, and terminal verifier fields all exist. Otherwise retain `gaps_found` and name the exact fresh p13-69 first checkpoint or missing prerequisite. Preserve p13-66 and all earlier carriers as frozen historical context; never retry, hash, inspect as a substitute, or rewrite them. Keep the historical original-creation-manifest item under `behavior_unverified`: do not fabricate a manifest, derive a baseline from the current tree, or convert it into a prerequisite for the new remote/Viewer evidence. If the project later elects to resolve that historical behavior, it requires a separate blocking human-verify plan. Do not modify Phase 14, RUNBOOK, roadmap, state, or requirements artifacts. This task implements D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>$report = Get-Content -Raw -LiteralPath .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md; if ($report -notmatch 'p13-69') { throw 'verification must cite the fresh carrier' }; if ($report -notmatch 'behavior_unverified') { throw 'historical manifest gap must remain behavior-unverified' }; if ($report -match 'http://localhost:8080.*production') { throw 'localhost must not be claimed as production evidence' }</automated>
  </verify>
  <acceptance_criteria>
    - The report distinguishes fresh p13-69 terminal, pending, and checkpoint facts without treating p13-66 as a retry target or local proof as production proof.
    - It retains the missing historical original-creation manifests as behavior-unverified without creating or reconstructing an authority baseline.
    - No Phase 14 or shared planning metadata file changes as part of this live-gap update.
  </acceptance_criteria>
  <done>Phase 13 verification reflects the new fresh-carrier outcome honestly and leaves the historical-manifest condition explicitly behavior-unverified.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Remote pending evidence to production IAB | A provider-backed tuple gates authenticated browser navigation under a different production session boundary. |
| Root production IAB to evidence core | The root adapter sends only observed tuple/status facts to repository-owned evidence logic. |
| Live p13-69 result to verification report | Terminal and checkpoint evidence controls the Phase 13 status consumed by later workflow steps. |
| Historical evidence to current verification | Missing original-creation manifests limit an immutability claim without authorizing reconstruction from the current tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-71-01 | Spoofing | Production Dashboard session | critical | mitigate | Require a root-owned selected-target canonical Movies surface after the p13-69 remote pending tuple exists; local session state is excluded. |
| T-13-71-02 | Tampering | Remote browser evidence | high | mitigate | Derive tuple only from p13-69 pending evidence, enforce Dashboard-before-Viewer core ordering, and freeze at the first non-success. |
| T-13-71-03 | Information disclosure | Root IAB and tracked summary | high | mitigate | Keep adapter and summary output to route class, status, code, itemId presence, and verifier category; exclude session/provider material. |
| T-13-71-04 | Repudiation | Phase verification state | high | mitigate | Update only from receipt-backed p13-69 evidence and exact verifier outcome while preserving frozen carrier and behavior-unverified facts. |
| T-13-71-05 | Elevation of privilege | Production Dashboard route | medium | mitigate | Retain the existing session/role guard and classify missing access as the carrier's first checkpoint. |
| T-13-71-06 | Tampering | Historical manifest assertion | medium | mitigate | Preserve the behavior-unverified state and require a separate human-verify plan before any original-creation baseline can be accepted. |
</threat_model>

<verification>
- Confirm the sole p13-69 remote handoff reached a pending tuple with a non-empty itemId before root IAB production navigation.
- Use focused evidence, remote-observation, and verifier tests to preserve canonical-origin, Dashboard-first, receipt, and first-checkpoint contracts.
- Confirm the refreshed report names p13-69, contains no local-as-production claim, and retains the historical manifest limitation as behavior-unverified.
</verification>

<success_criteria>
- A fresh p13-69 pending remote tuple is observed only through the selected canonical production Dashboard then Viewer routes under a real root IAB session.
- Terminal production proof requires both ordered receipts and exactly one terminal verifier result; any first non-success remains a frozen p13-69 checkpoint.
- The current Phase 13 report reflects only fresh facts and preserves historical manifest uncertainty without fabrication or scope expansion.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-71-SUMMARY.md` when done.
</output>
