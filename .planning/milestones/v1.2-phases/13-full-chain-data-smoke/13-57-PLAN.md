---
phase: 13-full-chain-data-smoke
plan: "57"
type: execute
wave: 51
depends_on: [13-56]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-57-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - TEST-05
must_haves:
  truths:
    - "Only after 13-56 standalone + nested remote preflight green may a new p13-57 local run be allocated."
    - "Exactly one local handoff produces one unobserved pending pair for a new p13-57-<32 hex> run id; p13-55/p13-52/p13-50/p13-49/p13-41 are never reused."
    - "Ordered Dashboard then Viewer observation uses the signed-in Codex in-app Browser session as the sole observeSurface adapter and reaches terminal_passed with provesExternalChain true, or freezes an honest post-allocation checkpoint without retry."
    - "All p13-57 evidence remains untracked; p13-55 remote evidence is never opened or rewritten; no remote or production claim is made."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-57-RUN-ID.txt
      provides: sole allocated p13-57 identifier when pre-allocation gates pass
    - path: .planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
      provides: non-secret local terminal or honest checkpoint closeout
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-57-run-id}/local.json
      provides: untracked tuple-bound local evidence
  key_links:
    - from: pnpm check:services
      to: http://localhost:8080/robots.txt,/auth,/auth/
      via: two consecutive accepted readiness records before allocation
      pattern: "starye-gateway-readiness"
    - from: pnpm smoke:data-chain:handoff
      to: observeDataChainSurfaces with IAB observeSurface adapter
      via: one pending local pair observed Dashboard-before-Viewer on signed-in local session
      pattern: "resolved_pending_observation"
---

<objective>
Create one fresh local-only terminal data-chain proof after nested preflight reliability is green, without reusing the frozen p13-55 carrier.

Purpose: p13-55 remote is permanently checkpointed and cannot carry production proof. This plan allocates `p13-57-<32 hex>`, runs one local handoff, one ordered Gateway Dashboard→Viewer observation through the signed-in Codex IAB tab, and dual exact local verifiers.

Output: `13-57-RUN-ID.txt` only if allocated, untracked local evidence, and `13-57-SUMMARY.md`. Local proof only.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-54-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
</context>

## Artifacts this phase produces
- `13-57-RUN-ID.txt` (conditional)
- untracked `evidence/starye-org/p13-57-*/local.{attempt,json,md}`
- `13-57-SUMMARY.md`

## Non-goals
- No reuse of p13-55 / p13-52 / p13-50 / p13-49 / p13-41 run ids
- No remote handoff, production browser, provider deploy, or migration
- No second handoff/observer/verifier after a post-allocation non-success
- No cookie/secret logging or tracked session material
- No read/write of p13-55 remote evidence

<tasks>
<task type="auto">
  <name>Task 1: Confirm 13-56 gates and dual Gateway readiness</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
  </read_first>
  <action>
Require 13-56-SUMMARY.md to show standalone + nested remote preflight green. Confirm 13-57-RUN-ID.txt and evidence/starye-org/p13-57-* are absent. Confirm operator-signed local Dashboard is available in Codex in-app Browser at http://localhost:8080/dashboard. Run two consecutive `pnpm check:services` with robots/auth/authSlash accepted. If any gate fails, write honest pre-allocation Summary and stop without creating a run id.
  </action>
  <acceptance_criteria>
    - 13-56 Summary authorizes a new local attempt via nested preflight green.
    - Two service checks accept all three Gateway probes before any p13-57 id exists.
    - Pre-allocation failure leaves no p13-57 run id or evidence tree.
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:services</automated>
  </verify>
  <done>Fresh local gates authorize exactly one p13-57 allocation, or only an honest pre-allocation Summary exists.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate one p13-57 local handoff and pending verifier</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-57-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
Allocate exactly one new run id `p13-57-` + 32 lowercase hex. Persist it to 13-57-RUN-ID.txt only after gates pass. Run one local handoff:

`pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <p13-57>`

Then one pending exact local verifier. Require unobserved local pending pair with non-empty item identity fields expected by the local contract. On non-success, freeze with honest Summary and stop; no second handoff. Never write remote artifacts.
  </action>
  <acceptance_criteria>
    - Exactly one p13-57 run id allocated.
    - Local handoff produces unobserved pending local evidence under that run only.
    - Historical run trees remain untouched.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-57-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>One unobserved local pending pair exists for p13-57, or post-allocation work is honestly frozen.</done>
</task>

<task type="auto">
  <name>Task 3: Ordered IAB Dashboard→Viewer observation and dual terminal verify</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
  </read_first>
  <action>
Using the signed-in Codex in-app Browser session as the sole observeSurface adapter, observe Gateway local surfaces in order: Dashboard `http://localhost:8080/dashboard` (movies surface as required by current observer) then Viewer `http://localhost:8080/movie/<item-code>` for the exact p13-57 tuple. Run dual exact local verifiers expecting `terminal_passed` and `provesExternalChain: true`. On dashboard_auth_unavailable or viewer failure, freeze honest local checkpoint without retry. Write non-secret 13-57-SUMMARY.md. Do not authorize or run remote handoff here.
  </action>
  <acceptance_criteria>
    - Observation order is Dashboard then Viewer on Gateway 8080 only.
    - Dual local verify is terminal_passed / provesExternalChain true, or honest local checkpoint is recorded.
    - No remote mode commands executed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-57-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>p13-57 local terminal proof exists and unlocks human-gated remote handoff planning, or local work is honestly blocked.</done>
</task>
</tasks>

## Verification
- New p13-57 local carrier only
- Gateway-first URLs only
- p13-55 remote untouched
- Evidence untracked

## Self-check (plan_checker disabled)
- Depends on 13-56 green nested preflight
- Mirrors 13-55 local terminal contract with a new run family
- Remote auth deferred to 13-58
