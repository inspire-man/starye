---
phase: 13-full-chain-data-smoke
plan: "44"
type: execute
wave: 42
depends_on: [13-43]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-44-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - TEST-05
user_setup:
  - service: Production-valid starye.org authenticated session material
    why: 13-43 production Dashboard observation stopped at dashboard_auth_unavailable. Local apps/api/.dev.vars BETTER_AUTH_SECRET cannot sign production cookies; unauthenticated observe redirects to /auth/login; p13-41 remote pair is already checkpointed and must not be reopened.
    env_vars:
      - name: BETTER_AUTH_SECRET
        source: production Worker secret for starye-api (or equivalent signing material). Store only in untracked local path if used for cookie signing; never commit. Prefer not printing values.
      - name: PRODUCTION_SESSION_COOKIE
        source: optional alternative — full signed starye.session_token value usable against https://starye.org/api/auth/get-session (untracked file only)
      - name: CLOUDFLARE_API_TOKEN
        source: packages/crawler/.env (already green from 13-40; keep for later remote handoff)
      - name: CLOUDFLARE_ACCOUNT_ID
        source: d6e57b25da320fae1bd0079fb3c316d4
must_haves:
  truths:
    - "A production-valid authenticated session material is available without committing secrets."
    - "GET https://starye.org/api/auth/get-session with the prepared Cookie returns a body containing user (HTTP 200), recorded only as success/failure and non-secret prefixes."
    - "p13-41 remote checkpoint and locked p13-17/p13-37 remotes are not rewritten."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-44-SUMMARY.md
      provides: production session gate authorizing Wave 43 fresh local+remote+browser chain
---

<objective>
Close the production authenticated-session gate that blocked 13-43. Root cause: production Dashboard requires a signed-in operator session on canonical https://starye.org; local BETTER_AUTH_SECRET does not validate against production; p13-41 remote is permanently checkpointed at dashboard_auth_unavailable. Do not reopen p13-41/p13-17/p13-37 remote trees. No smoke handoff in this plan.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-43-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-42-SUMMARY.md
@scripts/data-chain-surface-observation.ts
@apps/api/src/lib/auth.ts
</context>

## Artifacts this phase produces
- Operator-supplied untracked production session material (never committed)
- `13-44-SUMMARY.md` with non-secret get-session proof

## Non-goals
- No local/remote handoff
- No production Dashboard/viewer observe in this plan (Wave 45)
- No rewrite of p13-41 remote checkpoint or locked remotes
- No fabrication of historical A-E manifests

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Supply production session material</name>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-43-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
  </read_first>
  <action>
Present observed 13-43 failure: production Dashboard redirected to https://starye.org/auth/login?next=/dashboard/movies; remote D1 session token signed with local BETTER_AUTH_SECRET yields get-session null; p13-41 remote aggregate is permanent checkpoint dashboard_auth_unavailable. Operator must provide ONE of: (A) production BETTER_AUTH_SECRET stored only in untracked local file (e.g. packages/crawler/.env.production-session or apps/api/.dev.vars.production — never commit) sufficient to sign a live remote D1 session token for cookie starye.session_token; OR (B) a production-valid already-signed cookie value written to an untracked file only; OR (C) a live signed-in Chrome profile/session the executor can drive via available browser automation without extracting secrets into git. Stop until operator confirms material is ready without pasting secrets into chat when avoidable.
  </action>
  <done>Operator confirms untracked production session material is ready; secrets not committed.</done>
</task>
<task type="auto">
  <name>Task 2: Prove production get-session accepts the material</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-44-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-43-SUMMARY.md
    - packages/crawler/.env
  </read_first>
  <action>
Using only untracked session material, build Cookie header for starye.session_token against origin https://starye.org (never log full secrets; lengths/prefixes only). Require GET https://starye.org/api/auth/get-session returns HTTP 200 and body includes user. Optionally reconfirm remote live preflight still green (13-40 command) without rewriting evidence. Record p13-41 remote.json SHA-256 still matches 13-43 baseline `120db2e780b49825c49f5d0b923da80aa3238a4848cc966771a902b8eca4ce5c` and locked p13-17/p13-37 hashes. Write 13-44-SUMMARY.md authorizing Wave 43. Commit Summary only.
  </action>
  <acceptance_criteria>
    - 13-44-SUMMARY.md records get-session success on https://starye.org without secret values
    - No .env / evidence / cookie files staged
    - p13-41 remote checkpoint hash unchanged
  </acceptance_criteria>
  <verify>
    <automated>node -e "console.log('manual: GET https://starye.org/api/auth/get-session with prepared Cookie must include user')"</automated>
  </verify>
  <done>Production session gate is green; Wave 43 may allocate a new run.</done>
</task>
</tasks>