---
phase: 13-full-chain-data-smoke
plan: "16"
type: execute
wave: 14
depends_on: [13-13, 13-15]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md"
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - TEST-05
must_haves:
  truths:
    - "A newly started, task-owned local supervisor serves /robots.txt, /auth, and /auth/ through http://localhost:8080 before any new Phase 13 run is allocated."
    - "Startup follows exactly one fixed-port ownership branch: all required ports free permits cold start; a complete listener set uniquely owned by one current-repository local-dev supervisor permits exact replacement; every partial, ambiguous, duplicate, or unrelated state fails closed without stopping or starting a process."
    - "`blocked_ownership/blocked` is an honest terminal non-success checkpoint: its Summary records only a closed ownership reason plus blocked Task 2/13-17 eligibility, contains no workspace/launcher/supervisor PID markers, and authorizes no process start, run allocation, browser, or provider action."
    - "A fresh runtime that still fails any canonical route blocks Phase 13 execution and routes to a separately planned source-fix TDD gap; this plan does not speculate that localhost-to-127.0.0.1 or proxy edits are correct."
    - "Gateway handler regressions and the new readiness/auth/process suites pass against the repaired code before the runtime is declared eligible."
    - "No run id, evidence pair, browser action, provider command, code edit, schema action, or migration occurs."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md"
      provides: "Exact old/new supervisor ownership and fresh canonical HTTP eligibility, or an honest blocked-ownership non-success checkpoint with no launch/PID markers or downstream eligibility"
  key_links:
    - from: "new local-dev supervisor PID"
      to: "Gateway/Auth listener PIDs"
      via: "recorded parent/descendant ownership before canonical probes"
      pattern: "scripts/local-dev.ts"
    - from: "pnpm check:services"
      to: "scripts/gateway-readiness.ts"
      via: "starye-local-services-1 plus fixed canonical route probes"
      pattern: "healthy"
    - from: "fresh runtime eligibility"
      to: "13-17 run allocation"
      via: "all automated gates exit 0; any failure blocks dependency completion"
      pattern: "http://localhost:8080"
---

<objective>
Determine whether the diagnosed Gateway failure is stale runtime state by cold-starting only when every required port is free or replacing only a uniquely proven repository supervisor, then requiring a clean canonical HTTP response before any fresh evidence attempt (D-03).

Purpose: current `/robots.txt` also hangs, so changing only the auth origin would be speculation. A clean task-owned start or exact supervised replacement plus the new bounded readiness contract must establish whether source repair is justified.

Output: a task-owned local service tree, green fresh-runtime canonical readiness, and an accurate Summary. If fresh runtime remains broken, the output is a non-run blocker for a new source-fix plan, not an in-plan conditional code change.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/debug/phase13-gateway-auth-timeout.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-15-SUMMARY.md
@scripts/local-dev.ts
@scripts/check-services.ps1
@scripts/gateway-readiness.ts
@apps/gateway/src/index.ts
@apps/gateway/src/__tests__/routing.test.ts

<interfaces>
- Root `pnpm dev` owns `scripts/local-dev.ts`, which starts API, Gateway, Dashboard, Auth, Blog, Movie, and Comic children and handles SIGINT/SIGTERM cleanup.
- Root `pnpm check:services` now returns one `starye-local-services-1` machine record and nonzero unless every fixed listener and canonical Gateway route is healthy.
- Gateway `/robots.txt` is a direct 200 branch; exact `/auth` is a direct 301 to `/auth/`; `/auth/` proxies to Auth. These distinguish general runtime failure from an auth-only source defect.
</interfaces>
</context>

<discovery_evidence>
- Discovery level: 0. No source/library decision is made; this plan exercises established local process and readiness commands.
- Current workerd is supervised but non-serving and predates p13-12. Process continuity cannot prove its internal state at the historical request instant.
- Direct Auth returns valid 302; that direct-port observation is diagnostic only and cannot satisfy DATA-01.
- Gateway `fetch`/`proxy` impacts are currently LOW, but no source edit is authorized here. Any fresh-runtime source defect requires a later TDD plan with new impact analysis.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Order |
|---|---|---|---|
| 1 | Completed 13-13/13-15 repairs and the complete fixed-port ownership snapshot | One task-owned clean local supervisor tree or an ownership blocker | First |
| 2 | Task 1 PID-owned running services | Fresh canonical readiness eligibility or a non-run blocker | Second |
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Cold-start or replace under exact fixed-port ownership</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/debug/phase13-gateway-auth-timeout.md
    - .planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-15-SUMMARY.md
    - C:/Users/11407/.codex/gsd-core/templates/summary.md
    - scripts/local-dev.ts
    - scripts/check-services.ps1
    - package.json
  </read_first>
  <action>First require `pnpm target-profile project-local --target starye-org --check` and `pnpm target-profile validate --target starye-org` to exit 0. Resolve the repository root and take one PowerShell/CIM snapshot of listeners and process ancestry for the complete fixed service-port set `8080, 8787, 5173, 3002, 3003, 3000, 3001`. Select exactly one mutually exclusive branch. Branch 1, `cold_start`: every fixed port has zero listeners; assert the complete set is still free immediately before launch, stop no process, and start `pnpm dev`. Branch 2, `replace_repo_supervisor`: every fixed port has exactly one owning PID, every owner resolves through an unambiguous parent chain to the same unique `scripts/local-dev.ts` supervisor under the exact current repository root, and no fixed-port listener falls outside that tree; record only PID, start time, executable name, and matching script path, stop that exact supervisor and its verified descendants with native PowerShell process APIs, wait boundedly for those PIDs to exit, assert all seven fixed ports are free, then start `pnpm dev`. Branch 3, `blocked_ownership`: every other state, including a nonempty proper subset of fixed ports, duplicate listener owners, missing process records, multiple candidate supervisors, ambiguous ancestry, or any unrelated workspace/process owner; stop no process, launch nothing, write the closed non-secret ownership blocker, and fail Task 1. Never turn Branch 3 into cold start after killing anything. For either successful branch, launch through a hidden background process whose `WorkingDirectory` is the exact resolved repository root, capture its launcher PID, locate the new unique `scripts/local-dev.ts` descendant, and require exactly one listener on every fixed port with every owner descending from the new task-owned supervisor. Before verification, create or update the Summary from the referenced template with exactly one line each for `startup_ownership_branch: &lt;cold_start|replace_repo_supervisor|blocked_ownership&gt;`, `pre_start_fixed_ports: &lt;all_free|complete_single_repo_supervisor|blocked&gt;`, `new_local_dev_workspace: &lt;absolute-workspace-path&gt;`, `new_local_dev_launcher_pid: &lt;decimal-pid&gt;`, and `new_local_dev_supervisor_pid: &lt;decimal-pid&gt;` when startup occurs; these non-secret markers are the executable ownership contract for the verify block. Do not invoke `clean:ports`, a wildcard process kill, or any unrelated port cleanup, and do not inspect process environments, local secret files, or browser state. Immediately after the Task 1 ownership/PID/listener verification is green and immediately before the Task 1 Summary/task commit, run fresh GitNexus detect-changes for the whole working diff. Because this task changes only `13-16-SUMMARY.md`, require exactly zero changed code symbols and zero affected execution flows; any changed symbol/flow, unexpected scope, or inability to establish the zero-impact result stops before the Task 1 commit. This gate is Task 1-local and may not be deferred to Task 2 or the plan-level closeout.</action>
  <action_addendum>Per D-03 and D-06, `blocked_ownership` completes only the Task 1 checkpoint contract, never the Phase goal. Write `startup_ownership_branch: blocked_ownership`, `pre_start_fixed_ports: blocked`, one `ownership_block_reason` from `partial_fixed_ports|duplicate_listener_owner|missing_process_record|multiple_supervisors|ambiguous_ancestry|unrelated_owner`, `task2_eligibility: blocked`, and `13_17_eligibility: blocked`. Do not write `new_local_dev_workspace`, launcher PID, or supervisor PID markers; stop with no Task 2, no run allocation, no browser/provider action, and route the next execution to a diagnose/source-fix ownership gap. D-01/D-02/D-04/D-05/D-07 remain downstream and unexecuted.</action_addendum>
  <acceptance_criteria>
    - Target projection/validation are green before process replacement.
    - `cold_start` is accepted only when all seven fixed ports are free both in the ownership snapshot and immediately before launch; it stops no process and records `pre_start_fixed_ports: all_free`.
    - `replace_repo_supervisor` is accepted only when all seven fixed ports have unique listeners whose ancestry converges on one `scripts/local-dev.ts` supervisor in this exact repository; it stops only that verified tree, waits for every fixed port to become free, and records `pre_start_fixed_ports: complete_single_repo_supervisor`.
    - Every nonempty proper subset, duplicate owner, missing/ambiguous ancestry, multiple supervisor, or unrelated owner state selects `blocked_ownership`, stops and starts nothing, records only the closed blocker category plus `task2_eligibility: blocked` and `13_17_eligibility: blocked`, and blocks every later run/browser/provider action.
    - The blocked Summary contains no `new_local_dev_workspace`, `new_local_dev_launcher_pid`, or `new_local_dev_supervisor_pid` marker; Task 1 verification accepts the exact blocked checkpoint without claiming startup/readiness success.
    - After either successful branch, the new local-dev supervisor and all seven fixed service listeners have recorded task-owned PID ancestry; Summary markers identify the branch, pre-start assertion, exact workspace, launcher PID, and supervisor PID captured by this task.
    - Immediately before the Task 1 Summary/task commit, a fresh task-local GitNexus detect-changes result reports zero changed code symbols and zero affected execution flows; any nonzero or unexpected impact blocks that commit and is not deferred to Task 2.
    - No source, env, evidence, target profile, schema, migration, or provider resource is modified.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md'; $branch = [regex]::Match($summary, '(?m)^startup_ownership_branch:\s*(cold_start|replace_repo_supervisor|blocked_ownership)\s*$'); $preStart = [regex]::Match($summary, '(?m)^pre_start_fixed_ports:\s*(all_free|complete_single_repo_supervisor|blocked)\s*$'); if (-not $branch.Success -or -not $preStart.Success) { throw 'Startup ownership markers are missing or outside the closed vocabulary.' }; $pair = $branch.Groups[1].Value + ':' + $preStart.Groups[1].Value; if ($pair -notin @('cold_start:all_free', 'replace_repo_supervisor:complete_single_repo_supervisor', 'blocked_ownership:blocked')) { throw 'Startup ownership branch/pre-start pair is invalid.' }; if ($pair -eq 'blocked_ownership:blocked') { $reason = [regex]::Match($summary, '(?m)^ownership_block_reason:\s*(partial_fixed_ports|duplicate_listener_owner|missing_process_record|multiple_supervisors|ambiguous_ancestry|unrelated_owner)\s*$'); if (-not $reason.Success) { throw 'Blocked ownership reason is missing or outside the closed vocabulary.' }; if ($summary -notmatch '(?m)^task2_eligibility:\s*blocked\s*$' -or $summary -notmatch '(?m)^13_17_eligibility:\s*blocked\s*$') { throw 'Blocked downstream eligibility markers are missing.' }; foreach ($forbidden in @('new_local_dev_workspace', 'new_local_dev_launcher_pid', 'new_local_dev_supervisor_pid')) { if ($summary -match ('(?m)^' + [regex]::Escape($forbidden) + ':')) { throw ('Blocked ownership must not record launch marker ' + $forbidden + '.') } } }; exit 0"</automated>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md'; if ($summary -match '(?m)^startup_ownership_branch:\s*blocked_ownership\s*$') { if ($summary -notmatch '(?m)^pre_start_fixed_ports:\s*blocked\s*$' -or $summary -notmatch '(?m)^task2_eligibility:\s*blocked\s*$' -or $summary -notmatch '(?m)^13_17_eligibility:\s*blocked\s*$') { throw 'Blocked ownership checkpoint is incomplete.' }; exit 0 }; $workspaceMatch = [regex]::Match($summary, '(?m)^new_local_dev_workspace:\s*(.+?)\s*$'); $launcherMatch = [regex]::Match($summary, '(?m)^new_local_dev_launcher_pid:\s*(\d+)\s*$'); $supervisorMatch = [regex]::Match($summary, '(?m)^new_local_dev_supervisor_pid:\s*(\d+)\s*$'); if (-not $workspaceMatch.Success -or -not $launcherMatch.Success -or -not $supervisorMatch.Success) { throw 'Task-owned supervisor markers are missing.' }; $recordedWorkspace = (Resolve-Path -LiteralPath $workspaceMatch.Groups[1].Value.Trim()).Path; $currentWorkspace = (Resolve-Path '.').Path; if ($recordedWorkspace -ne $currentWorkspace) { throw 'Recorded supervisor workspace does not match this repository.' }; $expectedLauncher = [int]$launcherMatch.Groups[1].Value; $expectedSupervisor = [int]$supervisorMatch.Groups[1].Value; $processes = @(Get-CimInstance Win32_Process); $byId = @{}; foreach ($candidate in $processes) { $byId[[int]$candidate.ProcessId] = $candidate }; $supervisorRecord = $byId[$expectedSupervisor]; if ($null -eq $supervisorRecord -or $supervisorRecord.Name -ne 'node.exe') { throw 'Captured local-dev supervisor is not a live Node process.' }; $scriptPath = (Resolve-Path 'scripts/local-dev.ts').Path; $scriptMatch = ([string]$supervisorRecord.CommandLine -match [regex]::Escape($scriptPath)) -or ([string]$supervisorRecord.CommandLine -match '(?i)\.\.[\\/]\.\.[\\/]scripts[\\/]local-dev\.ts'); if (-not $scriptMatch) { throw 'Captured supervisor does not execute this workspace local-dev script.' }; $cursor = $supervisorRecord; $seen = @{}; $hasLauncher = $false; while ($null -ne $cursor -and -not $seen.ContainsKey([int]$cursor.ProcessId)) { $seen[[int]$cursor.ProcessId] = $true; if ([int]$cursor.ProcessId -eq $expectedLauncher) { $hasLauncher = $true; break }; $cursor = $byId[[int]$cursor.ParentProcessId] }; if (-not $hasLauncher) { throw 'Captured supervisor does not descend from the task-owned launcher.' }; foreach ($port in @(8080, 8787, 5173, 3002, 3003, 3000, 3001)) { $listeners = @(Get-NetTCPConnection -LocalPort $port -State Listen -ErrorAction Stop); $owners = @($listeners | Select-Object -ExpandProperty OwningProcess -Unique); if ($owners.Count -ne 1) { throw ('Expected one owning PID on port ' + $port + '.') }; $cursor = $byId[[int]$owners[0]]; $seen = @{}; $owned = $false; while ($null -ne $cursor -and -not $seen.ContainsKey([int]$cursor.ProcessId)) { $seen[[int]$cursor.ProcessId] = $true; if ([int]$cursor.ProcessId -eq $expectedSupervisor) { $owned = $true; break }; $cursor = $byId[[int]$cursor.ParentProcessId] }; if (-not $owned) { throw ('Listener on port ' + $port + ' is not owned by the captured supervisor.') } }; $launcher = Get-Process -Id $expectedLauncher -ErrorAction Stop; $supervisor = Get-Process -Id $expectedSupervisor -ErrorAction Stop; if ($launcher.StartTime -le (Get-Date).AddMinutes(-10) -or $supervisor.StartTime -le (Get-Date).AddMinutes(-10)) { throw 'Task-owned launcher or supervisor is not fresh.' }; exit 0"</automated>
    <automated>npx gitnexus detect-changes --repo starye --scope all</automated>
  </verify>
  <done>Either one unambiguous task-owned local supervisor tree is running, or an exact `blocked_ownership` non-success checkpoint is accepted with no launch/PID markers and Task 2/13-17 blocked before any run allocation.</done>
</task>

<task type="auto">
  <name>Task 2: Require fresh-runtime canonical HTTP and regression gates</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/debug/phase13-gateway-auth-timeout.md
    - .planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md
    - scripts/gateway-readiness.ts
    - scripts/check-services.ps1
    - apps/gateway/src/index.ts
    - apps/gateway/src/__tests__/routing.test.ts
    - packages/config/src/deployment-target/__tests__/gateway-readiness.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-process.test.ts
  </read_first>
  <action>Read Task 1's Summary and continue only when `startup_ownership_branch` is `cold_start` or `replace_repo_supervisor`, its paired `pre_start_fixed_ports` assertion matches that branch, and the recorded task-owned launcher/supervisor markers pass Task 1 verification. When Task 1 records the accepted `blocked_ownership/blocked` checkpoint, do not run Task 2 commands: confirm `task2_eligibility: blocked`, `13_17_eligibility: blocked`, no launch/PID markers, no run allocation/browser/provider action, and stop this execution for the named diagnose/source-fix ownership gap. Any other missing/mismatched marker fails closed. Poll `pnpm check:services` against the new supervisor for at most 60 seconds and accept only exit 0 plus exactly one valid `starye-local-services-1` record with all listeners true and canonical `/robots.txt`, `/auth`, and `/auth/` outcomes accepted. Run the Gateway routing suite and the focused gateway-readiness/local-smoke/process tests. If all pass, record the exact non-secret machine outcomes and mark 13-17 eligible without editing Gateway source. If `/robots.txt` or `/auth` remains non-serving on the fresh tree, stop: the failure is broader than the auth proxy and requires a new diagnose/source-fix gap plan. If only `/auth/` fails while direct branches pass, record that isolation and likewise stop for a separate TDD source-fix plan whose first step is a failing fresh-runtime integration test. Per D-03, only canonical Gateway readiness may unlock 13-17; per D-06, blocker details stay in the closed vocabulary. D-01/D-02/D-04/D-05/D-07 remain unexecuted on every blocker. Do not change `gatewayHandler.fetch`, `proxy`, `AUTH_ORIGIN`, `scripts/local-dev.ts`, or localhost/127.0.0.1 literals in this plan. Do not allocate a run or perform browser/provider work under either failure branch. Before committing only the Summary, run GitNexus detect-changes and require no code symbol or execution-flow impact.</action>
  <acceptance_criteria>
    - Fresh task-owned `/robots.txt`, `/auth`, and `/auth/` all emit acceptable headers through `http://localhost:8080` within the bounded readiness command.
    - Gateway routing tests and Phase 13 readiness/local/process regressions pass.
    - No direct Auth/Gateway implementation-port result is used as canonical success evidence.
    - Any fresh-runtime failure stops before 13-17 and records only the isolated route class; no speculative code edit occurs.
    - Summary is the only tracked change, evidence remains untouched/untracked, and GitNexus reports no code-flow impact.
    - `blocked_ownership` never enters Task 2; its accepted non-success Summary routes to the next diagnose/source-fix plan and cannot claim Phase 13 pass.
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:services</automated>
    <automated>pnpm --filter gateway test -- src/__tests__/routing.test.ts</automated>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-process.test.ts</automated>
  </verify>
  <done>The clean Gateway runtime is canonical-HTTP ready and may authorize a fresh local attempt; otherwise the accepted ownership/readiness non-success Summary blocks 13-17 and routes to a separate diagnose/source-fix plan without claiming Phase 13 pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing listener PID -> process ownership | A PID may be unrelated or reused; ancestry and command path must be unique before termination. |
| New supervisor -> readiness | New process ownership is necessary but not sufficient; canonical HTTP must pass. |
| Fresh failure -> source repair | Runtime evidence must isolate a code defect before any Gateway symbol edit is planned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-76 | Denial of Service | local process start/replacement | high | mitigate | Exact three-branch fixed-port ownership classification, bounded exact-tree stop/start, and no generic cleanup. |
| T-13-77 | Spoofing | fresh runtime identity | high | mitigate | Require task-owned supervisor ancestry and fresh start times. |
| T-13-78 | Tampering | source boundary | high | mitigate | No source edit; route-specific failure creates a new TDD planning requirement. |
| T-13-79 | Information Disclosure | process inspection/Summary | medium | mitigate | Record PIDs/times/script path only; never command env or secret values. |
| T-13-80 | Repudiation | readiness claim | medium | mitigate | One versioned machine record plus Gateway/unit/process test results. |
| T-13-SC | Tampering | package supply chain | low | accept | No install or dependency change occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Full selected-target chain through Gateway. | 13-16 gates; 13-17 through 13-20 prove. | COVERED | No proof claim here. |
| REQ | DATA-01 | Fresh canonical local Gateway routes. | Tasks 1-2 and 13-17/13-18 | COVERED | Exact current blocker. |
| REQ | DATA-02..DATA-07 | D1, fixture, admin/viewer, evidence. | 13-17 through 13-20 | COVERED | No run in this plan. |
| REQ | TEST-05 | Repeatable readiness and smoke behavior. | Task 2 plus 13-13/13-15 | COVERED | Fresh runtime backs contract tests. |
| RESEARCH | - | Local-first Gateway gate; direct ports diagnostic only. | Tasks 1-2 | COVERED | No package/source choice. |
| CONTEXT | D-01/D-02 | One bounded fixture. | 13-17/13-19 | COVERED | Zero fixture calls here. |
| CONTEXT | D-03 | Canonical local proof precedes remote. | Tasks 1-2 | COVERED | Clean runtime is a prerequisite. |
| CONTEXT | D-04..D-07 | Tuple, remote preflight, evidence, provider proof. | 13-17 through 13-20 | COVERED | Preserved. |
| VERIFICATION | G-1 | Fresh local path stops at Gateway. | Tasks 1-2 and 13-17/13-18 | COVERED | Source edit requires new evidence. |
| VERIFICATION | G-2/G-3 | Production proof absent. | 13-19/13-20 | COVERED | Blocked until this passes. |
| VERIFICATION | G-4 | Wrapper mismatch. | 13-14 | COVERED | Prior committed handoff repair. |

Phase 14 work remains deferred and is not pulled into process recovery.
</source_audit>

<verification>
- Apply exactly the three fixed-port ownership branches; only terminate the complete uniquely owned repository tree, and do not run `clean:ports` or a wildcard kill.
- Treat `blocked_ownership/blocked` as a verified terminal non-success Task 1 checkpoint: require one closed reason, blocked Task 2/13-17 eligibility, and absence of every workspace/launcher/supervisor marker; then stop without executing Task 2.
- Require fresh `pnpm check:services`, Gateway routing tests, and focused config tests.
- On any fresh route failure, stop before a run and route to a new source-fix plan; do not edit Gateway code here.
- Run GitNexus detect-changes before the Summary commit and require no code symbol/flow impact.
- Schema/migrations: N/A. No schema push, migration, D1/provider mutation, deploy, or resource action.
</verification>

<success_criteria>
- A successful branch starts a task-owned fresh local supervisor from the proven `cold_start` or `replace_repo_supervisor` branch and serves all three canonical readiness routes; a `blocked_ownership` branch is accepted only as a non-success stop with no Task 2/13-17 eligibility.
- Automated Gateway/readiness/auth/process regressions pass against that runtime contract.
- No speculative origin/proxy edit, run allocation, evidence write, browser/provider action, or unrelated process termination occurs.
- Summary accurately records eligibility or the exact non-run blocker.
</success_criteria>

## Artifacts this phase produces

- **New/modified code symbols:** none.
- **CLI/machine fields:** consumes existing `pnpm dev`, `pnpm check:services`, and `starye-local-services-1`; adds no CLI field or flag.
- **Tracked path:** `.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md` only.
- **Process state:** one task-owned local-dev supervisor and its fixed child services; no provider resource.
- **Evidence paths:** none created, read for execution, copied, staged, or committed.
- **Schema/migrations:** N/A; no schema push.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md`. A green Summary must include the startup ownership branch, its pre-start fixed-port assertion, applicable old ownership, new PID ownership, and exact canonical machine outcomes. A `blocked_ownership` Summary is an honest non-success checkpoint with one closed reason, `task2_eligibility: blocked`, `13_17_eligibility: blocked`, no launch/PID markers, and no Phase-pass claim; a later readiness failure likewise states that 13-17 is blocked and which separate diagnose/source-fix gap is next.
</output>
