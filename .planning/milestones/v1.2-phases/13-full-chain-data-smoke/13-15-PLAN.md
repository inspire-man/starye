---
phase: 13-full-chain-data-smoke
plan: "15"
type: execute
wave: 13
depends_on: [13-13]
files_modified:
  - "scripts/data-chain-smoke.ts"
  - "packages/config/src/deployment-target/data-chain-evidence.ts"
  - "packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts"
  - "packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts"
  - "packages/config/src/deployment-target/__tests__/fixtures/data-chain-smoke-auth-timeout-child.ts"
  - "packages/config/src/deployment-target/__tests__/data-chain-smoke-process.test.ts"
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "A bounded Gateway-auth failure persists one of four closed non-secret causes: timeout, fetch failure, unacceptable HTTP status, or invalid redirect; old gateway_auth_unavailable evidence remains valid and untouched."
    - "D-03/D-06: every auth failure creates exactly one pre-ingest checkpoint observation with itemId null, writes one JSON/Markdown pair, returns raw exit 2, and performs zero fixture, snapshot, API, browser, or provider calls."
    - "A process-level no-header regression proves the checkpoint pair exists before the child exits naturally and finishes within a measured 5000 ms child budget, far below the outer 300000 ms deadline."
    - "Response cancellation, timer cleanup, direct-port rejection, tuple/parity/redaction, and local/remote verifier behavior remain regression-green."
    - "Each execution receives an explicit root-user decision after the known HIGH validateDataChainEvidence blast-radius warning and before any evidence-owner or runner edit; an earlier approval is never reused."
    - "The HIGH-risk validateDataChainEvidence function body is not modified; only the LOW-impact closed checkpoint value owner may change."
  artifacts:
    - path: "packages/config/src/deployment-target/data-chain-evidence.ts"
      provides: "Backward-compatible closed Gateway-auth checkpoint vocabulary"
      contains: "gateway_auth_timeout"
    - path: "scripts/data-chain-smoke.ts"
      provides: "Probe-result-to-checkpoint mapping and checkpoint in runner machine output"
      exports: ["runDataChainSmoke", "runDataChainSmokeCli"]
    - path: "packages/config/src/deployment-target/__tests__/fixtures/data-chain-smoke-auth-timeout-child.ts"
      provides: "Injected in-memory no-header child with zero downstream calls"
    - path: "packages/config/src/deployment-target/__tests__/data-chain-smoke-process.test.ts"
      provides: "Bounded process exit and checkpoint-before-exit acceptance"
  key_links:
    - from: "scripts/gateway-readiness.ts:GatewayAuthProbeResult"
      to: "scripts/data-chain-smoke.ts:runDataChainSmoke"
      via: "exhaustive closed outcome mapping"
      pattern: "gateway_auth_timeout|gateway_auth_fetch_failed|gateway_auth_http_status_unaccepted|gateway_auth_redirect_invalid"
    - from: "scripts/data-chain-smoke.ts:preIngestCheckpoint"
      to: "data-chain-evidence.ts:dataChainCheckpointValues"
      via: "validated checkpoint union; no diagnostic message"
      pattern: "checkpoint"
    - from: "data-chain-smoke-auth-timeout-child.ts"
      to: "runDataChainSmoke"
      via: "injected short timeout, fetch, writer, and counters"
      pattern: "exitCode"
---

<objective>
Persist the bounded auth probe's exact closed failure class and prove at process level that timeout evidence is complete before the command exits (D-03, D-06), without revising any historical p13-12 claim.

Purpose: readiness now fails before allocation when the canonical Gateway is visibly unhealthy, but the runner still needs deterministic evidence if the auth boundary changes after allocation. The current generic checkpoint cannot distinguish transport timeout/fetch from status or redirect contract failure.

Output: four backward-compatible checkpoint codes, exhaustive runner mapping, optional allowlisted checkpoint machine output, and an in-memory subprocess regression with zero downstream work. No real run or external surface is used.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/phase13-gateway-auth-timeout.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-13-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md
@scripts/gateway-readiness.ts
@scripts/data-chain-smoke.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/data-chain-evidence.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts

<interfaces>
- Plan 13-13 `GatewayAuthProbeResult` is a closed discriminated result: accepted, timeout, fetch failure, unacceptable status, or invalid redirect.
- Existing `dataChainCheckpointValues` is the sole owner of persisted checkpoint strings and derives `DataChainCheckpoint`.
- Existing `preIngestCheckpoint` writes the validated JSON and deterministic Markdown peers, then returns raw exit 2.
- Existing `runDataChainSmokeCli` emits machine JSON after `runDataChainSmoke`; add only the persisted checkpoint value when present.
</interfaces>
</context>

<discovery_evidence>
- Discovery level: 0. Plan 13-13 supplies the transport classification and timeout; this plan only maps it through existing evidence/runner seams and adds tests.
- Fresh GitNexus impact: `validGatewayAuth` LOW (1 direct caller), `runDataChainSmoke` LOW (1 direct caller), and `dataChainCheckpointValues` LOW. Re-run before edit.
- `validateDataChainEvidence` is HIGH: 2 direct callers, 20 impacted symbols, and 3 flows (`runDataChainSmoke`, `observeDataChainSurfaces`, `appendBrowserObservation`). Its body is out of scope. If implementation would require changing it, stop, issue a blocking HIGH warning to the root user, and do not continue under this plan.
- Current p13-12 status/exception/location is irrecoverable. New code may classify only new observations.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Order |
|---|---|---|---|
| 1 | Known HIGH validator blast radius and this execution's root-user direction | One non-reusable scoped approval or a blocked plan | First |
| 2 | Task 1 approval, 13-13 closed probe result, and existing evidence union | Persisted distinct auth checkpoints and focused unit tests | Second |
| 3 | Task 2 mapping and injectable runner dependencies | Child-process lifecycle proof with in-memory peer writes | Third |
</dependency_graph>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm this execution's known HIGH downstream blast radius</name>
  <rationale>D-06 makes the shared evidence vocabulary a non-secret closed contract, so its HIGH downstream reach requires an explicit current-execution decision. D-01 through D-05 and D-07 remain unchanged execution boundaries and are not authorized by this checkpoint.</rationale>
  <files>N/A - read-only decision checkpoint; no repository file is modified.</files>
  <read_first>
    - AGENTS.md
    - .claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md
    - .planning/debug/phase13-gateway-auth-timeout.md
    - .planning/phases/13-full-chain-data-smoke/13-15-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-11-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md
    - scripts/gateway-readiness.ts
    - scripts/data-chain-smoke.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
  </read_first>
  <action>Before any source or test edit, present the known HIGH `validateDataChainEvidence` blast radius below to the root user, state that the approval applies only to this execution and only to the unchanged scoped boundary, and wait for an explicit option selection. Do not infer approval from prior discussion, planning, or execution history.</action>
  <decision>Whether to proceed with the narrowly scoped checkpoint-vocabulary and runner edit after the required HIGH-risk warning.</decision>
  <context>`validateDataChainEvidence` currently has HIGH upstream impact: 2 direct callers, 20 impacted symbols, and the `runDataChainSmoke`, `observeDataChainSurfaces`, and `appendBrowserObservation` flows. Its function body remains out of scope, but changing `dataChainCheckpointValues` and the derived `DataChainCheckpoint` type reaches that validator and therefore requires an explicit root-user decision before any source or test edit. This checkpoint is required for this execution and must not reuse or infer approval from an earlier planning or execution run.</context>
  <options>
    <option id="approve-scoped-edit">
      <name>Approve scoped edit</name>
      <pros>Allows only the four closed values, exhaustive runner mapping, and focused regressions while keeping the HIGH validator and three affected flow bodies unchanged.</pros>
      <cons>The shared type owner still has a broad downstream blast radius and must be guarded by the fresh impact comparison and full focused regressions in Task 2.</cons>
    </option>
    <option id="stop">
      <name>Stop execution</name>
      <pros>Leaves every HIGH-reach path and repository file untouched.</pros>
      <cons>The four bounded auth failure classes and process regression remain unimplemented.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The warning states HIGH risk and names exactly 2 direct callers, 20 impacted symbols, and the 3 affected flows `runDataChainSmoke`, `observeDataChainSurfaces`, and `appendBrowserObservation`.
    - The only approval is the exact `approve-scoped-edit` selection recorded for this Plan 13-15 execution; it authorizes only Tasks 2-3 and only their listed source/test paths.
    - Selecting `stop`, giving any non-matching response, or withholding a decision causes zero repository, evidence, index, staging, or process mutation and leaves Tasks 2-3 blocked.
    - `approve-scoped-edit` unlocks Tasks 2-3 only after their fresh GitNexus impact results exactly equal the approved 2-caller/20-symbol/3-flow validator boundary and every other named owner remains below HIGH; any mismatch requires a new warning and decision.
    - Approval never authorizes a `validateDataChainEvidence` function-body edit, and no approval from planning, an earlier execution, or another run is reused.
  </acceptance_criteria>
  <verify>
    <human-check>The root user explicitly selects `approve-scoped-edit` for this execution; any other response leaves all source and test files untouched.</human-check>
  </verify>
  <done>The current execution has a recorded root-user decision before any HIGH-reach edit, or the plan remains blocked without repository changes.</done>
  <resume-signal>Select `approve-scoped-edit` to continue this execution, or `stop` to leave the plan blocked.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist each closed Gateway-auth failure without widening evidence</name>
  <rationale>D-03 requires the local auth failure to stop before fixture or remote work, and D-06 requires exactly one closed persisted code with no transport detail. D-01/D-02/D-04/D-05/D-07 are preserved by the zero-downstream and unchanged-validator constraints.</rationale>
  <files>scripts/data-chain-smoke.ts, packages/config/src/deployment-target/data-chain-evidence.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/debug/phase13-gateway-auth-timeout.md
    - .planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md
    - scripts/gateway-readiness.ts
    - scripts/data-chain-smoke.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
  </read_first>
  <behavior>
    - Timeout maps to `gateway_auth_timeout`; non-timeout transport rejection maps to `gateway_auth_fetch_failed`.
    - A non-2xx/non-accepted-redirect status maps to `gateway_auth_http_status_unaccepted`.
    - Missing, external-origin, direct-port, or non-/auth/ redirect maps to `gateway_auth_redirect_invalid`.
    - Each case yields pre_ingest/checkpoint, itemId null, raw exit 2, one checkpoint observation, matching JSON/Markdown, and zero downstream calls.
    - Existing `gateway_auth_unavailable` and every old version-1 artifact remain valid and render identically; unknown values remain rejected.
  </behavior>
  <action>Proceed only after the root user selects `approve-scoped-edit` at Task 1 for this execution. Before editing, rerun GitNexus upstream impact for `validGatewayAuth`, `runDataChainSmoke`, `runDataChainSmokeCli`, `dataChainCheckpointValues`, the existing `DataChainCheckpoint` type owner, and `validateDataChainEvidence`; report direct callers, affected processes, and risk for every owner. Compare the fresh validator result with the checkpointed boundary of exactly 2 direct callers, 20 impacted symbols, and exactly the three named flows. Continue only when `validateDataChainEvidence` exactly matches that approved boundary and no other owner is HIGH or CRITICAL. If any count or flow differs, or any other owner is HIGH or CRITICAL, issue a new warning and pause for a new root-user direction; Task 1 approval does not cover changed impact. Modify only `dataChainCheckpointValues` and its derived `DataChainCheckpoint` type in the evidence owner to append exact `gateway_auth_timeout`, `gateway_auth_fetch_failed`, `gateway_auth_http_status_unaccepted`, and `gateway_auth_redirect_invalid`; do not edit the validator, serializer, renderer, clone, append, receipt, lifecycle, path, tuple, or remote-eligibility bodies. In `runDataChainSmoke`, exhaustively map the Plan 13-13 probe result to those codes and immediately return `preIngestCheckpoint` for every non-accepted result. Do not persist numeric status, location, exception text, errors, or multiple codes. Update `runDataChainSmokeCli` so runner machine JSON gains only the exact persisted checkpoint as an optional field derived from the validated returned evidence. Write tests first for all four failures, accepted 2xx/same-origin redirect, old generic compatibility, unknown-code rejection, secret-shaped key rejection, JSON/Markdown parity, direct-port rejection, and call counts. If a validator-body edit appears necessary, stop with a new HIGH warning instead of broadening scope. Immediately before every RED, GREEN, REFACTOR, or task commit, run `gitnexus_detect_changes`, compare the exact Task 2 paths/symbols against only the approved checkpoint-vocabulary and runner-mapping delta, and review all three named evidence flows even though their bodies remain unchanged. Any unexpected or HIGH/CRITICAL expansion stops before commit and requires a new root-user warning; rerun after any hook/autofix/restage before retry.</action>
  <acceptance_criteria>
    - Exactly four new persisted auth codes exist; no free-form diagnostic field or numeric/network detail is added.
    - Every auth failure remains non-success, pre-ingest, itemId-null, and downstream-call-free.
    - Runner JSON, evidence JSON, Markdown, and exact verifier agree on the one persisted code.
    - Historical `gateway_auth_unavailable` pairs remain byte-compatible and require no rewrite.
    - `validateDataChainEvidence` and the three HIGH-impact flow bodies are unchanged; focused regressions cover them.
    - Every RED/GREEN/REFACTOR or task commit is immediately preceded by a fresh GitNexus detect-changes comparison limited to the approved symbols/flows; any unexpected or HIGH/CRITICAL expansion blocks that commit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-evidence.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <done>Every new Gateway-auth failure is durably distinguishable without disclosing transport detail or changing old evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Prove checkpoint persistence and prompt process exit with no real run</name>
  <files>packages/config/src/deployment-target/__tests__/fixtures/data-chain-smoke-auth-timeout-child.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-process.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/debug/phase13-gateway-auth-timeout.md
    - scripts/gateway-readiness.ts
    - scripts/data-chain-smoke.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  </read_first>
  <action>Add a fixed test child that imports `runDataChainSmoke`, supplies a deterministic test tuple, injects all projection/preflight/D1/service prerequisites as passed, and injects a fetch that accepts the request but resolves only when the short test AbortSignal fires. Use the production default auth-observer path with an injected 50 ms test timeout. Inject an in-memory writer keyed by the expected JSON/Markdown paths; before the child exits, parse both peers and assert one checkpoint observation with `gateway_auth_timeout`, itemId null, and parity. Count fixture, snapshot, Gateway API, browser-observer, and provider/remote dependencies and require every count to remain zero. The child prints one allowlisted test result only after both peers are present, then exits with raw 2. The parent Vitest test must spawn the package-local Node/tsx child, require natural exit 2 within a measured 5000 ms budget (the debug import-only path measured about 2400 ms), require the allowlisted persistence result line to precede exit, and require no extra live handle. Decision trace: D-01/D-02 keep the child at one bounded fixture with zero downstream work; D-03 requires the local Gateway/auth gate to fail before remote; D-04/D-05 preserve exact tuple and remote-preflight boundaries; D-06 limits the in-memory output to closed non-secret peers; D-07 keeps real provider proof downstream. Immediately before every task commit for this code/test-producing task, run `gitnexus_detect_changes`, compare against only the new process-test child/harness plus the already-approved Task 2 runner/evidence delta, and stop before commit on any unexpected or HIGH/CRITICAL symbol/flow expansion; rerun after hook/autofix/restage. Do not create a directory or file under the Phase 13 evidence root, do not use a collision-style run id, and do not invoke root smoke/package commands.</action>
  <acceptance_criteria>
    - The child exits naturally with raw 2 within the measured 5000 ms budget under an injected 50 ms auth deadline; the bound remains far below the 300000 ms outer timeout.
    - JSON and Markdown peers exist in memory and validate before the process reports completion.
    - There is exactly one checkpoint observation and exactly two peer-write calls, with zero fixture/snapshot/API/browser/provider calls.
    - No real evidence directory, existing run, service, browser, provider, schema, or migration is touched.
    - The test would fail if a timer/body/loader handle kept the child alive near the outer deadline.
    - Every task commit is immediately preceded by a fresh detect-changes comparison limited to the planned child/harness and approved runner/evidence footprint; unexpected or HIGH/CRITICAL expansion blocks the commit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-smoke-process.test.ts</automated>
    <automated>pnpm --filter @starye/config type-check</automated>
    <automated>pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-readiness.json</automated>
    <automated>git diff --check -- scripts/data-chain-smoke.ts packages/config/src/deployment-target/data-chain-evidence.ts packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts packages/config/src/deployment-target/__tests__/fixtures/data-chain-smoke-auth-timeout-child.ts packages/config/src/deployment-target/__tests__/data-chain-smoke-process.test.ts</automated>
  </verify>
  <done>The timeout checkpoint is complete before bounded command exit, and no downstream or leaked-handle claim depends on p13-12 reconstruction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Transport outcome -> evidence vocabulary | Runtime detail must collapse into one exact non-secret code, not free-form diagnostics. |
| Shared evidence owner -> runner/observer/verifier flows | Even a union change can affect three high-value flows and requires full regression review. |
| Child process -> parent test | Exit status is accepted only after in-memory peer persistence and zero-call counters are parsed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-71 | Information Disclosure | auth diagnostic evidence | high | mitigate | Four closed codes only; no status/location/error/header/body values. |
| T-13-72 | Tampering | shared evidence union | high | mitigate | Do not edit HIGH validator body; run old/new parity and three-flow regressions. |
| T-13-73 | Repudiation | checkpoint-before-exit claim | high | mitigate | Child verifies both in-memory peers before printing completion and exiting 2. |
| T-13-74 | Denial of Service | timeout resource lifecycle | high | mitigate | Short injected deadline plus measured 5000 ms natural process-exit budget, still far below the 300000 ms outer timeout. |
| T-13-75 | Spoofing | downstream zero-call proof | medium | mitigate | Explicit counters for every fixture/API/browser/provider dependency. |
| T-13-SC | Tampering | package supply chain | low | accept | No package or lockfile change occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Selected target is usable through full chain. | 13-17 through 13-20 | COVERED | This plan makes failure bounded and diagnosable. |
| REQ | DATA-01 | Canonical local auth/Gateway path. | Tasks 2-3 plus 13-16 through 13-18 | COVERED | No direct port success. |
| REQ | DATA-02..DATA-06 | D1/fixture/admin/viewer proof. | 13-17 through 13-20 | COVERED | Zero external work here. |
| REQ | DATA-07 | Closed non-secret evidence. | Task 1 and 13-14/13-17 through 13-20 | COVERED | Old evidence preserved. |
| REQ | TEST-05 | Repeatable checkpoint and process output. | Tasks 2-3 | COVERED | Process lifecycle is explicit. |
| RESEARCH | - | Strict evidence schema and fail-closed local-first runner. | Tasks 1-3 | COVERED | Current one-item CONTEXT controls. |
| CONTEXT | D-01/D-02 | One bounded fixture. | Zero calls here; 13-17/13-19 execute it. | COVERED | No corpus or write. |
| CONTEXT | D-03 | Local gate precedes remote. | Tasks 2-3, 13-16/13-17 | COVERED | Timeout cannot reach remote. |
| CONTEXT | D-04/D-05 | Exact tuple and selected-target preflight. | 13-18 through 13-20 | COVERED | Unchanged here. |
| CONTEXT | D-06 | Non-secret repeatable evidence. | Tasks 1-2 | COVERED | Explicit HIGH checkpoint plus four exact values only. |
| CONTEXT | D-07 | Provider proof remains Phase 13. | 13-19/13-20 | COVERED | No static substitution. |
| VERIFICATION | G-1 | Fresh local pair blocked by ambiguous auth checkpoint. | Tasks 2-3 plus 13-16 through 13-18 | COVERED | Historical detail remains unknown. |
| VERIFICATION | G-2/G-3 | Production proof absent. | 13-19/13-20 | COVERED | Strictly downstream. |
| VERIFICATION | G-4 | Handoff wrapper broken. | 13-14 | COVERED | Same-wave disjoint repair after committed 13-13 baseline. |

Phase 14 literals, RUNBOOK work, and final matrix remain deferred and excluded.
</source_audit>

<verification>
- Run focused readiness/evidence/local/process/verifier tests, config type-check, and exact diff check.
- Present the known HIGH validator warning and receive this execution's Task 1 root-user approval before evidence-file editing; do not continue if the validator body would change or fresh impact exceeds the approved boundary.
- Run GitNexus detect-changes before each commit and inspect `runDataChainSmoke`, `observeDataChainSurfaces`, and `appendBrowserObservation` flows.
- Treat RED/GREEN/REFACTOR commits and ordinary task commits identically: the detect-changes result must be fresh immediately before the commit, compared with the task's expected symbol/flow set, and rerun after any restage.
- No smoke run, evidence directory, service restart, browser, provider, deploy, schema, migration, cleanup, or rollback command.
- Schema/migrations: N/A; no schema push.
</verification>

<success_criteria>
- Four exact auth failures round-trip through JSON, Markdown, runner output, and exact verifier with no free-form detail.
- Old generic evidence remains valid and byte-untouched.
- The no-header child persists one checkpoint pair, calls nothing downstream, and exits naturally well before the outer deadline.
- Focused/full regressions, type-check, GitNexus review, and diff check pass.
</success_criteria>

## Artifacts this phase produces

- **New symbols:** process-test child entry and its in-memory counters/result type; no new production evidence abstraction.
- **Modified symbols:** `scripts/data-chain-smoke.ts:runDataChainSmoke`, `runDataChainSmokeCli`; `data-chain-evidence.ts:dataChainCheckpointValues` and derived `DataChainCheckpoint` only.
- **CLI/machine fields:** existing runner machine JSON gains optional `checkpoint`; no new CLI flag.
- **Closed values:** `gateway_auth_timeout`, `gateway_auth_fetch_failed`, `gateway_auth_http_status_unaccepted`, `gateway_auth_redirect_invalid`; legacy `gateway_auth_unavailable` retained.
- **GitNexus scope:** Task 2's approved existing-symbol set is `validGatewayAuth`, `runDataChainSmoke`, `runDataChainSmokeCli`, `dataChainCheckpointValues`, `DataChainCheckpoint`, and the unchanged HIGH validator boundary; Task 3 adds only the process-test child/harness. Every commit is blocked on a fresh expected-flow comparison.
- **Paths:** the six frontmatter paths only; tests write peers in memory.
- **Schema/migrations:** N/A; no schema push.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-15-SUMMARY.md` after Task 1 approval and both implementation tasks pass. Record the current-execution approval, four codes, process timings/call counts, HIGH warning, tests/type-check, detect-changes scope, and zero live/evidence/schema actions.
</output>
