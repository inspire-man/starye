---
phase: 13-full-chain-data-smoke
plan: "21"
type: execute
wave: 15
depends_on: [13-13, 13-15, 13-16]
files_modified:
  - scripts/local-dev.ts
  - scripts/tsconfig.phase13-local-dev.json
  - packages/config/src/deployment-target/__tests__/local-dev.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md
autonomous: false
gap_closure: true
requirements: [DATA-01, DATA-07, TEST-05]
must_haves:
  truths:
    - "D-03: A fresh local supervisor either proves all seven fixed ports became ready within its bounded startup window, or it terminates only children it created, cleans only its materialized inputs, and exits nonzero before any smoke action."
    - "A Gateway child that remains live without binding 8080 is a startup failure, not a usable six-port tree."
    - "The historical 13-16 partial tree is never treated as replaceable: before Stop-Process, 13-21-SUMMARY.md persists a single authorization snapshot hash, exact supervisor PID, complete child-before-parent PID set, and pending/approved authorization state; the blocking checkpoint confirms that exact record."
    - "An authorization/snapshot/ancestry ambiguity publishes a closed blocked_pre_teardown branch with teardown_attempted:false and launch_attempted:false; a teardown failure publishes a closed blocked_after_teardown branch with launch_attempted:false."
    - "Only an approved, still-matching authorization record followed by a fresh task-owned supervisor with canonical Gateway readiness from pnpm check:services may publish runtime_eligibility: released for the deferred local handoff."
  artifacts:
    - path: scripts/local-dev.ts
      provides: Bounded atomic seven-service supervisor with task-owned startup cleanup
    - path: scripts/tsconfig.phase13-local-dev.json
      provides: Narrow type-check project for the local supervisor
    - path: packages/config/src/deployment-target/__tests__/local-dev.test.ts
      provides: Deterministic live-but-unbound and all-seven-ready supervisor tests
    - path: .planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md
      provides: Non-secret PID-bound authorization record, runtime ownership, branch-aware cold-start/readiness, and eligibility result
  key_links:
    - from: scripts/local-dev.ts
      to: expected fixed ports 8080, 8787, 5173, 3002, 3003, 3000, 3001
      via: One labeled child specification and bounded listener probe before startup succeeds
      pattern: runLocalDevSupervisor
    - from: live-but-unbound Gateway child
      to: task-owned cleanup
      via: Bounded failed readiness result invokes only recorded newly spawned child handles and materialized-input cleanup
      pattern: startup_timeout
    - from: 13-21-SUMMARY.md authorization record
      to: legacy process teardown
      via: Blocking checkpoint confirms one fresh snapshot hash, supervisor PID, and child-before-parent PID set; Task 3 recomputes the snapshot before any Stop-Process call
      pattern: authorization_snapshot_sha256
    - from: 13-21-SUMMARY.md
      to: 13-17 through 13-20
      via: Closed runtime_eligibility field is the latest runtime gate; historical 13-16 remains read-only diagnosis
      pattern: runtime_eligibility
---

<objective>
Repair the local-dev supervisor so it owns startup atomically across the seven fixed local ports, then convert the diagnosed pre-existing partial tree through an explicitly authorized, exact-PID operation into an all-free cold start and canonical Gateway readiness gate.

Purpose: Plan 13-16 correctly stopped on a six-port tree. The debug report proves local-dev can leave that state because it only observes child error and exit events. This plan repairs that root cause without claiming any smoke, schema, evidence, browser, provider, or run result.

Output: a tested bounded supervisor, a read-only-or-authorized runtime ownership record, and one closed runtime eligibility result for the revised downstream plan graph.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-15-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md
@.planning/debug/phase13-runtime-ownership.md
@scripts/local-dev.ts
@scripts/check-services.ps1
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
</context>

<discovery_evidence>
- The 2026-07-19 runtime diagnosis is authoritative for this gap: one current-repository local-dev ancestor owned six expected listeners while 8080 was absent, and local-dev has no all-seven bounded readiness contract.
- Current root pnpm dev launches scripts/local-dev.ts. The launcher already owns API, Gateway, Dashboard, Auth, Blog, Movie, and Comic child creation plus materialized target input cleanup; package.json and check-services remain consumer contracts, not files this plan changes.
- GitNexus pre-plan impacts are LOW: startPnpm, materializeLocalInputs, main, and local stop each have one direct caller (main) and no indexed execution flow. Executors must repeat impact analysis for every existing symbol they actually edit; this observation does not waive the AGENTS.md gate.
- No package installation is needed. Existing TypeScript, tsx, Vitest, PowerShell, and the Phase 13 canonical check:services contract are sufficient.
- Historical Plan 13-16 is an immutable blocked ownership checkpoint. This plan neither edits it nor reclassifies the previous partial tree as replaceable.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Order |
|---|---|---|---|
| 1 | 13-13/13-15 contracts and current local-dev source | Tested bounded task-owned supervisor | First; no runtime mutation |
| 2 | Task 1 committed and fresh read-only listener/CIM snapshot | PID-bound authorization artifact and blocking confirmation | Second; no Stop-Process or launch before confirmation |
| 3 | Approved authorization record whose recomputed snapshot still matches | Exact legacy-tree disposition, all-free gate, fresh task-owned cold start, canonical readiness, and branch-aware runtime eligibility | Last; only released result unblocks 13-17 |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD the atomic seven-port local-dev supervisor</name>
  <files>scripts/local-dev.ts, scripts/tsconfig.phase13-local-dev.json, packages/config/src/deployment-target/__tests__/local-dev.test.ts</files>
  <behavior>
    - A fake Gateway child that stays live while its 8080 listener probe remains false reaches the bounded startup failure, sets nonzero status, terminates only the seven fake child handles created by this invocation, and cleans the materialized inputs once.
    - A fake all-seven-ready tree returns success only after each required port is listener-ready; the exported result retains the task-owned child labels and PIDs needed by the operational gate.
    - A child error or early exit still follows the same idempotent task-owned cleanup path; no retry, port-cleaning command, wildcard kill, or process enumeration becomes part of source behavior.
  </behavior>
  <action>Before editing each existing symbol, run GitNexus upstream impact with the exact file hint for startPnpm, materializeLocalInputs, main, and the local stop symbol; run it again for any additional existing symbol selected during implementation. Report direct callers and affected flows in the task log. A HIGH or CRITICAL result is a blocking warning checkpoint: compare the returned symbols and flows with this task's declared supervisor boundary, record the scoped authorization, and stop rather than edit if the scope differs. Start RED by adding a deterministic Vitest harness that injects child spawning, fixed-port probing, sleeping/clock behavior, materialization, and cleanup; it must not bind real ports or start a real service. Refactor local-dev around one exported supervisor entry with a single labeled service specification containing API 8787, Gateway 8080, Dashboard 5173, Blog 3002, Auth 3003, Comic 3000, and Movie 3001. Preserve the existing target materialization and fixed argv/environment contracts. After spawning only that invocation's child handles, require a bounded all-seven listener-ready probe before declaring startup success. On timeout, probe failure, child error, or early child exit, use one idempotent shutdown path that signals only the recorded newly spawned children, cleans only the materialized target inputs owned by this invocation, and returns nonzero. Do not invoke clean:ports, taskkill by image name, a port-kill helper, process-tree discovery, or any broad kill from source. Keep SIGINT and SIGTERM on the same owned cleanup path. Add the narrow no-emit TypeScript project for local-dev and run RED, GREEN, and any REFACTOR checks with fresh GitNexus detect-changes immediately before each commit; stop on unexpected symbols, flows, or a HIGH/CRITICAL expansion outside the approved warning checkpoint.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
    <automated>pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json</automated>
    <automated>pnpm --filter @starye/config type-check</automated>
  </verify>
  <done>A live but unbound Gateway makes local-dev exit nonzero after only task-owned cleanup, while a complete seven-port fake tree remains live and returns success; no real runtime, smoke, schema, evidence, browser, provider, or run allocation occurs in this task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture and confirm the exact legacy-tree authorization record</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md</files>
  <what-built>Task 1 has produced the bounded supervisor. Before any process mutation, the executor records one fresh, non-secret legacy-tree authorization snapshot containing the exact PID set that would be eligible for teardown.</what-built>
  <action>Run only fresh read-only PowerShell inspection. Resolve the repository root and scripts/local-dev.ts path. For exactly 8080, 8787, 5173, 3002, 3003, 3000, and 3001, capture one listener/CIM ancestry snapshot without reading process environments or local secret files. It is eligible only when 8080 has no listener, every other fixed port has exactly one listener, every listener PID has a complete cycle-free parent chain to the same live Node process executing this exact workspace scripts/local-dev.ts, and no listener or candidate supervisor is ambiguous. Build the complete descendant PID set from that one supervisor in child-before-parent order; assert it includes every listener owner and no outside PID. Serialize the normalized non-secret port/PID/parent/workspace facts deterministically, calculate `authorization_snapshot_sha256`, and write exactly one pending authorization record to 13-21-SUMMARY.md: `legacy_teardown_authorization: pending_human_confirmation`, `authorization_snapshot_sha256`, `authorized_supervisor_pid`, `authorized_listener_owner_pids`, `authorized_descendant_pids_child_before_parent`, and `teardown_attempted: false`, `launch_attempted: false`. Do not call Stop-Process, pnpm dev, check:services, schema/migration, evidence, smoke, browser, provider, or run-id commands in this task. If the snapshot is missing, stale, ambiguous, incomplete, outside the current workspace, or cannot produce the exact descendant set, write only `runtime_eligibility: blocked`, `terminal_branch: blocked_pre_teardown`, one closed reason, `teardown_attempted: false`, and `launch_attempted: false`; do not offer approval and end the plan before any process mutation.</action>
  <how-to-verify>Inspect 13-21-SUMMARY.md. Confirm the pending record shows exactly one 64-hex snapshot hash, one supervisor PID, a non-empty child-before-parent PID set that includes the six listener owners, and both operation flags false. The approval applies only to that displayed PID set and hash.</how-to-verify>
  <verify>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md'; $pending = $summary -match '(?m)^legacy_teardown_authorization:\s*pending_human_confirmation\s*$' -and $summary -match '(?m)^authorization_snapshot_sha256:\s*[0-9a-f]{64}\s*$' -and $summary -match '(?m)^authorized_supervisor_pid:\s*\d+\s*$' -and $summary -match '(?m)^authorized_descendant_pids_child_before_parent:\s*\d+(,\d+)*\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blocked = $summary -match '(?m)^terminal_branch:\s*blocked_pre_teardown\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; if (-not ($pending -xor $blocked)) { throw 'Expected one pending PID-bound authorization record or one no-mutation blocked branch.' }; exit 0"</automated>
  </verify>
  <resume-signal>Type `approved legacy-tree PID set <authorization_snapshot_sha256>` only after the displayed hash and every PID are the intended legacy tree. Any other response or a changed snapshot leaves the tree untouched.</resume-signal>
  <done>A blocking checkpoint exposes one exact PID-bound authorization record with no teardown/launch, or a closed pre-teardown blocked record proves no mutation occurred.</done>
</task>

<task type="auto">
  <name>Task 3: Execute only the confirmed PID set, then publish released or blocked runtime eligibility</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md</files>
  <action>Proceed only after the Task 2 blocking resume signal exactly approves the Summary's displayed `authorization_snapshot_sha256`. Before Stop-Process, reload the record, require `legacy_teardown_authorization: pending_human_confirmation`, copy only the exact approved hash/PID set into `legacy_teardown_authorization: approved`, and take a second read-only snapshot using the same normalized serialization. Its hash, supervisor PID, listener-owner PIDs, complete child-before-parent descendant set, workspace path, and six-listener/missing-8080 state must equal the approved record exactly. Any mismatch, expired/missing approval, ambiguity, or parse failure appends `runtime_eligibility: blocked`, `terminal_branch: blocked_pre_teardown`, one closed reason, `teardown_attempted: false`, and `launch_attempted: false`, then ends before a Stop-Process or launch. Only on exact equality stop the approved captured PIDs in child-before-parent order with native PowerShell process APIs. Do not invoke clean:ports, dev:clean, wildcard matching, taskkill by image name, or any process/port cleanup outside the approved set. Wait within a bounded interval for every approved PID to exit and every fixed port to have zero listeners. A stop error, surviving approved PID, or remaining listener appends `runtime_eligibility: blocked`, `terminal_branch: blocked_after_teardown`, one closed reason, `teardown_attempted: true`, and `launch_attempted: false`, then ends without a launch. Only after a second immediate all-free assertion, start pnpm dev hidden from the resolved repository root, capture launcher and fresh scripts/local-dev.ts supervisor PIDs, and require every fixed listener to descend from that fresh supervisor. Poll pnpm check:services for at most 60 seconds and accept only exit 0 with exactly one starye-local-services-1 machine record whose seven listener rows are true and whose Gateway robots, auth, and auth-slash outcomes are accepted. Re-run the Task 1 focused supervisor/readiness/local-smoke tests, the local-dev narrow type-check, config type-check, and pnpm --filter gateway test. Any post-launch ancestry, machine-record, canonical-route, or test failure appends `runtime_eligibility: blocked`, `terminal_branch: blocked_after_launch`, the closed reason, `teardown_attempted: true`, and `launch_attempted: true`; it keeps all run allocation, D1/schema/migration work, evidence paths, browser observation, provider access, and remote work forbidden. Only when every gate passes, append `runtime_eligibility: released`, `terminal_branch: released`, the approved authorization hash/PID set, exact fresh supervisor facts, accepted check-services result, and `next_plan: 13-17`. This is a runtime prerequisite release, not a smoke pass, evidence claim, browser claim, provider claim, or Phase 13 completion. Run task-local GitNexus detect-changes before committing the Summary and require no uncommitted code symbol or execution-flow expansion.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md'; $released = $summary -match '(?m)^terminal_branch:\s*released\s*$' -and $summary -match '(?m)^runtime_eligibility:\s*released\s*$' -and $summary -match '(?m)^legacy_teardown_authorization:\s*approved\s*$' -and $summary -match '(?m)^legacy_partial_tree_disposition:\s*authorized_exact_teardown\s*$' -and $summary -match '(?m)^pre_cold_start_fixed_ports:\s*all_free\s*$' -and $summary -match '(?m)^fresh_local_dev_supervisor_pid:\s*\d+\s*$'; $blockedNoMutation = $summary -match '(?m)^terminal_branch:\s*blocked_pre_teardown\s*$' -and $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blockedAfterTeardown = $summary -match '(?m)^terminal_branch:\s*blocked_after_teardown\s*$' -and $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^teardown_attempted:\s*true\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blockedAfterLaunch = $summary -match '(?m)^terminal_branch:\s*blocked_after_launch\s*$' -and $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^teardown_attempted:\s*true\s*$' -and $summary -match '(?m)^launch_attempted:\s*true\s*$'; if ((@($released, $blockedNoMutation, $blockedAfterTeardown, $blockedAfterLaunch | Where-Object { $_ }).Count) -ne 1) { throw 'Expected exactly one released or closed blocked terminal branch.' }; exit 0"</automated>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md'; if ($summary -match '(?m)^terminal_branch:\s*blocked_(pre_teardown|after_teardown|after_launch)\s*$') { exit 0 }; if ($summary -notmatch '(?m)^terminal_branch:\s*released\s*$') { throw 'Missing terminal branch.' }; pnpm check:services; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-process.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter gateway test; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The exact approved legacy PID set is stopped only while its fresh snapshot remains identical, then a fresh seven-port supervisor releases downstream work after canonical readiness; authorization/snapshot ambiguity blocks with no teardown or launch, while any teardown/start/readiness failure remains a closed blocked result with no downstream action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local-dev parent -> child services | The supervisor can create seven local child processes but may only terminate its own recorded handles. |
| legacy partial tree -> authorized teardown | A pre-existing process tree is outside new-supervisor ownership until exact listener and ancestry gates prove the current authorized target. |
| all-free ports -> fresh cold start | A new tree can claim ownership only after every fixed port is proven free immediately before launch. |
| fresh listeners -> data-chain handoff | Listener state is diagnostic ownership proof; canonical Gateway HTTP readiness decides runtime eligibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-103 | Denial of Service | local-dev startup | high | mitigate | Bounded all-seven listener probe turns a live-but-unbound child into task-owned cleanup and nonzero exit. |
| T-13-104 | Tampering | pre-existing local process tree | critical | mitigate | A blocking checkpoint confirms one Summary-persisted snapshot hash, supervisor PID, and child-before-parent PID set; Task 3 recomputes exact equality before the captured-PID stop and blocks without mutation on mismatch. |
| T-13-105 | Spoofing | runtime ownership | high | mitigate | Fresh launcher/supervisor ancestry plus one listener per fixed port before check-services. |
| T-13-106 | Elevation of Privilege | downstream smoke eligibility | critical | mitigate | Only closed runtime_eligibility: released after canonical Gateway HTTP checks may unlock Plan 13-17; no schema, evidence, browser, provider, or run allocation earlier. |
| T-13-107 | Information Disclosure | process and runtime records | medium | mitigate | Inspect and persist only PIDs, start facts, script path match, port state, and closed readiness outcomes; never environment values or secret files. |
| T-13-SC | Tampering | package supply chain | low | accept | No package-manager install occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Prove selected target through Gateway/canonical chain. | 13-21 runtime prerequisite; 13-17 through 13-20 chain. | COVERED | 13-21 never claims chain success. |
| REQ | DATA-01 | Local Gateway/API/auth/dashboard/content route readiness. | Task 3 then 13-17/18. | COVERED | check:services remains canonical 8080 route gate. |
| REQ | DATA-02 | Local D1 readiness before ingest. | 13-17. | COVERED | Explicitly forbidden before runtime eligibility. |
| REQ | DATA-03 | One deterministic fixture. | 13-17/19. | COVERED | No fixture in 13-21. |
| REQ | DATA-04 | D1/API/admin correlation. | 13-17 through 13-20. | COVERED | No data surfaces in 13-21. |
| REQ | DATA-05 | Authorized Dashboard proof. | 13-18/20. | COVERED | Browser remains downstream. |
| REQ | DATA-06 | Canonical viewer proof. | 13-18/20. | COVERED | Browser remains downstream. |
| REQ | DATA-07 | Repeatable non-secret evidence. | Task 2/3 runtime Summary plus 13-17 through 13-20 pairs. | COVERED | Summary is runtime evidence, never smoke evidence. |
| REQ | TEST-05 | Repeatable smoke/runtime output. | Task 1/3 focused tests and existing later runner tests. | COVERED | No package additions. |
| RESEARCH | - | Local-first, Gateway-first, fail-closed service preflight. | Tasks 1-3. | COVERED | Canonical HTTP is after task-owned listener startup. |
| RESEARCH | - | No ambient target/parser or direct-port canonical proof. | Tasks 1-3 and 13-17 onward. | COVERED | Existing target materialization and check-services are reused. |
| CONTEXT | D-01 | Exactly one deterministic fixture. | 13-17/19. | COVERED | 13-21 allocates no item. |
| CONTEXT | D-02 | Closed fixture/crawler boundary. | 13-17/19. | COVERED | No crawler operation in 13-21. |
| CONTEXT | D-03 | Local-first via localhost 8080 before remote. | Tasks 1-3, then 13-17 onward. | COVERED | This is the direct runtime gate. |
| CONTEXT | D-04 | Exact tuple across D1/API/Dashboard/viewer. | 13-17 through 13-20. | COVERED | No tuple exists here. |
| CONTEXT | D-05 | Explicit provider preflight. | 13-19. | COVERED | Provider remains forbidden here. |
| CONTEXT | D-06 | Non-secret repeatable artifacts. | Tasks 2-3 and 13-17 through 13-20. | COVERED | PID/port/checkpoint facts only. |
| CONTEXT | D-07 | Real provider proof belongs in Phase 13. | 13-19/20. | COVERED | Runtime repair does not replace it. |
| VERIFICATION | G-1 | Fresh local path lacked a known Gateway cause. | 13-13/15 + Tasks 1-3 + 13-17/18. | COVERED | Debug identifies startup ownership root cause. |
| VERIFICATION | G-2 | Provider D1/API/admin proof missing. | 13-19/20. | COVERED | Still downstream. |
| VERIFICATION | G-3 | Production Dashboard/viewer proof missing. | 13-20. | COVERED | Still downstream. |
| VERIFICATION | G-4 | Nested verifier status mismatch. | 13-14 and 13-17 onward. | COVERED | Existing launcher contract remains consumed. |
</source_audit>

<verification>
- Task 1 uses deterministic injected child/port probes and never starts real services; it proves a live-but-unbound Gateway produces only new-tree cleanup and nonzero exit.
- Task 2 is a blocking PID-set authorization checkpoint. It persists/validates one fresh record before any Stop-Process or launch, while ambiguity ends as a no-mutation blocked branch.
- Task 3 recomputes the approved snapshot before the captured-PID stop, validates either a released or closed blocked terminal branch, and runs canonical readiness/tests only on the released branch.
- Every source symbol edit repeats AGENTS.md GitNexus impact analysis; HIGH/CRITICAL output is a scoped warning checkpoint. Fresh detect-changes is task-local before every code or Summary commit.
- Schema/migrations/evidence/browser/provider/run allocation: N/A and prohibited until a released runtime eligibility result is consumed by Plan 13-17.
</verification>

<success_criteria>
- local-dev cannot leave a new live six-port tree when Gateway fails to bind 8080.
- The diagnosed legacy partial tree is either left untouched by an authorization/snapshot ambiguity or stopped only through the explicitly confirmed, captured current-repository PID set, after which all seven ports are free before a fresh cold start.
- A newly started supervisor owns each fixed listener and pnpm check:services accepts canonical robots, auth, and auth-slash routes.
- 13-21-SUMMARY.md publishes runtime_eligibility: released only for that verified fresh runtime; otherwise it publishes a closed blocked result and no downstream smoke artifact exists.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md` when done. It must distinguish `runtime_eligibility: released` from `runtime_eligibility: blocked`, retain only non-secret PID/port/readiness facts, state whether 13-17 is eligible, and never alter historical attempts, evidence, Plans 13-01 through 13-16, or Phase status.
</output>
