---
phase: 13-full-chain-data-smoke
plan: "36"
type: execute
wave: 34
depends_on: [13-29]
files_modified:
  - scripts/local-dev.ts
  - packages/config/src/deployment-target/__tests__/local-dev.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-36-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-05
  - TEST-05
must_haves:
  truths:
    - "materializeLocalInputs passes profile: resolution.profile into materializeTargetDeployConfig for every Pages surface so pnpm dev no longer throws Selected Pages materialization requires a target profile."
    - "Focused local-dev regression still passes after the profile wiring fix."
    - "GitNexus impact for materializeLocalInputs remains LOW or is explicitly acknowledged if elevated."
  artifacts:
    - path: scripts/local-dev.ts
      provides: profile-aware Pages materialization for local supervisor
    - path: .planning/phases/13-full-chain-data-smoke/13-36-SUMMARY.md
      provides: committed local-dev fix evidence for Wave 35
---

<objective>
Commit and regression-lock the local-dev Pages materialization profile fix that unblocked `pnpm dev` after p13-17. This is required so a fresh local lifecycle can start for a new run_id. Does not allocate smoke runs or touch remote evidence.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-UAT.md
@scripts/local-dev.ts
@packages/config/src/deployment-target/deploy-config.ts
</context>

## Artifacts this phase produces
- Committed `profile: resolution.profile` wiring in `scripts/local-dev.ts`
- Optional strengthened assertion in local-dev tests if already covering materialize call shape
- `13-36-SUMMARY.md`

## Non-goals
- Do not reopen p13-17 remote checkpoint
- Do not run remote handoff
- Do not rewrite 13-VERIFICATION.md (owned by 13-39)
- Do not stage AGENTS.md / CLAUDE.md / evidence trees

<tasks>
<task type="auto">
  <name>Task 1: GitNexus impact + commit local-dev profile wiring</name>
  <files>scripts/local-dev.ts</files>
  <read_first>
    - scripts/local-dev.ts
    - packages/config/src/deployment-target/deploy-config.ts
    - packages/config/src/deployment-target/__tests__/local-dev.test.ts
  </read_first>
  <action>
Run GitNexus impact for `materializeLocalInputs` upstream; record blast radius. Confirm working tree already contains `profile: resolution.profile` inside the `materializeTargetDeployConfig({...})` call in `materializeLocalInputs`. If missing, add only that property from `resolution.profile`. Do not edit `materializeTargetDeployConfig` itself (HIGH blast radius). Run focused regression: `pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts` (and local-dev-authorization/gateway-readiness if already co-listed by prior phase scripts). Commit only `scripts/local-dev.ts` (+ test file if a minimal assertion was required). Write `13-36-SUMMARY.md` with impact risk, test exits, and commit SHA.
  </action>
  <acceptance_criteria>
    - scripts/local-dev.ts contains profile: resolution.profile adjacent to deploy in materializeTargetDeployConfig call
    - Focused vitest exits 0
    - 13-36-SUMMARY.md exists with status complete
    - git show --stat of the fix commit includes scripts/local-dev.ts and excludes evidence/
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts</automated>
  </verify>
  <done>Local supervisor can materialize Pages build inputs with target profile for a fresh pnpm dev.</done>
</task>
</tasks>
