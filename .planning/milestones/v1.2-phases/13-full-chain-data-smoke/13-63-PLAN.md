---
phase: 13-full-chain-data-smoke
plan: "63"
type: execute
wave: 57
depends_on: [13-56, 13-60]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-63-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Signed-in local Gateway Dashboard with a usable observation adapter
    why: p13-60 is permanently frozen at iab_observe_adapter_unavailable after allocation. The next carrier may be allocated only after the same signed IAB bridge or an untracked cookie-backed adapter is proven usable before allocation.
    env_vars:
      - name: STARYE_DATA_CHAIN_SESSION_COOKIE_FILE
        source: Untracked local cookie file only when the signed IAB observeSurface bridge is unavailable; never commit or print its value.
must_haves:
  truths:
    - "p13-60-5b545aa10389b50cfa86e78319665398, p13-57, p13-55, p13-52, p13-50, p13-49, and p13-41 remain immutable; this plan never observes, verifies, retries, or hands off any of them."
    - "A signed local observation adapter is proved usable before a p13-63 run id exists; adapter absence records a pre-allocation block and creates no carrier."
    - "Exactly one fresh p13-63-<32 lowercase hex> local handoff creates one deterministic non-R18 item with one non-empty local itemId and one pending local evidence pair."
    - "Only a signed IAB observeSurface bridge or an explicitly configured untracked cookie-backed observeSurfaceDefault may observe the new pair; a bare default observer is forbidden."
    - "The local observation order is http://localhost:8080/dashboard/movies followed by http://localhost:8080/movie/<item-code>; dual exact local verification becomes terminal_passed/provesExternalChain true or an honest no-retry checkpoint."
    - "All carrier evidence remains untracked and no remote, production, provider, deploy, or migration command is run."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-63-RUN-ID.txt
      provides: The only new local carrier identifier, created only after the signed adapter gate passes.
    - path: .planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md
      provides: Non-secret signed-adapter, local handoff, observation, and terminal/checkpoint outcome.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-63-run-id}/local.json
      provides: Untracked tuple-bound local evidence for the one-item carrier.
  key_links:
    - from: signed adapter pre-allocation probe
      to: p13-63 run allocation
      via: non-secret accepted adapter result before run-id creation
      pattern: "local_observation_adapter_ready"
    - from: local handoff pending pair
      to: ordered Gateway Dashboard then Viewer observation
      via: IAB or cookie-backed signed observer only
      pattern: "resolved_pending_observation"
---

<objective>
Restore a usable signed local observation path before allocating one new local carrier, then prove the local Dashboard-to-Viewer tuple or freeze an honest checkpoint.

Purpose: p13-60 already consumed its one permitted handoff and is immutable at `iab_observe_adapter_unavailable`. This plan must not repair that carrier. It first proves an actual signed observer can be invoked, then allocates a fresh `p13-63-*` carrier only after that proof.

Output: conditional `13-63-RUN-ID.txt`, untracked local evidence, and `13-63-SUMMARY.md`. No remote action.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-60-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
</context>

## Artifacts this phase produces

- `13-63-RUN-ID.txt` only after the adapter gate passes.
- Untracked `evidence/starye-org/p13-63-*/local.{attempt,json,md}`.
- `13-63-SUMMARY.md`.

## Non-goals

- No action against p13-60 or any earlier carrier/evidence tree.
- No unauthenticated observer, browser-cookie inspection, or tracked session material.
- No remote handoff, selected-production browser work, deploy, migration, or provider mutation.
- No second handoff, observation, or verifier after a post-allocation non-success.

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Prove the signed observation adapter before allocation</name>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-60-PLAN.md
    - scripts/data-chain-surface-observation.ts
  </read_first>
  <action>
Confirm the local Dashboard is signed in at `http://localhost:8080/dashboard/movies`, then prove one permitted observer is callable before allocating p13-63: either the Codex IAB `observeSurface` bridge can perform a non-mutating signed surface readiness probe, or an untracked `STARYE_DATA_CHAIN_SESSION_COOKIE_FILE` can be supplied to the repository-owned cookie-backed observer. Record only adapter kind and success/failure category. Do not navigate p13-60, read cookie values, copy browser profiles, or create a run id. If neither adapter is usable, write `13-63-SUMMARY.md` as `blocked_without_observation_adapter` and stop.
  </action>
  <acceptance_criteria>
    - A successful adapter proof is recorded without session material, or the summary records `blocked_without_observation_adapter` with no `13-63-RUN-ID.txt`.
    - p13-60 is not observed, verified, retried, or handed off.
  </acceptance_criteria>
  <done>A usable signed observation adapter is ready, or the plan stops before allocation.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate one fresh local pending carrier after dual Gateway readiness</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-63-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
    - .planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
  </read_first>
  <action>
Proceed only after Task 1 succeeds. Confirm `13-63-RUN-ID.txt` and every `evidence/starye-org/p13-63-*` path are absent. Run `pnpm check:services` twice and require the Gateway plus `robots`, `auth`, and `authSlash` checks to pass both times. Allocate exactly one `p13-63-<32 lowercase hex>` id, run `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <p13-63>`, then run one exact local verifier. Require one pending local pair with a non-empty itemId and `runnerInvocations: 1`; otherwise write a non-secret frozen summary and stop.
  </action>
  <acceptance_criteria>
    - Run id allocation occurs only after the signed adapter proof and two successful Gateway readiness checks.
    - Exactly one local handoff and one pending local verifier run occur for p13-63.
    - The evidence tuple contains the deterministic code and a non-empty local itemId without remote artifacts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-63-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>One unobserved p13-63 local pair exists, or the new carrier is honestly frozen before observation.</done>
</task>

<task type="auto">
  <name>Task 3: Observe the new local tuple in Dashboard-to-Viewer order and close local status</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>
Use only the adapter accepted in Task 1 to observe the p13-63 pair in exact order: `http://localhost:8080/dashboard/movies`, then `http://localhost:8080/movie/<item-code>`. Match the Dashboard code and local itemId to the pending evidence tuple. Run the exact local verifier after observation and require `terminal_passed` plus `provesExternalChain: true`. On adapter, Dashboard-auth, tuple, Viewer, or verifier failure, freeze p13-63 as a checkpoint without retry, second observer, or remote command. Write `13-63-SUMMARY.md` with non-secret adapter kind, exits, status, code, and itemId presence only.
  </action>
  <acceptance_criteria>
    - The observed order is Dashboard before Viewer through Gateway port 8080 only.
    - Success has matching code/itemId and `terminal_passed` / `provesExternalChain: true`; failure is an honest no-retry checkpoint.
    - No credentials, cookies, raw endpoints, or remote claims enter tracked artifacts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-63-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>p13-63 local terminal proof unlocks the new human-gated remote handoff, or its honest local checkpoint blocks downstream work.</done>
</task>
</tasks>

## Verification

- Signed observation adapter is proven before allocation.
- Gateway-only Dashboard then Viewer order is preserved.
- p13-60 and every historical carrier remain untouched.
- Local evidence is one-item, tuple-bound, untracked, and never used as a production claim.

## Self-check

- Supersedes the executable intent of 13-61/13-62 only for a future p13-63 terminal local carrier.
- Keeps remote authorization and production browser proof outside this local plan.
