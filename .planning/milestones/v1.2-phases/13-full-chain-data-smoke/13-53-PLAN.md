---
phase: 13-full-chain-data-smoke
plan: "53"
type: execute
wave: 48
depends_on: [13-52, 13-55]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-53-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Authorized starye-org selected-target remote context
    why: Fresh remote fixture for the 13-55 local-terminal run requires operator authorization and green Cloudflare credentials (13-40 pattern). Must not reopen p13-41 or p13-45.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: packages/crawler/.env or operator secret store (untracked)
      - name: CLOUDFLARE_ACCOUNT_ID
        source: target profile / packages/crawler/.env
must_haves:
  truths:
    - "13-55 local terminal_passed is the only authorization into this remote handoff; 13-52 remains an immutable local checkpoint and does not unlock remote work."
    - "Exactly one remote handoff for the same p13-55 run id yields remote pending with non-empty remote itemId, or an honest remote checkpoint, without reopening p13-41/p13-45."
    - "No production Dashboard/viewer observation happens in this plan."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-53-SUMMARY.md
      provides: authorized remote pending or honest remote checkpoint for p13-55
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-55-run-id}/remote.json
      provides: untracked remote pair evidence when handoff runs
---

<objective>
Run one human-gated selected-production remote handoff for the fresh p13-55 run, producing an unobserved remote pending pair (or honest checkpoint).

Purpose: VERIFICATION still needs production UI proof, but p13-41 remote is permanently checkpointed and must not be reopened. 13-45 is invalid_target history. Only a new local-terminal run may carry remote work.

Output: `13-53-SUMMARY.md` plus untracked remote evidence for p13-55 when executed.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-42-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@scripts/data-chain-handoff.ts
</context>

## Artifacts this phase produces
- `13-53-SUMMARY.md`
- untracked `remote.json` / `remote.md` under the p13-55 evidence root when handoff runs

## Non-goals
- No production browser observation
- No reopen of p13-41 / p13-17 / p13-37 / p13-45 remotes
- No second remote handoff after non-success

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Authorize remote handoff for p13-55 only</name>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
  </read_first>
  <action>
Require 13-55 Summary to show local `terminal_passed` / `provesExternalChain: true` for its p13-55 run. Treat 13-52 Summary as immutable checkpoint history only. Present operator gate: remote handoff will use mode remote / target starye-org / that exact run id, consume CLOUDFLARE_API_TOKEN from untracked env, and will not touch p13-41 or p13-45 trees. Stop until operator explicitly authorizes this single remote handoff (or records denial → Summary blocked_without_authorization, no commands).
  </action>
  <done>Operator authorizes or denies the single p13-55 remote handoff in writing.</done>
</task>

<task type="auto">
  <name>Task 2: One remote handoff and remote pending verify</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-53-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-55-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
    - scripts/data-chain-handoff.ts
  </read_first>
  <action>
Only after authorization: run official selected-target live preflight for starye-org remote/smoke if still required by current scripts, then exactly one `pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id <p13-55>`. Require pending remote outcome with non-empty remote itemId and runnerInvocations 1, or persist honest checkpoint. Run remote exact verifier once for the pending pair (`provesExternalChain` false until browser). Never open production Dashboard/viewer here. Write non-secret Summary with exits and itemId presence only. Freeze on failure; no retry.
  </action>
  <acceptance_criteria>
    - One remote handoff only, bound to p13-55.
    - Remote pending with itemId or honest checkpoint recorded.
    - p13-41 remote checkpoint hash unchanged if verified.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-55-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>Unobserved remote pending pair exists for production UI planning, or remote work is honestly blocked.</done>
</task>
</tasks>

