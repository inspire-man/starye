---
phase: 13-full-chain-data-smoke
plan: "56"
type: execute
wave: 50
depends_on: [13-54, 13-55]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-03
  - DATA-07
  - TEST-05
user_setup:
  - service: Authorized starye-org selected-target remote credential context
    why: Nested handoff preflight must see the same durable CLOUDFLARE_API_TOKEN / account binding that standalone live preflight uses. No remote handoff is authorized here.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: packages/crawler/.env or operator secret store (untracked)
      - name: CLOUDFLARE_ACCOUNT_ID
        source: target profile / packages/crawler/.env
must_haves:
  truths:
    - "p13-55 remote remains immutable at target_preflight_unmet; this plan never opens or rewrites that evidence tree."
    - "Standalone remote live preflight for starye-org exits 0 after the versions-list + runtime-env/timeout hardening."
    - "Nested/handoff-owned remote preflight (pickRuntimeEnvironment + pnpm target-profile preflight --scope remote --command smoke --live with 180s timeout) also exits 0 without creating remote.attempt."
    - "No remote handoff, no runner mutation, no new run_id allocation, and no production browser work happen in this plan."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
      provides: non-secret standalone + nested preflight green proof that unlocks a fresh local carrier
  key_links:
    - from: pnpm target-profile preflight --target starye-org --scope remote --command smoke --live
      to: nested invokeOfficialDataChainRemotePreflight
      via: same stripped Windows runtime env and spawnSync timeout contract as handoff
      pattern: "nested_preflight_green"
---

<objective>
Prove the post-p13-55 nested remote preflight reliability fix is green for `starye-org` without allocating a new smoke run or writing any remote evidence pair.

Purpose: Authorized p13-55 remote froze at `target_preflight_unmet` because nested handoff preflight hung under stripped env while standalone preflight was green. Durable fixes (wrangler `versions list`, expanded Windows `pickRuntimeEnvironment`, 60s/180s spawn timeouts) are already in tree. This plan re-proves both standalone and nested paths before any new local/remote carrier is created.

Output: `13-56-SUMMARY.md` only. No `remote.attempt`, no run id, no handoff.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-full-chain-data-smoke/13-54-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@scripts/data-chain-handoff.ts
@scripts/target-profile.ts
</context>

## Artifacts this phase produces
- `13-56-SUMMARY.md`

## Non-goals
- No reopen/rewrite of `evidence/starye-org/p13-55-*` remote artifacts
- No `pnpm smoke:data-chain:handoff` in any mode
- No new `p13-*` run id allocation
- No production Dashboard/viewer, deploy, or migration
- No secret values in Summary or commits

<tasks>
<task type="auto">
  <name>Task 1: Confirm p13-55 immutability and hardening presence</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-54-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
    - scripts/data-chain-handoff.ts
    - scripts/target-profile.ts
  </read_first>
  <action>
Read 13-54/13-55 summaries and confirm p13-55 local is terminal_passed while remote is immutable target_preflight_unmet. Confirm current source still contains: (1) worker live-check using wrangler versions list rather than deployments list, (2) Windows pickRuntimeEnvironment allowlist with Path/SystemRoot/ComSpec/etc., (3) invokeOfficialDataChainRemotePreflight spawnSync timeout 180_000. Record non-secret file presence only. Do not touch the p13-55 evidence tree.
  </action>
  <acceptance_criteria>
    - 13-54 records blocked_on_remote_checkpoint / target_preflight_unmet for p13-55.
    - Hardening symbols/timeouts are present in current scripts.
    - No p13-55 remote files are modified.
  </acceptance_criteria>
  <verify>
    <automated>git log -2 --oneline -- scripts/target-profile.ts scripts/data-chain-handoff.ts</automated>
  </verify>
  <done>Immutable p13-55 remote history and in-tree preflight hardening are confirmed without mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Standalone remote live preflight green</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md</files>
  <read_first>
    - scripts/target-profile.ts
    - packages/crawler/.env
  </read_first>
  <action>
With operator untracked Cloudflare credentials available, run exactly one standalone live preflight:

`pnpm target-profile preflight --target starye-org --scope remote --command smoke --live`

Require exit 0. If unmet/auth failure, write honest Summary blocked_on_standalone_preflight and stop; do not invent nested success. Never log token values.
  </action>
  <acceptance_criteria>
    - Standalone preflight exit code is 0, or Summary records honest standalone failure and stops.
    - No remote.attempt is created under any evidence root.
  </acceptance_criteria>
  <verify>
    <automated>pnpm target-profile preflight --target starye-org --scope remote --command smoke --live</automated>
  </verify>
  <done>Standalone remote live preflight is green for starye-org, or work is honestly blocked before nested proof.</done>
</task>

<task type="auto">
  <name>Task 3: Nested/handoff-owned preflight green without remote.attempt</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/target-profile.ts
  </read_first>
  <action>
Exercise the same nested path handoff uses without writing remote evidence. Prefer a one-shot Node/tsx invocation that imports `invokeOfficialDataChainRemotePreflight` with a resolved starye-org remote request object (synthetic disposable run id is allowed only as an in-memory request field and must never call writePair/handoff). Equivalent proof: spawn the exact package-manager invocation used by handoff under `pickRuntimeEnvironment(...)` plus CLOUDFLARE_API_TOKEN passthrough, CI/WRANGLER_SEND_METRICS/NO_UPDATE_NOTIFIER, timeout 180s. Require nested result `passed` / child status 0. Before and after, assert no new `remote.attempt` exists under `evidence/starye-org/**` for any run. Write 13-56-SUMMARY.md with exits, duration bounds, and unlock note for 13-57. Freeze on nested unmet; no retry loops, no handoff.
  </action>
  <acceptance_criteria>
    - Nested preflight returns passed / exit 0 under stripped runtime env.
    - No remote.attempt / remote.json / remote.md created by this plan.
    - Summary unlocks 13-57 only when both standalone and nested are green.
  </acceptance_criteria>
  <verify>
    <automated>node --import tsx -e "import { invokeOfficialDataChainRemotePreflight } from './scripts/data-chain-handoff.ts'; /* resolved starye-org remote request */"</automated>
  </verify>
  <done>Nested handoff preflight is proven green without remote evidence mutation, unlocking a fresh local carrier plan.</done>
</task>
</tasks>

## Verification
- Both preflight paths green for `starye-org`
- p13-55 remote tree byte-stable
- No handoff / no new run id

## Self-check (plan_checker disabled)
- Gap-only: reliability proof before new carrier
- Does not reopen historical remotes
- Human remote auth deferred to 13-58
