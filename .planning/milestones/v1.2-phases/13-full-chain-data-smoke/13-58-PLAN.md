---
phase: 13-full-chain-data-smoke
plan: "58"
type: execute
wave: 52
depends_on: [13-57]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-58-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Authorized starye-org selected-target remote context
    why: Fresh remote fixture for the 13-57 local-terminal run requires a new explicit operator authorization phrase bound to that exact run id. Must not reopen p13-55 or p13-41.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: packages/crawler/.env or operator secret store (untracked)
      - name: CLOUDFLARE_ACCOUNT_ID
        source: target profile / packages/crawler/.env
must_haves:
  truths:
    - "13-57 local terminal_passed is the only authorization into this remote handoff; p13-55 remote remains an immutable target_preflight_unmet checkpoint and does not unlock remote work."
    - "Operator must supply a new explicit phrase bound to the exact p13-57 run id before any remote command; historical p13-55 authorization does not transfer."
    - "Exactly one remote handoff for that p13-57 run id yields remote pending with non-empty remote itemId, or an honest remote checkpoint, without reopening p13-55/p13-41."
    - "Before the sole handoff, remote artifacts for the new run id are absent; after a non-success, no second remote handoff is attempted."
    - "No production Dashboard/viewer observation happens in this plan."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-58-SUMMARY.md
      provides: authorized remote pending or honest remote checkpoint for p13-57
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-57-run-id}/remote.json
      provides: untracked remote pair evidence when handoff runs
---

<objective>
Run one human-gated selected-production remote handoff for the fresh p13-57 local-terminal carrier, producing an unobserved remote pending pair (or honest permanent checkpoint).

Purpose: VERIFICATION still needs a remote pending fixture with non-empty itemId after p13-55 nested-preflight freeze. Only a new local-terminal run may carry remote work. Nested preflight reliability was proved in 13-56; this plan consumes one new human authorization for the new run only.

Output: `13-58-SUMMARY.md` plus untracked remote evidence for p13-57 when executed.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-54-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@scripts/data-chain-handoff.ts
</context>

## Artifacts this phase produces
- `13-58-SUMMARY.md`
- untracked `remote.json` / `remote.md` under the p13-57 evidence root when handoff runs

## Non-goals
- No production browser observation
- No reopen of p13-55 / p13-41 / p13-17 / p13-37 remotes
- No second remote handoff after non-success
- No reuse of the p13-55 authorization phrase

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Authorize remote handoff for p13-57 only</name>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
  </read_first>
  <action>
Require 13-57 Summary to show local `terminal_passed` / `provesExternalChain: true` for its p13-57 run, and 13-56 nested preflight green. Treat p13-55 remote as immutable checkpoint history only. Present operator gate: remote handoff will use mode remote / target starye-org / that exact p13-57 run id, consume CLOUDFLARE_API_TOKEN from untracked env, and will not touch p13-55 or p13-41 trees. Require a new explicit phrase of the form `授权 remote handoff p13-57-<32hex>` (or clear denial). Historical `授权 remote handoff p13-55-...` does not authorize this plan. Stop until operator authorizes or denies in writing.
  </action>
  <done>Operator authorizes or denies the single p13-57 remote handoff in writing with a run-id-bound phrase.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-handoff absence check + one remote handoff + pending verify</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-58-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-57-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
    - scripts/data-chain-handoff.ts
  </read_first>
  <action>
Only after authorization: confirm remote artifacts are absent for the p13-57 evidence root (`remote.attempt` / `remote.json` / `remote.md` must not exist yet). Optionally reconfirm nested preflight green without writing pairs. Then run exactly one:

`pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id <p13-57>`

Require pending remote outcome with non-empty remote itemId and runnerInvocations 1, or persist honest permanent checkpoint. Run remote exact verifier once for the pending pair (`provesExternalChain` false until browser) or checkpoint path. Never open production Dashboard/viewer here. Write non-secret Summary with exits and itemId presence only. Freeze on failure; no retry; never reopen p13-55.
  </action>
  <acceptance_criteria>
    - Pre-handoff remote artifacts for p13-57 were absent.
    - One remote handoff only, bound to p13-57.
    - Remote pending with non-empty itemId, or honest checkpoint recorded.
    - p13-55 remote checkpoint hashes unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-57-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>Unobserved remote pending pair exists for production UI planning, or remote work is honestly blocked.</done>
</task>
</tasks>

## Verification
- New run-id-bound human auth only
- One remote handoff maximum
- Historical remotes immutable

## Self-check (plan_checker disabled)
- Supersedes executable intent of frozen p13-55 remote path without reopening it
- Production surfaces deferred to 13-59
