---
phase: 13-full-chain-data-smoke
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/.dev.vars
  - apps/gateway/.dev.vars
  - .env.local
  - packages/crawler/.env
  - packages/config/src/deployment-target/data-chain-evidence.ts
  - packages/config/src/deployment-target/index.ts
  - packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-07
  - TEST-05
user_setup: []
must_haves:
  truths:
    - "D-03: the currently failing selected-target local projection is repaired through the existing managed-block writer, then `project-local --check` passes without changing user-managed secret values."
    - "D-01/D-06: every post-ingest data-chain record carries the authoritative non-empty correlation tuple `{ targetId, runId, itemCode, itemId }`, rejects direct application ports, and contains no secret-shaped data. `pre_ingest` may use `itemId: null` only for a failed/checkpoint gate before fixture-plus-snapshot resolution; `resolved_pending_observation` preserves the resolved tuple while Dashboard/viewer proof is still incomplete."
    - "D-03/D-06: JSON and Markdown evidence contain typed `local_projection`, `local_d1_readiness`, and `gateway_auth` observations before local D1/API/Dashboard/viewer correlation, distinguish pending, passed, failed, and checkpoint aggregate outcomes, and contain repeatable non-secret target, run, item, surface, and path fields."
  artifacts:
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
  key_links:
    - "selected TargetProfile plus run id -> deterministic fixture/snapshot correlation tuple -> resolved_pending_observation -> Dashboard append -> viewer append -> resolved passed JSON and Markdown evidence"
    - "local managed-block projection -> project-local --check -> later local D1/Gateway smoke gate"
---

<objective>
Remove the observed local target-managed projection blocker before any D1, crawler, browser, or provider attempt, then define the typed evidence contract consumed by every later smoke result.

Purpose: D-03 makes a valid local projection the first operational gate, while D-01 and D-06 require one deterministic item identity and redacted, reusable evidence before live integration.

Output: repaired machine-local target-managed projections (never staged), plus typed identity/evidence validation and tests.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/12-cloudflare-config-switching/12-VERIFICATION.md
@packages/config/src/deployment-target/projection-plan.ts
@packages/config/src/deployment-target/env-file-block.ts
@scripts/target-profile.ts
</context>

<dependency_graph>
| Task | Needs | Creates | Why it is ordered here |
|---|---|---|---|
| 1 | Existing four-file managed projection plan | A passing selected local projection or an honest hard failure | D-03 blocks every local ingest and remote attempt. |
| 2 | Explicit target profile and phase decisions | Typed identity, redacted evidence record, serializer, and regression suite | Later local and remote runners need one immutable evidence contract. |
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Repair the selected local projection as the first blocking readiness gate</name>
  <files>apps/api/.dev.vars, apps/gateway/.dev.vars, .env.local, packages/crawler/.env</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
    - .planning/phases/11-deployment-target-foundation/11-CONTEXT.md
    - packages/config/src/deployment-target/projection-plan.ts
    - packages/config/src/deployment-target/env-file-block.ts
    - scripts/target-profile.ts
  </read_first>
  <action>Reproduce the recorded blocking command `pnpm target-profile project-local --target starye-org --check` before changing any file. Use only `pnpm target-profile project-local --target starye-org --write` to update the four existing target-managed blocks, immediately rerun the check, and stop on malformed marker topology instead of hand-editing around it. Per D-03, this is a prerequisite rather than a warning: no D1, fixture, Gateway, Dashboard, or remote operation may start until the check passes. Preserve every user-managed secret line byte-for-byte; do not print, copy into evidence, add to Git, stage, or commit these machine-local files. Record only the non-secret command outcome and target id in the plan summary.</action>
  <verify>
    <automated>pnpm target-profile project-local --target starye-org --check</automated>
    <automated>pnpm target-profile validate --target starye-org</automated>
  </verify>
  <acceptance_criteria>
    - The projection check no longer reports a target-managed mismatch for API, gateway, root, or crawler consumer files.
    - `git status --short` does not show the protected user-owned planning, agent, or Claude compatibility files altered by this task.
    - No secret value, raw environment dump, or ignored local environment file is included in the task commit.
  </acceptance_criteria>
  <done>The observed `project-local --check` failure is resolved or the task stops before smoke work with its exact non-secret blocking diagnostic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define deterministic identity and allowlisted evidence serialization</name>
  <files>packages/config/src/deployment-target/data-chain-evidence.ts, packages/config/src/deployment-target/index.ts, packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - packages/config/src/deployment-target/target-profile.schema.ts
    - packages/config/src/deployment-target/target-resolver.ts
    - packages/config/src/deployment-target/preflight.ts
    - packages/config/src/deployment-target/index.ts
  </read_first>
  <behavior>
    - A fixed Phase 13 namespace plus explicit target and run id yields exactly one deterministic non-R18 fixture code; no random existing item can satisfy a run.
    - A post-ingest record is valid only when its authoritative non-empty correlation tuple is consistent across `local_projection`, `local_d1_readiness`, `service_readiness`, `gateway_auth`, D1, API, Dashboard, and viewer observations. A remote record requires the exact same target/run local record in terminal `ingestState: 'resolved'` with aggregate `passed`, including passed `local_projection`, D1 readiness, service readiness, Gateway auth, D1, API, Dashboard, and viewer rows; `resolved_pending_observation` is explicitly remote-ineligible.
    - Define an explicit three-state discriminated evidence lifecycle. `ingestState: 'pre_ingest'` has `itemId: null`, aggregate only `failed|checkpoint`, and exactly the failed/checkpoint prerequisite observation that stopped execution before fixture-plus-snapshot resolution. It may retain a deterministic candidate code for diagnostics, but it must never invent an item id, contain a passed/data-surface row, or be used as a remote prerequisite. `ingestState: 'resolved_pending_observation'` is created only after the fixed fixture plus read-only D1 snapshot resolve authoritative non-empty `itemCode` and `itemId`; it preserves buffered prerequisite rows and resolved D1/API observations, has aggregate `pending|failed|checkpoint` (never `passed`), and remains remote-ineligible. `ingestState: 'resolved'` is the only terminal passed state: it requires the same non-empty tuple and all required local or remote observations passed, including Dashboard and viewer. Buffer successful prerequisite results until the authoritative fixture-plus-snapshot result resolves the item id, then persist them in `resolved_pending_observation`; never demote that record to `pre_ingest` or discard its item id.
    - Local browser paths may use only `http://localhost:8080`; remote browser records identify the selected canonical surface by target id plus path, rather than storing an arbitrary remote origin. The only accepted surface names are `local_projection`, `local_d1_readiness`, `service_readiness`, `gateway_auth`, `remote_preflight`, `d1`, `api`, `dashboard`, and `viewer`.
    - A typed append operation accepts only an existing `resolved_pending_observation` record whose full target/run/code/id tuple matches. It permits only the fixed two-row browser sequence: first `dashboard` at `/dashboard/movies`, then `viewer` at `/movie/<item-code>`; it rejects a viewer skip, duplicate row, out-of-order append, mismatch, arbitrary data surface, raw input, free text, URL/origin, header, cookie, or success claim. A passed Dashboard append remains `resolved_pending_observation`/aggregate `pending` and returns 2. A passed viewer append promotes the same tuple to terminal `resolved`/aggregate `passed` and returns 0. A failed/checkpoint browser append persists a non-success aggregate and returns `CHECKPOINT_EXIT_CODE = 2`; it cannot synthetically promote a record to passed, and the append flow rejects any later duplicate/retry or viewer-after-nonpass row. Pre-ingest terminal records are writer-owned and cannot be opened by append.
    - `pending`, `failed`, and `checkpoint` are non-success aggregate states. A writer invocation that records a browser checkpoint must persist valid evidence before returning `CHECKPOINT_EXIT_CODE = 2`, while schema validation of that persisted non-success artifact returns code 0 and never promotes it to `passed`.
  </behavior>
  <action>Before modifying an existing exported deployment-target symbol, run the AGENTS.md-required GitNexus upstream impact analysis with the exact symbol/file hint and report the blast radius; explicitly warn and stop for HIGH or CRITICAL risk. Write failing contract tests first. Add a typed `data-chain-evidence.ts` module that accepts only explicit `targetId`, `runId`, deterministic candidate `itemCode`, mode, timestamp, and the fixed surface/status/checkpoint vocabulary. Model the record as a three-state discriminated union: a pre-ingest terminal record has `itemId: null`, aggregate `failed|checkpoint`, and exactly the failed/checkpoint prerequisite observation; a `resolved_pending_observation` record is created only after the fixture plus read-only snapshot provide authoritative non-empty `itemCode`/`itemId`, persists buffered prerequisite and D1/API observations, and has aggregate `pending|failed|checkpoint`; a terminal resolved record has the same tuple, aggregate `passed`, and all required surface rows passed. Never create a synthetic or placeholder id, regress a post-snapshot record to `pre_ingest`, or discard that resolved tuple after API/browser failure. The only pre-ingest surfaces are `local_projection`, `local_d1_readiness`, `service_readiness`, `gateway_auth`, and `remote_preflight`; use `remote_preflight` for a remote `target_preflight_unmet` checkpoint and `local_projection` for a missing/exact-local-run prerequisite. The typed append API consumed later by `smoke:data-chain-observe` must load only a `resolved_pending_observation` record, compare the full tuple, derive the allowed local Gateway origin or selected-canonical target-relative path internally, and permit exactly Dashboard then viewer. Dashboard passed stays pending and returns 2; viewer passed promotes the existing artifact to resolved/passed and returns 0. Failed/checkpoint browser append persists its non-success artifact and returns 2; reject duplicates, skipped viewer order, a viewer after Dashboard non-pass, or any later synthetic promotion. It must accept no free-form note, success claim, URL, origin, header, cookie, secret, arbitrary surface, or raw input. Permit `local_projection` alongside local D1/service/auth prerequisites, but reject a pre-ingest passed observation and reject every D1/API/Dashboard/viewer row until the fixture and read-only snapshot have supplied the authoritative id. Persist successful prerequisite observations only after that pending tuple exists; on a prerequisite failure persist the redacted pre-ingest terminal record instead. Implement a strict allowlist serializer for JSON and a Markdown renderer that do not accept environment objects, headers, tokens, secret-shaped keys, raw prepared context, arbitrary output paths, or full remote origins. Validate the four-element tuple, lifecycle transition/order, and required local projection/D1/service/auth/D1/API/Dashboard/viewer rows before terminal passed persistence; retain bounded retry/observation history. Reject direct service ports, including the auth service port, in canonical evidence, require the local Gateway origin for local mode, and represent remote public/admin paths only as selected-canonical target-relative paths. Define `CHECKPOINT_EXIT_CODE = 2` for callers but keep the pure schema validator separate so it returns zero for a valid pending/failed/checkpoint artifact. Export the new typed contract from the existing barrel without adding a second target resolver or a free-form command API. Cite D-01, D-03, and D-06 in exported contract comments only where they explain an invariant, not in evidence output.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-evidence.test.ts</automated>
    <automated>pnpm --filter @starye/config exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove equal target/run inputs generate the same one-item code, changing target or run changes the code, and a fixture cannot be R18 or contain more than one movie payload.
    - Tests reject tuple disagreement, missing item id after a claimed ingest, every pre-ingest `passed` record/observation, a pre-ingest D1/API/Dashboard/viewer row, a pre-ingest fabricated item id, arbitrary output roots, token-like field names, request headers, secret values, raw context objects, direct application-port URLs (including auth), unsupported surfaces/statuses/checkpoint categories, and free-form observation input.
    - Tests accept only a `failed|checkpoint` pre-ingest terminal record with `itemId: null` and one prerequisite checkpoint/failure, then create `resolved_pending_observation` only after fixture plus D1 snapshot resolve the non-empty tuple and preserve buffered prerequisite/D1/API rows. They prove no item id is lost after a post-snapshot API/browser failure; remote eligibility rejects this pending state.
    - Tests prove append accepts only the matching pending tuple and Dashboard-then-viewer grammar: reject duplicate Dashboard/viewer, skipped viewer, viewer-before-Dashboard, viewer after Dashboard non-pass, and mismatched tuple. A passed Dashboard remains pending/exit 2, a passed viewer alone promotes to resolved/passed/exit 0, and any browser checkpoint remains a persisted non-success/exit 2. JSON and Markdown preserve prior bounded observations and `canonical_api_unavailable`/checkpoint output remain distinct from success.
  </acceptance_criteria>
  <done>D-01 and D-06 have an importable, tested evidence contract that later runners can use without exposing target credentials or accepting an arbitrary endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator local env -> managed projection writer | The writer may see user-owned secret lines but may only change named target-managed blocks. |
| Runner observations -> evidence artifact | Inputs can include service output, headers, and remote addresses that must not be persisted. |
| Explicit target/run -> fixture identity | User-controlled identifiers must not become an arbitrary item, target selection, or bulk workload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-01 | Tampering | local projection files | high | mitigate | Use only the Phase 11 managed-block writer, re-check all four consumers, and never hand-edit user-managed values. |
| T-13-02 | Information Disclosure | evidence serializer | critical | mitigate | Typed allowlist, field-name validation, remote-origin normalization, and redaction regression tests. |
| T-13-03 | Spoofing | fixture identity | high | mitigate | Require explicit target/run and derive one deterministic correlation code instead of selecting existing content. |
| T-13-04 | Repudiation | smoke outcome | medium | mitigate | Stable passed/failed/checkpoint statuses and run timestamp in machine-readable and Markdown artifacts. |
</threat_model>

<verification>
- Run the two task commands above, then `pnpm --filter @starye/config test --run`.
- Inspect the evidence tests for direct-port rejection, secret-shaped data rejection, tuple mismatch, and checkpoint result semantics.
- Before the code commit, run GitNexus `detect_changes` for unstaged code changes and confirm the affected deployment-target exports are expected; do not stage machine-local env files.
</verification>

<success_criteria>
The phase has a passing selected local projection gate and a tested non-secret identity/evidence contract. Later plans have no permission to bypass this gate, use random existing data, write a direct-port canonical URL, or convert a blocked result into a success claim.
</success_criteria>

## Artifacts this phase produces

- Machine-local selected-target projection verified by the canonical check and kept out of Git.
- `data-chain-evidence.ts` with deterministic tuple validation, JSON serialization, and Markdown rendering.
- Focused evidence regression tests covering identity, redaction, URL policy, and status semantics.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-01-SUMMARY.md` when done. Generated run evidence belongs under `.planning/phases/13-full-chain-data-smoke/evidence/` and is not a substitute for the summary.
</output>
