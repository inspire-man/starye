---
phase: 13-full-chain-data-smoke
plan: "70"
type: execute
wave: 64
depends_on: [13-69]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-70-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Exact selected-production handoff authorization
    why: Provider-side mutation requires a new, literal authorization tied to the real fresh p13-69 local terminal run; no historical phrase or workflow invocation grants this authority.
must_haves:
  truths:
    - "Remote work is eligible only when p13-69 records terminal_passed, provesExternalChain true, a deterministic code, and a non-empty local itemId, per D-03 and D-04."
    - "The independent human checkpoint accepts only the literal `授权 remote handoff <actual-new-run-id>` where the value exactly equals the persisted p13-69 run id; before it, no preflight, handoff, remote verifier, production browser, or provider operation runs, per D-05."
    - "After one accepted authorization, exactly one repository-owned `starye-org` remote handoff owns the nested official selected-target preflight and either creates a pending tuple with a non-empty remote itemId or permanently freezes p13-69 remote mode at its first non-success, per D-01, D-02, D-05, D-06, and D-07."
    - "Only a passed nested preflight, runnerInvocations one, the target/run-derived count-one non-R18 item, and non-empty remote itemId make Plan 13-71 eligible for root IAB production observation."
    - "p13-66 and every earlier carrier/evidence tree remain frozen and are neither read as live execution input nor operated by this plan."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-70-SUMMARY.md
      provides: Non-secret p13-69 local eligibility, exact authorization decision, one nested-preflight handoff outcome, and frozen remote branch status.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-69-run-id}/remote.json
      provides: Untracked selected-target remote pending or checkpoint evidence owned by the sole authorized handoff.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-69-run-id}/remote.md
      provides: Untracked canonical Markdown rendering for the sole remote evidence JSON.
  key_links:
    - from: p13-69 terminal local evidence
      to: authorization checkpoint
      via: The persisted run id and terminal fields are checked before the literal authorization phrase is accepted.
      pattern: terminal_passed and provesExternalChain true
    - from: repository-owned remote handoff
      to: Plan 13-71 production IAB eligibility
      via: Its nested preflight, exactly one runner invocation, and remote pending tuple include a non-empty mode-owned itemId.
      pattern: preflightStatus passed and handoffReady true
---

<objective>
Require exact human authorization before one and only one nested-preflight remote handoff for the fresh p13-69 local carrier, producing a pending remote tuple or a durable frozen checkpoint.

Purpose: The live gap is not an invitation to retry p13-66. A fresh terminal local carrier may enter a remote operation only through a separate human decision, and the repository handoff must retain sole ownership of target selection, nested preflight, reservation, fixture invocation, and pending-evidence validation.

Output: `13-70-SUMMARY.md` plus conditional untracked p13-69 remote evidence. This plan does not navigate a production Dashboard or Viewer, and it does not run a standalone remote preflight or remote verifier outside the one repository-owned handoff.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-67-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-69-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-69-RUN-ID.txt
@scripts/data-chain-handoff.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
@packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
</context>

<interfaces>
- `DataChainHandoffResult` is the only handoff result consumed here: `handoffReady` must be true, `preflightStatus` must be passed, `runnerInvocations` must be one, and the returned itemId must be non-empty before any browser plan is eligible.
- `runDataChainHandoffCore` reserves the remote attempt before its nested preflight; a preflight failure writes a redacted checkpoint and starts no fixture runner.
- `inspectDataChainSmokeVerification` is used inside the handoff core to establish pending state. This plan must not issue an additional standalone remote verifier after the handoff.
</interfaces>

## Artifacts This Phase Produces

| Artifact | Owner | Produced state |
| --- | --- | --- |
| `13-70-SUMMARY.md` | Phase 13 tracked planning artifact | Exact authorization, p13-69 eligibility, one remote handoff result, and first-checkpoint freeze facts. |
| `evidence/starye-org/{p13-69-run-id}/remote.json` and `remote.md` | Untracked execution evidence | Pending remote tuple with non-empty itemId only after the nested preflight and sole handoff pass; otherwise checkpoint evidence. |
| `remote.attempt` under the same evidence directory | Untracked reservation marker | Persistent one-owner marker created by the repository handoff, never manually recreated or removed. |

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Validate fresh local proof and obtain the exact remote authorization</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-70-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-69-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
Read only the new p13-69 summary, run-id file, and its recorded post-observation verifier result. Before presenting the checkpoint, require a real p13-69 `terminal_passed` result, `provesExternalChain: true`, the deterministic code, and a non-empty local itemId. Record the authorization decision without sensitive material. Do not run a preflight, handoff, remote verifier, provider command, production browser navigation, or remote evidence inspection while awaiting authorization. The only acceptance string is `授权 remote handoff <actual-new-run-id>`, where the placeholder must be replaced by the actual p13-69 value from the run-id file. `继续`, a GSD command, any historical p13-55/p13-57/p13-60/p13-66 authorization, or a constructed identifier does not satisfy this gate. This task implements D-03, D-04, D-05, and D-06.
  </action>
  <what-built>Plan 13-69 has either supplied the sole eligible fresh local carrier or correctly stopped before remote eligibility.</what-built>
  <how-to-verify>
1. Read `13-69-SUMMARY.md` and `13-69-RUN-ID.txt`.
2. Confirm the persisted p13-69 local outcome is `terminal_passed` with `provesExternalChain: true`, a deterministic item code, and a non-empty local itemId.
3. To authorize the one remote operation, type exactly `授权 remote handoff <actual-new-run-id>` with the real p13-69 run id.
4. Any mismatch, missing local terminal fact, or choice not to authorize leaves the remote path untouched.
  </how-to-verify>
  <resume-signal>Type `授权 remote handoff <actual-new-run-id>` for the proven p13-69 run, or describe the local-proof mismatch.</resume-signal>
  <verify>
    <automated>if (-not (Test-Path -LiteralPath .planning/phases/13-full-chain-data-smoke/13-69-RUN-ID.txt)) { throw 'fresh p13-69 run id is required before authorization can be presented' }</automated>
  </verify>
  <acceptance_criteria>
    - The checkpoint accepts precisely one literal authorization bound to the persisted new p13-69 run id.
    - Before the literal authorization arrives, no remote preflight, handoff, verifier, browser observation, or provider command has run.
    - Historical carrier evidence is not treated as authorization or as current local proof.
  </acceptance_criteria>
  <done>The plan either holds at the independent authorization gate or has the only accepted authority for one p13-69 remote handoff.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the sole authorized nested-preflight remote handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-70-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
  </read_first>
  <action>
Proceed only after Task 1 contains the literal authorization matching the existing p13-69 run id. Before the command, confirm the exact p13-69 remote JSON, Markdown, and attempt marker are all absent. Invoke exactly one repository-owned command: `pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id <actual-p13-69-run-id>`. Do not run `target-profile preflight` separately: the handoff core must own the one nested official selected-target preflight and run no fixture when that preflight is unmet. Do not invoke a standalone remote verifier afterwards; use the handoff result that already owns its pending-evidence inspection. Only `preflightStatus: passed`, `runnerInvocations: 1`, `handoffReady: true`, the deterministic count-one non-R18 tuple, and a non-empty remote itemId permit Plan 13-71. Every authorization, reservation, preflight, handoff, tuple, or internal-verifier non-success permanently freezes p13-69 remote mode without a second preflight, handoff, verifier, browser observation, run allocation, or historical-run reuse. Record only non-secret status categories, code, itemId presence, and authorization result. This task implements D-01, D-02, D-04, D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The only remote handoff is explicitly `starye-org`, is bound to p13-69, and contains its nested official preflight.
    - A remote pending tuple is usable only with a non-empty remote itemId and one runner invocation; all first non-success outcomes are frozen evidence.
    - No production Dashboard or Viewer route is opened in this plan, and no extra remote verifier is issued.
  </acceptance_criteria>
  <done>p13-69 has exactly one remote pending tuple for Plan 13-71 or one permanent remote checkpoint that blocks it.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Human authorization to remote handoff | A user decision permits one stateful selected-target operation for one exact fresh local carrier. |
| Local terminal evidence to provider operation | The remote handoff consumes the new local tuple and performs the nested preflight before fixture work. |
| Provider command to untracked evidence | Remote facts return only through allowlisted status/code/itemId evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-70-01 | Spoofing | Remote authorization | critical | mitigate | Require the literal authorization phrase bound to the persisted p13-69 run id after verifying its terminal local facts. |
| T-13-70-02 | Tampering | Target/run and evidence reservation | high | mitigate | Use explicit `starye-org`, exact fixed-root paths, absent-pair checks, repository-owned attempt reservation, and one first-checkpoint freeze. |
| T-13-70-03 | Elevation of privilege | Provider mutation | critical | mitigate | Permit only the repository handoff to invoke its nested selected-target preflight and fixed fixture runner. |
| T-13-70-04 | Information disclosure | Remote output | high | mitigate | Persist only authorization category, status, deterministic code, and itemId presence; exclude tokens, cookies, raw provider data, and full endpoints. |
| T-13-70-05 | Denial of service | Remote fixture | medium | mitigate | Restrict execution to D-01/D-02 one-item fixture semantics and prohibit retry or broad crawler work. |
</threat_model>

<verification>
- Confirm the fresh p13-69 terminal local fields before the human checkpoint can accept an authorization string.
- Run the focused handoff, remote smoke, and artifact-verification tests without executing another provider command.
- Confirm Plan 13-71 eligibility only from the sole handoff result's passed nested preflight, one runner invocation, and non-empty remote itemId.
</verification>

<success_criteria>
- No remote operation starts before the literal authorization for the actual fresh p13-69 run.
- One authorized repository-owned handoff either creates a tuple-bound remote pending pair with non-empty itemId or freezes p13-69 at its first remote non-success.
- A remote pending pair, not a historical artifact or separate verifier result, is the only possible input to the production IAB plan.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-70-SUMMARY.md` when done.
</output>
