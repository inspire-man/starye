---
phase: 13-full-chain-data-smoke
plan: "64"
type: execute
wave: 58
depends_on: [13-63]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-64-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Explicit selected-production remote handoff authorization
    why: The fresh p13-63 carrier may move to remote mode only after its local verifier is terminal and the operator supplies the exact authorization phrase.
must_haves:
  truths:
    - "p13-64 can run only after p13-63 local verification is terminal_passed with provesExternalChain true and the same p13-63 code/item tuple is present in local evidence."
    - "Remote execution requires the exact user phrase `授权 remote handoff p13-63-<32hex>` for the actual p13-63 run id; no inferred or prior authorization is valid."
    - "One authorized remote handoff either creates one pending remote pair with a non-empty remote itemId or records a permanent non-success checkpoint before mutation; no retry occurs."
    - "p13-60, p13-57, p13-55, and every prior remote tree remain untouched; no production browser observation occurs here."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-64-SUMMARY.md
      provides: Non-secret remote authorization, preflight, handoff, and pending/checkpoint outcome for p13-63.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-63-run-id}/remote.json
      provides: Untracked selected-target remote tuple only when the authorized handoff reaches pending.
---

<objective>
Run at most one explicitly authorized selected-production handoff for a fresh terminal p13-63 local carrier, or retain an honest remote checkpoint.

Output: `13-64-SUMMARY.md` and, only after a successful handoff, untracked p13-63 remote evidence. No production browser work.
</objective>

<context>
@AGENTS.md
@.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/verify-data-chain-smoke.ts
</context>

## Artifacts this phase produces

- `13-64-SUMMARY.md`.
- Conditional untracked `evidence/starye-org/p13-63-*/remote.{attempt,json,md}`.

## Non-goals

- No remote work without the exact p13-63 authorization phrase.
- No production Dashboard or Viewer observation.
- No retry, second handoff, credential logging, or reuse of historical carriers.

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm p13-63 local terminal and obtain exact remote authorization</name>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-63-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
  </read_first>
  <action>
Read the p13-63 summary and exact local verifier output. Continue only when the local result is `terminal_passed`, `provesExternalChain: true`, and the p13-63 run id has the expected 32-hex suffix. Request and record the exact authorization phrase `授权 remote handoff <actual-p13-63-run-id>`. If local proof is missing or the phrase is absent/mismatched, write `13-64-SUMMARY.md` as blocked before any remote command or evidence write.
  </action>
  <acceptance_criteria>
    - The summary names the p13-63 run id, local terminal state, and authorization decision without secrets.
    - No remote command is run on missing, stale, or mismatched authorization.
  </acceptance_criteria>
  <done>Exact authorization for a terminal p13-63 local carrier exists, or remote work stops before invocation.</done>
</task>

<task type="auto">
  <name>Task 2: Run one selected-target remote preflight and handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-64-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/target-profile.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
After Task 1, assert p13-63 has no pre-existing remote evidence. Run the repository-owned selected-target remote preflight for `starye-org`; only a green result permits one `pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id <p13-63>` invocation. Require the resulting remote pair to have the deterministic code, exactly one runner invocation, and non-empty remote itemId before treating it as pending. Any preflight, provider, tuple, or handoff failure writes a permanent checkpoint summary and stops without a second attempt.
  </action>
  <acceptance_criteria>
    - Exactly one remote handoff is invoked only after green preflight and exact authorization.
    - Pending remote evidence has a non-empty itemId and the p13-63 deterministic code, or the result is a non-success checkpoint.
    - No historical evidence tree is read for reuse or modified.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-63-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>A fresh p13-63 remote pair is pending with non-empty itemId, or the only remote attempt is honestly checkpointed.</done>
</task>
</tasks>

## Verification

- Exact authorization precedes remote preflight and handoff.
- Remote pending requires non-empty itemId and tuple match.
- Any remote failure is permanent for the carrier and blocks 13-65.

## Self-check

- Supersedes only the p13-60 binding of 13-61; it does not reopen that frozen run.
