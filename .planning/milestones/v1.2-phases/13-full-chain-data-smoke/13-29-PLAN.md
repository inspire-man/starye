---
phase: 13-full-chain-data-smoke
plan: "29"
type: execute
wave: 31
depends_on: [13-18, 13-28]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.attempt"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md"
  - ".planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md"
autonomous: false
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Authorized starye-org selected-target remote context
    why: One provider-backed remote fixture requires explicit operator authorization plus Cloudflare/crawler credentials after live ownership checks.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: Authorized selected-target operator process (never commit)
      - name: CLOUDFLARE_ACCOUNT_ID
        source: Authorized selected-target operator process (never commit)
      - name: CRAWLER_SECRET
        source: Authorized selected-target operator process (never commit)
must_haves:
  truths:
    - "D-03: Local terminal prerequisite is current — 13-28-SUMMARY.md publishes runtime_eligibility: released, runtime_lifecycle: cleaned, post_cleanup_fixed_ports: all_free; 13-17-SUMMARY.md records run_id p13-17-[0-9a-f]{32} with handoffReady true; 13-18-SUMMARY.md records terminal_passed and provesExternalChain true for that same run. Never use 13-25 as lifecycle gate."
    - "Before any remote handoff, human authorization is recorded for selected-production provider/D1/API mutation on target starye-org for the extracted run_id only."
    - "Exact local verifier for that run_id returns raw exit 0, terminal_passed, provesExternalChain true, shared target/run/code before remote work."
    - "Pre-handoff Test-Path -LiteralPath for remote.json, remote.md, and remote.attempt are all false; any peer presence is a read-only terminal stop with zero provider/handoff calls."
    - "D-05: After gates pass, invoke pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id $runId exactly once. Handoff owns exclusive remote.attempt reservation, official read-only live preflight, then either redacted pre_ingest/remote_preflight/target_preflight_unmet checkpoint (itemId null, runnerInvocations 0) or the sole remote runner with one fixture and one remote D1 row."
    - "D-01/D-02/D-04: Remote reuses local target/run/code, establishes mode-owned non-empty remote itemId independently, and requires selected-canonical API proof matching remote D1 code/id before any production browser work."
    - "remote.attempt remains zero-byte ownership history only; pair-focused verifier never treats the marker as success. Evidence stays untracked and is never committed."
    - "No ad hoc Wrangler/SQL/deploy/DNS/migration/cleanup command supplements the fixed handoff path. Historical 13-09/13-10/13-24/13-25 evidence trees stay immutable and unread for mutation."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.attempt"
      provides: "Zero-byte exclusive remote attempt marker; untracked; never success evidence"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json"
      provides: "Provider-backed remote pending tuple or immutable remote checkpoint"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md"
      provides: "Deterministic remote Markdown peer"
    - path: ".planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md"
      provides: "Authorization gate, local prerequisite, one handoff result, pending or checkpoint verdict for 13-30"
  key_links:
    - from: "13-18 terminal local pair"
      to: "sole remote handoff"
      via: "exact local verifier + three-path absence + human auth"
      pattern: "terminal_passed then one remote handoff"
    - from: "remote handoff"
      to: "remote.json/md pending"
      via: "official preflight + one fixture + D1/API receipts"
      pattern: "resolved_pending_observation or closed checkpoint"
---

<objective>
Close the selected-production provider-backed proof gap that stale 13-19 left unexecuted: consume the live p13-17 local terminal evidence (from 13-17/13-18 after 13-28 lifecycle release), obtain explicit human authorization, then run exactly one remote data-chain handoff for the same target/run/code tuple.

Purpose: Satisfy DATA-03/04/07 and TEST-05 for the production mode without replaying local cold-start, 13-17, or 13-18.
Output: Untracked remote pair (or honest checkpoint) plus 13-29-SUMMARY.md. Does not claim Dashboard/viewer finalization or Phase 13 completion.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-19-PLAN.md
</context>

## Artifacts this phase produces
- `13-29-SUMMARY.md` (tracked Summary only)
- Untracked evidence under `evidence/starye-org/<run_id>/`: `remote.attempt`, `remote.json`, `remote.md`
- Explicit human authorization record inside the Summary (no secrets)

## Supersedes / sequencing note
Historical plan `13-19-PLAN.md` remains on disk as the original remote-handoff contract reference. This plan **does not rewrite** 13-19. Executors must prefer **13-29** for the remaining production gap because 13-19 still gates on immutable `13-25-SUMMARY.md` release fields that never published. Consume live `13-28` lifecycle + `13-17`/`13-18` terminal local evidence instead. Do not reopen blocked 13-24/13-25 trees.

## One-item smoke contract (locked)
- `DATA_CHAIN_FIXTURE_COUNT` remains one; single non-R18 fixture; no multi-item expansion (D-01/D-02).
- Shared target `starye-org`, shared `run_id` from 13-17, shared deterministic item code; mode-owned remote itemId.
- Canonical verification URLs stay Gateway-relative; never promote 3000/3001/3002/3003/5173 as evidence URLs (D-03).

## Dirty worktree / GitNexus
- Do not revert, stage, or commit unrelated dirty files.
- Evidence under `evidence/` must remain untracked and excluded from commits.
- If any production code symbol must be edited (unexpected), run GitNexus impact analysis first; HIGH/CRITICAL stops with user warning. Prefer zero code changes.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Authorize selected-production remote mutation</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
  </read_first>
  <what-built>
    Local chain is already closed: 13-28 released/cleaned/all_free; 13-17 allocated p13-17-b807d44deee04e1f85b42fd02ed8cf26 with non-empty local itemId and resolved_pending_observation; 13-18 recorded ordered Dashboard→Viewer and exact verifier terminal_passed with provesExternalChain true. No remote work has run.
  </what-built>
  <how-to-verify>
    1. Confirm you authorize **one** selected-production remote data-chain handoff for target `starye-org` on the exact run_id extracted from 13-17-SUMMARY.md (currently p13-17-b807d44deee04e1f85b42fd02ed8cf26 unless Summary changes under the same contract).
    2. Confirm operator process already holds non-committed CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, and CRAWLER_SECRET for that target (do not paste secrets).
    3. Confirm you understand local terminal proof is **not** remote completion, and that a preflight failure must persist an honest checkpoint without fabricating success.
    4. Reply with explicit approval phrase, for example: `approved remote handoff for starye-org p13-17`.
  </how-to-verify>
  <resume-signal>Type approved remote handoff for starye-org plus the run_id, or describe refusal/deferral</resume-signal>
  <action>
    Present the live local prerequisite facts from 13-28/13-17/13-18 to the human. Do not invoke any provider, handoff, preflight, Wrangler, deploy, D1, API, browser, or evidence-write command in this task. Record the human authorization phrase (or refusal) into the draft 13-29-SUMMARY notes only after approval is received. On refusal or deferral, stop the plan with no remote work and leave 13-19/13-20 historical files untouched.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md')) { throw '13-17-SUMMARY missing' }; if (-not (Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md')) { throw '13-18-SUMMARY missing' }; if (-not (Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md')) { throw '13-28-SUMMARY missing' }; $rootCandidates = Get-ChildItem -Directory '.planning/phases/13-full-chain-data-smoke/evidence/starye-org' -ErrorAction SilentlyContinue | Where-Object { $_.Name -like 'p13-17-*' }; foreach ($d in $rootCandidates) { foreach ($n in @('remote.json','remote.md','remote.attempt')) { if (Test-Path -LiteralPath (Join-Path $d.FullName $n)) { throw \"unexpected remote peer before auth: $($d.Name)/$n\" } } }; Write-Output 'pre-auth inventory clean'"</automated>
  </verify>
  <done>Human authorization for one remote handoff is recorded; no provider command has run yet.</done>
</task>

<task type="auto">
  <name>Task 2: Gate local terminal pair and three-path remote absence</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
  </read_first>
  <action>
    After Task 1 approval only. Fail closed before any handoff or provider call:

    1. Machine-read `13-28-SUMMARY.md` and require exact fields `runtime_eligibility: released`, `runtime_lifecycle: cleaned`, `post_cleanup_fixed_ports: all_free`. Never use `13-25-SUMMARY.md` as lifecycle gate (historical blocked_after_launch).
    2. Machine-read `13-17-SUMMARY.md` once; require exactly one standalone `run_id: p13-17-[0-9a-f]{32}` match; assign `$runId`; reject zero/multiple/malformed matches. Derive all paths only from fixed evidence root `.planning/phases/13-full-chain-data-smoke/evidence/starye-org/`, target `starye-org`, and `$runId`. Never hardcode a path or UUID outside that binding.
    3. Machine-read `13-18-SUMMARY.md` and require the same `$runId`, `terminal_passed` / exact verifier exit 0 narrative, and `provesExternalChain: true` (or equivalent explicit pass language). Local-only MVP is prerequisite, not remote proof.
    4. Run exact local verifier once:
       `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $runId`
       Require raw exit 0, terminal_passed, provesExternalChain true, shared target/run/code. Do not rerun handoff or local runner.
    5. Resolve expected remote LiteralPaths once (`remote.json`, `remote.md`, `remote.attempt`). Require all three `Test-Path -LiteralPath` false. Any true result is a read-only terminal stop: no Summary provider claim, no alternate run id, zero handoff/preflight/provider/fixture/D1/API/browser invocations.
    6. Confirm one-item contract remains active in current smoke scripts (`DATA_CHAIN_FIXTURE_COUNT` / single fixture). Do not expand fixtures.
    7. Do not touch unrelated dirty files. Do not commit evidence.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$s28 = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md'; if ($s28 -notmatch 'runtime_eligibility:\s*released' -or $s28 -notmatch 'runtime_lifecycle:\s*cleaned' -or $s28 -notmatch 'post_cleanup_fixed_ports:\s*all_free') { throw '13-28 lifecycle gate failed' }; $s17 = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md'; $m = @([regex]::Matches($s17, '(?m)^run_id:\s*(p13-17-[0-9a-f]{32})\s*$')); if ($m.Count -ne 1) { throw 'Expected exactly one run_id in 13-17' }; $runId = $m[0].Groups[1].Value; $root = Join-Path '.planning/phases/13-full-chain-data-smoke/evidence/starye-org' $runId; foreach ($n in @('remote.json','remote.md','remote.attempt')) { if (Test-Path -LiteralPath (Join-Path $root $n)) { throw \"remote peer already exists: $n\" } }; pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $runId; if ($LASTEXITCODE -ne 0) { throw \"local verify exit $LASTEXITCODE\" }"</automated>
  </verify>
  <done>Lifecycle, terminal local pair, and remote three-path absence gates all pass; remote handoff still not invoked.</done>
</task>

<task type="auto">
  <name>Task 3: Invoke one remote handoff and persist Summary</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.attempt, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md, .planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-19-PLAN.md
    - scripts/data-chain-handoff.ts
    - packages/config/src/deployment-target/data-chain-smoke.ts
    - packages/config/src/deployment-target/preflight.ts
  </read_first>
  <action>
    Only after Tasks 1–2 succeed. Re-bind `$runId` from 13-17-SUMMARY.md exactly as in Task 2.

    Invoke exactly once:
    `pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id $runId`

    Do **not** separately run `pnpm target-profile preflight`, remote `smoke:data-chain`, Wrangler, prepared-entry, crawler CLI, deploy, DNS, migration, rollback, or ad hoc D1 SQL. The handoff owns: pair-state revalidation, exclusive-create zero-byte `remote.attempt`, official selected-target read-only live preflight once, then either redacted checkpoint pair or the sole remote runner.

    Acceptable closed outcomes (record honestly in Summary):
    - **Pending success path:** handoffReady true; remote pair `resolved_pending_observation` / aggregate pending; non-empty remote itemId; D1 count one for the shared item code; selected-canonical API receipt matches remote D1 code/id; `remote.attempt` still zero-byte and untracked. This authorizes 13-30 only.
    - **Preflight checkpoint path:** `pre_ingest` / observation `remote_preflight` / checkpoint `target_preflight_unmet`; itemId null; runnerInvocations 0; binary handoff exit 1; marker remains; permanent for this run. Summary records checkpoint; do not invent success; do not start 13-30 browser work.
    - **Other fail-closed checkpoint:** pair-verify and stop; never synthesize terminal_passed.

    After the handoff returns, artifact-verify the remote pair once with:
    `pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId`
    - Pending path expects raw exit 2 with pending tuple.
    - Checkpoint path expects non-success and never claims production browser readiness.

    Write `13-29-SUMMARY.md` with: human authorization reference, 13-28/13-17/13-18 prerequisites consumed, `$runId`, handoff command once, preflight/runner invocation counts if exposed, remote pending or checkpoint verdict, evidence paths, SHA-256 sizes if available, explicit scope boundary (no production Dashboard/viewer claim, no Phase 13 completion). Keep all evidence untracked. Commit only the Summary after GitNexus detect-changes if any tracked file changes; default is Summary-only docs commit. Do not amend historical Summaries or rewrite 13-19.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$s17 = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md'; $m = @([regex]::Matches($s17, '(?m)^run_id:\s*(p13-17-[0-9a-f]{32})\s*$')); if ($m.Count -ne 1) { throw 'run_id bind failed' }; $runId = $m[0].Groups[1].Value; $root = Join-Path '.planning/phases/13-full-chain-data-smoke/evidence/starye-org' $runId; if (-not (Test-Path -LiteralPath (Join-Path $root 'remote.attempt'))) { throw 'remote.attempt missing' }; if ((Get-Item -LiteralPath (Join-Path $root 'remote.attempt')).Length -ne 0) { throw 'remote.attempt must be zero-byte' }; if (-not (Test-Path -LiteralPath (Join-Path $root 'remote.json'))) { throw 'remote.json missing' }; if (-not (Test-Path -LiteralPath (Join-Path $root 'remote.md'))) { throw 'remote.md missing' }; if (-not (Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md')) { throw '13-29-SUMMARY missing' }; pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId; exit 0"</automated>
  </verify>
  <done>Remote handoff completed once for the p13-17 tuple with either pending provider-backed pair authorizing 13-30 or an immutable honest remote checkpoint; Summary exists; evidence untracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human operator → remote handoff | Explicit authorization required before any provider mutation |
| Local terminal artifact → remote handoff | Exact verifier and three-path absence gate |
| Official preflight → fixture/D1/API | Fail closed on unmet; checkpoint only |
| Remote evidence → browser (13-30) | Only pending pair may authorize production IAB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-29-01 | Elevation of Privilege | remote handoff | critical | mitigate | Blocking human authorization before any remote command; no ambient deploy |
| T-13-29-02 | Spoofing | run identity | critical | mitigate | Bind run_id solely from 13-17-SUMMARY.md regex; shared target/run/code only |
| T-13-29-03 | Tampering | remote.attempt | high | mitigate | Zero-byte exclusive create; never success evidence; untracked |
| T-13-29-04 | Information Disclosure | credentials/evidence | critical | mitigate | No secret persistence in Summary; redacted checkpoints only |
| T-13-29-05 | Repudiation | remote outcome | high | mitigate | Single handoff invocation + pair verify + Summary counts |
| T-13-29-SC | Tampering | package installs | low | accept | No install/dependency action in this plan |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Selected target usable end to end | 13-29 + 13-30 + 13-31 | COVERED | Remote provider slice here |
| REQ | DATA-03 | One fixture identity | Task 3 | COVERED | One-item remote fixture |
| REQ | DATA-04 | D1/API/admin both modes | Task 3 | COVERED | Remote D1/API receipts |
| REQ | DATA-07 | Local/remote artifacts | Task 3 | COVERED | remote.json/md |
| REQ | TEST-05 | Repeatable smoke output | Task 3 | COVERED | Exact handoff + verify |
| CONTEXT | D-01 | One target/run/code mode ids | Task 2-3 | COVERED | Shared tuple |
| CONTEXT | D-02 | Bounded fixture | Task 3 | COVERED | Single fixture |
| CONTEXT | D-03 | Local before remote | Task 2 | COVERED | 13-28/17/18 + local verify |
| CONTEXT | D-05 | Explicit provider live checks | Task 3 | COVERED | Handoff-owned preflight |
| VERIFICATION | G-prod-provider | Provider D1/API/admin absent | Tasks 1-3 | COVERED | Remaining failed truth |
| VERIFICATION | G-local-pair | Fresh local pair | — | EXCLUDED | Closed by 13-17/13-18 live |
</source_audit>

<verification>
- Human auth recorded before remote work
- Local exact verifier raw 0 for bound run_id
- One remote handoff only; three-path absence pre-checked
- remote.attempt zero-byte untracked; pair exists pending or checkpoint
- No Phase completion claim
</verification>

<success_criteria>
- Selected-production provider-backed proof exists for the same p13-17 target/run/code, or an immutable honest remote checkpoint is recorded without success synthesis
- 13-29-SUMMARY.md is the only intended tracked artifact
- 13-30 may proceed only on remote pending (raw verify exit 2), never on checkpoint
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md` when done
</output>
