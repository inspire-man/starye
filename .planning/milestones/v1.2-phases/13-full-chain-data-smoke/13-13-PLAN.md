---
phase: 13-full-chain-data-smoke
plan: "13"
type: execute
wave: 12
depends_on: [13-12]
files_modified:
  - "scripts/gateway-readiness.ts"
  - "scripts/check-services.ps1"
  - "scripts/data-chain-smoke.ts"
  - "scripts/tsconfig.phase13-readiness.json"
  - "packages/config/src/deployment-target/__tests__/gateway-readiness.test.ts"
  - "packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts"
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "D-03: local readiness requires bounded HTTP responses from the canonical Gateway, so a LISTENING 8080 socket with no response headers is non-ready and cannot authorize run allocation."
    - "D-03: canonical readiness proves /robots.txt, /auth, and /auth/ through http://localhost:8080; direct implementation ports remain diagnostic inputs only and never become a success URL."
    - "D-06: readiness emits one closed non-secret machine record, and checkServicesDefault fails closed on missing, duplicate, malformed, incompatible, or unhealthy output regardless of listener text or the outer process code."
    - "The default Gateway auth transport has an explicit ten-second ceiling, clears its timer, and cancels any unconsumed response body before returning."
    - "No smoke run, evidence write, browser action, provider call, local service restart, schema action, or migration occurs in this plan."
  artifacts:
    - path: "scripts/gateway-readiness.ts"
      provides: "Import-safe bounded canonical Gateway probe and closed machine result"
      exports: ["GatewayAuthProbeResult", "GatewayReadinessResult", "observeCanonicalGatewayAuth", "probeCanonicalGatewayReadiness", "runGatewayReadinessCli"]
    - path: "scripts/check-services.ps1"
      provides: "Listener diagnostics plus authoritative canonical HTTP readiness and explicit process status"
    - path: "scripts/data-chain-smoke.ts"
      provides: "Strict service-readiness machine parser and bounded default Gateway-auth dependency"
      exports: ["checkServicesDefault", "observeGatewayAuthDefault", "validGatewayAuth"]
    - path: "scripts/tsconfig.phase13-readiness.json"
      provides: "Narrow root-script TypeScript project for the Phase 13 readiness/data-chain owners"
    - path: "packages/config/src/deployment-target/__tests__/gateway-readiness.test.ts"
      provides: "No-header listener, canonical redirect, timeout, cleanup, and redaction regressions"
  key_links:
    - from: "scripts/check-services.ps1"
      to: "scripts/gateway-readiness.ts:runGatewayReadinessCli"
      via: "fixed package-local node/tsx invocation with no caller-provided origin"
      pattern: "starye-gateway-readiness-1"
    - from: "scripts/data-chain-smoke.ts:checkServicesDefault"
      to: "scripts/check-services.ps1"
      via: "unique parsed machine record plus explicit exit status"
      pattern: "starye-local-services-1"
    - from: "scripts/data-chain-smoke.ts:observeGatewayAuthDefault"
      to: "scripts/gateway-readiness.ts:observeCanonicalGatewayAuth"
      via: "fixed canonical URL, manual redirect, bounded AbortController, and response cleanup"
      pattern: "gateway_auth_timeout"
---

<objective>
Replace the false listener-only readiness gate with a bounded canonical HTTP contract and give the default Gateway-auth observation a deterministic lifetime (D-03, D-06) while preserving the historical p13-12 classification as unknowable.

Purpose: the current Gateway owns 127.0.0.1:8080 but serves no headers, while direct Auth is responsive. `check-services.ps1` and `checkServicesDefault` currently call that state healthy, and the unbounded auth fetch can consume the same 300000 ms as the outer runner deadline.

Output: a tested canonical Gateway probe, one versioned non-secret service-readiness machine record, strict default wiring in the local runner, and a timeout materially below the outer command budget. This plan performs no run allocation or live data-chain execution.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/debug/phase13-gateway-auth-timeout.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md
@scripts/check-services.ps1
@scripts/data-chain-smoke.ts
@scripts/package-manager-command.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts

<interfaces>
- Existing `LocalCommandResult`: `{ exitCode: number; stdout: string; stderr: string }`.
- Existing `GatewayAuthResponse`: `{ status: number; location?: string | null }`; delete this legacy interface and replace every former type use with the closed `GatewayAuthProbeResult` without retaining exception text.
- Existing `checkServicesDefault(): Promise<LocalCommandResult>` invokes root `pnpm check:services`.
- Existing `observeGatewayAuthDefault(): Promise<GatewayAuthResponse>` fetches `http://localhost:8080/auth/` with manual redirects and no timeout.
- Existing `validGatewayAuth(response): boolean` accepts 2xx or a same-origin redirect whose pathname begins `/auth/`.
</interfaces>
</context>

<discovery_evidence>
- Discovery level: 0. The diagnosis and repair boundary are established, no dependency is added, and standard Node fetch/AbortController plus PowerShell are already in the repository toolchain.
- Current read-only probes prove only the current no-header Gateway state. They do not recover whether p13-12 observed timeout/fetch, status, or redirect failure; tests and new artifacts must never rewrite that history.
- Fresh GitNexus impact after reindex: `checkServicesDefault` LOW (0 mapped callers), `observeGatewayAuthDefault` LOW, `validGatewayAuth` LOW (1 direct caller, `runDataChainSmoke`), and `runDataChainSmoke` LOW (1 direct caller). The legacy `GatewayAuthResponse` interface is also scheduled for deletion and must be included in the pre-edit upstream impact/report rerun. Re-run every named impact before editing because callback/type wiring can be underrepresented by the graph.
- No package install exists, so package legitimacy audit and install checkpoint are N/A.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Order |
|---|---|---|---|
| 1 | Fixed canonical local origin and debug minimal-regression contract | Import-safe bounded HTTP probe plus tests | First |
| 2 | Task 1 machine type and CLI | PowerShell/service-runner wiring that fails closed | Second |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the bounded canonical Gateway readiness probe</name>
  <rationale>D-03 fixes readiness at the canonical Gateway and keeps direct ports diagnostic-only; D-06 keeps the machine record closed/non-secret. D-01/D-02/D-04/D-05/D-07 stay downstream and are not simulated.</rationale>
  <files>scripts/gateway-readiness.ts, packages/config/src/deployment-target/__tests__/gateway-readiness.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/debug/phase13-gateway-auth-timeout.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - scripts/data-chain-smoke.ts
    - packages/config/src/deployment-target/index.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  </read_first>
  <behavior>
    - A TCP server that accepts a request but emits no headers produces `timeout` before an injected short test deadline and overall readiness is false.
    - `/robots.txt` must return 200; `/auth` must return 301 to the same Gateway origin at `/auth/`; `/auth/` accepts 2xx or 301/302/303/307/308 to the same origin under `/auth/`.
    - Missing, external-origin, or direct-port auth redirects are `redirect_invalid`; disallowed response status is `http_status_unaccepted`; a non-timeout transport exception is `fetch_failed`.
    - Every header-bearing result cancels an unconsumed response body, every branch clears its timeout timer, and error messages/stacks/headers/body content are absent from the returned machine type.
    - The production CLI has no base-origin option and always probes `http://localhost:8080`; tests may inject an ephemeral loopback origin only through the exported function dependency.
  </behavior>
  <action>Create import-safe `scripts/gateway-readiness.ts` with the exact exported names in `must_haves.artifacts`. Use the existing `LOCAL_GATEWAY_ORIGIN` owner rather than parsing or accepting a caller origin. Define closed discriminated outcomes for accepted, timeout, fetch failure, unacceptable status, and invalid redirect; keep numeric status and redirect URL internal to validation and do not serialize exception detail or full upstream data. Set the production timeout to 10000 ms, well below the 300000 ms outer failure mechanism. Implement an explicit AbortController/timer lifecycle and cancel `response.body` when it was not consumed. `probeCanonicalGatewayReadiness` must check `/robots.txt`, `/auth`, and `/auth/` in a deterministic order and return `schema: 'starye-gateway-readiness-1'`, `healthy`, and closed per-route outcomes. `runGatewayReadinessCli` logs exactly one JSON machine line and returns only 0 or 1. Write tests first using `node:http` listeners on ephemeral loopback ports: include a socket that sends no headers, valid direct/redirect responses, invalid redirects, thrown fetch, body-cancel tracking, and timer cleanup. Test-only injected origins must not appear in a success artifact or production CLI contract. Decision trace: D-03 fixes readiness at the canonical Gateway and keeps direct ports diagnostic-only; D-06 keeps the machine record closed and non-secret; D-01, D-02, D-04, D-05, and D-07 remain downstream execution boundaries and are not simulated here. Immediately before every RED, GREEN, REFACTOR, or task commit emitted by execute-plan, run `gitnexus_detect_changes` against the exact Task 1 paths, compare the changed symbols/flows with only the new Gateway-readiness probe/CLI and its focused tests, and record the comparison. Any unexpected symbol/flow or HIGH/CRITICAL expansion stops before commit and requires a root-user warning; rerun the gate after any hook/autofix/restage before retrying a commit. Do not inspect or edit any historical evidence.</action>
  <acceptance_criteria>
    - LISTENING without response headers is rejected within the injected deadline; it can never be represented as healthy.
    - The three canonical route contracts are independently asserted and overall healthy requires all three.
    - Auth transport outcomes are closed and non-secret; no exception message, stack, headers, body, cookie, token, upstream origin, or direct app-port URL is serialized.
    - The ten-second production timeout, timer cleanup, body cancellation, and cleanup of every ephemeral test listener/socket have focused tests.
    - The CLI accepts no alternate base/origin and returns binary readiness status with exactly one versioned JSON line.
    - Every RED/GREEN/REFACTOR or task commit is immediately preceded by a fresh GitNexus detect-changes comparison limited to the planned Gateway-readiness symbols/tests; unexpected or HIGH/CRITICAL expansion blocks that commit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/gateway-readiness.test.ts</automated>
  </verify>
  <done>The repository has a deterministic, bounded, canonical HTTP readiness owner that rejects a listener with no headers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make check-services and the local runner trust the machine contract</name>
  <rationale>D-03 makes canonical HTTP authoritative over listener text; D-06 requires a single versioned fail-closed record. D-01/D-02/D-04/D-05/D-07 are preserved for later fixture, identity, remote-preflight, and provider-proof plans.</rationale>
  <files>scripts/check-services.ps1, scripts/data-chain-smoke.ts, scripts/tsconfig.phase13-readiness.json, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts</files>
  <read_first>
    - AGENTS.md
    - .claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md
    - .planning/debug/phase13-gateway-auth-timeout.md
    - scripts/check-services.ps1
    - scripts/gateway-readiness.ts
    - scripts/data-chain-smoke.ts
    - scripts/package-manager-command.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  </read_first>
  <behavior>
    - Listener rows may all be present while Gateway HTTP is unhealthy; `check-services.ps1` then emits `healthy:false` and exits nonzero.
    - `checkServicesDefault` rejects missing, duplicate, malformed, wrong-schema, and `healthy:false` machine lines even if human text says `[OK]` or the child reports zero.
    - A valid `starye-local-services-1` record with all required listeners and healthy Gateway HTTP is accepted.
    - A service-readiness failure creates one pre-ingest checkpoint pair and fixture, D1 snapshot, Gateway API, browser, and provider dependencies remain uncalled.
    - The default auth observer delegates to Task 1's bounded canonical probe and `validGatewayAuth` accepts only its closed accepted result; tests may inject only `gatewayAuthFetch` and `gatewayAuthTimeoutMs` through typed dependencies.
  </behavior>
  <action>Before editing, run GitNexus upstream impact for `GatewayAuthResponse`, `checkServicesDefault`, `observeGatewayAuthDefault`, `validGatewayAuth`, `runDataChainSmoke`, and the existing `DataChainSmokeDependencies` interface owner whose `gatewayAuthFetch` and `gatewayAuthTimeoutMs` fields will change; report direct callers, affected processes, and risk for every owner, including the legacy type scheduled for deletion. If any result becomes HIGH or CRITICAL, explicitly warn the root user and pause before the edit. Update `scripts/check-services.ps1` so listener checks remain diagnostics but are no longer sufficient. Invoke the fixed `scripts/gateway-readiness.ts` CLI through the existing package-local node/tsx pattern, parse its single machine line, and emit one combined `schema: 'starye-local-services-1'` JSON record with `healthy`, the fixed listener names/ports/listening booleans, and the closed Gateway outcomes. Explicitly exit 1 for a missing listener, probe failure, malformed child output, or unhealthy result; exit 0 only for a healthy combined record. Do not accept a caller-provided canonical origin. Export and parameterize `checkServicesDefault` only enough for tests to inject the command executor; parse exactly one combined machine record and normalize any incompatibility to a failed `LocalCommandResult`. Delete the legacy `GatewayAuthResponse` interface and replace every former return, dependency, and local-variable type use with Task 1's `GatewayAuthProbeResult`; update `observeGatewayAuthDefault` and `validGatewayAuth` to consume that closed result. Add narrowly typed `DataChainSmokeDependencies.gatewayAuthFetch` and `gatewayAuthTimeoutMs` fields solely to feed the same default observer in tests; production defaults remain global fetch and 10000 ms, and neither value is a CLI/evidence field. Leave evidence checkpoint mapping for Plan 13-15. Extend local runner tests so listener-looking text cannot override unhealthy machine JSON and every downstream call count stays zero. Create `scripts/tsconfig.phase13-readiness.json` with `extends: ../tsconfig.json`, `compilerOptions.noEmit: true`, `compilerOptions.composite: false`, `compilerOptions.allowImportingTsExtensions: true`, and an explicit `files` list containing only `gateway-readiness.ts` and `data-chain-smoke.ts`; transitive helper imports are type-checked normally. Do not use the broad root `tsconfig.json` default file discovery or treat `packages/config/tsconfig.json` as coverage for root scripts. Decision trace: D-03 keeps local proof on Gateway; D-06 keeps parser failure closed; D-01/D-02/D-04/D-05/D-07 are preserved for later fixture, identity, remote-preflight, and provider-proof plans. Immediately before every RED, GREEN, REFACTOR, or task commit, run `gitnexus_detect_changes` for the exact owned paths and compare against only the expected local-readiness/service-parser/Gateway-auth smoke symbols and flows. Unexpected or HIGH/CRITICAL expansion stops before commit and requires a root-user warning; rerun after any hook/autofix/restage before retry.</action>
  <acceptance_criteria>
    - `pnpm check:services` has an explicit nonzero unhealthy status and exactly one authoritative combined machine record.
    - `checkServicesDefault` uses machine schema/state rather than `[OK]`/`[!!]` text as the decision source.
    - Local runner service failure persists only one non-success observation pair and performs zero fixture/API/browser/provider work.
    - Default auth observation is bounded and resource-clean; direct-port observations remain diagnostics only.
    - GitNexus impact is recorded before edits, and every RED/GREEN/REFACTOR or task commit is immediately preceded by a fresh detect-changes comparison with no unrelated or HIGH/CRITICAL symbol/flow expansion.
    - `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-readiness.json` exits 0 with no diagnostics and covers both modified root TypeScript scripts that package-local config type-check omits.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
    <automated>pnpm --filter @starye/config type-check</automated>
    <automated>pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-readiness.json</automated>
    <automated>git diff --check -- scripts/gateway-readiness.ts scripts/check-services.ps1 scripts/data-chain-smoke.ts packages/config/src/deployment-target/__tests__/gateway-readiness.test.ts packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
  </verify>
  <done>The listener-only false readiness path is closed, and the local runner cannot wait 300 seconds at its default Gateway-auth boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| TCP listener state -> application readiness | A bound socket is untrusted until canonical HTTP routes emit acceptable headers within the bound. |
| Gateway response -> local runner | Status/redirect/transport information is reduced to a closed non-secret result and response resources are released. |
| PowerShell human output -> TypeScript gate | Human text is non-authoritative; one versioned JSON object and explicit exit status decide readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-62 | Spoofing | listener-only service check | high | mitigate | Require all three fixed canonical HTTP routes and reject no-header listeners. |
| T-13-63 | Denial of Service | auth fetch | high | mitigate | Ten-second AbortController, timer cleanup, body cancellation, and process regression in Plan 13-15. |
| T-13-64 | Information Disclosure | readiness machine output | medium | mitigate | Closed schema and outcomes only; no errors, headers, bodies, credentials, or remote endpoints. |
| T-13-65 | Tampering | machine parser | medium | mitigate | Require one exact schema line, fixed listener set, healthy state, and binary child status. |
| T-13-SC | Tampering | package supply chain | low | accept | No package install, dependency, or lockfile change occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Prove the selected target through Gateway/canonical domain. | 13-13 repairs the gate; 13-17 through 13-20 produce proof. | COVERED | Repair is necessary but not proof. |
| REQ | DATA-01 | Local API/auth/dashboard/content use localhost:8080. | 13-13, 13-16 through 13-18 | COVERED | Direct ports are diagnostics only. |
| REQ | DATA-02..DATA-06 | D1, fixture, admin, and viewer chain. | 13-17 through 13-20 | COVERED | No data mutation belongs in this plan. |
| REQ | DATA-07 | Repeatable non-secret evidence. | 13-13 machine boundary; 13-14/13-15; 13-17 through 13-20 | COVERED | Readiness output remains outside evidence. |
| REQ | TEST-05 | Repeatable local/remote smoke outputs. | Tasks 1-2 plus 13-14/13-15 | COVERED | No-header regression is explicit. |
| RESEARCH | - | Gateway-first, local-first, no new dependency. | Tasks 1-2 | COVERED | RESEARCH preserves the historical 10-item draft only as explicitly SUPERSEDED; current CONTEXT D-01 is exactly one item. |
| CONTEXT | D-01/D-02 | One bounded fixture only. | Preserved here; executed 13-17/13-19 | COVERED | Zero fixture calls in this plan. |
| CONTEXT | D-03 | Local HTTP/D1 proof precedes remote. | Tasks 1-2 and 13-16/13-17 | COVERED | This plan closes the false readiness gate. |
| CONTEXT | D-04/D-05 | Exact tuple and explicit remote preflight. | 13-18 through 13-20 | COVERED | Unchanged here. |
| CONTEXT | D-06 | Closed non-secret output. | Tasks 1-2 and 13-14/13-15 | COVERED | No free-form diagnostics. |
| CONTEXT | D-07 | Real provider proof remains Phase 13. | 13-19/13-20 | COVERED | No documentation substitution. |
| VERIFICATION | G-1 | Fresh local path cannot pass current Gateway auth. | 13-13, 13-15, 13-16, 13-17/13-18 | COVERED | Historical p13-12 details remain uncertain. |
| VERIFICATION | G-2/G-3 | Production D1/API/admin/browser proof absent. | 13-19/13-20 | COVERED | Ordered after repair. |
| VERIFICATION | G-4 | Pending wrapper exit mismatch. | 13-14 | COVERED | Independent parallel repair. |

Phase 14 source-literal cleanup, RUNBOOK stabilization, and final matrix are explicitly excluded, as are broader crawler corpus and multi-target automation.
</source_audit>

<verification>
- Run the two focused Vitest files, config type-check, and exact diff check.
- Do not require the currently wedged live Gateway to pass during this code-contract plan; Plan 13-16 owns the clean supervised runtime proof.
- Before each source commit, run GitNexus detect-changes and exclude every evidence path and unrelated worktree path.
- Do not allocate a run id, invoke smoke mode, restart services, or use a browser/provider command.
- Schema/migrations: N/A. No schema file, migration, D1 schema push, table/index operation, or provider resource change is authorized.
</verification>

<success_criteria>
- A listening-but-no-header Gateway deterministically fails canonical readiness within the configured bound.
- The production probe accepts only the fixed local Gateway route contracts and cleans every timer/response resource.
- PowerShell and TypeScript share one strict machine decision; listener text alone cannot authorize local smoke.
- Focused tests, type-check, GitNexus impact/detect-changes, and diff check pass without evidence or external mutation.
</success_criteria>

## Artifacts this phase produces

- **New symbols:** `GatewayAuthProbeResult`, `GatewayReadinessResult`, `observeCanonicalGatewayAuth`, `probeCanonicalGatewayReadiness`, and `runGatewayReadinessCli` in `scripts/gateway-readiness.ts`.
- **Deleted symbol:** `scripts/data-chain-smoke.ts:GatewayAuthResponse`; every former type use is replaced by `GatewayAuthProbeResult`.
- **Modified symbols:** `scripts/data-chain-smoke.ts:checkServicesDefault`, `observeGatewayAuthDefault`, `validGatewayAuth`, and typed `DataChainSmokeDependencies.gatewayAuthFetch` / `gatewayAuthTimeoutMs` auth-test seams.
- **CLI contract:** existing `pnpm check:services`; no new caller flags. Its final machine line uses `schema`, `healthy`, `listeners`, and `gateway` fields.
- **Machine fields:** Gateway probe `schema`, `healthy`, `robots`, `auth`; combined service output `schema`, `healthy`, `listeners[].name`, `listeners[].port`, `listeners[].listening`, and `gateway`.
- **GitNexus scope:** Task 1 inventories all new Gateway-readiness symbols and validates their post-write graph footprint before each RED/GREEN/REFACTOR/task commit; Task 2 reruns upstream impact for every listed existing owner and blocks any unexpected or HIGH/CRITICAL expansion before commit.
- **Paths:** the six `files_modified` paths in frontmatter. No evidence, target profile, environment, package, schema, migration, or provider path is written.
- **Schema/migrations:** N/A; no schema push.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md` after both tasks pass. Record exact timeout constants, test results, GitNexus impact/detect-changes, and that no run/evidence/service/provider/browser/schema action occurred.
</output>
