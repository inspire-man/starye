---
phase: 25
plan: 25-02
type: execute
wave: 2
depends_on: [25-01]
files_modified: [apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/schemas/crawler-tasks.ts, apps/api/src/domain/crawler-tasks/repository.ts, apps/api/src/domain/crawler-tasks/state-machine.ts, apps/api/src/domain/crawler-tasks/template-registry.ts, apps/api/src/domain/crawler-tasks/types.ts, apps/api/src/middleware/audit-logger.ts, apps/api/src/schemas/admin.ts, apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts, apps/api/src/domain/crawler-tasks/__tests__/state-machine.test.ts, apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts, apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts]
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06]
must_haves:
  truths: ["Registry-owned closed commands create immutable snapshots", "Task lifecycle is separate from crawler_run execution status", "Archive retains all child facts and supersede creates a new snapshot", "Cancel/retry/replay/late callback behavior is bounded and deterministic", "Task-scoped audit history is bounded and redacted"]
  artifacts: ["Lifecycle repository/state guards", "Strict admin lifecycle routes and schemas", "Task-scoped audit writer/readback", "Focused lifecycle and callback regression tests"]
  key_links: ["admin command -> operation registry -> request_snapshot_json -> crawler tuple", "lifecycle CAS -> run transition/lease/provider cancellation", "supersede -> new task while old facts remain readable", "mutation -> redacted audit_log -> bounded history route"]
---

<objective>
实现任务 CRUD 运管与生命周期：接入 25-01 operation/evidence contract，提供分页 list/detail、allowlisted metadata update、逻辑归档、不可变 snapshot supersede、queued/running cancel、失败/取消 bounded retry、idempotency/replay/conflict 与 task-scoped audit history。

Purpose: 分离 task lifecycle 与 crawler_run execution status，保持 request_snapshot_json 和既有 run/attempt/lease/provider/receipt/observation/audit 事实可追溯。
Output: admin lifecycle API、repository/state-machine 防护、兼容创建入口和 focused regression coverage。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-task-operations-and-availability-contract/25-RESEARCH.md
@.planning/phases/25-task-operations-and-availability-contract/25-01-PLAN.md
@apps/api/src/routes/admin/crawler-tasks/index.ts
@apps/api/src/domain/crawler-tasks/repository.ts
@apps/api/src/domain/crawler-tasks/state-machine.ts
@apps/api/src/domain/crawler-tasks/template-registry.ts
@apps/api/src/middleware/audit-logger.ts
@packages/db/src/schema.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement operation-bound lifecycle repository and state guards</name>
  <read_first>apps/api/src/domain/crawler-tasks/types.ts; apps/api/src/domain/crawler-tasks/repository.ts; apps/api/src/domain/crawler-tasks/state-machine.ts; apps/api/src/domain/crawler-tasks/template-registry.ts; apps/api/src/domain/crawler-tasks/provider-association.ts; apps/api/src/domain/crawler-tasks/operation-registry.ts; apps/api/src/domain/crawler-tasks/availability-contract.ts</read_first>
  <files>apps/api/src/domain/crawler-tasks/types.ts; apps/api/src/domain/crawler-tasks/repository.ts; apps/api/src/domain/crawler-tasks/state-machine.ts; apps/api/src/domain/crawler-tasks/template-registry.ts</files>
  <action>按 25-01 contract 接入 server-owned operation command、snapshot 和 fingerprint；扩展 active/archived/superseded task lifecycle/read model，并与 run 的 queued/dispatching/running/cancel_requested/succeeded/failed/cancelled 分离。集中实现兼容 create-or-get、paged list/detail、allowlisted metadata update、archive、supersede、cancel、bounded retry、audit readback 所需 repository methods。以 task/latest run/attempt/lifecycle version/snapshot fingerprint/idempotency identity 做条件保护；request_snapshot_json 创建后保持原值，目标或 policy 变化只能 supersede。复用 batch、lease、provider、receipt、transition；archive 只更新 task lifecycle，保留 runs/attempts/leases/provider/receipts/observations/audit。每个既有 symbol 修改前执行 GitNexus upstream impact 并记录 callers/flows/risk；HIGH/CRITICAL 在执行摘要告警；提交前执行 gitnexus_detect_changes。</action>
  <verify><automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__/repository.test.ts src/domain/crawler-tasks/__tests__/state-machine.test.ts</automated><automated>pnpm --filter api type-check; git diff --check</automated><automated>GitNexus upstream impact covers modified existing symbols and gitnexus_detect_changes reports only expected crawler lifecycle symbols/flows.</automated></verify>
  <acceptance_criteria>repository provides deterministic create/list/detail/update/archive/supersede/cancel/retry outcomes; retry is limited to failed/cancelled and the configured maximum; duplicate replay, key conflict, stale/late callback and newer-attempt writes preserve the authoritative state and all historical facts.</acceptance_criteria>
  <done>domain/repository/state guards expose the complete lifecycle contract consumed by routes.</done>
</task>
<task type="auto">
  <name>Task 2: Wire strict admin lifecycle routes and task-scoped audit</name>
  <read_first>apps/api/src/routes/admin/crawler-tasks/index.ts; apps/api/src/schemas/crawler-tasks.ts; apps/api/src/middleware/audit-logger.ts; apps/api/src/schemas/admin.ts; apps/api/src/routes/admin/audit-logs/index.ts; apps/api/src/domain/crawler-tasks/repository.ts; apps/api/src/domain/crawler-tasks/operation-registry.ts</read_first>
  <files>apps/api/src/routes/admin/crawler-tasks/index.ts; apps/api/src/schemas/crawler-tasks.ts; apps/api/src/middleware/audit-logger.ts; apps/api/src/schemas/admin.ts</files>
  <action>将 POST `/admin/crawler-tasks` 和 `/repair-players` 接入 operation registry，保留 repair 兼容输入与当前用户已有 SQL alias 修复。新增严格、allowlisted 的 list/detail、PATCH description/intent、archive、supersede、run cancel、bounded retry 和 task-scoped cursor/limit audit routes。复用 session/template/task/run access 与 provider boundary；统一投影 lifecycle、execution、target、latest attempt、provider、receipt、safe logs、transitions。生命周期操作调用既有 createAuditLog，扩展 crawler_task resource type/filter，固定 action/actor/reason/target/snapshot hash/run/attempt/outcome，脱敏并排除 workflow/URL/command/secrets/signed material/raw provider response/无界 logs。既有 route/schema/audit symbol 修改前执行 GitNexus upstream impact；提交前执行 gitnexus_detect_changes。</action>
  <verify><automated>pnpm --filter api exec vitest run src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</automated><automated>pnpm --filter api type-check; pnpm check:services</automated><automated>Bounded HTTP probes use only `http://localhost:8080/api/admin/crawler-tasks...`; verify direct dev origins are absent.</automated><automated>GitNexus impact and gitnexus_detect_changes cover modified route/schema/audit symbols and expected flows.</automated></verify>
  <acceptance_criteria>unknown fields and client workflow/provider/URL/command/secrets are rejected; list/detail/history are bounded; metadata update is allowlisted; archive/supersede/cancel/retry are access-controlled and idempotent; audit history is ordered, task-scoped and redacted.</acceptance_criteria>
  <done>Gateway-mounted admin lifecycle API emits and reads bounded task audit facts while retaining both creation paths.</done>
</task>
<task type="auto">
  <name>Task 3: Add replay, race, retention and redaction regression coverage</name>
  <read_first>apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts; apps/api/src/domain/crawler-tasks/__tests__/state-machine.test.ts; apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts; apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts; packages/db/src/schema.ts</read_first>
  <files>apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts; apps/api/src/domain/crawler-tasks/__tests__/state-machine.test.ts; apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts; apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</files>
  <action>覆盖 TASK-01..06：registered/unknown operation 与 forbidden fields；same-key exact replay 与 changed-command conflict；cursor/limit；allowlisted update；archive retention 与 immutable snapshot；supersede 新身份；queued/running cancel；failed/cancelled bounded retry；cancel/supersede/newer attempt 后 late callback、stale revision、duplicate event 的拒绝；create/update/archive/supersede/cancel/retry/repair audit 的 actor/reason/outcome、retention/order 和 redaction。既有 test helper/symbol 修改前执行 GitNexus upstream impact；提交前执行 gitnexus_detect_changes。</action>
  <verify><automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__/repository.test.ts src/domain/crawler-tasks/__tests__/state-machine.test.ts src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</automated><automated>pnpm --filter api type-check; pnpm --filter @starye/db type-check; git diff --check</automated><automated>pnpm check:services; bounded Gateway 8080 requests assert pagination, conflict, stale/late rejection, retention and redaction.</automated><automated>gitnexus_detect_changes reports only expected lifecycle tests and flows.</automated></verify>
  <acceptance_criteria>duplicate/stale/late/conflict, cancel/supersede race, retry bound, historical retention, audit ordering, redaction and Gateway 8080 assertions pass.</acceptance_criteria>
  <done>lifecycle behavior has executable focused coverage and bounded canonical verification.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| admin client -> task routes | untrusted command, metadata, lifecycle and idempotency input |
| runner/provider -> repository | replayed, stale or late result attempts mutation |
| API -> audit_log | bounded redacted facts become durable operator history |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-25-02-01 | Tampering/Elevation | task routes | high | mitigate | strict schemas, session/resource access and operation registry |
| T-25-02-02 | Tampering/Repudiation | repository/state machine | high | mitigate | lifecycle/run separation, snapshot/fingerprint/revision CAS and replay identity |
| T-25-02-03 | Information disclosure | audit/detail projections | high | mitigate | allowlists, redaction, bounded history and no provider secrets/raw responses |
| T-25-02-04 | Denial of service | list/history/retry | medium | mitigate | cursor/limit, fixed caps and retry maximum |
| T-25-02-SC | Tampering | package installs | low | accept | no package installation; existing toolchain only |
</threat_model>
<verification>Run focused Vitest, API/DB type-checks, `pnpm check:services`, bounded D1 readback and Gateway-only probes at `http://localhost:8080`; verify duplicate/stale/late/conflict, immutable snapshot, archive retention, retry bounds, audit redaction and SQL alias preservation. Run GitNexus impact before each existing-symbol change and gitnexus_detect_changes before any commit; this plan creates no commit.</verification>
<success_criteria>TASK-01..06 pass through strict routes, repository/state guards and tests; lifecycle remains separate from execution status; archive/supersede preserve immutable and historical facts; cancel/retry/replay/late callback is deterministic; audit is bounded/redacted; HTTP evidence uses Gateway 8080.</success_criteria>
<source_audit>GOAL, TASK-01..06, research constraints and the 25-01 dependency are covered by Tasks 1-3. No Dashboard, availability persistence, scheduler or video/chapter/image probe is included.</source_audit>
## Artifacts this phase produces
- Symbols: lifecycle-aware crawler task types, repository/state guards, operation route adapters and task audit projection.
- Routes: existing create and repair compatibility paths; paged list/detail/audit; metadata PATCH; archive/supersede; run cancel/retry.
- Schema/migration: existing audit_log reuse; lifecycle/availability persistence and generated migration belong to 25-03.
- Tests: repository/state/admin/callback lifecycle regressions.
<output>Create `.planning/phases/25-task-operations-and-availability-contract/25-02-SUMMARY.md` when done.</output>
