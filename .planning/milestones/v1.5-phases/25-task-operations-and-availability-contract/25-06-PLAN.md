---
phase: 25-task-operations-and-availability-contract
plan: 25-06
type: execute
wave: 5
depends_on:
  - 25-05
files_modified:
  - scripts/phase25-dashboard-gateway-proof.ts
  - scripts/phase25-dashboard-gateway-proof.test.ts
autonomous: false
gap_closure: true
gap_ids:
  - G-25-1
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - TASK-05
  - TASK-06
must_haves:
  truths:
    - "The proof validates and preserves the original fresh terminal tuple before any auxiliary action can transfer authoritative availability ownership."
    - "After an accepted supersede action, cache and dashboard availability readback follow the single authoritative owner task identified by the response while the matrix retains the original tuple as proof identity."
    - "A missing or ambiguous supersede owner produces a checkpoint outcome instead of a passing matrix."
    - "The authoritative owner exposes a newer projection whose history retains the original observation identity, with target, content, provider, policy, and revision continuity explicitly proven."
    - "All action readbacks pass, task-scoped audit evidence has a positive count, cache refresh and dashboard trace pass, the live matrix confirms original-task cleanup, and deterministic tests prove `finally` cleanup is invoked for every auxiliary task identifier."
    - "The evidence matrix remains bounded and redacted, and neither raw response payloads nor authenticated browser-session material is persisted."
  artifacts:
    - path: "scripts/phase25-dashboard-gateway-proof.ts"
      provides: "Ownership-aware Phase 25 Gateway proof orchestration and bounded matrix evidence"
    - path: "scripts/phase25-dashboard-gateway-proof.test.ts"
      provides: "Regression coverage for authoritative-owner transfer, checkpoint behavior, audit scoping, redaction, and cleanup"
  key_links:
    - from: "accepted supersede action response"
      to: "Phase25ActionReadback.taskId"
      via: "supersededTaskId extracts task.run.taskId from the accepted response"
    - from: "executeActions()"
      to: "post-action availability readback"
      via: "named result containing action readbacks and authoritativeOwnerTaskId"
    - from: "original terminal detail"
      to: "cache refresh before-state"
      via: "snapshot captured before ownership-changing auxiliary actions"
    - from: "authoritative owner detail"
      to: "cache refresh and dashboard after-state"
      via: "task-scoped Gateway detail readback after refresh"
    - from: "existing evidence matrix fields"
      to: "original tuple and ownership transition"
      via: "the existing tuple remains the proof identity while actions.supersede.taskId supplies bounded owner evidence without a matrix-shape expansion"
    - from: "action tasks"
      to: "audit evidence"
      via: "explicit task-scoped audit reads without attributing the original task audit trail to the new owner"
---

<objective>
Close G-25-1 by making the Phase 25 authenticated Gateway proof ownership-aware when an auxiliary supersede action legally transfers the target-unique current availability projection to another task.

Purpose: Preserve the established server-owned availability contract while producing trustworthy live evidence for TASK-01 through TASK-06. Earlier Phase 25 plans implement those requirements; this plan repairs only the final evidence orchestration and does not redesign or reimplement API, database, Dashboard, repository, runner, or ownership semantics.
Output: Updated proof orchestration, focused regression coverage, and one human-confirmed redacted evidence matrix generated through `http://localhost:8080`.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-task-operations-and-availability-contract/25-RESEARCH.md
@.planning/phases/25-task-operations-and-availability-contract/25-PATTERNS.md
@.planning/phases/25-task-operations-and-availability-contract/25-VERIFICATION.md
@.planning/phases/25-task-operations-and-availability-contract/25-UAT.md
@.planning/phases/25-task-operations-and-availability-contract/25-05-PLAN.md
@.planning/phases/25-task-operations-and-availability-contract/25-05-SUMMARY.md
@scripts/phase25-dashboard-gateway-proof.ts
@scripts/phase25-dashboard-gateway-proof.test.ts
@scripts/phase24-production-proof.ts
@scripts/phase24-production-proof.test.ts
@packages/db/src/schema.ts
@apps/api/src/routes/admin/crawler-tasks/index.ts

The spec-less probe may report TASK-01 through TASK-06 as unresolved or unclassified. Treat that as a bounded evidence limitation: earlier Phase 25 plans own requirement implementation, while this gap-closure plan owns only G-25-1 proof orchestration. Do not broaden the implementation surface in response to the probe.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the live proof follow authoritative availability ownership</name>
  <files>scripts/phase25-dashboard-gateway-proof.ts, scripts/phase25-dashboard-gateway-proof.test.ts</files>
  <behavior>
    - Regression case: a fresh original task reaches a terminal availability tuple; an auxiliary supersede source transfers the target-unique current row to a distinct owner task; the original task then has `availability.current: null`, and the proof still passes by reading the new owner.
    - Regression case: the accepted supersede response supplies the authoritative owner only through `task.run.taskId`; `executeActions()` returns both all action readbacks and that single owner identifier.
    - Regression case: absent, malformed, conflicting, or non-distinct supersede ownership evidence produces a checkpoint result and never a passing result.
    - Regression case: the owner projection has a higher projection version, its history retains the original observation identity, and its target/content/provider/policy/revision fields remain continuous with the original terminal tuple.
    - Regression case: audit evidence is read from the task that owns each action or transition and does not require the new availability owner to contain the original task's audit trail.
    - Regression case: route-aware fake call assertions prove the `finally` path invokes cleanup for the original task and every auxiliary task identifier, while matrix redaction rejects credentials, session material, and unbounded raw payloads.
  </behavior>
  <action>
Before editing any function, class, or method, run upstream GitNexus impact analysis for `executeActions` and every other symbol that the implementation will modify. Report direct callers, affected execution flows, and the blast radius before applying edits. If any result is HIGH or CRITICAL, warn explicitly and pause edits until the warning has been acknowledged. Keep analysis and implementation scope limited to `scripts/phase25-dashboard-gateway-proof.ts` and `scripts/phase25-dashboard-gateway-proof.test.ts`.

Update the test fake first so it models three distinct identities: the original task/run, the auxiliary supersede-source task/run, and the authoritative superseded-owner task/run. Make task-detail responses task-scoped, transfer current availability away from the original task after supersede, retain the original observation identity in the owner's history at a newer projection version, and return the real accepted response shape `{ task: { run: { taskId: ownerId } } }`. Add focused assertions for successful transfer, missing or ambiguous ownership checkpointing, task-scoped audit reads, cleanup coverage, and redaction before changing production proof logic.

Reuse the proven analogs in `scripts/phase24-production-proof.ts` and `scripts/phase24-production-proof.test.ts`: bounded authoritative owner discovery, a route-aware fake that varies responses by requested resource identity, fail-closed checkpoint conversion, and matrix redaction enforcement. Adapt those patterns to Phase 25 task ownership; do not introduce a parallel proof framework.

In `phase25-dashboard-gateway-proof.ts`, introduce a named `executeActions()` result type containing `actions` and `authoritativeOwnerTaskId`. Derive the owner from the accepted supersede readback already represented by `Phase25ActionReadback.taskId`; require exactly one distinct, bounded owner consistent with the accepted action and return a checkpoint outcome when that evidence is absent or ambiguous.

Capture and validate the original terminal tuple projection before executing ownership-changing auxiliary actions. Use that captured terminal detail as the cache-refresh before-state. After actions and refresh, fetch task detail using `authoritativeOwnerTaskId` for cache and Dashboard availability proof. Keep `classifyPhase25CacheRefresh()` owner-agnostic, but replace same-task identity assumptions around it with explicit continuity checks across target kind/id, content id, provider, policy, and revision; require a newer owner projection and history retention of the original observation identity.

Keep the original fresh tuple unchanged as the matrix proof identity. Preserve the current matrix structure: use the existing `actions.supersede.taskId` readback as bounded authoritative-owner evidence and the existing cache/dashboard/check fields as transition results; add no top-level or nested matrix evidence object. Read audit records explicitly from the original and action-owning tasks as appropriate; report positive bounded audit counts without asserting that the new owner inherited another task's audit history. Preserve the authenticated browser adapter, canonical `http://localhost:8080` routing, unique matrix write, bounded waits, `finally` cleanup, and `assertPhase25Redacted()` enforcement. Make the deterministic fake record cleanup requests and assert that `finally` attempts cleanup for the original and every auxiliary task identifier on pass, checkpoint, and failure paths. Do not change API, schema, Dashboard application, repository, runner, or availability ownership behavior.

Before any commit, run `gitnexus_detect_changes()` and inspect the reported symbols and execution flows. Proceed only when the result is limited to the two Phase 25 proof files and the expected proof orchestration/test flow; report and resolve any broader result before committing.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/phase25-dashboard-gateway-proof.test.ts &amp;&amp; pnpm exec tsc --noEmit -p scripts/tsconfig.json &amp;&amp; git diff --check -- scripts/phase25-dashboard-gateway-proof.ts scripts/phase25-dashboard-gateway-proof.test.ts</automated>
    <human-check>Before commit, run `gitnexus_detect_changes()` and confirm it reports only `scripts/phase25-dashboard-gateway-proof.ts`, `scripts/phase25-dashboard-gateway-proof.test.ts`, and the expected Phase 25 proof orchestration/test execution flow.</human-check>
  </verify>
  <done>The regression test reproduces the legal ownership transfer that caused G-25-1, the proof follows the accepted authoritative owner without overwriting the original tuple identity or expanding the matrix structure, incomplete owner evidence checkpoints, audit reads remain task-scoped, deterministic assertions cover `finally` cleanup for every auxiliary ID, focused test/type/diff gates pass, and pre-commit `gitnexus_detect_changes()` is limited to the two proof files and expected flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the repaired authenticated Gateway evidence</name>
  <files>None; verification-only checkpoint reads the uniquely written Phase 25 evidence matrix.</files>
  <what-built>An ownership-aware Phase 25 browser-assisted proof that preserves the original terminal tuple, follows the accepted supersede owner, verifies projection continuity, and writes a bounded redacted matrix.</what-built>
  <action>Run the established authenticated browser-assisted Phase 25 proof through the canonical Gateway, inspect the uniquely written redacted matrix using the steps below, and block completion unless every stated ownership, audit, cache, Dashboard, cleanup, and redaction condition is directly observed.</action>
  <how-to-verify>
    1. Start the existing supported local stack and authenticated browser-session adapter using the repository's documented Phase 25 proof procedure; access the application only through `http://localhost:8080`.
    2. Run `pnpm exec tsx scripts/phase25-dashboard-gateway-proof.ts` so the proof creates a fresh original tuple and all auxiliary tasks itself.
    3. Open the newly and uniquely written Phase 25 evidence matrix. Confirm `outcome` is `passed`, every action readback is passed, the bounded audit count is positive, `cacheRefresh` is passed, `dashboardTrace` is passed, and cleanup reports success for the original task.
    4. Confirm the matrix preserves the original fresh tuple as proof identity and that the existing `actions.supersede.taskId` readback identifies one distinct authoritative owner without adding a matrix section. The owner projection must be newer and its history must retain the original observation identity while target, content, provider, policy, and revision remain continuous.
    5. Confirm the matrix contains bounded identifiers and projection metadata only, with no credentials, cookies, browser-session material, authorization headers, or raw response payloads.
  </how-to-verify>
  <verify>
    <automated>pnpm exec vitest run scripts/phase25-dashboard-gateway-proof.test.ts</automated>
    <human-check>Complete all five live Gateway and evidence-matrix checks above and record approval only when each expected result is present.</human-check>
  </verify>
  <done>A fresh authenticated run through `http://localhost:8080` produces a redacted passing matrix with the original tuple, one valid owner in the existing supersede action readback, passed action/audit/cache/Dashboard evidence, and successful original-task cleanup.</done>
  <resume-signal>Type "approved" or describe the failed field and observed evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated browser to canonical Gateway/API | Browser-held authenticated state crosses into local proof requests routed through `http://localhost:8080`. |
| Action response to proof ownership state | A server response determines which task now owns the target-unique current availability projection. |
| API readback to evidence matrix | Task details, availability history, audits, and Dashboard trace are reduced into persisted proof evidence. |
| Proof-created tasks to cleanup | Original and auxiliary task identifiers drive bounded cleanup after success, checkpoint, or failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Spoofing / Tampering | Supersede owner extraction and post-action readback | medium | mitigate | Accept one distinct bounded owner only from the accepted supersede response, verify tuple-field continuity and projection/history linkage, and checkpoint missing or ambiguous ownership evidence. |
| T-25-02 | Information Disclosure | Persisted Phase 25 evidence matrix | medium | mitigate | Retain `assertPhase25Redacted()`, persist only bounded identifiers and projection metadata, and exclude credentials, browser-session material, authorization headers, and raw response bodies. |
| T-25-03 | Denial of Service | Browser proof polling and cleanup | medium | mitigate | Keep waits bounded; deterministic tests assert that `finally` invokes cleanup for the original and every auxiliary task on pass, checkpoint, and failure paths, while live matrix acceptance remains bounded to original-task cleanup evidence. |
| T-25-SC | Tampering | Existing package-manager dependency graph | low | accept | This plan installs no dependency and uses the repository lockfile and existing proof toolchain, so no new package legitimacy boundary is introduced. |
</threat_model>

<source_coverage_audit>
| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Phase 25 operators can manage crawler tasks and trust availability/audit evidence | COVERED | Earlier plans implement the control plane; Task 1 closes the final live-evidence ownership orchestration gap and Task 2 verifies it through the canonical Gateway. |
| REQ | TASK-01 create/list and TASK-02 detail/current-state behavior | COVERED | Existing implementation remains authoritative; the repaired proof preserves the fresh tuple and uses task-scoped detail readback. |
| REQ | TASK-03 update/archive and TASK-04 cancel/retry actions | COVERED | Existing action behavior is exercised through structured action readbacks and correctly scoped audit evidence. |
| REQ | TASK-05 availability ownership/readback and TASK-06 audit acceptance | COVERED | Task 1 follows the accepted authoritative owner, proves projection/history continuity, and requires positive bounded audits. |
| RESEARCH | Existing control plane and server-owned ownership semantics | COVERED | The plan changes proof orchestration only and preserves the existing API/database/Dashboard contract. |
| RESEARCH | Append-only observations, target-unique current row, and task-scoped readback | COVERED | Original tuple identity, transferred current ownership, owner history, and task-scoped reads are modeled and asserted explicitly. |
| RESEARCH | Bounded/redacted evidence through canonical Gateway | COVERED | Both tasks retain `http://localhost:8080`, browser authentication, redaction, bounded matrix fields, cleanup, and unique output. |
| RESEARCH | Dependency constraints | COVERED | No package installation or dependency change is planned. |
| CONTEXT | Phase 25 CONTEXT.md decisions | N/A | No phase CONTEXT.md exists; the explicit gap-closure constraints and established Phase 25 artifacts govern this plan, with no invented D-NN decisions. |
| EXCLUSION | Phase 26-28 detector and downstream availability work | EXCLUDED | Scoped to later roadmap phases and not part of G-25-1. |
</source_coverage_audit>

<verification>
- `pnpm exec vitest run scripts/phase25-dashboard-gateway-proof.test.ts` passes the distinct-owner transfer and checkpoint regressions.
- `pnpm exec tsc --noEmit -p scripts/tsconfig.json` passes with the named action result and matrix evidence types.
- `git diff --check -- scripts/phase25-dashboard-gateway-proof.ts scripts/phase25-dashboard-gateway-proof.test.ts` reports no whitespace errors.
- Human verification produces one fresh, redacted, passing matrix through `http://localhost:8080` with a positive audit count, passed cache/Dashboard evidence, a valid owner in the existing supersede action readback, and successful original-task cleanup.
</verification>

<success_criteria>
- G-25-1 is closed without changing product ownership semantics: an original task losing `availability.current` after a legal supersede no longer invalidates the proof.
- The original terminal tuple remains the proof identity, while the post-action availability readback follows exactly one accepted authoritative owner.
- Incomplete or conflicting owner evidence yields a checkpoint rather than a false pass.
- Projection continuity, observation-history retention, task-scoped audits, bounded redaction, and canonical Gateway routing are covered by automated tests and the live checkpoint; auxiliary cleanup is proven by deterministic `finally` call assertions, and the live checkpoint confirms original-task cleanup only.
- TASK-01 through TASK-06 retain their existing implementation ownership; this plan supplies the missing live acceptance evidence only.
</success_criteria>

<output>
Create `.planning/phases/25-task-operations-and-availability-contract/25-06-SUMMARY.md` when done.
</output>
