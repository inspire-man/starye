---
phase: 25
plan: 25-05
type: execute
wave: 4
depends_on: [25-04]
files_modified:
  - apps/api/src/domain/crawler-tasks/types.ts
  - apps/api/src/domain/crawler-tasks/provider-association.ts
  - apps/api/src/domain/crawler-tasks/operation-registry.ts
  - apps/api/src/domain/crawler-tasks/repository.ts
  - apps/api/src/domain/crawler-tasks/availability-contract.ts
  - apps/api/src/routes/admin/crawler-tasks/index.ts
  - apps/api/src/routes/internal/crawler-runs/index.ts
  - apps/api/src/schemas/crawler-run-events.ts
  - packages/db/src/schema.ts
  - packages/db/drizzle/<generated-local-proof-provider-migration>.sql
  - packages/crawler/src/task-runner/runner-client.ts
  - packages/crawler/src/task-runner/local-runner.ts
  - packages/crawler/src/task-runner/template-adapters.ts
  - packages/crawler/src/task-runner/local-proof-adapter.ts
  - scripts/local-task-runner.ts
  - scripts/local-task-runner.e2e.ts
  - scripts/local-dev.ts
  - packages/crawler/src/task-runner/__tests__/runner-client.test.ts
  - packages/crawler/src/task-runner/__tests__/local-runner.test.ts
  - apps/api/src/domain/crawler-tasks/__tests__/provider-association.test.ts
  - apps/api/src/domain/crawler-tasks/__tests__/availability-contract.test.ts
  - apps/api/src/domain/crawler-tasks/__tests__/availability-repository.test.ts
  - apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts
  - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
  - apps/api/src/routes/internal/crawler-runs/__tests__/availability.route.test.ts
  - packages/config/src/deployment-target/__tests__/local-dev.test.ts
  - scripts/phase25-dashboard-gateway-proof.ts
  - scripts/phase25-dashboard-gateway-proof.test.ts
autonomous: true
requirements: [TASK-04, TASK-05, TASK-06]
must_haves:
  truths:
    - "A fresh local proof task created through the canonical Gateway is bound to an explicit local-proof provider and advances beyond queued without borrowing GitHub Actions identity or metadata."
    - "The local runner receives server-owned task, target, policy, content and source-revision bindings, posts one bounded ordinary movie availability observation through the existing signed boundary, and obtains authoritative current/history readback."
    - "Duplicate, conflict, stale and late observations remain bounded history; only the newest accepted observation with matching task/run/attempt/provider, policy and source revision can advance current through CAS and cache invalidation after readback."
    - "The local development supervisor starts and stops the runner as an owned child after Gateway readiness, cleans its ignored runtime inputs and cancels unfinished proof work without leaving a live queued/running task."
    - "The canonical 8080 proof emits a redacted matrix for a fresh task/run/attempt/provider tuple, availability history, audit readback and cache-refresh advancement while retaining production GitHub Actions proof as a separate identity."
  artifacts:
    - path: "apps/api/src/domain/crawler-tasks/types.ts"
      provides: "Closed provider-name union and bounded read-model types that distinguish github-actions from local-proof."
      contains: "local-proof"
    - path: "apps/api/src/routes/admin/crawler-tasks/index.ts"
      provides: "Server-owned provider dispatch selection; local proof is opt-in, bounded and never exposes workflow or secret controls."
      contains: "provider_not_configured"
    - path: "apps/api/src/domain/crawler-tasks/repository.ts"
      provides: "Local provider association, task-id/target/policy/source-revision candidate binding and unchanged production provider gates."
      contains: "pollDispatch"
    - path: "apps/api/src/routes/internal/crawler-runs/index.ts"
      provides: "Signed availability observation boundary that accepts the explicit local-proof provider without weakening replay, freshness, tuple or evidence checks."
      contains: "availability-observation"
    - path: "packages/crawler/src/task-runner/runner-client.ts"
      provides: "Signed ordinary availability observation command and server-bound candidate parsing."
      contains: "availability"
    - path: "packages/crawler/src/task-runner/local-runner.ts"
      provides: "Lifecycle-first local execution followed by bounded availability observation and proof-history scenario handling."
      contains: "observeAvailability"
    - path: "scripts/local-dev.ts"
      provides: "Owned local runner process, runtime config materialization, readiness ordering and cleanup."
      contains: "local-task-runner"
    - path: "scripts/phase25-dashboard-gateway-proof.ts"
      provides: "Canonical Gateway proof that accepts local-proof for local contract evidence, keeps production provider assertions separate, and cancels/cleans unfinished tasks."
      contains: "http://localhost:8080"
  key_links:
    - from: "admin task create/supersede/retry"
      to: "local-proof provider association"
      via: "server-side feature flag and immutable run binding before local runner polling"
    - from: "pollDispatch response"
      to: "LocalTaskRunner availability event"
      via: "taskId, target, policyVersion, sourceRevision and proofMode derived from the stored operation snapshot"
    - from: "LocalTaskRunner ordinary movie adapter"
      to: "POST /api/internal/crawler-runs/:runId/availability-observation"
      via: "RunnerClient HMAC envelope with monotonic event sequence and bounded summary"
    - from: "availability observation repository"
      to: "admin task detail"
      via: "append-first history, projection CAS, authoritative D1 readback, then existing cache invalidation"
    - from: "local-dev supervisor"
      to: "canonical proof"
      via: "runner starts only after Gateway 8080 readiness and uses the same ignored per-run callback material"
---

<objective>
Close G-25-1 by making the current Phase 25 task/availability chain executable in local Gateway acceptance: a server-owned local proof provider dispatches a fresh operation, the local runner claims it and emits a bounded ordinary movie availability observation, the API persists current/history with the existing signed and CAS boundaries, and the Dashboard proof reads the same tuple after refresh.

Purpose: resolve the real blocker recorded in 25-UAT without claiming GitHub Actions or Phase 26 detector behavior. The local provider is an explicit `local-proof` identity used only when the local runtime flag is enabled; production continues to require the configured GitHub App provider. The proof uses an existing target movie and a server-owned fixture adapter, so it exercises lifecycle, receipt, availability and readback contracts without implementing direct-source, magnet, Aria2/TorrServer, comic or image detectors.

Output: provider contract extension and D1 compatibility, bounded local dispatch/candidate binding, signed ordinary availability observation producer, supervised local runner startup, deterministic duplicate/conflict/stale/late coverage, canonical redacted Gateway proof, and cleanup of failed or unfinished live tasks.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
@D:/my-workspace/starye/.agents/skills/starye-db-migration/SKILL.md
@D:/my-workspace/starye/.agents/skills/starye-hono-rpc/SKILL.md
@D:/my-workspace/starye/.agents/skills/starye-crawler-strategy/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-task-operations-and-availability-contract/25-RESEARCH.md
@.planning/phases/25-task-operations-and-availability-contract/25-VERIFICATION.md
@.planning/phases/25-task-operations-and-availability-contract/25-UAT.md
@.planning/phases/25-task-operations-and-availability-contract/25-04-PLAN.md
@.planning/phases/25-task-operations-and-availability-contract/25-04-SUMMARY.md
@apps/api/src/domain/crawler-tasks/types.ts
@apps/api/src/domain/crawler-tasks/provider-association.ts
@apps/api/src/domain/crawler-tasks/operation-registry.ts
@apps/api/src/domain/crawler-tasks/repository.ts
@apps/api/src/domain/crawler-tasks/availability-contract.ts
@apps/api/src/domain/crawler-tasks/availability-repository.ts
@apps/api/src/routes/admin/crawler-tasks/index.ts
@apps/api/src/routes/internal/crawler-runs/index.ts
@apps/api/src/schemas/crawler-run-events.ts
@packages/db/src/schema.ts
@packages/crawler/src/task-runner/runner-client.ts
@packages/crawler/src/task-runner/local-runner.ts
@packages/crawler/src/task-runner/template-adapters.ts
@scripts/local-task-runner.ts
@scripts/local-task-runner.e2e.ts
@scripts/local-dev.ts
@scripts/phase25-dashboard-gateway-proof.ts
</context>

<constraints>
- Preserve the mixed dirty worktree. Do not revert, format or stage unrelated user/prior-phase changes.
- Before modifying any function, class or method, run GitNexus impact analysis for the exact symbol, report direct callers/affected flows and risk, and stop for an explicit warning if risk is HIGH or CRITICAL.
- Before committing, run GitNexus detect-changes with the intended scope and confirm only the provider/local-runner/availability/proof symbols are affected. No broad Phase 25 or Phase 24 commit is allowed.
- Use `local-proof` as the only local provider identity. Do not fabricate `github-actions`, GitHub run IDs, workflow URLs, repository metadata, or production receipts for local evidence.
- The local feature flag and callback key/secret are server/runtime inputs only. Read them from the generated ignored local runtime directory or existing local secret source; never print, serialize into evidence, or commit them.
- Keep availability evidence closed and bounded: counts/samples only; no raw response, URL, cookie, command, token, signed material, media or provider payload.
- Keep Phase 26 out of scope: no real direct HTTP/Range/browser probe, magnet parse/resolver, Aria2/TorrServer call, playback detector or repair implementation.
- All browser/API acceptance commands use `http://localhost:8080`; direct development ports are only internal service inputs to the existing supervisor.
- Use the database migration skill order: edit Drizzle schema, run `pnpm --filter @starye/db drizzle-kit generate`, inspect the generated SQL, apply local D1 with the project Wrangler scope, then run DB/API type checks. If the provider enum is compile-time-only and generation produces no SQL, record that fact and keep the migration test as the compatibility proof.
</constraints>

<tasks>

<task type="code" id="25-05-01">
  <name>Bind an explicit local-proof provider and server-owned runner candidate</name>
  <files>
    apps/api/src/domain/crawler-tasks/types.ts
    apps/api/src/domain/crawler-tasks/provider-association.ts
    apps/api/src/domain/crawler-tasks/operation-registry.ts
    apps/api/src/domain/crawler-tasks/repository.ts
    apps/api/src/domain/crawler-tasks/availability-contract.ts
    apps/api/src/routes/admin/crawler-tasks/index.ts
    apps/api/src/routes/internal/crawler-runs/index.ts
    apps/api/src/schemas/crawler-run-events.ts
    packages/db/src/schema.ts
    packages/db/drizzle/<generated-local-proof-provider-migration>.sql
    apps/api/src/domain/crawler-tasks/__tests__/provider-association.test.ts
    apps/api/src/domain/crawler-tasks/__tests__/availability-contract.test.ts
    apps/api/src/domain/crawler-tasks/__tests__/availability-repository.test.ts
    apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts
    apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
    apps/api/src/routes/internal/crawler-runs/__tests__/availability.route.test.ts
  </files>
  <action>
    Run GitNexus impact analysis first for `dispatchCreatedRun`, `createProviderAssociationSummary`, `pollDispatch`, `processRunnerEvent`, `validateAvailabilityObservation`, and the availability observation route handler; record the direct consumers and risk in the execution handoff before editing. Extend the closed provider union with `local-proof` while leaving the production GitHub Actions snapshot and workflow registry unchanged. Add a server-only local dispatch path selected only by an explicit local binding/feature flag; when disabled or when the required local callback material is absent, retain the existing `provider_not_configured` behavior. The local path must atomically create/update the provider association before the runner can claim the run, use a bounded local provider run identity derived from the server run ID, set safe local status/conclusion facts, and never produce a GitHub URL or production workflow metadata. Make create, supersede, retry and any automatic retry select the same immutable provider mode for the run rather than allowing the client to select it.

    Extend `CrawlerRunDispatchCandidate` and `/poll` only with server-derived bounded fields needed by the local proof: task ID, operation target, policy reference/version, source revision and an exact proof profile when the stored operation snapshot matches the Phase 25 proof policy. Parse the wrapper snapshot at the API boundary; do not accept these fields from the runner. The target must remain an existing movie/manga operation target and the source revision must come from the server-owned source state/receipt binding. Make ordinary availability tuples accept both `github-actions` and `local-proof`, with the provider association and operation snapshot still checked before persistence. Keep signed envelope, replay, timestamp, event sequence, tuple, evidence, current CAS and authoritative readback rules unchanged.

    Update the read model to include the provider identity and to project local metadata as bounded local facts without `providerRunUrl`, GitHub repository, workflow or secrets. Add/adjust the Drizzle provider enum declarations and migration compatibility coverage. Preserve all existing GitHub tests and add local tests for disabled flag, local association identity, duplicate task creation, provider failure, attempt mismatch, late callback and production snapshot rejection.
  </action>
  <verify>
    Run the focused API provider, repository, admin route and availability route suites. Run `pnpm --filter @starye/db drizzle-kit generate`, inspect that the migration only represents the intended provider contract (or document no physical SQL change), apply the local D1 migration through the app's Wrangler scope, then run `pnpm --filter @starye/db type-check` and `pnpm --filter api type-check`.
  </verify>
  <acceptance_criteria>
    - With the local flag enabled and no GITHUB_* bindings, a fresh operation returns a local-proof dispatch and its detail shows provider local-proof, not provider_not_configured or github-actions.
    - With the flag disabled, the existing GitHub configuration gate and provider_not_configured result remain unchanged.
    - A polled candidate contains a server-verified task/target/policy/source-revision binding; changing or replaying any identity is rejected and no current projection advances.
    - Local and production availability observations share the same bounded signed/CAS/readback path, while provider identity is preserved in D1 history/current and the Dashboard DTO.
  </acceptance_criteria>
</task>

<task type="code" id="25-05-02">
  <name>Produce bounded ordinary movie availability facts from the local runner</name>
  <files>
    packages/crawler/src/task-runner/runner-client.ts
    packages/crawler/src/task-runner/local-runner.ts
    packages/crawler/src/task-runner/template-adapters.ts
    packages/crawler/src/task-runner/local-proof-adapter.ts
    scripts/local-task-runner.ts
    scripts/local-task-runner.e2e.ts
    packages/crawler/src/task-runner/__tests__/runner-client.test.ts
    packages/crawler/src/task-runner/__tests__/local-runner.test.ts
  </files>
  <action>
    Run GitNexus impact analysis for `RunnerClient`, `LocalTaskRunner`, `createTemplateAdapterRegistry`, `createMovieAdapter` and the local task-runner entry before editing. Add a signed `observeAvailability` command that serializes only the candidate's server-owned task/run/attempt/target/policy/source-revision/provider bindings, a monotonic sequence, bounded status/reason/freshness/next action and a small counts/samples summary. The client must reject a candidate missing its local proof binding and must treat non-2xx observation responses as bounded outcomes rather than hiding a conflict or stale result.

    Make the local runner send lifecycle/receipt first and then one ordinary movie availability observation using the authoritative receipt content ID and source revision. The default movie and manga adapters retain their current crawler behavior; add a narrowly scoped local proof adapter/profile that uses an existing target movie ID and returns a bounded receipt fixture without making a direct-source, magnet or TorrServer claim. The proof profile is server-selected from the immutable policy, not a free-form runner config value. Keep cancellation checkpoints ahead of terminal/observation writes and do not emit an accepted availability current for a cancelled, failed, stale or missing-receipt run.

    Add a bounded history exercise for the Phase 25 proof profile: after the first accepted observation, issue deterministic duplicate, identity-conflict, stale-CAS and late-sequence observations with fresh event identities. Persist only the server's redacted outcomes; the local runner's evidence output contains outcome codes and tuple IDs, never raw request bodies, signatures or secrets. The normal local E2E flow must still cover a failed empty receipt and controlled cancellation, and its finally path must cancel any unfinished task before returning.
  </action>
  <verify>
    Run `pnpm --filter @starye/crawler exec vitest run src/task-runner/__tests__/runner-client.test.ts src/task-runner/__tests__/local-runner.test.ts`, the existing task-runner unit/integration suites, and `pnpm exec vitest run scripts/phase25-dashboard-gateway-proof.test.ts`. Run `pnpm --filter @starye/crawler type-check` and `pnpm exec tsc --noEmit -p scripts/tsconfig.json`.
  </verify>
  <acceptance_criteria>
    - A successful local proof run has lifecycle succeeded, a validated receipt, an accepted local-proof availability observation and an authoritative current projection with matching task/run/attempt/content/source revision/policy.
    - Duplicate, conflict, stale and late outcomes are visible in bounded history and never replace the accepted current row.
    - Empty receipt, adapter error and cancellation paths terminate with bounded failure/cancel status and do not write a false availability current.
    - Existing production runner client envelopes and repair source observation tests remain green.
  </acceptance_criteria>
</task>

<task type="code" id="25-05-03">
  <name>Supervise the local runner and its ignored runtime inputs</name>
  <files>
    scripts/local-dev.ts
    scripts/local-task-runner.ts
    packages/config/src/deployment-target/__tests__/local-dev.test.ts
  </files>
  <action>
    Run GitNexus impact analysis for `runLocalDevSupervisor`, `localDevServiceSpecs`, `materializeLocalInputs` and the local task-runner entry before editing. Extend the existing supervisor with an ignored per-run local runner config materialization that uses the same callback key/secret source as the API runtime, writes only to the validated `.target-runs` directory, and passes `TASK_RUNNER_LOCAL_CONFIG` plus the explicit local-proof mode to the child process without logging values. Start the runner only after the existing seven service ports, especially Gateway 8080, are ready; keep it as an owned non-port child so readiness does not fake a listener. Include it in the same signal, startup-failure, unexpected-exit and cleanup lifecycle, and remove the config/temporary materialized files on every stop path.

    Keep the local runner's crawler config bounded and local. Do not read production GitHub bindings into the runner, do not add a second scheduler, and do not make direct development ports the public acceptance origin. Add supervisor tests for runner ordering, missing callback material, runner early exit, API/Gateway readiness failure, signal cleanup, and idempotent removal of the ignored config. Ensure test fixtures assert that serialized runtime/evidence output does not contain callback secrets or local provider control material.
  </action>
  <verify>
    Run `pnpm exec vitest run packages/config/src/deployment-target/__tests__/local-dev.test.ts`, `pnpm check:services`, and the local task-runner E2E in a disposable local runtime with a bounded timeout. Confirm the supervisor reports the runner as managed while readiness still requires only the existing Gateway/API/Page ports, and confirm the cleanup path kills the child and removes the ignored runtime file.
  </verify>
  <acceptance_criteria>
    - `pnpm dev` can reach `http://localhost:8080/dashboard/crawlers` and a local proof task is claimable without manually starting a second runner process.
    - Runner startup cannot race API/Gateway readiness and runner failure stops owned services with one idempotent cleanup.
    - Callback key/secret and local runtime config are absent from logs, matrix JSON, summaries and tracked files.
  </acceptance_criteria>
</task>

<task type="integration" id="25-05-04">
  <name>Close the canonical Gateway proof and clean unfinished tasks</name>
  <files>
    scripts/phase25-dashboard-gateway-proof.ts
    scripts/phase25-dashboard-gateway-proof.test.ts
    scripts/local-task-runner.e2e.ts
  </files>
  <action>
    Run GitNexus impact analysis for `runPhase25DashboardGatewayProof`, `createFreshTask`, `executeActions` and `assertDashboardTrace` before editing. Keep the proof origin hard-coded to `http://localhost:8080` and require an explicit existing movie target ID for the live local proof (the proof must preflight the target/read model rather than inventing a movie). Update tuple and redaction assertions to accept the explicit local-proof provider for local contract evidence while continuing to require github-actions for production evidence scripts. Wait through a bounded poll/readback loop for the local runner to reach terminal lifecycle, validated receipt, accepted current and retained duplicate/conflict/stale/late history; never treat `queued`, `provider_not_configured`, local fixtures, or a direct-port response as a pass.

    After each action/proof branch, reload authoritative task detail through the Gateway, assert current/history/audit tuple consistency, refresh the Dashboard, and require a newer projection version plus retention of the prior current observation. Put cleanup in `finally`: cancel any non-terminal run, wait for cancellation readback, and record only a redacted cancellation fact. The matrix must include gateway path, provider identity, tuple IDs, statuses, reason codes, projection versions, history outcome codes and audit count, while excluding raw response data, URL/source material, cookies, commands, tokens, signatures, media and runtime config paths. Add deterministic tests for local provider acceptance, missing current checkpoint, timeout, history completeness, cache advancement, cleanup and production/local identity separation.
  </action>
  <verify>
    Run the focused proof tests, then run the canonical command with a disposable authenticated browser profile and an existing local movie target:
    `pnpm tsx scripts/phase25-dashboard-gateway-proof.ts --gateway-origin http://localhost:8080 --browser-profile TARGET_PROFILE --target-id TARGET_MOVIE_ID --evidence-dir ABSOLUTE_IGNORED_EVIDENCE_DIR`.
    Inspect the redacted matrix, rerun `pnpm check:services`, `git diff --check`, and run GitNexus `detect_changes` with the intended scope before any commit. A live run that remains queued, lacks provider/receipt/current, fails cleanup, or exposes a forbidden field is a failed verification, not a checkpoint pass.
  </verify>
  <acceptance_criteria>
    - One fresh local task/run/attempt/local-proof tuple is visible from Dashboard command through Gateway API/D1 detail, receipt, current availability, bounded history, audit and post-refresh readback.
    - Matrix `outcome` is passed only when action readbacks, current/history, dashboard trace, audit and cache-refresh advancement all pass; otherwise it records a bounded failure/checkpoint and cleanup status.
    - The final live task is terminal and no proof-created task remains queued/running; the matrix and phase artifacts contain no callback or provider-sensitive material.
    - Production GitHub Actions paths remain semantically distinct and their focused regression suites stay green.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Run the pre-edit GitNexus impact reports for every changed function/class/method and review all HIGH/CRITICAL warnings before implementation.
2. Execute the focused API, DB, crawler, config and proof tests from the task sections.
3. Run DB migration generation, inspect/apply the intended local D1 migration, then run DB/API/dashboard/crawler/script type-checks.
4. Start the local stack through the existing supervisor, use only the authenticated `http://localhost:8080` boundary, and run the fresh target proof with a bounded timeout.
5. Inspect the matrix for a local-proof tuple, current/history/audit/readback/cache advancement and redaction; verify cancellation/cleanup and no leaked active task.
6. Run `pnpm check:services`, `git diff --check`, and GitNexus `detect_changes` before any scoped commit. Do not claim Phase 25 complete until the UAT gap is cleared by the live matrix.
</verification>

<success_criteria>
- The authenticated local Gateway can create a fresh operation with an explicit local-proof provider and the task advances past queued under the supervised local runner.
- The availability current row is authoritative, revision/policy/tuple-bound and redacted; duplicate/conflict/stale/late observations remain history and cache invalidation follows authoritative readback.
- The Dashboard displays the same local-proof task/run/attempt/content/provider/current/history/audit facts after refresh, and the proof matrix is redacted.
- Local runner startup, failure, cancellation, signal handling and ignored runtime cleanup are deterministic and tested.
- GitHub Actions production provider behavior, Phase 24 proof identity and Phase 26-28 scope remain unchanged.
</success_criteria>

<rollback>
If a provider contract or migration change breaks an existing production path, stop before commit, preserve the failing tests and migration preview, and use the phase manifest/GSD undo workflow only after the exact affected files are reviewed. Never reset the mixed worktree. A local proof failure leaves the generated runtime directory and task IDs as bounded diagnostic evidence, then cleanup must cancel the run before retrying.
</rollback>
