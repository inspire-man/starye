---
phase: 04-progress
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/comic-app/src/types.ts
  - apps/comic-app/src/lib/api-client.ts
  - apps/comic-app/src/views/Reader.vue
  - apps/comic-app/src/views/Profile.vue
autonomous: true
requirements:
  - PROG-06
  - PROG-07
  - PROG-08

must_haves:
  truths:
    - "D-18: 章节读到最后一页时写 `completed=true`"
    - "D-19: 完成章节时 `position` 仍保留最后一页，不通过篡改存储位置实现重开回第一页"
    - "D-20: 已完成章节再次打开时默认回第一页"
    - "D-21: 已完成章节重新开始新会话时，`completed` 立即清掉"
    - "D-22: 已完成章节仍保留在阅读历史 / 进度列表中，不会完成后直接消失"
    - "D-28: 本 plan 只更新现有 comic 进度消费面与必要边界"
    - "D-29: 不新增 comic 首页继续阅读横幅或独立 comic 历史页"
    - "comic 进度读写使用真实 chapter identity，不再使用 `${slug}-${chapterId}` 这种拼接键"
    - "翻页/滚动进度保存使用 500ms debounce + `pagehide` flush"
    - "最后一页显式写 `completed=true`，完成记录仍保留最后一页位置"
    - "已完成章节再次打开默认回第一页；重新开始时清掉 completed"
    - "comic Profile 至少能读到稳定 progress 与 completed 语义，不再只是裸 chapterId 列表"
  artifacts:
    - path: "apps/comic-app/src/views/Reader.vue"
      provides: "reader restore/save/completed/pagehide semantics"
      contains: "pagehide"
    - path: "apps/comic-app/src/lib/api-client.ts"
      provides: "comic progress client aligned with unified API"
      contains: "/public/progress"
    - path: "apps/comic-app/src/views/Profile.vue"
      provides: "reading history consumes unified progress data"
      contains: "completed"
  key_links:
    - from: "apps/comic-app/src/views/Reader.vue"
      to: "apps/comic-app/src/lib/api-client.ts"
      via: "progressApi"
      pattern: "saveReadingProgress"
    - from: "apps/comic-app/src/views/Reader.vue"
      to: "apps/comic-app/src/types.ts"
      via: "ReadingProgress"
      pattern: "completed"
---

<objective>
把 comic-app 从“疑似错误的 chapter key + 1000ms scroll save + 无 completed 语义”的状态，收口为 Phase 4 需要的统一阅读进度：真实 chapter identity、500ms debounce、pagehide flush、最后一页 completed、已完成重开回第一页。

Purpose: 如果 comic 端继续用错误 chapter key 或只保存 page 而不保存 completed，统一 progress 表只会把旧问题搬到新表中，无法支撑“打开即恢复”和“完成后重读”的正式语义。

Output: comic progress client、Reader.vue、Profile.vue 与相关类型全部切到 unified progress contract。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-progress/04-CONTEXT.md
@.planning/phases/04-progress/04-RESEARCH.md
@.planning/phases/04-progress/04-PATTERNS.md
@.planning/phases/04-progress/04-VALIDATION.md
@.planning/phases/04-progress/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 comic progress 类型与 client，统一 chapter identity</name>
  <files>apps/comic-app/src/types.ts, apps/comic-app/src/lib/api-client.ts</files>
  <read_first>
    - apps/comic-app/src/types.ts
    - apps/comic-app/src/lib/api-client.ts
    - apps/api/src/schemas/progress.ts
    - .planning/phases/04-progress/04-RESEARCH.md
  </read_first>
  <action>
    1. 更新 `ReadingProgress` 类型到 unified contract：至少补上 `completed`，并确保字段语义与 API 一致。
    2. 让 `progressApi` 包装能够围绕真实 chapter identity 读写，不再默认接受 `${slug}-${chapterId}` 这种前端拼接键。
    3. 若 API history item 能提供章节标题 / 漫画 slug / contentId，优先在类型层显式承接，避免 Profile 继续只能展示裸 `chapterId`。
  </action>
  <verify>
    <automated>pnpm --filter @starye/comic-app exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/comic-app/src/types.ts` 的 progress 类型包含 `completed`
    - `apps/comic-app/src/lib/api-client.ts` 不再隐含 `${slug}-${chapterId}` 作为唯一保存键
    - comic-app typecheck 通过
  </acceptance_criteria>
  <done>
    comic progress 类型与 client 已能承接 unified contract。
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 Reader.vue 的恢复 / 保存 / completed 语义</name>
  <files>apps/comic-app/src/views/Reader.vue</files>
  <read_first>
    - apps/comic-app/src/views/Reader.vue
    - apps/comic-app/src/lib/api-client.ts
    - .planning/phases/04-progress/04-CONTEXT.md
    - .planning/phases/04-progress/04-RESEARCH.md
    - .planning/phases/04-progress/04-PATTERNS.md
  </read_first>
  <action>
    1. 读取进度时基于真实 chapter identity 获取 unified progress。
    2. 若记录 `completed=true`，再次进入章节默认从第 1 页开始，而不是直接跳回最后一页；但存储里的 `position` 仍保留最后一页。
    3. 把当前 1000ms debounce 调整为 500ms，并补 `pagehide` flush。
    4. 在读到最后一页时显式写 `completed=true`；重新开始阅读时及时清掉 `completed`。
    5. 不改变现有滚动式阅读模型，只收口 progress 语义。
  </action>
  <verify>
    <automated>pnpm --filter @starye/comic-app exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Reader.vue` 中不存在 `` `${route.params.slug}-${route.params.chapterId}` ``
    - `Reader.vue` 中存在 500ms debounce
    - `Reader.vue` 中存在 `pagehide` flush
    - `Reader.vue` 中存在显式 `completed` 处理
    - 已完成章节重开时的读取逻辑能回到第一页
  </acceptance_criteria>
  <done>
    Reader 已按 unified comic progress 语义恢复、保存与完成。
  </done>
</task>

<task type="auto">
  <name>Task 3: 收口 comic Profile 的阅读历史消费</name>
  <files>apps/comic-app/src/views/Profile.vue</files>
  <read_first>
    - apps/comic-app/src/views/Profile.vue
    - apps/comic-app/src/types.ts
    - apps/comic-app/src/lib/api-client.ts
    - .planning/phases/04-progress/04-CONTEXT.md
  </read_first>
  <action>
    1. 让 `Profile.vue` 消费 unified reading history 数据，而不是只展示裸 `chapterId + page`。
    2. 至少显式反映 `completed`，让“读完但保留在历史里”的 Phase 4 决策对用户可见。
    3. 不新增独立历史页，不把这里扩成复杂列表；保持轻量 Profile 信息面。
  </action>
  <verify>
    <automated>pnpm --filter @starye/comic-app exec vue-tsc --noEmit && rg -n "completed|chapterId" apps/comic-app/src/views/Profile.vue</automated>
  </verify>
  <acceptance_criteria>
    - `Profile.vue` 能读取 `completed`
    - `Profile.vue` 不再只有“章节 ID: xxx”这一种信息形态
    - comic-app typecheck 通过
  </acceptance_criteria>
  <done>
    comic Profile 已承接统一阅读历史语义。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser scroll/page events → progress API | 阅读器的频繁滚动事件会驱动保存 |
| Reader state → history UI | completed / page position 进入 history 后必须可解释 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | wrong chapter identity in Reader | mitigate | 统一使用真实 chapter identity，禁止继续用 `${slug}-${chapterId}` 拼接键污染 unified table |
| T-04-10 | Denial of Service | scroll 高频写库 | mitigate | 使用 500ms debounce + `pagehide` flush，不在每次 scroll frame 都发请求 |
| T-04-11 | Repudiation | completed chapter reopen ambiguity | mitigate | completed 显式持久化，读取逻辑明确“completed 重开回第一页，但存储位置仍保留最后页” |
| T-04-12 | Information Disclosure | profile history exposed to anonymous | accept | comic Profile 仍是登录页面，匿名门控由 Phase 4 路由收口 plan 处理 |

</threat_model>

<verification>
## 整体验证

```bash
pnpm --filter @starye/comic-app exec vue-tsc --noEmit
rg -n "completed|pagehide|500|route.params.slug-\\$\\{route.params.chapterId\\}" apps/comic-app/src/views/Reader.vue apps/comic-app/src/views/Profile.vue
```

</verification>

<success_criteria>
1. comic 进度使用真实 chapter identity
2. Reader 的 debounce / flush / completed 语义与 Phase 4 决策一致
3. 已完成章节重开回第一页的规则显式落地
4. comic Profile 能消费统一 history 语义
</success_criteria>

<output>
After completion, create `.planning/phases/04-progress/04-03-SUMMARY.md`
</output>
