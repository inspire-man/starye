---
phase: 04-progress
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - apps/movie-app/src/router.ts
  - apps/comic-app/src/router.ts
  - apps/movie-app/src/views/Home.vue
  - apps/movie-app/src/views/Profile.vue
  - apps/comic-app/src/views/Profile.vue
  - .planning/phases/04-progress/04-HUMAN-UAT.md
autonomous: true
requirements:
  - PROG-04
  - PROG-05
  - PROG-06
  - PROG-07
  - PROG-08

must_haves:
  truths:
    - "D-23: 匿名用户面对进度入口时，入口可见，但点击后直接跳 `/auth/login?next=...`"
    - "D-24: 匿名用户若直接访问进度页面本身，也应前置重定向到登录页"
    - "D-25: 门控落点只做到进度入口与进度页面路由，不把 Reader / Player 内部未登录不写扩成新系统"
    - "D-26: movie / comic 现有 toast 阻断统一改成 `next` 登录跳转语义"
    - "D-27: 继续复用现有 `useAuthGuard(nextPath?)`，不另造第二套 guard"
    - "D-28: 只更新现有进度消费面与必要入口"
    - "D-29: 不新增 comic 首页继续阅读横幅或独立 comic 历史页"
    - "匿名访问进度页面或点击进度入口时，直接跳 `/auth/login?next=...`，不再 toast 阻断"
    - "movie/comic 两个 app 的 progress 门控都复用现有 `useAuthGuard(nextPath?)`"
    - "Phase 4 人工验收覆盖 movie 标准源、`streamUrl`、comic 最后一页完成、pagehide flush 和重开语义"
    - "迁移验证明确确认旧表已退场，新表可读写"
  artifacts:
    - path: "apps/movie-app/src/router.ts"
      provides: "history/profile routes redirect to login with next"
      contains: "/auth/login?next="
    - path: "apps/comic-app/src/router.ts"
      provides: "profile route redirects to login with next"
      contains: "/auth/login?next="
    - path: ".planning/phases/04-progress/04-HUMAN-UAT.md"
      provides: "Phase 4 manual verification checklist"
      contains: "pagehide"
  key_links:
    - from: "apps/movie-app/src/router.ts"
      to: "apps/movie-app/src/composables/useAuthGuard.ts"
      via: "requireLogin"
      pattern: "useAuthGuard"
    - from: "apps/comic-app/src/router.ts"
      to: "apps/comic-app/src/composables/useAuthGuard.ts"
      via: "requireLogin"
      pattern: "useAuthGuard"
---

<objective>
在 unified progress 功能落地后，最后收口门控与验收：把 movie/comic 的 progress 页面和入口都改成 `next` 登录跳转，补齐 human UAT，并显式验证 migration 后旧表退场与新表可用。

Purpose: Phase 4 的代码即使写完，如果门控还停留在 toast 阻断、或者没有验证 `pagehide` / completed / 重开语义，这个 phase 仍然不能算“已计划可执行”。

Output: 两个 app 的 progress 门控一致化、`04-HUMAN-UAT.md`、Phase 4 的最终人工与迁移验证路径。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-progress/04-CONTEXT.md
@.planning/phases/04-progress/04-RESEARCH.md
@.planning/phases/04-progress/04-PATTERNS.md
@.planning/phases/04-progress/04-VALIDATION.md
@.planning/phases/04-progress/04-02-PLAN.md
@.planning/phases/04-progress/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一 movie/comic progress 页面与入口门控</name>
  <files>apps/movie-app/src/router.ts, apps/comic-app/src/router.ts, apps/movie-app/src/views/Home.vue, apps/movie-app/src/views/Profile.vue, apps/comic-app/src/views/Profile.vue</files>
  <read_first>
    - apps/movie-app/src/router.ts
    - apps/comic-app/src/router.ts
    - apps/movie-app/src/composables/useAuthGuard.ts
    - apps/comic-app/src/composables/useAuthGuard.ts
    - apps/movie-app/src/views/Home.vue
    - apps/movie-app/src/views/Profile.vue
    - apps/comic-app/src/views/Profile.vue
    - .planning/phases/04-progress/04-CONTEXT.md
  </read_first>
  <action>
    1. 把 `apps/movie-app/src/router.ts` 中对 `/history`、`/profile` 等 progress 相关页面的未登录处理改成直接登录跳转，不再 `showToast(...) + next(false)`。
    2. 把 `apps/comic-app/src/router.ts` 中对 progress 相关页面的未登录处理改成直接登录跳转，不再 `warning(...) + next(false)`。
    3. 现有进度入口若仍有点击后 toast 阻断，也统一改成 `useAuthGuard(nextPath?)`；入口可见，但匿名点击直接跳登录页。
    4. 不把 Reader / Player 内部“未登录不写”扩成全站新 guard，只收口 progress 入口与 progress 页面。
  </action>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vue-tsc --noEmit && pnpm --filter @starye/comic-app exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/movie-app/src/router.ts` 中不再出现 `showToast('请先登录以访问该页面'`
    - `apps/comic-app/src/router.ts` 中不再出现 `warning('请先登录以访问个人中心'`
    - 两个 router 对未登录 progress 页面都能构造 `/auth/login?next=...`
    - progress 相关入口点击逻辑复用 `useAuthGuard`
  </acceptance_criteria>
  <done>
    progress 门控已统一为 `next` 登录跳转语义。
  </done>
</task>

<task type="auto">
  <name>Task 2: 编写 Phase 4 human UAT，并显式覆盖 pagehide / completed / reopen 语义</name>
  <files>.planning/phases/04-progress/04-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/04-progress/04-CONTEXT.md
    - .planning/phases/04-progress/04-VALIDATION.md
    - .planning/phases/03-movie-app-r2/03-HUMAN-UAT.md
  </read_first>
  <action>
    1. 新建 `04-HUMAN-UAT.md`，覆盖至少以下场景：
       - 标准 movie 播放到中段后关闭标签页，重开恢复位置
       - `streamUrl` / TorrServer 模式下的保存与恢复
       - movie 看完后再次打开从头开始
       - comic 中途阅读后关闭标签页，重开恢复页码
       - comic 读到最后一页后再次打开回第一页
       - 匿名访问 `/history` / comic progress 页面与入口点击时跳登录页
    2. 每个场景写清前置条件、步骤、预期结果、通过/失败记录位。
  </action>
  <verify>
    <automated>Test-Path '.planning\\phases\\04-progress\\04-HUMAN-UAT.md'</automated>
  </verify>
  <acceptance_criteria>
    - `04-HUMAN-UAT.md` 文件存在
    - 文档中包含 `pagehide`
    - 文档中包含 `streamUrl` 或 `TorrServer`
    - 文档中包含“已完成后再次打开”
    - 文档中包含匿名门控验证步骤
  </acceptance_criteria>
  <done>
    Phase 4 的人工验收脚本已覆盖所有关键用户语义。
  </done>
</task>

<task type="auto">
  <name>Task 3: 做 migration / route / UI 的最终 smoke 验证说明</name>
  <files>.planning/phases/04-progress/04-HUMAN-UAT.md</files>
  <read_first>
    - packages/db/MIGRATION.md
    - .agents/skills/starye-db-migration/SKILL.md
    - .planning/phases/04-progress/04-01-PLAN.md
    - .planning/phases/04-progress/04-VALIDATION.md
  </read_first>
  <action>
    1. 在 `04-HUMAN-UAT.md` 或同文档验证段落中加入 migration smoke：
       - 应用本地 D1 migration
       - 检查 `progress` 表存在
       - 检查 `reading_progress` / `watching_progress` 不再存在
    2. 写明本 phase 执行结束前至少要跑哪些自动化命令和哪些人工步骤，作为 verify-work 前 gate。
  </action>
  <verify>
    <automated>rg -n "reading_progress|watching_progress|progress 表|wrangler d1" .planning/phases/04-progress/04-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `04-HUMAN-UAT.md` 中存在本地 migration 验证步骤
    - 文档明确要求验证 `progress` 表存在
    - 文档明确要求验证旧 `reading_progress` / `watching_progress` 已退场
    - 文档列出 verify-work 前必跑自动化命令
  </acceptance_criteria>
  <done>
    Phase 4 最终 smoke / migration / UAT gate 已被写成可执行清单。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous browser → protected progress routes | 未登录用户只能被引导去登录，不应停留在假空态或 toast 阻断 |
| Migration operator → local D1 | 旧表删除后必须确认 unified 表真实可用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Spoofing | progress page auth gate | mitigate | router 与入口统一复用 `useAuthGuard(nextPath?)`，直接跳 `/auth/login?next=...` |
| T-04-14 | Repudiation | migration 完成但旧表残留 | mitigate | human UAT 中加入 D1 schema smoke，显式检查旧表退场与新表存在 |
| T-04-15 | Information Disclosure | anonymous user sees stale personal progress UI | mitigate | protected progress 页面前置重定向登录，不显示伪空态 |

</threat_model>

<verification>
## 整体验证

```bash
pnpm --filter @starye/movie-app exec vue-tsc --noEmit
pnpm --filter @starye/comic-app exec vue-tsc --noEmit
rg -n "/auth/login\\?next=|showToast\\(|warning\\(" apps/movie-app/src/router.ts apps/comic-app/src/router.ts
rg -n "pagehide|reading_progress|watching_progress|wrangler d1" .planning/phases/04-progress/04-HUMAN-UAT.md
```

</verification>

<success_criteria>
1. progress 页面与入口门控统一为 `next` 登录跳转
2. `04-HUMAN-UAT.md` 覆盖 movie/comic/pagehide/completed/reopen/anonymous gate
3. migration smoke 明确验证旧表退场与新表存在
</success_criteria>

<output>
After completion, create `.planning/phases/04-progress/04-04-SUMMARY.md`
</output>
