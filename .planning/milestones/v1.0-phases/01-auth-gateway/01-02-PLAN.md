---
phase: 01-auth-gateway
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/gateway/src/cache-middleware.ts
  - apps/gateway/src/__tests__/cache-middleware.test.ts
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
  - AUTH-07
tags: [gateway, cache, bypass, security]
must_haves:
  truths:
    - "Gateway 收到带 Cookie 或 Authorization 头的请求 → 100% bypass KV 读写"
    - "`/api/auth/*` 无论请求带头与否都 bypass（NO_STORE_PREFIXES 独立命中，D-08 第二层）"
    - "响应 `Set-Cookie` 时 shouldCacheResponse 仍返回 false（D-09 防线保留）"
    - "D-12: cache-middleware.ts 中 PRIVATE scope 所有分支被物理删除（PRIVATE_CACHE_PREFIXES 常量 + resolveBasePolicy 私有分支 + createCacheKey userScope hash + decorateResponse Vary: Cookie 分支，四处同时清）"
    - "D-13: CacheScope 收窄为 'public' | 'bypass'；CacheGroup 不含 'favorites'"
    - "D-14: hashValue 函数本体保留（静态资源等后续可能复用），仅移除它在 cookie 上的调用点（createCacheKey 内 userScope = hashValue(cookie) 分支删除）"
    - "响应头 `X-Cache-Reason: auth-headers` 在 BYPASS 因请求头触发时被注入（D-10）"
  artifacts:
    - path: "apps/gateway/src/cache-middleware.ts"
      provides: "新的 cache policy：按请求头 bypass，私有 scope 代码已清理"
      contains: "hasAuthHeaders"
    - path: "apps/gateway/src/cache-middleware.ts"
      provides: "X-Cache-Reason 响应头常量与注入点"
      contains: "X-Cache-Reason"
    - path: "apps/gateway/src/__tests__/cache-middleware.test.ts"
      provides: "D-11 #1..#4 四条完整测试（从 it.todo 转为活用例）"
      contains: "X-Cache-Reason"
  key_links:
    - from: "apps/gateway/src/cache-middleware.ts resolveCachePolicy"
      to: "apps/gateway/src/cache-middleware.ts decorateResponse"
      via: "CachePolicy.bypassReason 字段串联 D-07 检测结果与 D-10 响应头"
      pattern: "bypassReason"
    - from: "apps/gateway/src/cache-middleware.ts createCachedProxy"
      to: "Workers KV binding"
      via: "带头请求的 shouldStore=false 路径保证零 KV 读写"
      pattern: "policy\\.shouldStore"
---

<objective>
落实 D-07 / D-10 / D-12 / D-13 / D-14：Gateway `resolveCachePolicy` 按请求头触发 bypass，删除 private scope 全部死代码，新增 `X-Cache-Reason` 诊断头，并把 Plan 01 留下的 D-11 四条 it.todo 转为完整用例。

Purpose: 闭合 T1（缓存投毒）/T2 的一部分（better-auth session 与缓存的一致性）。这是 AUTH-07（核心）+ AUTH-06（回归）+ AUTH-05（保 Cookie 透传，对已有行为回归）三个需求在代码侧的唯一改动点。

Output:
- `apps/gateway/src/cache-middleware.ts` 内容修改：类型收窄、私有 scope 删除、header-based bypass、X-Cache-Reason 注入
- `apps/gateway/src/__tests__/cache-middleware.test.ts` 骨架填充 + 既有 `isolates favorites cache per user cookie` it 删除
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-gateway/01-CONTEXT.md
@.planning/phases/01-auth-gateway/01-RESEARCH.md
@.planning/phases/01-auth-gateway/01-PATTERNS.md
@.planning/phases/01-auth-gateway/01-VALIDATION.md
@.planning/phases/01-auth-gateway/01-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- 核心类型变更：收窄 + 删除 -->

**Before:**
```ts
type CacheScope = 'public' | 'private' | 'bypass'
type CacheGroup = 'admin' | 'api' | 'auth' | 'favorites' | 'misc' | 'movies' | 'public-pages' | 'static-assets'
```

**After:**
```ts
type CacheScope = 'public' | 'bypass'
type CacheGroup = 'admin' | 'api' | 'auth' | 'misc' | 'movies' | 'public-pages' | 'static-assets'
type CacheBypassReason = 'auth-headers' | 'no-store-path' | 'set-cookie-response' | 'cache-bypass-option' | 'non-cacheable-group'

interface CachePolicy {
  scope: CacheScope
  group: CacheGroup
  ttl?: number
  staleWhileRevalidate?: number
  cacheControl?: string
  shouldStore: boolean
  bypassReason?: CacheBypassReason  // 新增：供 decorateResponse 注入 X-Cache-Reason
}
```

`buildCacheControl` 签名：`scope: 'public', ttl: number, staleWhileRevalidate?: number` —— 去掉 `'private'` 分支。

**响应头常量：** 在既有 `CACHE_STATUS_HEADER` 等常量旁新增：
```ts
const CACHE_REASON_HEADER = 'X-Cache-Reason'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重构 cache-middleware.ts — header bypass + private scope 清理 + X-Cache-Reason</name>
  <read_first>
    - apps/gateway/src/cache-middleware.ts（整文件，重点 L12-13, L42-51, L53-62, L109-177, L179-196, L202-213, L215-223, L339-371）
    - .planning/phases/01-auth-gateway/01-RESEARCH.md L234-290（Gateway Cache Bypass Implementation 精确改动位点）
    - .planning/phases/01-auth-gateway/01-CONTEXT.md L38-51（D-07/D-09/D-10/D-12/D-13/D-14 原文）
    - .planning/phases/01-auth-gateway/01-PATTERNS.md L31-85（分析同文件 analog 说明与插入点）
    - apps/gateway/src/index.ts（L123-141，理解 proxy 层对 cache 层的调用 —— 本 task 不改此文件，但要理解边界）
  </read_first>
  <behavior>
    新用例（由 Task 2 在 cache-middleware.test.ts 里实装）：
    - Test D-11 #1: 无头 GET `/api/movies` → 首次 MISS，二次 HIT（回归）
    - Test D-11 #2: 带 `cookie` 头 GET `/api/movies` → BYPASS + `X-Cache-Reason: auth-headers` + 不写 KV（calls 连续 +1）
    - Test D-11 #3: 带 `authorization` 头 GET `/api/movies` → 同上
    - Test D-11 #4: GET `/api/auth/get-session` 有/无 cookie 两 case → 都 BYPASS（bypassReason 为 `no-store-path`）
    - 既有 Set-Cookie 不缓存测试（L150-160 invalidates cache entries by prefix 里含 `favorites:private` key 的 store mock）需同步更新
  </behavior>
  <action>
    对 `apps/gateway/src/cache-middleware.ts` 做以下精确改动。严格按顺序执行，避免 TypeScript 漂移。

    **Step 1：类型收窄（D-13）**
    - L12 `type CacheScope = 'public' | 'private' | 'bypass'` → `type CacheScope = 'public' | 'bypass'`
    - L13 `type CacheGroup` 删 `'favorites'`（保留其余 7 项）
    - 在 L13 下方新增：
      ```ts
      type CacheBypassReason = 'auth-headers' | 'no-store-path' | 'set-cookie-response' | 'cache-bypass-option' | 'non-cacheable-group'
      ```
    - `interface CachePolicy`（L15-22）新增可选字段 `bypassReason?: CacheBypassReason`

    **Step 2：删除 PRIVATE_CACHE_PREFIXES 常量（D-12）**
    - 整行删 L49 `const PRIVATE_CACHE_PREFIXES = ['/api/favorites', '/api/history']`

    **Step 3：新增 CACHE_REASON_HEADER 常量（D-10）**
    - 在 L44-47 的响应头常量组中紧随 `CACHE_TTL_HEADER` 之后插入：
      ```ts
      const CACHE_REASON_HEADER = 'X-Cache-Reason'
      ```

    **Step 4：收窄 buildCacheControl 签名（D-13）**
    - L53 签名改为：
      ```ts
      function buildCacheControl(scope: 'public', ttl: number, staleWhileRevalidate?: number): string
      ```
    - L54 `const visibility = scope === 'private' ? 'private' : 'public'` → 删除该行，直接用 `'public'`：
      ```ts
      const directives = [`public, max-age=${ttl}`]
      ```
    - L57-59 `stale-while-revalidate` 逻辑保持

    **Step 5：删除 resolveBasePolicy 里 PRIVATE 分支（D-12）**
    - 整块删 L142-151（`if (PRIVATE_CACHE_PREFIXES.some(prefix => pathname.startsWith(prefix))) { ... }`）
    - 删除后 `/api/favorites` 和 `/api/history` 自然落入 L153 `pathname.startsWith('/api')` 分支，返回 `bypass / api / no-store` —— 符合 D-12 推论

    **Step 6：resolveBasePolicy 每个 return 都标注 bypassReason（D-10 支撑）**
    - 静态资源分支（L112-119）：return 里加 `bypassReason: 'non-cacheable-group'`（或保持无值 —— 本 task 允许静态资源不带 reason，因为 decorateResponse 只在 BYPASS 且有 reason 时注入）
    - NO_STORE_PREFIXES 分支（L122-129）：return 里加 `bypassReason: 'no-store-path'`
    - MOVIE_LIST_PATHS 分支（L131-140）：**不加** bypassReason（这里 shouldStore=true）
    - `/api` 默认分支（L153-160）：加 `bypassReason: 'non-cacheable-group'`
    - isPublicDocumentPath 分支（L162-170）：不加（shouldStore=false 但非 bypass 语义，保持原状）
    - misc 分支（L172-176）：加 `bypassReason: 'non-cacheable-group'`

    **Step 7：resolveCachePolicy 新增 header 检测（D-07，核心）**
    - L179-196 整体重构为：
      ```ts
      export function resolveCachePolicy(request: Request, options?: CacheOptions): CachePolicy {
        const url = new URL(request.url)
        const basePolicy = resolveBasePolicy(url)

        // D-07: 带 Cookie 或 Authorization 的请求强制 bypass，覆盖路径 policy
        const hasAuthHeaders = request.headers.has('cookie') || request.headers.has('authorization')

        const ttl = options?.ttl ?? basePolicy.ttl
        const shouldStore = !options?.bypassCache && !hasAuthHeaders && basePolicy.shouldStore
        const scope: CacheScope = shouldStore ? basePolicy.scope : 'bypass'

        // 计算 bypassReason：优先级 auth-headers > options.bypassCache > basePolicy.bypassReason
        let bypassReason: CacheBypassReason | undefined
        if (!shouldStore) {
          if (hasAuthHeaders) bypassReason = 'auth-headers'
          else if (options?.bypassCache) bypassReason = 'cache-bypass-option'
          else bypassReason = basePolicy.bypassReason
        }

        return {
          ...basePolicy,
          scope,
          ttl,
          shouldStore,
          bypassReason,
          cacheControl: ttl && basePolicy.scope === 'public' && shouldStore
            ? buildCacheControl('public', ttl, basePolicy.staleWhileRevalidate)
            : basePolicy.cacheControl,
        }
      }
      ```

    **Step 8：createCacheKey 删除 userScope 分支（D-12）**
    - 整块删 L208-210（`const userScope = policy.scope === 'private' ? ... : ''`）
    - L212 return 改为不含 `${userScope}`：
      ```ts
      return `${GATEWAY_CACHE_PREFIX}:${policy.group}:${policy.scope}:${encodedKey}`
      ```
    - 注意：`hashValue` 函数本体（L81-90）**保留**，D-14 明确只删调用点

    **Step 9：shouldCacheResponse 保留（D-09 防线）**
    - L215-223 不改
    - 但建议在函数开头添加注释说明为什么保留：
      ```ts
      // D-09 防线：即便 D-07 未来被改坏，带 Set-Cookie 的响应也绝不写入 KV
      ```

    **Step 10：decorateResponse 注入 X-Cache-Reason + 删 Vary: Cookie 分支（D-10 / D-12）**
    - L346-353 既有 header set 之后插入：
      ```ts
      if (cacheStatus === 'BYPASS' && policy.bypassReason) {
        headers.set(CACHE_REASON_HEADER, policy.bypassReason)
      }
      ```
    - 整块删 L358-360（`if (policy.scope === 'private') { mergeVaryHeader(headers, 'Cookie') }`）
    - `mergeVaryHeader` 函数本体（L92-107）**保留**（未来可能复用于静态资源 `Vary: Accept-Encoding`）

    **Step 11：set-cookie 响应 bypassReason 传递（W3 修订：从"可选 polish"升为必做）**
    - 在 `createCachedProxy` 里找到 proxy 返回后判断 `shouldCacheResponse(response)` 为 false 的分支（约 L405-420，`decorateResponse(response, policy, 'BYPASS')`）：如果当前 policy 的 bypassReason 未设但响应有 `set-cookie`，把 policy 克隆一份加上 `bypassReason: 'set-cookie-response'` 再传给 decorateResponse —— 这样 Set-Cookie 拦截路径也能输出 X-Cache-Reason 诊断。
    - **实现要求（示意）：**
      ```ts
      const canStore = shouldCacheResponse(response)
      const finalPolicy = !canStore && response.headers.has('set-cookie') && !policy.bypassReason
        ? { ...policy, bypassReason: 'set-cookie-response' as CacheBypassReason, scope: 'bypass' as CacheScope, shouldStore: false }
        : policy
      return decorateResponse(response, finalPolicy, canStore ? 'HIT' : 'BYPASS', ...)
      ```
    - 如果设置了 bypassReason 就透传，保留 D-07 触发的 `auth-headers` 优先级高于 `set-cookie-response`。

    **类型驱动验证：** 改完 Step 1 后，TypeScript 会把所有 `'private'` 引用点标红；逐一按 Step 5/8/10 处理。如改完后仍报 `'private'` 不可赋值，说明漏了某个位点，回到 Step 1 检查 grep 结果。
  </action>
  <verify>
    <automated>cd apps/gateway && pnpm type-check 2>&1 | grep -c "error TS" | awk '$1 == 0 { exit 0 } $1 > 0 { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'private'" apps/gateway/src/cache-middleware.ts` 返回 `0`（所有 private 引用已删）
    - `grep -c "PRIVATE_CACHE_PREFIXES" apps/gateway/src/cache-middleware.ts` 返回 `0`
    - `grep -c "'favorites'" apps/gateway/src/cache-middleware.ts` 返回 `0`
    - `grep -c "hasAuthHeaders" apps/gateway/src/cache-middleware.ts` 返回 `1`
    - `grep -c "CACHE_REASON_HEADER" apps/gateway/src/cache-middleware.ts` >= `2`（定义 + 使用）
    - **W3 修订：** `grep -c "bypassReason" apps/gateway/src/cache-middleware.ts` >= `6`（类型 1 + CachePolicy 字段 1 + resolveBasePolicy 多分支标签 + resolveCachePolicy 写值 + decorateResponse 读取 + Step 11 set-cookie 克隆）
    - **W3 修订：每档 bypassReason 枚举值都必须出现在源码中，不是死码：**
      - `grep -c "bypassReason: 'no-store-path'" apps/gateway/src/cache-middleware.ts` >= `1`
      - `grep -c "bypassReason: 'non-cacheable-group'" apps/gateway/src/cache-middleware.ts` >= `1`
      - `grep -c "bypassReason: 'set-cookie-response'" apps/gateway/src/cache-middleware.ts` >= `1`
      - `grep -c "bypassReason = 'auth-headers'\|bypassReason: 'auth-headers'" apps/gateway/src/cache-middleware.ts` >= `1`
    - `grep -c "mergeVaryHeader" apps/gateway/src/cache-middleware.ts` == `1`（函数定义保留，但 decorateResponse 内调用已删）
    - `grep -n "hashValue" apps/gateway/src/cache-middleware.ts` 显示函数定义仍在但无调用（只剩定义行）
    - `pnpm --filter @starye/gateway type-check` 零错误
    - `pnpm --filter @starye/gateway lint` 零错误
  </acceptance_criteria>
  <done>类型收窄成功；私有 scope 所有位点物理删除；hasAuthHeaders 检测与 X-Cache-Reason 注入到位；type-check + lint 全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 填充 cache-middleware.test.ts D-11 四条用例 + 清理 favorites 旧 mock</name>
  <read_first>
    - apps/gateway/src/__tests__/cache-middleware.test.ts（整文件 — Plan 01 已加 4 条 it.todo，本 task 负责转为活用例）
    - apps/gateway/src/cache-middleware.ts（Task 1 已改 — 用来核对 BYPASS 时响应头值）
    - .planning/phases/01-auth-gateway/01-VALIDATION.md L57-66（D-11 矩阵期望值）
    - .planning/phases/01-auth-gateway/01-PATTERNS.md L113-148（测试骨架模板）
  </read_first>
  <behavior>
    - D-11 #1: 无头 `/api/movies` → 首次 MISS，二次 HIT，`calls === 1`（回归既有行为）
    - D-11 #2: 带 `cookie: starye.session_token=xxx` `/api/movies` → 首次 BYPASS + `X-Cache-Reason: auth-headers`，二次仍 BYPASS，`calls === 2`
    - D-11 #3: 带 `authorization: Bearer xxx` `/api/movies` → 同上
    - D-11 #4a: `GET /api/auth/get-session` 无 cookie → BYPASS + `X-Cache-Reason: no-store-path`
    - D-11 #4b: `GET /api/auth/get-session` 带 cookie → BYPASS + `X-Cache-Reason: auth-headers`（因为 auth-headers 优先级高于 no-store-path —— 参照 Task 1 Step 7）
    - 既有 "isolates favorites cache per user cookie" 测试**整块删除**（private scope 已不存在）
    - L150-160 "invalidates cache entries by prefix" 里的 `gateway-cache:v2:favorites:private:abc:...` key 改为真实 public key（例如 `gateway-cache:v2:movies:public:%2Fapi%2Fmovies%3Fpage%3D2`），断言相应修改（删除数从 1 改为 1，剩余键改为新 mock）
  </behavior>
  <action>
    在 `apps/gateway/src/__tests__/cache-middleware.test.ts` 做以下改动：

    **Step 1：整块删除既有 `it('isolates favorites cache per user cookie', ...)`（L68-106）**

    **Step 2：把 Plan 01 的四条 `it.todo` 替换为完整 `it(...)`**

    ```ts
    it('D-11 #1: caches /api/movies on public group when no auth headers present (MISS then HIT)', async () => {
      const { kv } = createMockKv()
      let calls = 0
      const cachedProxy = createCachedProxy(kv, async () => {
        calls += 1
        return Response.json({ calls })
      })
      const request = new Request('https://starye.org/api/movies')

      const miss = await cachedProxy(request, 'https://api.starye.org')
      expect(miss.headers.get('X-Cache-Status')).toBe('MISS')
      const hit = await cachedProxy(request, 'https://api.starye.org')
      expect(hit.headers.get('X-Cache-Status')).toBe('HIT')
      expect(calls).toBe(1)
    })

    it('D-11 #2: bypasses KV on /api/movies when request has Cookie header (BYPASS, X-Cache-Reason=auth-headers)', async () => {
      const { kv } = createMockKv()
      let calls = 0
      const cachedProxy = createCachedProxy(kv, async () => {
        calls += 1
        return Response.json({ calls })
      })
      const request = new Request('https://starye.org/api/movies', {
        headers: { cookie: 'starye.session_token=xxx' },
      })

      const r1 = await cachedProxy(request, 'https://api.starye.org')
      expect(r1.headers.get('X-Cache-Status')).toBe('BYPASS')
      expect(r1.headers.get('X-Cache-Reason')).toBe('auth-headers')

      const r2 = await cachedProxy(request, 'https://api.starye.org')
      expect(r2.headers.get('X-Cache-Status')).toBe('BYPASS')
      expect(calls).toBe(2) // 不写 KV → 每次都穿透
    })

    it('D-11 #3: bypasses KV on /api/movies when request has Authorization header (BYPASS, X-Cache-Reason=auth-headers)', async () => {
      const { kv } = createMockKv()
      let calls = 0
      const cachedProxy = createCachedProxy(kv, async () => {
        calls += 1
        return Response.json({ calls })
      })
      const request = new Request('https://starye.org/api/movies', {
        headers: { authorization: 'Bearer xxx' },
      })

      const r1 = await cachedProxy(request, 'https://api.starye.org')
      expect(r1.headers.get('X-Cache-Status')).toBe('BYPASS')
      expect(r1.headers.get('X-Cache-Reason')).toBe('auth-headers')

      const r2 = await cachedProxy(request, 'https://api.starye.org')
      expect(calls).toBe(2)
    })

    it('D-11 #4: bypasses /api/auth/get-session regardless of request headers', async () => {
      const { kv } = createMockKv()
      const cachedProxy = createCachedProxy(kv, async () =>
        Response.json({ session: null }))

      // 无 cookie — NO_STORE_PREFIXES 命中
      const noCookie = await cachedProxy(
        new Request('https://starye.org/api/auth/get-session'),
        'https://api.starye.org',
      )
      expect(noCookie.headers.get('X-Cache-Status')).toBe('BYPASS')
      expect(noCookie.headers.get('X-Cache-Reason')).toBe('no-store-path')

      // 带 cookie — auth-headers 优先级高于 no-store-path
      const withCookie = await cachedProxy(
        new Request('https://starye.org/api/auth/get-session', {
          headers: { cookie: 'starye.session_token=xxx' },
        }),
        'https://api.starye.org',
      )
      expect(withCookie.headers.get('X-Cache-Status')).toBe('BYPASS')
      expect(withCookie.headers.get('X-Cache-Reason')).toBe('auth-headers')
    })
    ```

    **Step 3：更新 "invalidates cache entries by prefix" 测试（L150-160 在删 favorites 后行号会变）**
    - 找到该 it 块里的 `store.set('gateway-cache:v2:favorites:private:abc:%2Fapi%2Ffavorites', 'favorite-cache')` 行
    - 改为：`store.set('gateway-cache:v2:api:bypass:%2Fapi%2Fhistory', 'history-cache')`
    - 断言的 expect 数组相应更新为剩余 key：`['gateway-cache:v2:api:bypass:%2Fapi%2Fhistory']`
    - 注意：`invalidateCache(kv, 'gateway-cache:v2:movies:')` 调用不变，期望删除数仍为 1

    **关键审视：** `buildCacheControl` 改签名后 `Cache-Control` 值仅 `public, max-age=...`，既有 D-11 #1 的 `Cache-Control` contain `max-age=300` 断言不受影响。如果有测试用到 `private, max-age` 字样，同步更新。
  </action>
  <verify>
    <automated>cd apps/gateway && pnpm test -- cache-middleware.test.ts --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @starye/gateway test -- cache-middleware.test.ts --run` 全部通过（不再有 todo / skip，且 4 条 D-11 用例绿）
    - `grep -c "isolates favorites cache per user cookie" apps/gateway/src/__tests__/cache-middleware.test.ts` 返回 `0`
    - `grep -c "it('D-11 #" apps/gateway/src/__tests__/cache-middleware.test.ts | grep -v todo` 返回 `4`（四条完整 it）
    - `grep -c "it.todo('D-11" apps/gateway/src/__tests__/cache-middleware.test.ts` 返回 `0`（todo 已全部转正）
    - `grep -c "favorites:private" apps/gateway/src/__tests__/cache-middleware.test.ts` 返回 `0`
    - `grep -c "X-Cache-Reason" apps/gateway/src/__tests__/cache-middleware.test.ts` >= `4`（至少四条用例都断言该头）
    - 总 it 数量：测试 runner 输出至少 7 个 passed（原 4 条保留 + 删 1 条 favorites + 新加 4 条 D-11 = 7）
  </acceptance_criteria>
  <done>cache-middleware.test.ts 全绿；四条 D-11 用例均为活用例；favorites 私有 scope 相关用例与 mock 全部清零。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → Gateway | untrusted Cookie/Authorization 头到达 Worker fetch handler |
| Gateway → KV | 本 plan 的核心：决定哪些响应允许落 KV |
| Gateway → API Worker | proxy 透传请求头（Plan 不改 index.ts，但依赖其行为） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 (T1) | Information Disclosure | `resolveCachePolicy` / `createCacheKey` / `shouldCacheResponse` 三层 | mitigate | D-07 `hasAuthHeaders` 拦截写入路径；D-09 `shouldCacheResponse` 第二道防线；D-12 删除 private scope 消除"按 cookie 切片"的代码路径；D-11 #2/#3 单测回归 |
| T-01-06 (T1 变种) | Information Disclosure | `createCacheKey` 的 key 结构 | mitigate | userScope 调用点删除（D-14）；即便未来 hash 函数回归误用，测试回归 D-11 #2 会立刻抓到（BYPASS 时不产生 key） |
| T-01-07 (T1 set-cookie) | Information Disclosure | `shouldCacheResponse` 的 Set-Cookie 屏蔽 | accept（已有行为保留 + 注释标记 D-09 防线） | L215 处保留逻辑；bypassReason `'set-cookie-response'` 使此路径在诊断上可见 |
| T-01-08 | Tampering | `decorateResponse` 响应头注入 | mitigate | 新增 `X-Cache-Reason` 仅在 BYPASS 时输出，且值为枚举，不包含用户输入 |
</threat_model>

<verification>
```bash
# Task 1 静态检查
cd apps/gateway && pnpm type-check
cd apps/gateway && pnpm lint

# Task 1 + 2 测试
pnpm --filter @starye/gateway test

# 全仓回归（确保未破坏 api / 其他包）
pnpm -r test

# grep 质检
grep -c "'private'" apps/gateway/src/cache-middleware.ts   # 0
grep -c "PRIVATE_CACHE_PREFIXES" apps/gateway/src/cache-middleware.ts  # 0
grep -c "hasAuthHeaders" apps/gateway/src/cache-middleware.ts           # 1
grep -c "X-Cache-Reason" apps/gateway/src/cache-middleware.ts           # >= 2
```
</verification>

<success_criteria>
- AUTH-07：带 cookie / authorization 的 `/api/movies` 请求触发 BYPASS 且响应含 `X-Cache-Reason: auth-headers`
- AUTH-06：`/api/auth/*` 路径无论头如何都 BYPASS
- AUTH-05（回归）：本 plan 不改 `apps/gateway/src/index.ts`，Cookie/Set-Cookie 透传行为保持
- 私有 scope 代码路径完全删除，`CacheScope` 类型仅剩 `'public' | 'bypass'`
- Plan 01 留下的 4 条 `it.todo` 全部转为通过的 `it`
- `pnpm --filter @starye/gateway test` 绿；`pnpm -r test` 绿
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-gateway/01-02-SUMMARY.md` recording:
- cache-middleware.ts 删除/新增的精确行数统计
- D-11 四条测试的实际断言（供 Plan 06 冒烟引用）
- bypassReason 枚举值的最终命名（可能偏离 interfaces 示例）
</output>
