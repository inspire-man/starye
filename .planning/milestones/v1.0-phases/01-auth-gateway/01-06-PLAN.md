---
phase: 01-auth-gateway
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - apps/dashboard/e2e/auth-crosspath.spec.ts
  - .planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
tags: [smoke, e2e, phase-gate, manual]
user_setup:
  - service: github-oauth
    why: "D-19 step 2 需要真实 GitHub 账号授权才能完成 OAuth 往返"
    dashboard_config:
      - task: "使用作者 GitHub 账号完成 /auth/login 流程"
        location: "https://starye.org/auth/login（或本地 http://localhost:8080/auth/login）"
must_haves:
  truths:
    - "`apps/dashboard/e2e/auth-crosspath.spec.ts` Plan 01 骨架中的 test.skip 转为活用例并全绿"
    - "D-19 六步冒烟 checklist 以 Markdown 落地到 `.planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md`"
    - "Checkpoint：作者按 checklist 手工跑一遍，签字 6 项全通过"
    - "Phase 1 的 5 条 ROADMAP Success Criteria 全部被实锚：ROADMAP #1..#5 每条对应至少一条自动化 verify 或手动 checklist 项"
    - "AUTH-03 的 `Set-Cookie` Domain/SameSite/Secure/Path 通过手动 curl 或真实浏览器 DevTools 观测签字"
  artifacts:
    - path: "apps/dashboard/e2e/auth-crosspath.spec.ts"
      provides: "AUTH-01 跨路径 session 自动化断言"
      contains: "'user-1'"
    - path: ".planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md"
      provides: "D-19 六步冒烟 checklist，Phase gate 签字用"
      contains: "D-19 step 1"
  key_links:
    - from: ".planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md"
      to: "ROADMAP Phase 1 Success Criteria #1..#5"
      via: "每条 checklist 项标注对应 AUTH-XX + Success Criteria 编号"
      pattern: "Success Criteria"
---

<objective>
Phase 1 的集成验证和 phase gate。Plan 02/03/04/05 已把单测与 middleware/plugin 实装完成；本 plan 负责把"作者真的跑过一遍"这一层搭起来：
1. 激活 dashboard 跨路径 session 共享的 e2e 活用例（Plan 01 骨架的最后一条 test.skip）
2. 产出 D-19 六步手动冒烟 checklist
3. Human checkpoint：作者按 checklist 跑一遍并签字（phase gate）

Purpose: ROADMAP Phase 1 Success Criteria 五条必须被实锚验证过，才能声明 Phase 1 完成。自动化单测覆盖不了 GitHub OAuth 往返、真实浏览器 DevTools Set-Cookie 观测、跨 tab 登出自然回收等场景 —— 这些必须靠手动。

Output:
- `apps/dashboard/e2e/auth-crosspath.spec.ts`（最后一条 test.skip 转为活用例）
- `.planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md`（新建，6 步 checklist + 签字位）
- Human checkpoint 通过后 phase 1 关闭
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-gateway/01-CONTEXT.md
@.planning/phases/01-auth-gateway/01-RESEARCH.md
@.planning/phases/01-auth-gateway/01-VALIDATION.md
@.planning/phases/01-auth-gateway/01-01-PLAN.md
@.planning/phases/01-auth-gateway/01-02-PLAN.md
@.planning/phases/01-auth-gateway/01-03-PLAN.md
@.planning/phases/01-auth-gateway/01-04-PLAN.md
@.planning/phases/01-auth-gateway/01-05-PLAN.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
**ROADMAP Phase 1 Success Criteria → 验证路径映射：**

| # | Success Criteria | 验证路径 |
|---|------------------|----------|
| 1 | 登录后刷新/跨子路径/Nuxt SSR 保持登录 | auth-crosspath.spec.ts（dashboard 单 app 自动化）+ D-19 step 3/4/5（手动跨 movie/comic/blog） |
| 2 | 登出后服务端 session 立刻失效、跨端刷新变匿名 | signout.test.ts（Plan 05 单测）+ D-19 step 6（手动） |
| 3 | 带 Cookie/Authorization 请求默认绕过 gateway KV；匿名永不读到他人缓存 | cache-middleware.test.ts D-11 #2/#3（Plan 02 单测）|
| 4 | /api/auth/* 全路径不落 KV；Set-Cookie 正确透传；cookie domain 正确 | cache-middleware.test.ts D-11 #4（Plan 02）+ D-19 step 2（手动 DevTools Set-Cookie）|
| 5 | better-auth 升级到 ^1.6.10 并通过冒烟 | Plan 04 + D-19 六步全部通过 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 激活 dashboard auth-crosspath.spec.ts 活用例</name>
  <read_first>
    - apps/dashboard/e2e/auth-crosspath.spec.ts（Plan 01 骨架）
    - apps/dashboard/e2e/auth-flow.spec.ts（L34-210 完整范例）
    - apps/dashboard/playwright.config.ts（确认 baseURL）
    - apps/dashboard/src/lib/auth-client.ts（确认 SPA 读 session 的途径）
  </read_first>
  <action>
    把 `apps/dashboard/e2e/auth-crosspath.spec.ts` 里唯一一条 `test.skip` 转为 `test`，填充断言。
    **不使用恒真断言**：用 `page.route` handler 内的 `sessionRequests` 计数器作为 SPA 真的消费了 session 的客观证据（W1 revision）。

    ```ts
    test('D-19 step 4: 同 cookie 跨 /movie /comic /blog /dashboard 读到同一 user.id', async ({ page }) => {
      // 注意：dashboard e2e 单独起服务，movie/comic/blog 不在本进程内
      // 本用例验证 dashboard 单 app 读 session 成功；跨 app 真实跳转由 D-19 step 4 手动覆盖

      // W1 修订：用计数器代替恒真断言。SPA 真的读 session 才会让 sessionRequests 递增。
      let sessionRequests = 0
      await page.route('**/api/auth/get-session', (route) => {
        sessionRequests += 1
        return route.fulfill({
          status: 200,
          contentType: 'application/json',
          body: JSON.stringify({
            user: { id: 'user-1', email: 'author@starye.org', role: 'admin' },
            session: { id: 'session-1' },
          }),
        })
      })
      await interceptExternalRedirects(page)

      await page.goto('/')
      await page.waitForLoadState('networkidle')

      // 核心断言 1：SPA 至少打过一次 /api/auth/get-session（Better Auth 客户端消费 session）
      // 这条断言在 SPA 没读 session / session 未消费时会失败 —— 非恒真
      expect(sessionRequests).toBeGreaterThanOrEqual(1)

      // 核心断言 2：若 dashboard 首页已暴露 [data-user-id] 则额外比对 user.id（强断言）
      // 若未暴露，上面的 sessionRequests 断言已足以锚定 AUTH-01 "SPA 读到 session" 的不变量
      const userId = await page.evaluate(() =>
        document.querySelector('[data-user-id]')?.getAttribute('data-user-id') ?? null,
      )
      if (userId !== null) {
        expect(userId).toBe('user-1')
      }
    })
    ```

    **关键约束：**
    - 本 task 只改 `auth-crosspath.spec.ts`，不动 dashboard 业务代码
    - **禁止 `expect(true).toBe(true)` 或等价恒真断言** —— 失败信号必须能由 SPA 行为缺失真实触发
    - 若 dashboard 首页确实没有 `data-user-id` 或等价 DOM 标记，保留 userId 条件分支；不要为测试临时加 data-* 到业务组件
    - 不要真的启动 movie/comic/blog 三个 dev server —— D-19 step 4 的"真实跨 app"由手动 checklist 覆盖
    - 本任务用内联 `page.route` 带 `sessionRequests` 计数替代 `mockSession()` 调用；`mockSession` 顶部定义保留即可
  </action>
  <verify>
    <automated>cd apps/dashboard && pnpm test:e2e -- auth-crosspath.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip" apps/dashboard/e2e/auth-crosspath.spec.ts` 返回 `0`
    - `grep -c "test(" apps/dashboard/e2e/auth-crosspath.spec.ts` 返回 `1`
    - `grep -c "expect(true).toBe(true)" apps/dashboard/e2e/auth-crosspath.spec.ts` 返回 `0`（禁止恒真断言）
    - `grep -c "sessionRequests" apps/dashboard/e2e/auth-crosspath.spec.ts` >= `3`（声明 + 递增 + 断言至少三处）
    - `grep -c "toBeGreaterThanOrEqual(1)" apps/dashboard/e2e/auth-crosspath.spec.ts` >= `1`
    - `pnpm --filter dashboard test:e2e -- auth-crosspath.spec.ts` 通过（绿）
    - 不存在 `test.fixme` 或 `test.only`
  </acceptance_criteria>
  <done>dashboard 跨路径 e2e 活用例全绿；断言基于 sessionRequests 计数器（可被真实失败触发）而非恒真。</done>
</task>

<task type="auto">
  <name>Task 2: 产出 D-19 六步冒烟 checklist</name>
  <read_first>
    - .planning/phases/01-auth-gateway/01-CONTEXT.md L60-66（D-19 原文六步）
    - .planning/phases/01-auth-gateway/01-VALIDATION.md L80-87（Manual-Only Verifications 表）
    - .planning/ROADMAP.md（Phase 1 Success Criteria 文本）
    - 上述 5 个 SUMMARY 文件（若 Plan 02/03/04/05 已产出）
  </read_first>
  <action>
    创建 `.planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md`：

    ```markdown
    # Phase 1 冒烟 Checklist — D-19 六步手动签字

    **目的：** Phase 1 gate 最终签字。Plan 02/03/04/05 已把单测与 middleware 实装完成；
    本 checklist 是作者亲自跑一遍，确认 ROADMAP 5 条 Success Criteria 在真实浏览器 + 真实
    GitHub OAuth + 真实 Cloudflare 部署（或本地 gateway:8080）下全部达成。

    **前置条件：**
    - [ ] Plan 02 已合入（Gateway bypass 实装）
    - [ ] Plan 03 已合入（Nuxt SSR session 通道）
    - [ ] Plan 04 已合入（better-auth ^1.6.10）
    - [ ] Plan 05 已合入（signout 单测）
    - [ ] 本地或部署环境可访问 `/auth /movie /comic /blog /dashboard` 五个路径
    - [ ] 作者有有效 GitHub 账号用于 OAuth 登录

    ---

    ## Step 1: 未登录浏览公开目录
    **验证：** ROADMAP Success Criteria #1 前半（公开路径匿名可访问）
    **AUTH 需求：** 未特指，但作为后续 step 的基线

    - [ ] 清除浏览器所有 `starye.org` cookies（或使用隐身窗口）
    - [ ] 访问 `/movie/`
    - [ ] ✓ 页面正常渲染公开目录内容
    - [ ] ✓ DevTools Network：无 `/api/auth/get-session` 的 401（或请求根本没发）
    - [ ] ✓ DevTools Application/Storage → Cookies：无 `starye.session_token`

    ---

    ## Step 2: GitHub OAuth 登录
    **验证：** AUTH-03（cookie domain / SameSite / Secure / Path）+ AUTH-04（升级后 OAuth 回调稳定，含 1.6.10 的 Set-Cookie 去重修复）
    **Success Criteria #4/#5 的实锚**

    - [ ] 从 `/auth/login` 点击 GitHub 授权
    - [ ] ✓ GitHub 授权页成功跳转
    - [ ] ✓ 授权后跳回发起页（原访问路径）
    - [ ] DevTools Network 选中 `/api/auth/callback/github` 响应，查看 `Set-Cookie` 头：
      - [ ] ✓ 含 `starye.session_token=` 前缀（cookie 名正确）
      - [ ] ✓ 含 `Domain=starye.org` 或无 `Domain` 属性（D-05 等价语义）
      - [ ] ✓ 含 `SameSite=Lax`
      - [ ] ✓ 含 `Secure`（生产 HTTPS）或在本地 HTTP 下不含 Secure（D-06 isHttps 判定）
      - [ ] ✓ 含 `Path=/`
      - [ ] ✓ Set-Cookie 条目不重复（1.6.10 修复验证）
    - [ ] DevTools Application/Storage → Cookies：`starye.session_token` 存在且 Domain 正确

    ---

    ## Step 3: 刷新页面仍登录
    **验证：** ROADMAP Success Criteria #1（刷新不掉线）+ AUTH-01

    - [ ] 在 Step 2 跳回后的页面按 F5 刷新
    - [ ] ✓ 页面仍显示登录态（用户头像 / 用户名 / dashboard 入口可见）
    - [ ] ✓ 无"先显示匿名，再切换到登录"的闪烁

    ---

    ## Step 4: 跨子路径 session 共享
    **验证：** AUTH-01（跨 5 端互通）+ AUTH-05（gateway 透传 cookie）
    **Success Criteria #1 核心**

    按顺序访问并逐项打勾：
    - [ ] `/movie/` → ✓ 登录态可见（有收藏按钮 / 用户菜单）
    - [ ] `/comic/` → ✓ 登录态可见
    - [ ] `/blog/` → ✓ 登录态可见（注意：Nuxt SSR 渲染；见 Step 5 view-source 断言）
    - [ ] `/dashboard/` → ✓ 登录态可见（非 admin 账号预期被 Phase 2 拦截，Phase 1 只验证 session 读取成功）
    - [ ] ✓ DevTools Network：每次导航都有 `/api/auth/get-session` 请求；都返回 200 + 同一 user.id

    ---

    ## Step 5: Nuxt SSR view-source 含登录态
    **验证：** AUTH-02（Nuxt SSR 服务端读 session）
    **Success Criteria #1 后半（Nuxt SSR 不漏登录态）**

    - [ ] DevTools 禁用 JavaScript（Settings → Debugger → "Disable JavaScript"）
    - [ ] 硬刷新 `/blog/`
    - [ ] ✓ HTML 源码（`view-source:https://starye.org/blog/`）含登录态标识（用户名 / 用户 id / `__NUXT__.state` 里的 session 对象）
    - [ ] ✓ 不存在"SSR 渲染为匿名 → 客户端脚本切到登录"的闪烁（因为 JS 禁用时客户端不会接管，所以 HTML 本身必须正确）
    - [ ] 重新启用 JS，刷新 → 体感无闪烁

    ---

    ## Step 6: 登出彻底失效
    **验证：** AUTH-08 + D-15 / D-16 / D-17
    **Success Criteria #2**

    - [ ] 在任一登录页点击登出按钮
    - [ ] ✓ DevTools Network：`/api/auth/sign-out` 响应 200
    - [ ] ✓ 响应 `Set-Cookie` 头含 `starye.session_token=` 且包含 `Max-Age=0` 或 `Expires=Thu, 01 Jan 1970`
    - [ ] ✓ DevTools Application/Storage → Cookies：`starye.session_token` 已消失
    - [ ] 手动刷新 `/movie/` `/comic/` `/blog/` `/dashboard/`：
      - [ ] ✓ 每个 app 都回到匿名态（登录按钮重现 / 用户菜单消失）
    - [ ] 额外（D-16 跨 tab 自然回收）：
      - [ ] 登录 → 打开两个 tab A / B → tab A 点登出 → tab B 刷新 → ✓ 变匿名（无需 BroadcastChannel）

    ---

    ## Sign-Off

    **执行日期：** YYYY-MM-DD
    **执行人：** @作者

    | Step | 状态 | 备注（异常 / 可接受偏差） |
    |------|------|----------------------------|
    | 1 匿名公开目录 | ⬜ pass / ❌ fail | |
    | 2 GitHub OAuth + Set-Cookie | ⬜ pass / ❌ fail | |
    | 3 刷新仍登录 | ⬜ pass / ❌ fail | |
    | 4 跨子路径共享 | ⬜ pass / ❌ fail | |
    | 5 Nuxt SSR view-source | ⬜ pass / ❌ fail | |
    | 6 登出彻底失效 | ⬜ pass / ❌ fail | |

    **Phase 1 Gate 签字：** ⬜ 通过 / ❌ 返工（记录返工原因 + 触发的 plan）
    ```

    **关键约束：**
    - checklist 必须覆盖 ROADMAP Phase 1 所有 5 条 Success Criteria
    - 每一 step 标注对应的 AUTH-XX 需求 + Success Criteria 编号（可溯源）
    - 签字栏留空，由作者实际跑时填充
    - 不要把自动化能覆盖的条目移到 checklist（例如 D-11 #2 的 bypass 行为是单测范围，不要让作者手动 curl）
    - 文件路径严格是 `.planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md`
  </action>
  <verify>
    <automated>test -f .planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md && grep -c "^## Step [1-6]" .planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在
    - `grep -c "^## Step [1-6]" .planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md` 返回 `6`
    - `grep -c "AUTH-0[1-8]" .planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md` >= `8`（8 个 AUTH 需求至少各出现一次）
    - `grep -c "Success Criteria #" .planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md` >= `5`（5 条 ROADMAP SC 编号都被引用）
    - `grep -c "^- \[ \]" .planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md` >= `30`（足够颗粒度的 checkbox）
    - 含 "Sign-Off" 章节
  </acceptance_criteria>
  <done>Checklist 覆盖 D-19 六步，每步锚定 AUTH-XX + Success Criteria；签字栏空白待作者填。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human checkpoint — 作者执行 D-19 六步冒烟并签字</name>
  <what-built>
    Plan 01..05 已完成：
    - Gateway 缓存按 cookie/authorization bypass（D-07/D-10）
    - private scope 死代码清理（D-12/D-13/D-14）
    - Nuxt blog + auth SSR session 通道（D-01..D-04）
    - better-auth 升级到 ^1.6.10（D-18）
    - signout 单测（D-15）
    - 4 条自动化测试（cache-middleware D-11 四条、blog e2e session 3 条、dashboard e2e crosspath 1 条、api signout 3 条）
    - D-19 六步 checklist 已产出
  </what-built>
  <how-to-verify>
    1. 打开 `.planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md`
    2. 按 Step 1 → Step 6 顺序执行，每条 checkbox 打勾或记录偏差
    3. 特别关注：
       - Step 2 的 Set-Cookie DevTools 观测（对应 AUTH-03 的 cookie domain / SameSite / Secure / Path）
       - Step 5 的 view-source（禁 JS 刷新）—— 这是 AUTH-02 的终极验证
       - Step 6 的跨 tab 自然回收 —— 这是 D-16 的验证
    4. 在 checklist Sign-Off 表填入通过/失败状态
    5. 如有任何 step fail，把失败点写到 Sign-Off 的"触发返工的 plan"列，orchestrator 将以 `--gaps` 模式创建 gap closure plan
  </how-to-verify>
  <resume-signal>
    在对话里贴出 checklist Sign-Off 章节（含填好的 Pass/Fail + 日期 + 签字），或直接说：
    - "phase 1 通过" → 结束 Phase 1
    - "step N fail: <描述>" → 触发 gap closure
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 自动化 → 手动 | 单测不能覆盖的行为（OAuth 往返、真实 DevTools 观测、跨 tab）在这里闭环 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 (T1 + T2 + T3 + T4 合计) | Information Disclosure / Tampering / DoS / Elevation | 真实部署环境的端到端 session 流 | mitigate | D-19 六步手动 checklist 覆盖 4 大威胁模型在真实浏览器 + Cloudflare 部署下的表现；失败即 gap closure |
| T-01-20 | Supply Chain | 1.6.10 升级后生产环境观察窗口 | accept | Plan 04 已跑全仓 test + build；本 checkpoint 要求作者在真实部署（或预发）上跑冒烟，偏差触发回滚（Plan 04 SUMMARY 建议独立 PR 保留回滚位点） |
</threat_model>

<verification>
```bash
# 自动化部分
pnpm --filter dashboard test:e2e -- auth-crosspath.spec.ts

# 签字文件存在
test -f .planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md

# Task 3 无自动化命令 — 由 human checkpoint 产出文字签字
```
</verification>

<success_criteria>
- AUTH-01..08 八项需求全部通过：单测自动化 + 作者手动冒烟双重签字
- ROADMAP Phase 1 五条 Success Criteria 全部"可观测为真"
- `.planning/phases/01-auth-gateway/01-SMOKE-CHECKLIST.md` 里 Sign-Off 表的 6 行全部 Pass
- Phase 1 关闭，可进入 Phase 2 规划
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-gateway/01-06-SUMMARY.md` recording:
- checklist 执行日期与签字
- 六 step 实际结果（pass / 偏差记录）
- 若触发 gap closure，记录 fail 的 step 编号与根因
- Phase 1 最终状态：Completed / Partially-completed-with-deferrals（后者需记录哪些项移到 v2 或独立 follow-up）
</output>
