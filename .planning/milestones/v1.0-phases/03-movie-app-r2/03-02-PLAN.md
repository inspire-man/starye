---
phase: 03-movie-app-r2
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - apps/movie-app/src/views/Player.vue
autonomous: true
requirements:
  - VIDEO-04
  - VIDEO-05

must_haves:
  truths:
    - "Player.vue 的错误 UI 不再只服务 TorrServer 分支，普通播放源错误也能看到统一卡片"
    - "waiting 不再无限期停留在“正在缓冲”，超时后会转入明确错误态"
    - "重试只针对当前源，不自动切换 player，不引入 codec swap"
    - "重试后尽可能从上次位置恢复，不重置用户观看上下文"
  artifacts:
    - path: "apps/movie-app/src/views/Player.vue"
      provides: "统一 loading/error/retry 状态机"
      contains: "retry"
  key_links:
    - from: ".planning/phases/03-movie-app-r2/03-CONTEXT.md"
      to: "apps/movie-app/src/views/Player.vue"
      via: "D-04..D-11"
      pattern: "Player.vue"
---

<objective>
将 `Player.vue` 从“TorrServer 专用 overlay + 其他情况无反馈”的状态，重构为统一的播放 loading / error / retry 行为：普通源与 TorrServer 源都能显示同一套错误卡片，且重试只重试当前源。

Purpose: 这是 Phase 3 的核心用户价值。用户真正抱怨的是“偶尔出错但没有清楚反馈”，而不是底层流媒体协议。

Output: `Player.vue` 内形成统一错误卡片、等待超时、同源重试、返回详情页、条件性 Aria2 补救的稳定交互。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-movie-app-r2/03-CONTEXT.md
@.planning/phases/03-movie-app-r2/03-RESEARCH.md
@.planning/phases/03-movie-app-r2/03-PATTERNS.md
@apps/movie-app/src/views/Player.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 合并 TorrServer 专用 overlay 为统一 player 状态</name>
  <files>apps/movie-app/src/views/Player.vue</files>
  <read_first>
    - apps/movie-app/src/views/Player.vue
    - .planning/phases/03-movie-app-r2/03-PATTERNS.md
  </read_first>
  <action>
    1. 把 `torrServerBuffering` / `torrServerError` 合并成统一的 loading + error 状态。
    2. 保留 `currentMagnetUrl`、`goToDetail()`、`fallbackToAria2()` 这类现有补救动作。
    3. 错误卡片统一承载：
       - 错误说明
       - 重试按钮
       - 返回详情页按钮
       - magnet + aria2 已连接时的 Aria2 补救按钮
    4. 不引入 composable 或共享组件，改动集中留在 Player.vue。
  </action>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vue-tsc --noEmit</automated>
  </verify>
  <done>
    TorrServer 分支与普通分支不再分裂出两套错误 UI。
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 waiting 超时与同源重试</name>
  <files>apps/movie-app/src/views/Player.vue</files>
  <read_first>
    - apps/movie-app/src/views/Player.vue
    - .planning/phases/03-movie-app-r2/03-RESEARCH.md（Retry path tradeoff）
  </read_first>
  <action>
    1. 对 `waiting` 增加超时计时器，超时后进入 recoverable error。
    2. 采用保守路线：记录最后时间 -> destroy 当前 player -> 用同一 URL 和 `startTime` 重建。
    3. 明确禁止自动切源、自动重试、codec swap。
    4. 若短时间内重复失败，升级错误文案为“多次失败，请返回详情页切换源”。
  </action>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vue-tsc --noEmit</automated>
  </verify>
  <done>
    重试语义已与 D-09/D-10/D-11 对齐。
  </done>
</task>

</tasks>

<verification>
## 整体验证

```bash
pnpm --filter @starye/movie-app exec vue-tsc --noEmit
pnpm --filter @starye/movie-app test --run
```

并结合 `03-HUMAN-UAT.md` 执行真实播放验证。
</verification>

<success_criteria>
1. 统一错误卡片覆盖普通源与 TorrServer 源
2. waiting 超时会转为明确错误态
3. 重试只作用于当前源，不自动切源
4. 重试时尽可能恢复到上次位置
</success_criteria>

<output>
After completion, create `.planning/phases/03-movie-app-r2/03-02-SUMMARY.md`
</output>

