---
phase: 03-movie-app-r2
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - apps/movie-app/src/composables/__tests__/*
  - apps/movie-app/src/utils/__tests__/*
  - .planning/phases/03-movie-app-r2/03-HUMAN-UAT.md
autonomous: false
requirements:
  - VIDEO-04
  - VIDEO-05
tags: [smoke, human-uat, player]

must_haves:
  truths:
    - "Phase 3 自动化验证只覆盖高价值、低脆弱度的逻辑点，不强行堆砌 Player DOM 测试"
    - "真实播放失败、重试、离线按钮反馈已通过人工冒烟确认"
    - "R18 detail 防御未因 Phase 3 回退"
  artifacts:
    - path: ".planning/phases/03-movie-app-r2/03-HUMAN-UAT.md"
      provides: "真实播放与错误恢复检查项"
      contains: "同源重试行为"
  key_links:
    - from: ".planning/phases/03-movie-app-r2/03-HUMAN-UAT.md"
      to: "apps/movie-app/src/views/Player.vue"
      via: "真实错误与重试验证"
      pattern: "重试"
---

<objective>
为 Phase 3 建立“少量稳定自动化 + 一份强人工播放 UAT”的收口方式，确保播放器行为通过真实浏览器和真实流源验证，而不是只靠 mock。

Purpose: `Player.vue` 依赖真实媒体栈；如果在这个阶段强行做大而全的 DOM 自动化，测试很容易脆弱且失真。

Output: 必要的自动化回归 + 一份能真正验收 Phase 3 成败的人类播放检查清单。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-movie-app-r2/03-VALIDATION.md
@.planning/phases/03-movie-app-r2/03-HUMAN-UAT.md
@.planning/phases/03-movie-app-r2/03-RESEARCH.md
@apps/movie-app/vitest.config.ts
@apps/movie-app/playwright.config.ts
@apps/movie-app/src/utils/__tests__/torrServerClient.test.ts
@apps/movie-app/src/composables/__tests__/useAuthGuard.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 只补轻量自动化验证</name>
  <files>apps/movie-app/src/composables/__tests__/*, apps/movie-app/src/utils/__tests__/*</files>
  <read_first>
    - apps/movie-app/vitest.config.ts
    - apps/movie-app/src/composables/__tests__/useAuthGuard.test.ts
    - apps/movie-app/src/utils/__tests__/torrServerClient.test.ts
  </read_first>
  <action>
    只为 Phase 3 新增低脆弱度测试：
    - 若抽出了纯函数（如错误文案映射、重试资格判断、按钮 title 计算），测试这些纯函数
    - 若没有可抽纯函数，不强行补大型 Player.vue 视图测试
    - 允许零新增测试文件，但必须跑通现有 movie-app unit tests
  </action>
  <verify>
    <automated>pnpm --filter @starye/movie-app test --run</automated>
  </verify>
  <done>
    自动化层不为形式感而写脆弱测试。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human playback UAT</name>
  <what-built>
    Phase 3 的文档收敛、统一错误卡片、同源重试、离线按钮反馈都已落地。现在需要真实浏览器和真实流源确认用户体验是否成立。
  </what-built>
  <how-to-verify>
    打开 `.planning/phases/03-movie-app-r2/03-HUMAN-UAT.md`，至少执行 5 项：
    1. 离线按钮反馈
    2. 标准播放源错误可见化
    3. TorrServer 路径错误可见化
    4. 同源重试行为
    5. R18 detail 防御未回退
  </how-to-verify>
  <resume-signal>
    回复：
    - `phase 3 uat pass`
    - 或 `step N fail: <描述>`
  </resume-signal>
</task>

</tasks>

<verification>
```bash
pnpm --filter @starye/movie-app test --run
```
</verification>

<success_criteria>
1. movie-app unit tests green
2. 人工播放 UAT 完成并记录结果
3. Phase 3 成败通过真实播放场景而不是纯 mock 判断
</success_criteria>

<output>
After completion, create `.planning/phases/03-movie-app-r2/03-04-SUMMARY.md`
</output>
