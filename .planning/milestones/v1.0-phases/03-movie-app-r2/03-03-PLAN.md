---
phase: 03-movie-app-r2
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - apps/movie-app/src/views/MovieDetail.vue
  - apps/movie-app/src/views/Player.vue
autonomous: true
requirements:
  - VIDEO-05

must_haves:
  truths:
    - "Aria2 / TorrServer 离线时按钮仍可见，不再直接消失"
    - "离线按钮是 disabled 态，而不是点击后才报错"
    - "提示信息足够明确，用户知道需要去设置页配置连接"
    - "实现不强绑 movie-app 的 i18n 初始化改造"
  artifacts:
    - path: "apps/movie-app/src/views/MovieDetail.vue"
      provides: "离线 disabled + title 提示"
      contains: "disabled"
    - path: "apps/movie-app/src/views/Player.vue"
      provides: "错误卡片中的条件性 Aria2 动作与离线提示"
      contains: "title"
  key_links:
    - from: "apps/movie-app/src/views/MovieDetail.vue"
      to: "apps/movie-app/src/composables/useAria2.ts"
      via: "isConnected"
      pattern: "aria2Connected"
    - from: "apps/movie-app/src/views/MovieDetail.vue"
      to: "apps/movie-app/src/composables/useTorrServer.ts"
      via: "isConnected"
      pattern: "torrServerConnected"
---

<objective>
把 Aria2 / TorrServer 相关动作从“在线时才显示”改为“始终可见，但离线时 disabled + 轻提示”，让播放失败后的补救路径对用户始终可见。

Purpose: Phase 3 的另一半体验问题不是“播放器报错”，而是“用户根本不知道当前有哪些可用补救动作”。

Output: `MovieDetail.vue` 和 `Player.vue` 上的相关按钮都具备一致的离线反馈语义。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-movie-app-r2/03-CONTEXT.md
@.planning/phases/03-movie-app-r2/03-RESEARCH.md
@.planning/phases/03-movie-app-r2/03-PATTERNS.md
@apps/movie-app/src/views/MovieDetail.vue
@apps/movie-app/src/views/Player.vue
@apps/movie-app/src/composables/useAria2.ts
@apps/movie-app/src/composables/useTorrServer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MovieDetail 按钮从条件显示改为条件禁用</name>
  <files>apps/movie-app/src/views/MovieDetail.vue</files>
  <read_first>
    - apps/movie-app/src/views/MovieDetail.vue
    - .planning/phases/03-movie-app-r2/03-PATTERNS.md
  </read_first>
  <action>
    1. `Aria2` / `TorrServer` 按钮的显示条件只保留“是否适用于 magnet 源”。
    2. 在线状态从 `v-if` 迁移到 `:disabled`。
    3. 增加 `title` 文案说明离线原因；本期不引入 i18n 初始化改造。
    4. 保留现有 click handler；disabled 时不应触发操作。
  </action>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vue-tsc --noEmit</automated>
  </verify>
  <done>
    MovieDetail 的补救动作在离线时不再“消失”。
  </done>
</task>

<task type="auto">
  <name>Task 2: Player 错误卡片补齐条件性补救动作</name>
  <files>apps/movie-app/src/views/Player.vue</files>
  <read_first>
    - apps/movie-app/src/views/Player.vue
    - .planning/phases/03-movie-app-r2/03-RESEARCH.md
  </read_first>
  <action>
    1. 错误卡片保留“返回详情页”。
    2. 若 `currentMagnetUrl` 存在，且 Aria2 已连接，则展示“添加到 Aria2”。
    3. 若 Aria2 未连接，不展示无效动作，避免再加第二层 toast 纠正。
  </action>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vue-tsc --noEmit</automated>
  </verify>
  <done>
    Player 错误卡片的补救动作与 MovieDetail 一致。
  </done>
</task>

</tasks>

<verification>
## 整体验证

```bash
pnpm --filter @starye/movie-app exec vue-tsc --noEmit
pnpm --filter @starye/movie-app test --run
```

并人工确认 disabled 按钮与 title 文案。
</verification>

<success_criteria>
1. Aria2 / TorrServer 按钮离线时仍可见
2. 离线状态用 disabled + title 表达
3. 不为此引入 movie-app i18n 初始化改造
</success_criteria>

<output>
After completion, create `.planning/phases/03-movie-app-r2/03-03-SUMMARY.md`
</output>

