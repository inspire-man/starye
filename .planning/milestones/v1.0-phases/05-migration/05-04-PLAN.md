---
phase: 05-migration
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - apps/movie-app/package.json
  - apps/comic-app/package.json
  - apps/dashboard/package.json
  - apps/blog/package.json
  - apps/auth/package.json
  - apps/movie-app/src/main.ts
  - apps/comic-app/src/main.ts
  - apps/dashboard/src/main.ts
  - apps/blog/nuxt.config.ts
  - apps/auth/nuxt.config.ts
  - RUNBOOK.md
  - .planning/phases/05-migration/05-HUMAN-UAT.md
autonomous: true
requirements:
  - OBS-02
  - DEPLOY-05
  - OBS-05

must_haves:
  truths:
    - "D-11: Vue app 用 `@sentry/vue`，Nuxt app 用 `@sentry/nuxt`"
    - "D-12: 所有端先汇入同一 Sentry 项目"
    - "D-15: 根 `RUNBOOK.md` 是唯一正式运维手册"
    - "D-16: RUNBOOK 必须吸收 WAF 与 migration smoke 经验"
    - "D-17: crawler 失败告警沿用 GitHub Actions 默认邮件"
    - "本 plan 负责 frontend observability 与运维收口，不再扩 Worker 或 migration 逻辑"
  artifacts:
    - path: "apps/movie-app/src/main.ts"
      provides: "Vue app Sentry bootstrap"
      contains: "Sentry"
    - path: "apps/blog/nuxt.config.ts"
      provides: "Nuxt app Sentry bootstrap"
      contains: "sentry"
    - path: "RUNBOOK.md"
      provides: "deploy/rollback/migration/observability response guide"
      contains: "rollback"
    - path: ".planning/phases/05-migration/05-HUMAN-UAT.md"
      provides: "Phase 5 manual verification checklist"
      contains: "workflow_dispatch"
  key_links:
    - from: "RUNBOOK.md"
      to: ".github/workflows/rollback.yml"
      via: "operator rollback path"
      pattern: "rollback"
---

<objective>
完成 frontend observability 接入和运维文档收口：Vue/Nuxt 应用进入统一 Sentry 项目，RUNBOOK 成为 deploy/rollback/migration/monitoring 的唯一正式手册，并为 verify-work 准备人工验收脚本。

Purpose: 没有 frontend observability 和 RUNBOOK，Phase 5 只能算“CI 变复杂了”，还不算真正的生产运维基线。

Output: frontend Sentry 接入、根 RUNBOOK 扩写、`05-HUMAN-UAT.md`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-migration/05-CONTEXT.md
@.planning/phases/05-migration/05-RESEARCH.md
@.planning/phases/05-migration/05-PATTERNS.md
@.planning/phases/05-migration/05-VALIDATION.md
@RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 Vue / Nuxt 前端接入统一 Sentry 项目</name>
  <files>apps/movie-app/package.json, apps/comic-app/package.json, apps/dashboard/package.json, apps/blog/package.json, apps/auth/package.json, apps/movie-app/src/main.ts, apps/comic-app/src/main.ts, apps/dashboard/src/main.ts, apps/blog/nuxt.config.ts, apps/auth/nuxt.config.ts</files>
  <read_first>
    - apps/movie-app/src/main.ts
    - apps/comic-app/src/main.ts
    - apps/dashboard/src/main.ts
    - apps/blog/nuxt.config.ts
    - apps/auth/nuxt.config.ts
    - .planning/phases/05-migration/05-RESEARCH.md
  </read_first>
  <action>
    1. Vue apps 接入 `@sentry/vue`。
    2. Nuxt apps 接入 `@sentry/nuxt`。
    3. 保持一个共享 Sentry 项目 / DSN 模型，不拆多项目。
    4. 只做骨架接入与最小必要配置，不在本 plan 里过度优化每个 app 的高级 feature。
  </action>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vue-tsc --noEmit && pnpm --filter @starye/comic-app exec vue-tsc --noEmit && rg -n "@sentry/vue|@sentry/nuxt|Sentry" apps/movie-app apps/comic-app apps/dashboard apps/blog apps/auth</automated>
  </verify>
  <acceptance_criteria>
    - Vue apps package.json 中出现 `@sentry/vue`
    - Nuxt apps 中出现 `@sentry/nuxt`
    - 前端入口文件存在 Sentry init
  </acceptance_criteria>
  <done>
    frontend observability 骨架已建立。
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩写根 RUNBOOK.md 为正式运维手册</name>
  <files>RUNBOOK.md</files>
  <read_first>
    - RUNBOOK.md
    - .planning/phases/05-migration/05-CONTEXT.md
    - .planning/phases/04-progress/04-HUMAN-UAT.md
    - .planning/phases/02-dashboard/02-CONTEXT.md
  </read_first>
  <action>
    1. 保留现有 WAF / ADMIN_GITHUB_ID 段落。
    2. 新增 deploy matrix、rollback matrix、D1 migration safety、Sentry/故障响应等章节。
    3. 明确哪些步骤自动、哪些步骤要人工在 Cloudflare / GitHub 上执行。
  </action>
  <verify>
    <automated>rg -n "rollback|migration|Sentry|WAF|deploy" RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - RUNBOOK 仍是单一根文件
    - 包含 deploy / rollback / migration / observability 章节
    - 吸收 Phase 2 WAF 与 Phase 4 migration smoke 经验
  </acceptance_criteria>
  <done>
    RUNBOOK 成为正式运维手册。
  </done>
</task>

<task type="auto">
  <name>Task 3: 编写 Phase 5 human UAT / ops checklist</name>
  <files>.planning/phases/05-migration/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-migration/05-VALIDATION.md
    - RUNBOOK.md
    - .github/workflows/rollback.yml
    - .github/workflows/deploy-migrations.yml
  </read_first>
  <action>
    1. 新建 `05-HUMAN-UAT.md`。
    2. 覆盖至少：手动触发 deploy、rollback、migration backup、dangerous gate、Sentry message 到达、crawler 失败告警路径确认。
    3. 每项写清前置条件、步骤、预期结果、通过/失败记录位。
  </action>
  <verify>
    <automated>Test-Path '.planning\\phases\\05-migration\\05-HUMAN-UAT.md' && rg -n "workflow_dispatch|rollback|Sentry|crawler|migration" .planning/phases/05-migration/05-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `05-HUMAN-UAT.md` 存在
    - 覆盖 deploy / rollback / migration / Sentry / crawler 告警
  </acceptance_criteria>
  <done>
    Phase 5 人工验收脚本已准备完毕。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser / Nuxt runtime -> Sentry | 前端错误和 message 会发往外部观测平台 |
| Human operator -> RUNBOOK | 运维响应依赖文档清晰度与正确性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure | frontend telemetry payloads | mitigate | 保持统一 DSN，但只发送必要诊断数据，不带敏感凭据 |
| T-05-11 | Repudiation | undocumented rollback/migration/manual ops | mitigate | 根 RUNBOOK 收口所有人工路径与责任边界 |
| T-05-12 | Availability | production incident handling confusion | mitigate | 用 `05-HUMAN-UAT.md` 把 deploy/rollback/migration/Sentry 演练成显式 checklist |

</threat_model>

<verification>
## 整体验证

```bash
pnpm --filter @starye/movie-app exec vue-tsc --noEmit
pnpm --filter @starye/comic-app exec vue-tsc --noEmit
rg -n "@sentry/vue|@sentry/nuxt|Sentry" apps/movie-app apps/comic-app apps/dashboard apps/blog apps/auth
rg -n "rollback|migration|Sentry|WAF|deploy" RUNBOOK.md
rg -n "workflow_dispatch|rollback|Sentry|crawler|migration" .planning/phases/05-migration/05-HUMAN-UAT.md
```

</verification>

<success_criteria>
1. frontend observability 骨架完成
2. RUNBOOK 成为单一正式运维手册
3. Phase 5 人工验收脚本准备完毕
</success_criteria>

<output>
After completion, create `.planning/phases/05-migration/05-04-SUMMARY.md`
</output>
