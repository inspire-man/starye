---
phase: 16-task-domain-foundation
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - apps/api/src/schemas/crawler-tasks.ts
  - apps/api/src/routes/admin/crawler-tasks/index.ts
  - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
  - apps/api/src/routes/admin/main/index.ts
  - apps/api/src/__tests__/app-type.test.ts
autonomous: true
requirements: [CTRL-01, CTRL-04]
must_haves:
  truths:
    - "已登录且具备对应资源权限的管理员只能选择 movie 或 manga 模板创建或查询任务。"
    - "携带服务 token、任意命令/URL/workflow/target/环境/密钥字段的请求不能进入 task command/query path。"
    - "取消和重试通过 task-domain repository 产生合法的可审计结果；重试仅创建新的 attempt。"
  artifacts:
    - path: apps/api/src/schemas/crawler-tasks.ts
      provides: strict command and bounded cursor/query schemas
    - path: apps/api/src/routes/admin/crawler-tasks/index.ts
      provides: session-only task command and query router
    - path: apps/api/src/routes/admin/main/index.ts
      provides: mounted task router under the existing admin root
  key_links:
    - from: apps/api/src/routes/admin/crawler-tasks/index.ts
      to: apps/api/src/domain/crawler-tasks/template-registry.ts
      via: server-resolved template permission and snapshot
      pattern: crawlerTaskTemplates
    - from: apps/api/src/routes/admin/crawler-tasks/index.ts
      to: apps/api/src/domain/crawler-tasks/repository.ts
      via: create, query, cancel, and retry command calls
      pattern: crawlerTaskRepository
    - from: apps/api/src/routes/admin/main/index.ts
      to: apps/api/src/routes/admin/crawler-tasks/index.ts
      via: admin router mount
      pattern: crawler-tasks
---

<objective>
提供严格的 session-only 管理员 task 命令与查询 API，并把它接入既有 `/api/admin` Hono router。

Purpose: 让后续 Dashboard 具备稳定控制面接口，同时避免旧服务 token 和可控执行参数扩大权限面。
Output: strict Valibot DTO、resource-scoped session router、mount 与集成测试。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-task-domain-foundation/16-CONTEXT.md
@.planning/phases/16-task-domain-foundation/16-RESEARCH.md
@.agents/skills/starye-hono-rpc/SKILL.md
@.planning/phases/16-task-domain-foundation/16-01-SUMMARY.md

<interfaces>
- Plan 16-01 exports the server-owned task template map, transition/retry semantics, and D1 repository; this plan does not duplicate them in route-local data structures.
- `authMiddleware()` supplies Better Auth in Hono context, while `canAccessCrawler(user, 'movie' | 'comic')` is the established per-resource authorization check.
- `adminMainRoutes` is already mounted at `/api/admin`; task routes belong at `/crawler-tasks`, not inside legacy `/crawlers` where `serviceAuth` accepts `CRAWLER_SECRET`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build strict session command/query schemas and task router</name>
  <files>apps/api/src/schemas/crawler-tasks.ts, apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</files>
  <read_first>
    - apps/api/src/routes/admin/crawlers/index.ts — legacy crawler router uses `serviceAuth` and optional URL action input; retain it untouched and do not inherit its authorization boundary.
    - apps/api/src/routes/admin/main/index.ts — existing admin sub-router mounting convention.
    - apps/api/src/middleware/auth.ts and apps/api/src/middleware/guard.ts — session initialization/role verification pattern.
    - apps/api/src/lib/permissions.ts — `canAccessCrawler` resource mapping.
    - apps/api/src/schemas/admin.ts — existing Valibot/Hono validator style, including the legacy action shape the new strict DTO must not reuse.
    - apps/api/src/test/helpers.ts — API mock/session test helpers.
    - .planning/phases/16-task-domain-foundation/16-CONTEXT.md — D-01, D-02, D-03, D-06, and D-08 command decisions.
  </read_first>
  <behavior>
    - Test 1: create accepts exactly `{ template: 'movie' | 'manga' }`, creates or returns the existing active task/run, and resolves permission from the server registry.
    - Test 2: any added execution-shaped or secret-shaped property is rejected by strict validation before repository invocation.
    - Test 3: no session receives 401, wrong resource role receives 403, and an old service-token header alone cannot authorize create, list, detail, cancel, retry, or log query.
    - Test 4: cancel makes queued immediately cancelled or active work cancel_requested; retry requires explicit confirmation and only calls the immutable-attempt repository path for failed/cancelled runs.
    - Test 5: detail, attempt history, and cursor-bounded log query return only repository safe projections for templates the session may access.
  </behavior>
  <action>Before modifying existing API symbols, invoke GitNexus upstream impact for the affected admin-router and auth symbols; report callers and execution flows, and give a clear user warning before any HIGH or CRITICAL change. Start with failing route tests. Create strict Valibot schemas for create, confirmation-only retry, bounded list/detail/log cursors, and identifiers; schema ownership stays in `schemas/crawler-tasks.ts`. Create an isolated Hono router whose handler obtains a real Better Auth session from request headers, resolves `movie`/`manga` through the Plan 16-01 registry, and applies `canAccessCrawler` to the resolved `movie`/`comic` resource. Implement create/idempotent-active lookup, list, task/attempt detail, cursor log read, cancel, and explicit retry through the Plan 16-01 repository. Implement D-01 through D-03 and D-06/D-08 without forwarding client executable settings or treating request retries as new queued work. Do not import `serviceAuth`, `serviceAuthWithResource`, `CRAWLER_SECRET`, workflow files, crawler code, or provider clients into this router.</action>
  <verify>
    <automated>pnpm --filter api test --run src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</automated>
  </verify>
  <acceptance_criteria>Task administration is session-only and template/resource scoped; strict request schemas prevent arbitrary crawler/provider input; command/query paths expose only the durable task-domain projections.</acceptance_criteria>
  <done>The new router implements controlled create/read/cancel/retry behavior against the Plan 16-01 domain with focused authorization and input-rejection coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount the isolated task router and preserve API type reachability</name>
  <files>apps/api/src/routes/admin/main/index.ts, apps/api/src/__tests__/app-type.test.ts</files>
  <read_first>
    - apps/api/src/routes/admin/main/index.ts — the canonical `/api/admin` composition point.
    - apps/api/src/index.ts — global `/api/admin` mount and exported `AppType` inference boundary.
    - apps/api/src/__tests__/app-type.test.ts — current route/type reachability regression style.
    - .planning/phases/16-task-domain-foundation/16-CONTEXT.md — the phase provides API contracts only; Dashboard task UI is outside this plan.
  </read_first>
  <behavior>
    - Test 1: the new router is reachable beneath the existing `/api/admin` composition and appears in inferred API route types.
    - Test 2: existing legacy `/api/admin/crawlers` import/mount remains present and the task router has its own path/authorization chain.
  </behavior>
  <action>Run GitNexus upstream impact on the existing admin composition symbol before adding the mount, then report a HIGH or CRITICAL blast radius before proceeding. Import and mount the new router at `/crawler-tasks` in the existing admin main router; retain the legacy `/crawlers` mount and its behavior unchanged. Add an AppType reachability regression so future typed clients can discover the route through the already-mounted `/api/admin` chain. This task adds no Dashboard component, no browser fetch wrapper, no workflow dispatch, and no crawler execution.</action>
  <verify>
    <automated>pnpm --filter api test --run src/__tests__/app-type.test.ts src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts; pnpm --filter api type-check</automated>
  </verify>
  <acceptance_criteria>The task router is reachable at the established admin root without changing the old crawler monitor or widening its service-token behavior.</acceptance_criteria>
  <done>Existing API composition exposes a separately protected task-domain endpoint family and type inference remains green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- Strict create/retry/query DTOs that only represent fixed `movie` and `manga` task operations.
- A Better-Auth-session and template-resource-scoped administrative router under `/api/admin/crawler-tasks`.
- Route and AppType regression coverage proving the isolation from legacy service-token crawler routes.
</artifacts_this_phase_produces>

<threat_model>
ASVS level: L1. Any high-severity threat is a blocking mitigation requirement for this plan.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard session → admin task route | User-controlled command/query input crosses into durable task state. |
| Admin task route → task repository | Authorized command is translated into a D1 state mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Elevation | admin task router | high | mitigate | Use Better Auth session lookup plus server-resolved `canAccessCrawler`; exclude the legacy service-token middleware from this router. |
| T-16-06 | Tampering | create/retry DTO | high | mitigate | Strict Valibot objects permit only template/confirmation/cursor fields, while registry owns executable and target metadata. |
| T-16-07 | Repudiation | cancel/retry command | medium | mitigate | Repository transition/audit rows retain session operator and safe reason for every accepted mutation. |
| T-16-08 | Information Disclosure | task/log query | medium | mitigate | Resource access is checked per template and responses are limited to safe repository projections. |
| T-16-SC | Tampering | package supply chain | low | accept | This plan adds no dependency or package-manager install. |
</threat_model>

<verification>
Run focused router and AppType tests plus API type-check. Before the plan commit, run `npx gitnexus detect-changes --repo starye --scope all`; confirm only the intended admin task-router composition and tests are affected, leaving existing `serviceAuth` callers and unrelated dirty files untouched.
</verification>

<success_criteria>
An authenticated administrator can use only the two registry templates through stable internal Hono commands/queries, with no path to pass arbitrary crawler or provider parameters and no dependency on the legacy service token.
</success_criteria>

<output>
Create `.planning/phases/16-task-domain-foundation/16-02-SUMMARY.md` when done.
</output>
