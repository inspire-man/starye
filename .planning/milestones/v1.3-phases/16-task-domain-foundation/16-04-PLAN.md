---
phase: 16-task-domain-foundation
plan: 04
type: execute
wave: 3
depends_on: [16-02, 16-03]
files_modified:
  - apps/api/src/routes/admin/crawler-tasks/index.ts
  - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
  - apps/api/src/domain/crawler-tasks/log-redaction.ts
  - apps/api/src/domain/crawler-tasks/__tests__/runner-event-auth.test.ts
autonomous: true
gap_closure: true
requirements: [CTRL-01, CTRL-04, OPS-01]
must_haves:
  truths:
    - "具备 movie 资源权限的管理员携带自有 movie taskId 与 manga runId 时，logs/cancel/retry 都在任何读取或 mutation 前返回 404；权限不足的模板仍返回 403。"
    - "跨 task/run 请求在 repository 调用前返回，数据库中的 foreign run 状态与日志保持原值。"
    - "runner 日志与终态摘要同时支持敏感键的冒号和等号分隔格式，X-API-Key、secret、authorization、cookie 的值都被替换后才进入 projection。"
  artifacts:
    - path: apps/api/src/routes/admin/crawler-tasks/index.ts
      provides: taskId + runId 原子归属检查，并由所有 run-scoped handlers 复用
    - path: apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
      provides: 跨模板 logs/cancel/retry 的 403/404、repository side-effect 与状态不变回归覆盖
    - path: apps/api/src/domain/crawler-tasks/log-redaction.ts
      provides: 冒号/等号双格式的敏感文本脱敏和统一 storage projection
    - path: apps/api/src/domain/crawler-tasks/__tests__/runner-event-auth.test.ts
      provides: 四类敏感键在 normalized log 与 terminal summary 中均不泄露的回归覆盖
  key_links:
    - from: apps/api/src/routes/admin/crawler-tasks/index.ts
      to: crawler_task + crawler_run
      via: 单次参数绑定查询确认 run.task_id 与 path taskId 相等，再执行日志读取或 repository mutation
      pattern: task_id
    - from: apps/api/src/routes/admin/crawler-tasks/index.ts
      to: apps/api/src/domain/crawler-tasks/repository.ts
      via: 归属检查通过后才创建 repository 并调用 applyTransition/retryRun
      pattern: createCrawlerTaskRepository
    - from: apps/api/src/domain/crawler-tasks/log-redaction.ts
      to: normalized.log.message / terminalSummary
      via: 同一 redactRunnerEventText 输出先于 truncate 与 projection 构建
      pattern: redactRunnerEventText
---

<objective>
关闭 Phase 16 验证报告中的两个 blocker：为管理员 run-scoped 查询/命令建立 task-run 归属边界，并补齐常见 header/key 表示法的日志脱敏。

Purpose: 让 CTRL-01/CTRL-04 的资源边界与 OPS-01 的日志安全契约在跨模板和终态摘要路径上可重复验证。
Output: 共享的 task+run 归属检查、跨模板路由回归测试、冒号/等号双格式 redaction 实现及 normalized log/terminal summary 测试。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-task-domain-foundation/16-CONTEXT.md
@.planning/phases/16-task-domain-foundation/16-RESEARCH.md
@.planning/phases/16-task-domain-foundation/16-VERIFICATION.md
@.planning/phases/16-task-domain-foundation/16-02-SUMMARY.md
@.planning/phases/16-task-domain-foundation/16-03-SUMMARY.md

<interfaces>
- requireTaskAccess() currently authenticates the path task only; the run-scoped handlers in apps/api/src/routes/admin/crawler-tasks/index.ts must gain one reusable task/run ownership lookup before log SQL or repository calls.
- Plan 16-01 repository mutations are applyTransition(runId, event) and retryRun(runId); the route keeps these APIs and gates their invocation rather than duplicating lifecycle rules.
- normalizeRunnerEventForStorage() calls redactRunnerEventText() once for the message and uses that result for both detailed log and terminal summary; the redaction helper is the single storage-facing choke point.
- movie_admin maps to the movie resource and manga maps to the comic resource through the server-owned template registry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 原子校验管理员 task/run 归属并覆盖跨模板命令回归</name>
  <files>apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</files>
  <read_first>
    - apps/api/src/routes/admin/crawler-tasks/index.ts — 当前 session、template 权限和 logs/cancel/retry handler；保留既有 endpoint shape。
    - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts — 现有 Hono 测试 harness、session fixture 与 D1 statement mock。
    - apps/api/src/domain/crawler-tasks/repository.ts — createCrawlerTaskRepository、applyTransition、retryRun 的调用边界和错误语义。
    - apps/api/src/domain/crawler-tasks/template-registry.ts — movie/manga 到权限资源的 server-owned 映射。
    - .planning/phases/16-task-domain-foundation/16-CONTEXT.md — D-01、D-02、D-06、D-08、D-09 的资源、取消和重试约束。
    - .planning/phases/16-task-domain-foundation/16-VERIFICATION.md — blocker 对 task_id + run_id 归属及跨模板测试的原始描述。
  </read_first>
  <behavior>
    - Test 1: 对已授权 task，绑定另一模板 run 的 logs 请求返回 404；D1 query 以 taskId 和 runId 同时约束，不返回 foreign 日志。
    - Test 2: 同一跨模板组合的 cancel 请求返回 404，applyTransition 未调用，foreign run 状态与 transition 记录保持不变。
    - Test 3: 同一跨模板组合的 retry 请求返回 404，retryRun 未调用，foreign run 与 attempt 历史保持不变。
    - Test 4: task 本身的模板资源权限不足时仍返回 403；合法 task/run 组合继续走原有 logs/cancel/retry 行为。
  </behavior>
  <action>在编辑 requireTaskAccess、新增 task/run helper 或调整 run-scoped handler 前，执行 GitNexus upstream impact，至少覆盖 requireTaskAccess、adminCrawlerTasksRoutes、applyTransition 与 retryRun，查看直接 caller 和 execution flow；若结果为 HIGH 或 CRITICAL，在执行摘要中先报告 blast radius 再落地修改。先写失败的跨模板路由测试，使用 movie_admin 的自有 movie task、foreign manga run 和可观察的 repository spies；分别覆盖 logs、cancel、retry，并核验响应 404/403 矩阵、repository 未触发以及 D1 fixture 状态/attempt 未变化。实现一个所有 run-scoped handler 复用的归属检查：在已完成 session 与 task template 权限校验后，用一次参数化的 crawler_task 与 crawler_run 绑定查询确认 path taskId 对应的 runId，查询无绑定行时返回 404；通过后才执行日志 SELECT 或创建 repository。保留既有资源权限 403、合法 cancel/retry 状态机调用和 task/detail/list 路由语义；可在日志 SQL 中继续保留 task_id 防御性谓词，确保查询、取消、重试三条路径共享同一归属结论。runId 只在归属成功后进入 repository 授权路径；repository 状态机、legacy /crawlers service-token 路由和 Phase 17-19 执行器维持原边界。</action>
  <verify>
    <automated>pnpm --filter api test --run src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</automated>
    <automated>pnpm --filter api type-check</automated>
  </verify>
  <acceptance_criteria>每个 run-scoped handler 都先完成 taskId + runId 绑定检查；跨模板组合得到 404 且 repository 没有调用、D1 状态和 attempt 历史没有变化；无权限模板得到 403；合法组合的既有命令行为保持通过。</acceptance_criteria>
  <done>管理员路由已具备统一、参数绑定的 task/run 归属边界，并由跨 movie/manga 的 logs/cancel/retry 自动化回归证明。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展双格式日志脱敏并验证终态摘要</name>
  <files>apps/api/src/domain/crawler-tasks/log-redaction.ts, apps/api/src/domain/crawler-tasks/__tests__/runner-event-auth.test.ts</files>
  <read_first>
    - apps/api/src/domain/crawler-tasks/log-redaction.ts — 当前 authorization、cookie、token/secret/api-key 正则和 normalized log/terminalSummary 投影顺序。
    - apps/api/src/domain/crawler-tasks/__tests__/runner-event-auth.test.ts — 现有 redaction、UTF-8 上限和 terminal summary 测试样式。
    - apps/api/src/routes/internal/crawler-runs/index.ts — normalized projection 进入 repository 的 safeSummary/log 字段路径。
    - apps/api/src/domain/crawler-tasks/repository.ts — safe summary 与 bounded log 的持久化入口，确认 helper 输出覆盖所有 storage-facing 文本。
    - .planning/phases/16-task-domain-foundation/16-CONTEXT.md — D-10、D-11、D-12、D-13 的结构化、容量、保留与写入前脱敏要求。
    - .planning/phases/16-task-domain-foundation/16-VERIFICATION.md — blocker 对 colon-style header/key 泄露及 normalized log/terminal summary 覆盖的原始描述。
  </read_first>
  <behavior>
    - Test 1: 冒号与等号两种分隔形式下，X-API-Key、secret、authorization（含 bearer）和 cookie 的 fixture values 都从普通 normalized log message 中消失。
    - Test 2: failed、succeeded 或 cancelled 终态事件生成的 terminalSummary 与 log 使用同一安全文本，四类 fixture values 都不存在。
    - Test 3: 非敏感生命周期文本、URL/query redaction、counts/receipt allowlist 与 4 KiB UTF-8 截断行为继续通过。
  </behavior>
  <action>在编辑 redactRunnerEventText 或 normalizeRunnerEventForStorage 前，执行 GitNexus upstream impact，覆盖这两个 symbol 及 internal crawler-runs event flow，记录直接 caller 和风险等级；若为 HIGH 或 CRITICAL，在执行摘要中先报告 blast radius。先补充失败测试，使用 API_KEY_VALUE、SECRET_VALUE、AUTH_VALUE、COOKIE_VALUE 作为 fixture marker，分别放入冒号和等号分隔的 header/key 文本，并对普通 log 与 failed terminal event 同时断言 marker 不在 normalized message 或 terminal summary 中。扩展敏感键匹配以识别冒号与等号两种 delimiter、带前缀的 API-key header、authorization bearer 组合和 cookie 值；保持现有 URL/query redaction、allowlist、UTF-8 byte cap 和 terminal summary 由同一已脱敏 message 构建，保证 redact 在 truncate 与所有 storage projection 之前发生。原始 runner message、header 或 credential 只作为测试输入，不进入任何 storage-facing projection；HMAC、receipt、retention 与 runner-event route contract 保持原样。</action>
  <verify>
    <automated>pnpm --filter api test --run src/domain/crawler-tasks/__tests__/runner-event-auth.test.ts</automated>
    <automated>pnpm --filter api type-check</automated>
  </verify>
  <acceptance_criteria>常见敏感键在冒号/等号两种形式下均被替换；normalized log 与 terminal summary 都只含安全文本，既有结构化 allowlist、URL/query 脱敏和 4 KiB 上限回归保持通过。</acceptance_criteria>
  <done>日志脱敏 choke point 已覆盖验证报告列出的四类敏感键和两种分隔形式，且终态摘要与详细日志共享已验证的安全输出。</done>
</task>

</tasks>

<source_coverage_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 16 | 持久化受控任务、运行、日志、状态机与安全回调契约 | 16-01/02/03 + 16-04 | COVERED | 16-04 只收口验证报告中的两个 blocker。 |
| REQ | CTRL-01 | 固定模板与资源边界覆盖到 run-scoped 路由 | 16-02 + 16-04 | COVERED | task/run 绑定消除 foreign-template run 的授权绕过。 |
| REQ | CTRL-02 | task/attempt/log/operator/snapshot 持久化 | 16-01 | COVERED | 既有 schema/repository 验证保持有效。 |
| REQ | CTRL-03 | 闭合状态迁移与签名事件 | 16-01/03 | COVERED | 归属检查位于状态机调用之前，不复制状态规则。 |
| REQ | CTRL-04 | 失败/取消后的可追溯 retry attempt | 16-01/02 + 16-04 | COVERED | foreign retry 在 repository 前终止，合法 retry 仍沿原 immutable path。 |
| REQ | CTRL-05 | 模板 lease 防重 | 16-01 | COVERED | 本 gap 计划不改变 lease。 |
| REQ | OPS-01 | 独立 HMAC、时间窗、nonce、幂等与脱敏 | 16-03 + 16-04 | COVERED | 16-04 补齐 colon/equal credential redaction。 |
| RESEARCH | D1 control-plane boundary | 16-01/02/03 + 16-04 | COVERED | Worker 仍只做受控查询/状态写入，未引入执行器。 |
| RESEARCH | atomic task/run ownership | 16-04 | COVERED | 参数绑定 join 与 handler 前置检查落实研究约束。 |
| RESEARCH | structured redaction before persistence | 16-03 + 16-04 | COVERED | redaction choke point 覆盖 log 与 terminal summary。 |
| CONTEXT | D-01 | closed movie/manga registry | 16-02 + 16-04 | COVERED | 归属检查复用 registry 权限映射。 |
| CONTEXT | D-02 | logical task and shared lease | 16-01 | COVERED | task/run relation 验证依赖既有 D1 model。 |
| CONTEXT | D-03 | active duplicate idempotency | 16-01/02 | COVERED | gap 修复保持原 create semantics。 |
| CONTEXT | D-04 | queued→dispatching→running | 16-01/03 | COVERED | run mutation 仍只经 repository。 |
| CONTEXT | D-05 | lease renewal/lost-run failure | 16-01/03 | COVERED | gap 修复不触及 runner lease。 |
| CONTEXT | D-06 | queued cancel vs cancel_requested | 16-01/02 + 16-04 | COVERED | 归属通过后保持原 cancel transition。 |
| CONTEXT | D-07 | valid receipt wins cancel race | 16-03 | COVERED | gap 修复不改变 receipt precedence。 |
| CONTEXT | D-08 | confirmed immutable retry | 16-01/02 + 16-04 | COVERED | 归属通过后保持 retry eligibility。 |
| CONTEXT | D-09 | CAS/sequence and stale audit | 16-01/03 | COVERED | route 只增加前置 ownership check。 |
| CONTEXT | D-10 | structured lifecycle/progress logs | 16-03 + 16-04 | COVERED | tests 保持 allowlist projection。 |
| CONTEXT | D-11 | 4 KiB/500-row cap and terminal facts | 16-01/03 + 16-04 | COVERED | redaction 先于既有 cap，terminal summary 继续保留。 |
| CONTEXT | D-12 | permanent terminal data and 90-day detail retention | 16-01/03 | COVERED | 本 gap 计划不扩展清理范围。 |
| CONTEXT | D-13 | redact before D1/response | 16-03 + 16-04 | COVERED | 两种 delimiter 的敏感值都在 projection 前替换。 |
| CONTEXT | D-14 | independent runner-event HMAC | 16-03 | COVERED | callback secret 边界保持独立。 |
| CONTEXT | D-15 | signed binding and replay controls | 16-03 | COVERED | gap 修复不改变事件 envelope。 |
| CONTEXT | D-16 | current/previous key overlap | 16-03 | COVERED | gap 修复不改变 key rotation。 |

Deferred Ideas（Phase 17-19 runner、Actions、Dashboard、RUNBOOK、实时日志、额外模板和自动重试）保持在原阶段边界内，本计划没有新增覆盖项。
</source_coverage_audit>

<threat_model>
ASVS level: L1. 两个 blocker 均属于需要立即收口的高影响边界。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard session → admin crawler-task route | path taskId/runId 与用户资源权限进入 D1 查询和管理员 mutation。 |
| Admin route → crawler task repository | 已授权的 run 标识进入 cancel/retry 状态写入。 |
| Runner event text → log/terminal projections | runner 控制的消息进入持久化日志和管理员可读摘要。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-14 | Elevation | admin crawler-task run-scoped handlers | high | mitigate | 在日志查询或 repository 创建前，以参数绑定 task/run 归属查询和 registry resource check 共同决定 403/404。 |
| T-16-15 | Information Disclosure | logs endpoint and cross-template path | high | mitigate | foreign task/run 组合固定返回 404；回归测试验证日志为空、repository 无调用且 D1 状态不变。 |
| T-16-16 | Information Disclosure | log-redaction and terminalSummary | high | mitigate | 敏感键统一识别冒号/等号 delimiter，并在 normalized log 与 terminal summary 共用的 message projection 前替换。 |
| T-16-SC | Tampering | package-manager changes | low | accept | 计划无依赖安装，继续沿用现有包和锁文件。 |
</threat_model>

<verification>
先执行两个 focused test 文件和 pnpm --filter api type-check。提交实现前执行 npx gitnexus detect-changes --repo starye --scope all，审阅受影响 symbols/flows 仅包含管理员 task/run ownership、redaction helper 及其测试；工作树已有的无关变更保留原状。
</verification>

<success_criteria>
Phase 16 验证报告的两个 blocker 均关闭：所有 run-scoped 管理操作以 taskId + runId 绑定为前置条件，跨模板请求得到预期 403/404 且没有 repository side effect；日志和终态摘要对冒号/等号敏感键均完成脱敏，API focused tests 与 type-check 通过。
</success_criteria>

<output>
Create .planning/phases/16-task-domain-foundation/16-04-SUMMARY.md when done.
</output>
