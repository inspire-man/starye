---
phase: 17-local-runner-vertical-slice
plan: 03
type: execute
wave: 3
depends_on: [17-01, 17-02]
files_modified:
  - apps/dashboard/src/lib/api.ts
  - apps/dashboard/src/views/Crawlers.vue
  - apps/dashboard/src/views/__test__/Crawlers.test.ts
  - apps/dashboard/src/views/Movies.vue
  - apps/dashboard/src/views/__test__/Movies.test.ts
  - apps/dashboard/src/views/Comics.vue
  - apps/dashboard/src/views/__test__/Comics.test.ts
  - scripts/local-task-runner.ts
  - scripts/data-chain-surface-observation.ts
  - scripts/local-task-runner.e2e.ts
autonomous: false
requirements: [LOCAL-03, LOCAL-01, LOCAL-02, DATA-01]
must_haves:
  truths:
    - "D-13/D-14/D-15：Gateway Dashboard 的 Crawlers.vue 只显示可访问 movie/manga 固定模板，创建/取消/重试后立即刷新，页面可见时每 5 秒轮询、hidden/unmount 时停止，默认显示最新 50 条脱敏日志并用 cursor 加载更早记录。"
    - "D-10：取消走 ConfirmDialog 二次确认并准确保留 `cancel_requested` 文案，重试二次确认创建新 attempt；页面不乐观伪造 cancelled/succeeded。"
    - "D-05/D-06/D-07/D-11：仅 `succeeded` 加 API-validated receipt 才显示 primary ID、counts 和管理链接；receipt_missing/failed/cancelled 隐藏链接。"
    - "D-08：receipt 深链打开现有 Movies/Comics 编辑能力，实际验收执行一次可回退修改、读回确认、恢复原值。"
    - "D-04/D-12：本地 Gateway 验收绑定同一 run/template/validated content ID；受控 adapter 稳定证明取消，真实 movie/manga crawl 分别证明纵向执行，不触发 GitHub Actions。"
  artifacts:
    - path: apps/dashboard/src/lib/api.ts
      provides: typed session-bound task/run/log/receipt and direct-content read methods
    - path: apps/dashboard/src/views/Crawlers.vue
      provides: minimal fixed-template task panel and visibility-safe polling
    - path: apps/dashboard/src/views/Movies.vue
      provides: validated receipt query opens existing movie edit modal
    - path: apps/dashboard/src/views/Comics.vue
      provides: validated receipt query fetches/directly opens existing comic edit modal
    - path: scripts/local-task-runner.e2e.ts
      provides: local-only run-bound acceptance evidence and restore proof
  key_links:
    - from: apps/dashboard/src/views/Crawlers.vue
      to: apps/api/src/routes/admin/crawler-tasks/index.ts
      via: typed API wrapper and API-owned template/run/receipt projection
      pattern: crawlerTasks
    - from: apps/dashboard/src/views/Crawlers.vue
      to: apps/dashboard/src/views/Movies.vue
      via: validated receipt primaryContentId query parameter only
      pattern: receipt
    - from: scripts/local-task-runner.e2e.ts
      to: http://localhost:8080
      via: canonical Gateway browser observation and reversible CRUD confirmation
      pattern: localhost:8080
---

<objective>
在既有 Crawler 页面交付最小本地任务面板与 validated receipt 内容交接，并通过 Gateway 完成真实 movie/manga 纵向运行、协作取消及可回退 CRUD 的本地验收。

Purpose: 把已经受控的 API/run/receipt 合同变成管理员可见、可操作、不会泄露执行输入的 Dashboard 闭环。
Output: typed client、Crawlers.vue task panel、movie/comic receipt modal handoff、component/E2E proof scripts。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-local-runner-vertical-slice/17-CONTEXT.md
@.planning/phases/17-local-runner-vertical-slice/17-RESEARCH.md
@.planning/phases/17-local-runner-vertical-slice/17-PATTERNS.md
@.planning/phases/17-local-runner-vertical-slice/17-UI-SPEC.md
@.planning/phases/17-local-runner-vertical-slice/17-01-SUMMARY.md
@.planning/phases/17-local-runner-vertical-slice/17-02-SUMMARY.md
@.agents/skills/starye-ui-components/SKILL.md
@.agents/skills/starye-hono-rpc/SKILL.md

<interfaces>
- `apiFetch` and `api.admin` own dashboard session-bound fetching; views must not use raw fetch.
- `ConfirmDialog`, `SkeletonCard`, `ErrorDisplay`, toast and semantic tokens from `@starye/ui` are the UI-SPEC owner; no new registry, hard-coded hex, command/URL form, streaming log client, or third-party component library.
- Browser acceptance canonical origin is `http://localhost:8080`; API/Vite ports only support the local supervisor and are not final evidence origins.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 typed Dashboard API 并实现最小任务面板</name>
  <files>apps/dashboard/src/lib/api.ts, apps/dashboard/src/views/Crawlers.vue, apps/dashboard/src/views/__test__/Crawlers.test.ts</files>
  <read_first>
    - apps/dashboard/src/lib/api.ts — typed `apiFetch` and current admin method grouping.
    - apps/dashboard/src/views/Crawlers.vue — existing resource guard, refresh, confirmation, skeleton and cleanup patterns.
    - apps/dashboard/src/views/__test__/Comics.test.ts and Crawlers tests — component mock/fake-timer conventions.
    - .planning/phases/17-local-runner-vertical-slice/17-UI-SPEC.md — mandatory copy, token, spacing, state, polling, cursor and receipt rules.
    - apps/api/src/routes/admin/crawler-tasks/index.ts — server response shapes and ownership boundary from Plan 17-02.
  </read_first>
  <behavior>
    - Test 1: resource guards hide inaccessible template cards; visible CTAs post exact `{ template: 'movie' | 'manga' }`, select either newly created or `existing_active_run`, and do not expose free-form inputs.
    - Test 2: queued/dispatching/running/cancel_requested/succeeded/failed/cancelled use exact Chinese UI-SPEC semantics; cancellation/retry ConfirmDialog behavior and requests are not optimistic terminal changes.
    - Test 3: visible document polls selected run/logs every 5000ms, immediately refreshes after commands/visibility return, and clears on hidden/unmount while retaining prior valid data on refresh error.
    - Test 4: latest 50 safe logs render newest first, older cursor appends at the bottom without overlap; only validated succeeded receipt renders counts and a management link.
  </behavior>
  <action>Before modifying API client methods, Crawlers lifecycle handlers, or confirmation functions, run GitNexus upstream impact and report HIGH/CRITICAL blast radius. First add typed task/run/log/receipt interfaces and `api.admin` methods that call existing routes; no raw `fetch`, runner output, executable config, secret, command or source URL type may enter the view. Add UI-SPEC-conforming local task panel below existing header while retaining legacy statistics/failure areas: 0–2 accessible fixed template cards, exact primary CTA copy, semantic token utilities, 44px controls, SkeletonCard first load, ErrorDisplay/toast that retains last data, and a 448px safe-log scroller. Use a selected run detail and 5-second visibility-aware interval; reset immediately after create/cancel/retry. Implement destructive cancel and ordinary retry dialogs using the locked copy; preserve `cancel_requested` until server terminal state. Render receipt card/link only from a succeeded API validated receipt, and keep receipt_missing/cancelled content-link-free. Cover all behavior with component/fake-timer tests; do not redesign historical task views, add history/filtering, websocket streaming, or registry dependencies.</action>
  <verify>
    <automated>pnpm --filter dashboard test --run src/views/__test__/Crawlers.test.ts</automated>
    <automated>pnpm --filter dashboard type-check</automated>
  </verify>
  <acceptance_criteria>The dashboard exposes no executable crawler input, faithfully renders server run states/logs/receipt, and visible-only polling plus confirmed cancel/retry behavior pass deterministic component tests.</acceptance_criteria>
  <done>An authorized operator can create and observe the two local fixed templates, request cancellation/retry, and safely reach a verified receipt from the existing Crawler page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 validated receipt 接入既有 movie/comic 编辑器并完成 Gateway 证据</name>
  <files>apps/dashboard/src/views/Movies.vue, apps/dashboard/src/views/__test__/Movies.test.ts, apps/dashboard/src/views/Comics.vue, apps/dashboard/src/views/__test__/Comics.test.ts, scripts/local-task-runner.ts, scripts/data-chain-surface-observation.ts, scripts/local-task-runner.e2e.ts</files>
  <read_first>
    - apps/dashboard/src/views/Movies.vue and Comics.vue — existing edit modal, update, load and refresh lifecycle.
    - apps/dashboard/src/views/__test__/Movies.test.ts and Comics.test.ts — route/query and API mock patterns.
    - scripts/data-chain-smoke.ts and scripts/data-chain-surface-observation.ts — run-bound local Gateway evidence convention.
    - scripts/local-dev.ts — local supervisor/Gateway topology.
    - .planning/phases/17-local-runner-vertical-slice/17-CONTEXT.md — D-04, D-08 and production/Actions scope fence.
  </read_first>
  <behavior>
    - Test 1: a receipt query parameter is parsed once, validated as a route-owned primary ID, directly read through typed API, opens existing edit modal, and retains current content on 404/error.
    - Test 2: movie and comic edit test changes a reversible field, reads the persisted value, restores the original, and re-reads restoration without creating a new editor or crossing resource boundaries.
    - Test 3: local E2E starts/uses local runner and Gateway, binds a created run to template and verified receipt, proves movie and manga real vertical runs, proves controlled cancellation, and emits no secret/raw crawler data.
  </behavior>
  <action>Run GitNexus upstream impact before changing `openEditModal`, content update/load functions, or evidence helpers; report HIGH/CRITICAL before edits. Add a receipt-specific query contract to Movies and Comics that uses only the validated server `primaryContentId`, calls typed direct lookup, and then reuses the existing edit modal/update/refresh flows. Never accept a runner candidate query, fallback to an unscoped list, or create a second editor. Add focused UI tests for success, missing/foreign/error receipt and reversible patch/readback/restore. Add local-only acceptance script(s) that orchestrate no production provider: create each API-owned template, start/observe the local Node runner, bind emitted evidence to run/attempt/template/validated primary ID/counters, execute deterministic controlled-adapter cancellation, and use browser automation only at `http://localhost:8080` to follow the receipt then edit/readback/restore. Sanitize evidence to status, IDs and counts; never record sessions, cookies, callback secrets, raw source URL, headers or crawler stdout. Keep GitHub Actions, target-profile, Worker/Pages, production cancellation, full task history and Phase 19 features untouched.</action>
  <verify>
    <automated>pnpm --filter dashboard test --run src/views/__test__/Movies.test.ts src/views/__test__/Comics.test.ts</automated>
    <automated>pnpm --filter dashboard type-check; pnpm local:task-runner:e2e --target local</automated>
    <automated>pnpm --filter dashboard exec playwright test --config playwright.config.ts --grep "local task runner receipt"</automated>
  </verify>
  <acceptance_criteria>For both content types, a verified receipt opens the existing editor and a reversible edit is read back then restored through Gateway; local evidence proves run-to-receipt-to-CRUD, cancellation, and excludes remote/credential data.</acceptance_criteria>
  <done>Phase 17 has a locally reproducible dashboard-to-runner-to-validated-content management proof without claiming production/GitHub Actions execution.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- Typed Dashboard task API and local task panel in `Crawlers.vue`.
- Receipt-query handoff behavior for the existing Movie and Comic edit modals.
- Gateway-scoped run-bound local acceptance script/evidence contract.
</artifacts_this_phase_produces>

<source_coverage_audit>
| Source | ID | Plan coverage |
|---|---|---|
| Requirement | LOCAL-03 | Task 1 delivers status/cancel/retry/receipt/log UI through Gateway. |
| Requirement | LOCAL-01, LOCAL-02, DATA-01 | Task 2 proves both real templates, signed lifecycle/receipt and verified CRUD handoff. |
| Context | D-04, D-08, D-10, D-13–D-15 | real templates and rollback proof; confirmation, minimal panel, polling/cursor. |
| UI-SPEC | all covered considerations | Task 1 implements empty/loading/error/populated/partial/overflow/zero-one-many; Task 2 backstops long text in browser test. |
</source_coverage_audit>

<threat_model>
ASVS level: L1. High-severity threats block execution.

| Threat | Severity | Mitigation |
|---|---|---|
| Dashboard sends executable crawler input | high | only literal fixed template payloads; no command/URL/workflow/config fields or controls |
| receipt link reaches foreign/unverified content | high | link rendered only from succeeded API projection; direct route/resource guard and typed ID read |
| UI declares terminal cancel early | high | ConfirmDialog requests cancellation and preserves server `cancel_requested` until callback terminal event |
| runner/source secrets leak in logs or evidence | high | API safe projection only; component/evidence omit raw output, headers, cookies and local config |
| local proof mistaken for production | medium | evidence labels target local/Gateway and leaves Actions/Worker/remote workflow out of scripts and summary |
</threat_model>

<verification>
Run focused Dashboard tests, type check, local runner end-to-end and Gateway Playwright proof. Before each implementation commit run `npx gitnexus detect-changes --repo starye --scope all`; do not stage unrelated Phase 13 evidence or any local credentials.
</verification>

<success_criteria>
At `http://localhost:8080`, an authorized operator can complete the controlled local run lifecycle and reach a verified movie/comic editor for a reversible CRUD operation; the proof is explicitly local and leaves production Actions untouched.
</success_criteria>

<output>Create `.planning/phases/17-local-runner-vertical-slice/17-03-SUMMARY.md` when complete.</output>
