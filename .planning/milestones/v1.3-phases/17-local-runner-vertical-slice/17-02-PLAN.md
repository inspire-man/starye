---
phase: 17-local-runner-vertical-slice
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - apps/api/src/domain/crawler-tasks/receipt-validation.ts
  - apps/api/src/domain/crawler-tasks/types.ts
  - apps/api/src/domain/crawler-tasks/repository.ts
  - apps/api/src/domain/crawler-tasks/__tests__/receipt-validation.test.ts
  - apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts
  - apps/api/src/routes/internal/crawler-runs/index.ts
  - apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts
  - apps/api/src/schemas/crawler-tasks.ts
  - apps/api/src/routes/admin/crawler-tasks/index.ts
  - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
  - apps/api/src/routes/admin/comics/index.ts
  - apps/api/src/routes/admin/comics/handlers.ts
  - apps/api/src/routes/admin/comics/__tests__/comics.route.test.ts
autonomous: true
requirements: [LOCAL-02, DATA-01]
must_haves:
  truths:
    - "D-05/D-06：API 只在 D1 重查匹配 movie/comic 内容与入库摘要后保存 one verified primaryContentId、createdCount、updatedCount receipt。"
    - "D-07：空、缺失、错误模板或不匹配 summary 的 runner candidate 都走 safe `receipt_missing` failed terminal，不得标记成功。"
    - "D-08：validated receipt 是电影/漫画既有 CRUD 的稳定深链输入；漫画在需要时有受资源保护的直接 detail 读取。"
    - "D-11：已取消 run 即使保留既有入库审计也没有成功 receipt；有效 receipt 在取消竞态中仍遵循既有状态机优先级。"
    - "D-13/D-15：管理员 read model 仅暴露 API 验证后的 safe receipt，并以最新 50 条、oldest cursor 的无重叠日志分页读取。"
  artifacts:
    - path: apps/api/src/domain/crawler-tasks/receipt-validation.ts
      provides: template-aware D1 revalidation of runner candidates
    - path: apps/api/src/domain/crawler-tasks/repository.ts
      provides: receipt-safe terminal persistence and latest-first run log cursor
    - path: apps/api/src/routes/admin/crawler-tasks/index.ts
      provides: resource-guarded task/read model with verified receipt projection
    - path: apps/api/src/routes/admin/comics/index.ts
      provides: guarded direct comic lookup for validated management handoff
  key_links:
    - from: apps/api/src/routes/internal/crawler-runs/index.ts
      to: apps/api/src/domain/crawler-tasks/receipt-validation.ts
      via: signed succeeded candidate is revalidated before repository terminal transition
      pattern: validateReceiptCandidate
    - from: apps/api/src/routes/admin/crawler-tasks/index.ts
      to: apps/api/src/domain/crawler-tasks/repository.ts
      via: verified receipt and reverse-sequence cursor read model
      pattern: nextCursor
    - from: apps/api/src/routes/admin/comics/index.ts
      to: apps/dashboard/src/views/Comics.vue
      via: receipt primary ID direct detail handoff in Plan 17-03
      pattern: getComic
---

<objective>
把 runner candidate 变成 API/D1 验证过的 receipt，收紧任务读模型和日志 cursor，并为 movie/manga 既有内容编辑建立稳定、受资源保护的管理交接。

Purpose: 让 process exit 或非空 JSON 不能伪造入库成功，且 Dashboard 只能显示服务器验证后的内容标识与摘要。
Output: receipt validation owner、terminal/replay coverage、latest-first log contract、comics detail lookup and guarded task projection。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-local-runner-vertical-slice/17-CONTEXT.md
@.planning/phases/17-local-runner-vertical-slice/17-RESEARCH.md
@.planning/phases/17-local-runner-vertical-slice/17-PATTERNS.md
@.planning/phases/17-local-runner-vertical-slice/17-01-SUMMARY.md
@.agents/skills/starye-db-migration/SKILL.md
@.agents/skills/starye-hono-rpc/SKILL.md

<interfaces>
- `crawlerRuns.receiptSummaryJson` is the existing receipt storage field; this plan adds no parallel task store or unplanned migration.
- `decideCrawlerRunTransition()` keeps terminal/cancel race authority; API validation determines whether the attempted event is succeeded or `receipt_missing` failed.
- `adminMovies.get('/:id')` is the exact shape for a minimal guarded `adminComics.get('/:id')` when the current comic list cannot resolve a receipt ID.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 以 D1 内容重查取代 runner 自报 receipt</name>
  <files>apps/api/src/domain/crawler-tasks/receipt-validation.ts, apps/api/src/domain/crawler-tasks/types.ts, apps/api/src/domain/crawler-tasks/repository.ts, apps/api/src/domain/crawler-tasks/__tests__/receipt-validation.test.ts, apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts, apps/api/src/routes/internal/crawler-runs/index.ts, apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</files>
  <read_first>
    - packages/db/src/schema.ts — movie/comic rows and crawler run receipt JSON owner.
    - apps/api/src/domain/crawler-tasks/repository.ts — CAS transition/event outcome and safe terminal projection.
    - apps/api/src/domain/crawler-tasks/state-machine.ts — terminal and cancel-race semantics.
    - apps/api/src/routes/internal/crawler-runs/index.ts — signed succeeded event boundary.
    - .planning/phases/17-local-runner-vertical-slice/17-RESEARCH.md — candidate/receipt distinction and current repository outcome defect.
  </read_first>
  <behavior>
    - Test 1: seeded movie and manga candidates re-query their matching table and persist exactly templateKey, primaryContentId, createdCount and updatedCount only after verified aggregate facts exist.
    - Test 2: empty ID, missing row, wrong-template row, zero/missing aggregate, duplicate conflicting event, and stale CAS produce safe `receipt_missing`/non-success result with no successful receipt.
    - Test 3: `cancelled` persists no success receipt; a valid receipt in the existing `cancel_requested` race preserves the state-machine's `cancel_not_effective` priority and audit facts.
  </behavior>
  <action>Run GitNexus upstream impact before touching `processRunnerEvent`, `applyTransition`, `decideCrawlerRunTransition`, or existing DB receipt symbols; report HIGH/CRITICAL callers before edits. Write the fixture-backed receipt tests first. Add a narrow `ValidatedCrawlerRunReceipt` type and `validateReceiptCandidate({ database, candidate, templateKey })` that queries only movie for `movie` and comic for `manga`, verifies template-compatible identity plus non-empty ingestion aggregate, chooses one stable primary ID, and retains only safe counts/summary. Invoke validation after HMAC/envelope identity verification but before terminal state persistence. Invalid candidates must use the existing failed transition with safe code `receipt_missing`, preserve prior content/audit, and never write success receipt. Do not infer success from crawler return, runner log, candidate JSON, or `ApiClient.syncMovie`; do not alter shared content-sync transport or create a database migration unless an inspected compatibility requirement proves it essential.</action>
  <verify>
    <automated>pnpm --filter api test --run src/domain/crawler-tasks/__tests__/receipt-validation.test.ts src/domain/crawler-tasks/__tests__/repository.test.ts src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</automated>
    <automated>pnpm --filter api type-check</automated>
  </verify>
  <acceptance_criteria>Only a template-matched D1 row with a verified aggregate can become `succeeded`; all malformed/missing candidates are `failed:receipt_missing` and cancelled attempts display no success receipt.</acceptance_criteria>
  <done>Receipt success is a server-side persistence fact bound to one stable editable content ID, not a runner assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 暴露受保护 verified receipt、最新日志和漫画直接读取</name>
  <files>apps/api/src/schemas/crawler-tasks.ts, apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts, apps/api/src/routes/admin/comics/index.ts, apps/api/src/routes/admin/comics/handlers.ts, apps/api/src/routes/admin/comics/__tests__/comics.route.test.ts</files>
  <read_first>
    - apps/api/src/routes/admin/crawler-tasks/index.ts — session/resource/task-run ownership guard and current cursor query.
    - apps/api/src/routes/admin/movies/index.ts — authenticated `GET /:id` direct-resource pattern.
    - apps/api/src/routes/admin/comics/index.ts and handlers.ts — existing comic middleware/handlers to retain.
    - apps/api/src/lib/permissions.ts — movie/comic access vocabulary.
    - .planning/phases/17-local-runner-vertical-slice/17-UI-SPEC.md — latest-first 50-log, receipt visibility and cancellation copy contract.
  </read_first>
  <behavior>
    - Test 1: task detail only projects receipt fields for a server-validated succeeded run; failed `receipt_missing` and cancelled attempts contain no management ID/counts.
    - Test 2: first log page is 50 newest rows descending by sequence, nextCursor is the oldest returned sequence, and `sequence < cursor` returns older non-overlapping rows.
    - Test 3: every task/run/log/cancel/retry path retains Phase 16 task-run ownership plus movie/comic authorization; direct comic GET returns matching resource/404 behind existing auth.
  </behavior>
  <action>Before changing admin route handlers or existing comic symbols, run GitNexus upstream impact and report blast radius for HIGH/CRITICAL. Extend the existing strict task read DTOs rather than adding raw fetch endpoints. Project only API-validated receipt summary on succeeded runs; render no candidate ID, raw stdout, headers, cookie, URL, secret, or success fields for failed/cancelled. Replace oldest-first cursor SQL with `ORDER BY sequence DESC`, initial limit 50, `sequence < oldestCursor` for older rows, and explicit nullable `nextCursor`; add no full history/filtering surface. If direct comic lookup is absent, add the minimal resource-guarded `GET /admin/comics/:id` equivalent to movies' detail route, returning 404 without widening list behavior. Preserve session-only `canAccessCrawler`, task/run atomic ownership checks, existing retry/cancel state semantics, and all legacy crawler routes.</action>
  <verify>
    <automated>pnpm --filter api test --run src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts src/routes/admin/comics/__tests__/comics.route.test.ts</automated>
    <automated>pnpm --filter api type-check</automated>
  </verify>
  <acceptance_criteria>API consumers receive only validated success receipts, 50 newest safe logs with non-overlapping older pages, and resource-protected direct movie/comic lookup for a receipt ID.</acceptance_criteria>
  <done>The task read model makes D1-verified receipt and safe log pagination available for the Dashboard without expanding the production execution surface.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `ValidatedCrawlerRunReceipt` and `validateReceiptCandidate()`.
- Template-aware terminal event validation and latest-first cursor read model.
- A minimal guarded comic direct-detail API when required by receipt handoff.
</artifacts_this_phase_produces>

<source_coverage_audit>
| Source | ID | Plan coverage |
|---|---|---|
| Requirement | LOCAL-02 | signed terminal receipt reaches a transition only after API validation. |
| Requirement | DATA-01 | validated primary ID and counters are persisted/projection-safe; bad receipts fail. |
| Context | D-05–D-08 | D1 revalidation, stable ID, receipt_missing, reversible CRUD handoff. |
| Context | D-11, D-13, D-15 | cancelled receipt fence, safe audit projection, 50/latest cursor. |
</source_coverage_audit>

<threat_model>
ASVS level: L1. High-severity threats block execution.

| Threat | Severity | Mitigation |
|---|---|---|
| candidate references foreign/missing content | high | frozen template key plus server-side single-table D1 re-query and summary validation |
| stale/replayed receipt mutates terminal state | high | existing HMAC/idempotency/CAS before validated terminal write |
| receipt/log leaks crawler-controlled data | high | allowlisted verified fields and normalized safe logs only |
| direct comic lookup crosses resource boundary | high | existing auth and resource guard, 404 for absent/foreign content |
</threat_model>

<verification>
Focused receipt, repository, internal-route, admin-task and comic-route tests plus API type-check. Before every implementation commit run `npx gitnexus detect-changes --repo starye --scope all`, and stage only expected receipt/read-model files.
</verification>

<success_criteria>
No run can succeed from a self-reported identifier; a verified receipt is safe to hand to existing content management and task logs page backwards without overlap.
</success_criteria>

<output>Create `.planning/phases/17-local-runner-vertical-slice/17-02-SUMMARY.md` when complete.</output>
