---
phase: 24-fresh-production-dashboard-viewer-playback-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/domain/playback-evidence/types.ts
  - apps/api/src/schemas/playback-evidence.ts
  - apps/api/src/schemas/index.ts
  - apps/api/src/domain/playback-evidence/redaction.ts
  - apps/api/src/domain/playback-evidence/__tests__/contract.test.ts
  - apps/api/src/domain/movies/source-contract.ts
  - apps/api/src/domain/movies/__tests__/source-contract.test.ts
autonomous: true
requirements: [EVID-01, EVID-02, EVID-03]
decision_anchors: [D-05, D-06, D-07, D-08, D-11, D-12, D-14, D-15, D-19, D-20, D-21, D-22, D-23, D-25, D-26, D-27, D-28, D-29]
must_haves:
  truths:
    - "The playback evidence DTO accepts only one tuple-bound terminal summary with an explicit allowlisted event set and finite progress values."
    - "Redaction removes raw media/source, authentication, signature, runner, HTML, network, and exception material before any artifact or D1 consumer receives the summary."
    - "The public playback_verified projection remains separate from source readiness and receipt and requires the tuple, content, source revision, playing, and one-second progress gate."
  artifacts:
    - path: "apps/api/src/domain/playback-evidence/types.ts"
      provides: "Closed tuple-bound playback summary, event, outcome, projection, and rejection-history types"
    - path: "apps/api/src/schemas/playback-evidence.ts"
      provides: "Valibot request/response schemas and bounded allowlists"
    - path: "apps/api/src/domain/playback-evidence/redaction.ts"
      provides: "Redacted canonical object builder and forbidden-field/value scan"
  key_links:
    - from: "apps/api/src/schemas/playback-evidence.ts"
      to: "apps/api/src/domain/playback-evidence/types.ts"
      via: "schema output is narrowed to the closed domain DTO"
    - from: "apps/api/src/domain/movies/source-contract.ts"
      to: "apps/api/src/domain/playback-evidence/types.ts"
      via: "derivePlaybackProof consumes validated matching playback facts only"
---

<objective>
建立 tuple-bound playback evidence 的共享契约、运行时校验、脱敏边界和保守的 playback_verified projection。

Purpose: 为 D1、受保护 endpoint、Dashboard、Viewer 和 canonical verifier 提供同一组可执行字段，确保 D-05 至 D-08 的播放门槛、D-11 至 D-15 的分层投影、D-19 至 D-22 的 artifact 输入和 D-23、D-25 至 D-29 的终态/历史语义不会由各层自行解释。
Output: API domain/type/schema/redaction 模块与覆盖契约、脱敏、事件 allowlist、进度阈值和 projection 分离语义的测试。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
@.planning/phases/24-fresh-production-dashboard-viewer-playback-proof/24-CONTEXT.md
@.planning/phases/24-fresh-production-dashboard-viewer-playback-proof/24-RESEARCH.md
@.planning/phases/24-fresh-production-dashboard-viewer-playback-proof/24-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@apps/api/src/domain/movies/source-contract.ts
@apps/api/src/domain/movies/__tests__/source-contract.test.ts
@scripts/phase19-evidence.ts
@packages/config/src/deployment-target/data-chain-evidence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义闭合 playback evidence DTO 与 Valibot schema</name>
  <files>apps/api/src/domain/playback-evidence/types.ts, apps/api/src/schemas/playback-evidence.ts, apps/api/src/schemas/index.ts</files>
  <action>创建 tuple identity、provider/repair/receipt/source/playback 分层状态、Viewer path、source attempt、allowlisted event timeline、currentTime samples、artifact reference、outcome code 和 bounded rejection history 的闭合类型。按 D-05 至 D-08 固定 direct-first、TorrServer/Aria2 fallback、最多两次当前源尝试和显式未观察事件；按 D-11 至 D-12 只允许 task/run/attempt/provider、content/source revision、source type、分层状态、路径、事件时间和进度字段。使用现有 Hono/Valibot schema 习惯定义受控 POST 输入和响应，不接受 target/workflow/command/URL/secret 等客户端字段，并导出到 schema index 供 route 与客户端复用。</action>
  <verify>
    <automated>pnpm --filter api exec vitest run src/domain/playback-evidence/__tests__/contract.test.ts -x</automated>
  </verify>
  <done>类型与 schema 对 tuple、content ID、source revision、source type、事件 allowlist、数值边界、终态 outcome 和受控字段形成单一契约；D-05、D-06、D-07、D-08、D-11、D-12 的字段要求在测试中可见。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 artifact 输入的脱敏与确定性字段校验</name>
  <files>apps/api/src/domain/playback-evidence/redaction.ts, apps/api/src/domain/playback-evidence/__tests__/contract.test.ts</files>
  <action>实现只从闭合 DTO 构造安全对象的 redaction helper，并扫描 key 与 value 形状，覆盖 D-12、D-19、D-20、D-21、D-22、D-25 和 D-29：保留 tuple-bound 标识、非敏感 target label、bounded path、事件时间、currentTimeBefore/After/delta、artifact reference 和 outcome；拒绝 raw media/source URL、签名 query、token、cookie、session/nonce/signature、Authorization、workflow/command、原始 runner JSON、HTML、network log、exception 和媒体内容。让 JSON 序列化输入稳定，pair builder 可复用同一安全对象；不得把脱敏失败转换为成功状态。</action>
  <verify>
    <automated>pnpm --filter api exec vitest run src/domain/playback-evidence/__tests__/contract.test.ts -x</automated>
  </verify>
  <done>敏感字段和值在 schema 通过后仍会被安全扫描拦截；相同输入产生稳定安全对象；脱敏/校验失败返回可区分的 checkpoint 输入错误，且测试证明失败 evidence 不会升级任何 success projection。</done>
</task>

<task type="auto">
  <name>Task 3: 收紧 public playback_verified projection</name>
  <files>apps/api/src/domain/movies/source-contract.ts, apps/api/src/domain/movies/__tests__/source-contract.test.ts</files>
  <action>修改 `derivePlaybackProof` 前先运行 GitNexus upstream impact analysis，并记录其直接调用者与风险；在现有 projection 边界上接入 D-15、D-23、D-26、D-27、D-28 的条件：只有 matching task/run/attempt、content ID、source revision、允许的 repair terminal/readback、`canplay` 与 `playing`、两次有限 currentTime 且 delta 至少 1 秒、无终态 error、处于 evidence window 内的安全事实才返回 `playback_verified`。source health 与 receipt 字段保持原值，失败只返回 playback history 状态；旧、迟到、过期和 revision mismatch 输入只产生 `late/stale/ignored` 语义，不改变 current projection。</action>
  <verify>
    <automated>pnpm --filter api exec vitest run src/domain/movies/__tests__/source-contract.test.ts src/domain/playback-evidence/__tests__/contract.test.ts -x</automated>
  </verify>
  <done>projection 测试覆盖成功门槛、缺少任一事件、delta 不足、终态 error、tuple/content/source revision mismatch、late/stale 输入和 source/receipt 独立性；`pnpm --filter api type-check` 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/verifier -> API DTO | Untrusted browser observations enter schema validation. |
| API DTO -> artifact/D1 projections | Only closed, redacted, finite fields may cross the persistence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | playback evidence schema/projection | high | mitigate | Closed Valibot fields, tuple/content/source-revision checks, and conservative projection tests. |
| T-24-02 | Information Disclosure | redaction.ts | high | mitigate | Allowlist object construction plus forbidden key/value scans before artifact or D1 use. |
| T-24-03 | Repudiation | outcome/history types | medium | mitigate | Explicit accepted/duplicate/conflict/stale/late/ignored outcomes remain typed facts. |
| T-24-04 | Denial of Service | event/progress bounds | medium | mitigate | Bound event count, string lengths, retry count, timestamps, and observation window values. |
</threat_model>

<verification>
<automated>pnpm --filter api exec vitest run src/domain/playback-evidence/__tests__/contract.test.ts src/domain/movies/__tests__/source-contract.test.ts -x</automated>
<automated>pnpm --filter api type-check</automated>
<automated>git diff --check</automated>
</verification>

<success_criteria>
- A single closed DTO defines every field that can reach the Phase 24 endpoint, artifact pair, or public playback projection.
- Redaction and event/progress gates are regression-tested, including explicit negative cases for sensitive media/session material.
- Playback verification is derived only from the complete browser evidence gate, never from provider, receipt, source readiness, page load, or one event alone.
</success_criteria>

<output>
Create `.planning/phases/24-fresh-production-dashboard-viewer-playback-proof/24-01-SUMMARY.md` when done.
</output>
