---
phase: 21-source-health-and-local-repair-players-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/domain/movies/source-contract.ts
  - apps/api/src/domain/movies/__tests__/source-contract.test.ts
  - apps/api/src/domain/crawler-tasks/types.ts
  - apps/api/src/schemas/crawler-tasks.ts
  - packages/db/src/schema.ts
autonomous: true
requirements: [SRC-02, REP-01]
must_haves:
  truths:
    - "每个受控源的公开 projection 只包含 direct/magnet/TorrServer、inactive/unverified/failed、observedAt、bounded reason 和独立 eligibility 语义；magnet 默认 unverified。"
    - "repair_players 的输入、快照和 receipt 是 operation-discriminated 的单电影契约，固定 reason 和 targetIntent，普通 movie receipt 与 repair receipt 保持可区分。"
    - "D1 schema 为 source observation 事实、source revision、operation 和事件身份提供可追加且可去重的持久化边界。"
  artifacts:
    - path: "apps/api/src/domain/movies/source-contract.ts"
      provides: "SourceType/SourceHealth、bounded reason、per-source projection、eligibility 与 redaction contracts"
      exports: ["SourceType", "SourceHealth", "SourceHealthProjection", "isEligiblePlayer"]
    - path: "apps/api/src/domain/crawler-tasks/types.ts"
      provides: "repair_players snapshot、repair receipt union、operation and replay identity types"
      contains: "repair_players"
    - path: "apps/api/src/schemas/crawler-tasks.ts"
      provides: "strict repair command schema with fixed target intent"
      contains: "restore_playable_sources"
    - path: "packages/db/src/schema.ts"
      provides: "crawler task operation and append-only source observation schema"
      contains: "movie_source_observation"
  key_links:
    - from: "apps/api/src/schemas/crawler-tasks.ts"
      to: "apps/api/src/domain/crawler-tasks/types.ts"
      via: "strict Valibot request parser produces the operation-bound repair command"
    - from: "apps/api/src/domain/movies/source-contract.ts"
      to: "packages/db/src/schema.ts"
      via: "bounded source fields and sourceRevision map to persisted observation/current projection"
    - from: "apps/api/src/domain/crawler-tasks/types.ts"
      to: "packages/db/src/schema.ts"
      via: "operation, movieId, runId, attempt, sequence and eventId are persisted as the same identity"
---

<objective>
建立 Phase 21 的 source health、repair_players operation 和数据库契约，使后续 API、runner 与 Dashboard 都消费同一套 bounded identity。

Purpose: per-source observation 必须与 Phase 20 的 movie readiness 并存，repair 必须保留单电影、sourceRevision 和专用 receipt 边界。
Output: typed source/repair contracts、strict command schema、additive Drizzle schema changes。
</objective>

## Artifacts this phase produces

- SourceType/SourceHealth/SourceHealthProjection、bounded reason allowlist、repair_players snapshot/receipt unions and strict repair command schema.
- Drizzle crawler operation discriminator、movie_source_observation table definition、relations/index declarations。

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-CONTEXT.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-RESEARCH.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-PATTERNS.md
@.planning/phases/20-source-contract-receipt-boundary-and-sun-064/20-03-SUMMARY.md
@packages/db/MIGRATION.md
</context>

<assumption_delta_decision>
采用 add-alongside：保留 `movie.id` 作为 canonical content identity 和既有 `movie_source_state` readiness current projection，新增 `(movieId, sourceRevision, operation, runId, attempt, sequence, eventId, sourceOrdinal)` 组成的 source observation identity，并把 operation 同步放入 task snapshot/列。理由是 Phase 20 消费者与普通 movie crawler 权限继续稳定，而 repair facts 获得独立审计身份。接受的债务是 operation 枚举与 observation rows 后续需要随新的受控 operation 扩展 migration。
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 bounded source health 与 repair operation contracts</name>
  <files>apps/api/src/domain/movies/source-contract.ts, apps/api/src/domain/movies/__tests__/source-contract.test.ts, apps/api/src/domain/crawler-tasks/types.ts, apps/api/src/schemas/crawler-tasks.ts</files>
  <read_first>
    - apps/api/src/domain/movies/source-contract.ts
    - apps/api/src/domain/movies/__tests__/source-contract.test.ts
    - apps/api/src/domain/crawler-tasks/types.ts
    - apps/api/src/schemas/crawler-tasks.ts
    - .planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-CONTEXT.md
  </read_first>
  <depends_on>Phase 20 source contract and the existing crawler task snapshot/receipt types are present.</depends_on>
  <action>扩展 `source-contract.ts` 的 server-owned projection：增加 `SourceType` 的 `direct|magnet|TorrServer`、`SourceHealth` 的 `inactive|unverified|failed`、bounded reason allowlist 与每源 projection；复用 `isEligiblePlayer`，让 inactive 行保留在展示集合而 eligibility 仅由 active 且有内容的受控 source 决定。为 magnet 固定默认 `unverified`，让 failed 只经 bounded reason 映射文案，保留 ready/readiness 与 browser playback proof 的独立层级，落实 D-01、D-02、D-03、D-04、D-16。扩展 `crawler-tasks/types.ts` 为 operation-discriminated `repair_players` snapshot、单 `movieId`/`sourceRevision` 绑定、`runId`/`attempt`/`sequence`/`eventId` 身份和专用 repair receipt；普通 movie receipt 继续使用原 union，落实 D-09、D-10、D-13。扩展 `crawler-tasks.ts` 的 Valibot strict schema，只接收 canonical movie identity、当前 disposition 派生的 `no_source|source_failed` 与字面量 `restore_playable_sources`，将 adapter、workflow、target、secret 保留为 server-owned 字段，落实 D-06、D-07。先补表格化 source contract、magnet、inactive、failed、redaction、receipt discriminator 和 extra-field rejection 测试，再实现 contracts。</action>
  <acceptance_criteria>
    - `SourceHealthProjection` 的公开字段没有 raw source URL、page、exception 或 signature material。
    - magnet fixture 得到 `unverified`，inactive fixture 可见且 eligible count 为零贡献，failed fixture 只返回 allowlist reason。
    - repair command 只接受 movie identity、`no_source|source_failed` 和 `restore_playable_sources`，额外字段被 strict schema 拒绝。
    - repair receipt 至少携带 `operation`、movie ID、sourceRevision、observedAt 和 bounded source summary，普通 receipt 不会通过 repair validator 的 discriminator。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter api exec vitest run src/domain/movies/__tests__/source-contract.test.ts</automated>
  </verify>
  <done>D-01、D-02、D-03、D-04、D-06、D-07、D-09、D-10、D-13、D-16 的 typed contract、strict input 和 focused tests 已存在，且 ready 与 browser playback proof 保持独立。</done>
</task>

<task type="auto">
  <name>Task 2: 增加 source observation 与 task operation 的 Drizzle schema</name>
  <files>packages/db/src/schema.ts</files>
  <read_first>
    - packages/db/src/schema.ts
    - packages/db/drizzle/0029_source_contract_receipt_boundary.sql
    - packages/db/MIGRATION.md
    - .planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-PATTERNS.md
  </read_first>
  <depends_on>Task 1 的 operation、source health 和 sourceRevision contracts 已确定。</depends_on>
  <action>在 `packages/db/src/schema.ts` 中采用 additive schema：为现有 crawler task 增加独立 `operation` discriminator，历史普通任务使用既有 movie/manga 行为；新增 `movie_source_observation` append-only fact table，逐源保存 movieId、operation、runId、attempt、event sequence、eventId、sourceRevision、sourceOrdinal、sourceType、health、observedAt、bounded reason 和 eligibility，并建立 movie/revision 与 `(runId,eventId,sourceOrdinal)` 去重索引及必要 foreign keys/relations。仅保存 bounded source facts，原始来源值、请求材料、页面内容、异常详情和签名材料留在服务端边界内，落实 D-01、D-03、D-04、D-10、D-11、D-13、D-15；让 `movie.id` 继续作为 canonical content identity，落实 assumption-delta decision。此任务只改 Drizzle schema，迁移生成与 apply 由下一计划的 blocking task 执行。</action>
  <acceptance_criteria>
    - Drizzle schema 导出 task operation、observation table、relations、foreign keys 和 replay uniqueness 所需定义。
    - schema 字段覆盖 sourceRevision、operation、runId、attempt、sequence、eventId、sourceOrdinal 和全部 bounded source health 字段。
    - schema 没有 raw URL、page body、exception detail、token、private key 或签名 payload 字段。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/db type-check</automated>
  </verify>
  <done>数据库代码层已能表达 append-only source observation、current movie identity 和独立 repair operation；迁移 apply 由下一计划的 [BLOCKING] migration task 完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard/API -> contract parser | 用户输入跨入 strict repair command schema。 |
| API -> D1 | server-owned bounded projection、operation 和 replay identity 写入持久层。 |
| Runner callback -> API types | 后续签名事件只能携带可验证的 operation/source identity。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | `crawler-tasks.ts` repair command | high | mitigate | strict Valibot allowlist 固定 reason 与 `restore_playable_sources`，额外 executable fields 直接拒绝。 |
| T-21-02 | Information Disclosure | source contract / observation schema | high | mitigate | public DTO 只允许 bounded type/health/time/reason/eligibility，schema 只保存 bounded facts。 |
| T-21-03 | Tampering | observation identity | high | mitigate | sourceRevision、runId、attempt、sequence、eventId 和 sourceOrdinal 进入 schema/index，供后续 CAS/replay 校验。 |
| T-21-04 | Elevation | repair operation snapshot | high | mitigate | operation 与单 movieId/sourceRevision 绑定，后续 route 复用 session/resource permission。 |
</threat_model>

<verification>
运行 source-contract focused tests、`pnpm --filter @starye/db type-check`，并确认 schema diff 只包含 Phase 21 的 observation/operation 边界。
</verification>

<success_criteria>
后续计划可以直接导入 typed source health、repair snapshot、strict command 和 observation schema；Phase 20 的 readiness 与 ordinary crawler receipt contract 保持兼容。
</success_criteria>

<output>
Create `.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-01-SUMMARY.md` when done.
</output>
