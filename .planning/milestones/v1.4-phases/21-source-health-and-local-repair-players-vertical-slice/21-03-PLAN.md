---
phase: 21-source-health-and-local-repair-players-vertical-slice
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - apps/api/src/domain/crawler-tasks/template-registry.ts
  - apps/api/src/domain/crawler-tasks/receipt-validation.ts
  - apps/api/src/domain/crawler-tasks/__tests__/receipt-validation.test.ts
  - apps/api/src/domain/crawler-tasks/repository.ts
  - apps/api/src/domain/crawler-tasks/state-machine.ts
  - apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts
  - apps/api/src/domain/crawler-tasks/__tests__/state-machine.test.ts
autonomous: true
requirements: [SRC-02, REP-01]
must_haves:
  truths:
    - "repair_players task snapshot 只绑定一个 movieId、当前 sourceRevision、固定 targetIntent 和 server-owned operation/template，普通 movie template 选择保持兼容。"
    - "专用 repair receipt 只有在 observation persistence/readback 已验证时才可进入 succeeded；普通 contentIds/templateKey receipt 不会被当作修复成功。"
    - "transient execution/write/readback failure 只新增一次 automatic attempt；确定性失败终止，manual retry 在新任务创建前重新读取当前 disposition。"
    - "重复 eventId replay 幂等，旧 attempt、乱序 sequence、sourceRevision 冲突事件保持当前 source health。"
  artifacts:
    - path: "apps/api/src/domain/crawler-tasks/template-registry.ts"
      provides: "operation-aware server-owned snapshot registry and fail-closed selection metadata"
    - path: "apps/api/src/domain/crawler-tasks/receipt-validation.ts"
      provides: "repair receipt discriminator/readback validator and redacted projection"
    - path: "apps/api/src/domain/crawler-tasks/repository.ts"
      provides: "single-movie task creation, CAS event binding, automatic retry and manual new-task retry"
    - path: "apps/api/src/domain/crawler-tasks/state-machine.ts"
      provides: "bounded retry/terminal transition decisions"
  key_links:
    - from: "template-registry.ts"
      to: "repository.ts"
      via: "validated operation snapshot is persisted with task/run/attempt rows"
    - from: "receipt-validation.ts"
      to: "source-reconciliation.ts"
      via: "repair receipt is validated against authoritative observation/readback"
    - from: "repository.ts"
      to: "state-machine.ts"
      via: "CAS transition and retry outcome share the same state rules"
---

<objective>
把现有 crawler control plane 扩展为 operation-aware repair_players 生命周期，并固定 receipt、retry、manual retry、replay 和 stale-event 的服务端规则。

Purpose: repair task 需要与普通 movie crawler 明确分流，同时把 D-14/D-15 的状态保护接在已有 D1 task/run/attempt/lease/CAS 事实之上。
Output: operation registry、repair receipt validator、repository/state-machine implementation 和 focused lifecycle tests。
</objective>

## Artifacts this phase produces

- Operation-aware template registry、repair receipt discriminator validator、single-movie task creation and state-machine retry/replay branches。
- receipt-validation、repository、state-machine focused regression fixtures for ordinary/repair receipts, automatic retry and manual new-task retry。

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-CONTEXT.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-RESEARCH.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-PATTERNS.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-02-PLAN.md
</context>

<assumption_delta_decision>
采用 add-alongside：task templateKey 继续支持现有 permission/read-model，operation 作为独立 normalized discriminator 并复制到 immutable snapshot；repair receipt 作为普通 movie receipt 旁的专用 union。理由是 operation 身份可被 repository、runner 和 route 各层 fail-closed 校验，既有 movie crawler 数据仍可读。接受的债务是每个新 operation 需要显式 registry 分支与 receipt validator 分支。
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 operation registry 与 repair receipt validation</name>
  <files>apps/api/src/domain/crawler-tasks/template-registry.ts, apps/api/src/domain/crawler-tasks/receipt-validation.ts, apps/api/src/domain/crawler-tasks/__tests__/receipt-validation.test.ts</files>
  <read_first>
    - apps/api/src/domain/crawler-tasks/template-registry.ts
    - apps/api/src/domain/crawler-tasks/receipt-validation.ts
    - apps/api/src/domain/crawler-tasks/__tests__/receipt-validation.test.ts
    - apps/api/src/domain/crawler-tasks/types.ts
    - apps/api/src/domain/movies/source-reconciliation.ts
  </read_first>
  <depends_on>21-02 的 observation readback service 与 21-01 的 operation/receipt types 已可导入。</depends_on>
  <action>在 template registry 旁增加 operation-aware snapshot builder/validator：普通 movie/manga 继续按既有 template 与 entrypoint 选择，repair_players 必须校验 operation、version、单 movieId、sourceRevision、固定 reason/targetIntent 和 server-owned target，缺少或错配 operation 时返回 fail-closed result，落实 D-06、D-07、D-09、D-10。扩展 receipt-validation 的 validateReceiptCandidate 分支，要求 repair receipt 的 operation、movieId、sourceRevision、observedAt、bounded source summary 与 source-reconciliation authoritative readback 一致；普通 contentIds/templateKey receipt 保持原验证路径，raw source/request/runner/signature fields 通过 redaction projection 排除，落实 D-03、D-13、D-16。测试普通与 repair discriminator、wrong operation、missing summary、revision mismatch、ordinary receipt masquerading、sensitive sentinel redaction。</action>
  <acceptance_criteria>
    - operation snapshot 解析器对缺失/错配 operation fail closed，普通 movie/manga snapshot 的既有测试继续通过。
    - repair receipt 只由同 movieId/sourceRevision/observedAt 的 authoritative readback 产生，普通 receipt shape 不会被接受。
    - public receipt projection 仅暴露 operation、movie identity、sourceRevision、observedAt、bounded source summary 与有限状态。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__/receipt-validation.test.ts</automated>
  </verify>
  <done>D-03、D-06、D-07、D-09、D-10、D-13、D-16 的 operation registry 与专用 receipt validator 已建立。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 task/run/attempt CAS、bounded retry 与 manual new-task retry</name>
  <files>apps/api/src/domain/crawler-tasks/repository.ts, apps/api/src/domain/crawler-tasks/state-machine.ts, apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts, apps/api/src/domain/crawler-tasks/__tests__/state-machine.test.ts</files>
  <read_first>
    - apps/api/src/domain/crawler-tasks/repository.ts
    - apps/api/src/domain/crawler-tasks/state-machine.ts
    - apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts
    - apps/api/src/domain/crawler-tasks/__tests__/state-machine.test.ts
    - apps/api/src/domain/crawler-tasks/receipt-validation.ts
  </read_first>
  <depends_on>Task 1 的 operation snapshot 和 repair receipt validator 已完成；21-02 的 sourceRevision/readback service 已完成。</depends_on>
  <action>扩展 createCrawlerTaskRepository 的 repair creation、dispatch validation、recordRunnerEvent 和 retry paths：创建时重新读取 movie disposition/revision，只接受当前 no_source 或 source_failed 与固定 intent，单 task 绑定单 movie，并在一个 batch 中写入 server-owned snapshot、operation、sourceRevision、task/run/attempt/lease rows，落实 D-06、D-07、D-09。让 terminal success 先经过 observation persistence 与 authoritative readback，再持久化 repair receipt；将明确 transient execution/write/readback codes 限定为最多一次 additional automatic attempt，确定性 input/contract/source failure进入终态；manual retry 创建新 task，并在创建前重新读取当前 source disposition，落实 D-12、D-14。复用 findRunnerEvent、recordRunnerEvent、stateVersion/lastEventSequence CAS 与 eventId/nonce identity：相同 replay 返回幂等结果，旧 attempt、乱序 sequence、sourceRevision conflict 只返回受控 stale/conflict，不覆盖当前 source health，落实 D-15。测试 duplicate/conflict/stale event、one-attempt cap、deterministic terminal failure、manual new-task reread、single-movie binding 和 receipt-before-success。</action>
  <acceptance_criteria>
    - repair task snapshot 和 persisted rows 始终绑定一个 movieId 与创建时 sourceRevision。
    - 自动 retry 对 transient 分类最多产生一个新 attempt；manual retry 产生新 task 并使用创建前 reread 的当前 reason/revision。
    - identical replay 幂等，old/sequence/revision conflict 保持 current projection，terminal task 只返回受控结果。
    - succeeded 状态的 repair receipt 已通过 observation write、same-movie readback 和专用 validator。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__/repository.test.ts src/domain/crawler-tasks/__tests__/state-machine.test.ts</automated>
  </verify>
  <done>D-06、D-07、D-09、D-12、D-14、D-15 的 lifecycle/CAS/retry/replay contract 已由 repository 与 state-machine focused tests 证明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin command -> repository | current disposition/revision 与 permission 进入 task creation guard。 |
| Signed runner event -> repository | event identity、sequence、attempt 和 source revision 进入 CAS。 |
| Receipt candidate -> terminal state | readback/validator 作为 succeeded 前置条件。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-09 | Tampering | repair task creation | high | mitigate | current-state reread、single movie binding、operation snapshot 和 sourceRevision CAS。 |
| T-21-10 | Repudiation | runner event replay | high | mitigate | eventId/nonce/body identity、attempt/sequence checks、duplicate/conflict audit result。 |
| T-21-11 | Tampering | retry state machine | high | mitigate | explicit transient allowlist with one automatic additional attempt and separate manual new-task path。 |
| T-21-12 | Information Disclosure | receipt validator | high | mitigate | repair-specific bounded union and raw-field redaction tests before terminal persistence。 |
</threat_model>

<verification>
运行 receipt、repository、state-machine focused tests与 API type-check；检查 automatic retry、manual retry、replay/conflict 和 success/readback 证据分别呈现。
</verification>

<success_criteria>
control plane 能区分 ordinary crawler 与 repair_players，且 receipt、attempt、sourceRevision、event replay 和 stale protection 均由可重复测试覆盖。
</success_criteria>

<output>
Create .planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-03-SUMMARY.md when done.
</output>
