---
phase: 21-source-health-and-local-repair-players-vertical-slice
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - packages/db/drizzle/0030_source_health_repair.sql
  - packages/db/src/__tests__/source-health-repair-migration.test.ts
  - apps/api/src/domain/movies/source-reconciliation.ts
  - apps/api/src/domain/movies/__tests__/source-reconciliation.test.ts
  - apps/api/src/routes/movies/__tests__/services/sync.service.test.ts
autonomous: true
requirements: [SRC-02, REP-01]
must_haves:
  truths:
    - "本地 D1 已 apply source health/repair migration，schema readback 能证明 observation、operation、索引和 relation 存在。"
    - "受控 observation 写入会 append fact、更新同一 movie 的 current projection、递增 sourceRevision，并在完成后返回权威 bounded readback。"
    - "stale sourceRevision、写入失败和 readback 失败均返回可解释的 bounded result，inactive 保留展示但排除 eligibility。"
  artifacts:
    - path: "packages/db/drizzle/0030_source_health_repair.sql"
      provides: "generated additive local D1 migration for task operation and source observations"
    - path: "apps/api/src/domain/movies/source-reconciliation.ts"
      provides: "observation acceptance, current projection update, CAS and authoritative readback"
      exports: ["acceptRepairSourceObservation", "readRepairSourceReadback"]
    - path: "packages/db/src/__tests__/source-health-repair-migration.test.ts"
      provides: "migration schema/index/foreign-key and sensitive-field checks"
  key_links:
    - from: "apps/api/src/domain/movies/source-reconciliation.ts"
      to: "packages/db/src/schema.ts"
      via: "bounded observation facts and movie_source_state current projection are written atomically"
    - from: "apps/api/src/domain/movies/source-reconciliation.ts"
      to: "apps/api/src/domain/movies/source-contract.ts"
      via: "the same eligibility and bounded reason derivation produces the readback DTO"
    - from: "packages/db/drizzle/0030_source_health_repair.sql"
      to: "local D1"
      via: "repository migration command applies and sqlite PRAGMA reads back the schema"
---

<objective>
完成 Phase 21 的本地数据库迁移与 source observation 服务边界，让 repair adapter 后续只能通过服务/API 获得同一 movie/sourceRevision 的权威 readback。

Purpose: D-11、D-12、D-13 要求服务端拥有 observation persistence、projection、readback 和专用 receipt 前置条件。
Output: local-applied additive migration、observation reconciliation service、CAS/readback regression tests。
</objective>

## Artifacts this phase produces

- Generated packages/db/drizzle/0030_source_health_repair.sql and packages/db/src/__tests__/source-health-repair-migration.test.ts。
- acceptRepairSourceObservation/readRepairSourceReadback service boundary、append-only observation/current projection persistence、CAS/readback tests and cache invalidation wiring。

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-CONTEXT.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-RESEARCH.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-PATTERNS.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-01-PLAN.md
@packages/db/MIGRATION.md
</context>

<assumption_delta_decision>
采用 add-alongside：movie_source_state 继续作为 movie.id 的当前 readiness projection，movie_source_observation 作为按 sourceRevision/run identity 追加的 bounded fact。理由是 accepted observation 可以解释 retry/stale 结果，同时既有 readiness API 不需要改写 canonical content identity。接受的债务是 observation projection 与既有 player rows 之间需要由 reconciliation service 持续保持 readback 一致。
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] 生成并 apply 本地 D1 migration，再做 live schema readback</name>
  <files>packages/db/drizzle/0030_source_health_repair.sql, packages/db/src/__tests__/source-health-repair-migration.test.ts</files>
  <read_first>
    - packages/db/src/schema.ts
    - packages/db/MIGRATION.md
    - packages/db/drizzle/0029_source_contract_receipt_boundary.sql
    - packages/db/src/__tests__/crawler-provider-association-migration.test.ts
  </read_first>
  <depends_on>21-01 Task 2 已完成全部 Drizzle schema edits。</depends_on>
  <action>[BLOCKING] 严格按 packages/db/MIGRATION.md 执行 pnpm --filter=@starye/db run generate，在 apps/api 目录执行 pnpm exec wrangler d1 migrations apply starye-db --local，再运行 migration test 与 Wrangler sqlite_master/PRAGMA readback，证明 operation、observation table、foreign keys 和 indexes 已实际存在。使用仓库生成器产生的下一 migration 序号；migration 只保留 additive bounded schema facts，敏感 sentinel 不进入 SQL。此任务必须在 21-01 全部 schema edits 之后完成，并在后续 API/runner/UI 与最终 verification 之前通过。</action>
  <acceptance_criteria>
    - local D1 migration apply 命令成功，live schema readback 能看到 operation、observation table、foreign keys、indexes。
    - migration test 通过，migration 文本不含 raw URL、page、exception、token、private key 或 signature material。
    - 生成的 migration 与 packages/db/src/schema.ts 一致，且后续任务以这份 live schema 为前提。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter=@starye/db run generate; cd apps/api; pnpm exec wrangler d1 migrations apply starye-db --local; pnpm --filter @starye/db exec vitest run src/__tests__/source-health-repair-migration.test.ts</automated>
  </verify>
  <done>[BLOCKING] 本地 schema migration 已生成、apply、readback 和 migration test 全部通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 observation acceptance、current projection 与 authoritative readback</name>
  <files>apps/api/src/domain/movies/source-reconciliation.ts, apps/api/src/domain/movies/__tests__/source-reconciliation.test.ts, apps/api/src/routes/movies/__tests__/services/sync.service.test.ts</files>
  <read_first>
    - apps/api/src/domain/movies/source-reconciliation.ts
    - apps/api/src/domain/movies/source-contract.ts
    - apps/api/src/routes/movies/__tests__/services/sync.service.test.ts
    - packages/db/src/schema.ts
    - apps/api/src/lib/gateway-cache.ts
  </read_first>
  <depends_on>Task 1 的 generated migration 已 apply 到本地 D1；21-01 的 contracts/schema 已完成。</depends_on>
  <action>在现有 reconciliation boundary 中增加 acceptRepairSourceObservation 与 readRepairSourceReadback（或同职责的现有命名）：校验 operation、single movieId、runId、attempt、sequence/eventId 与 expected sourceRevision，按 bounded source rows append observation fact，并在同一受控事务中更新 movie_source_state current projection；随后从持久化状态重新读取同一 movie/sourceRevision，返回 per-source type/health/observedAt/reason/eligibility 和有限 summary。复用 isEligiblePlayer，使 inactive 仍可读但不贡献 eligibility；对 magnet 派生 unverified；将写入、CAS 和 readback 错误映射为 bounded repairable/source_failed result，raw DB/runner exception 留在服务端，落实 D-01、D-02、D-03、D-04、D-11、D-12、D-13、D-15。成功写入时清理现有 API detail 与 Gateway movies cache group，使 canonical Gateway read 能看到同一 movie 的新 projection，落实研究中的 cache boundary。补测试覆盖 append/current projection、revision increment、duplicate/stale rejection、write failure、readback failure、active/inactive/blank source eligibility 和 raw sentinel redaction。</action>
  <acceptance_criteria>
    - accepted observation 先完成 D1 persistence/readback，再允许调用方得到 repair-success candidate。
    - returned movieId/sourceRevision/observedAt/source summary 与刚读回的同一 movie 完全一致，source health 只来自 persisted facts。
    - stale revision、重复 observation、写入失败和 readback failure 各自产生 bounded outcome，当前 projection 保持有效版本。
    - cache invalidation 后通过同一 movie identity 的 Gateway read 能读取新 source projection。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter api exec vitest run src/domain/movies/__tests__/source-reconciliation.test.ts src/routes/movies/__tests__/services/sync.service.test.ts</automated>
  </verify>
  <done>D-01、D-02、D-03、D-04、D-11、D-12、D-13、D-15 的服务端 persistence/readback/CAS 事实成立，且本地 live schema apply/readback 已有可核验证据。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repair adapter/API -> reconciliation service | 未受信 observation 只能进入 operation/sourceRevision 校验。 |
| Reconciliation service -> D1 | projection 与 append-only fact 在同一事务边界内更新。 |
| API cache -> Gateway read | cache invalidation 后才允许对外宣称新 source state。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-05 | Tampering | acceptRepairSourceObservation | high | mitigate | sourceRevision、run/attempt/event identity 做 CAS，旧 observation 返回受控 stale 结果。 |
| T-21-06 | Information Disclosure | reconciliation DTO/cache | high | mitigate | persisted raw materials 进入 DTO 前经过 bounded projection，测试用 sentinel 检查 API/cache projection。 |
| T-21-07 | Denial of Service | D1 observation append | medium | mitigate | bounded source row count、summary size 和 reason allowlist，duplicate identity 快速收敛。 |
| T-21-08 | Tampering | cache freshness | high | mitigate | write/readback 成功路径清理 API detail 与 Gateway movies group 后再提供 fresh read。 |
</threat_model>

<verification>
完成 blocking migration apply/readback，再运行 source reconciliation 与 sync regression tests、API/db type-check，并记录同一 movie/sourceRevision 的 readback 证据。
</verification>

<success_criteria>
本地 D1 schema 实际包含 Phase 21 表结构；受控 observation 经过 persistence、projection、cache invalidation 和 authoritative readback 后才可被后续专用 receipt 使用。
</success_criteria>

<output>
Create .planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-02-SUMMARY.md when done.
</output>
