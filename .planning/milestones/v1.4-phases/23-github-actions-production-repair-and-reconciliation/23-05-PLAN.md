---
phase: 23-github-actions-production-repair-and-reconciliation
plan: 05
type: execute
wave: 4
depends_on:
  - 23-03
  - 23-04
files_modified:
  - apps/api/src/routes/admin/crawler-tasks/index.ts
  - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
  - apps/dashboard/src/lib/api.ts
  - apps/dashboard/src/views/Crawlers.vue
  - apps/dashboard/src/views/__test__/Crawlers.test.ts
autonomous: true
requirements:
  - REP-02
  - REP-03
must_haves:
  truths:
    - "D-14: Dashboard task detail keeps latestRunId/current application attempt as the focal point and places older attempts in collapsed expandable history."
    - "D-15 and D-17: current and historical rows show only bounded provider, lease, reconciliation, receipt, source revision, status, and safe-log facts; provider run navigation uses an allowlisted link and never displays raw runner material."
    - "D-16: an active repair locks same-movie duplicate entry and focuses the current task; a terminal repairable task rereads disposition before creating a new task."
    - "D-17: provider accepted/running/completed, repair result, receipt validation, and current source state are separate visible fact layers."
    - "Phase 24 boundary: Phase 23 shows `播放未验证` for the repair/reconciliation surface and does not claim `playing` or `currentTime` production proof; fresh tuple, Viewer, and playback events remain Phase 24 evidence."
    - "UI-SPEC empty consideration: zero movie repair tasks show the documented empty heading/body and fixed production repair path."
    - "UI-SPEC empty current-attempt consideration: a task without a reported run keeps identity visible and shows `等待 attempt 上报` with pending facts."
    - "UI-SPEC loading collection/detail consideration: initial load uses SkeletonCard and visible refresh retains the last valid projection with inline refresh state."
    - "UI-SPEC loading confirmation/action consideration: submission has the existing busy state and closes confirmation only after the command response is consumed."
    - "UI-SPEC error collection/detail consideration: task-load failure uses role=alert, exposes refresh, and retains prior valid detail."
    - "UI-SPEC error provider/reconciliation/receipt consideration: each failure has an independent bounded outcome and next action; provider completion remains a provider fact."
    - "UI-SPEC populated current-attempt consideration: latestRunId drives identity, status timeline, fact grid, source projection, and safe logs at the top."
    - "UI-SPEC populated source projection consideration: ready, no_source, repairing, and source_failed show source revision, observedAt, eligible count, bounded rows, and next action."
    - "UI-SPEC partial provider/lease/reconciliation/receipt consideration: missing facts render `尚未上报` or `等待对账` without inferring success."
    - "UI-SPEC partial receipt/readback consideration: incomplete or mismatched identity/revision/readback remains `receipt 校验失败` with current projection and bounded reason."
    - "UI-SPEC overflow older-history consideration: old attempts stay collapsed and expanded content uses allowlisted fields in a constrained region."
    - "UI-SPEC overflow safe-log consideration: logs keep the 448px bounded viewport, wrapped messages, structured-column overflow, and cursor pagination."
    - "UI-SPEC zero-one-many consideration: zero attempts show pending identity, one shows only current focus, and many show current first with older attempts collapsed."
    - "UI-SPEC long-text identity/failure/provider consideration: IDs and messages wrap anywhere while codes and provider metadata retain readable bounded labels."
    - "UI-SPEC long-text repair-action consideration: movie title and bounded reason wrap in confirmation while actionable controls keep stable 44px targets."
    - "UI-SPEC coverage status: all 15 covered considerations remain explicit, with 0 backstop and 0 unresolved items."
  artifacts:
    - path: "apps/api/src/routes/admin/crawler-tasks/index.ts"
      provides: "Bounded task/run/attempt/lease/reconciliation/provider/receipt/source DTOs, active duplicate lock, and fresh manual-retry disposition check."
    - path: "apps/dashboard/src/lib/api.ts"
      provides: "Typed allowlisted provider, lease, reconciliation, retry, receipt/readback, and attempt-history client contracts."
    - path: "apps/dashboard/src/views/Crawlers.vue"
      provides: "Current-attempt focal Dashboard surface, expandable bounded history, movie repair entry/lock, safe logs, and distinct fact layers."
    - path: "apps/dashboard/src/views/__test__/Crawlers.test.ts"
      provides: "UI state-shape coverage for all 15 UI-SPEC considerations and Phase 24 playback boundary."
  key_links:
    - from: "apps/api/src/routes/admin/crawler-tasks/index.ts"
      to: "apps/dashboard/src/lib/api.ts"
      via: "Admin route response projection matches typed current-attempt/history/provider/lease/reconciliation/receipt fields. D-14, D-15, D-17"
    - from: "apps/dashboard/src/lib/api.ts"
      to: "apps/dashboard/src/views/Crawlers.vue"
      via: "Dashboard renders only bounded DTO groups, keeps current attempt pinned, and never selects provider controls. D-15, D-16, D-17"
    - from: "apps/dashboard/src/views/Crawlers.vue"
      to: "apps/api/src/routes/admin/crawler-tasks/index.ts"
      via: "Repair action sends only confirmed/movieId/reason/targetIntent, honors active lock, and uses server-owned same-movie navigation. D-03, D-16"
---

<objective>
把 production reconciliation 的 bounded DTO 接回 Dashboard：当前 attempt focal point、可展开历史、lease/reconciliation/provider-run-link projection、active duplicate lock 和 same-movie return 都可见，同时保持 provider/repair/receipt/source/playback fact 分层与 Phase 24 播放边界（D-03, D-14, D-15, D-16, D-17）。

Purpose: 完成 REP-02/REP-03 的用户可观察闭环，让用户能从 `no_source` 或 `source_failed` 进入受控生产修复、看到 retry 与旧事实，并回到同一 content identity；Dashboard 不把 provider completion 或 ready projection 冒充实际播放证据。
Output: admin read model、Dashboard API types、Crawlers view 和 focused UI/route tests。
</objective>

## Artifacts This Phase Produces

- Server-owned lease/reconciliation/provider-run-link/attempt history DTO projection (D-14, D-15, D-17).
- Active same-movie repair duplicate lock and fresh disposition reread for manual new-task entry (D-16).
- Current attempt focal Dashboard with all 15 UI-SPEC state considerations covered and no unresolved/backstop items.
- Explicit Phase 24 playback boundary: no Phase 23 fresh tuple or `playing`/`currentTime` proof claim.

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-github-actions-production-repair-and-reconciliation/23-CONTEXT.md
@.planning/phases/23-github-actions-production-repair-and-reconciliation/23-PATTERNS.md
@.planning/phases/23-github-actions-production-repair-and-reconciliation/23-UI-SPEC.md
@.planning/phases/23-github-actions-production-repair-and-reconciliation/23-03-SUMMARY.md
@.planning/phases/23-github-actions-production-repair-and-reconciliation/23-04-SUMMARY.md
@apps/api/src/routes/admin/crawler-tasks/index.ts
@apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
@apps/dashboard/src/lib/api.ts
@apps/dashboard/src/views/Crawlers.vue
@apps/dashboard/src/views/__test__/Crawlers.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project bounded admin repair DTOs and active duplicate lock (D-03, D-14, D-15, D-16, D-17)</name>
  <read_first>apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts, apps/api/src/domain/crawler-tasks/repository.ts, apps/api/src/domain/crawler-tasks/types.ts, apps/api/src/domain/crawler-tasks/provider-association.ts</read_first>
  <files>apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts, apps/dashboard/src/lib/api.ts</files>
  <behavior>
    - Repair task detail returns the same movie identity, operation, reason, target intent, source revision, latestRunId, allowedNextAction, current attempt, bounded older attempts, provider summary, lease result, reconciliation outcome, receipt validation, source readback, and safe-log cursor.
    - The repair command accepts only confirmed, movieId, reason, and targetIntent; server state supplies operation, snapshot, workflow, target, authorization, and callback configuration.
    - An active repair for the same movie returns a bounded existing-active/current-task projection and prevents a duplicate task; a terminal repair rereads current source disposition before a new task is created.
    - Provider run links are generated from the allowlisted repository and numeric run ID, and raw URL, command, secret, signature, and runner JSON fields are absent from the DTO.
  </behavior>
  <action>Extend `readRepairTaskResponse` and the admin task detail projection to expose the repository fields from Plans 03 and 04 as typed, bounded DTO groups. Keep the current run first and older attempts queryable, derive task-level retry beside the current application attempt per D-14, and include provider, lease, reconciliation, receipt, and source facts as separate fields per D-15 and D-17. Enforce D-16 at both the active repair command and manual new-task retry boundary: lock same-movie active work, focus the current task, and reread current disposition before allowing a new task. Preserve the D-03 command shape and update `apps/dashboard/src/lib/api.ts` without leaking server/provider controls to the client.</action>
  <verify>
    <automated>pnpm --filter api exec vitest run src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</automated>
    <automated>pnpm --filter api type-check</automated>
  </verify>
  <done>Admin route tests prove current/history DTO shape, task-level retry projection, active same-movie duplicate lock, fresh disposition reread, allowlisted provider link, redaction, same-content identity, and closed repair command fields (D-03, D-14, D-15, D-16, D-17).</done>
</task>

<task type="auto">
  <name>Task 2: Build the current-attempt Dashboard and bounded history surface (D-10, D-12, D-14, D-15, D-16, D-17)</name>
  <read_first>apps/dashboard/src/views/Crawlers.vue, apps/dashboard/src/views/__test__/Crawlers.test.ts, apps/dashboard/src/lib/api.ts, .planning/phases/23-github-actions-production-repair-and-reconciliation/23-UI-SPEC.md, .agents/skills/starye-ui-components/SKILL.md</read_first>
  <files>apps/dashboard/src/views/Crawlers.vue, apps/dashboard/src/views/__test__/Crawlers.test.ts</files>
  <action>Extend the existing Crawlers view with the UI-SPEC order and tokens: movie identity strip, current attempt focal timeline, independent provider/lease/reconciliation/repair/receipt facts, current bounded source projection, safe logs, and collapsed older attempt disclosures. Implement D-14 current-attempt focus and D-15 bounded history, including late/stale/ignored/duplicate/conflict labels on the originating old attempt. Implement D-16 active same-movie lock and fixed `发起生产修复` confirmation, keep provider navigation as the only provider link, and follow D-17 separation of provider completion, repair success, receipt validation, and source readiness. Cover empty, loading, error, populated, partial, overflow, zero-one-many, and long-text states listed in the 15 UI-SPEC considerations with existing `@starye/ui` SkeletonCard/ConfirmDialog/Toast, token colors, lucide icons, keyboard focus, aria-live/status/alert, visible-page 5-second refresh, and reduced-motion behavior. Keep any browser playback evidence field sourced only from the Phase 24 evidence contract: Phase 23 renders `播放未验证` for repair/reconciliation data and does not claim a fresh `playing` or `currentTime` proof.</action>
  <verify>
    <automated>pnpm --filter dashboard exec vitest run src/views/__test__/Crawlers.test.ts</automated>
    <automated>pnpm --filter dashboard type-check</automated>
  </verify>
  <done>Dashboard tests cover all 15 UI-SPEC considerations with 0 backstop and 0 unresolved states, current attempt pinned above expandable bounded history, active duplicate lock and same-movie return, safe provider link/redaction, distinct fact layers, and no Phase 23 claim of fresh Viewer playback evidence (D-10, D-12, D-14, D-15, D-16, D-17).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Dashboard browser to admin API | User input and task identifiers cross into authenticated repair and retry endpoints. |
| Admin API to Dashboard DTO | Server/provider facts are reduced to a browser-safe projection. |
| Dashboard to provider navigation | A bounded provider run link crosses to an external provider page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-23-20 | Tampering | Repair command and manual retry | high | mitigate | D-03 closed command fields, D-16 active lock, and server-side disposition reread prevent arbitrary or duplicate repair creation. |
| T-23-21 | Information Disclosure | Task detail DTO and safe logs | high | mitigate | D-15 and D-17 use explicit projection allowlists and exclude raw URLs, commands, secrets, signatures, and runner JSON. |
| T-23-22 | Spoofing | Same-movie navigation and provider link | medium | mitigate | Generate content and provider links from server-owned IDs and allowlisted repository/run IDs only. |
| T-23-23 | Tampering | Current versus historical UI facts | high | mitigate | D-10 through D-13 render old outcomes on their originating attempt and consume CAS-selected current projection. |
| T-23-24 | Information Disclosure | Playback evidence boundary | medium | mitigate | Keep Phase 24 playback evidence separate from Phase 23 reconciliation DTO and display `播放未验证` until the evidence contract exists. |
</threat_model>

<verification>
Run admin route tests, Dashboard Crawlers tests, API type-check, Dashboard type-check, and `git diff --check`. For the eventual canonical browser check, use the Gateway route `http://localhost:8080/dashboard/crawlers`; verify the current attempt/history hierarchy and bounded links without treating the page as Phase 24 playback proof.
</verification>

<success_criteria>
REP-02 and REP-03 are observable from a bounded admin DTO and Dashboard surface: current attempt and task-level retry are focal, old facts remain expandable, same-movie repair is locked while active, and provider, repair, receipt, source, and Phase 24 playback facts remain distinct (D-14, D-15, D-16, D-17).
</success_criteria>

<output>
Create `.planning/phases/23-github-actions-production-repair-and-reconciliation/23-05-SUMMARY.md` when done.
</output>
