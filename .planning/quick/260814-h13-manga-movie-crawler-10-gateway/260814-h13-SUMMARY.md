---
quick_id: 260814-h13
slug: manga-movie-crawler-10-gateway
status: complete
completed_at: 2026-08-14
---

# Quick Task Summary

将 Manga/Movie crawler 的默认处理批次收敛到约 10 条，并完成本地运行状态验证。

## Changes

- Movie `DEFAULT_LIMITS` 与 GitHub Actions profile 均为 `maxMovies=10`、`maxPages=10`。
- Manga 默认 `maxMangasPerRun=10`、`maxChaptersPerNew=10`、`maxChaptersPerUpdate=10`。
- `.env.example` 同步为 10 条批次示例。
- 新增 Movie/Manga 配置回归测试。

## Verification

- 定向配置与 crawler 测试：通过，3 files / 9 tests。
- crawler 全量测试：通过，29 files / 153 tests。
- `pnpm lint`：通过。
- `pnpm type-check`：通过。
- `pnpm check:services`：通过，Gateway/API/Dashboard/Blog/Auth/Comic/Movie listeners healthy，Gateway readiness healthy。
- `http://localhost:8080/api/health`：最终返回 HTTP 200；首次请求偶发慢，重试后稳定成功。
- `git diff --check`：通过。
