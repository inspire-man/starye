---
quick: 260818-p7i
title: JavDB canonical source switch
status: complete
branch: main
---

# Goal

将电影爬虫的 JavDB 生产入口统一到 `https://javdb.com`，避免新任务继续使用旧镜像域名。

# Tasks

1. 更新 `JavDBStrategy.baseUrl` 和回归测试中的详情 URL。
2. 同步可读 JavDB HTML fixtures 的导航入口，保留 Cloudflare challenge 历史快照原样。
3. 运行 crawler 定向/全量单测、类型检查、diff 校验和 GitNexus staged 变更检测。
