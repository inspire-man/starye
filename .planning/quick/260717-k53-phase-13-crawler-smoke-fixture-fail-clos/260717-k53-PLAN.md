---
quick: 260717-k53-phase-13-crawler-smoke-fixture-fail-clos
phase: 13-full-chain-data-smoke
plan: "260717-k53"
type: execute
mode: quick-full
wave: 1
depends_on: ["13-03", "13-04"]
files_modified:
  - packages/config/src/deployment-target/data-chain-evidence.ts
  - packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
  - packages/crawler/src/smoke/data-chain-fixture.ts
  - packages/crawler/src/smoke/__tests__/data-chain-fixture.test.ts
  - packages/config/src/deployment-target/mutation-entry.ts
  - packages/config/src/deployment-target/__tests__/mutation-entry.test.ts
  - packages/db/scripts/target-d1-mutation.ts
  - packages/db/scripts/__tests__/target-d1-mutation.test.ts
  - scripts/data-chain-smoke.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
autonomous: false
requirements: [DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, TEST-05]
user_setup:
  - service: Selected Cloudflare target
    why: Remote acceptance needs the existing authorized credentials and Dashboard session; missing access remains checkpoint evidence.
must_haves:
  truths:
    - "Each successful controlled smoke ingest writes exactly ten deterministic, non-R18 fixtures; any invalid payload, acknowledgement failure, or count mismatch has no success claim."
    - "Local and remote D1 smoke verify the exact derived ten-code set and one active player per item before recording the audited count."
    - "The Phase 13 evidence and remote Dashboard-to-viewer path remain bound to the single primary itemCode/itemId tuple; count evidence does not introduce a second UI tuple."
    - "Remote execution remains explicit-target, prepared-entry-only, preflight-first, and checkpointed before mutation when provider access is unavailable."
  artifacts:
    - packages/crawler/src/smoke/data-chain-fixture.ts
    - packages/db/scripts/target-d1-mutation.ts
    - scripts/data-chain-smoke.ts
  key_links:
    - "targetId/runId -> deterministic primary code plus nine closed sibling codes -> exactly ten ApiClient upserts -> prepared-child count parser -> D1 cardinality check -> typed D1 evidence row -> existing primary Dashboard/viewer proof"
---

<objective>
Extend the Phase 13 controlled crawler fixture from one to exactly ten fixed deterministic non-R18 records, and make the local/remote smoke path audit that complete batch without changing the one primary item tuple used for canonical API, Dashboard, and viewer evidence.

Purpose: provide a meaningful bounded ingest sample while preserving the established fail-closed selected-target boundary.
Output: tested ten-item fixture contract, closed D1 cardinality verification, and count-bearing non-secret smoke evidence.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-04-PLAN.md
@packages/crawler/src/smoke/data-chain-fixture.ts
@packages/crawler/scripts/target-crawl-mutation.ts
@packages/config/src/deployment-target/mutation-entry.ts
@packages/db/scripts/target-d1-mutation.ts
@scripts/data-chain-smoke.ts
</context>

<source_audit>
SOURCE | ID | Requirement | Plan coverage
GOAL | Phase 13 | Selected-target local-to-production data chain remains real, bounded, and Gateway/canonical-domain based | This plan extends only the controlled ingest sample and preserves the existing primary tuple flow.
REQ | DATA-03 | Controlled deterministic ingest records an identity | Task 1 creates the fixed ten-record batch while retaining one primary identity.
REQ | DATA-04 | D1/API/admin correlation | Tasks 2-3 require exact D1 batch cardinality and retain primary D1/API/admin tuple correlation.
REQ | DATA-05 | Dashboard proof | Task 3 regression-protects the existing primary Dashboard append contract; no new Dashboard items/paths are introduced.
REQ | DATA-06 | Canonical viewer proof | Task 3 regression-protects the existing primary canonical viewer contract; no direct origin is added.
REQ | DATA-07 | Non-secret repeatable evidence | Tasks 1-3 add typed, non-secret batch-count evidence and checkpoint tests.
REQ | TEST-05 | Repeatable smoke output | All tasks add focused Vitest contracts plus guarded local/remote smoke checks.
RESEARCH | bounded fixture | No corpus crawl, free argv, endpoint, or target | All tasks retain the prepared registry and fixed deterministic inputs.
CONTEXT | D-01 | Auditable target/run identity | Tasks 1-3 retain the sole primary code/id, deriving siblings only from it.
CONTEXT | D-02 | Restricted fixture adapter only | Task 1 uses only existing ApiClient sync and closed prepared entry.
CONTEXT | D-03 | Local-first Gateway contract | Task 3 keeps the existing local prerequisites and Gateway-only evidence paths.
CONTEXT | D-04 | Same item through D1/API/Dashboard/viewer | Task 3 preserves the primary tuple for all UI/API surfaces.
CONTEXT | D-05 | Explicit preflight and fail-closed remote mutation | Task 3 retains remote preflight before the batch fixture.
CONTEXT | D-06 | Redacted evidence | Tasks 1-3 store only typed count and primary identity, never secrets or origins.
CONTEXT | D-07 | Phase 13 real execution, not docs-only | Task 3 supplies actual guarded smoke acceptance; Phase 14 work remains excluded.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and ingest the closed deterministic ten-item fixture batch</name>
  <files>packages/config/src/deployment-target/data-chain-evidence.ts, packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts, packages/crawler/src/smoke/data-chain-fixture.ts, packages/crawler/src/smoke/__tests__/data-chain-fixture.test.ts</files>
  <behavior>
    - The explicit target/run candidate deterministically produces a ten-item batch with one unchanged primary code and nine fixed sibling codes; every record is non-R18 and has exactly one controlled player.
    - The fixture validates the entire closed batch before its first transport call, invokes the existing service-auth ApiClient exactly ten times, and reports only the primary code plus the literal audited count after all ten acknowledgements succeed.
    - Invalid shapes, duplicate/mismatched codes, R18 data, wrong cardinality, or a failed acknowledgement cannot return a synced observation.
  </behavior>
  <action>Before editing the existing exported symbols, rerun GitNexus upstream impact for `createDataChainFixture` and `runDataChainFixture`; the planning result is LOW and limited to the controlled smoke runner/child, but report any changed result before editing. Per D-01, define one immutable batch-size constant of ten and derive the primary fixture plus nine sibling fixtures solely from the existing explicit target/run candidate; retain the primary `itemCode` as the only cross-surface identity. Per D-02, use only the existing fixed fixture adapter and `ApiClient.syncMovie` transport. Validate all ten generated records before calling the transport, require one successful acknowledgement per record, and return a closed observation containing the primary code and exact item count only after all ten succeed. Extend the typed evidence model so a D1 observation may record the validated batch count, rejecting an absent, non-D1, or non-ten value; preserve redaction and the current single primary itemCode/itemId evidence schema per D-06. Do not add a corpus crawler, fixture file argument, endpoint/target argument, source-site request, R18 record, new player relation, or success fallback after a partial failure. Add RED-first tests for stable composition, exact ten calls, all-non-R18/player constraints, deterministic rerun, rejected malformed batch before transport, and no synced result when an acknowledgement fails.</action>
  <verify>
    <automated>pnpm --filter @starye/crawler exec vitest run src/smoke/__tests__/data-chain-fixture.test.ts</automated>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-evidence.test.ts</automated>
  </verify>
  <done>The controlled fixture emits exactly ten deterministic non-R18 upserts and exposes no batch identity other than the existing primary tuple plus typed count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify the complete batch through closed prepared-entry and D1 snapshots</name>
  <files>packages/config/src/deployment-target/mutation-entry.ts, packages/config/src/deployment-target/__tests__/mutation-entry.test.ts, packages/db/scripts/target-d1-mutation.ts, packages/db/scripts/__tests__/target-d1-mutation.test.ts</files>
  <behavior>
    - The prepared crawler child parser accepts only a synced observation with the primary code and an exact count of ten.
    - The fixed remote D1 snapshot derives the same ten codes from prepared target/run context, confirms exactly those ten non-R18 movie rows and one active player per row, and returns the existing primary code/id together with count ten.
    - Missing, duplicate, extra, mismatched, R18, inactive-player, malformed, or query-failure results produce only existing non-success snapshot states.
  </behavior>
  <action>Before editing `runTargetD1Mutation` or prepared-entry parsing, rerun the required GitNexus upstream impact analysis and record the resulting blast radius; stop for a HIGH or CRITICAL warning. Per D-01/D-02/D-05, keep `smokeItemCode` as the sole prepared primary identity and retain the current closed registry, fresh environment, and no-caller-argv boundary. Tighten the prepared-child observation parser to require the exact ten-item count emitted by Task 1. Replace the single-code D1 snapshot with a fixed parameterized query whose complete expected code set is deterministically derived from the prepared target/run context; it must verify ten distinct expected non-R18 movie rows, exactly one active player per row, and identify only the primary row's id for the existing UI tuple. Keep the query parameters internally derived, shell-free, and unavailable to CLI input; redact command diagnostics and retain checkpoint/not-found behavior on every failed cardinality or parse check. Add regression tests for valid ten rows and each wrong-count, duplicate, missing, extra, primary-id, player-count, R18, malformed-output, registry-entry, and injected executor failure path. Do not introduce raw D1 clients, arbitrary SQL, target/endpoint fallback, a free-form entry, or additional remote output fields beyond primary identity and typed count.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/mutation-entry.test.ts</automated>
    <automated>pnpm --filter @starye/db exec vitest run scripts/__tests__/target-d1-mutation.test.ts</automated>
  </verify>
  <done>Only a closed prepared run with a complete valid deterministic batch can supply count ten and the unchanged primary D1 item id to the smoke runner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire audited cardinality into local and remote smoke without changing UI proof</name>
  <files>scripts/data-chain-smoke.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</files>
  <behavior>
    - Local and remote runners accept a fixture/snapshot only when each reports exactly ten; successful D1 evidence persists that count while retaining one primary itemCode/itemId.
    - Batch mismatch becomes a persisted non-success checkpoint and cannot create API, Dashboard, or viewer pass evidence.
    - Remote execution still performs explicit target resolution and preflight before mutation, and its Dashboard then viewer append remains exclusively for the unchanged primary tuple.
  </behavior>
  <action>Before modifying `runDataChainSmoke`, rerun GitNexus upstream impact analysis and report the result; do not continue silently if it is HIGH or CRITICAL. Per D-03/D-04, retain all existing local readiness gates, Gateway/canonical-only paths, and the one primary itemCode/itemId contract used by API, Dashboard, and viewer. Per D-05, preserve explicit selected-target remote preflight and return its existing redacted checkpoint before the fixture when credentials or ownership are unavailable. Consume the Task 1/2 exact cardinality observations in both runner modes, write the typed ten-item count only on the validated D1 evidence row, and convert every count/tuple mismatch into existing non-success evidence with no pass claim or browser append. Per D-06, keep JSON/Markdown evidence non-secret: no complete endpoint, prepared context, headers, cookies, token, raw SQL, or sibling code list. Per D-07, add injected local and remote runner regressions for valid ten-item propagation, wrong fixture count, wrong snapshot count, and persistence of the primary tuple; assert Dashboard/viewer behavior is unchanged and no remote child starts before preflight. Run the guarded real commands only after automated checks; missing local secrets, provider authorization, or Dashboard session must remain checkpoint evidence, never a simulated pass. Do not alter Phase 14 literal/RUNBOOK/evidence-matrix scope, add a second viewer/dashboard identity, or accept free target, endpoint, or fixture input.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</automated>
    <automated>pnpm --filter @starye/crawler type-check && pnpm --filter @starye/config type-check && pnpm --filter @starye/db type-check</automated>
    <human-check>With authorized existing prerequisites only, run `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id &lt;new-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence`, then the same command with `--mode remote`; confirm the typed D1 row records count 10 while Dashboard and viewer remain correlated solely by the primary code/id. A missing prerequisite must leave a checkpoint artifact and no remote mutation.</human-check>
  </verify>
  <done>Local and remote smoke safely audit exactly ten deterministic writes while the Dashboard-to-viewer evidence continues to prove only the original primary tuple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| target/run -> fixture batch | Only explicit selected target/run may derive the ten expected codes. |
| prepared child -> D1 snapshot | Child output and D1 results are untrusted until the typed closed parser verifies exact count and primary tuple. |
| remote smoke -> evidence/UI | Count auditing must not become an alternate target, browser, or production-success path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-Q1 | Tampering | fixture batch/count | high | mitigate | Derive all ten codes from target/run, validate complete shape before transport, and verify D1 cardinality. |
| T-13-Q2 | Elevation of Privilege | prepared remote entry | critical | mitigate | Retain closed registry, explicit target preflight, fixed internal parameters, and no free CLI input. |
| T-13-Q3 | Repudiation | production evidence | high | mitigate | Persist typed D1 count with the unchanged primary code/id and checkpoint mismatches. |
| T-13-Q4 | Information Disclosure | diagnostics/evidence | high | mitigate | Allowlist count and primary tuple only; retain existing redaction and reject raw context/output. |
</threat_model>

<verification>
- Run all focused Vitest commands above, then `pnpm --filter @starye/crawler test --run`, `pnpm --filter @starye/config test --run`, and `pnpm --filter @starye/db test --run`.
- Confirm the guarded remote command produces either a count-10 primary-tuple artifact or the existing redacted checkpoint, never a synthetic success.
- Before commit, run GitNexus `detect-changes`; expected symbols are the controlled fixture, prepared observation parser, D1 smoke snapshot, typed D1 observation, and smoke orchestration/tests only.
</verification>

<success_criteria>
The single controlled Phase 13 fixture writes and independently verifies exactly ten deterministic non-R18 records, while every local/remote UI and evidence claim remains anchored to one primary itemCode/itemId and every unavailable remote condition fails closed.
</success_criteria>

<output>
Create `.planning/quick/260717-k53-phase-13-crawler-smoke-fixture-fail-clos/260717-k53-SUMMARY.md` when execution completes.
</output>
