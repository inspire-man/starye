---
phase: 09-documentation-restructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/documentation-ownership.md
  - AGENTS.md
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - `D-01:` canonical owner split remains `README.md` for human entry, `AGENTS.md` for agent rules, `RUNBOOK.md` for operations, `.planning/*` for active execution truth, `docs/` for stable topic docs, and `openspec/` for spec history.
    - `D-02:` non-owner docs keep only short summaries and links; they no longer duplicate full manuals.
    - `D-04:` both `README.md` and `AGENTS.md` expose a short "去哪读什么" index so future work starts from the right source of truth.
    - `D-05:` `AGENTS.md` stops acting as a full project handbook; `D-06:` it keeps only 5-10 hard rules.
    - `D-08:` GitNexus content inside `AGENTS.md` shrinks to mandatory guardrails only, not a long embedded handbook.
  artifacts:
    - `docs/documentation-ownership.md::Canonical Owners`
    - `docs/documentation-ownership.md::Update Triggers`
    - `AGENTS.md::Hard Rules`
    - `README.md::Doc Map`
  key_links:
    - `docs/documentation-ownership.md` becomes the detailed owner map that both `README.md` and `AGENTS.md` link to instead of re-copying the same guidance.
    - `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `RUNBOOK.md` remain the detailed downstream owners that root entry docs route into.
---

<objective>
Establish the canonical documentation map and shrink the two root entry docs for DOC-01 and DOC-02.

Purpose: Make both humans and agents land on the right source of truth immediately instead of reading duplicated manuals.
Output: new documentation owner map + slimmed `AGENTS.md` + slimmed `README.md`
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-documentation-restructure/09-CONTEXT.md
@.planning/phases/09-documentation-restructure/09-RESEARCH.md
@.planning/phases/09-documentation-restructure/09-PATTERNS.md
@README.md
@AGENTS.md
</context>

## Artifacts this plan produces

- `docs/documentation-ownership.md::Canonical Owners`
- `docs/documentation-ownership.md::Root Entry Docs`
- `docs/documentation-ownership.md::Update Triggers`
- `AGENTS.md::Hard Rules`
- `AGENTS.md::Doc Map`
- `README.md::Current Milestone`
- `README.md::Doc Map`

<tasks>

<task type="auto">
  <name>Task 1: 建立长期可维护的文档 owner 地图</name>
  <read_first>
- .planning/phases/09-documentation-restructure/09-CONTEXT.md
- .planning/phases/09-documentation-restructure/09-RESEARCH.md
- .planning/PROJECT.md
- .planning/ROADMAP.md
- RUNBOOK.md
  </read_first>
  <files>docs/documentation-ownership.md</files>
  <action>新建稳定文档 `docs/documentation-ownership.md`，把 Phase 9 已锁定的 owner 边界写成长期参考：按 `D-01` 和 `D-02` 明确 `README.md`、`AGENTS.md`、`CLAUDE.md`、`RUNBOOK.md`、`.planning/*`、`docs/`、`openspec/` 各自负责什么；按 `D-04` 给出人类入口和 agent 入口怎么跳转；补一张 update-trigger 表，说明哪些主题变化要回写到哪个 owner。内容要短、表格化、能被 root docs 直接链接，不要再复制技术栈或完整 quick start。</action>
  <acceptance_criteria>
- `docs/documentation-ownership.md` 存在，并清楚列出 root docs、runbook、planning、docs、openspec 的 canonical owner 边界。
- 文档包含 update-trigger 表，能回答“改了某类规则后该更新哪份文档”。
- 文档没有重复展开成完整项目手册，而是保持入口地图定位。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^## Canonical Owners|^## Root Entry Docs|^## Update Triggers" docs/documentation-ownership.md</automated>
    <automated>rg -n "README.md|AGENTS.md|RUNBOOK.md|\\.planning/|docs/|openspec/" docs/documentation-ownership.md</automated>
  </verify>
  <done>仓库有了一份稳定的“文档 source of truth 地图”，后续 root docs 只需要链接这里而不是重新解释全仓。</done>
</task>

<task type="auto">
  <name>Task 2: 把 `AGENTS.md` 与 `README.md` 收缩成真正的入口文档</name>
  <read_first>
- AGENTS.md
- README.md
- docs/documentation-ownership.md
- .planning/PROJECT.md
- .planning/phases/09-documentation-restructure/09-RESEARCH.md
  </read_first>
  <files>
AGENTS.md
README.md
  </files>
  <action>重写两个 root 入口文档，但保持角色分离。`AGENTS.md` 按 `D-05`、`D-06`、`D-08` 收缩成：中文协作约定、短 doc map、5-10 条硬规则、精简 GitNexus guardrails；删去完整 Quick Start、Project Structure、Technology Stack、Testing、Common Issues 正文。`README.md` 则保留人类需要的最短项目介绍、当前 milestone、最小本地启动说明和 doc map，并把具体技术/运维/agent 细节改成链接到 `docs/documentation-ownership.md`、`RUNBOOK.md`、`.planning/PROJECT.md`、`.planning/ROADMAP.md`。两份文档都必须有短索引表，并互不复制长正文。</action>
  <acceptance_criteria>
- `AGENTS.md` 不再包含完整项目百科式章节，只保留入口级规则和链接。
- `README.md` 变成人类入口速览，而不是项目全量功能/模型/部署大全。
- 两份文档都包含短 doc map，并且都链接到 `docs/documentation-ownership.md`。
- `AGENTS.md` 中 GitNexus 内容只剩下 mandatory rules，不再保留长资源表。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^\\| Topic \\| Canonical Owner \\||documentation-ownership\\.md" AGENTS.md README.md</automated>
    <automated>powershell -NoProfile -Command "$a=Get-Content AGENTS.md -Raw; if ($a -match '## Quick Start|## Project Structure|## Technology Stack|## Development Workflow|## Testing|## Common Issues') { throw 'AGENTS still contains handbook sections' }"</automated>
  </verify>
  <done>人和 agent 都能从 root docs 快速跳到正确 owner，不会再先读到一份过长、重复、容易漂移的总手册。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root entry docs -> canonical owners | 如果入口文档继续复制长正文，未来任何规则更新都会再次漂移 |
| agent rules -> execution behavior | 如果 `AGENTS.md` 太长，真正必须执行的 repo 边界会被淹没 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information disclosure / drift | `AGENTS.md` | high | mitigate | 只保留硬规则和索引，移除大段镜像说明 |
| T-09-02 | Misrouting | `README.md` | medium | mitigate | 用 doc map 直接路由到 canonical owners |
</threat_model>

<verification>
- `rg -n "^## Canonical Owners|^## Update Triggers" docs/documentation-ownership.md`
- `rg -n "^\\| Topic \\| Canonical Owner \\||documentation-ownership\\.md" AGENTS.md README.md`
- `powershell -NoProfile -Command "$a=Get-Content AGENTS.md -Raw; if ($a -match '## Quick Start|## Project Structure|## Technology Stack|## Development Workflow|## Testing|## Common Issues') { throw 'AGENTS still contains handbook sections' }"`
</verification>

<success_criteria>
- `docs/documentation-ownership.md` 成为长期 doc map。
- `AGENTS.md` 收缩成硬规则 + 索引。
- `README.md` 收缩成人类入口速览。
</success_criteria>

<output>
Create `.planning/phases/09-documentation-restructure/09-01-SUMMARY.md` when done
</output>
