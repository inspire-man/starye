---
phase: 10-storage-code-cleanup
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - packages/crawler/src/lib/image-processor.ts
  - packages/crawler/test/image-processor-purpose-policy.test.ts
  - apps/api/src/routes/admin/actors/index.ts
  - apps/api/src/routes/admin/publishers/index.ts
  - apps/api/src/routes/admin/actors/__tests__/pending.test.ts
  - apps/api/src/routes/admin/publishers/__tests__/pending.test.ts
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
must_haves:
  truths:
    - `D-05:` 后台 heuristics 里把“不是 R2”直接视为待修复的旧假设必须被清掉。
    - `D-06:` 合法 external URL 是可接受终态；后台 heuristics 不能再把“不是 R2”直接判成待修复。
    - `D-08:` 保持对外字段名兼容，优先修正内部语义，不扩大前后端联动 blast radius。
    - `D-09:` 测试以 shared helper adoption 的最小 regression 为主，而不是重跑整套高层链路。
    - `D-11:` API 与 crawler 至少各保留一处接线回归。
    - `D-12:` Phase 7 的 public comic external URL 与 Reader failure behavior 只复用，不在本 plan 重做。
  artifacts:
    - `packages/crawler/src/lib/image-processor.ts` consumes shared namespace/purpose helper
    - `packages/crawler/test/image-processor-purpose-policy.test.ts` locks crawler-side helper adoption
    - `apps/api/src/routes/admin/actors/index.ts` policy-aware `needsAvatarUpdate`
    - `apps/api/src/routes/admin/publishers/index.ts` policy-aware `needsLogoUpdate`
    - focused admin pending route regressions
  key_links:
    - Plan 10-01 已建立 helper truth source；本 plan 负责 runtime consumers 的 adoption。
    - 保持 `needsAvatarUpdate` / `needsLogoUpdate` 字段名不变，但内部意义要从“不是 R2”改为“真的缺资产/需补全”。
---

<objective>
Move crawler and admin pending heuristics onto the shared storage semantics for CODE-01/02/03.

Purpose: eliminate the last runtime surfaces that still treat R2 presence as the only “correct” asset state.
Output: crawler seam adoption + policy-aware actor/publisher heuristics + focused regression coverage
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-storage-code-cleanup/10-CONTEXT.md
@.planning/phases/10-storage-code-cleanup/10-RESEARCH.md
@.planning/phases/10-storage-code-cleanup/10-PATTERNS.md
@packages/api-types/src/storage-purpose-policy.ts
@packages/crawler/src/lib/image-processor.ts
@packages/crawler/test/image-processor-purpose-policy.test.ts
@apps/api/src/routes/admin/actors/index.ts
@apps/api/src/routes/admin/publishers/index.ts
</context>

## Artifacts this plan produces

- `packages/crawler/src/lib/image-processor.ts` shared-helper adoption
- `packages/crawler/test/image-processor-purpose-policy.test.ts`
- `apps/api/src/routes/admin/actors/index.ts` policy-aware pending logic
- `apps/api/src/routes/admin/publishers/index.ts` policy-aware pending logic
- `apps/api/src/routes/admin/actors/__tests__/pending.test.ts`
- `apps/api/src/routes/admin/publishers/__tests__/pending.test.ts`

<tasks>

<task type="auto">
  <name>Task 1: 让 `ImageProcessor` 从 crawler 私有 policy 迁到 shared helper</name>
  <read_first>
- packages/api-types/src/storage-purpose-policy.ts
- packages/crawler/src/lib/image-processor.ts
- packages/crawler/test/image-processor-purpose-policy.test.ts
- .planning/phases/10-storage-code-cleanup/10-RESEARCH.md
  </read_first>
  <files>
packages/crawler/src/lib/image-processor.ts
packages/crawler/test/image-processor-purpose-policy.test.ts
  </files>
  <action>在不改变 `ImageProcessor` 作为单一 upload seam 的前提下，把 crawler 私有的 namespace / purpose semantics 收口到 Plan 10-01 产出的 shared helper。重点不是改 caller 签名，而是让 `ImageProcessor` 自己停止维护一套独立 policy，实现上可以保留当前 `process(target)` 入口，但内部应使用 shared helper 计算“允许的 managed asset prefix”，并把变量/注释改成更显式的必要资产语义（例如 `managedAssetPrefix`，而不是含糊的 `keyPrefix`/“默认进 R2”叙事）。扩展 `image-processor-purpose-policy.test.ts`，确认 chapter-page reject、movie/comic cover allow、actor/publisher allow 这些 seam-level 规则在 helper adoption 后仍保持不变。</action>
  <acceptance_criteria>
- crawler 不再维护一套和 shared helper 平行的 namespace/purpose source of truth。
- `ImageProcessor` 仍然是 fail-closed seam，chapter-page reject 发生在下载/上传前。
- seam-level tests 继续覆盖 allowed / rejected namespace matrix。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/crawler exec vitest run test/image-processor-purpose-policy.test.ts</automated>
    <automated>pnpm --filter @starye/crawler exec tsc --noEmit</automated>
    <automated>rg -n "resolveCrawlerManagedAssetPrefix|classifyStorageUrlKind|comic_chapter_page" packages/crawler/src/lib/image-processor.ts</automated>
  </verify>
  <done>crawler upload seam 正式消费 shared contract，不再是第二套独立 policy。</done>
</task>

<task type="auto">
  <name>Task 2: 把 actor/publisher pending heuristics 改成 policy-aware，而不是 R2-aware</name>
  <read_first>
- apps/api/src/routes/admin/actors/index.ts
- apps/api/src/routes/admin/publishers/index.ts
- packages/api-types/src/storage-purpose-policy.ts
- .planning/phases/10-storage-code-cleanup/10-CONTEXT.md
  </read_first>
  <files>
apps/api/src/routes/admin/actors/index.ts
apps/api/src/routes/admin/publishers/index.ts
apps/api/src/routes/admin/actors/__tests__/pending.test.ts
apps/api/src/routes/admin/publishers/__tests__/pending.test.ts
  </files>
  <action>修正 actor/publisher pending route 内部判断逻辑：保留 `needsAvatarUpdate` / `needsLogoUpdate` 字段名和 response shape，但把它们从“URL 不以 `R2_PUBLIC_URL` 开头”改为“资产确实缺失、无效、或仍需特定来源补全”的 policy-aware 语义。合法 external avatar/logo 不再自动进入 pending/update 队列。实现时应复用 shared URL classifier，而不是复制一套新的 startsWith/regex 判断。为两个 pending route 新增 focused regression tests，至少覆盖：已爬取 + 合法 external URL 时不再标记为 `needs*Update`；资产缺失时仍正确标记；原有 pending / recrawl 高优先逻辑不被误删。</action>
  <acceptance_criteria>
- actor/publisher pending heuristics 不再把 external URL 当作天然错误状态。
- `needsAvatarUpdate` / `needsLogoUpdate` 字段名保持兼容，但语义被修正。
- focused route tests 能证明“合法外链”和“真正缺失资产”被正确区分。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter api exec vitest run src/routes/admin/actors/__tests__/pending.test.ts src/routes/admin/publishers/__tests__/pending.test.ts</automated>
    <automated>pnpm --filter api exec tsc --noEmit</automated>
    <automated>rg -n "startsWith\\(r2PublicUrl\\)|needsAvatarUpdate|needsLogoUpdate" apps/api/src/routes/admin/actors/index.ts apps/api/src/routes/admin/publishers/index.ts</automated>
  </verify>
  <done>admin pending queues 的判断口径从“是不是 R2”改回“是否真的缺必要资产/需要补全”，与当前存储策略一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared helper -> crawler seam | 如果 crawler adoption 不完整，后续 scripts/crawlers 仍可能从 `ImageProcessor` 私有逻辑重新漂移 |
| policy-aware heuristics -> admin queue | 如果 pending 语义修正不严谨，会误清空或误放大 actor/publisher 待爬取队列 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering / Drift | crawler seam | high | mitigate | 让 `ImageProcessor` 直接消费 shared helper，并保留 seam-level regression |
| T-10-04 | Logic regression | admin pending heuristics | high | mitigate | 新增 focused route tests，明确 external-valid vs missing-asset 的语义分界 |
</threat_model>

<verification>
- `pnpm --filter @starye/crawler exec vitest run test/image-processor-purpose-policy.test.ts`
- `pnpm --filter @starye/crawler exec tsc --noEmit`
- `pnpm --filter api exec vitest run src/routes/admin/actors/__tests__/pending.test.ts src/routes/admin/publishers/__tests__/pending.test.ts`
- `pnpm --filter api exec tsc --noEmit`
</verification>

<success_criteria>
- `ImageProcessor` 完成 shared-helper adoption。
- actor/publisher pending heuristics 接受合法 external URLs。
- API/crawler focused regressions 到位。
</success_criteria>

<output>
Create `.planning/phases/10-storage-code-cleanup/10-02-SUMMARY.md` when done
</output>
