---
phase: 10-storage-code-cleanup
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - packages/crawler/scripts/backfill-covers.ts
  - packages/crawler/scripts/test-full-flow.ts
  - packages/crawler/scripts/test-single-movie.ts
autonomous: true
requirements:
  - CODE-03
  - CODE-04
must_haves:
  truths:
    - `D-05:` 脚本/测试里把 R2/CDN 当唯一正确结果的硬编码必须被清掉。
    - `D-07:` legacy scripts 默认行为必须接受“合法外链”和“必要时存 R2”并存，而不是继续强化 CDN-only / R2-only 叙事。
    - `D-08:` 本 plan 修正脚本注释、日志、成功口径和验证提示，不扩大到 public API field rename。
    - `D-10:` 不重做 Phase 7/8 高层 external-image 回归；只复跑已有 `public-comics` / `Reader` 测试。
    - `D-11:` shared helper direct tests 与 API/crawler seam regressions 已在前两个 plans 立住；本 plan 只负责 legacy script 语义收口和最终验证矩阵。
  artifacts:
    - `packages/crawler/scripts/backfill-covers.ts` policy-aware success wording
    - `packages/crawler/scripts/test-full-flow.ts` updated smoke checklist
    - `packages/crawler/scripts/test-single-movie.ts` updated smoke checklist
    - phase-level verification reruns for existing public comic / Reader coverage
  key_links:
    - `backfill-covers.ts` 已经具备 fallback 外链行为；本 plan 重点是把 operator-facing default narrative 拉正。
    - `public-comics.test.ts` 与 `Reader.test.ts` 是 Phase 7 已锁 coverage，本 plan 只复跑保护，不新增更重链路。
---

<objective>
Clean up legacy R2-first script assumptions and close the Phase 10 verification matrix for CODE-03/04.

Purpose: stop repo-maintenance scripts and smoke checklists from teaching future maintainers that “cover must be an R2 URL”.
Output: policy-aware script wording + final targeted regression reruns
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-storage-code-cleanup/10-CONTEXT.md
@.planning/phases/10-storage-code-cleanup/10-RESEARCH.md
@.planning/phases/10-storage-code-cleanup/10-PATTERNS.md
@packages/crawler/scripts/backfill-covers.ts
@packages/crawler/scripts/test-full-flow.ts
@packages/crawler/scripts/test-single-movie.ts
@apps/api/src/routes/public/comics/__tests__/public-comics.test.ts
@apps/comic-app/src/views/__tests__/Reader.test.ts
</context>

## Artifacts this plan produces

- `packages/crawler/scripts/backfill-covers.ts` updated policy-aware comments/logs
- `packages/crawler/scripts/test-full-flow.ts` updated success checklist
- `packages/crawler/scripts/test-single-movie.ts` updated success checklist
- final regression evidence that existing public comic / Reader coverage still passes

<tasks>

<task type="auto">
  <name>Task 1: 修正 legacy cover scripts 的默认叙事与成功标准</name>
  <read_first>
- packages/crawler/scripts/backfill-covers.ts
- packages/crawler/scripts/test-full-flow.ts
- packages/crawler/scripts/test-single-movie.ts
- .planning/phases/10-storage-code-cleanup/10-RESEARCH.md
  </read_first>
  <files>
packages/crawler/scripts/backfill-covers.ts
packages/crawler/scripts/test-full-flow.ts
packages/crawler/scripts/test-single-movie.ts
  </files>
  <action>保留现有脚本控制流和用途边界，但修正 operator-facing 叙事。`backfill-covers.ts` 需要把“R2 CDN（标准流程）/ JavBus 原始 URL（降级）”这类单向价值判断改成更符合当前 policy 的表达：必要资产可进 R2，合法 external URL 也可以是成功终态。`test-full-flow.ts` 与 `test-single-movie.ts` 中“验证 coverImage 是 R2 URL”一类 checklist，要改成“验证结果符合当前 storage policy”，例如 movie cover 若通过必要资产路径存入 managed URL 当然成立，但合法 external terminal 也不应被描述成失败。仅在当前 grep 已暴露的同类 smoke 脚本范围内做这类收口，不扩大到所有 crawler utility 脚本。</action>
  <acceptance_criteria>
- 仓库里关键 legacy cover scripts 不再把 R2/CDN 视为唯一正确结果。
- 已存在的 fallback external 路径被表述为 policy-compatible 终态，而不是“虽然能用但不算正确”。
- 本 plan 的脚本改动不引入新的上传流程或 runtime dependency。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "R2 URL|CDN|合法外链|policy|JavBus|coverImage" packages/crawler/scripts/backfill-covers.ts packages/crawler/scripts/test-full-flow.ts packages/crawler/scripts/test-single-movie.ts</automated>
    <automated>pnpm --filter @starye/crawler exec tsc --noEmit</automated>
  </verify>
  <done>后续维护者再读这些脚本时，不会继续被“封面一定要进 R2”这套旧假设带偏。</done>
</task>

<task type="auto">
  <name>Task 2: 复跑现有 external-image regression，确认 Phase 10 没把 Phase 7/8 语义拉回去</name>
  <read_first>
- apps/api/src/routes/public/comics/__tests__/public-comics.test.ts
- apps/comic-app/src/views/__tests__/Reader.test.ts
- .planning/phases/10-storage-code-cleanup/10-VALIDATION.md
  </read_first>
  <files>packages/crawler/scripts/backfill-covers.ts</files>
  <action>不新增新的高层测试文件，只按 `10-VALIDATION.md` 复跑现有 `public-comics.test.ts` 与 `Reader.test.ts`，并在本 plan 的执行总结里明确记录它们作为 Phase 7 protected coverage 的通过结果。这样可以证明 Phase 10 的 helper 收口、heuristic 修正和 script 语义收口，没有把“external URL 是合法终态”这条跨 phase contract 拉回旧状态。</action>
  <acceptance_criteria>
- `public-comics.test.ts` 与 `Reader.test.ts` 被明确作为 Phase 10 的 protected reuse coverage。
- 本 plan 不新增更重的外链集成测试。
- 执行总结会记录这些复用回归的结果，便于 verify-work 直接引用。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter api exec vitest run src/routes/public/comics/__tests__/public-comics.test.ts</automated>
    <automated>pnpm --filter @starye/comic-app exec vitest run src/views/__tests__/Reader.test.ts</automated>
  </verify>
  <done>Phase 7/8 的 external-image contract 被明确纳入 Phase 10 收尾保护网，而没有被重复重建。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy scripts -> future maintainers | 如果脚本口径不修，后续维护者仍会把 R2-only 当 canonical expectation |
| focused Phase 10 refactor -> Phase 7 protected behavior | 如果不复跑 public comic / Reader regressions，helper 收口可能悄悄伤到 external-image contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Information confusion | legacy crawler scripts | high | mitigate | 改写脚本注释、日志和 checklist，使其与当前 storage policy 一致 |
| T-10-06 | Regression | external-image protected behavior | medium | mitigate | 复跑现有 `public-comics` / `Reader` regressions 作为 Phase 10 closeout guard |
</threat_model>

<verification>
- `rg -n "R2 URL|CDN|合法外链|policy|JavBus|coverImage" packages/crawler/scripts/backfill-covers.ts packages/crawler/scripts/test-full-flow.ts packages/crawler/scripts/test-single-movie.ts`
- `pnpm --filter @starye/crawler exec tsc --noEmit`
- `pnpm --filter api exec vitest run src/routes/public/comics/__tests__/public-comics.test.ts`
- `pnpm --filter @starye/comic-app exec vitest run src/views/__tests__/Reader.test.ts`
</verification>

<success_criteria>
- legacy scripts 停止强化 R2-only 叙事。
- Phase 7 protected external-image coverage 被复跑并通过。
- Phase 10 对 CODE-03/04 的验证路径闭环。
</success_criteria>

<output>
Create `.planning/phases/10-storage-code-cleanup/10-03-SUMMARY.md` when done
</output>
