---
phase: 17-local-runner-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crawler/src/task-runner/event-signer.ts
  - packages/crawler/src/task-runner/runner-client.ts
  - packages/crawler/src/task-runner/local-runner.ts
  - packages/crawler/src/task-runner/template-adapters.ts
  - packages/crawler/src/task-runner/movie-adapter.ts
  - packages/crawler/src/task-runner/manga-adapter.ts
  - packages/crawler/src/task-runner/controlled-adapter.ts
  - packages/crawler/package.json
  - scripts/local-task-runner.ts
  - apps/api/src/schemas/crawler-run-events.ts
  - apps/api/src/routes/internal/crawler-runs/index.ts
  - apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts
  - packages/crawler/src/task-runner/__tests__/event-signer.test.ts
  - packages/crawler/src/task-runner/__tests__/runner-client.test.ts
  - packages/crawler/src/task-runner/__tests__/local-runner.test.ts
  - packages/crawler/src/task-runner/__tests__/template-adapters.test.ts
  - packages/config/src/deployment-target/__tests__/crawler-source-entry-contract.test.ts
autonomous: true
requirements: [LOCAL-01, LOCAL-02]
must_haves:
  truths:
    - "D-01/D-02/D-03：本地 Node runner 只轮询 API 分配的 run，并在同一进程全局串行 claim；离线时 queued run 保持 queued，恢复后可领取。"
    - "D-04：API 固定 movie 与 manga 两个模板都由本地薄适配器实际调用既有 crawler；受控测试 adapter 不会成为第三个可创建模板。"
    - "D-09/D-12：runner 只在心跳和 adapter 安全检查点协作取消，不杀 Puppeteer；取消 run 不写成功 receipt。"
    - "D-01/D-03/D-05/D-06/D-07/D-09/D-11：poll、claim 和 lifecycle callback 使用 API-owned run/attempt、严格 schema、raw-body HMAC、key rotation、timestamp、nonce/event_id、sequence 与 repository 状态机；runner 不拥有第二套队列或状态机。"
    - "D-01/D-03：本地启动约定只读取 ignored local 配置，绝不把 callback/crawler 凭据、URL、命令、workflow、目标、环境或密钥交给 Dashboard、Worker/Pages、公开配置或 GitHub Actions。"
  artifacts:
    - path: packages/crawler/src/task-runner/local-runner.ts
      provides: one-active-run local daemon with visible-safe polling and cooperative shutdown
    - path: packages/crawler/src/task-runner/template-adapters.ts
      provides: API-snapshot-only movie/manga adapter registry
    - path: packages/crawler/src/task-runner/runner-client.ts
      provides: signed poll, claim, heartbeat, log, terminal and cancellation reads
    - path: apps/api/src/routes/internal/crawler-runs/index.ts
      provides: strict signed poll/claim/event boundary backed by the Phase 16 repository
  key_links:
    - from: packages/crawler/src/task-runner/runner-client.ts
      to: apps/api/src/routes/internal/crawler-runs/index.ts
      via: one serialized UTF-8 envelope signed before POST with x-runner-key-id/x-runner-signature
      pattern: verifyRunnerEventSignature
    - from: packages/crawler/src/task-runner/template-adapters.ts
      to: apps/api/src/domain/crawler-tasks/template-registry.ts
      via: frozen API-issued movie/manga snapshot only
      pattern: crawlerTaskTemplates
    - from: apps/api/src/routes/internal/crawler-runs/index.ts
      to: apps/api/src/domain/crawler-tasks/repository.ts
      via: claimDispatch and actual CAS decision persistence
      pattern: processRunnerEvent
---

<objective>
交付本地 Node runner 与严格的 runner control-plane 边界，证明 API 创建的 movie/manga run 能被唯一的本地进程领取、执行、回写安全生命周期事件并协作取消。

Purpose: 将 Phase 16 的 D1 task domain 接到本地数据面，而不将 Node/Puppeteer 引入 Worker/Pages 或 GitHub Actions。
Output: signed runner client、closed adapter registry、serial daemon、poll/claim callbacks 与确定性 cancellation tests。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-local-runner-vertical-slice/17-CONTEXT.md
@.planning/phases/17-local-runner-vertical-slice/17-RESEARCH.md
@.planning/phases/17-local-runner-vertical-slice/17-PATTERNS.md
@.planning/phases/17-local-runner-vertical-slice/17-UI-SPEC.md
@.agents/skills/starye-crawler-strategy/SKILL.md
@.agents/skills/starye-hono-rpc/SKILL.md

<interfaces>
- `createCrawlerTaskRepository().claimDispatch()` and `decideCrawlerRunTransition()` remain the only lease/state authority.
- `verifyRunnerEventSignature()` verifies exact request bytes before Valibot parsing; runner signs the exact serialized body once.
- `OptimizedCrawler` and `ComicCrawler` retain their content-sync transport; adapters only observe safe candidate facts at their established sync seams.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先用测试固定 strict poll/claim 与实际 CAS outcome</name>
  <files>apps/api/src/schemas/crawler-run-events.ts, apps/api/src/routes/internal/crawler-runs/index.ts, apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</files>
  <read_first>
    - apps/api/src/routes/internal/crawler-runs/index.ts — 当前 raw-body HMAC、event route 和 repository 调用顺序。
    - apps/api/src/schemas/crawler-run-events.ts — 现有 strict envelope vocabulary。
    - apps/api/src/domain/crawler-tasks/repository.ts — `claimDispatch()`、event idempotency 与 CAS decision contract。
    - apps/api/src/domain/crawler-tasks/runner-event-auth.ts — current/previous key rotation and five-minute verification.
    - .planning/phases/17-local-runner-vertical-slice/17-CONTEXT.md — D-01, D-03, D-09, D-11 and the closed-template scope fence.
  </read_first>
  <behavior>
    - Test 1: signed poll is read-only and returns only an API-owned queued candidate/snapshot; unknown or surplus template/command/url/workflow/secret input is rejected.
    - Test 2: signed claim binds run id and attempt, maps only to `claimDispatch()`, and response/replay storage reflects the actual transition decision rather than a pre-CAS caller-supplied accepted outcome.
    - Test 3: tampered bytes, expired timestamp, unknown key id, conflicting nonce/event_id, stale claim, and foreign attempt return non-success without changing lifecycle state.
  </behavior>
  <action>Before changing `processRunnerEvent`, `claimDispatch`, route handlers, or schemas, run GitNexus upstream impact for each symbol and report direct callers/processes; if HIGH or CRITICAL, report the blast radius before editing. Write the route tests first with exact raw JSON bytes and current/previous HMAC key fixtures. Add server-only poll/claim request and response DTOs with strict Valibot objects, run/attempt/sequence identity, timestamp/nonce/event ID rules, and no execution fields. Verify signature before parsing every mutation payload; leave poll read-only and map a valid claim only through the repository. Move stored/replayed event outcome creation after the repository CAS decision so stale/rejected claims never report `accepted`. Keep existing terminal event normalization, Phase 16 caps/redaction, and `crawler_task` access model; do not add an admin command, target profile, Actions dispatch, Worker child process, or a new lifecycle enum.</action>
  <verify>
    <automated>pnpm --filter api test --run src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</automated>
    <automated>pnpm --filter api type-check</automated>
  </verify>
  <acceptance_criteria>Poll never mutates D1; only a valid signed API-issued run/attempt can claim; every stale/replay/tamper result is non-success and repository state remains unchanged.</acceptance_criteria>
  <done>The local runner has a strict, auditable protocol to discover and claim API-owned work without bypassing Phase 16 controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立串行本地 runner、固定 movie/manga adapter 与协作取消</name>
  <files>packages/crawler/src/task-runner/event-signer.ts, packages/crawler/src/task-runner/runner-client.ts, packages/crawler/src/task-runner/local-runner.ts, packages/crawler/src/task-runner/template-adapters.ts, packages/crawler/src/task-runner/movie-adapter.ts, packages/crawler/src/task-runner/manga-adapter.ts, packages/crawler/src/task-runner/controlled-adapter.ts, packages/crawler/src/task-runner/__tests__/event-signer.test.ts, packages/crawler/src/task-runner/__tests__/runner-client.test.ts, packages/crawler/src/task-runner/__tests__/local-runner.test.ts, packages/crawler/src/task-runner/__tests__/template-adapters.test.ts</files>
  <read_first>
    - packages/crawler/src/utils/api-client.ts — fetch timeout/error shape; its service-token behavior must not be reused for runner events.
    - packages/crawler/src/core/optimized-crawler.ts and packages/crawler/src/crawlers/comic-crawler.ts — movie/manga sync seams and internal failure behavior.
    - apps/api/src/domain/crawler-tasks/template-registry.ts — the closed two-template API snapshot.
    - scripts/local-dev.ts — ESM entry, cleanup, and local process conventions.
    - .planning/phases/17-local-runner-vertical-slice/17-RESEARCH.md — adapter candidate rules and cancellation checkpoints.
    - .agents/skills/starye-crawler-strategy/SKILL.md — crawler strategy and source isolation rules.
  </read_first>
  <behavior>
    - Test 1: one daemon maintains exactly one `activeRun`; it polls again only after terminal acknowledgement, and an API outage leaves queued work untouched for later recovery.
    - Test 2: movie and manga snapshots select only their matching fixed adapter; arbitrary argv/config/source URL and the controlled adapter are rejected as public templates.
    - Test 3: every callback signs one serialized body; heartbeat/checkpoint observes `cancel_requested`, emits signed `cancelled`, stops future units without killing a browser, and returns no success receipt.
    - Test 4: the adapter observes safe template-compatible candidate IDs/counts but neither changes `ApiClient.syncMovie()` nor treats crawler process completion as persistence proof.
  </behavior>
  <action>Run GitNexus upstream impact before changing `ApiClient`, `OptimizedCrawler`, `ComicCrawler`, or any existing process entry; for HIGH/CRITICAL results report before work. Implement a local-only ESM runner client that derives callback signing headers from ignored local configuration, serializes before HMAC-SHA-256 signing, uses bounded fetch timeouts, and never logs secrets/raw headers. Implement an API-snapshot-only discriminated `movie`/`manga` adapter registry with a single in-memory active run and a poll→claim→heartbeat→safe log→terminal loop. Bind each adapter to existing crawler classes through narrow candidate observation at their sync seam; retain their transport and raw source-data boundary. Add a non-public `controlled-adapter` used only by deterministic cancellation tests. Expose a clearly named local CLI in `packages/crawler/package.json` (and root wrapper only if needed), classify it `local-or-external-only` in the existing entry-contract test, and keep it out of Worker/Pages, remote target mutation, public config, and GitHub Actions.</action>
  <verify>
    <automated>pnpm --filter @starye/crawler test --run src/task-runner/__tests__/event-signer.test.ts src/task-runner/__tests__/runner-client.test.ts src/task-runner/__tests__/local-runner.test.ts src/task-runner/__tests__/template-adapters.test.ts</automated>
    <automated>pnpm --filter @starye/crawler type-check; pnpm --filter @starye/crawler run local:task-runner --help</automated>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/crawler-source-entry-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>Only API-owned movie/manga snapshots execute, max concurrency is one local run, all lifecycle calls are byte-signed, and a deterministic cancellation stops at a safe checkpoint without a success receipt or raw-secret log.</acceptance_criteria>
  <done>A restart-safe local data-plane process can claim and drive existing video/manga crawlers while Phase 16 remains the durable state authority.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `event-signer.ts`, `runner-client.ts`, `local-runner.ts`, and a local-only package entry.
- Closed movie/manga adapter registry plus deterministic non-public cancellation adapter.
- Strict internal poll/claim contracts and tests that persist actual CAS/replay outcomes.
</artifacts_this_phase_produces>

<source_coverage_audit>
| Source | ID | Plan coverage |
|---|---|---|
| Requirement | LOCAL-01 | Task 1 grants only signed API-owned claims; Task 2 executes only fixed existing movie/manga crawlers. |
| Requirement | LOCAL-02 | Tasks 1–2 cover signed startup/heartbeat/log/terminal events and cooperative cancellation. |
| Context | D-01–D-04 | API-issued ID, global single run, offline queued recovery, both real templates. |
| Context | D-09, D-11, D-12 | safe-checkpoint cancel, retained audit/no receipt, deterministic controlled adapter. |
</source_coverage_audit>

<threat_model>
ASVS level: L1. High-severity threats block execution.

| Threat | Severity | Mitigation |
|---|---|---|
| forged/replayed runner callback | high | exact raw-body HMAC, key rotation, timestamp, event ID/nonce, run/attempt/sequence and post-CAS outcome storage |
| command/template injection | high | strict schema and server/API-snapshot-only two-template registry; no executable client fields |
| concurrent crawler execution | high | one local `activeRun` plus repository lease/claim CAS |
| secret or source leakage | high | ignored local config, safe structured callbacks, no raw stdout/headers/cookies/source URL persistence |
| forced cancellation data loss | high | heartbeat/checkpoint cancellation; no process kill; terminal cancelled omits success receipt |
</threat_model>

<verification>
Run all focused route/runner/source-entry tests and API/crawler type checks. Before every implementation commit run `npx gitnexus detect-changes --repo starye --scope all`, verify only intended runner/API symbols and flows changed, and leave historical Phase 13 artifacts unstaged.
</verification>

<success_criteria>
The local data plane serially claims only server-controlled video or manga runs and reports trustworthy signed lifecycle facts without changing production Actions/Worker responsibilities.
</success_criteria>

<output>Create `.planning/phases/17-local-runner-vertical-slice/17-01-SUMMARY.md` when complete.</output>
