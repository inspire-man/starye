---
phase: 19-dashboard-operations-and-end-to-end-proof
plan: 03
type: tdd
wave: 2
depends_on: [19-01]
files_modified:
  - apps/dashboard/src/lib/api.ts
  - apps/dashboard/src/views/Crawlers.vue
  - apps/dashboard/src/views/__test__/Crawlers.test.ts
autonomous: true
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "Dashboard 按 movie/manga 资源权限分组展示完整 task 历史，加载更多使用稳定 nextCursor；每个 task 可在同页详情切换全部 attempt。"
    - "详情显示状态、failure/cancel 原因、validated receipt、脱敏 provider 摘要和 descending sequence 分页日志。"
    - "可见页面每 5 秒刷新列表/当前详情/日志，hidden 暂停、恢复立即刷新，并保留手动刷新。"
    - "取消确认后显示 `cancel_requested` 等待 runner；重试确认展示终态、原因与 attempt 并切换新 attempt；冲突操作处于禁用态。"
  artifacts:
    - path: apps/dashboard/src/lib/api.ts
      provides: "typed list/detail/log/cancel/retry methods with nextCursor and safe DTOs"
    - path: apps/dashboard/src/views/Crawlers.vue
      provides: "grouped history, in-page detail, attempt/log paging, visibility-safe polling and actions"
    - path: apps/dashboard/src/views/__test__/Crawlers.test.ts
      provides: "permission, pagination, polling, attempt, provider, cancel/retry and receipt-link UI coverage"
  key_links:
    - from: apps/dashboard/src/views/Crawlers.vue
      to: apps/dashboard/src/lib/api.ts
      via: "typed Gateway `/api` calls only"
    - from: apps/dashboard/src/views/Crawlers.vue
      to: apps/api/src/routes/admin/crawler-tasks/index.ts
      via: "server-owned cursor/attempt/provider/receipt DTOs"
    - from: visibilitychange
      to: loadTaskPanel
      via: "stop hidden interval, immediate refresh on visible"
---

<objective>
把 Phase 19 API 读模型接入既有 Crawler 页面，完成完整历史、同页详情、attempt/log 分页、可见性刷新和确认后的取消/重试操作。

Purpose: 满足 DASH-01/02，并保留现有固定模板、resource guard、ConfirmDialog 和 Gateway-only 前端边界。
Output: typed dashboard client、Crawlers.vue 扩展和 deterministic component tests。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-CONTEXT.md
@.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-RESEARCH.md
@.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-PATTERNS.md
@.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-01-PLAN.md
@apps/dashboard/src/views/Crawlers.vue
@apps/dashboard/src/views/__test__/Crawlers.test.ts
@apps/dashboard/src/lib/api.ts
@apps/dashboard/src/composables/useResourceGuard.ts
</context>

<tasks>
<task type="tdd" tdd="true">
  <name>Task 1: 扩展 typed client 与分组历史状态</name>
  <files>apps/dashboard/src/lib/api.ts, apps/dashboard/src/views/__test__/Crawlers.test.ts</files>
  <behavior>
    - typed list returns tasks and nextCursor; detail returns task/runs/provider/receipt; logs return nextCursor.
    - inaccessible template is never requested/rendered; visible templates post only fixed movie/manga values.
    - source query parameters contain only controlled task/run/attempt IDs.
  </behavior>
  <action>沿用 apiFetch 与现有 `CrawlerTask` types，补齐 opaque cursor、task detail、provider summary、receipt and log DTO。保留 relative `/api` Gateway boundary；client methods只构造 template/cursor/task/run/attempt 的 typed params。扩展 tests 断言 movie/comic permission mapping、response decoding、nextCursor 和 receipt handoff source params。</action>
  <verify><automated>pnpm --filter dashboard test --run src/views/__test__/Crawlers.test.ts -t "history|cursor|permission|receipt"</automated></verify>
  <done>Dashboard client exposes the server DTOs with typed cursor/detail/provider/receipt boundaries and no free-form execution inputs.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: 实现同页历史详情、刷新、日志和确认动作</name>
  <files>apps/dashboard/src/views/Crawlers.vue, apps/dashboard/src/views/__test__/Crawlers.test.ts</files>
  <behavior>
    - task groups show full history and load more; selected detail preserves selection across refresh and supports attempt switching.
    - visible intervals run every 5000ms, hidden clears timers, visible immediately reloads, manual refresh remains available.
    - cancel/retry dialogs show the exact current terminal state/reason/attempt, and server responses control `cancel_requested`/new-attempt state.
  </behavior>
  <action>按 D-01、D-02、D-03、D-04、D-05、D-06、D-07、D-08 将单个最新 task refactor 为 movie/manga grouped arrays、task cursor map、selected task/attempt/log cursor state。复用 ConfirmDialog、SkeletonCard、error/toast 和既有 visibilitychange 监听；每 5 秒只刷新可见页，隐藏停止，恢复立即刷新。日志继续 bounded safe page 加 sequence cursor。receipt link 只从 succeeded + validated primaryContentId 生成，并附加受控 task/run/attempt source params；无权限模板完全隐藏，provider 仅显示脱敏摘要。</action>
  <verify>
    <automated>pnpm --filter dashboard test --run src/views/__test__/Crawlers.test.ts</automated>
    <automated>pnpm --filter dashboard type-check</automated>
  </verify>
  <done>Component tests cover grouped history, attempt switching, provider redaction, visible polling, load-more logs, permission hiding, cancel_requested and retry-new-attempt behavior.</done>
</task>
</tasks>

<verification>
<automated>pnpm --filter dashboard test --run src/views/__test__/Crawlers.test.ts</automated>
<automated>pnpm --filter dashboard type-check</automated>
</verification>

<success_criteria>
An authorized operator can scan complete movie/manga task history, inspect any attempt and safe logs on one page, and perform confirmed cancel/retry with truthful server lifecycle state.
</success_criteria>

<output>Create `.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-03-SUMMARY.md` when done.</output>
