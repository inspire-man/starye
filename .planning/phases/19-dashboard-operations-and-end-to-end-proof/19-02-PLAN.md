---
phase: 19-dashboard-operations-and-end-to-end-proof
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/config/src/deployment-target/data-chain-evidence.ts
  - packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
  - scripts/phase19-evidence.ts
  - .planning/phases/19-dashboard-operations-and-end-to-end-proof/19-EVIDENCE-SCHEMA.md
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "Local and production evidence use different mode/target labels and a single explicit run tuple; local contract output cannot be serialized as provider-backed success."
    - "Evidence records contain required task/run/attempt, callback IDs/nonces, validated receipt, Gateway URL, CRUD mutation/readback/restore, command and timestamp fields."
    - "Evidence validation rejects missing tuple identity, raw secret/header/cookie fields, unvalidated receipt IDs and arbitrary provider URLs."
  artifacts:
    - path: packages/config/src/deployment-target/data-chain-evidence.ts
      provides: "Phase 19 local/production evidence schema and deterministic validator"
    - path: scripts/phase19-evidence.ts
      provides: "JSON + Markdown evidence pair builder with redaction and mode separation"
    - path: .planning/phases/19-dashboard-operations-and-end-to-end-proof/19-EVIDENCE-SCHEMA.md
      provides: "canonical field contract consumed by local and production proof plans"
  key_links:
    - from: scripts/phase19-evidence.ts
      to: packages/config/src/deployment-target/data-chain-evidence.ts
      via: "typed validation before writing JSON/Markdown"
    - from: evidence mode
      to: production sign-off
      via: "credentialed_provider tuple requires provider facts and validated terminal receipt"
---

<objective>
固化 Phase 19 的本地/生产 evidence schema、脱敏规则和 JSON+Markdown 生成器，给后续双模板本地 proof 与单条 provider-backed sign-off 共用同一份可验证契约。

Purpose: 满足 TEST-01 的可重复、可追踪证据要求，同时保持 local contract 与 credentialed production 的语义边界。
Output: typed schema、validator、evidence builder 和字段契约文档。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-CONTEXT.md
@.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-RESEARCH.md
@.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-PATTERNS.md
@.planning/phases/18-github-actions-production-orchestration/COVERAGE.md
@packages/config/src/deployment-target/data-chain-evidence.ts
</context>

<tasks>
<task type="tdd" tdd="true">
  <name>Task 1: 扩展 run-bound evidence schema 与 mode gate</name>
  <files>packages/config/src/deployment-target/data-chain-evidence.ts, packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts</files>
  <behavior>
    - local records require Gateway URL, application run/attempt, template and validated receipt plus CRUD readback/restore.
    - credentialed production records additionally require fixed target/workflow/repository/ref/Environment and provider run/attempt/SHA/URL plus signed callback facts.
    - a local record cannot satisfy production status and a production record with missing provider facts remains checkpoint/failed.
  </behavior>
  <action>沿用现有 data-chain evidence discriminated-union、status/checkpoint 和 deterministic timestamp pattern，加入 D-13、D-14、D-18 所需 tuple 字段及 movie/manga CRUD mutation steps。对 receipt 只接受 API validated projection；对 provider URL 要求 server-owned workflow/run identity；对 command 记录使用 allowlisted command label 而非原始 argv。添加 secret/header/cookie/raw receipt rejection tests。</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-evidence.test.ts</automated></verify>
  <done>Schema tests prove both modes, tuple completeness, receipt validation, redaction and truthful checkpoint statuses.</done>
</task>

<task type="auto">
  <name>Task 2: 实现 JSON+Markdown evidence pair builder</name>
  <files>scripts/phase19-evidence.ts, .planning/phases/19-dashboard-operations-and-end-to-end-proof/19-EVIDENCE-SCHEMA.md</files>
  <action>新增只读输入、typed validator、稳定字段排序和 JSON/Markdown 双写 builder。按照 D-13、D-14 将 mode、target、template、fixed workflow、repository/ref/Environment、D1 run/attempt、provider facts、callback event IDs/nonces、validated receipt、Gateway URL、CRUD mutation/readback/restore、命令标签与 UTC timestamp 固定落盘；生成前执行敏感字段扫描并在失败时保留 checkpoint，不把原始响应或 secret 写入文件。文档列出 local_contract 与 credentialed_provider 的必填差异和 evidence owner 边界。</action>
  <verify><automated>pnpm exec tsx scripts/phase19-evidence.ts --self-test</automated></verify>
  <done>Builder self-test emits deterministic JSON and Markdown pairs, rejects unsafe inputs, and records local/production labels without semantic upgrade.</done>
</task>
</tasks>

<verification>
<automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-evidence.test.ts</automated>
<automated>pnpm exec tsx scripts/phase19-evidence.ts --self-test</automated>
</verification>

<success_criteria>
Every Phase 19 proof artifact can be validated against one deterministic, mode-aware evidence contract that preserves failures and keeps secrets/provider payloads out of durable evidence.
</success_criteria>

<output>Create `.planning/phases/19-dashboard-operations-and-end-to-end-proof/19-02-SUMMARY.md` when done.</output>
