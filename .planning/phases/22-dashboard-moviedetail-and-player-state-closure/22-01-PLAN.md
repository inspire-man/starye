---
phase: 22-dashboard-moviedetail-and-player-state-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/movie-app/src/utils/playbackSources.ts
  - apps/movie-app/src/utils/__tests__/playbackSources.test.ts
  - apps/movie-app/src/views/Player.vue
  - apps/movie-app/src/views/__tests__/Player.security.test.ts
autonomous: true
requirements: [PLAY-02, PLAY-03]
must_haves:
  truths:
    - "标准 Player 只初始化 eligible direct source；magnet 入口回到同一电影详情页。"
    - "每个 source 在一个播放会话拥有最多 2 次重试额度，切换 source 或重新进入 Player 会建立新的计数范围。"
    - "网络、缓冲和 TorrServer 流失败进入重试，source-invalid 与 magnet 进入 MovieDetail 受控路径。"
    - "source type 与 eligibility 先于评分、画质和时间排序，默认候选沿用服务端顺序。"
  artifacts:
    - path: "apps/movie-app/src/utils/playbackSources.ts"
      provides: "可复用的 source type、eligibility、候选选择和分组排序策略"
    - path: "apps/movie-app/src/views/Player.vue"
      provides: "受控 direct 播放、loading/buffering/error 状态和 bounded retry 生命周期"
    - path: "apps/movie-app/src/views/__tests__/Player.security.test.ts"
      provides: "Player source guard、retry cap、磁力回流和 waiting/error 竞态回归"
  key_links:
    - from: "apps/movie-app/src/views/Player.vue"
      to: "apps/movie-app/src/utils/playbackSources.ts"
      via: "fetchMovieAndPlay 与 source selection 使用同一 eligibility/type policy"
    - from: "apps/movie-app/src/views/Player.vue"
      to: "apps/movie-app/src/views/MovieDetail.vue"
      via: "source-invalid、magnet 和 retry cap 通过现有 movie code 路由返回详情"
---

<objective>
建立唯一的前端 playback source policy，并把 Player 的加载、缓冲、失效、错误、重试和回流状态闭合。

Purpose: 让 PLAY-02/PLAY-03 的行为由 source type 与 server-owned eligibility 驱动，评分和排序只承担展示职责；为 MovieDetail 复用同一选择契约。
Output: `playbackSources` eligibility/type helper、Player bounded retry/error flow，以及对应 Vitest 回归覆盖。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-dashboard-moviedetail-and-player-state-closure/22-CONTEXT.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-VERIFICATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@apps/api/src/domain/movies/source-contract.ts
@apps/movie-app/src/types.ts
@apps/movie-app/src/utils/playerSecurity.ts
@apps/movie-app/src/composables/useAria2.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 eligibility-aware source policy</name>
  <files>apps/movie-app/src/utils/playbackSources.ts, apps/movie-app/src/utils/__tests__/playbackSources.test.ts</files>
  <read_first>
    apps/movie-app/src/utils/playbackSources.ts
    apps/movie-app/src/utils/__tests__/playbackSources.test.ts
    apps/movie-app/src/types.ts:87-103
    apps/api/src/domain/movies/source-contract.ts:1-180
    .planning/phases/22-dashboard-moviedetail-and-player-state-closure/22-CONTEXT.md:19-23
  </read_first>
  <behavior>
    - Test 1: active direct and active magnet candidates are eligible; inactive, blank-url and missing-url candidates are ineligible.
    - Test 2: source classification honors an explicit TorrServer source marker, recognizes magnet URLs, and treats other valid URLs as direct.
    - Test 3: default playback/display groups are eligible direct, eligible magnet, then inactive/ineligible; each group preserves incoming server order.
    - Test 4: rating, quality and latest sorting can order presentation inside a group, while score alone never changes eligibility or the default candidate.
  </behavior>
  <action>Extend `apps/movie-app/src/utils/playbackSources.ts` with named pure helpers for source type classification, eligibility, direct candidate selection, and the D-04 display grouping. Use `isActive === true` plus a trimmed non-empty `sourceUrl` as the client-side eligibility gate, honor `source === 'TorrServer'` before URL inference, recognize magnet URLs, and preserve array order as the server-controlled tie-breaker per D-01, D-02 and D-04. Keep `sortPlaybackSources` available for presentation; make its score/quality/latest behavior an intra-group concern so D-02 and D-12 remain true. Add focused fixtures to `playbackSources.test.ts` that prove PLAY-03 with an intentionally high-scoring ineligible row and mixed direct/magnet/TorrServer rows.</action>
  <acceptance_criteria>Helpers expose deterministic type and eligibility decisions; the default ordered result is eligible direct, eligible magnet, inactive/ineligible; server order survives equal policy keys; score, quality and timestamp never create a playback candidate.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vitest run src/utils/__tests__/playbackSources.test.ts</automated>
  </verify>
  <done>Shared source policy tests pass and provide the exact contract consumed by Player and MovieDetail.</done>
</task>

<task type="auto">
  <name>Task 2: 完成 Player 的受控路径与 bounded retry</name>
  <files>apps/movie-app/src/views/Player.vue, apps/movie-app/src/views/__tests__/Player.security.test.ts</files>
  <read_first>
    apps/movie-app/src/views/Player.vue:18-61
    apps/movie-app/src/views/Player.vue:168-300
    apps/movie-app/src/views/Player.vue:302-489
    apps/movie-app/src/views/Player.vue:559-611
    apps/movie-app/src/views/Player.vue:641-774
    apps/movie-app/src/views/__tests__/Player.security.test.ts
    apps/movie-app/src/utils/playbackSources.ts
    apps/movie-app/src/utils/playerSecurity.ts
    apps/movie-app/src/composables/useAria2.ts:130-275
  </read_first>
  <action>Update `fetchMovieAndPlay`, `isEligiblePlayer`, `getPlaybackErrorState`, `scheduleWaitingTimeout`, `showPlayerError`, `initPlayer`, `retryCurrentSource` and `goToDetail` in `Player.vue` to consume Task 1 policy. In standard mode select the first eligible direct in server order per D-01/D-02; an explicit magnet `player` query or magnet-only selection renders a source-invalid state and returns to MovieDetail per D-03, while trusted `streamUrl` keeps the existing TorrServer security gate and Aria2 fallback. Track the current source identity and playback-session token, grant each source exactly 2 retry actions per session per D-05/D-06, and escalate to a visible source-switch/detail action after the cap. Classify transient network, waiting/buffering and TorrServer failures as retryable per D-07; make source-invalid and magnet failures detail-bound. Add a loading-cycle token and consumed-failure marker so one waiting timeout followed by xgplayer `error` emits one failure and consumes at most one retry slot per D-08. Keep view tracking and xgplayer construction behind readiness, eligibility and direct-source gates; expose loading, buffering, invalid-source and playback-error feedback with stable status/alert semantics and existing design tokens.</action>
  <acceptance_criteria>Player construction receives only an eligible direct URL in standard mode; trusted TorrServer mode remains available; magnet and invalid source states route to the same movie detail; two retry clicks are available for a source per session, a third retry action escalates to source switching/detail, and waiting/error races consume one slot; Aria2 remains available only for an eligible magnet fallback.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vitest run src/views/__tests__/Player.security.test.ts</automated>
    <automated>pnpm --filter @starye/movie-app exec vue-tsc -b</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Player security and behavior regressions pass for direct, magnet, inactive, trusted TorrServer, retry-cap and waiting/error race fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway/API DTO -> Movie Player | `players`, readiness and query parameters reach browser playback code. |
| Browser query -> TorrServer stream | `streamUrl` is untrusted client-controlled input crossing into xgplayer. |
| Browser -> Aria2/TorrServer composables | Magnet actions reach user-configured local services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | `fetchMovieAndPlay` / `playbackSources.ts` | high | mitigate | Apply explicit active/non-empty eligibility and source-type routing before xgplayer construction; reject magnet as browser media. |
| T-22-02 | Tampering | `Player.vue` `streamUrl` mode | high | mitigate | Preserve `isTrustedTorrServerStreamUrl` and trusted-origin resolution before `initPlayer`; test trusted and untrusted origins. |
| T-22-03 | Denial of service | retry and waiting handlers | medium | mitigate | Session/source retry cap plus loading-cycle deduplication bounds repeated network and player events. |
| T-22-04 | Information disclosure | Player error reporting | medium | mitigate | Keep user-facing errors and Sentry context bounded to existing movie/source classification fields; retain raw media URLs behind current trusted flow only. |
| T-22-SC | Tampering | package installs | low | accept | This plan adds no package installation; existing workspace dependencies remain the supply-chain boundary. |
</threat_model>

<verification>
Run the focused movie-app tests and type-check through the existing workspace, then inspect the real local surface at `http://localhost:8080/movie/SUN-064` and `http://localhost:8080/movie/SUN-064/play` after the local stack is running. The browser check confirms source-invalid/magnet return, loading/buffering/error copy, and direct-only Player initialization; it is a local UI observation, separate from Phase 24 playback proof.
</verification>

<success_criteria>
PLAY-02 and PLAY-03 are satisfied by observable direct-only initialization, type-specific fallback, bounded per-source/session retry, race deduplication, and focused regressions with score/order separated from eligibility.
</success_criteria>

<output>
Artifacts this phase produces:
- Eligibility/type/display policy in `apps/movie-app/src/utils/playbackSources.ts` with focused tests.
- Player bounded retry/error implementation in `apps/movie-app/src/views/Player.vue` with security regressions.
- Phase boundary: GitHub Actions production repair/reconciliation remains Phase 23; fresh production browser playback evidence remains Phase 24.
Create `.planning/phases/22-dashboard-moviedetail-and-player-state-closure/22-01-SUMMARY.md` when done.
</output>
