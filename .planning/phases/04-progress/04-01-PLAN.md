---
phase: 04-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema.ts
  - packages/db/drizzle/*.sql
  - apps/api/src/schemas/progress.ts
  - apps/api/src/routes/public/progress/index.ts
  - apps/api/src/routes/public/progress/__tests__/progress.test.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03

must_haves:
  truths:
    - "D-01: 旧 `reading_progress` / `watching_progress` 数据不迁移，不做兼容兜底读取"
    - "D-02: 同一 phase 内统一 `progress` 表、新 API、新前端调用全量切换，不保留旧 API 外壳转发"
    - "D-03: cutover 后按空进度处理，不新增系统已重置提示、toast、banner 或公告"
    - "D-04: migration 同期直接删除 `reading_progress` / `watching_progress`，不留 `_legacy`"
    - "D-05: 统一表只保留一套正式语义，不维护双表、双写或双读逻辑"
    - "统一 `progress` 表成为唯一正式进度模型，history 查询以 `(userId, updatedAt)` 索引和统一表为基础，不再依赖两张旧表"
    - "progress upsert 使用单表 conflict 目标，不再手写 select-then-update 的双轨逻辑"
    - "API response 显式返回 `completed`，供 movie/comic 前端停止猜测完成状态"
    - "所有 progress route 继续要求登录；匿名读写仍是 401"
  artifacts:
    - path: "packages/db/src/schema.ts"
      provides: "统一 progress schema + relations"
      contains: "progress"
    - path: "packages/db/drizzle/*.sql"
      provides: "新表/索引/旧表删除 migration"
      contains: "reading_progress"
    - path: "apps/api/src/schemas/progress.ts"
      provides: "统一 progress request/response schema"
      contains: "completed"
    - path: "apps/api/src/routes/public/progress/index.ts"
      provides: "单表 unified progress route"
      contains: "completed"
    - path: "apps/api/src/routes/public/progress/__tests__/progress.test.ts"
      provides: "route-level contract regression coverage"
      contains: "需要登录"
  key_links:
    - from: "packages/db/src/schema.ts"
      to: "apps/api/src/routes/public/progress/index.ts"
      via: "统一 progress table / conflict key"
      pattern: "progress"
    - from: "apps/api/src/schemas/progress.ts"
      to: "apps/api/src/routes/public/progress/index.ts"
      via: "validator + response contract"
      pattern: "completed"
---

<objective>
先完成 Phase 4 的数据与接口基线：把 `reading_progress` / `watching_progress` 两张旧表收口为一张统一 `progress` 表，并让 `apps/api/src/routes/public/progress/index.ts` 切换到单表读写、显式 `completed`、统一 history 查询。

Purpose: 没有这一步，movie/comic 两端会继续围绕不同 schema 和不同完成规则写逻辑，后续任何“恢复 / 完成 / 历史”修正都会建立在错误基座上。

Output: `packages/db/src/schema.ts`、migration、`apps/api/src/schemas/progress.ts`、`apps/api/src/routes/public/progress/index.ts`、`progress.test.ts` 全部切到 unified progress 语义。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-progress/04-CONTEXT.md
@.planning/phases/04-progress/04-RESEARCH.md
@.planning/phases/04-progress/04-PATTERNS.md
@.planning/phases/04-progress/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一 schema / relations / migration，删除旧表定义</name>
  <files>packages/db/src/schema.ts, packages/db/drizzle/*.sql</files>
  <read_first>
    - packages/db/src/schema.ts
    - packages/db/MIGRATION.md
    - .agents/skills/starye-db-migration/SKILL.md
    - .planning/phases/04-progress/04-RESEARCH.md
    - .planning/phases/04-progress/04-PATTERNS.md
  </read_first>
  <action>
    1. 在 `packages/db/src/schema.ts` 中新增统一 `progress` 表，列语义至少包含：`id`（如保留 surrogate key）、`userId`、`contentType`、`contentId`、`position`、`duration`、`completed`、`updatedAt`。
    2. conflict 键必须能唯一标识 `(userId, contentType, contentId)`；若不采用真正复合主键，则必须至少建立等价唯一索引，并在 schema 中写清楚这是 unified progress 的唯一约束。
    3. 建立 `(userId, updatedAt)` 索引满足 `PROG-02` 的 history query。
    4. 删除 `readingProgress` / `watchingProgress` 两张旧表定义及其 relations，引入统一 `progressRelations` 与 `userRelations` 新关联。
    5. 生成 migration：创建新表与索引，并在同一 migration 中删除 `reading_progress` / `watching_progress` 旧表与旧索引，不保留 `_legacy`。
    6. migration SQL 必须显式体现旧表退场与新表生效，不接受“只改 schema.ts，migration 留空”。
  </action>
  <verify>
    <automated>pnpm --filter @starye/db run generate && pnpm --filter @starye/api exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/src/schema.ts` 中存在统一 `progress` 表定义
    - `packages/db/src/schema.ts` 中不存在 `export const readingProgress =` 与 `export const watchingProgress =`
    - `packages/db/src/schema.ts` 中存在 `contentType`、`contentId`、`position`、`completed`
    - `packages/db/src/schema.ts` 中存在 `(userId, contentType, contentId)` 的唯一约束或复合主键语义
    - `packages/db/src/schema.ts` 中存在 `(userId, updatedAt)` 索引
    - 新 migration SQL 同时包含创建 `progress` 与删除旧 `reading_progress` / `watching_progress`
  </acceptance_criteria>
  <done>
    unified progress schema、relations 与 migration 已成为唯一正式模型。
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 progress route 与 schema 为单表 contract</name>
  <files>apps/api/src/schemas/progress.ts, apps/api/src/routes/public/progress/index.ts</files>
  <read_first>
    - apps/api/src/schemas/progress.ts
    - apps/api/src/routes/public/progress/index.ts
    - apps/api/src/index.ts
    - .agents/skills/starye-hono-rpc/SKILL.md
    - .planning/phases/04-progress/04-CONTEXT.md
    - .planning/phases/04-progress/04-RESEARCH.md
  </read_first>
  <action>
    1. 把 progress schema 改成 unified contract：movie 与 comic 的单条记录、history 记录、保存 payload 都围绕统一 `position / duration / completed / updatedAt` 语义。
    2. route 继续挂在 `/api/public/progress` 命名空间下，但内部要以 `contentType` 驱动统一读写；允许保留 reading/watching 兼容 wrapper 作为短期入口，只要底层已经只走统一表，不保留双表逻辑。
    3. save route 必须使用单表 upsert；不再手写“先 select 再 update/insert”。如执行层选用 `ON CONFLICT DO UPDATE` 或等价 builder，必须明确 conflict 目标就是 unified progress 唯一键。
    4. movie history 查询要继续返回影片展示所需的 join 字段，并新增显式 `completed`；comic history 至少返回稳定 chapter 标识与 `completed`。
    5. 所有 GET/PUT/POST 路由继续保持登录要求；未登录统一 401，不引入新的匿名 progress 语义。
  </action>
  <verify>
    <automated>pnpm --filter @starye/api test --run src/routes/public/progress/__tests__/progress.test.ts && pnpm --filter @starye/api exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/schemas/progress.ts` 中存在 `completed` 字段定义
    - `apps/api/src/routes/public/progress/index.ts` 不再 import `readingProgress` / `watchingProgress`
    - `apps/api/src/routes/public/progress/index.ts` 中的写入逻辑不再包含“select existing -> update else insert”双分支
    - movie history 返回模型包含 `completed`
    - 匿名请求 progress route 仍返回 401 与 `需要登录`
  </acceptance_criteria>
  <done>
    progress route 与 response schema 已切到 unified contract。
  </done>
</task>

<task type="auto">
  <name>Task 3: 扩展 progress route tests，锁定 unified contract 与旧表退场</name>
  <files>apps/api/src/routes/public/progress/__tests__/progress.test.ts</files>
  <read_first>
    - apps/api/src/routes/public/progress/__tests__/progress.test.ts
    - apps/api/src/routes/public/progress/index.ts
    - apps/api/src/schemas/progress.ts
    - .planning/phases/04-progress/04-VALIDATION.md
  </read_first>
  <action>
    1. 保留现有 route test 的 Hono + mock db 结构。
    2. 新增 unified contract 断言：movie 单条读取包含 `completed`；history 列表包含 `completed`；limit / auth guard 仍然正确。
    3. 若 comic history 或 unified contentType route 引入新 query 形状，补对应 case，确保 route test 能证明“旧双表逻辑已退场、统一表语义已生效”。
    4. 如 API 形状变化影响旧断言（例如字段名从 `progress` 改为 `position`），同步把测试改到新 contract，不留旧字段的假绿测试。
  </action>
  <verify>
    <automated>pnpm --filter @starye/api test --run src/routes/public/progress/__tests__/progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `progress.test.ts` 仍覆盖未登录 401
    - `progress.test.ts` 覆盖 movie history 列表
    - `progress.test.ts` 覆盖单条读取的 `completed` 语义
    - route test 全绿且不再依赖旧 `readingProgress` / `watchingProgress` 表字段
  </acceptance_criteria>
  <done>
    API route regression suite 已锁定 unified progress contract。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | 登录用户通过 progress route 读写个人进度 |
| API → D1 | unified progress 表承载用户消费历史，写入必须按 userId 隔离 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | unified progress history query | mitigate | history / single progress 查询继续以 `userId` 为 where 条件前缀，匿名请求仍 401，不引入共享历史列表 |
| T-04-02 | Tampering | upsert conflict key 错配 | mitigate | conflict 目标显式绑定 `(userId, contentType, contentId)` 等价唯一键，避免不同内容互相覆盖 |
| T-04-03 | Repudiation | migration 删除旧表后无法解释数据重置 | accept | 本 phase 已锁定 hard cutover；不迁移旧数据属于明确产品决策，不做误导性兼容 |
| T-04-04 | Information Disclosure | old table dual-read path 残留 | mitigate | route、schema、tests 全部去除旧表 import / 字段依赖，避免执行阶段继续读旧表 |

</threat_model>

<verification>
## 整体验证

```bash
pnpm --filter @starye/db run generate
pnpm --filter @starye/api test --run src/routes/public/progress/__tests__/progress.test.ts
pnpm --filter @starye/api exec tsc --noEmit
rg -n "readingProgress|watchingProgress" apps/api/src/routes/public/progress/index.ts apps/api/src/schemas/progress.ts
```

</verification>

<success_criteria>
1. unified `progress` schema 成为唯一正式模型
2. 旧 `reading_progress` / `watching_progress` 在 schema 与 migration 中同步退场
3. API progress route 切到单表 unified contract
4. route tests 锁定 `completed` 与 history 查询语义
</success_criteria>

<output>
After completion, create `.planning/phases/04-progress/04-01-SUMMARY.md`
</output>
