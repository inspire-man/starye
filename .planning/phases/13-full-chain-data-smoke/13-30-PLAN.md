---
phase: 13-full-chain-data-smoke
plan: "30"
type: execute
wave: 32
depends_on: [13-29]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md"
  - ".planning/phases/13-full-chain-data-smoke/13-30-SUMMARY.md"
autonomous: false
gap_closure: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Root Codex persistent in-app browser (selected-production authorized session)
    why: Only the existing signed-in selected-target Dashboard session may supply production Dashboard and viewer observations for the exact remote pending tuple.
    dashboard_config:
      - task: Preserve the existing authorized selected-target Movies session; do not export cookies or storage.
        location: Selected-canonical Dashboard movies route for starye-org
must_haves:
  truths:
    - "D-03: 13-29-SUMMARY.md records remote handoffReady true and remote pending for the run_id bound from 13-17-SUMMARY.md; remote verify raw exit 2 with resolved_pending_observation/pending. Checkpoint outcomes permanently block this plan."
    - "Zero-byte remote.attempt remains read-only ownership history; untracked; never treated as success."
    - "D-04/D-05/D-06: Root persistent Codex IAB observes selected-canonical Dashboard first, then canonical /movie/<item-code> viewer second, for the remote itemId/itemCode from the repository-derived pending tuple."
    - "Repository core owns receipt construction and pair writes; browser adapter supplies navigation outcome only. No Chrome/Playwright/Puppeteer/manual attestation substitutes."
    - "After ordered receipts, exact remote verifier and exact local verifier both return raw exit 0, terminal_passed, provesExternalChain true, shared target/run/code, mode-owned itemIds."
    - "No remote handoff/runner rerun occurs in this plan. Evidence remains untracked. Historical blocked trees stay immutable."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.attempt"
      provides: "Read-only zero-byte remote ownership marker"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json"
      provides: "Ordered production Dashboard/viewer receipts or immutable browser checkpoint"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md"
      provides: "Exact deterministic peer"
    - path: ".planning/phases/13-full-chain-data-smoke/13-30-SUMMARY.md"
      provides: "Production surface finalization and dual exact verifier handoff for 13-31"
  key_links:
    - from: "13-29 remote pending pair"
      to: "production IAB"
      via: "artifact-only remote verify exit 2"
      pattern: "resolved_pending_observation"
    - from: "ordered Dashboard then viewer receipts"
      to: "dual exact verifiers"
      via: "repository core observeDataChainSurfaces"
      pattern: "terminal_passed + provesExternalChain"
---

<objective>
Close the selected-production ordered Dashboard → viewer finalization gap that stale 13-20 left unexecuted: after 13-29 remote pending, record real selected-target Dashboard observation then matching canonical viewer observation, then dual exact local/remote verifier terminal results.

Purpose: Satisfy DATA-04/05/06/07 and TEST-05 production surface proof without replaying remote handoff or local lifecycle.
Output: Updated untracked remote pair with browser receipts (or honest checkpoint) plus 13-30-SUMMARY.md. Does not rewrite 13-VERIFICATION.md (that is 13-31).
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-20-PLAN.md
</context>

## Artifacts this phase produces
- `13-30-SUMMARY.md` (tracked Summary only)
- Updated untracked `remote.json` / `remote.md` with ordered production surface receipts (or checkpoint)
- Dual exact verifier command results recorded in Summary

## Supersedes / sequencing note
Historical plan `13-20-PLAN.md` remains as the original production IAB contract reference. Prefer **13-30** because 13-20 still depends on unexecuted 13-19 and stale 13-25 lifecycle gates. Consume **13-29-SUMMARY.md** remote pending + live p13-17 run binding. Do not execute 13-20 in parallel.

## Constraints
- Gateway/canonical domain for production surfaces; never treat direct app ports as canonical evidence URLs.
- One-item contract unchanged; shared target/run/code; mode-owned itemIds.
- Dirty worktree: do not stage/commit unrelated files or evidence.
- GitNexus impact analysis only if code symbols must change (prefer zero code edits).

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm selected-production browser session readiness</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-30-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
  </read_first>
  <what-built>
    Plan 13-29 produced (or should produce) one remote pending provider-backed tuple for the p13-17 run. Local pair is already terminal from 13-18. This plan only observes production Dashboard then viewer for that remote tuple.
  </what-built>
  <how-to-verify>
    1. Confirm 13-29-SUMMARY.md reports remote pending / handoffReady true (not preflight checkpoint). If checkpoint, stop and do not authorize browser work.
    2. Confirm the root Codex persistent IAB still has an authorized selected-target Dashboard session for starye-org (no cookie export).
    3. Confirm you authorize one ordered Dashboard-then-viewer observation for the remote pending item only (no deploy, no second remote handoff).
    4. Reply: `approved production IAB for starye-org` plus the bound run_id.
  </how-to-verify>
  <resume-signal>Type approved production IAB for starye-org plus run_id, or refuse/defer</resume-signal>
  <action>
    Present 13-29 remote pending status and the bound run_id to the human. Do not open browser routes, rewrite remote pair files, or re-invoke remote handoff in this task. On approval, record the authorization phrase for Task 2. On refusal or if 13-29 is a checkpoint, stop without production surface observation.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md')) { throw '13-29-SUMMARY missing before production IAB auth' }; $s = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md'; if ($s -match 'target_preflight_unmet' -and $s -notmatch 'handoffReady:\s*true|handoffReady true|pending success') { throw '13-29 is checkpoint-only; production IAB must not proceed' }; Write-Output '13-29 Summary present for auth gate'"</automated>
  </verify>
  <done>Human confirms pending remote tuple and authorizes one production IAB observation sequence.</done>
</task>

<task type="auto">
  <name>Task 2: Artifact-verify remote pending and bind run paths</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-30-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-29-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
  </read_first>
  <action>
    After Task 1 approval. Fail closed:

    1. Read 13-29-SUMMARY.md; require remote pending success language (handoffReady true, not target_preflight_unmet). Checkpoint stops permanently for this run with no browser action.
    2. Bind `$runId` from 13-17-SUMMARY.md with exactly one `run_id: p13-17-[0-9a-f]{32}` match; derive remote.json, remote.md, remote.attempt LiteralPaths from fixed evidence root + starye-org + `$runId` only.
    3. Require remote.attempt exists, length 0, untracked (`??`); treat as read-only ownership history only.
    4. Run exact remote verifier once:
       `pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId`
       Require raw exit 2 and pending / resolved_pending_observation for that runId. Do not rerun handoff or remote runner.
    5. Run exact local verifier once for the same `$runId` and require it still terminal (raw 0) so dual-mode correlation remains available after browser closeout.
    6. Extract repository-derived remote itemId and itemCode only from the verified remote pair (core-owned fields). Never invent identity from operator memory.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$s17 = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md'; $m = @([regex]::Matches($s17, '(?m)^run_id:\s*(p13-17-[0-9a-f]{32})\s*$')); if ($m.Count -ne 1) { throw 'run_id bind failed' }; $runId = $m[0].Groups[1].Value; $attempt = Join-Path '.planning/phases/13-full-chain-data-smoke/evidence/starye-org' $runId 'remote.attempt'; if (-not (Test-Path -LiteralPath $attempt)) { throw 'remote.attempt missing' }; if ((Get-Item -LiteralPath $attempt).Length -ne 0) { throw 'remote.attempt not zero-byte' }; pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId; if ($LASTEXITCODE -ne 2) { throw \"expected remote pending exit 2, got $LASTEXITCODE\" }; pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $runId; if ($LASTEXITCODE -ne 0) { throw \"local terminal exit expected 0, got $LASTEXITCODE\" }"</automated>
  </verify>
  <done>Remote pending and local terminal verified for bound run_id; browser not yet navigated.</done>
</task>

<task type="auto">
  <name>Task 3: Ordered production Dashboard then viewer observation and dual terminal verify</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md, .planning/phases/13-full-chain-data-smoke/13-30-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - .planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-20-PLAN.md
  </read_first>
  <action>
    Re-bind `$runId` as in Task 2. Use only root persistent Codex IAB under the repository browser_execution_contract. Repository core owns tuple derivation, route order, receipt construction, validation, JSON/Markdown rendering, and writes; the browser adapter supplies navigation outcome only.

    Observation order (locked D-04):
    1. Selected-canonical Dashboard movies surface first for the remote pending item.
    2. Matching canonical viewer route `/movie/<item-code>` second (same itemCode as repository-derived code).

    On each settled route require exactly one element whose `data-phase13-item-id` attribute equals the repository-provided remote itemId, and whose settled visible textContent contains the repository-provided itemCode. Never require remote itemId in visible text. Do not persist cookies, storage, HTML, screenshots, headers, or direct-port URLs.

    Any auth/role/route/DOM/tuple/write failure must return unavailable so core persists an allowlisted checkpoint; close the run with no retry and no second handoff.

    After core writes only the remote pair (and never the attempt marker), run both exact verifiers with `$runId`:
    - `pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId` → require raw 0, terminal_passed, provesExternalChain true
    - `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $runId` → require raw 0, terminal_passed, provesExternalChain true
    - Shared target/run/code; mode-owned itemIds remain internally consistent
    - Recheck remote.attempt still zero-byte and untracked

    Write `13-30-SUMMARY.md` with bound run_id, pre-browser pending exits, ordered surface results, post dual-verifier exits, marker integrity, scope boundary (no Phase completion claim until 13-31 canonical re-verify). Keep evidence untracked. Commit only the Summary after detect-changes if needed. Do not edit 13-VERIFICATION.md, ROADMAP, STATE, or REQUIREMENTS here.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$s17 = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md'; $m = @([regex]::Matches($s17, '(?m)^run_id:\s*(p13-17-[0-9a-f]{32})\s*$')); if ($m.Count -ne 1) { throw 'run_id bind failed' }; $runId = $m[0].Groups[1].Value; pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId; if ($LASTEXITCODE -ne 0) { throw \"remote terminal expected 0, got $LASTEXITCODE\" }; pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $runId; if ($LASTEXITCODE -ne 0) { throw \"local terminal expected 0, got $LASTEXITCODE\" }; if (-not (Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-30-SUMMARY.md')) { throw '13-30-SUMMARY missing' }"</automated>
  </verify>
  <done>Ordered production Dashboard then viewer receipts exist for the remote pending tuple; dual exact verifiers terminal_passed; Summary complete; phase status still open until 13-31.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Remote pending artifact → production browser | Artifact-only verify exit 2 before any navigation |
| Authorized IAB → repository core | Browser navigates only; core writes receipts |
| Dual exact proof → phase status | Phase close deferred to 13-31 canonical re-verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-30-01 | Spoofing | production browser receipts | critical | mitigate | Require data-phase13-item-id attribute + visible itemCode match |
| T-13-30-02 | Elevation of Privilege | production Dashboard | critical | mitigate | Existing authorized session only; no session fabrication/export |
| T-13-30-03 | Information Disclosure | browser/provider state | critical | mitigate | Allowlisted tuple/path receipts; no secrets/cookies/HTML |
| T-13-30-04 | Tampering | cross-mode identity | critical | mitigate | Shared target/run/code; independent mode itemIds |
| T-13-30-05 | Repudiation | surface finalization | high | mitigate | Dual exact verifier raw 0 + Summary command exits |
| T-13-30-SC | Tampering | package installs | low | accept | No install/dependency action |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Selected target usable end to end | 13-29 + 13-30 + 13-31 | COVERED | Surfaces here |
| REQ | DATA-04 | D1/API/admin | Task 3 admin via Dashboard | COVERED | Production admin surface |
| REQ | DATA-05 | Authorized Dashboard | Task 3 | COVERED | Ordered first |
| REQ | DATA-06 | Canonical viewer | Task 3 | COVERED | Ordered second |
| REQ | DATA-07 | Artifacts | Task 3 | COVERED | remote pair updated |
| REQ | TEST-05 | Repeatable smoke | Task 3 dual verify | COVERED | Exact commands |
| CONTEXT | D-04 | Exact surfaces | Task 3 | COVERED | Dashboard then viewer |
| CONTEXT | D-06 | Non-secret artifacts | Task 3 | COVERED | Core-owned receipts |
| VERIFICATION | G-prod-surfaces | Production Dashboard/viewer/terminal absent | Tasks 1-3 | COVERED | Remaining failed truth |
</source_audit>

<verification>
- Remote pending exit 2 before browser
- Ordered Dashboard then viewer only
- Dual exact verifiers raw 0 after receipts
- remote.attempt unchanged zero-byte untracked
- No handoff rerun; no Phase completion claim
</verification>

<success_criteria>
- Selected-production item is visibly usable through Dashboard and canonical viewer for the p13-17 remote tuple
- Dual mode exact verifiers terminal_passed with provesExternalChain true
- 13-30-SUMMARY.md is the tracked handoff to re-verification
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-30-SUMMARY.md` when done
</output>
