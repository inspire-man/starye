---
phase: 13-full-chain-data-smoke
plan: "66"
type: execute
wave: 60
depends_on: [13-56]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "The root agent's claimed in-app Browser proves a signed-in local Movies surface before p13-66 is allocated; a child agent's lack of that binding is not evidence that the Browser is unavailable."
    - "p13-55, p13-57, p13-60, and every earlier carrier/evidence tree remain frozen: this plan does not observe, verify, retry, hand off, or otherwise operate on them."
    - "Exactly one fresh p13-66-<32 lowercase hex> carrier creates one deterministic non-R18, one-player item and one non-empty local itemId through the local handoff."
    - "The root IAB observes only the fresh p13-66 tuple in the approved Dashboard then Viewer order through http://localhost:8080; success requires terminal_passed and provesExternalChain true, while any checkpoint freezes p13-66 without a retry."
    - "All local evidence is non-secret and tuple-bound; no remote, provider, deploy, migration, production-browser, cookie, storage, or session material action occurs."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt
      provides: The only fresh local carrier identifier, written after root-IAB readiness and dual Gateway readiness both pass.
    - path: .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
      provides: Non-secret root-IAB readiness, handoff, observation, verifier, and frozen-checkpoint outcome for p13-66.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-66-run-id}/local.json
      provides: Untracked one-item local evidence correlated by target, run, deterministic code, and local itemId.
  key_links:
    - from: root-owned signed local IAB readiness
      to: p13-66 run allocation
      via: claimed Dashboard Movies surface with signed-in and hasMoviesSurface proof before any run-id file exists
      pattern: root_iab_local_ready
    - from: p13-66 local pending evidence
      to: ordered Gateway Dashboard then Viewer observations
      via: the existing observeDataChainSurfaces core with a root-IAB-only observeSurface adapter
      pattern: terminal_passed and provesExternalChain true
---

<objective>
Use the root agent's currently claimable in-app Browser to create one new local terminal carrier only after non-sensitive signed-session and Movies-surface readiness is observed, then preserve either terminal proof or one frozen p13-66 checkpoint.

Purpose: `13-63-SUMMARY.md` recorded a child-scoped IAB-binding limitation, not a universal absence of the root IAB. Root evidence now shows a claimed `http://localhost:8080/dashboard/movies` tab with signed-in and Movies-surface state. This plan supersedes the executable local intent of 13-63 without altering 13-63's historical record, and begins a new carrier rather than reusing any historical run.

Output: Conditional `13-66-RUN-ID.txt`, untracked local evidence, and `13-66-SUMMARY.md`. Remote execution and production observation remain outside this plan.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-57-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-63-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
</context>

## Supersession And Frozen History

- `13-63` remains an honest historical pre-allocation block. Its conclusion about a child agent's missing IAB binding is not a prerequisite for this root-owned plan and is not rerun.
- `13-64` and `13-65` require a p13-63 run id that was never created. Plans 13-67 and 13-68 supersede their executable intent with a p13-66 carrier.
- `p13-55`, `p13-57`, `p13-60`, and all earlier carrier/evidence trees are immutable. Do not invoke a verifier, observer, handoff, checksum, retry, or remote action for any of them.

<tasks>
<task type="auto">
  <name>Task 1: Root-own the local signed IAB pre-allocation gate</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
    - scripts/data-chain-surface-observation.ts
  </read_first>
  <action>
This task is root-agent owned: claim the existing application in-app Browser and inspect only the current `http://localhost:8080/dashboard/movies` surface. Before any p13-66 allocation, write the initial `13-66-SUMMARY.md` with non-sensitive readiness facts that the tab is on the canonical local Gateway route, its real signed-in state is present, and the Movies collection surface is available. Do not read, print, export, persist, or transfer cookies, storage, headers, tokens, browser-profile files, or session values. A delegated child must not call its inability to bind the root IAB an unavailable-adapter result; it must return control to the root without allocating or summarizing a false block. If the root-owned check itself lacks signed-in or Movies-surface proof, retain the summary as a pre-allocation root-IAB readiness checkpoint, leave `13-66-RUN-ID.txt` absent, and stop. This implements D-03, D-05, and D-06 while consuming the approved `13-UI-SPEC.md` session boundary and Movies-surface contract.
  </action>
  <verify>
    <automated>if (-not (Test-Path -LiteralPath .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md)) { throw 'root-IAB readiness summary is required' }</automated>
  </verify>
  <acceptance_criteria>
    - Root-only readiness evidence contains only route, signed-in, and Movies-surface result categories.
    - A non-ready root check creates no p13-66 run id or local evidence directory.
    - No historical carrier is operated, and no child asserts a universal IAB outage.
  </acceptance_criteria>
  <done>The root IAB is proved ready for a new local carrier, or the plan freezes before allocation.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate one deterministic local pending carrier</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
    - packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
  </read_first>
  <action>
Proceed only after Task 1 records root-IAB readiness. Confirm that `13-66-RUN-ID.txt` and every `evidence/starye-org/p13-66-*` path are absent. Run `pnpm check:services` twice and require both structured records to accept Gateway plus `robots`, `auth`, and `authSlash`. Allocate exactly one `p13-66-<32 lowercase hex>` identifier, write it once, run `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <p13-66>`, then run its one pre-observation exact local verifier. Continue only when the persisted tuple is pending with one deterministic non-R18 item, one player, a non-empty local itemId, `runnerInvocations: 1`, and `provesExternalChain: false`. Any non-pending result is the terminal state of this carrier: record its non-secret category in the summary and stop. Do not run another handoff or verifier after that checkpoint. This implements D-01, D-02, D-03, D-04, D-06, and D-07 with the repository-owned one-item handoff and verifier contracts.
  </action>
  <verify>
    <automated>$runId = (Get-Content -Raw -LiteralPath .planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt).Trim(); pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $runId; if ($LASTEXITCODE -notin 0, 2) { exit $LASTEXITCODE }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Allocation follows root IAB readiness and two accepted canonical Gateway readiness records.
    - Exactly one local handoff creates a tuple-bound pending pair with a non-empty itemId, or p13-66 freezes at its first non-success state.
    - No remote evidence, provider command, or production claim exists for p13-66.
  </acceptance_criteria>
  <done>One unobserved local p13-66 tuple is ready for root observation, or the only new carrier is frozen.</done>
</task>

<task type="auto">
  <name>Task 3: Root-observe the fresh local tuple and close its exact verifier</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>
Only the root agent holding the claimed IAB may observe the new pending pair. Invoke the existing `observeDataChainSurfaces` core with an in-memory root-IAB `observeSurface` adapter, keeping the core responsible for tuple derivation, receipt construction, ordering, validation, and evidence writes. The adapter may return only `status`, `itemCode`, and `itemId` from actual root-IAB navigation. It must visit `http://localhost:8080/dashboard/movies` first and confirm the exact p13-66 code plus local itemId in the Dashboard row, then visit `http://localhost:8080/movie/<item-code>` and confirm the same tuple in the Viewer. Do not call the default observer, create a child-owned browser, or read session material. Run one exact local verifier after the ordered observation: only `terminal_passed` with `provesExternalChain: true` unlocks Plan 13-67. Any adapter, auth, code, id, Viewer, or verifier checkpoint freezes p13-66 permanently with no replacement observer, retry, handoff, remote command, or production observation. Record only status, code, itemId presence, route class, and verifier exit category in the summary. This implements D-03, D-04, D-06, and D-07 and consumes the UI contract's Dashboard-first identity and session requirements.
  </action>
  <verify>
    <automated>$runId = (Get-Content -Raw -LiteralPath .planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt).Trim(); pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $runId; if ($LASTEXITCODE -notin 0, 2) { exit $LASTEXITCODE }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - The only observation order is canonical local Dashboard then canonical local Viewer.
    - A local terminal result has exact code/itemId correlation and both terminal verifier fields; every other result is a permanent checkpoint.
    - Summary and untracked evidence contain no cookie, storage, token, raw provider, or full endpoint data.
  </acceptance_criteria>
  <done>p13-66 is either a fresh local terminal carrier for the independent remote authorization gate or an immutable local checkpoint.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root IAB -> local Dashboard | A real signed browser session exposes only non-sensitive tuple observations to repository-owned evidence logic. |
| local handoff -> D1/API/Gateway | The controlled fixture crosses into stateful local data surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|-----------------|
| T-13-66-01 | Spoofing | root-IAB session readiness | high | mitigate | Require a root-owned claim on the canonical Movies route with real signed-in and Movies-surface facts before allocation; a child binding failure cannot substitute for that proof. |
| T-13-66-02 | Tampering | p13-66 local evidence | high | mitigate | Use the fixed-root handoff reservation, one exact target/run tuple, repository evidence validation, and freeze after the first checkpoint. |
| T-13-66-03 | Information disclosure | session and evidence boundary | high | mitigate | Allow only status/code/itemId from the root adapter; prohibit cookie, storage, token, raw endpoint, profile, and provider material in output or tracked artifacts. |
| T-13-66-04 | Elevation of privilege | Dashboard observation | medium | mitigate | Preserve the real Dashboard session/role boundary and treat auth or tuple failure as a checkpoint, never as a client-side pass. |
| T-13-66-05 | Denial of service | fixture execution | medium | mitigate | Enforce D-01/D-02's single deterministic non-R18 item and one handoff reservation; no broad crawler or repeated carrier. |
</threat_model>

<verification>
- Confirm the run id is absent on the pre-allocation checkpoint path and has the p13-66 format after allocation.
- Run the exact local verifier after the required observation; accept only its recorded terminal or checkpoint exit category and never initiate a follow-up attempt.
- Confirm the summary names the root-IAB ownership boundary, canonical local routes, tuple result, and frozen-history preservation without sensitive material.
</verification>

<success_criteria>
- A new p13-66 run exists only after root-owned signed local IAB readiness.
- The one-item local carrier is terminal with `provesExternalChain: true`, or its single checkpoint leaves no downstream remote path.
- 13-63's child-scoped observation conclusion and 13-64/13-65's absent-p13-63 dependency are superseded for execution while their historical documents remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md` when done.
</output>
