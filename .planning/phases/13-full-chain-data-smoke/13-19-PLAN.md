---
phase: 13-full-chain-data-smoke
plan: "19"
type: execute
wave: 19
depends_on: [13-18, 13-22]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.attempt"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md"
  - ".planning/phases/13-full-chain-data-smoke/13-19-SUMMARY.md"
autonomous: true
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Authorized starye-org selected-target context
    why: The one bounded remote fixture requires the existing explicit Cloudflare/crawler credentials after live ownership checks.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: Authorized selected-target operator process
      - name: CLOUDFLARE_ACCOUNT_ID
        source: Authorized selected-target operator process
      - name: CRAWLER_SECRET
        source: Authorized selected-target operator process
must_haves:
  truths:
    - "D-03/D-05: exactly one `run_id` is extracted and validated from 13-17-SUMMARY.md before any path or command is derived; 13-22 still reports released fresh-runtime eligibility, the exact local pair for that run is terminal and reverified, and the 13-17 Summary proves remote-runner, mutation-entry, handoff collision, and CLI-process focused tests passed before the unique p13-17 run-id allocation."
    - "Before the sole handoff, exact `Test-Path -LiteralPath` checks require expected remote JSON, Markdown, and `remote.attempt` all absent; any peer or marker presence is a read-only terminal stop with no Summary/write, no alternate run ID, and zero handoff/preflight/provider/fixture/D1/API/browser invocations."
    - "Only after all recorded pre-allocation suites and the three-path absence gate, 13-19 invokes `pnpm smoke:data-chain:handoff -- --mode remote ...` exactly once. The handoff itself atomically reserves `remote.attempt`, invokes the exact official outer read-only live preflight once, then either persists a redacted checkpoint or immediately enters the sole remote runner; 13-19 issues no separate preflight or runner command."
    - "D-05: credential/provider/live-check failure after reservation persists and pair-verifies `pre_ingest/remote_preflight/target_preflight_unmet` with itemId null, no raw output/path/secret, and zero runner/fixture/D1/API/browser calls. The marker remains, handoff returns binary 1, Summary records the checkpoint, and the run ends permanently."
    - "D-01/D-02/D-04: the remote handoff reuses local target/run/code, executes one closed fixture, requires one remote D1 row, and establishes its non-empty remote itemId from the remote snapshot independently of the local artifact; coincidental cross-mode value equality is allowed."
    - "Remote selected-canonical API proof matches the remote D1 code/id before any production browser action."
    - "Machine JSON and exact remote peers certify either the closed preflight/downstream checkpoint or resolved_pending_observation/pending; `remote.attempt` records ownership but never success, and only exact pending may authorize production IAB. Any checkpoint/reservation/provider/D1/API/tuple/crash failure permanently closes the run."
    - "No ad hoc provider/SQL/Wrangler/crawler/deploy/DNS/resource/schema/migration/cleanup command supplements the fixed preflight and handoff."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.attempt"
      provides: "Zero-byte exclusive remote attempt ownership marker; persistent after pending/checkpoint/failure/crash, untracked, and never verifier success"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json"
      provides: "Provider-backed fixture/D1/API pending tuple or immutable remote checkpoint"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md"
      provides: "Deterministic remote peer"
    - path: ".planning/phases/13-full-chain-data-smoke/13-19-SUMMARY.md"
      provides: "Eligible-attempt-only local prerequisite, three-path absence, one handoff invocation, internal reservation/preflight/runner counts, exact redacted checkpoint or eligible pending verdict; pre-existence/race loss creates no provider-attempt Summary"
  key_links:
    - from: "13-18 terminal local pair"
      to: "sole remote handoff"
      via: "artifact-only terminal verifier, exact target/run/code, 13-17 pre-allocation regressions, and three-path absence"
      pattern: "terminal_passed"
    - from: "exact p13-17 remote.json, remote.md, and remote.attempt LiteralPaths"
      to: "sole remote handoff"
      via: "all three `Test-Path -LiteralPath` results must be false; any true result terminates read-only without changing run ID or invoking handoff"
      pattern: "Test-Path -LiteralPath"
    - from: "remote handoff"
      to: "remote.attempt -> official live preflight -> checkpoint or provider runner"
      via: "atomic `wx` reservation first; unmet persists/verifies redacted D-05 pair with zero provider calls, passed immediately invokes the sole runner whose internal gates remain"
      pattern: "attempt_already_reserved|attempt_reservation_unavailable"
    - from: "remote_fixture_snapshot"
      to: "selected-canonical API receipt"
      via: "remote D1-owned itemId and count one"
      pattern: "itemCount"
    - from: "13-17-SUMMARY.md run_id"
      to: "remote handoff, verifier, and LiteralPaths"
      via: "PowerShell validates one canonical p13-17 UUID then derives all remote arguments and paths from it"
      pattern: "^run_id:\\s*p13-17-[0-9a-f]{32}$"
---

<objective>
After exact terminal local proof, reject any remote pair or `remote.attempt` presence for the unique p13-17 run, then invoke one handoff that owns atomic reservation, the exact official selected-target live preflight, D-05 checkpoint persistence, and the sole closed fixture/D1/API runner path (D-01, D-02, D-04, D-05, D-06).

Purpose: Phase 13 still lacks provenance-valid provider D1/API evidence. This plan obtains it without browser action and without any supplemental mutation path.

Output: one persistent zero-byte `remote.attempt` plus either a read-only-verified redacted remote-preflight/downstream checkpoint pair or a remote pending JSON/Markdown pair with a remote D1-owned itemId, plus an accurate Summary for the owned handoff outcome. Pre-existence/reservation loss remains a closed non-owner refusal with no retry or provider claim.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md
@scripts/target-profile.ts
@scripts/data-chain-handoff.ts
@scripts/data-chain-smoke.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/mutation-entry.ts
</context>

<discovery_evidence>
- Discovery level: 0. Remote preflight, closed fixture/snapshot entries, exact local eligibility, selected-canonical API check, evidence schema, and handoff wrapper already exist.
- Remote D1 establishes its own itemId. Cross-mode numeric itemId equality is neither required nor forbidden; copying or sourcing the remote itemId from the local artifact is a provenance failure.
- Missing provider access or any live-check issue is an honest non-success gate that the handoff must persist as the D-05 pair after reservation, never permission to issue diagnostic mutation commands.
- Browser action is structurally deferred to 13-20 and requires the exact remote pending tuple.
- The runner's ordinary writer can overwrite existing peers; therefore this task proves exact remote JSON, Markdown, and `remote.attempt` absent before the sole handoff. Plan 13-14 repeats pair validation, atomically creates `remote.attempt`, runs the outer official preflight, writes/verifies its own redacted failure pair, and only on success enters the runner. Marker-only is a terminal prior/crashed attempt and never permission to retry.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Order |
|---|---|---|---|
| 1 | 13-17 pre-allocation test record, 13-18 terminal local pair, exact remote pair+marker absence, and authorized selected-target process | One handoff-owned persistent marker plus read-only-verified redacted checkpoint or eligible pending pair | Only task; any pre-existence/reservation loss/checkpoint/non-pending blocks 13-20 |
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Invoke one remote handoff that owns preflight and runner</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.attempt, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/remote.md, .planning/phases/13-full-chain-data-smoke/13-19-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/STATE.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
    - scripts/target-profile.ts
    - scripts/data-chain-handoff.ts
    - scripts/data-chain-smoke.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/mutation-entry.ts
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.json
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.md
  </read_first>
  <action>First require 13-22-SUMMARY to retain runtime_eligibility: released for the fresh task-owned canonical Gateway runtime; historical ownership checkpoints are not permanent remote refusals. Run the Summary-to-PowerShell binding before any evidence-path lookup: read 13-17-SUMMARY.md once, require exactly one standalone `run_id: p13-17-[0-9a-f]{32}` match, reject zero/multiple/malformed matches, assign it to `$runId`, and derive all local/remote LiteralPaths only from fixed evidence root, target `starye-org`, and `$runId`. Never consume an inline UUID, a placeholder, an environment value, or a manually typed evidence path. Then require the same Summary to record green remote runner, mutation-entry, dedicated handoff-parser/target-first ordering, exact internal path-bundle/non-redirection, pair-state, outer-preflight checkpoint, atomic reservation, concurrency/crash/marker-only, and actual-root CLI process suites from before the unique allocation; absence/failure stops without provider access or writes. Run the direct exact local verifier with `--run-id $runId` and require raw 0, `terminal_passed`, `provesExternalChain:true`, and exact target/run/code. Resolve the derived expected remote JSON, Markdown, and marker LiteralPaths once and require all three `Test-Path -LiteralPath` results false. Any true result stops read-only with no Summary/write/alternate ID and zero handoff/preflight/provider/fixture/D1/API/browser calls. Only after all gates pass, invoke exactly `pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id $runId` once. Do not run `pnpm target-profile preflight`, remote `smoke:data-chain`, Wrangler, prepared-entry, crawler, D1, API, deploy, DNS, cleanup, rollback, or any other provider command separately. Inside the handoff, require the fixed sequence: revalidate pair state; exclusive-create zero-byte `remote.attempt`; invoke the exact official selected-target/environment read-only live preflight once; if unmet, write the internally derived JSON/Markdown pair as `pre_ingest/checkpoint`, observation `remote_preflight`, checkpoint `target_preflight_unmet`, `itemId:null`, verify that pair read-only once, emit only allowlisted `preflightStatus:unmet` and `runnerInvocations:0`, and return binary 1; if passed, immediately invoke the sole remote runner, preserving its own internal defensive gates, then artifact-verify the produced pair once. On outer-preflight failure require fixture/D1/API/browser/provider mutation counts zero and no stderr/path/secret persistence. If checkpoint-pair write/parity/verification fails, keep `remote.attempt`, return 1, do not retry, and do not create a Summary that implies a verified pair. On green preflight, the runner may produce only an exact pending pair or a closed checkpoint; any checkpoint ends the run permanently. After the one handoff, run only the artifact-focused exact verifier with `$runId`: for handoff 0 require raw verifier 2 with exact `resolved_pending_observation/pending`, non-empty remote D1-owned itemId, `provesExternalChain:false`, and no browser receipt; for handoff 1 with a complete pair require raw verifier 2 with exact `pre_ingest` or closed checkpoint, matching allowlisted checkpoint, and `provesExternalChain:false`. Never rerun preflight/handoff/runner/provider. A pre-existing/concurrent reservation loser or marker-only/write-failure state remains non-success, receives no alternate ID/retry, and cannot authorize a provider-attempt Summary. Require the owned marker to remain zero-byte and `??` untracked. Create `13-19-SUMMARY.md` only after an exact pair is artifact-verified; its outcome must be either the redacted checkpoint (including preflightStatus unmet, runnerInvocations zero, itemId null, provider fixture/D1/API/browser zero, permanent stop) or eligible pending (preflightStatus passed, runnerInvocations one, remote snapshot/API tuple, 13-20 eligible). Any checkpoint blocks root IAB permanently. Compare the pre-existing baseline, keep marker/pair untracked and excluded from commits, run GitNexus detect-changes immediately before committing only the Summary, and stop on any code symbol/flow impact.</action>
  <action_addendum>Decision trace for the sole remote handoff: D-01/D-02 keep the provider mutation to one deterministic closed fixture; D-03 requires released 13-22 fresh runtime and terminal local proof before this plan; D-04 reuses the exact target/run/code while remote D1 owns its own item ID; D-05 requires explicit selected-target live preflight and a redacted checkpoint on any unavailable credential/ownership result; D-06 keeps marker/pair output immutable and non-secret; D-07 requires real provider evidence here rather than a static or simulated substitute. Any blocked 13-22 runtime result or 13-17/18 non-pending result stops before the remote handoff.</action_addendum>
  <acceptance_criteria>
    - The 13-17 Summary proves dedicated-parser target-first/fixed-root rejection, exact internal bundle/non-redirection, six pair states, remote-preflight checkpoint, atomic reservation/concurrency/crash/marker non-success, remote runner/mutation-entry, and root process suites passed before run allocation.
    - Exact local terminal verification passes and remote JSON, Markdown, and marker LiteralPaths are all absent before the one handoff; any pre-existence stops read-only with zero handoff/preflight/provider/browser calls and no Summary/alternate ID.
    - 13-19 invokes only one remote handoff command; it invokes no separate official preflight, runner, Wrangler, prepared entry, crawler, D1, API, browser, or supplemental provider command.
    - Handoff reserves zero-byte `remote.attempt` before its outer official selected-target live preflight.
    - Outer preflight unmet produces an exact verified redacted `pre_ingest/remote_preflight/target_preflight_unmet` pair with itemId null, binary handoff 1, runnerInvocations zero, and zero fixture/D1/API/browser/provider mutation; no raw output/path/secret persists.
    - A preflight checkpoint write/parity/verification failure leaves marker-only/non-success state, performs no retry, and cannot create a verified-outcome Summary or authorize 13-20.
    - Outer preflight passed immediately invokes the sole remote runner while retaining its internal defensive gates; the runner may produce exact pending or a closed checkpoint only.
    - After handoff, verification is pair-focused/artifact-only exactly once. Exact checkpoint blocks the run permanently; exact pending with remote-snapshot-owned itemId is the sole 13-20 eligibility.
    - Summary, when an exact pair verifies, records either the allowlisted checkpoint/zero-provider facts or the eligible pending tuple; narration/marker/outer exit alone is never authoritative.
    - Prior evidence is baseline-identical/untracked; new marker/pair remain `??`, only Summary is committed, and task-local GitNexus detects zero code symbol/flow impact.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$summaryPath = '.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md'; $matches = @([regex]::Matches((Get-Content -Raw -LiteralPath $summaryPath), '(?m)^run_id:\s*(p13-17-[0-9a-f]{32})\s*$')); if ($matches.Count -ne 1) { throw 'Expected exactly one canonical run_id in 13-17 Summary.' }; $runId = $matches[0].Groups[1].Value; $output = @(pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId); $code = $LASTEXITCODE; $lines = @($output | Where-Object { $_ -match '^\s*\{.*\}\s*$' }); if ($code -ne 2 -or $lines.Count -ne 1) { throw 'Expected one exact non-terminal artifact result with raw exit 2.' }; $machine = $lines[0] | ConvertFrom-Json; if ($machine.mode -ne 'remote' -or $machine.target -ne 'starye-org' -or $machine.runId -ne $runId -or [bool]$machine.provesExternalChain -ne $false) { throw 'Exact remote artifact tuple is invalid for the canonical run_id.' }; $pending = $machine.state -eq 'resolved_pending_observation' -and $machine.aggregate -eq 'pending' -and $machine.outcome -eq 'pending' -and $null -eq $machine.checkpoint; $checkpoint = $machine.state -eq 'pre_ingest' -and $machine.aggregate -eq 'checkpoint' -and $machine.outcome -eq 'checkpoint' -and $machine.checkpoint -eq 'target_preflight_unmet'; if (-not ($pending -xor $checkpoint)) { throw 'Expected exactly one eligible pending or redacted preflight checkpoint outcome.' }; exit 0"</automated>
  </verify>
  <done>The sole handoff owns reservation and official preflight, then yields either a verified redacted D-05 checkpoint that permanently stops with zero provider mutation or an exact provider-backed pending tuple eligible for root production IAB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Terminal local pair -> provider eligibility | Only exact terminal local target/run/code may authorize remote work. |
| Exact remote peer/marker paths -> provider access | JSON, Markdown, and `remote.attempt` must all be absent; any pair/marker pre-existence ends read-only before preflight or handoff. |
| Authorized process -> sole handoff -> reservation -> live preflight -> checkpoint/runner | Credentials stay process-only; handoff owns exact ordering and an unmet live gate must become redacted pair evidence before stopping. |
| Closed fixture -> remote D1/API tuple | Remote D1, not local evidence, owns the remote itemId. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-91 | Spoofing | remote local prerequisite | critical | mitigate | Exact terminal local verifier and shared target/run/code. |
| T-13-92 | Elevation of Privilege | provider mutation | critical | mitigate | One explicit-target handoff reserves first, invokes exact official live preflight internally, and only a passed status enters the fixed registry runner; no external/supplemental command. |
| T-13-93 | Tampering | remote identity | critical | mitigate | Count-one remote snapshot establishes the mode-owned id; copying identity from local evidence is rejected while coincidental value equality is allowed. |
| T-13-94 | Information Disclosure | provider process/output and preflight checkpoint | critical | mitigate | Process-only secrets, status-only preflight result, and allowlisted checkpoint/pending evidence; no raw stderr/path/provider output/endpoints/context. |
| T-13-95 | Repudiation | pair/marker collision, missing preflight evidence, concurrent/crashed reservation, or failed provider attempt | critical | mitigate | Exact three-path gate precedes one handoff; owned preflight failure writes+verifies the D-05 pair with zero runner/provider calls, while loser/write failure keeps marker-only non-success. No alternate run/retry/success claim exists. |
| T-13-SC | Tampering | package supply chain | low | accept | No install or dependency action. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Selected-production full chain. | Task 1 D1/API; 13-20 browser/terminal. | COVERED | Pending is not terminal. |
| REQ | DATA-01/DATA-02 | Local prerequisite. | 13-17/13-18 | COVERED | Reverified here. |
| REQ | DATA-03 | One selected-target fixture. | Task 1 | COVERED | Closed one-item child. |
| REQ | DATA-04 | Remote D1/API/admin. | Task 1 D1/API; 13-20 admin | COVERED | Browser remains. |
| REQ | DATA-05/DATA-06 | Production Dashboard/viewer. | 13-20 | COVERED | No browser here. |
| REQ | DATA-07/TEST-05 | Remote exact immutable artifacts/output. | 13-17 pre-allocation suite, Task 1, and 13-20 | COVERED | Pair/reservation/concurrency/crash regressions and exact three-path absence gate precede the single live attempt; persistent marker alone is non-success. |
| RESEARCH | - | Official live gate, redacted failure evidence, and closed mutation registry. | Task 1 | COVERED | Sole handoff owns all three; no free-form/external preflight command. |
| CONTEXT | D-01/D-02 | One item, bounded fixture. | Task 1 | COVERED | Remote own id. |
| CONTEXT | D-03/D-04 | Terminal local then exact remote tuple. | Task 1 | COVERED | Strict prerequisite. |
| CONTEXT | D-05 | Explicit live ownership checks and checkpoint evidence on unavailable credentials/provider access. | Task 1 | COVERED | Pair+marker absence precedes one handoff; reservation then official preflight either writes/verifies redacted target_preflight_unmet with zero provider mutation or immediately enters the runner. |
| CONTEXT | D-06 | Non-secret immutable evidence. | Task 1 | COVERED | Zero-byte untracked marker plus closed reservation refusals; marker-only never proves provider success. |
| CONTEXT | D-07 | Real provider work in Phase 13. | Task 1 | COVERED | Actual bounded path. |
| VERIFICATION | G-1 | Fresh terminal local missing. | 13-17/18 prerequisite | COVERED | Must pass. |
| VERIFICATION | G-2 | Provider D1/API/admin missing. | Task 1 + 13-20 | COVERED | D1/API here. |
| VERIFICATION | G-3 | Production browser/terminal missing. | 13-20 | COVERED | Strict dependency. |
| VERIFICATION | G-4 | Wrapper mismatch. | Consumes 13-14 | COVERED | One binary remote handoff; artifact-only post-check distinguishes checkpoint from pending. |

Phase 14 deferred scope remains excluded.
</source_audit>

<verification>
- Require 13-17's pre-allocation remote/mutation/handoff parser/path/preflight/reservation suites and exact terminal local verification before any remote action.
- Require exact remote JSON, Markdown, and marker LiteralPaths all absent. Any pre-existence stops read-only with no handoff, Summary, alternate ID, or provider/browser call.
- Invoke the remote handoff exactly once and issue no separate live preflight, remote runner, prepared entry, Wrangler, crawler, D1, API, or supplemental provider command.
- Inside handoff require pair recheck -> atomic zero-byte marker -> one official status-only live preflight -> exact verified checkpoint or immediate sole runner with its internal defensive gates intact.
- On outer preflight unmet, require binary handoff 1, exact `pre_ingest/remote_preflight/target_preflight_unmet`, itemId null, runnerInvocations zero, zero fixture/D1/API/browser/provider mutation, no raw output/path/secret, and permanent no-retry stop.
- On checkpoint pair write/parity/verifier failure, require marker persistence and no Summary that claims a verified outcome. Marker-only remains non-success.
- On preflight passed, require one runner and one artifact-only verifier. Exact downstream checkpoint permanently stops; exact pending with remote D1-owned id/API match is the only 13-20 eligibility.
- After the sole handoff, run only the pair-focused artifact verifier once; never repeat preflight/handoff/runner/provider. Summary records either verified redacted checkpoint or eligible pending.
- Run GitNexus detect-changes immediately before Summary commit; marker/evidence remains untracked/excluded and any code symbol/flow impact blocks commit.
- Schema/migrations: N/A. No schema push, migration, deploy, DNS/resource, cleanup, rollback, or supplemental provider action.
</verification>

<success_criteria>
- Terminal local proof and exact remote three-path absence precede one and only one remote handoff.
- Handoff, not 13-19 shell sequencing, owns atomic reservation, exact official outer live preflight, D-05 checkpoint persistence, and the sole remote runner.
- Unmet preflight yields an exact read-only-verified redacted checkpoint pair with itemId null and zero provider mutation; write/verify failure retains marker-only non-success. Both end permanently without retry.
- Green preflight immediately enters the sole still-defensive runner; one closed fixture and count-one remote snapshot may yield an API-matching pending itemId.
- Post-handoff verification is artifact-only. Only exact pending authorizes 13-20/root IAB; every checkpoint/refusal/crash blocks it.
- Summary contains only checkpoint or eligible pending facts, all marker/evidence stays untracked/immutable, and only Summary may be committed.
</success_criteria>

## Artifacts this phase produces

- **New/modified code symbols:** none.
- **CLI consumed:** exact local verifier, one `smoke:data-chain:handoff --mode remote`, then one artifact-only remote verifier; no separate preflight or runner CLI.
- **Machine fields consumed:** handoff/verifier tuple/state fields, closed `preflightStatus: passed|unmet|not_applicable`, `runnerInvocations: 0|1`, `target_preflight_unmet`, and closed pair/reservation refusals; no free-form/provider-output field.
- **Evidence paths:** exact new-run zero-byte `remote.attempt` plus remote JSON/Markdown remain untracked. Marker is persistent ownership history and never exact-verifier success.
- **Tracked path:** `13-19-SUMMARY.md` only after an owned exact pair verifies, recording either the redacted checkpoint or eligible pending result; collision/marker-only write failure cannot claim a verified outcome.
- **Provider mutation:** zero on outer preflight unmet; otherwise one existing closed fixture upsert only. No resource creation/configuration.
- **Schema/migrations:** N/A; no schema push.

<output>
Only after the three-path absence gate and sole handoff, create `.planning/phases/13-full-chain-data-smoke/13-19-SUMMARY.md` when the exact pair artifact-verifies. Record local prerequisite, absence proof, one handoff, internal atomic reservation, official preflight status/count, either redacted D-05 checkpoint with itemId null/runner and provider counts zero or eligible pending D1/API tuple, artifact-only post-check, permanent stop/13-20 eligibility, baseline, and task-local GitNexus result. Any pre-existing marker/pair, reservation loss, or marker-only write/verify failure creates no verified-outcome Summary. Do not include secrets/endpoints or commit marker/evidence.
</output>
