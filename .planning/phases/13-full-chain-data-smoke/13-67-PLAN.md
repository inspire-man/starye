---
phase: 13-full-chain-data-smoke
plan: "67"
type: execute
wave: 61
depends_on: [13-66]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Explicit selected-production remote handoff authorization
    why: Remote mutation is eligible only after a new p13-66 local terminal carrier. The authorization phrase is bound to its real run id and is not implied by a GSD execution command or historical authorization.
must_haves:
  truths:
    - "Remote execution is eligible only when p13-66 local evidence is terminal_passed with provesExternalChain true and a non-empty local tuple."
    - "The exact authorization phrase `授权 remote handoff <actual-p13-66-run-id>` is a new, independent human gate; `$gsd-execute-phase`, `继续`, old authorization, and a constructed run id do not satisfy it."
    - "One authorized remote handoff runs the nested official selected-target preflight and either creates one new pending remote tuple with a non-empty remote itemId or freezes the p13-66 remote mode at its first checkpoint."
    - "No production browser observation occurs in this plan, and p13-55, p13-57, p13-60, p13-63, and every older carrier/evidence tree remain untouched."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
      provides: Non-secret p13-66 local eligibility, exact authorization, nested preflight, handoff, and remote pending/checkpoint status.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-66-run-id}/remote.json
      provides: Untracked, selected-target remote evidence only when the single authorized handoff reaches a tuple-bound pending state.
  key_links:
    - from: p13-66 local terminal evidence
      to: remote handoff authorization
      via: exact run-id validation plus independently supplied authorization phrase
      pattern: terminal_passed and provesExternalChain true
    - from: nested selected-target preflight
      to: one remote fixture handoff
      via: repository-owned handoff core with a passed preflight status
      pattern: runnerInvocations equals 1 and remote itemId is non-empty
---

<objective>
Perform at most one newly authorized selected-production handoff for the fresh p13-66 local terminal carrier, producing a remote pending tuple with a non-empty itemId or one permanent checkpoint.

Purpose: This closes the first current `13-VERIFICATION.md` gap: a fresh local terminal run followed by a green nested handoff preflight and one authorized pending remote pair. It supersedes the executable intent of 13-61, which is bound to frozen p13-60, and 13-64, which is bound to a p13-63 carrier that does not exist.

Output: `13-67-SUMMARY.md` and conditional untracked p13-66 remote evidence. Production Dashboard and Viewer work remains exclusively in Plan 13-68.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-56-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-60-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-63-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-64-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt
@scripts/data-chain-handoff.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Validate the fresh local terminal and obtain run-bound remote authorization</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md</files>
  <action>
Read the p13-66 summary, run-id file, and exact local verifier result before presenting the gate. Accept remote authority only when they establish a fresh `terminal_passed` local tuple with `provesExternalChain: true`, deterministic code, and non-empty local itemId. Record the authorization decision without secret material. The current GSD invocation, `继续`, old authorization, and an anticipated run id are not authorization for this task.
  </action>
  <what-built>Plan 13-66 has produced the only eligible local carrier candidate for remote execution.</what-built>
  <how-to-verify>
1. Read `13-66-SUMMARY.md`, `13-66-RUN-ID.txt`, and the exact local verifier result.
2. Confirm the actual p13-66 run reports `terminal_passed`, `provesExternalChain: true`, a deterministic code, and a non-empty local itemId.
3. Supply exactly `授权 remote handoff <actual-p13-66-run-id>` using the real run id from the run-id file.
4. Do not treat the current GSD execution invocation, `继续`, any previous p13-55/p13-57/p13-60 phrase, or an anticipated p13-66 value as authorization.
  </how-to-verify>
  <resume-signal>Type the exact authorization phrase for the real p13-66 run id, or describe the local terminal/authorization mismatch.</resume-signal>
  <verify>
    <automated>Test-Path -LiteralPath .planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; exit 0</automated>
  </verify>
  <done>The authorization gate has either received the exact phrase for the proven p13-66 run or prevented every remote invocation.</done>
</task>

<task type="auto">
  <name>Task 2: Run the sole authorized nested-preflight remote handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
  </read_first>
  <action>
Proceed only after Task 1 supplies the exact phrase bound to the existing p13-66 run id. Assert that no p13-66 remote evidence pair or `remote.attempt` exists. Invoke exactly one `pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id <p13-66>`; its repository-owned handoff core must execute the nested official selected-target preflight before any remote fixture work. A pending result is valid only with `preflightStatus: passed`, `runnerInvocations: 1`, the target/run-derived one-item code, one non-R18 item, and a non-empty remote itemId. Immediately run the exact remote verifier once to record that pending state. Any authorization mismatch, preflight, provider, tuple, handoff, or verifier failure freezes p13-66 remote evidence on its first checkpoint with no second preflight, handoff, verifier, carrier, or browser observation. Record authorization status and non-secret terminal fields only. This implements D-01, D-02, D-04, D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>$runId = (Get-Content -Raw -LiteralPath .planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt).Trim(); pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId; if ($LASTEXITCODE -notin 0, 2) { exit $LASTEXITCODE }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - The only remote handoff follows both a terminal local p13-66 pair and the exact new authorization phrase.
    - A pending remote result contains a non-empty remote itemId and received the nested green preflight; all other results remain frozen non-success evidence.
    - No production browser route is opened and no historical carrier/evidence tree is operated.
  </acceptance_criteria>
  <done>p13-66 has one remote pending tuple for Plan 13-68, or one durable remote checkpoint blocks it.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human authorization -> remote handoff | A human decision permits one stateful selected-target operation for one exact local terminal carrier. |
| local terminal evidence -> provider operation | The remote runner consumes local tuple evidence before target preflight and fixture execution. |
| provider command -> untracked evidence | Remote facts cross back into a non-secret evidence pair and summary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|-----------------|
| T-13-67-01 | Spoofing | remote authorization | critical | mitigate | Require the literal authorization phrase with the real p13-66 run id after confirming its terminal local evidence; reject inferred, historical, and command-derived approval. |
| T-13-67-02 | Tampering | target/run and remote evidence | high | mitigate | Use explicit `starye-org`, fixed handoff paths, one atomic remote reservation, exact verifier, and a freeze on the first non-success result. |
| T-13-67-03 | Elevation of privilege | provider mutation | critical | mitigate | Delegate preflight to the repository-owned nested handoff before its fixed remote fixture and require passed account/resource/read-only checks. |
| T-13-67-04 | Information disclosure | provider and session data | high | mitigate | Summary records only authorization decision, status categories, code, and itemId presence; secrets, tokens, raw responses, and endpoints stay out of tracked output. |
| T-13-67-05 | Denial of service | remote fixture | medium | mitigate | Permit exactly one deterministic non-R18 item and one handoff for p13-66; no broad crawl, retry, or additional carrier. |
</threat_model>

<verification>
- Verify p13-66 local terminal state before accepting authorization.
- Verify the remote result with the exact p13-66 run id and accept only its recorded pending or checkpoint state.
- Confirm no production browser receipt, historical-run operation, sensitive output, or second remote attempt is present.
</verification>

<success_criteria>
- A fresh terminal local carrier receives a distinct, exact-run remote authorization before provider-side work.
- The p13-66 remote pair is a new pending tuple with a non-empty itemId and green nested preflight, or it is permanently checkpointed without retry.
- Plans 13-61 and 13-64 remain historical/superseded contracts and do not direct execution.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md` when done.
</output>
