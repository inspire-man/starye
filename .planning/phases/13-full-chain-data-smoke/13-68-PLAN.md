---
phase: 13-full-chain-data-smoke
plan: "68"
type: execute
wave: 62
depends_on: [13-67]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
  - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
autonomous: false
gap_closure: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Signed selected-production Dashboard session in the root agent's in-app Browser
    why: The root IAB must prove a real session and Movies surface on the selected target after the fresh p13-66 remote pending tuple exists. Localhost session state is not production session proof.
must_haves:
  truths:
    - "Production observation begins only when 13-67 records a new p13-66 remote pending tuple with a non-empty remote itemId and the root IAB separately proves a signed selected-production Movies surface."
    - "The root IAB observes the selected TargetProfile canonical Dashboard before its canonical Viewer, and both surfaces correlate the exact p13-66 remote code and remote itemId."
    - "The only terminal production result is an exact remote verifier outcome of terminal_passed with provesExternalChain true; any non-success observation or verification freezes p13-66 without retry."
    - "13-VERIFICATION.md is refreshed exclusively from the p13-66 live outcome and its two current gap truths; it never uses localhost state as production proof or changes Phase 14 scope."
    - "p13-55, p13-57, p13-60, p13-63, p13-64, p13-65, and all earlier evidence remain historical and untouched."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
      provides: Non-secret selected-production root-IAB readiness, ordered observation, tuple correlation, and final remote verifier status.
    - path: .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
      provides: The current Phase 13 production-pending and production-UI gap status derived from p13-66 evidence only.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-66-run-id}/remote.json
      provides: Untracked selected-target terminal or frozen checkpoint evidence for the fresh remote tuple.
  key_links:
    - from: p13-67 remote pending tuple
      to: root-owned production IAB readiness
      via: non-empty remote itemId and canonical selected-target Dashboard session proof
      pattern: resolved_pending_observation
    - from: ordered production observations
      to: Phase 13 verification status
      via: exact remote verifier plus p13-66 summary; local evidence is not an input to production success
      pattern: terminal_passed and provesExternalChain true
---

<objective>
Use the root agent's in-app Browser to observe one new p13-66 remote pending tuple on the selected production canonical Dashboard then Viewer routes, freeze any checkpoint, and update the Phase 13 verifier only from those live facts.

Purpose: This closes the second current `13-VERIFICATION.md` gap: a fresh pending remote pair with a production-valid signed session and ordered Dashboard-to-Viewer terminal evidence. It supersedes the executable production-observation intent of 13-62 and 13-65 because their p13-60 and p13-63 prerequisite carriers are frozen or absent.

Output: `13-68-SUMMARY.md`, a live-aligned `13-VERIFICATION.md`, and untracked p13-66 remote observation evidence. Phase 14 documentation and requirement-matrix work remain out of scope per D-07.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-64-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-65-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
@packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Gate production observation on a fresh remote tuple and root-owned production IAB</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
    - scripts/data-chain-surface-observation.ts
  </read_first>
  <action>
Read the p13-66 remote verifier result and proceed only when 13-67 records `resolved_pending_observation`, the exact p13-66 deterministic code, and a non-empty remote itemId. Then the root agent holding the claimed IAB must navigate the selected TargetProfile canonical production Dashboard route and establish non-sensitive readiness facts: selected-target canonical origin, real signed-in/authorized state, and Movies surface availability. Do not regard `http://localhost:8080` session state as proof for this task, and do not allow a child agent to declare the root IAB unavailable because it lacks the binding. Do not inspect or export cookies, storage, tokens, profile files, raw provider responses, or full endpoints. If the remote pair is missing/checkpointed or the root production session is absent, write the matching non-secret blocked status to `13-68-SUMMARY.md`; do not navigate a production Viewer, observe the remote tuple, or re-run p13-67. This implements D-04, D-05, D-06, and D-07 using the approved production-session and canonical-route UI contract.
  </action>
  <verify>
    <automated>$runId = (Get-Content -Raw -LiteralPath .planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt).Trim(); pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId; if ($LASTEXITCODE -notin 0, 2) { exit $LASTEXITCODE }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - A remote pending tuple and an independently signed production Movies surface are both present before production tuple observation.
    - Localhost session evidence is excluded from the production readiness decision.
    - Ineligible paths leave the fresh remote carrier frozen without additional remote or browser actions.
  </acceptance_criteria>
  <done>The root IAB and p13-66 remote tuple are both eligible for production observation, or the plan stops with an honest blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Root-observe canonical production Dashboard then Viewer</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>
Only the root agent holding the signed selected-production IAB may perform this observation. Invoke the existing `observeDataChainSurfaces` core with an in-memory root-IAB adapter that returns only real navigation `status`, `itemCode`, and `itemId`; the core remains responsible for exact tuple derivation, receipt creation, order, validation, and writes. Resolve the base only from the selected TargetProfile and observe its canonical production Dashboard first, then its canonical Viewer for the p13-66 item code. Require the Dashboard row and Viewer heading to expose the exact p13-66 remote code; correlate the remote itemId through `data-phase13-item-id` or permitted non-secret item metadata rather than requiring it in the Viewer heading. Run one exact remote verifier after the ordered observation. Only `terminal_passed` with `provesExternalChain: true` is production proof. Any IAB, auth, canonical-origin, code, itemId, Viewer, or verifier non-success freezes p13-66 and ends the plan without retry, second observer, second verifier, new handoff, or reuse of historical runs. This implements D-01, D-04, D-05, D-06, and D-07 and applies the UI contract's Dashboard-first identity, route, status, and session boundaries.
  </action>
  <verify>
    <automated>$runId = (Get-Content -Raw -LiteralPath .planning/phases/13-full-chain-data-smoke/13-66-RUN-ID.txt).Trim(); pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id $runId; if ($LASTEXITCODE -notin 0, 2) { exit $LASTEXITCODE }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Production Dashboard is observed before production Viewer on the selected canonical origin only.
    - Terminal success has exact remote code/itemId correlation plus terminal verifier fields; a checkpoint remains a frozen non-success.
    - Root IAB output and evidence stay limited to non-secret tuple/status facts.
  </acceptance_criteria>
  <done>The fresh p13-66 remote tuple is terminally proved in production or permanently checkpointed.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh Phase 13 verification from the p13-66 live result</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md, .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>
Update only the current verification frontmatter, re-verification, gaps, observable-truth evidence, and requirement rows that are affected by the two current `13-VERIFICATION.md` gaps. Derive them strictly from p13-66 local terminal status, p13-66 remote pending/terminal evidence, root-IAB production receipt ordering, and the exact remote verifier. Set `status: passed` only when the fresh remote pair has a non-empty itemId, both selected-production observations are terminal in order, the exact remote verifier is `terminal_passed` with `provesExternalChain: true`, and neither current production gap remains. Otherwise retain `status: gaps_found`, name the precise p13-66 missing prerequisite or checkpoint, and preserve historical p13-55/p13-57/p13-60/p13-63 outcomes as immutable context rather than substitute proof. Do not edit Phase 14 artifacts, RUNBOOK, old-domain cleanup, or the final milestone matrix. This implements D-05, D-06, and D-07 and makes `13-UI-SPEC.md`'s terminal-vs-checkpoint contract observable to the phase verifier.
  </action>
  <verify>
    <automated>$verification = Get-Content -Raw -LiteralPath .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md; if ($verification -notmatch 'p13-66') { throw 'verification must reference the fresh p13-66 carrier' }; if ($verification -notmatch 'status: (passed|gaps_found)') { throw 'verification must declare a supported status' }; if ($verification -match 'http://localhost:8080.*production') { throw 'localhost must not be claimed as production evidence' }</automated>
  </verify>
  <acceptance_criteria>
    - Verification status is based on fresh p13-66 evidence and distinguishes remote pending, terminal, and checkpoint facts.
    - `passed` is unavailable without a real selected-production Dashboard then Viewer terminal pair and exact remote verifier proof.
    - The update remains inside Phase 13 and does not expose sensitive material or recast historical carriers as current proof.
  </acceptance_criteria>
  <done>Phase 13 verification reflects the fresh remote carrier honestly, with no Phase 14 work pulled into the closure.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote pending evidence -> production IAB | A provider-backed tuple gates authenticated browser navigation on a different production session boundary. |
| root production IAB -> evidence core | The root adapter reports only observed tuple/status facts to the repository-owned evidence writer. |
| live result -> verification document | Terminal and checkpoint evidence determines the phase status consumed by later workflow steps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|-----------------|
| T-13-68-01 | Spoofing | production Dashboard session | critical | mitigate | Require a root-owned selected-target canonical session proof after the fresh remote pending tuple exists; local IAB state never qualifies. |
| T-13-68-02 | Tampering | remote browser evidence | high | mitigate | Derive code/id only from p13-66 pending evidence, enforce Dashboard-before-Viewer core ordering, and freeze the remote run after its first non-success. |
| T-13-68-03 | Information disclosure | production IAB and summary | high | mitigate | Limit adapter output and tracked prose to non-secret route class, status, code, itemId presence, and verifier category. |
| T-13-68-04 | Repudiation | Phase 13 completion state | high | mitigate | Refresh `13-VERIFICATION.md` only from exact p13-66 evidence plus verifier output; retain checkpoint details and historical-carrier boundaries. |
| T-13-68-05 | Elevation of privilege | production dashboard path | medium | mitigate | Keep the existing session/role guard, require a real authorized Movies surface, and classify missing access as a frozen checkpoint. |
</threat_model>

<verification>
- Confirm p13-67's remote pair is new, pending, and has a non-empty itemId before production IAB navigation.
- Confirm the root IAB observed canonical selected-production Dashboard before Viewer and the exact remote verifier records terminal or checkpoint state.
- Validate the Phase 13 report names p13-66 and carries no localhost-as-production claim, secret, raw provider response, or Phase 14 mutation.
</verification>

<success_criteria>
- A fresh p13-66 remote pending tuple and independently signed production IAB session gate the only production observation.
- Production success requires exact Dashboard-to-Viewer evidence and `terminal_passed` / `provesExternalChain: true`; checkpoints remain frozen.
- 13-62 and 13-65 are superseded for execution, while their historical plan files remain unchanged and Phase 14 stays out of scope.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md` when done.
</output>
