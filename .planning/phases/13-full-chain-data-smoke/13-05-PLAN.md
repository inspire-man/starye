---
phase: 13-full-chain-data-smoke
plan: "05"
type: execute
wave: 4
depends_on:
  - "13-04"
files_modified:
  - packages/config/src/deployment-target/data-chain-evidence.ts
  - packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
  - packages/crawler/src/smoke/data-chain-fixture.ts
  - packages/crawler/src/smoke/__tests__/data-chain-fixture.test.ts
autonomous: true
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup: []
must_haves:
  truths:
    - "G-01: Every Phase 13 local or remote smoke run derives one deterministic non-R18 movie from its selected target/run tuple and writes it through exactly one ApiClient.syncMovie call."
    - "The fixture has exactly one deterministic non-secret player, no actors or publishers, and the same code remains the D1/API/Dashboard/viewer correlation key."
    - "The evidence contract accepts an item count only for one successfully observed D1 row and rejects sibling-code/batch-shaped state."
  artifacts:
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/crawler/src/smoke/data-chain-fixture.ts
    - packages/crawler/src/smoke/__tests__/data-chain-fixture.test.ts
  key_links:
    - "selected target plus run id -> createDataChainCandidate -> one fixture code -> one ApiClient.syncMovie payload -> later D1 snapshot correlation"
---

<objective>
Restore the canonical one-item smoke contract that the verifier found drifted to ten items, while retaining the existing deterministic target/run identity and non-secret evidence boundary.

Purpose: G-01 is a direct Phase 13 verification failure. The canonical 13-02 plan requires one payload and one sync call; the latest gap-closure instruction requires restoring that contract because no accepted override exists.
Output: one-item identity/evidence contract, one-item crawler fixture, and focused regressions that fail on batch behavior.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-02-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-02-SUMMARY.md
@packages/config/src/deployment-target/data-chain-evidence.ts
@packages/crawler/src/smoke/data-chain-fixture.ts
@packages/crawler/src/utils/api-client.ts
</context>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | Canonical 13-02 one-item contract and existing tuple/evidence types | Single-code candidate and single-row evidence semantics | First; crawler behavior must consume one unambiguous source contract. |
| 2 | Task 1 single-code identity | One fixture and one service-auth sync call | Second; transport behavior is tested against the restored contract. |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore the one-code evidence and correlation contract</name>
  <files>packages/config/src/deployment-target/data-chain-evidence.ts, packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-02-PLAN.md
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
  </read_first>
  <behavior>
    - A candidate created from one explicit target and run has one stable primary code and an item count of one.
    - Successful D1 evidence carries an item count of one only; a count above one, a sibling-code set, or a D1 row for a non-primary code fails validation.
    - Existing redaction, local Gateway/canonical-path validation, tuple matching, and checkpoint lifecycle behavior remain unchanged.
  </behavior>
  <action>Before changing exported functions or constants in `data-chain-evidence.ts`, run GitNexus upstream impact analysis for each modified symbol and report direct callers, affected execution flows, and risk. Warn before editing if a result is HIGH or CRITICAL. Write failing regression tests first. Per the latest verified gap directive, restore the canonical 13-02 one-item contract: replace batch/sibling-code derivation with one target/run-derived primary code and make the item-count type/value one throughout the evidence validator, renderer, and typed observations. Treat the ten-item statement in stale Context decision D-01 as a conflict superseded by the explicit gap-closure instruction and the canonical 13-02 must-have; do not create an override or preserve ten-item compatibility. Preserve D-03/D-04/D-06 gateway-only path rules, non-secret allowlists, immutable tuple validation, and truthful checkpoint states. Do not alter selected-target resolution, preflight, secret handling, artifact root, or any operator environment file.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-evidence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove one target/run maps to exactly one item code and successful D1 evidence records an item count of one.
    - Tests reject a sibling code, a count other than one, a non-primary D1 item, malformed tuple data, a direct application origin, and a secret-bearing evidence key.
    - Existing checkpoint evidence remains valid only as non-success and cannot be promoted without the later required observations.
  </acceptance_criteria>
  <done>The shared Phase 13 identity/evidence layer establishes one item as the only valid smoke correlation tuple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the crawler fixture perform one bounded service-auth upsert</name>
  <files>packages/crawler/src/smoke/data-chain-fixture.ts, packages/crawler/src/smoke/__tests__/data-chain-fixture.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-02-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/crawler/src/smoke/data-chain-fixture.ts
    - packages/crawler/src/smoke/__tests__/data-chain-fixture.test.ts
    - packages/crawler/src/utils/api-client.ts
    - packages/crawler/src/utils/__tests__/api-client.test.ts
  </read_first>
  <behavior>
    - A valid prepared target/run builds one deterministic non-R18 movie fixture with one player and no actor/publisher relations.
    - `runDataChainFixture()` invokes `ApiClient.syncMovie()` once for that one fixture and accepts only the existing upsert acknowledgement contract.
    - Invalid fixture shape, an R18 flag, no player, multiple players, a mismatched code, or an attempted batch fails before transport begins.
  </behavior>
  <action>Before modifying `runDataChainFixture`, `createDataChainFixture`, or `ApiClient.syncMovie`, run GitNexus upstream impact analysis for each exact symbol and report blast radius; warn before continuing if HIGH or CRITICAL. Follow the crawler skill's fixture-first rule: write focused fake-ApiClient tests before implementation and perform no live network/provider run. Remove batch creation/iteration from the bounded smoke adapter and consume the Task 1 primary code. Construct only one non-R18 payload with one deterministic non-secret player and no external crawler source, actors, publishers, corpus traversal, or raw fetch. Call the existing `ApiClient.syncMovie()` once through its service-auth upsert transport, validate acknowledgement, and return only safe operation/status/code/count metadata for the downstream snapshot. Per D-02 and the verified one-item gap, reject array/list injection, multiple payloads, and every malformed fixture before constructing the API client request. Do not change general crawler strategies, API endpoints, source crawlers, or local/remote secrets.</action>
  <verify>
    <automated>pnpm --filter @starye/crawler exec vitest run src/smoke/__tests__/data-chain-fixture.test.ts src/utils/__tests__/api-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The valid regression observes exactly one `syncMovie` invocation with one non-R18 movie, one player, and upsert mode.
    - Every invalid or batch-shaped fixture is rejected before `syncMovie` is called.
    - The returned observation remains non-secret and contains the primary code and a count of one, not an API response dump or provider configuration.
  </acceptance_criteria>
  <done>The only Phase 13 write path is one deterministic non-R18 service-auth fixture upsert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Target/run tuple -> fixture identity | Untrusted caller values must not expand the smoke into multiple records. |
| Fixture adapter -> service-auth sync API | A bounded write crosses into API/D1 and must remain deterministic and non-R18. |
| Evidence observation -> verifier | Serialized item counts and codes must not make a batch look like the canonical single-item smoke. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-20 | Tampering | fixture identity | high | mitigate | Derive one primary code from explicit target/run and reject sibling/batch shapes in contract tests. |
| T-13-21 | Elevation of Privilege | crawler fixture | high | mitigate | Keep the existing service-auth transport and prohibit raw fetch, corpus traversal, and caller-owned payload lists. |
| T-13-22 | Information Disclosure | evidence serialization | high | mitigate | Preserve allowlisted tuple/observation fields and regression-test unexpected key rejection. |
| T-13-23 | Repudiation | smoke result | medium | mitigate | Tie one acknowledged write to one target/run/code correlation tuple and a typed D1 count of one. |
</threat_model>

<verification>
- Run the focused Config and crawler tests above, then `pnpm --filter @starye/config test --run` and `pnpm --filter @starye/crawler test --run`.
- Before committing, run GitNexus `detect_changes`; expected changes are the Phase 13 evidence identity and bounded fixture flow only.
- Do not execute local or remote smoke in this plan; the subsequent plans must first propagate the one-item type through snapshots and runners.
</verification>

<success_criteria>
The verifier can observe one canonical write contract from target/run identity through the existing service-auth adapter, without a ten-item compatibility path or unbounded crawler behavior.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-05-SUMMARY.md` when done.
</output>
