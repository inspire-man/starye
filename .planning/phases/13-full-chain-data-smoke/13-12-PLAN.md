---
phase: 13-full-chain-data-smoke
plan: "12"
type: execute
wave: 11
depends_on: [13-11]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/local.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/local.md"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/remote.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/remote.md"
  - ".planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md"
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Local Starye operator context
    why: The official local projection/preflight, one-item smoke, and authorized Dashboard observation use existing user-managed local secrets and services; values remain outside Git, logs, and evidence.
    env_vars:
      - name: BETTER_AUTH_SECRET
        source: Existing user-managed local API Worker secret store
      - name: CRAWLER_SECRET
        source: Existing user-managed local crawler/API service-auth secret store
      - name: ADMIN_GITHUB_ID
        source: Existing user-managed local Dashboard authorization configuration
    dashboard_config:
      - task: Keep the persistent Codex in-app browser signed in through the local Gateway with Movies management access.
        location: http://localhost:8080/auth/
  - service: Authorized starye-org selected-target context
    why: Remote mutation requires the existing explicit credential-key, account/resource ownership, read-only live-check, and authorized canonical Dashboard boundaries.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: Authorized selected-target operator context
      - name: CLOUDFLARE_ACCOUNT_ID
        source: Authorized selected-target operator context
      - name: CRAWLER_SECRET
        source: Authorized selected-target operator context
    dashboard_config:
      - task: Keep the same persistent Codex in-app browser authorized for Movies access on the repository-resolved selected canonical Gateway.
        location: Repository-resolved selected canonical Gateway
must_haves:
  truths:
    - "D-03: official project-local check, target validation, repaired local preflight, and Plan 13-11 focused regressions are green before any p13-12 run id is generated, collision-checked, or used."
    - "D-01/D-02/D-03/D-04: one newly proven-absent opaque p13-12 run invokes local mode exactly once, establishes one deterministic non-R18 code and one local D1-owned itemId, then records authorized Dashboard before viewer through http://localhost:8080 only."
    - "D-05: only the exact terminal local target/run/code tuple authorizes a fresh official remote live preflight immediately before one remote runner invocation; the closed fixture and count-one D1 snapshot establish the remote itemId."
    - "D-04/D-06/D-07: the same persistent Codex in-app browser records selected-canonical Dashboard before viewer, repository core writes tuple-bound receipts, and exact local and remote verifiers both return terminal_passed with provesExternalChain true."
    - "Any failed p13-12 run is immutable and permanently non-retryable; a checkpoint proves only its recorded non-success state and never provider success, zero side effects, historical immutability, or Phase completion."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/local.json"
      provides: "Fresh local runner receipts plus ordered persistent-IAB Dashboard/viewer receipts for one exact tuple"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/local.md"
      provides: "Deterministic Markdown peer of the exact local JSON artifact"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/remote.json"
      provides: "Provider-backed remote D1/API and selected-canonical Dashboard/viewer receipts for the same target/run/code"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/remote.md"
      provides: "Deterministic Markdown peer of the exact remote JSON artifact"
    - path: ".planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md"
      provides: "Accurate closeout of gates, run allocation, local/remote outcomes, IAB handoffs, exact verifiers, and Phase status"
  key_links:
    - from: "official repaired local gates"
      to: "p13-12 run allocation"
      via: "every command exits 0 before GUID generation and exact Test-Path collision gate"
      pattern: "project-local.*check.*preflight"
    - from: "fresh local pending pair"
      to: "persistent Codex IAB local observation"
      via: "repository core derives exact tuple and enforces Dashboard before viewer through localhost:8080"
      pattern: "resolved_pending_observation.*dashboard.*viewer"
    - from: "terminal fresh local pair"
      to: "remote runner"
      via: "exact target/run/code prerequisite plus immediately preceding official remote live preflight"
      pattern: "terminal_passed.*remote_preflight"
    - from: "remote D1-established tuple"
      to: "selected-canonical Dashboard and viewer"
      via: "repository core plus persistent-IAB observeSurface adapter preserves remote itemId and order"
      pattern: "remote_fixture_snapshot.*dashboard.*viewer"
    - from: "local and remote JSON/Markdown pairs"
      to: "scripts/verify-data-chain-smoke.ts"
      via: "exact schema, parity, lifecycle, receipt, path, order, and tuple verification"
      pattern: "terminal_passed.*provesExternalChain"
  prohibitions:
    - statement: "No p13-12 run id, collision check, evidence path, smoke, browser, or provider action may occur before all official local gates and Plan 13-11 regressions pass."
      status: resolved
      verification: judgment
    - statement: "Chrome, incognito, isolated browser contexts, default Puppeteer, screenshots, caller-supplied status/tuple/order/receipt data, and manual evidence edits must not be used for Dashboard/viewer proof."
      status: resolved
      verification: judgment
    - statement: "No ad hoc Wrangler, SQL, D1, crawler, provider, deploy, permission, schema, migration, cleanup, rollback, or schema-push command may supplement the official closed commands."
      status: resolved
      verification: judgment
    - statement: "Secrets, cookies, headers, browser storage, raw provider context, raw endpoints, prepared context, and direct local application ports must not enter evidence, plans, summaries, or logs."
      status: resolved
      verification: judgment
    - statement: "Attempts A-E, Plan 13-09, Plan 13-10, and every other prior evidence file must not be modified, deleted, copied, reused, staged, or committed."
      status: resolved
      verification: judgment
    - statement: "A checkpoint, missing receipt, or missing verifier success must not be represented as provider success, Phase completion, zero provider side effects, or historical-immutability proof."
      status: resolved
      verification: judgment
---

<objective>
Close all three remaining verifier blockers after Plan 13-11: allocate one fresh collision-gated run only after the repaired official local gates pass, obtain terminal local D1/API/authorized Dashboard/viewer proof, then obtain provider-backed selected-production D1/API/Dashboard/viewer proof and dual terminal exact verification for the same target/run/code correlation (D-01 through D-07).

Purpose: p13-10 is permanently closed at its honest local checkpoint. The repair plan corrects and tests the caller mismatch but intentionally performs no live proof. Phase 13 remains incomplete until a different immutable run demonstrates both local and selected-production outcomes with real repository-owned receipts.

Output: one new `p13-12-*` local/remote JSON+Markdown evidence set and `13-12-SUMMARY.md`. Any checkpoint is the final state of that run and leaves Phase 13 open; only dual terminal exact-verifier results permit canonical phase re-verification.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-09-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-11-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-11-SUMMARY.md
@scripts/target-profile.ts
@scripts/data-chain-smoke.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
</context>

<discovery_evidence>
- Discovery level: 0. This execution plan consumes the repaired established CLIs, closed fixture/snapshot children, strict evidence schema, exact verifier, and accepted persistent-IAB adapter; it introduces no source, dependency, schema, provider resource, or browser technology.
- The exact immutable run `p13-10-a2917dfa94a74108afd2c6c696dfdbb8` is permanently non-retryable. Attempts A-E, 13-09, 13-10, and every other existing evidence path remain read-only.
- The root execution orchestrator owns Codex in-app browser tools. Executor subagents may not expose that surface, so each pending-observation boundary is an explicit orchestrator handoff; absence of the root IAB is a checkpoint, never permission to use Chrome/Puppeteer/incognito or fabricate receipts.
- Local and remote share `targetId`, `runId`, and `itemCode`; each D1 independently establishes its own `itemId`. Dashboard must precede viewer in each mode.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | Completed 13-11 Summary, green official local gates, running local stack | One absent-before-use p13-12 local pair in pending state, an immutable checkpoint, or pre-run gate stop | First and blocking. No IAB or remote work before pending local evidence. |
| 2 | Task 1 exact pending local tuple, persistent authorized root IAB | Ordered local Dashboard/viewer receipts and terminal exact local proof, or immutable browser checkpoint | Second. Remote remains forbidden unless exact local verifier is terminal. |
| 3 | Task 2 exact terminal local pair, fresh authorized remote live preflight, persistent authorized root IAB | Provider-backed remote pair, ordered remote browser receipts, dual terminal verifiers, and accurate Summary | Third. Canonical phase verifier decides final closure. |
</dependency_graph>

<accepted_execution_override date="2026-07-19" surface="codex-in-app-browser">
Use only the persistent Codex in-app browser for local and remote Dashboard/viewer proof. Do not launch Chrome, an incognito or isolated context, the observer CLI's default Puppeteer profile, or a new login browser. If an executor subagent reaches `resolved_pending_observation`, return a blocking handoff to the root execution orchestrator with only mode, target, run id, exact evidence paths, and the repository-derived pending tuple; never manufacture an observation result inside the subagent.

The root orchestrator must read the exact pair, then invoke exported repository core `observeDataChainSurfaces()` with exactly one injected dependency, `observeSurface`. Repository core retains `read`, `write`, `evidenceRoot`, `resolveTarget`, `now`, validation, tuple derivation, canonical base resolution, Dashboard-before-viewer order, receipt construction, deterministic rendering, and persistence. The adapter uses the persistent IAB to navigate to `new URL(input.path, input.baseUrl)`, requires the exact final origin/path, rejects direct application ports, waits for exactly one `[data-phase13-item-id="<input.itemId>"]` marker, and requires the settled DOM to contain both `input.itemCode` and `input.itemId`. It returns the same tuple as `passed` only after those live checks; otherwise it returns `unavailable` so repository core persists a checkpoint. It cannot supply status, tuple, surface order, receipt, cookie/storage state, screenshot-only claim, or artifact content. A plain user approval is not a receipt and cannot resume the plan.
</accepted_execution_override>

<tasks>

<task type="auto">
  <name>Task 1: Gate and invoke one fresh local runner up to the IAB boundary</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.md, .planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/STATE.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-11-SUMMARY.md
    - scripts/target-profile.ts
    - scripts/data-chain-smoke.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/preflight.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  </read_first>
  <action>First confirm `13-11-SUMMARY.md` reports all focused tests/type-checks green and the code-contract commit exists. Before generating or collision-checking any run id, run exactly and in order: `pnpm target-profile project-local --target starye-org --check`, `pnpm target-profile validate --target starye-org`, `pnpm target-profile preflight --target starye-org --scope local --command validate --wrangler-profile starye-org`, then the exact six-file focused Vitest command from Plan 13-11. Every command must exit 0; do not invoke write mode. Any failure stops before run allocation and is recorded only as a non-secret gate result in `13-12-SUMMARY.md`. After all gates pass, capture a session-only SHA256 map of every pre-existing Phase 13 evidence file without writing/staging a manifest; this can prove stability only from that timestamp. Generate one opaque `$runId = 'p13-12-' + [guid]::NewGuid().ToString('N')`. Require `Test-Path -LiteralPath ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/$runId"` to return `False` before using it. A collision discards only the unused candidate string and repeats generation/check; it never touches a directory. Invoke `pnpm smoke:data-chain -- --mode local --target starye-org --run-id &lt;p13-12-run-id&gt;` exactly once. Then invoke the Task 1 PowerShell verification wrapper exactly once; the wrapper captures the exact verifier stdout and exit code, parses its machine JSON plus the exact local JSON artifact, and exits 0 only when verifier exit is 2, state is `resolved_pending_observation`, aggregate/outcome are `pending`, `provesExternalChain` is false, and artifact `itemId` is non-empty. Verifier exit 2 with those assertions is the successful IAB handoff state, not a task failure. It MUST NOT trigger another verifier call, local runner rerun, retry, evidence overwrite, or append. If the wrapper fails any assertion, stop and inspect the existing exact pair without invoking the runner/verifier again. A pre-ingest or other checkpoint is immutable, permanently non-retryable, updates the Summary honestly, and blocks Tasks 2-3. Only the wrapper-certified pending tuple may be handed to the root IAB orchestrator. Do not run a browser or remote command in this task. Keep the new evidence untracked and preserve all existing index/worktree state.</action>
  <acceptance_criteria>
    - 13-11 tests/type-check and all three official local commands exit 0 before any p13-12 candidate exists; project-local write mode is never invoked.
    - The accepted run directory returns `False` from exact `Test-Path -LiteralPath` immediately before the only local runner invocation.
    - The run id matches `p13-12-[0-9a-f]{32}`, the runner is invoked once, and no failed run is retried, reopened, overwritten, or appended.
    - Pending local evidence contains passed projection, D1 readiness, service readiness, Gateway auth, count-one D1, and Gateway API receipts for one exact local itemId, with no browser receipt yet.
    - The Task 1 wrapper invokes the exact verifier once, converts expected verifier exit 2 plus pending machine/artifact assertions into wrapper exit 0, and treats that result as a successful IAB handoff without rerunning the verifier or local runner.
    - Any non-pending result is retained as honest non-success evidence and prevents every provider/browser action; prior evidence remains hash-identical to the session baseline.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/preflight.test.ts src/deployment-target/__tests__/identity-boundary.test.ts src/deployment-target/__tests__/data-chain-evidence.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
    <automated>powershell -NoProfile -Command "$output = @(pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id '&lt;p13-12-run-id&gt;' --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence); $verifierExit = $LASTEXITCODE; $machineLine = @($output | Where-Object { $_ -match '^\s*\{.*\}\s*$' } | Select-Object -Last 1); if ($verifierExit -ne 2) { throw ('Expected exact verifier exit 2, got ' + $verifierExit) }; if ($machineLine.Count -ne 1) { throw 'Exact verifier emitted no unique machine JSON line.' }; $machine = $machineLine[0] | ConvertFrom-Json; $artifact = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.json' | ConvertFrom-Json; if ($machine.state -ne 'resolved_pending_observation' -or $machine.aggregate -ne 'pending' -or $machine.outcome -ne 'pending' -or [bool]$machine.provesExternalChain -ne $false -or [string]::IsNullOrWhiteSpace([string]$artifact.itemId)) { throw 'Exact verifier/artifact is not the pending IAB handoff tuple.' }; exit 0"</automated>
  </verify>
  <done>Exactly one fresh local attempt and exactly one verifier call have produced wrapper exit 0 for the valid exit-2 pending IAB handoff tuple without any rerun/retry/overwrite, or the run has closed immutably at its first honest non-handoff result and Phase 13 remains open.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Let the root persistent IAB record local Dashboard then viewer</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.md, .planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-08-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.json
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.md
  </read_first>
  <what-built>Task 1 produced the only eligible exact pending local tuple. Repository core owns validation, route order, receipt construction, rendering, and writes; the root Codex IAB supplies live navigation only.</what-built>
  <how_to_verify>The root orchestrator reuses the persistent signed-in Codex in-app browser, lets repository core navigate first to `http://localhost:8080/dashboard/movies` and then to `http://localhost:8080/movie/&lt;item-code&gt;`, requires the exact final route and code/itemId DOM marker at each surface, and then runs the exact local verifier.</how_to_verify>
  <action>Enter this task only for Task 1's exact `resolved_pending_observation/pending` pair. If the executor subagent lacks the Codex IAB tool, return the handoff defined in `accepted_execution_override` and wait; do not launch another browser or construct an observation. The root orchestrator must use the same persistent Codex IAB session, call repository core with only the injected live `observeSurface` adapter, and allow core to record Dashboard before viewer. Local base is exactly `http://localhost:8080`; direct app ports, Chrome, incognito/isolated contexts, default Puppeteer, cookie/storage export, screenshots, manual artifact edits, and caller-supplied status/tuple/order/receipt fields are forbidden. After repository core writes the pair, run `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id &lt;p13-12-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence`. Continue only on exit 0 with `terminal_passed` and `provesExternalChain: true`. Any browser/auth/route/DOM/tuple/verifier failure must be persisted by repository core as the allowlisted checkpoint, closes this run without retry, updates the Summary, and blocks Task 3.</action>
  <acceptance_criteria>
    - The local pair advances only through repository core from the exact pending tuple; no caller or user statement supplies a receipt or success status.
    - Browser order is Dashboard `/dashboard/movies` then viewer `/movie/&lt;item-code&gt;`, both at exact `http://localhost:8080` final origin/path and both containing the exact code/itemId marker.
    - The resulting pair is `resolved/passed`, carries tuple-bound browser_observer receipts after all runner receipts, and retains one internally consistent local itemId.
    - The exact local verifier exits 0 and reports `terminal_passed` plus `provesExternalChain: true`; otherwise Task 3 is forbidden and the checkpoint remains immutable.
    - The root IAB action and result are recorded in `13-12-SUMMARY.md` without cookies, storage, raw HTML, screenshot claims, or endpoint values.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id &lt;p13-12-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
  </verify>
  <resume-signal>The root execution orchestrator resumes only after it completed the persistent-IAB handoff and the exact local verifier returned terminal_passed with provesExternalChain true. Plain approval is insufficient.</resume-signal>
  <done>The fresh p13-12 local pair is terminal and remote-eligible, or the same run is an immutable honest browser checkpoint that leaves Phase 13 open.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Produce provider-backed remote proof and selected-canonical IAB receipts</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/remote.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/remote.md, .planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-09-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md
    - scripts/target-profile.ts
    - scripts/data-chain-smoke.ts
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.json
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-12-run-id&gt;/local.md
  </read_first>
  <what-built>Task 2 supplies the only eligible terminal local pair. Remote mode already owns exact-local loading, explicit selected target, live ownership checks, closed one-item fixture/snapshot children, selected-canonical API correlation, and fail-closed evidence.</what-built>
  <how_to_verify>Immediately before the only remote runner invocation, require the official remote live preflight to exit 0. After the runner produces a pending remote tuple, the root persistent IAB lets repository core record selected-canonical Dashboard before viewer. Both exact verifiers must then return terminal_passed and provesExternalChain true.</how_to_verify>
  <action>Proceed only from Task 2's exact terminal local pair. In the authorized selected-target context, run exactly `pnpm target-profile preflight --target starye-org --scope remote --command smoke --ci-environment starye-org --live` immediately before remote mutation and require exit 0. Do not supplement it with any provider/permission/resource command. Invoke `pnpm smoke:data-chain -- --mode remote --target starye-org --run-id &lt;p13-12-run-id&gt;` exactly once. The runner must reload the exact terminal local pair, preserve targetId/runId/itemCode, execute only the closed one-item fixture child, and require the count-one remote_fixture_snapshot to establish a non-empty remote itemId before selected-canonical API proof. Local and remote itemIds may differ; every remote receipt must use the remote D1-owned id. A pre-ingest or non-pending checkpoint is immutable, permanently non-retryable, and never enters browser observation. For a receipt-backed remote pending tuple, use the root handoff in `accepted_execution_override`: the same persistent Codex IAB supplies live selected-canonical Dashboard `/dashboard/movies` before viewer `/movie/&lt;item-code&gt;`; repository core resolves the canonical base and owns every receipt/write. Do not reveal or persist the resolved base, tokens, cookies, storage, raw endpoint, or prepared context. After core writes the remote pair, run the exact remote verifier and rerun the exact local verifier. Both must exit 0 and report `terminal_passed` with `provesExternalChain: true`, share target/run/code, and remain internally consistent on their own itemId. Compare all prior evidence against Task 1's session baseline and limit the claim to this execution window. Finalize `13-12-SUMMARY.md` accurately. Before any exact-path Summary commit, run GitNexus detect-changes and require no code symbol/execution-flow impact; generated evidence stays untracked and every unrelated worktree path stays excluded.</action>
  <acceptance_criteria>
    - The official remote live preflight exits 0 immediately before the only remote runner call and passes mapped D1/R2/KV/API Worker/Gateway Worker read-only checks without exposing values or raw output.
    - The closed fixture executes once; count-one remote D1 snapshot establishes a non-empty remote itemId, and selected-canonical API matches the exact remote code/id before IAB handoff.
    - Repository core plus the persistent Codex IAB records authorized Dashboard before viewer for the exact selected-canonical remote tuple, with no alternate browser or caller-generated receipt.
    - Exact local and remote verifiers both report `terminal_passed` and `provesExternalChain: true`; target/run/code match across modes and each mode's itemId is internally consistent.
    - Any provider/D1/API/auth/browser/route/DOM/receipt/verifier failure remains an immutable p13-12 checkpoint with no retry, promotion, zero-side-effect claim, or Phase completion claim.
    - Prior evidence is unchanged since the Task 1 baseline, generated evidence remains untracked, Summary is the only executor-owned tracked closeout path, and GitNexus reports no code flow for its commit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm target-profile preflight --target starye-org --scope remote --command smoke --ci-environment starye-org --live</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id &lt;p13-12-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id &lt;p13-12-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
  </verify>
  <resume-signal>Approve canonical Phase 13 re-verification only after both exact verifiers are terminal and the root IAB handoffs are receipt-backed. Otherwise retain the checkpoint and report only the exact non-secret gate.</resume-signal>
  <done>The selected target has provider-backed D1/API/admin and selected-canonical Dashboard/viewer proof for one exact fresh tuple, or p13-12 is an immutable honest checkpoint and Phase 13 remains open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Green repair/local gates -> run allocation | A run allocated before every gate passes would consume another immutable attempt without trustworthy readiness. |
| Candidate run id -> evidence path | Any collision could overwrite untracked evidence; exact absence must be proven before first use. |
| Pending tuple -> persistent root IAB | Browser session state is external; only exact route and code/itemId DOM observations may become repository-owned receipts. |
| Terminal local pair -> provider runner | Remote mutation may consume only the exact terminal target/run/code; local itemId cannot substitute for remote D1 identity. |
| Authorized provider process -> closed fixture/snapshot children | Credentials authorize one bounded path but must not escape into argv, artifacts, logs, or alternate commands. |
| Checkpoint -> Phase status | Honest non-success evidence must not be promoted by narration, user approval, or a missing receipt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-55 | Tampering | pre-run ordering and evidence path | high | mitigate | All repaired local gates precede GUID generation; exact LiteralPath absence gate; each mode invoked once; failed runs never reopen. |
| T-13-56 | Spoofing | local and remote tuple | critical | mitigate | Exact artifact loaders, deterministic code, count-one D1 snapshots, mode-owned itemIds, and terminal local prerequisite. |
| T-13-57 | Elevation of Privilege | provider mutation | critical | mitigate | Immediate official live preflight, explicit target/environment, closed fixed children, and no supplemental command. |
| T-13-58 | Information Disclosure | provider/browser/evidence | critical | mitigate | Process-only secrets, no cookie/storage export, repository-derived paths, strict allowlisted evidence, and exact parity verifier. |
| T-13-59 | Spoofing / Repudiation | IAB Dashboard/viewer receipts | high | mitigate | Persistent root IAB supplies navigation only; repository core derives tuple/order and writes receipts after exact DOM checks. |
| T-13-60 | Tampering / Repudiation | historical/new evidence | high | mitigate | Session-only pre-run hashes, no prior write paths, immutable new checkpoint policy, and time-bounded stability claims. |
| T-13-61 | Denial of Service | repeated provider/browser attempts | medium | mitigate | One invocation per mode, bounded observer waits, and terminal no-retry policy. |
| T-13-SC | Tampering | package supply chain | low | accept | No install, dependency, package, lockfile, or toolchain resolution occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Prove selected target through full local-to-production Gateway/canonical data chain. | Tasks 1-3 | COVERED | Dual exact terminal proof is the only passing outcome. |
| REQ | DATA-01 | Local API/auth/dashboard/content proof uses localhost:8080. | Tasks 1-2 | COVERED | No direct port can enter evidence. |
| REQ | DATA-02 | Local D1 schema/readiness precedes ingest and remote. | Task 1 | COVERED | Existing runner checks; no schema mutation. |
| REQ | DATA-03 | One deterministic bounded fixture records identity. | Tasks 1/3 | COVERED | One item, one code, each D1 owns its id. |
| REQ | DATA-04 | D1/API/admin correlation exists locally and remotely. | Tasks 1-3 | COVERED | Remote provider blocker closes in Task 3. |
| REQ | DATA-05 | Authorized Dashboard proves exact item. | Tasks 2-3 | COVERED | Persistent Codex IAB only. |
| REQ | DATA-06 | Gateway/canonical viewer proves exact item. | Tasks 2-3 | COVERED | Dashboard precedes viewer. |
| REQ | DATA-07 | Local/remote evidence is repeatable, non-secret, and terminal. | Tasks 1-3 | COVERED | Exact JSON/Markdown pairs and verifiers. |
| REQ | TEST-05 | Runner output distinguishes terminal/checkpoint deterministically. | 13-11 regressions plus Tasks 1-3 | COVERED | Exact verifier is authoritative. |
| RESEARCH | - | Projection/readiness hard gate precedes local ingest. | Task 1 | COVERED | Repair is proven before allocation. |
| RESEARCH | - | Remote work uses explicit target/live ownership and closed entries. | Task 3 | COVERED | No ad hoc provider path. |
| RESEARCH | - | Gateway-first browser proof and strict non-secret evidence. | Tasks 2-3 | COVERED | Root IAB handoff is bounded. |
| CONTEXT | D-01 | Exactly one deterministic non-R18 item with shared target/run/code and per-mode id. | Tasks 1/3 | COVERED | No sibling/batch compatibility. |
| CONTEXT | D-02 | Only bounded fixture/targeted adapter writes. | Tasks 1/3 | COVERED | Closed fixture child only. |
| CONTEXT | D-03 | Local projection/D1/Gateway proof precedes remote. | Tasks 1-2 | COVERED | Run allocation itself is gated. |
| CONTEXT | D-04 | Both modes prove D1/API/Dashboard/viewer with canonical routes and mode-owned id. | Tasks 1-3 | COVERED | Exact tuple checks at every surface. |
| CONTEXT | D-05 | Explicit target, credentials, ownership, and read-only live checks precede provider mutation. | Task 3 | COVERED | Exact official command immediately precedes runner. |
| CONTEXT | D-06 | Evidence is allowlisted, non-secret, tuple-bound, ordered, and checkpoint-aware. | Tasks 1-3 | COVERED | Repository core and exact verifier own writes. |
| CONTEXT | D-07 | Real provider/data-chain execution remains Phase 13 scope. | Task 3 | COVERED | Phase 14 cannot substitute documentation. |
| VERIFICATION | G-13-12-01 | Fresh selected local path reaches terminal pair. | Tasks 1-2 | COVERED | New id only after repaired gates. |
| VERIFICATION | G-13-12-02 | Provider-backed D1/API/admin proof is missing. | Task 3 | COVERED | Closed fixture/snapshot/API/Dashboard. |
| VERIFICATION | G-13-12-03 | Selected-canonical Dashboard/viewer receipts and terminal remote verifier are missing. | Task 3 | COVERED | Root IAB plus dual exact verifiers. |

Deferred Phase 14 literal cleanup, RUNBOOK switching/recovery documentation, and final milestone evidence mapping remain excluded. Broader crawler corpus and multi-target scheduling remain future work. No source item is missing from the 13-11/13-12 plan set.
</source_audit>

<verification>
- Task 1 must prove all repaired local gates before run allocation, then invoke local mode once. Any checkpoint stops the plan and permanently blocks Tasks 2-3.
- Task 2 is a root-orchestrator IAB handoff, not a user attestation. Only repository core may persist Dashboard/viewer receipts, and the exact local verifier alone decides remote eligibility.
- Task 3 reruns the exact official remote live preflight immediately before one remote invocation, uses only closed fixture/snapshot children, and requires root persistent-IAB selected-canonical observation.
- Both exact artifact verifiers must return `terminal_passed` and `provesExternalChain: true`. Then the orchestrator must rerun the canonical Phase verifier; only a passing `13-VERIFICATION.md` closes Phase 13.
- Before any Summary commit, run GitNexus detect-changes on the exact tracked closeout path and require no code symbol/flow impact. Evidence remains untracked and unrelated worktree changes remain excluded.
- No schema-relevant file is proposed; schema push is N/A and forbidden.
</verification>

<success_criteria>
- One p13-12 run directory was proven absent after all local gates passed and before the only local runner call.
- The local pair is terminal through D1, Gateway API, authorized Dashboard, and Gateway viewer using one internally consistent local tuple and localhost:8080 routes.
- Official selected-target live preflight passes immediately before one remote runner call; remote D1 independently establishes the itemId used by API, Dashboard, and viewer receipts.
- Root persistent Codex IAB records Dashboard before viewer in both modes through repository core; no alternate browser, caller receipt, or raw session data is used.
- Both exact verifiers and the canonical Phase verifier pass. Otherwise the run stays an immutable checkpoint and Phase 13 stays open.
</success_criteria>

## Artifacts this phase produces

- **New or modified code symbols:** none. This is execution/evidence only; it consumes the repaired Plan 13-11 symbols without editing them.
- **CLI flags:** none. It consumes existing `project-local --check`, `validate`, local/remote `preflight`, `smoke:data-chain --mode/--target/--run-id`, and `smoke:data-chain:verify --evidence-dir` contracts.
- **Generated evidence paths:** `.planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-12-run-id>/{local,remote}.{json,md}`. A pre-remote checkpoint may create only a subset. Every generated path remains untracked.
- **Tracked closeout path:** `.planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md`. ROADMAP/STATE/VERIFICATION updates are orchestrator/verifier-owned, not executor task files.
- **Provider resources:** none created or modified outside the one existing closed fixture upsert. No deploy, permission, DNS, resource, cleanup, or rollback operation is authorized.
- **Schema/migrations:** N/A. No schema file, migration, schema push, table/index change, or migration command is created or modified.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md` after Task 1 attempts execution and update it after each eligible handoff. Record exact non-secret gate/outcome/verifier facts and distinguish pending/checkpoint/terminal states. Keep all evidence untracked. If dual terminal proof succeeds, hand control to canonical Phase verification; if any gate checkpoints, leave Phase 13 open and never retry this run.
</output>
