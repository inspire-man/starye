---
phase: 13-full-chain-data-smoke
plan: "26"
type: execute
wave: 28
depends_on: [13-25]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - packages/config/src/deployment-target/__tests__/local-dev.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-26-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [TEST-05]
must_haves:
  truths:
    - "On Windows, root `pnpm.cmd dev` resolves the root-declared, integrity-pinned `tsx` loader and launches one direct Node/tsx local-dev entry command instead of a nested pnpm wrapper whose lifetime can end before the supervisor is observable."
    - "The repair is regression-tested without starting services, allocating a PID, creating evidence, or claiming runtime eligibility."
    - "A later fresh retry may use only a newly all-free, task-owned lifecycle; it cannot reuse 13-25's launcher PID, Summary, or blocked result."
  artifacts:
    - path: package.json
      provides: "Direct root dev command for the existing local-dev entry."
    - path: pnpm-lock.yaml
      provides: "Root importer declaration for the existing integrity-pinned tsx 4.21.0 package."
    - path: packages/config/src/deployment-target/__tests__/local-dev.test.ts
      provides: "Static regression for the root dev command shape."
    - path: .planning/phases/13-full-chain-data-smoke/13-26-SUMMARY.md
      provides: "Non-runtime repair result and the Plan 13-27 retry handoff."
  key_links:
    - from: "root pnpm.cmd dev"
      to: "scripts/local-dev-entry.ts"
      via: "root-declared tsx loader and one direct Node command without nested pnpm"
      pattern: "node --import tsx scripts/local-dev-entry.ts"
    - from: "13-26-SUMMARY.md"
      to: "future 13-27 fresh retry"
      via: "passing static regression only; no lifecycle release fields"
      pattern: "repair_status: passed"
---

<objective>
Close the Windows root-command wrapper gap that left Plan 13-25 unable to attribute a current-workspace `scripts/local-dev.ts` supervisor.

Purpose: the nested root pnpm command may end before the direct local-dev entry/supervisor is observable. The root script must make that entry a direct child while retaining all existing task-owned cleanup boundaries.
Output: a direct root dev command, a static regression test, and a repair-only Summary that requires a separately planned all-free retry.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-25-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md
@scripts/local-dev-entry.ts
@scripts/local-dev.ts
@packages/config/src/deployment-target/__tests__/local-dev.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resume from the committed RED regression and declare the root loader for one direct local-dev entry invocation</name>
  <files>package.json, pnpm-lock.yaml, packages/config/src/deployment-target/__tests__/local-dev.test.ts</files>
  <behavior>
    - Test 1: root `package.json` declares `tsx` at exactly 4.21.0 and the root lock importer resolves that already-integrity-pinned package version.
    - Test 2: the root `dev` script is exactly one Node command using the root-resolvable `tsx` import hook and `scripts/local-dev-entry.ts`, with no nested pnpm invocation.
    - Test 3: a root-cwd `node --import tsx --input-type=module --eval` can import `scripts/local-dev-entry.ts` without entering its CLI main path or starting a service.
    - Test 4: existing `buildLocalDevSupervisorInvocation()` coverage imports `isAbsolute`, `join`, and `normalize` from `node:path`, requires its supervisor script argument to be absolute, and requires `normalize(scriptPath)` to equal `normalize(join(repositoryRoot, 'scripts', 'local-dev.ts'))` exactly.
  </behavior>
  <action>Begin with the safe-resume gate. Treat commit `1e13a62` (`test(13-26): add root local-dev loader regression`) as the completed RED precondition: verify it is an ancestor of `HEAD` and inspect that it changes only `packages/config/src/deployment-target/__tests__/local-dev.test.ts`. Do not amend, revert, replay, or recreate that regression; the gate must accept the committed RED evidence rather than requiring a second failing-test run. Before any further code, script, or test action, compare the task-owned uncommitted `package.json` and `pnpm-lock.yaml` diffs against this complete candidate whitelist: (1) root `devDependencies` adds only `tsx: "4.21.0"`; (2) the root `.` lock importer adds only `tsx` with `specifier: 4.21.0` and `version: 4.21.0`; (3) only the nested `jiti` peer token in the `vitest` dependency value of `@vitest/coverage-v8@4.1.4(vitest@4.1.4)` and `@vitest/ui@4.1.4(vitest@4.1.4)` changes from `jiti@2.6.1` to `jiti@2.7.0`; and (4) only the old-jiti `@vitest/mocker@4.1.4(...)` snapshot and old-jiti `vitest@4.1.4(...)` snapshot gain `optional: true`. Require that the existing `tsx@4.21.0` package snapshot and its `sha512-5C1sg4USs1lfG0GFb2RLXsdpXqBSEhAaA/0kPL01wxzpMqLILNxIxIOKiILz+cdg/pLnOUxFYOR5yhHU666wbw==` integrity remain byte-identical. No added, removed, or otherwise modified importer, package declaration, package snapshot, integrity field, peer closure, or lock hunk is permitted. Use read-only package/lock and `git diff` checks to assert that exact set before continuing. A whitelist mismatch immediately stops at the executor checkpoint: do not create `13-26-SUMMARY.md` in any form, do not commit, and do not launch, inspect, stop, or claim any runtime process. Do not run `pnpm add`, `pnpm install`, `pnpm update`, `pnpm remove`, or any other dependency-resolution/package-mutation command or registry request again; the approved configured-registry exception has already produced this candidate and is now closed. The focused static `pnpm --filter ... exec` and `pnpm exec tsc` invocations named in this plan remain allowed only after the whitelist passes. Before modifying the remaining package script/test contract, run GitNexus impact for `buildLocalDevSupervisorInvocation` in `scripts/local-dev-entry.ts` with upstream direction, record direct callers, affected processes, and risk in the task output, and stop for an explicit warning if it reports HIGH or CRITICAL. Use the whitelisted candidate as-is, replace only the root `scripts.dev` value with `node --import tsx scripts/local-dev-entry.ts`, and do not touch the candidate package/lock lines. Strengthen the existing committed `buildLocalDevSupervisorInvocation()` test in place, rather than adding another RED case: import `isAbsolute`, `join`, and `normalize` from `node:path`; require a defined script argument; assert `isAbsolute(scriptPath)`; and assert `normalize(scriptPath)` equals `normalize(join(repositoryRoot, 'scripts', 'local-dev.ts'))` exactly. Do not retain the suffix-only or `..` checks as the proof of workspace ownership. Run the focused regression so its child root-loader import proves resolver availability without entering the CLI main path or starting services. If the root loader import, strengthened regression, or any scoped diff check fails, immediately stop at the same executor checkpoint with no Summary, commit, or runtime action; do not broaden the install or repair the lock manually. Task 2 is unreachable unless every preceding Task 1 gate succeeds. Do not change `scripts/local-dev-entry.ts`, `scripts/local-dev.ts`, `scripts/local-dev-authorization.ts`, service commands, port rules, cleanup logic, evidence logic, or downstream plans. This plan intentionally changes no existing production function, class, or method symbol; if implementation requires changing one, stop and obtain a new GitNexus impact analysis and gap plan before editing that symbol.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts</automated>
  </verify>
  <done>Only after every registry, package, importer, loader, scoped-diff, and strengthened absolute-path regression gate passes, the root direct loader is declared, lock-pinned, and resolved without a service launch; the root dev command no longer invokes pnpm from inside the pnpm lifecycle, while the supervisor argument exactly resolves to the current repository's `scripts/local-dev.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the repair without launching a runtime and hand off the new retry contract</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-26-SUMMARY.md</files>
  <action>Task 2 is reachable only after Task 1 fully succeeds: committed RED validation; exact candidate-whitelist match for the root declaration/importer, two named Vitest peer-string substitutions, two named optional snapshot flags, and unchanged tsx snapshot/integrity; root loader import; strengthened absolute-path regression; and scoped diff gates must all have passed without creating an executor checkpoint. Do not run `pnpm add`, `pnpm install`, `pnpm update`, `pnpm remove`, another dependency-resolution/package-mutation command, or another registry request. The focused static `pnpm --filter ... exec` and `pnpm exec tsc` commands named below remain allowed. Then run the focused local-dev test, the no-service root resolver command `node --import tsx --input-type=module --eval "await import('./scripts/local-dev-entry.ts')"`, `pnpm --filter @starye/config type-check`, and `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json`; run `git diff --check` only for `package.json`, `pnpm-lock.yaml`, and `packages/config/src/deployment-target/__tests__/local-dev.test.ts`. Re-run the exact candidate whitelist before the repair commit and require no candidate package/lock lines changed during the remaining static work. Before the repair commit, run `npx gitnexus detect-changes --repo starye` against this plan's diff and require package/test scope only, with no changed production symbol or unexpected execution flow; stop before commit if it reports unexpected scope or HIGH/CRITICAL expansion. Do not run `pnpm dev`, `pnpm.cmd dev`, `check:services`, any port/listener snapshot, smoke, fixture, D1, API, browser, provider, migration, deploy, or cleanup command. Write `13-26-SUMMARY.md` only after every Task 1 and Task 2 static gate passes, with the RED resume commit, configured-registry fact without credentials, exact root script, candidate-whitelist outcome, root tsx version and unchanged lockfile snapshot/integrity fact, impact/detect-changes outcome, test/typecheck outcomes, `repair_status: passed`, and no `runtime_eligibility`, `runtime_lifecycle`, or `post_cleanup_fixed_ports` field. The Summary must state that Plan 13-26 is isolated and was executed only with `$gsd-execute-phase 13 --gaps-only --wave 28`; do not execute 13-17 through 13-20 in that invocation. It must then require a new Plan 13-27, planned only after this repair result, to start from a fresh all-seven-port-free observation, launch exactly one newly task-owned root `pnpm.cmd dev` tree, attribute its current absolute `scripts/local-dev.ts` supervisor and listener ancestry, clean only that revalidated tree, and publish the three release fields only after post-cleanup all-free ports. It must forbid reusing, stopping, reinterpreting, or overwriting Plan 13-25 PIDs, Summary, evidence, or blocked status; 13-17 through 13-20 remain blocked until that later fresh retry releases eligibility.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/13-full-chain-data-smoke/13-26-SUMMARY.md'; if (-not (Test-Path -LiteralPath $p)) { throw '13-26 Summary missing' }; $raw=Get-Content -Raw -LiteralPath $p; if ($raw -notmatch '(?m)^repair_status:\s*passed\s*$') { throw 'Repair did not pass static gates' }; foreach($field in 'runtime_eligibility','runtime_lifecycle','post_cleanup_fixed_ports') { if ($raw -match ('(?m)^' + $field + ':')) { throw \"Repair Summary must not publish $field\" } }; if ($raw -notmatch '(?m)^next_required_plan:\s*13-27\s*$') { throw 'Missing fresh retry handoff' }"</automated>
  </verify>
  <done>The repair is proven by static regression/type gates only, and the next permitted runtime action is a separately planned all-free, task-owned Plan 13-27 retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Windows pnpm.cmd -> root dev script | The Windows command shim owns the direct child only when the root script does not introduce a nested pnpm layer. |
| Repair validation -> runtime lifecycle | Static command-shape proof cannot certify a PID, listener, readiness, cleanup, or release. |
| Historical 13-25 result -> fresh retry | Historical PIDs and blocked facts are read-only diagnostic input, never authorization to stop or reuse a process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-26-01 | Tampering | Root dev invocation | high | mitigate | Assert one direct Node/tsx entry and reject nested pnpm in the focused regression. |
| T-13-26-02 | Elevation of Privilege | Process ownership | high | mitigate | No runtime launch or PID operation occurs here; Plan 13-27 must establish new ownership from all-free ports. |
| T-13-26-03 | Repudiation | Runtime release | high | mitigate | Any Task 1 failure creates no Summary at all; only a fully static-passing Task 2 may create the repair-only Summary, which mechanically excludes all release fields. |
| T-13-26-04 | Denial of Service | Existing local processes | medium | mitigate | The plan forbids dev, readiness, listener, and cleanup commands. |
| T-13-26-05 | Information Disclosure | Planning Summary | low | mitigate | Record only command shape and gate results; no process, provider, endpoint, or secret data. |
| T-13-26-SC | Tampering | Root tsx dependency | high | mitigate | Resume from the committed RED only once, accept only the explicit task-owned candidate whitelist, reject every other package/lock diff including snapshot/integrity drift, and make no further package-manager or registry request. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | A usable local Gateway-first path must be restartable. | Tasks 1-2, then future 13-27 | COVERED | This repair is not runtime proof. |
| REQ | TEST-05 | Smoke runtime contracts have repeatable regression output. | Tasks 1-2 | COVERED | Focused root-launch and TypeScript gates. |
| RESEARCH | Existing tsx tooling | Existing workspace tooling may be reused but new package surface must be justified. | Task 1 | COVERED | Root declaration reuses only locked 4.21.0; the already-produced candidate must match the named whitelist before actual root loader resolution. |
| CONTEXT | D-03 | Local Gateway prerequisite remains before data-chain work. | Task 2 handoff | COVERED | No direct-port or smoke substitution. |
| CONTEXT | D-06 | Evidence must be non-secret and repeatable. | Task 2 Summary | COVERED | Static gate facts only. |
| VERIFICATION | G-1 | Fresh local pair is blocked by unowned cold-start. | Tasks 1-2, future 13-27 | COVERED | 13-26 removes the wrapper cause but cannot release eligibility. |
</source_audit>

<verification>
- Accept committed RED `1e13a62` through the safe-resume gate, then assert the explicit task-owned candidate whitelist before any remaining code/script/test action. A whitelist, integrity, root-loader, regression, or scoped-diff failure terminates Task 1 at an executor checkpoint with no `13-26-SUMMARY.md`, commit, runtime action, dependency-mutation command, or registry request; only complete Task 1 success reaches Task 2. Then run the pre-change GitNexus impact baseline, focused local-dev unit test with exact normalized supervisor path, no-service root loader import, config type-check, narrow local-dev TypeScript project, scoped diff/snapshot checks, and the explicit `npx gitnexus detect-changes --repo starye` pre-commit gate.
- Do not launch, inspect, stop, or claim ownership of any local process.
- Do not modify 13-25 Summary, historical evidence, AGENTS.md, CLAUDE.md, 13-VERIFICATION.md, or Phase 13 runtime/data-chain artifacts.
</verification>

<success_criteria>
- The root `dev` script is a direct Node/tsx invocation of `scripts/local-dev-entry.ts`, with no nested pnpm wrapper, and root Node can resolve the declared locked loader without starting a service.
- Static regressions prove the package/lock contract, new root command, root loader import, and the exact normalized current-repository absolute supervisor path.
- 13-26 produces no runtime release and explicitly routes one new all-free task-owned cold-start to separately planned Plan 13-27.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-26-SUMMARY.md` only after Task 1 fully succeeds and every Task 2 static gate passes. On any Task 1 failure, create no `13-26-SUMMARY.md` at all. Do not modify 13-25 Summary, historical evidence, 13-VERIFICATION.md, ROADMAP, STATE, or application runtime evidence during execution.
</output>
