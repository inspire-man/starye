---
phase: 13-full-chain-data-smoke
plan: "04"
type: execute
wave: 3
depends_on:
  - "13-03"
files_modified:
  - scripts/data-chain-smoke.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
autonomous: false
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Selected Cloudflare target
    why: Remote proof requires authorized provider credentials, resource access, and real deployed Gateway surfaces outside the repository.
    env: [CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, CRAWLER_SECRET]
    dashboard: "Use a real authorized Dashboard browser session on the selected target canonical domain."
must_haves:
  truths:
    - "D-03: remote mode requires an explicit existing `--run-id` and validates only `evidence/<target>/<run>/local.json|.md`; that exact terminal `resolved`/passed local record must have passed `local_projection`, D1 readiness, service readiness, Gateway `/auth/`, API, Dashboard, and viewer correlation for the same tuple. A missing, stale/different-run, pre-ingest, `resolved_pending_observation`, or incomplete record writes `local_prerequisite_unmet` before any provider preflight or mutation."
    - "D-05: remote mode has an explicit tracked target and `smoke` preflight with credential-key, account, resource-ownership, and read-only live checks before any fixture or D1 snapshot child runs."
    - "D-04/D-06: after a passing remote gate, the same tuple is checked through selected-target D1, a fixed selected-canonical Gateway API GET, real Dashboard, and only the selected canonical movie path."
    - "D-05/D-06: absent local proof, credentials, or provider access before remote fixture/snapshot produces a schema-valid `pre_ingest` checkpoint artifact with `itemId: null`; a post-snapshot API/Dashboard/viewer non-success preserves its authoritative tuple in `resolved_pending_observation`. Neither path creates a synthetic production success."
    - "D-07: remote provider/data-chain evidence is delivered in Phase 13, while literal cleanup, RUNBOOK finalization, and the final matrix remain deferred to Phase 14."
  artifacts:
    - scripts/data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/evidence/
  key_links:
    - "exact terminal passed local `<target>/<run>/local.json|.md` tuple -> remote --target --run-id -> smoke preflight/live ownership -> closed fixture and D1 snapshot -> resolved_pending_observation -> fixed selected-canonical API GET -> Dashboard append -> viewer append -> terminal passed evidence"
    - "failed remote gate -> redacted checkpoint evidence -> non-success exit with no child mutation"
---

<objective>
Complete the selected-target production half of the same smoke runner. It must either produce evidence for the real provider-backed D1/API/Dashboard/viewer chain or stop before mutation with a redacted checkpoint that explains the non-secret gate category.

Purpose: Phase 12 deliberately deferred real provider behavior. D-05 and D-07 require this phase to execute it when authorized, without replacing it with local simulation or documentation-only completion.

Output: tested remote runner branch, selected-target success-or-checkpoint evidence, and the sole external provider/session checkpoint needed for real production proof.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

  <context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-02-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-03-PLAN.md
@.planning/phases/12-cloudflare-config-switching/12-VERIFICATION.md
@packages/config/src/deployment-target/preflight.ts
@packages/config/src/deployment-target/live-checks.ts
@packages/config/src/deployment-target/mutation-entry.ts
@scripts/data-chain-smoke.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@RUNBOOK.md
</context>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | Exact Wave 2 passed `<target>/<run>/local.json|.md` evidence plus closed fixture/snapshot registry | Remote preflight, checkpoint semantics, and selected-canonical verification path | First, so a stale/missing local proof, failed local readiness, or a missing credential cannot reach remote preflight or mutation. |
| 2 | Task 1 preflight result and remote evidence item tuple | Real provider/browser proof or the final authorized-session checkpoint | Second; this is external only after all automation is complete. |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit remote smoke preflight, ownership gates, and checkpoint evidence</name>
  <files>scripts/data-chain-smoke.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
    - .planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
    - .planning/phases/13-full-chain-data-smoke/13-03-PLAN.md
    - packages/config/src/deployment-target/preflight.ts
    - packages/config/src/deployment-target/live-checks.ts
    - packages/config/src/deployment-target/mutation-entry.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - scripts/target-remote-entry.ts
    - scripts/verify-data-chain-smoke.ts
    - RUNBOOK.md
  </read_first>
  <behavior>
    - Remote mode requires `--mode remote`, an explicit tracked target, and an explicit existing local `--run-id`. It reads exactly `.planning/phases/13-full-chain-data-smoke/evidence/<target-id>/<run-id>/local.json` and `.md`, validates both typed artifacts and their matching target/run/mode fields, then rejects every directory scan, `latest` lookup, target-only fallback, or arbitrary old run. Only after this deterministic local gate passes does it execute `runTargetPreflight` using command `smoke`, live mode, credential-key presence, account match, and injected read-only ownership checks before any prepared child.
    - The exact local record must be terminal `ingestState: 'resolved'`, aggregate `passed`, and include passed `local_projection`, local D1 readiness, service readiness, Gateway auth, D1, API, Dashboard, and viewer rows for the same non-empty code/id tuple. A missing local pair, a stale/different-run pair, a pre-ingest record, a `resolved_pending_observation` record, an absent/not-passed `local_projection`, or any failed/checkpoint/malformed/mismatched required row writes a remote `pre_ingest` `local_prerequisite_unmet` checkpoint with `itemId: null` and returns before provider preflight or fixture execution. A Dashboard session is not a substitute for the local Gateway auth row.
    - Every remote preflight/provider/authorization failure writes a valid `pre_ingest` `remote_preflight` checkpoint record with `itemId: null`, stable non-secret categories, and a non-zero checkpoint exit; it invokes neither fixture nor D1 snapshot. It buffers any passed remote preflight observation until the snapshot supplies the authoritative id, so no pre-ingest artifact contains a success or data-surface row.
    - Only after the gate passes does remote mode invoke the Wave 1 registry operations and validate the same tuple. Immediately after the read-only remote D1 snapshot resolves its non-empty code/id, it creates `resolved_pending_observation`, preserving buffered remote-preflight and D1 rows, then derives the selected target canonical base internally and issues the fixed target-relative `GET /api/public/movies/<item-code>`. It records an `api` observation only when the returned immutable code and id exactly equal the snapshot tuple. A response mismatch, unavailable response, or direct-port/origin attempt records `canonical_api_unavailable` as a non-success pending artifact that preserves the tuple and blocks Dashboard/viewer pass claims. Only the Wave 2 typed observer may append Dashboard then viewer and promote it to terminal resolved/passed. It never accepts a remote host or direct port from CLI input.
  </behavior>
  <action>Before changing the existing `scripts/data-chain-smoke.ts` runner or any exported preflight integration symbol, run GitNexus upstream impact analysis and report the blast radius. Stop and present the required warning before a HIGH or CRITICAL edit. Write failing injected-dependency tests first. Extend only the remote branch of the existing strict runner. Require remote CLI parsing to have `--mode remote --target <tracked-id> --run-id <existing-local-run>`; derive and load only `.planning/phases/13-full-chain-data-smoke/evidence/<target>/<run>/local.json|.md`, validate both, and reject any latest-by-target, timestamp, glob, mode fallback, or arbitrary prior run selection. Require that exact local record to be terminal `resolved`/aggregate-passed with `local_projection`, D1 readiness, service readiness, Gateway `/auth/`, D1, API, Dashboard, and viewer observations all passed with the same non-empty item code/id. Treat `resolved_pending_observation` exactly like pre-ingest or malformed evidence: when either exact artifact is missing, invalid, pre-ingest, pending, stale/different-run, missing `local_projection`, or otherwise not passed, write a remote pre-ingest `local_projection/local_prerequisite_unmet` checkpoint (`itemId: null`, no success/API/D1/Dashboard/viewer row) and return 2 before `runTargetPreflight`, provider executor, fixture, or D1 snapshot. Only after that gate call the existing remote `smoke` preflight with `live: true` and a read-only ownership executor, map every blocking issue to a redacted pre-ingest `remote_preflight/target_preflight_unmet` checkpoint, and invoke the closed prepared fixture/snapshot only on success. Buffer passed gate observations until the snapshot supplies the authoritative id; immediately create `resolved_pending_observation` with the authoritative tuple and preserve remote-preflight/D1 rows. Never mint a placeholder id, regress a post-snapshot result to pre-ingest, or discard that id after API/browser failure. After a successful remote snapshot, construct no caller-supplied URL: derive the selected canonical base from the resolved profile and send exactly `GET /api/public/movies/<item-code>`. Validate the response's immutable `code` and `id` against the snapshot tuple before persisting the `api` evidence observation; an unavailable response, mismatched code/id, malformed payload, or attempted direct-port/origin request persists `canonical_api_unavailable` in the non-success pending artifact and prevents Dashboard/viewer success claims. Reuse the Wave 2 `smoke:data-chain-observe` command for remote browser rows only in its append form: it accepts only the matching pending tuple, Dashboard then viewer, and selected-canonical paths. Dashboard passed remains pending/exit 2; only viewer passed after Dashboard promotes that same artifact to terminal resolved/passed/exit 0. A failed/checkpoint browser append persists non-success/exit 2 and closes later append attempts; no retry can synthesize pass. Validate only with its mode-free form. Exercise the existing shared `smoke:data-chain:verify` wrapper in injected remote tests: it must accept only runner `0|2`, locate `.planning/phases/13-full-chain-data-smoke/evidence/<target>/<run>/remote.json|.md`, call the typed validator, require `0 => terminal resolved/aggregate passed`, and preserve `2 => persisted pre-ingest or resolved-pending non-success`; no raw child command or exit-code workaround is allowed. Do not add a raw Wrangler/D1/crawler command, target fallback, provider URL option, secret CLI flag, remote-output dump, handwritten success JSON, or retry that changes a checkpoint into a pass. Persist checkpoint JSON/Markdown before returning the stable checkpoint exit status. Test an otherwise passed old local run beside a missing/different requested run and assert it is ignored; test an exact local pending artifact and one without a passed `local_projection`; in all cases assert the remote record is a valid pre-ingest checkpoint and preflight/provider/fixture/snapshot calls are all zero. Also test missing service/Gateway auth, missing credentials, wrong account, live ownership failure, prepared-child failure, tuple mismatch, post-snapshot item-id preservation, canonical API code/id mismatch, canonical API unavailability, direct-port attempt, duplicate/skip browser order, and canonical viewer absence; before the remote gate assert zero fixture/D1 child calls, and after a snapshot API/browser checkpoint assert no Dashboard/viewer pass claim.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-smoke-remote.test.ts src/deployment-target/__tests__/mutation-entry.test.ts</automated>
    <automated>pnpm --filter @starye/config test --run && pnpm --filter @starye/crawler test --run</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id &lt;run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove only the explicit requested `<target>/<run>/local.json|.md` pair is considered: an otherwise passed older run, a missing requested run, a mode/target/run mismatch, a pre-ingest or `resolved_pending_observation` local artifact, an absent/not-passed `local_projection`, malformed/failed/checkpointed local evidence, tuple mismatch, or missing Gateway auth all prevent remote preflight and both prepared children. Each writes `local_prerequisite_unmet` as a valid remote pre-ingest checkpoint with `itemId: null`, exit code 2, no data-surface/success row, and zero provider-preflight/fixture/snapshot calls.
    - Tests prove each remote preflight/provider gate failure writes a valid pre-ingest checkpoint JSON and Markdown record with `itemId: null`, returns the checkpoint exit state, and starts neither prepared child; tests reject a fabricated id or a pre-ingest API/D1/Dashboard/viewer/pass row.
    - Tests prove a passed remote gate uses only the registered fixture/snapshot operations and, after the D1 snapshot, creates a tuple-preserving `resolved_pending_observation` before exactly the selected-canonical target-relative `GET /api/public/movies/<item-code>` may add an `api` row. Mismatch, malformed/unavailable response, or a direct-port/origin attempt is `canonical_api_unavailable` non-success evidence that preserves item id and cannot produce Dashboard/viewer pass evidence.
    - Remote wrapper tests prove it deterministically reads the `<target>/<run>/remote.json|.md` pair, accepts only `0 => terminal resolved/aggregate passed` or `2 => persisted pre-ingest/resolved-pending non-success`, and rejects any other runner exit or code/artifact mismatch.
    - The actual authorized invocation leaves one remote success artifact or one checkpoint artifact; neither artifact has a token, raw remote endpoint, header, cookie, prepared context, or direct service port.
  </acceptance_criteria>
  <done>Remote execution is a real, selected-target smoke attempt with fail-closed evidence rather than an unbounded provider script or synthetic production success.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Complete selected-target Dashboard and canonical viewer proof after automated gates pass</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
    - .planning/phases/13-full-chain-data-smoke/evidence/
    - RUNBOOK.md
    - apps/dashboard/src/lib/hono-rpc-client.ts
    - apps/movie-app/src/lib/api-client.ts
  </read_first>
  <what-built>Task 1 has either created a remote checkpoint before mutation or produced a `resolved_pending_observation` record for the exact target/run/item tuple after preflight, ownership, fixture, D1, and API automation. Wave 2 already provided the constrained typed surface-observation writer.</what-built>
  <how-to-verify>Only when Task 1's remote artifact is `resolved_pending_observation` and its persisted `api` row contains the exact snapshot code/id, open the selected target's canonical Gateway dashboard with an existing authorized session and find the recorded item in Movies management. Append Dashboard first: `pnpm smoke:data-chain-observe -- --mode remote --target starye-org --run-id <run-id> --item-code <item-code> --item-id <item-id> --surface dashboard --path /dashboard/movies --status passed`; this remains pending and exits 2. Then open the selected target's canonical `/movie/<item-code>` path, confirm the same item, and append viewer second: `pnpm smoke:data-chain-observe -- --mode remote --target starye-org --run-id <run-id> --item-code <item-code> --item-id <item-id> --surface viewer --path /movie/<item-code> --status passed`; only this promotion exits 0. If provider browser access, session, role, or canonical viewer is unavailable, append the matching first unavailable surface with `--status checkpoint --checkpoint dashboard_auth_unavailable` or, only after passed Dashboard, `--checkpoint canonical_viewer_unavailable`; the append persists non-success evidence and exits 2. Do not skip Dashboard, retry/duplicate an append, substitute a local browser, direct app origin, API-only request, or fabricate a cookie.</how-to-verify>
  <action>This checkpoint is limited to external provider access and a real authorized browser session. Append accepts only the matching pending tuple, selected-canonical target-relative paths, enumerated surface/status/checkpoint category, and exact Dashboard-then-viewer order. Dashboard pass remains pending/exit 2; viewer pass after it promotes the same artifact to resolved/passed/exit 0. A failed/checkpoint append remains non-success/exit 2, closes later append attempts, and cannot be turned into a synthetic pass. Validate any result only with `pnpm smoke:data-chain-observe -- --validate --target starye-org --run-id <run-id> --item-code <item-code> --item-id <item-id>`; it accepts no mode or append-only argument, finds the unique target/run/item artifact, and schema validation of a persisted pending/checkpoint exits 0 without making the remote run passed. Do not request manual provider commands, mutate Dashboard data beyond the bounded fixture upsert, claim a local result as remote evidence, or change RUNBOOK/source literals. No note, URL, cookie, header, or direct port is accepted. A missing real session/viewer remains checkpoint evidence even when other remote checks passed.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A passed remote evidence record is terminal resolved and has matching target/run/item code/id across D1, API, Dashboard, and viewer rows, with viewer path `/movie/<code>` on the selected canonical target.
    - Dashboard then viewer rows are passed only after real selected-target browser observation through the exact typed command. A passed Dashboard stays pending/exit 2; only viewer promotes it. Every absent credential, provider, role, session, or viewer condition remains checkpoint/exit 2 with no success claim or later synthetic promotion.
    - No Phase 14 literal cleanup, stable RUNBOOK editing, or final requirement-to-evidence matrix is introduced.
  </acceptance_criteria>
  <done>The production data-chain proof is either terminally complete through ordered real selected-target Dashboard then viewer evidence, or accurately remains a non-success checkpoint at the external provider/session boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider credentials/account -> remote preflight | Credentials prove access but must not be treated as target identity or evidence data. |
| Read-only ownership check -> remote mutation | Provider check results must decide whether the one-item fixture may run. |
| Selected canonical Gateway -> browser proof | A matching API result cannot substitute for a real Dashboard/viewer route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-15 | Spoofing | Cloudflare target/account | critical | mitigate | Explicit target, account comparison, and read-only ownership check before closed fixture execution. |
| T-13-16 | Elevation of Privilege | remote runner | critical | mitigate | `smoke` preflight plus closed prepared entries; no arbitrary provider command/endpoint/SQL input. |
| T-13-17 | Information Disclosure | remote diagnostics/evidence | critical | mitigate | Key-presence/category-only checkpoints and evidence allowlist; tests prohibit raw provider output and secrets. |
| T-13-18 | Repudiation | production claim | high | mitigate | Correlated tuple and per-surface status; checkpoint is non-success and cannot be overwritten by text. |
| T-13-19 | Tampering | Dashboard/viewer proof | high | mitigate | Require selected-canonical real session/browser result; direct origins and fabricated cookies are rejected. |
</threat_model>

<verification>
- Run the remote contract tests and complete suite above before any authorized provider attempt.
- Validate the exact requested remote `<target>/<run>/remote.json|.md` pair with the same schema used locally; require either all required surface rows passed or at least one explicit checkpoint/failure row. Never infer a remote result from the latest target directory entry.
- Run GitNexus `detect_changes` before committing. Expected changes are remote runner behavior and tests only; no RUNBOOK, literal cleanup, or generated evidence should be staged.
</verification>

<success_criteria>
When an authorized environment exists, the exact selected target runs one real provider-backed fixture and proves the same item in D1, API, Dashboard, and canonical viewer. In the current credential-free environment, the same command reliably writes a redacted, non-success checkpoint before any remote mutation. Both outcomes are truthful Phase 13 evidence.
</success_criteria>

## Artifacts this phase produces

- Remote smoke preflight/result branch with stable checkpoint behavior.
- Selected-target JSON and Markdown production evidence, either all-surface success or an explicit gated checkpoint.
- Focused remote regression tests covering no-mutation-on-checkpoint, target ownership, tuple correlation, and canonical-only URL policy.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-04-SUMMARY.md` when done.
</output>
