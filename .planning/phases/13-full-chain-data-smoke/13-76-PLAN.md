---
phase: 13-full-chain-data-smoke
plan: "76"
type: tdd
wave: 70
depends_on: [13-72, 13-73]
files_modified:
  - apps/dashboard/src/views/Movies.vue
  - apps/dashboard/src/views/__test__/Movies.test.ts
  - apps/movie-app/src/views/MovieDetail.vue
  - apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
  - scripts/data-chain-surface-observation.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "An authenticated Dashboard Movies row exposes the exact movie code and its non-secret mode-local itemId in a stable DOM contract before the Viewer action, per D-04, D-05, and D-06."
    - "The canonical Viewer detail surface exposes the same exact code and mode-local itemId through its own stable DOM contract, so a title, route fragment, or player-only match cannot synthesize tuple proof."
    - "The root-IAB observation port accepts a passed Dashboard or Viewer result only when its visible code and itemId match the repository-owned pending tuple; missing, duplicate, or mismatched visible identity becomes the existing first checkpoint."
    - "This repair changes no run id, evidence artifact, session material, provider call, handoff, preflight, local verifier, remote verifier, or production navigation; p13-73 and every prior carrier remain frozen."
  artifacts:
    - path: apps/dashboard/src/views/Movies.vue
      provides: Canonical Dashboard row DOM markers for exact code and itemId without exposing secrets.
    - path: apps/movie-app/src/views/MovieDetail.vue
      provides: Canonical Viewer detail DOM markers for the same code and itemId.
    - path: scripts/data-chain-surface-observation.ts
      provides: Root-IAB result normalization that requires tuple-bound visible identity before a receipt can pass.
    - path: apps/dashboard/src/views/__test__/Movies.test.ts
      provides: Dashboard DOM contract regression coverage.
    - path: apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
      provides: Viewer DOM contract regression coverage.
    - path: packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
      provides: Local root-IAB tuple and first-checkpoint regression coverage.
    - path: packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
      provides: Selected-target root-IAB tuple and first-checkpoint regression coverage.
  key_links:
    - from: Dashboard Movies DOM contract
      to: RootIabSurfaceObserver Dashboard result
      via: The adapter reads only the row for the repository-derived itemCode and returns its paired itemId.
      pattern: data-smoke-item-code and data-smoke-item-id
    - from: Viewer detail DOM contract
      to: RootIabSurfaceObserver Viewer result
      via: The adapter confirms the same repository-derived code and itemId before returning passed.
      pattern: exact code and itemId
    - from: RootIabSurfaceObserver normalized result
      to: appendBrowserObservation
      via: Existing captureSurface tuple equality keeps the Dashboard-before-Viewer receipt grammar and freezes first non-success.
      pattern: dashboard then viewer
---

<objective>
Repair the observable identity boundary that still turns an authenticated root-IAB Dashboard session into `dashboard_auth_unavailable`: make the exact pending `itemCode + itemId` tuple visible and machine-readable on the canonical Dashboard and Viewer surfaces, then require that visible tuple in the root-IAB observer result.

Purpose: Plan 13-72 removed the independent browser fallback, but Plan 13-73 still checkpointed because a real authenticated Dashboard surface could not prove the repository-owned itemId. The next safe action is a code-only DOM/adapter repair, not a retry of p13-73 or a new carrier.

Output: Stable Dashboard and Viewer DOM identity contracts, root-IAB observation normalization tests, and `13-76-SUMMARY.md`. No execution evidence or external operation is created.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
@apps/dashboard/src/lib/api.ts
@apps/dashboard/src/views/Movies.vue
@apps/dashboard/src/views/__test__/Movies.test.ts
@apps/movie-app/src/views/MovieDetail.vue
@apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
@scripts/data-chain-surface-observation.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
</context>

<interfaces>
- `Movie` already contains `id` and `code`; no API, database, or route contract changes are required.
- The Dashboard row for the exact movie exposes `data-smoke-item-code` and `data-smoke-item-id` with the existing code/id values. The itemId must remain readable text or accessible metadata, must be scoped to its row, and must not be copied into URLs, query parameters, logs, or session storage.
- `MovieDetail.vue` exposes equivalent item-level markers for the loaded detail. The Viewer uses the route code only to load the movie; a route match alone is not a data-chain tuple assertion.
- `RootIabSurfaceObservationResult` remains allowlisted and non-secret. Its `passed` branch is valid only after the root-owned adapter obtains exactly one matching visible code/itemId pair from the requested canonical surface; existing `captureSurface` retains Dashboard-first receipt ordering and existing checkpoint codes.
</interfaces>

## Artifacts This Phase Produces

| Artifact | Owner | Produced state |
| --- | --- | --- |
| Dashboard Movies DOM identity markers | Dashboard UI | Exact tuple is visible in the matching canonical row before `查看影片`. |
| Viewer detail DOM identity markers | Movie app UI | Exact tuple is visible on the matching canonical detail surface. |
| Root-IAB result normalization | Observation boundary | Only exact visible tuple results can produce passed receipts. |
| Focused UI and deployment-target tests | Regression boundary | DOM scope, exact correlation, and checkpoint-first behavior remain deterministic. |
| `13-76-SUMMARY.md` | Phase 13 | Code-only repair and validation record without a carrier identifier. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify visible Dashboard and Viewer tuple identity contracts</name>
  <files>apps/dashboard/src/views/__test__/Movies.test.ts, apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts</files>
  <read_first>
    - apps/dashboard/src/lib/api.ts
    - apps/dashboard/src/views/Movies.vue
    - apps/dashboard/src/views/__test__/Movies.test.ts
    - apps/movie-app/src/views/MovieDetail.vue
    - apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <behavior>
    - A populated Dashboard row has one scoped identity marker pair whose code and itemId originate from the same `Movie` item, and its Viewer link keeps the canonical code route.
    - A Dashboard row with a different code or itemId cannot satisfy the queried item marker pair.
    - A loaded Viewer detail renders one matching code/itemId pair before player-state content, while loading, error, and no-player states never manufacture a tuple.
  </behavior>
  <action>
Before changing any Vue component symbol, run GitNexus upstream impact analysis for the affected component functions and report direct callers plus affected flows in the execution summary. If an analysis is HIGH or CRITICAL, stop for acknowledgement before editing. Preserve existing user changes.

Write focused red tests around the existing component contracts. In `Movies.test.ts`, render a list containing the deterministic smoke-shaped movie plus a decoy item and assert the exact-code row carries exactly one `data-smoke-item-code` and paired `data-smoke-item-id`; assert the `查看影片` link remains `/movie/{code}` and no itemId is placed in that URL. In `MovieDetail.dom-contract.test.ts`, load a detail fixture with an id and code, assert the same marker names and values are visible before player content, then cover missing detail/player/error states without a synthetic marker. Treat titles, URLs, and player labels as insufficient identity evidence. Implement D-03, D-04, D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter dashboard test --run src/views/__test__/Movies.test.ts; pnpm --filter @starye/movie-app test --run src/views/__tests__/MovieDetail.dom-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both focused test commands initially fail without the new DOM identity contracts and pass after Task 2.
    - The tests prove code and itemId are paired on the same rendered item rather than found independently anywhere on the page.
    - The tests prove the canonical Viewer path remains code-only and the UI does not expose session, provider, or endpoint material.
  </acceptance_criteria>
  <done>Focused tests define the exact, scoped, non-secret DOM identity behavior required by the observer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the scoped tuple and make the root-IAB port require it</name>
  <files>apps/dashboard/src/views/Movies.vue, apps/movie-app/src/views/MovieDetail.vue, scripts/data-chain-surface-observation.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts, .planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md</files>
  <read_first>
    - apps/dashboard/src/views/Movies.vue
    - apps/movie-app/src/views/MovieDetail.vue
    - scripts/data-chain-surface-observation.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - .planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
  </read_first>
  <behavior>
    - The root-IAB adapter returns `passed` only when its requested Dashboard or Viewer surface reports one matching visible code/itemId pair.
    - Missing, duplicate, or mismatched visible markers normalize to unavailable and cause the existing first-surface checkpoint without a Viewer callback after a Dashboard non-success.
    - Local and selected-target tests retain their canonical origins, root-IAB-only ownership, repository-derived paths, and evidence grammar.
  </behavior>
  <action>
Run GitNexus upstream impact analysis for `normalizeRootIabObservationResult`, `captureSurface`, and each Vue symbol changed in this task before editing. Record the blast radius in `13-76-SUMMARY.md`; if any result is HIGH or CRITICAL, stop for acknowledgement. Do not alter general fixture creation, handoff, evidence serialization, or verifier behavior.

Implement the Task 1 DOM contracts in `Movies.vue` and `MovieDetail.vue` using the existing `Movie.id` and `Movie.code` data already loaded by each view. Scope the marker pair to its concrete row/detail root, retain the documented Dashboard/Viewer theme boundary, and avoid new buttons, retry controls, routes, target selectors, or persistence. Update the root-IAB adapter/result normalization so `passed` denotes a tuple extracted from those scoped markers, never a title match, route-only match, or caller-provided value. Keep the adapter result allowlist restricted to status, itemCode, and itemId; do not add cookie, local-storage, profile, header, provider, or raw-response fields.

Extend the local and remote deployment-target tests with root-IAB doubles that model the visible scoped pair. Assert exact-match Dashboard then Viewer receipts pass, while missing/duplicate/mismatched DOM tuple facts create the existing Dashboard or Viewer checkpoint and preserve the first-checkpoint stop. Do not allocate a run id, invoke a browser, read or write execution evidence outside test doubles, invoke handoff/preflight/verifier commands, or contact a provider. Record only source changes, GitNexus scope, focused results, and the absence of execution operations in `13-76-SUMMARY.md`. This task implements D-03 through D-07.
  </action>
  <verify>
    <automated>pnpm --filter dashboard test --run src/views/__test__/Movies.test.ts; pnpm --filter @starye/movie-app test --run src/views/__tests__/MovieDetail.dom-contract.test.ts; pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts; pnpm --filter dashboard exec vue-tsc --noEmit; pnpm --filter @starye/movie-app exec vue-tsc --noEmit; pnpm --filter @starye/config type-check</automated>
  </verify>
  <acceptance_criteria>
    - The two canonical surfaces expose one scoped, matching code/itemId pair from their loaded movie data.
    - Root-IAB observation passes only for an exact visible tuple and retains existing first-checkpoint behavior for every other result.
    - The focused UI and data-chain tests plus config type-check exit zero, and the summary confirms no carrier or external operation occurred.
  </acceptance_criteria>
  <done>The authenticated root-IAB can distinguish a real exact tuple from an ambiguous Dashboard/Viewer surface without retrying frozen evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| API movie data to Dashboard/Viewer DOM | Non-secret `id` and `code` become a narrowly scoped identity assertion on rendered canonical surfaces. |
| Root-owned IAB DOM result to evidence core | The adapter may supply allowlisted observation facts but cannot choose the repository-owned pending tuple or write evidence. |
| Frozen execution evidence to repair work | Historical carriers remain read-only context and must not be retried or reclassified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-76-01 | Spoofing | Root-IAB observation result | high | mitigate | Require exactly one visible code/itemId pair equal to the pending evidence tuple before returning passed. |
| T-13-76-02 | Tampering | DOM-to-receipt correlation | high | mitigate | Keep path derivation and tuple equality in `captureSurface`; the adapter cannot choose evidence values. |
| T-13-76-03 | Information disclosure | UI identity markers | medium | mitigate | Render only existing non-secret movie id/code; keep all session, provider, header, and endpoint data out of DOM contracts and evidence. |
| T-13-76-04 | Denial of service | Observation checkpoint flow | medium | mitigate | Preserve Dashboard-first, first-non-success stop and prohibit retry/allocation in this code-only plan. |
| T-13-76-05 | Elevation of privilege | Dashboard UI | high | mitigate | Add no controls or mutation endpoints; retain existing auth/role guard and code-only Viewer link. |
</threat_model>

<verification>
- Validate both plan frontmatter and XML structure before execution.
- Execute the focused Dashboard, Viewer, and deployment-target test commands plus `@starye/config` type-check.
- Run `git diff --check`; before committing, run GitNexus detect-changes and stage only the plan-owned source, test, and summary files.
- Confirm the summary contains no run id and no evidence path for a newly allocated carrier.
</verification>

<success_criteria>
- The exact Dashboard and Viewer movie tuple is visible through scoped, non-secret DOM markers.
- Root-IAB capture rejects ambiguous surface identity before it can produce a passed receipt.
- Existing checkpoint/freeze behavior remains intact, with no live local or remote data-chain activity during the repair.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md` when execution is complete.
</output>
