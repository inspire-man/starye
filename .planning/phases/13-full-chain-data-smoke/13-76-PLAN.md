---
phase: 13-full-chain-data-smoke
plan: "76"
type: tdd
wave: 70
depends_on: [13-73]
files_modified:
  - apps/dashboard/src/views/Movies.vue
  - apps/dashboard/src/views/__test__/Movies.test.ts
  - apps/movie-app/src/views/MovieDetail.vue
  - apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
  - scripts/data-chain-surface-observation.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "The authenticated Dashboard Movies row and public Viewer detail expose one exact, non-secret itemCode/itemId pair on the same visible code element, so a title, route, or a detached field cannot identify the smoke item, per D-01 and D-04."
    - "The root-IAB observer receives repository-owned names for those two DOM attributes with the pending tuple and accepts a passed result only when returned code and ID exactly match, per D-04 and D-06."
    - "The repair allocates no run ID and does not observe, retry, verify, hand off, preflight, mutate, or rewrite the frozen p13-73 carrier or prior history, per D-05, D-06, and D-07."
  artifacts:
    - path: apps/dashboard/src/views/Movies.vue
      provides: Stable Dashboard row DOM contract for a visible item code and matching mode-owned item ID.
    - path: apps/movie-app/src/views/MovieDetail.vue
      provides: Stable Viewer detail DOM contract for the same visible item code and matching mode-owned item ID.
    - path: scripts/data-chain-surface-observation.ts
      provides: Closed root-IAB tuple-attribute descriptor sent with the existing expected tuple and canonical route.
    - path: apps/dashboard/src/views/__test__/Movies.test.ts
      provides: Dashboard same-element tuple regression coverage.
    - path: apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
      provides: Viewer same-element tuple regression coverage.
    - path: packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
      provides: Local observer descriptor, exact-match, and first-checkpoint coverage.
    - path: packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
      provides: Selected-target observer descriptor, exact-match, and first-checkpoint coverage.
  key_links:
    - from: apps/dashboard/src/views/Movies.vue
      to: RootIabSurfaceObserver
      via: The canonical /dashboard/movies code cell exposes the fixed code/id attributes requested by the root-IAB observation input.
      pattern: data-phase13-item-code
    - from: apps/movie-app/src/views/MovieDetail.vue
      to: RootIabSurfaceObserver
      via: The canonical /movie/{itemCode} detail code marker exposes the same fixed code/id attributes requested by the root-IAB observation input.
      pattern: data-phase13-item-id
    - from: scripts/data-chain-surface-observation.ts
      to: packages/config/src/deployment-target/data-chain-evidence.ts
      via: captureSurface keeps existing exact returned tuple equality before appendBrowserObservation can write a Dashboard or Viewer receipt.
      pattern: observation.itemCode === evidence.itemCode
---

<objective>
Repair the real Dashboard and Viewer identity seam so the authenticated root-IAB can read the exact itemCode/itemId tuple it already validates, without allocating a carrier or retrying the immutable p13-73 checkpoint.

Purpose: p13-73 reached an authenticated Dashboard Movies surface but stopped at `dashboard_auth_unavailable` because the observer could not prove the exact tuple from that real surface. The existing observation core already derives and compares the tuple; this plan provides a stable, non-secret DOM source and a closed attribute descriptor for the root-IAB port.

Output: Two scoped DOM contracts, focused UI/observer regression coverage, and `13-76-SUMMARY.md`. No run or execution evidence is created.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-72-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
@apps/dashboard/src/views/Movies.vue
@apps/movie-app/src/views/MovieDetail.vue
@scripts/data-chain-surface-observation.ts
</context>

<interfaces>
- `Movie` already supplies `code` and `id` to both real surfaces. The repair adds no API, database, route, or player contract.
- `RootIabSurfaceObservationInput` gains a repository-owned readonly pair of attribute names. It continues to receive only the existing mode, target ID, expected code, expected ID, canonical base, and canonical path; no caller provides selectors, credentials, session values, headers, profiles, endpoints, or identity claims.
- `RootIabSurfaceObservationResult` remains the existing allowlisted `{ status, itemCode, itemId }`. The external root-IAB reads the named attributes, while `captureSurface` preserves existing normalization, exact equality, Dashboard-before-Viewer ordering, and first-checkpoint behavior.
</interfaces>

<source_audit>

| SOURCE | ID | Feature / constraint | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 13 | Selected target requires terminal exact Dashboard-to-Viewer chain proof | 13-66 through 13-76 | COVERED | 13-76 repairs the missing real-surface tuple source; execution remains separately gated. |
| REQ | DATA-01 | Gateway-only local surface proof | 13-66 through 13-73 | COVERED | This repair changes no URL, route, or origin. |
| REQ | DATA-02 | Local D1 readiness before ingest | 13-66 | COVERED | Outside this DOM/observer-only repair. |
| REQ | DATA-03 | One deterministic non-R18 fixture | 13-66 through 13-67 | COVERED | 13-76 creates no fixture, carrier, or write. |
| REQ | DATA-04 | D1/API/admin tuple correlation | 13-67, 13-76 | COVERED | 13-76 makes the real admin identity source exact. |
| REQ | DATA-05 | Session-gated Dashboard validation | 13-72, 13-73, 13-76 | COVERED | Protected Movies row exposes code/id together. |
| REQ | DATA-06 | Canonical Viewer validation | 13-72, 13-76 | COVERED | Viewer detail exposes the matching tuple contract. |
| REQ | DATA-07 | Non-secret evidence contract | 13-72, 13-75, 13-76 | COVERED | Descriptor and result remain allowlisted and non-secret. |
| REQ | TEST-05 | Exact identity mismatch regression coverage | 13-72, 13-76 | COVERED | Focused Vue and config Vitest suites cover the new contract. |
| RESEARCH | R-01 | One target/run-derived item, never title-only identity | 13-05 through 13-08, 13-76 | COVERED | Code/id attributes bind the existing one-item tuple. |
| RESEARCH | R-02 | Gateway-relative Dashboard and canonical Viewer retain real auth boundaries | 13-72 through 13-76 | COVERED | No API client, route, or auth change is planned. |
| RESEARCH | R-03 | Existing stack only, no package install | 13-76 | COVERED | Existing Vue, Vitest, and TypeScript tooling only. |
| CONTEXT | D-01 | One deterministic item with mode-owned item ID | 13-76 | COVERED | The DOM contract describes one code/id pair and cannot add siblings or batches. |
| CONTEXT | D-02 | Restricted fixture/crawler write boundary | 13-76 | COVERED | No crawler, fixture, API write, or mutation code is in scope. |
| CONTEXT | D-03 | Local Gateway-first route contract | 13-76 | COVERED | Canonical route derivation remains untouched. |
| CONTEXT | D-04 | Exact tuple across Dashboard and Viewer | 13-76 | COVERED | Primary repair target. |
| CONTEXT | D-05 | Explicit-target, fail-closed provider boundary | 13-76 | COVERED | This plan contains no provider command, preflight, handoff, or retry. |
| CONTEXT | D-06 | Non-secret evidence fields | 13-76 | COVERED | Fixed attribute names and allowlisted tuple facts add no sensitive material. |
| CONTEXT | D-07 | Actual chain stays Phase 13-owned | 13-76 | COVERED | This remains a Phase 13 code/test repair, not a later-phase substitute. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing exact-tuple contracts for both real surfaces and the observer input</name>
  <files>apps/dashboard/src/views/__test__/Movies.test.ts, apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</files>
  <read_first>
    - apps/dashboard/src/views/Movies.vue
    - apps/dashboard/src/views/__test__/Movies.test.ts
    - apps/movie-app/src/views/MovieDetail.vue
    - apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
    - scripts/data-chain-surface-observation.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
    - .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
  </read_first>
  <behavior>
    - Dashboard: a deterministic row exposes its visible code and its own item ID through both stable phase-13 attributes on one element; a decoy row cannot satisfy that pair.
    - Viewer: a loaded detail exposes its visible code and its own item ID through those same attributes before player-state content; loading, error, and no-player states do not manufacture a tuple.
    - Observer: both local and remote root-IAB calls receive the same fixed two-attribute descriptor plus the existing evidence-derived expected tuple and canonical path; a partial or mismatched return remains a checkpoint.
  </behavior>
  <action>
    Add RED assertions to the two existing Vue test files. Use distinct title, code, and ID values so each test proves `data-phase13-item-code` and `data-phase13-item-id` belong to the same code-rendering element, while a cross-pair lookup does not identify the target. Retain the documented code-only Viewer link and confirm that item IDs do not enter route/query values. Cover the existing Viewer loading, error, and no-player cases without a synthetic identity marker.

    Extend the existing local and remote observer tests to require a closed tuple-attribute descriptor on every root-IAB callback and to preserve the current exact returned code/id mismatch checkpoint behavior. Test doubles may return only status, code, and ID facts. Do not use browser automation, cookies, profiles, headers, direct origins, external endpoints, evidence paths, run IDs, or live commands. This task implements D-01, D-03, D-04, D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter dashboard test --run src/views/__test__/Movies.test.ts; if ($LASTEXITCODE -eq 0) { throw 'Dashboard tuple tests must be red before the marker exists.' }; pnpm --filter @starye/movie-app test --run src/views/__tests__/MovieDetail.dom-contract.test.ts; if ($LASTEXITCODE -eq 0) { throw 'Viewer tuple tests must be red before the marker exists.' }; pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts; if ($LASTEXITCODE -eq 0) { throw 'Observer descriptor tests must be red before the input changes.' }</automated>
  </verify>
  <acceptance_criteria>
    - All three focused test commands fail against the current source for the intended absent tuple marker or descriptor, not for service availability.
    - Tests bind code and ID on one rendered item rather than proving separately found fields or title text.
    - Tests introduce no data-chain execution surface or secret-shaped fixture value.
  </acceptance_criteria>
  <done>Focused RED tests specify the exact real-surface tuple and root-IAB input contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the scoped tuple and pass its closed descriptor to root-IAB observation</name>
  <files>apps/dashboard/src/views/Movies.vue, apps/dashboard/src/views/__test__/Movies.test.ts, apps/movie-app/src/views/MovieDetail.vue, apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts, scripts/data-chain-surface-observation.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts, .planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md</files>
  <read_first>
    - apps/dashboard/src/views/Movies.vue
    - apps/dashboard/src/views/__test__/Movies.test.ts
    - apps/movie-app/src/views/MovieDetail.vue
    - apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
    - scripts/data-chain-surface-observation.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-72-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
  </read_first>
  <behavior>
    - The Dashboard `cell-code` and Viewer detail code marker each render the existing code text and carry the same fixed code/id attribute names from their own loaded movie object.
    - captureSurface supplies the root-IAB port a repository-owned descriptor for those two attribute names on both Dashboard and Viewer requests, while normalizer/equality behavior stays exact and fail-closed.
    - Local and selected-target tests retain their existing canonical bases, root-IAB-only ownership, Dashboard-before-Viewer ordering, and first-checkpoint stop.
  </behavior>
  <action>
    Before changing `RootIabSurfaceObservationInput` or `captureSurface`, run GitNexus upstream impact analysis for both symbols with file-path hints; record direct callers, affected flows, and risk in `13-76-SUMMARY.md`. If either report is HIGH or CRITICAL, present the blast-radius warning and wait for acknowledgement before editing. After the changes, run GitNexus detect-changes before committing and preserve every unrelated dirty worktree file.

    Add `data-phase13-item-code` beside the existing `data-phase13-item-id` on the Dashboard `cell-code` span and Viewer detail code span, binding both values from that surface's already-loaded movie object. Add one readonly repository constant/type for those two attribute names and include it in `RootIabSurfaceObservationInput`; have `captureSurface` pass the descriptor for both ordered requests. The root-IAB adapter reads the named real-surface attributes and returns only the existing allowlisted status/code/id result. Retain existing result normalization and strict equality; do not have the runner parse browser DOM, accept a caller selector, or introduce an alternate success path.

    Preserve component visual tokens, table selection/sorting/filtering/edit behavior, Viewer loading/error/player behavior, Dashboard auth/role guard, Gateway/canonical routes, evidence JSON/Markdown parity, and first-checkpoint freeze. Do not add a smoke action, retry control, route/query value, API endpoint, fixture/crawler operation, browser adapter, session storage, provider call, preflight, handoff, verifier, run allocation, or history/evidence mutation. This task implements D-01 through D-07.
  </action>
  <verify>
    <automated>pnpm --filter dashboard test --run src/views/__test__/Movies.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter @starye/movie-app test --run src/views/__tests__/MovieDetail.dom-contract.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter dashboard type-check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter @starye/movie-app exec vue-tsc --noEmit; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter @starye/config type-check</automated>
  </verify>
  <acceptance_criteria>
    - Both real code elements expose exactly one matching code/id pair using the existing `data-phase13-*` naming convention.
    - Both local and remote root-IAB callbacks receive the closed descriptor and evidence-derived expected tuple; a partial or mismatched result remains the existing first checkpoint.
    - Focused UI and observer suites plus all three type checks pass without a run ID, smoke execution, evidence write, provider interaction, or frozen-carrier operation.
    - The summary contains only changed symbols, GitNexus scope, validation results, and the absence of execution operations.
  </acceptance_criteria>
  <done>The authenticated root-IAB has a stable real-surface source for its exact tuple while all existing Gateway, security, ordering, and frozen-history boundaries remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Dashboard/Viewer rendered DOM to root-IAB | A privileged authenticated Browser reads only visible code/id metadata from the canonical surface. |
| Root-IAB result to evidence core | The root port can return allowlisted status/code/id facts; repository-owned expected tuple and route remain outside its control. |
| Pending evidence to browser receipt logic | Existing `captureSurface` decides whether exact returned tuple facts may append a receipt. |
| Frozen carrier history to repair code | p13-73 and older evidence remain context only and receive no operation or rewrite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-76-01 | Spoofing | Dashboard/Viewer identity marker | high | mitigate | Bind code and ID on the same visible code element and require the exact evidence pair rather than a title, route, or detached field. |
| T-13-76-02 | Tampering | Root-IAB observation input | high | mitigate | Pass a repository-owned readonly descriptor, expected tuple, and derived canonical route; keep caller selectors and identity claims out of the interface. |
| T-13-76-03 | Information disclosure | DOM contract and observer result | medium | mitigate | Limit values to existing non-secret code/ID and retain exclusions for sessions, cookies, headers, profiles, provider data, and endpoints. |
| T-13-76-04 | Repudiation | Browser observation evidence | high | mitigate | Preserve existing exact equality, receipt ordering, JSON/Markdown parity, and first-checkpoint freeze; execute no carrier in this repair. |
| T-13-76-05 | Elevation of privilege | Dashboard route | medium | mitigate | Retain existing real auth/role guard and root-IAB-only ownership; add no fallback session, action, API, or mutation path. |
| T-13-76-SC | Tampering | Package installation | low | accept | No package-manager operation is in scope; existing Vue, Vitest, and TypeScript tooling only. |
</threat_model>

<verification>
- Complete the RED test task, then run the focused Dashboard, Viewer, local-observer, and remote-observer suites after implementation.
- Run Dashboard, Movie App, and Config type checks after the source changes.
- Confirm no test or implementation step creates a carrier, invokes an observer, reads/writes live execution evidence, starts a provider command, or operates frozen history.
- Before commit, use GitNexus detect-changes and keep staging limited to the listed source, test, and summary files.
</verification>

<success_criteria>
- The real Dashboard Movies row and Viewer detail code marker each expose the exact non-secret itemCode/itemId tuple on one element.
- Root-IAB observation is directed to read only those fixed attributes and remains unable to turn an ambiguous tuple into a passed receipt.
- All focused tests/type checks pass, with p13-73 and every historical carrier still unmodified and no new run ID.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md` when done.
</output>
