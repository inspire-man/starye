---
phase: 13-full-chain-data-smoke
plan: "09"
type: execute
wave: 8
depends_on: [13-08]
files_modified:
  # Conditional outputs: Task 1 failure modifies no repository file; local checkpoint writes only the local pair; remote execution writes both pairs; SUMMARY requires Task 1 pass plus actual Task 2 execution.
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/local.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/local.md"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/remote.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/remote.md"
  - ".planning/phases/13-full-chain-data-smoke/13-09-SUMMARY.md"
autonomous: false
gap_closure: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Authorized starye-org selected-target context
    why: The official read-only live preflight must prove access to the mapped D1, R2, KV, API Worker, and Gateway Worker resources before any fresh remote mutation.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: Authorized selected-target operator context
      - name: CLOUDFLARE_ACCOUNT_ID
        source: Authorized selected-target operator context
      - name: CRAWLER_SECRET
        source: Authorized selected-target operator context
  - service: Persistent Codex in-app browser session
    why: Dashboard and viewer receipts require the already-authorized persistent browser accepted by Plan 13-08.
    dashboard_config:
      - task: Keep the local Gateway and selected-target canonical Dashboard sessions authorized for Movies access.
        location: Local http://localhost:8080 and repository-resolved selected canonical origin
must_haves:
  truths:
    - "D-05: the exact official selected-target live preflight exits successfully before Task 2 or any fresh remote mutation begins, including passing the R2 starye-media and gateway-worker starye-gateway read-only checks that blocked Attempt E."
    - "D-03/D-04: one new opaque run id creates fresh terminal local evidence first; local D1 establishes the local itemId, which stays consistent across local D1/API/Dashboard/viewer receipts, while remote mode reuses exactly the targetId, runId, and deterministic itemCode but not the local itemId; local browser evidence uses only http://localhost:8080."
    - "D-02/D-04: the bounded remote path requires the closed fixture child to succeed, then one count-one D1 `remote_fixture_snapshot` receipt independently establishes the remote itemId, followed by one selected-canonical API receipt and authorized Dashboard-before-viewer receipts that all match that remote itemId; the remote itemId may differ from the local itemId."
    - "D-06/D-07: a Task 1 preflight failure creates no repository artifact and leaves the plan paused; after Task 2 actually starts, every failed smoke gate persists a new immutable, non-secret checkpoint and never synthesizes success; Phase 13 completes only when both exact artifact verifiers report terminal_passed with provesExternalChain true."
    - "Attempts A-E and every existing untracked evidence pair remain immutable; when Task 2 executes, Plan 13-09 writes only to a newly proven-absent evidence directory and never appends to or overwrites an earlier attempt."
  artifacts:
    - "After Task 1 passes and Task 2 local smoke executes: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/local.json"
    - "After Task 1 passes and Task 2 local smoke executes: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/local.md"
    - "Only after local terminal proof permits remote smoke: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/remote.json"
    - "Only after local terminal proof permits remote smoke: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/remote.md"
    - "Only after Task 1 passes and Task 2 actually executes: .planning/phases/13-full-chain-data-smoke/13-09-SUMMARY.md"
  key_links:
    - "authorized selected-target context -> exact official live preflight -> permission to start one fresh local/remote smoke attempt"
    - "new terminal local pair -> exact shared targetId/runId/deterministic-itemCode remote eligibility -> closed fixture child succeeds -> count-one D1 remote_fixture_snapshot receipt establishes remote itemId -> selected-canonical API"
    - "remote D1-established tuple -> persistent Codex IAB adapter -> Dashboard receipt -> viewer receipt -> per-mode exact artifact verifiers plus shared target/run/code correlation"
  prohibitions:
    - "Do not modify, delete, stage, or reuse Attempts A-E or any of their untracked evidence."
    - "Do not prescribe or run ad hoc Wrangler, SQL, deploy, crawler, provider, permission, secret, schema, or rollback mutation commands."
    - "Do not write secret values, cookies, headers, raw provider endpoints, raw prepared context, or direct application ports into plans, logs, summaries, or evidence."
    - "Do not describe a checkpoint artifact as independently proving zero provider side effects; it proves only its recorded non-success state and absence of success receipts."
    - "Do not add schema changes or run schema push; schema push is not applicable to this execution/evidence-only gap plan."
---

<objective>
Close the two remaining Phase 13 production gaps by first establishing an authorized selected-target context that passes the official live preflight, then executing one fresh bounded local-to-remote proof through provider-backed D1, canonical API, authorized Dashboard, and viewer.

Purpose: Attempt E already proves an internally consistent local tuple but stops at `remote_preflight/target_preflight_unmet`; this additive plan must produce fresh production receipts or a new honest checkpoint without altering Attempts A-E.

Output: conditional run-scoped evidence and summary only after Task 1 passes: a local checkpoint produces only the local pair, while local terminal proof followed by remote execution produces both pairs; `13-09-SUMMARY.md` is created only after Task 2 actually executes. A Task 1 failure creates no repository artifact and leaves this plan paused. This plan is execution/evidence-only: it creates no code symbols, makes no schema change, and schema push is not applicable.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-07-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-08-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
@scripts/data-chain-smoke.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@scripts/target-profile.ts
</context>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | Authorized selected-target account/resource context | Passing official live preflight; on failure, only a non-secret terminal classification and a paused plan, with no repository artifact | First and blocking; Task 2 is forbidden while any read-only live check is red. |
| 2 | Task 1 passed preflight, a freshly proven-absent evidence path, existing hardened runners, persistent authorized Codex IAB | Local pair for any executed local smoke; remote pair only after local terminal proof permits remote smoke; summary only after Task 2 actually executes | Second; local terminal proof gates the matching remote run. |
</dependency_graph>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Establish authorized selected-target access and pass the official live preflight</name>
  <files>None; this gate changes no repository, schema, evidence, provider resource, permission, or secret value.</files>
  <read_first>
    - AGENTS.md
    - .planning/STATE.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
    - scripts/target-profile.ts
  </read_first>
  <what-built>The repository already has the canonical explicit-target, credential-key, account/resource ownership, and read-only live preflight. Attempt E failed only the R2 `starye-media` and gateway-worker `starye-gateway` live checks.</what-built>
  <how-to-verify>Activate an existing authorized `starye-org` operator context through the established secure mechanism, without placing values in Git, evidence, logs, or the plan. Then run exactly `pnpm target-profile preflight --target starye-org --scope remote --command smoke --ci-environment starye-org --live`. Resume only when it exits successfully and every mapped live check passes.</how-to-verify>
  <action>Human action is limited to making the authorized selected-target context available; do not prescribe account, provider, permission, token, secret, deploy, resource, or Wrangler mutation commands. After the context is available, execute only the exact official command above as the automated gate. Per D-05, Task 2 and every fresh remote mutation remain forbidden unless it passes. If it fails, stop immediately, report only the command identity, exit classification, and non-secret failed gate categories in the active terminal/session, and leave the plan paused. Do not create `13-09-SUMMARY.md`, any evidence pair, or any other repository artifact; do not mark Task 1, Plan 13-09, or Phase 13 complete; do not change provider state or infer production success. Wait for a newly authorized context, then re-execute the same exact preflight command before reconsidering Task 2.</action>
  <acceptance_criteria>
    - The exact command exits successfully in the authorized `starye-org` context with all D1, R2, KV, API Worker, and Gateway Worker read-only live checks passed.
    - R2 `starye-media` and gateway-worker `starye-gateway` no longer appear as failed live checks.
    - No secret value or raw prepared/provider context is printed into a tracked file, summary, or evidence artifact.
    - On failure, no summary, evidence, or other repository artifact is created; the plan remains paused, Task 2 is not entered, and Attempts A-E remain byte-for-byte unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm target-profile preflight --target starye-org --scope remote --command smoke --ci-environment starye-org --live</automated>
  </verify>
  <resume-signal>Type "preflight-passed" only after the exact command exits successfully. Otherwise report the non-secret failed gate categories, leave the plan paused with no repository artifact, establish a new authorized context, and rerun the exact command.</resume-signal>
  <done>The official selected-target live preflight is green and authorizes one fresh bounded smoke attempt; any other result remains a blocking checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute one fresh shared-correlation local and selected-target smoke</name>
  <files>Conditional: local execution writes .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;new-run-id&gt;/local.json and local.md; remote.json and remote.md exist only after local terminal proof permits remote execution; 13-09-SUMMARY.md exists only after Task 1 passes and Task 2 actually executes.</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-08-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
    - scripts/data-chain-smoke.ts
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <what-built>The hardened runner already enforces one deterministic item, terminal local prerequisite plus shared targetId/runId/itemCode remote eligibility, explicit selected-target preflight, closed provider children, per-mode count-one D1 correlation, canonical API paths, provenance receipts, and artifact-only verification. Local D1 and selected-target D1 independently establish their mode's itemId. The accepted Plan 13-08 Codex IAB adapter supplies live navigation only while repository core retains evidence ownership.</what-built>
  <how-to-verify>Create one new opaque `p13-09-*` run id. Before the first smoke command, require `Test-Path -LiteralPath` for its exact run directory to return false; the runner itself does not guarantee collision rejection. Run local mode with that id, use the accepted persistent Codex IAB adapter to observe local Dashboard then viewer through `http://localhost:8080`, and require the exact local verifier to return `terminal_passed` with `provesExternalChain: true` for one internally consistent local D1/API/Dashboard/viewer tuple. Only then run remote mode with the same targetId, runId, and deterministic itemCode. Require the closed fixture child to succeed, then require the count-one D1 `remote_fixture_snapshot` receipt to establish the remote itemId, followed by the selected-canonical API receipt and the same adapter's Dashboard-then-viewer receipts matching that remote itemId. Evidence starts with the D1 receipt after child success rather than a separate child-level evidence row, and the remote itemId may differ from the local itemId. Finally run both exact artifact verifier commands listed below; together they must validate each mode's internal tuple and the shared target/run/code correlation, never cross-environment itemId equality.</how-to-verify>
  <action>After Task 1 passes, generate one new opaque run id and form the fixed directory `.planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>/`. Before invoking any smoke command, execute the exact Windows path gate `Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/evidence/starye-org/<new-run-id>'` and require the result to be `False`. The runner does not guarantee collision rejection: if the gate returns `True`, do not invoke it, choose another opaque run id, and repeat the gate until the exact path is absent. Never select, copy, append to, or overwrite Attempts A-E. Execute `pnpm smoke:data-chain -- --mode local --target starye-org --run-id <new-run-id>`. If it reaches a pending tuple, invoke exported repository core `observeDataChainSurfaces()` from the persistent Codex IAB runtime with exactly the accepted `observeSurface` dependency from Plan 13-08: repository core must retain `read`, `write`, `evidenceRoot`, `resolveTarget`, `now`, tuple derivation, canonical base resolution, Dashboard-before-viewer ordering, receipt construction, rendering, and persistence. The adapter navigates only the core-derived URL, requires the exact final origin/path and the single tuple marker, and returns unavailable rather than accepting caller status or evidence input. Local navigation is restricted to `http://localhost:8080/dashboard/movies` followed by `http://localhost:8080/movie/<item-code>`. Run the exact local artifact verifier and proceed only when it reports terminal proof with the local D1-established itemId consistent across local D1/API/Dashboard/viewer receipts. Then execute `pnpm smoke:data-chain -- --mode remote --target starye-org --run-id <new-run-id>`; the remote run must reuse the same targetId, runId, and deterministic itemCode, but it must neither copy the local itemId nor require UUID equality across environments. Rely only on the runner's existing explicit-target preflight and closed fixed entries, with no ad hoc provider command. Require the closed fixture child to succeed; it does not emit an independent fixture receipt. Before browser observation, require a count-one read-only D1 `remote_fixture_snapshot` receipt whose itemCode matches the shared deterministic code and whose itemId becomes the remote itemId, and a selected-canonical API receipt matching that remote code/id. The remote itemId may differ from the local itemId, but it must match every remote D1/API/Dashboard/viewer receipt exactly. Use the accepted persistent Codex IAB adapter for repository-resolved production origin, Dashboard first and `/movie/<item-code>` viewer second; no caller supplies the origin, status, tuple, surface order, cookie, receipt, or artifact content. Finish by running both exact artifact verifiers so each mode's target/run/code/id tuple is internally consistent and the local/remote artifacts share target/run/code without requiring cross-environment itemId equality. If local smoke checkpoints, preserve only its local JSON/Markdown pair and do not start remote mode. If any later provider, D1, API, browser, session, route, correlation, receipt, or verifier gate fails, stop at once, preserve the newly created local and remote pairs as an immutable honest checkpoint, and do not retry or promote that run. Only after Task 1 has passed and Task 2 has actually executed may the executor create `13-09-SUMMARY.md`, accurately describing whether evidence stopped at the local pair or advanced to both pairs. The checkpoint artifact alone may establish only its recorded outcome and absence of terminal success receipts; it must not be described as proof of zero provider side effects. Do not change code symbols or schema, and do not run schema push because it is not applicable.</action>
  <acceptance_criteria>
    - Before the first smoke command, `Test-Path -LiteralPath` returns `False` for the exact run directory; this explicit executor gate, not the runner, proves collision absence, and Attempts A-E remain unchanged.
    - A local checkpoint creates only local.json/local.md and no remote pair; after local terminal proof permits remote execution, a remote checkpoint or terminal result creates both local and remote JSON/Markdown pairs.
    - `13-09-SUMMARY.md` is absent after any Task 1 failure and is created only after Task 1 passes and Task 2 actually executes.
    - Local terminal evidence uses only `http://localhost:8080`; local D1 establishes its itemId, and that target/run/code/id is consistent across local D1/API and ordered authorized Dashboard-then-viewer receipts with count one.
    - Remote terminal evidence shares exactly the local targetId, runId, and deterministic itemCode; selected-target D1 independently establishes the remote itemId, which may differ from the local itemId and must match the remote D1/API and authorized Dashboard-before-viewer receipts from the persistent Codex IAB adapter.
    - Both exact verifiers return `terminal_passed` and `provesExternalChain: true`, validate each mode's internally consistent tuple, and preserve the shared target/run/code correlation without requiring cross-environment itemId equality before Phase 13 can be called complete.
    - Any failed gate produces a new immutable checkpoint with `provesExternalChain: false`, no synthesized success, no secret value, and no claim that the artifact independently proves zero provider side effects.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;new-run-id&gt;') { throw 'Evidence run directory already exists.' }"</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id &lt;new-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id &lt;new-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
  </verify>
  <done>The fresh run either proves the complete selected-target external chain with per-mode exact tuple-bound receipts and shared target/run/code correlation, or remains an immutable non-success checkpoint that leaves the Phase 13 goal open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authorized operator context -> official live preflight | Key presence is not enough; account/resource read access must pass before fresh remote mutation. |
| Terminal local pair -> selected-target runner | Remote work may consume only the new terminal local targetId/runId/deterministic-itemCode correlation; the local itemId stays local and selected-target D1 independently establishes the remote itemId. |
| Provider/browser observations -> evidence writer | External facts must become allowlisted, mode-internal tuple-bound receipts without secrets, raw endpoints, cookies, or caller status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-38 | Spoofing | selected-target authorization | critical | mitigate | Blocking human-action gate plus the exact official explicit-target live preflight before Task 2. |
| T-13-39 | Tampering | Attempts A-E and fresh evidence | high | mitigate | New collision-free run directory, immutable failed runs, per-mode exact tuple validators plus shared target/run/code correlation, and no append/overwrite of prior attempts. |
| T-13-40 | Information Disclosure | operator context and receipts | critical | mitigate | Process-only secret values and allowlisted evidence containing only key names, paths, mode-owned tuple, result, and integrity metadata. |
| T-13-41 | Elevation of Privilege | remote mutation path | critical | mitigate | Existing closed one-item runner only; prohibit ad hoc Wrangler, SQL, crawler, provider, deploy, permission, or schema mutation commands. |
| T-13-42 | Repudiation | production success claim | high | mitigate | Closed fixture-child success plus D1/API/browser receipts and final exact local and remote artifact verification; checkpoint artifacts remain explicitly non-success. |
| T-13-SC | Tampering | package supply chain | low | accept | No package install or dependency change occurs in this execution/evidence-only plan. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | - | Prove the selected production target through provider-backed D1, canonical API, authorized Dashboard, and viewer. | Tasks 1-2 | COVERED |
| REQ | DATA-04 | Correlate the selected-target D1-established item across remote D1, API, and admin state while retaining the shared target/run/code link to local evidence. | Task 2 | COVERED |
| REQ | DATA-05 | Validate the item through an authorized Dashboard session. | Task 2 | COVERED |
| REQ | DATA-06 | View the same item through the selected canonical Gateway. | Task 2 | COVERED |
| REQ | DATA-07 | Capture fresh local and production non-secret evidence. | Tasks 1-2 | COVERED |
| REQ | TEST-05 | Use repeatable smoke commands and exact artifact verification. | Task 2 | COVERED |
| RESEARCH | - | Explicit live preflight, closed fixture/snapshot runner, Gateway/canonical-only browser proof, and checkpoint on unavailable external access. | Tasks 1-2 | COVERED |
| CONTEXT | D-01 | The current canonical contract requires one deterministic item; no batch compatibility is reintroduced. | Task 2 | COVERED |
| CONTEXT | D-02 | Only the bounded fixture adapter may write. | Task 2 | COVERED |
| CONTEXT | D-03 | Local terminal proof precedes the remote run sharing its targetId, runId, and deterministic itemCode. | Task 2 | COVERED |
| CONTEXT | D-04 | Local and remote share targetId, runId, and deterministic itemCode and use canonical browser routes; each D1 independently establishes the itemId used consistently within that mode. | Task 2 | COVERED |
| CONTEXT | D-05 | Official live preflight and ownership checks block remote mutation. | Task 1 | COVERED |
| CONTEXT | D-06 | Evidence is repeatable, allowlisted, non-secret, and checkpoint-aware. | Tasks 1-2 | COVERED |
| CONTEXT | D-07 | Real provider execution remains Phase 13 scope; Phase 14 work is excluded. | Task 2 | COVERED |

Deferred Phase 14 literal cleanup, RUNBOOK stabilization, and final requirement-to-evidence mapping are excluded and are not source gaps.
</source_audit>

<verification>
- Task 1 must pass the exact official live preflight before Task 2 starts; a failed result leaves the plan paused with no evidence, summary, or other new repository artifact.
- Before the first smoke command, the exact run-directory `Test-Path -LiteralPath` gate must return `False`; runner execution is not accepted as collision protection.
- Task 2 must use a previously nonexistent run directory, preserve Attempts A-E, and run local before remote with exactly the same targetId, runId, and deterministic itemCode; local and selected-target D1 independently establish their mode's itemId.
- Final acceptance requires both exact artifact verifier commands to return terminal proof with `provesExternalChain: true`, each mode's D1/API/Dashboard/viewer receipts to match its own itemId, and the two artifacts to share target/run/code without requiring UUID equality; schema-valid checkpoint output alone is non-success.
- This plan modifies no code symbol, dependency, target profile, provider permission, schema, or canonical documentation. GitNexus symbol impact/detect-changes is not applicable because execution creates evidence only and no commit is authorized by this plan.
</verification>

<success_criteria>
- **Task 1 completion:** the exact official live preflight exits successfully. A failed invocation creates no repository artifact, leaves the plan paused, and does not authorize Task 2 or summary creation.
- **Plan 13-09 execution completion:** after Task 1 passes, Task 2 actually executes against a path proven absent. A local checkpoint leaves only the immutable local pair plus the execution summary; local terminal proof followed by remote checkpoint or terminal execution leaves immutable local and remote pairs plus the execution summary. This records the attempt accurately but does not by itself complete Phase 13.
- **Phase 13 goal completion:** both exact verifiers report `terminal_passed` and `provesExternalChain: true`; the closed fixture child succeeded, the local pair is internally consistent on its local D1-established itemId, and the remote pair shares target/run/code with local and contains the count-one D1 `remote_fixture_snapshot` receipt, selected-canonical API receipt, authorized Dashboard receipt, and ordered canonical viewer receipt all bound to its independently established remote itemId.
- Any checkpoint, including a valid remote artifact with `provesExternalChain: false`, leaves the Phase 13 goal and DATA-04 through DATA-07 open.
</success_criteria>

## Artifacts this phase produces

- Task 1 failure produces no new repository artifact and leaves the plan paused. After Task 1 passes and Task 2 local smoke executes, a local checkpoint leaves only local.json/local.md; after local terminal proof permits remote execution, a remote checkpoint or terminal result leaves both local and remote JSON/Markdown pairs sharing target/run/code while each pair retains its own D1-established, internally consistent itemId.
- One `13-09-SUMMARY.md` is created only after Task 1 passes and Task 2 actually executes; it distinguishes local-only checkpoint, remote checkpoint, and Phase goal completion without storing secret values.
- No code symbols, schema files, migrations, dependencies, target profiles, or provider resources are created or modified. Schema push is not applicable.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-09-SUMMARY.md` only after Task 1 passes and Task 2 actually executes. If Task 1 fails, create no summary or evidence and leave the plan paused. If local smoke checkpoints, summarize the local pair only; if local terminal proof permits remote execution, summarize the local and remote pairs. Do not commit evidence or planning artifacts unless a later explicit instruction authorizes it.
</output>
