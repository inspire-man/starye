---
phase: 13-full-chain-data-smoke
plan: "14"
type: execute
wave: 13
depends_on: [13-13]
files_modified:
  - "scripts/data-chain-handoff.ts"
  - "packages/crawler/scripts/data-chain-cli.mjs"
  - "package.json"
  - "scripts/tsconfig.phase13-handoff.json"
  - "packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts"
  - "packages/config/src/deployment-target/__tests__/data-chain-cli-process.test.ts"
autonomous: true
gap_closure: true
requirements:
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "Production `parseDataChainSmokeArgs`, `DATA_CHAIN_EVIDENCE_ROOT`, existing runner/verifier fixed-root parsers, and the verifier fixed-root guard retain their post-13-13 semantics: handoff adds a dedicated import-safe `parseDataChainHandoffArgs` and no env var, CLI flag, config, or alternate evidence-root override. Plan 13-14 does not modify or claim ownership of those 13-13 symbols."
    - "The handoff entry's static prelude contains only pure argument/target-resolution code and type-only contracts: it does not statically import `node:path`, `node:fs`, `node:fs/promises`, `scripts/data-chain-smoke.ts`, `scripts/verify-data-chain-smoke.ts`, `scripts/data-chain-surface-observation.ts`, or the data-chain evidence owner. Those modules are dynamically/lazily loaded only after canonical target/profile validation."
    - "`parseDataChainHandoffArgs` tokenizes only `--mode`, `--target`, and `--run-id` without filesystem/path calls, rejects every occurrence of `--evidence-dir` (canonical or otherwise), validates the closed mode/runId contract, then `runDataChainHandoffCli` resolves target through `resolveTargetProfile`/`isTargetProfileId` before any path-module call, dependency factory, storage construction, pair inspection, directory creation, reservation, runner, verifier, or provider boundary."
    - "Unknown, traversal-shaped, POSIX-absolute, Windows drive-absolute, UNC, mixed-separator, or encoded-separator target input returns binary 1 with exactly one closed `invalid_target` refusal; stdout/stderr/evidence contain no raw input, normalized rejected value, path, error, stack, endpoint, or secret."
    - "Direct imported `runDataChainHandoffCore` callers cannot supply raw target text, an evidence root, or any JSON/Markdown/marker bundle: its request accepts only a validated `TargetResolution`/tracked target ID, validated runId, and closed mode, defensively confirms tracked membership/profile agreement, and derives identity/paths internally."
    - "The real handoff CLI rejects canonical, noncanonical, duplicate, value-less, or encoded `--evidence-dir` attempts with binary 1 before any path-module call, injected dependency factory, reservation storage, runner, verifier, or provider boundary; runner/verifier parsers keep their existing fixed-root behavior."
    - "After canonical target resolution and closed mode/runId validation, one `deriveDataChainHandoffPaths` owner uses `DATA_CHAIN_EVIDENCE_ROOT` plus the validated targetId/runId/mode only. It applies `path.posix` and `path.win32` lexical guards before native resolution, then native `path.resolve`/`path.relative` strict-descendant and exact-equality checks for `<root>/<target>/<run>/<mode>.json`, `<mode>.md`, and `<mode>.attempt`, rejecting equality, parent/absolute/drive/UNC/backslash/separator/case/volume escape, aliasing, same-directory shadows, swapped peers, and wrong-mode markers before storage/`lstat`/`mkdir`/`open`."
    - "The core creates and freezes one readonly exact `DataChainHandoffPaths` bundle and passes it into logical storage operations; no dependency or test adapter may return, replace, mutate, or override paths. Production operates on those exact real paths, while tests may map only inside an adapter after asserting the received bundle is exactly the internally derived canonical bundle."
    - "Before reservation or any runner/verifier/provider/artifact read/evidence write, the core classifies neither, both, JSON-only, Markdown-only, JSON-inspection-error, and Markdown-inspection-error pair states; every pair collision or non-ENOENT inspection failure returns binary 1 with one closed refusal and zero reservation/downstream calls."
    - "Only the neither-exists pair state may atomically reserve its mode by exclusive create (`open(..., 'wx')` or an equivalent no-clobber primitive); successful creation is the ownership linearization point and occurs before every runner/provider/verifier call."
    - "A pre-existing or concurrent-loser marker returns binary 1 with only `attempt_already_reserved`; any non-EEXIST reservation failure returns binary 1 with only `attempt_reservation_unavailable`; both paths invoke runner/verifier/provider zero times and disclose no raw path or error."
    - "The marker is never deleted, overwritten, repaired, reused, or backfilled, including after checkpoint, failure, or crash; marker-only state blocks re-entry but never counts as exact-verifier success."
    - "After owning a remote marker, the core invokes one closed official selected-target read-only live-preflight dependency before the runner. A red preflight persists and read-only verifies exactly one redacted `pre_ingest/remote_preflight/target_preflight_unmet` JSON/Markdown pair with `itemId:null`, invokes fixture/D1/API/browser zero times, keeps the marker, returns binary 1, and never retries; pair write/verification failure also keeps the marker and returns 1 without fabricating success."
    - "Only a green outer remote preflight immediately invokes the sole remote runner; the runner's existing internal defensive gates remain unchanged. Local ownership invokes its runner directly. In either mode the core invokes the pair-focused verifier once after the produced pair and returns 0 only for resolved_pending_observation/pending with a non-empty itemId and provesExternalChain false."
    - "The actual root `pnpm run smoke:data-chain` and `pnpm run smoke:data-chain:verify` process harness preserves injected CLI results 0, 1, and 2 unchanged; `pnpm run smoke:data-chain:handoff` exposes only 0 or 1 and never treats outer 1 as pending."
    - "Machine JSON plus the exact persisted artifact tuple is authoritative; outer package status alone can neither accept nor reject a handoff."
    - "D-06/D-07: malformed output, mismatch, incompatible lifecycle state, collision, inspection failure, reservation loss, or crash re-entry fails closed without retry or disclosure; tests never allocate, read, rewrite, stage, or commit a Phase 13 evidence run."
  artifacts:
    - path: "scripts/data-chain-handoff.ts"
      provides: "True no-path/no-data-chain static prelude, target-first lazy path/evidence owner loading, validated tracked-target request, sole exact fixed-root derivation, readonly non-redirectable storage contract, persistent atomic reservation, official remote-preflight checkpoint writer/verifier, production dependency constructor, and binary CLI"
      exports: ["DataChainHandoffParsedArgs", "ResolvedDataChainHandoffRequest", "DataChainHandoffPaths", "DataChainRemotePreflightStatus", "DataChainHandoffResult", "DataChainHandoffStorageAdapter", "DataChainHandoffDependencies", "DataChainHandoffCliDependencies", "parseDataChainHandoffArgs", "validateResolvedDataChainHandoffRequest", "assertExactDataChainHandoffPaths", "deriveDataChainHandoffPaths", "invokeOfficialDataChainRemotePreflight", "writeDataChainRemotePreflightCheckpointPair", "runDataChainHandoffCore", "createProductionDataChainHandoffDependencies", "runDataChainHandoffCli"]
    - path: "packages/crawler/scripts/data-chain-cli.mjs"
      provides: "Closed package-local tsx bootstrap for run, verify, and handoff root commands"
      exports: ["dispatchDataChainCli"]
    - path: "package.json"
      provides: "Direct Node smoke:data-chain, smoke:data-chain:verify, and smoke:data-chain:handoff scripts without nested pnpm"
    - path: "packages/config/src/deployment-target/__tests__/data-chain-cli-process.test.ts"
      provides: "Actual root pnpm-script exit propagation through a test-process-only controlled module boundary that cannot configure production evidence roots"
    - path: "scripts/tsconfig.phase13-handoff.json"
      provides: "Narrow no-emit TypeScript project for the handoff and fixed-root launcher/verifier root scripts"
    - path: "packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts"
      provides: "Process/import-hook proof of invalid-input zero lazy loads/calls and valid-target resolve-before-load/core entry, plus validated-core type/runtime boundary, foreign-syntax/exact-bundle/non-redirection containment, pair-state, remote-preflight checkpoint, atomic reservation, concurrency, crash-reentry, marker-only non-success, and valid starye-org matrices through createTempMappedDataChainHandoffStorage"
  key_links:
    - from: "scripts/data-chain-handoff.ts:runDataChainHandoffCli"
      to: "packages/config/src/deployment-target/target-resolver.ts:resolveTargetProfile"
      via: "resolve parsed target before dependency factory; catch every resolution failure into the single non-reflective invalid_target refusal"
      pattern: "resolveTargetProfile|invalid_target"
    - from: "scripts/data-chain-handoff.ts:parseDataChainHandoffArgs"
      to: "scripts/data-chain-handoff.ts:runDataChainHandoffCli -> resolveTargetProfile/isTargetProfileId"
      via: "tokenize and validate mode/run/target text without path/filesystem work; reject every evidence-dir flag; resolve the tracked target before factory or path derivation"
      pattern: "parseDataChainHandoffArgs|invalid_target"
    - from: "scripts/data-chain-handoff.ts:runDataChainHandoffCore"
      to: "DATA_CHAIN_EVIDENCE_ROOT + deriveDataChainHandoffPaths"
      via: "accept only the resolved tracked target/mode/runId, apply POSIX+Win32 lexical guards, derive and freeze the one exact root/target/run/pair/marker bundle, then enforce host strict-descendant plus exact equality; no caller or dependency supplies paths"
      pattern: "deriveDataChainHandoffPaths|DATA_CHAIN_EVIDENCE_ROOT"
    - from: "canonical logical JSON/Markdown paths"
      to: "DataChainHandoffStorageAdapter"
      via: "adapter methods receive the readonly frozen exact bundle and return only logical state/content/status, never replacement paths; production uses literal paths and tests may only observe/map after exact-bundle assertion"
      pattern: "DataChainHandoffPaths|inspectPair"
    - from: "scripts/data-chain-handoff.ts:runDataChainHandoffCore"
      to: "<target>/<runId>/local.attempt|remote.attempt"
      via: "pair validation followed by atomic exclusive creation; marker creation is the ownership linearization point before downstream work"
      pattern: "attempt_already_reserved|attempt_reservation_unavailable"
    - from: "scripts/data-chain-handoff.ts:runDataChainHandoffCore"
      to: "DataChainHandoffDependencies"
      via: "injected exact-bundle inspect/read/write/reserve logical operations, closed outer remote live preflight, runner invocation, verifier invocation, and result sink; dependencies cannot return paths"
      pattern: "evidence_pair_state_unavailable"
    - from: "scripts/data-chain-handoff.ts:runDataChainHandoffCore"
      to: "official remote live preflight -> checkpoint pair or sole remote runner"
      via: "after remote.attempt ownership invoke the exact selected target/environment read-only preflight once; unmet writes+verifies redacted checkpoint with zero provider mutation, passed immediately enters the existing defensively gated runner"
      pattern: "remote_preflight|target_preflight_unmet"
    - from: "package.json:smoke:data-chain"
      to: "scripts/data-chain-smoke.ts:runDataChainSmokeCli"
      via: "closed package-local Node/tsx launcher"
      pattern: "data-chain-cli.mjs run"
    - from: "package.json:smoke:data-chain:verify"
      to: "scripts/verify-data-chain-smoke.ts:runVerifyDataChainSmokeCli"
      via: "same direct launcher preserving inner 0/2"
      pattern: "data-chain-cli.mjs verify"
    - from: "scripts/data-chain-handoff.ts"
      to: "runDataChainSmoke and runVerifyDataChainSmokeCli"
      via: "createProductionDataChainHandoffDependencies binds exact-bundle storage, atomic `wx` reservation, one closed official outer remote preflight, and one runner/verifier invocation; CLI uses its dedicated no-path parser"
      pattern: "resolved_pending_observation"
    - from: "scripts/data-chain-handoff.ts:runDataChainHandoffCli"
      to: "parseDataChainHandoffArgs -> resolveTargetProfile"
      via: "dedicated parser accepts no evidence-root flag and performs no path work; canonical/alternate evidence-dir and invalid target inputs reach no path/factory/storage/downstream seam"
      pattern: "--evidence-dir|invalid_target"
    - from: "data-chain-cli-process.test.ts"
      to: "actual root pnpm script names"
      via: "test-process-only module interception supplies fixed exit results; production launcher accepts no module path/env/config evidence-root override"
      pattern: "smoke:data-chain"
---

<objective>
Replace the broken nested-pnpm exit assumption with one repository-owned handoff boundary whose dedicated no-path parser resolves an exact tracked target before factory/path work, whose imported core accepts only a validated target/mode/run request and internally derives one exact fixed-root bundle, and whose exclusive remote owner records a redacted D-05 checkpoint when the official live preflight fails before provider mutation (D-05, D-06, D-07).

Purpose: the inner runner/verifier correctly use exit 2 for non-terminal evidence, but the root package scripts normalize that code to 1. Plan 13-12 therefore could reject a legitimate future pending tuple before reading its authoritative machine/artifact state.

Output: a dedicated handoff parser, validated-request core, canonical exact-path derivation owner, readonly non-redirectable storage operations, persistent atomic per-mode reservation, closed remote-preflight checkpoint writer/verifier, direct Node launchers, binary handoff semantics, pair/reservation/concurrency/crash/foreign-syntax regressions, and separate actual-root exit-propagation regressions. No data-chain run or canonical Phase 13 evidence file is used by these repair tests.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/debug/phase13-gateway-auth-timeout.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-12-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md
@package.json
@scripts/data-chain-smoke.ts
@scripts/verify-data-chain-smoke.ts
@packages/crawler/package.json
@packages/config/src/deployment-target/target-resolver.ts
@packages/config/src/deployment-target/target-profile.schema.ts
@packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts

<interfaces>
- `runDataChainSmoke(options, dependencies): Promise<{ exitCode: 0 | 2; evidence: DataChainEvidence }>` is the existing single runner owner.
- `runVerifyDataChainSmokeCli(argv, dependencies): Promise<0 | 2>` logs exactly one machine JSON line and validates the exact pair.
- `inspectDataChainSmokeVerification(options, dependencies)` returns `{ exitCode, outcome, provesExternalChain, evidence }`.
- `resolveTargetProfile(targetId: string | undefined): TargetResolution` trims surrounding whitespace, rejects missing/unknown IDs, validates the tracked profile, and returns canonical `TargetResolution.id: TargetProfileId`; `isTargetProfileId` is the tracked-map membership guard.
- Existing exact-verifier machine keys are `mode`, `target`, `runId`, `state`, `aggregate`, `outcome`, `provesExternalChain`, and optional `checkpoint`.
</interfaces>
</context>

<discovery_evidence>
- Discovery level: 0. All required runner/verifier exports and package-local `tsx` dependency already exist; no package or external library is added.
- Corrected import-only diagnosis proved package-local Node/tsx exits naturally. The failure is nested pnpm status normalization, not the TS loader or imported modules.
- The launcher must use a fixed dispatch table. It cannot accept a caller module path, free-form child command, origin, evidence root, or prepared context.
- Existing `scripts/verify-data-chain-smoke.ts` core semantics are correct and remain the verifier owner; do not duplicate its schema/parity/tuple rules.
- Existing `parseDataChainSmokeArgs` performs evidence-root `path.resolve` before canonical target resolution, so it cannot implement the handoff target-first contract. Keep it unchanged for runner/verifier and add `parseDataChainHandoffArgs`, which tokenizes/validates only mode/target/run, recognizes no evidence-root option, and performs no filesystem/path operation.
- After dedicated parsing, the CLI resolves through `resolveTargetProfile`/`isTargetProfileId` before any path-module call or dependency factory. The imported core receives the validated resolution instead of raw target input and has no resolver/path-bundle injection seam.
- `getDataChainEvidencePaths` remains an unchanged runner/verifier owner. Handoff instead owns one `deriveDataChainHandoffPaths` from fixed root plus validated target/run/mode; POSIX/Win32 lexical guards run before host-native resolution, and host strict-descendant plus exact expected-path equality prevents foreign syntax, alias/shadow, swapped peer, and wrong-marker acceptance.
- Collision/error/reservation tests call the imported production handoff core with a validated `starye-org` resolution. Their OS-temp adapter receives the frozen internally derived bundle, may map it only after exact equality assertion, and cannot return replacement paths; actual-root process tests remain limited to launcher exit propagation.
- The existing runner writer creates directories and uses ordinary `writeFile`, so exact-pair absence alone has a TOCTOU gap. The handoff must create the canonical mode marker with Node filesystem exclusive creation (`open(path, 'wx')` or equivalent) after pair validation and before runner/provider/verifier; the never-removed zero-byte marker, not ordinary pair writing, is the ownership linearization point.
- The existing remote runner already has defensive credential/live checks and checkpoint writing. Handoff adds the authoritative outer official live preflight after `remote.attempt` ownership: failure writes and artifact-verifies the closed D-05 checkpoint itself with zero runner/fixture/D1/API/browser calls; success immediately invokes the runner without removing its internal gates.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Order |
|---|---|---|---|
| 1 | Tracked-target resolver, fixed root, evidence builders, official remote preflight, existing runner/verifier, and ordinary overwrite-capable writer | Dedicated no-path handoff parser, validated-request/exact-path core, non-redirectable storage API, production dependency/CLI factory, atomic marker, redacted preflight checkpoint, pair/reservation/concurrency/crash refusal, and pending matrix | First |
| 2 | Task 1 CLI export and package-local tsx | Direct root launcher, package scripts, and process-only exit propagation matrix | Second |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the single-invocation runner-to-handoff contract</name>
  <files>scripts/data-chain-handoff.ts, scripts/tsconfig.phase13-handoff.json, packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md
    - scripts/data-chain-smoke.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/target-resolver.ts
    - packages/config/src/deployment-target/target-profile.schema.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
  </read_first>
  <behavior>
    - `parseDataChainHandoffArgs` accepts exactly one each of `--mode`, `--target`, and `--run-id`, validates the closed mode and existing runId grammar, performs no path/filesystem/dependency work, and rejects unknown/duplicate/missing flags including every canonical or noncanonical `--evidence-dir` form before any downstream spy.
    - After parsing, the real CLI resolves target through `resolveTargetProfile`, confirms `isTargetProfileId` and profile/id equality, and only then may call the dependency factory or any `path.posix`, `path.win32`, or native path function. Unknown, traversal, POSIX absolute, Windows drive, UNC, forward/backslash, encoded-separator, mixed-separator, blank, and embedded-whitespace target forms return one non-reflective `invalid_target` and binary 1.
    - `runDataChainHandoffCore` accepts `ResolvedDataChainHandoffRequest` containing closed mode, validated runId, and canonical `TargetResolution`; raw target strings, evidenceRoot, JSON/Markdown/marker paths, derivation callbacks, or caller filesystem bundles are absent from its type and runtime contract. A forged/untracked resolution fails before derivation/storage.
    - `deriveDataChainHandoffPaths` is the only path owner and always calls `assertExactDataChainHandoffPaths` on its own output before returning. The guard uses fixed `DATA_CHAIN_EVIDENCE_ROOT`, canonical targetId, validated runId, and mode; POSIX/Win32 lexical checks precede host resolution, then strict-descendant and exact expected-path equality prove root/target/run plus `<mode>.json`, `<mode>.md`, and `<mode>.attempt`. Tests may call the pure guard with hostile candidates, but core accepts no guard result or bundle input.
    - The core freezes the one derived bundle. Storage methods receive that readonly bundle and return only pair state, contents, reservation status, or write status; adapters cannot return replacement paths. Tests prove exact observed equality/Object.freeze behavior, ignore malicious extra returned path fields, and show shadow/swapped/wrong-marker files cannot redirect I/O.
    - A valid canonical `starye-org` request in each mode reaches the preserved six pair states: neither, both, JSON-only, Markdown-only, JSON inspection error, and Markdown inspection error. Every collision/error refuses before reservation/read/write/preflight/runner/verifier/provider work.
    - Only neither may exclusive-create the internally derived zero-byte marker. EEXIST/pre-existing/concurrent loser maps only to `attempt_already_reserved`; other reservation errors map only to `attempt_reservation_unavailable`. Markers remain persistent after pending/checkpoint/failure/crash and marker-only stays non-success.
    - A local owner invokes the runner once and pair-focused verifier once. Exact pending returns handoff 0; malformed/mismatched/checkpoint/terminal/verifier error returns 1 without retry or repair.
    - A remote owner invokes the closed outer official selected-target live preflight once after reservation. Production uses the exact repository target-profile remote smoke command, canonical target, resolver-mapped CI environment, `--live`, fresh allowlisted credential environment, shell false, and status-only result; tests inject only `passed|unmet`, never stderr/path/secret data.
    - On outer preflight `unmet`, core writes the exact internally derived JSON/Markdown pair as `pre_ingest/checkpoint`, surface `remote_preflight`, checkpoint `target_preflight_unmet`, `itemId:null`, then invokes the pair-focused verifier read-only once. Handoff returns 1; runner/fixture/D1/API/browser calls are zero. Write or verification failure also returns 1 with marker retained and no retry/success fabrication.
    - On outer preflight `passed`, the immediately next operation is the sole remote runner invocation. The runner's existing internal defensive preflight/credential/resource gates remain unchanged; outer preflight does not replace or weaken them. One artifact-only verifier follows the runner result.
    - Invalid/refusal/checkpoint output is allowlisted and non-reflective. Pending requires exact machine/artifact mode/target/run/state/aggregate/item identity, runner/verifier checkpoint code consistency, non-empty itemId, and `provesExternalChain:false`; outer package status alone is never authoritative.
  </behavior>
  <implementation_constraints>
    1. Inventory and upstream-impact every existing symbol before edit: `DATA_CHAIN_EVIDENCE_ROOT`, `parseDataChainSmokeArgs`, `evidencePaths`, `getDataChainEvidencePaths`, `runDataChainSmoke`, `runDataChainSmokeCli`, `runVerifyDataChainSmokeCli`, `resolveTargetProfile`, `isTargetProfileId`, `runTargetPreflight`, `createPreIngestEvidence`, `serializeDataChainEvidenceJson`, and `renderDataChainEvidenceMarkdown`. Inventory new `parseDataChainHandoffArgs`, `deriveDataChainHandoffPaths`, request/path/storage/core/preflight symbols. HIGH/CRITICAL blocks for explicit user approval; unchanged owners are consumed, not edited.
    2. Keep runner/verifier parser/path owners outside Plan 13-14 ownership. Verify the committed post-13-13 runner, verifier, and resolver through exact symbol/source anchors rather than any file-wide diff assertion. Handoff parser must reject `--evidence-dir`, `--evidence-dir=<value>`, duplicated/encoded/case variants outside its exact allowlist, and any unknown flag by closed binary 1 before target/path/factory work; it never calls a path API.
    3. Core request has no raw target or path fields. Use the CLI-resolved `TargetResolution`, defensively require tracked ID/profile agreement, then derive internally; compile-time tests use `@ts-expect-error` for raw target/evidenceRoot/path bundle calls and runtime forged-resolution tests stop before storage.
    4. Remove every derivation/bundle override seam. `deriveDataChainHandoffPaths` internally computes the bundle and calls pure `assertExactDataChainHandoffPaths`; the guard performs POSIX/Win32 lexical rejection, host strict-descendant checks, and exact string equality to recomputed root/target/run/mode files. Tests may exercise the guard directly, but core never accepts its result. Storage receives only the frozen internally derived bundle and cannot return a path.
    5. Test exact canonical bundle observation plus foreign syntax, equality, root/volume/case escape, same-directory shadow, swapped JSON/Markdown, wrong-mode marker, alias, and adapter attempted redirection. All invalid inputs fail before physical I/O/downstream; valid `starye-org` continues into six-state collision and reservation/concurrency/crash matrices.
    6. Production outer remote preflight is one closed shell-false invocation of the repository official target-profile remote smoke `--live` command using only canonical target, mapped CI environment, PATH, and named credentials. Return `passed|unmet` only and discard stdout/stderr/error objects.
    7. Build the preflight checkpoint with existing evidence owners, write both exact peers through one exact-bundle logical operation, read/verify them through the existing pair-focused verifier, and retain `remote.attempt` on every outcome. Zero runner/provider/fixture/D1/API/browser calls on unmet; no retry if write/parity/verifier fails.
    8. Immediately before every RED/GREEN/REFACTOR/task commit, run GitNexus detect-changes against the five code/test owned paths plus the narrow tsconfig and compare every existing/new parser/path/core/preflight/storage symbol plus affected execution flow to the declared Artifacts. Unexpected or HIGH/CRITICAL expansion blocks commit; rerun after hook/autofix/restage.
    9. The static handoff prelude may load only pure argument/target-resolution code and type-only contracts. It must not statically import Node path/filesystem modules, `data-chain-smoke.ts`, `verify-data-chain-smoke.ts`, `data-chain-surface-observation.ts`, or the data-chain evidence owner; load those modules dynamically only after `resolveTargetProfile` and `isTargetProfileId` confirm the canonical tracked profile.
    10. Add a process/import-hook regression that records module loads and every path/factory/storage/downstream seam. Invalid target, unknown/duplicate/value-less flags, and every evidence-dir form must exit before any lazy path/evidence module load or seam call; a valid target must show resolution first, then lazy module load, then injected-core entry. The hook is test-only and cannot become an env/config/module/root override in production.
    11. Create `scripts/tsconfig.phase13-handoff.json` with `extends: ../tsconfig.json`, `compilerOptions.noEmit: true`, `compilerOptions.composite: false`, `compilerOptions.allowImportingTsExtensions: true`, and an explicit `files` list containing only `data-chain-handoff.ts`, `data-chain-smoke.ts`, and `verify-data-chain-smoke.ts`; transitive imports are type-checked normally. Do not rely on `packages/config/tsconfig.json`, whose include covers only `packages/config/src/**/*`, or on broad root file discovery.
  </implementation_constraints>
  <action>Before editing, run upstream GitNexus impact for every existing symbol named in the implementation constraints and record direct callers/processes/risk; stop for explicit approval on HIGH/CRITICAL. Write failing tests first in `data-chain-handoff.test.ts`. Add dedicated import-safe `parseDataChainHandoffArgs` that only tokenizes exact `--mode`, `--target`, and `--run-id`, rejects every other token including canonical/noncanonical/equals-form `--evidence-dir`, validates closed mode/runId, and performs no path/filesystem work. Drive the real CLI across invalid target and evidence-dir matrices and prove binary 1 plus one non-reflective refusal before dependency factory/storage/reservation/preflight/runner/verifier/provider. In `runDataChainHandoffCli`, resolve parsed target through `resolveTargetProfile`, confirm `isTargetProfileId` and profile/id agreement, then construct a `ResolvedDataChainHandoffRequest`; no path API or factory may run first. Define `runDataChainHandoffCore(request, dependencies)` so request contains only closed mode, validated runId, and validated resolution, with no raw target, evidenceRoot, path bundle, path factory, or filesystem dependency. Add compile-time negative cases for raw-target/root/bundle calls and runtime forged-resolution refusal. Implement one `deriveDataChainHandoffPaths` that uses `DATA_CHAIN_EVIDENCE_ROOT`, canonical target ID, runId, and mode only, then always passes its internally computed output through pure `assertExactDataChainHandoffPaths` before returning. The guard applies `path.posix` and `path.win32` lexical absolute/separator/drive/UNC checks before native resolution, then enforces resolved relative strict-descendant and exact equality to the recomputed root/target/run/`<mode>.json`/`<mode>.md`/`<mode>.attempt` strings. Tests may call the pure guard with hostile candidates, but neither core nor adapter accepts a guard result or caller bundle. Freeze the returned readonly bundle. Replace path-by-path/return-a-path seams with `DataChainHandoffStorageAdapter` logical methods that receive that exact bundle and return only pair state, pair contents, reservation status, or write status. Production methods must use the received literal paths; `createTempMappedDataChainHandoffStorage` may map only after asserting exact bundle equality and must not return replacement paths. Test foreign syntax, equality, parent/absolute/volume/case escape, alias, same-directory shadows, swapped peers, wrong-mode marker, bundle mutation, and malicious extra return fields, then prove valid `starye-org` enters the six pair states plus atomic local/remote marker, EEXIST/non-EEXIST, exactly-one-owner race, crash re-entry, persistence, and marker-only non-success matrices. After remote ownership only, invoke a closed `invokeRemotePreflight` dependency once. Production binds a shell-false status-only invocation of the exact repository `target-profile preflight --target <canonical-id> --scope remote --command smoke --ci-environment <profile.ci.githubEnvironment> --live` contract with fresh allowlisted environment; discard stdout/stderr/error details. Tests inject only `passed|unmet`. On unmet, build `pre_ingest/remote_preflight/target_preflight_unmet` evidence with existing typed evidence/serialization/rendering owners, `itemId:null`, write the exact pair through the adapter, then call the pair-focused verifier read-only once; assert zero runner/fixture/D1/API/browser calls, binary 1, and persistent marker. If pair write/parity/verification fails, retain marker and return 1 without retry or synthesis. On passed, invoke the sole remote runner immediately; do not remove or claim replacement of the runner's existing internal defensive gates. Local owners invoke their sole runner directly. In both modes call the existing exact verifier once after the produced pair, cross-check exact machine/artifact state, and return handoff 0 only for resolved pending with non-empty itemId and provesExternalChain false. Preserve all collision/no-retry/redaction behavior and never touch canonical or historical evidence in tests. Immediately before each RED/GREEN/REFACTOR/task commit, run fresh GitNexus detect-changes for the five owned paths, compare all declared existing/new parser/path/core/storage/preflight symbols and flows, and block unexpected or HIGH/CRITICAL expansion.</action>
  <action_addendum>Action ordering requirement: the first executable handoff code is the no-path/no-data-chain prelude and its process/import-spy test. It tokenizes and rejects mode/target/run/evidence-dir input, resolves the tracked target, and only then dynamically imports path/fs, runner, verifier, surface, and evidence owners; invalid input must make zero path/factory/storage/downstream calls. This addendum is part of Task 1's action and overrides any earlier wording that could imply constructing the core or loading `DATA_CHAIN_EVIDENCE_ROOT` before target validation.</action_addendum>
  <rationale>D-01 and D-02 constrain the one-item fixture and closed mutation boundary; D-03 enforces local-first/Gateway ordering; D-04 preserves one target/run/code with environment-owned item IDs; D-05 requires the handoff-owned live preflight and fail-closed checkpoint; D-06 requires non-secret immutable pair/marker behavior; D-07 keeps real provider proof in Phase 13. The static-prelude and import-hook requirements translate these locked decisions before any path/evidence owner can evaluate.</rationale>
  <acceptance_criteria>
    - Static handoff import evaluation loads no Node path/filesystem module and no runner/verifier/surface/evidence owner; invalid target or evidence-dir input exits before the import hook observes those modules, while a valid tracked target resolves first and only then loads them and enters core.
    - Dedicated parser performs no path/filesystem work, accepts no evidence-root flag, and the CLI resolves/validates the tracked target before path or dependency-factory construction.
    - Every invalid/foreign target and any canonical/noncanonical evidence-dir attempt returns binary 1 with one closed refusal, no reflection, and zero path-core/factory/storage/reservation/preflight/runner/verifier/provider calls.
    - Imported core accepts only validated resolution/mode/runId; raw target, root, bundle, or derivation overrides fail type checks, and forged resolutions fail before derivation/storage.
    - One internally called `deriveDataChainHandoffPaths` applies POSIX/Win32 lexical guards, native strict-descendant checks, and exact expected-path equality for fixed root/target/run/mode peers/marker.
    - Storage receives a frozen readonly exact bundle and cannot redirect paths; shadow, swapped-peer, wrong-marker, alias, equality, root/volume/case escape, mutation, and malicious-return tests cannot influence I/O.
    - Valid canonical `starye-org` reaches all six pair states and the unchanged atomic reservation/concurrency/crash matrix in local and remote modes.
    - Remote owner runs the official outer live preflight exactly once after marker ownership. Unmet persists and read-only verifies the closed D-05 checkpoint pair with itemId null, zero runner/provider fixture/D1/API/browser calls, binary 1, and no retry.
    - Preflight checkpoint write/parity/verifier failure leaves the marker and returns 1 without success fabrication; marker alone remains non-success.
    - Green outer preflight immediately enters the sole remote runner while preserving its internal defensive gates; exact pending alone returns handoff 0 and all checkpoint/malformed/mismatched/terminal states return 1.
    - Every existing/new parser/path/core/storage/preflight symbol is inventoried in Artifacts, existing edits have pre-edit impacts, and each task commit has a fresh expected-flow detect-changes gate.
    - Tests use OS-temp state only and never allocate/read/rewrite/stage/commit Phase 13 canonical or historical evidence.
    - `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-handoff.json` exits 0 with no diagnostics and covers the new handoff plus the root runner/verifier imports that package-local config type-check omits.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-handoff.test.ts</automated>
    <automated>pnpm --filter @starye/config type-check</automated>
    <automated>pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-handoff.json</automated>
    <automated>powershell -NoProfile -Command "$owners = @(@{Path='scripts/data-chain-smoke.ts'; Anchors=@('export const DATA_CHAIN_EVIDENCE_ROOT = path.resolve','export function getDataChainEvidencePaths','export function parseDataChainSmokeArgs','const evidenceRoot = values.evidenceRoot ?? DATA_CHAIN_EVIDENCE_ROOT','return { mode: values.mode, target: values.target, runId, evidenceRoot: DATA_CHAIN_EVIDENCE_ROOT }')},@{Path='scripts/verify-data-chain-smoke.ts'; Anchors=@('DATA_CHAIN_EVIDENCE_ROOT,','getDataChainEvidencePaths,','parseDataChainSmokeArgs,','if (options.evidenceRoot !== DATA_CHAIN_EVIDENCE_ROOT)')},@{Path='packages/config/src/deployment-target/target-resolver.ts'; Anchors=@('export function isTargetProfileId','export function resolveTargetProfile','const selectedTargetId = targetId?.trim()')}); foreach ($owner in $owners) { $source = Get-Content -Raw -LiteralPath $owner.Path; foreach ($anchor in $owner.Anchors) { if ($source.IndexOf($anchor) -lt 0) { throw ('Missing fixed-owner source anchor in ' + $owner.Path + ': ' + $anchor) } } }; exit 0"</automated>
  </verify>
  <done>The dedicated CLI rejects invalid input before every lazy path/evidence module load, resolves a canonical target before dynamically loading path/factory owners, and then lets the validated core derive one exact fixed-root bundle so only one marker owner can persist a verified D-05 checkpoint or enter the sole defensively gated runner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve canonical runner and verifier exit codes at the root scripts</name>
  <files>packages/crawler/scripts/data-chain-cli.mjs, package.json, packages/config/src/deployment-target/__tests__/data-chain-cli-process.test.ts, packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts</files>
  <read_first>
    - AGENTS.md
    - package.json
    - packages/crawler/package.json
    - scripts/data-chain-handoff.ts
    - scripts/data-chain-smoke.ts
    - scripts/verify-data-chain-smoke.ts
    - scripts/package-manager-command.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
  </read_first>
  <behavior>
    - Closed launcher subcommand `run` dispatches once to `runDataChainSmokeCli`; actual root `pnpm run smoke:data-chain` maps injected fixed CLI 0 -> outer 0, 1 -> outer 1, and 2 -> outer 2.
    - Closed launcher subcommand `verify` dispatches once to `runVerifyDataChainSmokeCli`; actual root `pnpm run smoke:data-chain:verify` maps injected fixed CLI 0 -> outer 0, 1 -> outer 1, and 2 -> outer 2.
    - Closed launcher subcommand `handoff` dispatches once to `runDataChainHandoffCli`; actual root `pnpm run smoke:data-chain:handoff` maps 0 -> outer 0, 1 -> outer 1, and any unexpected nonzero such as 2 -> outer 1, never a pending/ready result.
    - Unknown subcommand, missing entry, thrown import/CLI error, or duplicate dispatch exits 1; no arbitrary module/command can be selected.
    - Root package scripts call direct `node packages/crawler/scripts/data-chain-cli.mjs <fixed-entry>` and contain no nested `pnpm --filter ... exec` for the three owned commands.
    - Actual-root process tests prove only launcher/package-manager exit propagation through a test-process-only controlled module boundary. They pass no temporary/noncanonical `--evidence-dir`, do not claim collision classification, and add no production env/config/module/evidence-root override.
    - The six collision/inspection states and downstream counts remain Task 1's imported-core matrix; existing parser/verifier tests continue to prove noncanonical roots are rejected.
  </behavior>
  <action>Add a small ESM launcher under the crawler package that imports package-local `tsx/esm`, then dynamically imports only the three fixed root TypeScript modules from a closed dispatch table. Export new `dispatchDataChainCli(entry, argv, dependencies)` so unit tests can inject only the three fixed CLI functions; the actual entrypoint always supplies the closed production map and owns the top-level catch/exit assignment. For `run` and `verify`, preserve only 0, 1, or 2 unchanged and normalize any other result to 1. For `handoff`, preserve 0 and 1 but normalize every other result, including an unexpected 2, to 1 so outer 1 can never be interpreted as pending. The launcher must not accept a filesystem/module path, command, origin, evidence root, prepared context, or child argv beyond forwarding the established CLI arguments. Update only `smoke:data-chain`, `smoke:data-chain:verify`, and new `smoke:data-chain:handoff` in root `package.json`; keep observe, target, and prepared-entry commands unchanged. In `data-chain-cli-process.test.ts`, process-spawn from repository root the actual script names `pnpm run smoke:data-chain`, `pnpm run smoke:data-chain:verify`, and `pnpm run smoke:data-chain:handoff`. Use a test-process-only Node preload/import-hook boundary, created and removed under an OS temp directory by the test, that allowlists the three canonical TS module specifiers/CLI exports and returns a requested fixed code. The child-test harness may configure that hook through child-only process state, but production code must neither read nor expose it; the hook must not alter `parseDataChainSmokeArgs`, the verifier fixed-root check, argv, logical pair/marker paths, filesystem/provider behavior, or any production dependency constructor. Invoke the root scripts only to prove process-level mapping: run 0/1/2 -> 0/1/2; verify 0/1/2 -> 0/1/2; handoff 0 -> 0, 1 -> 1, and unexpected 2 -> 1. Make the handoff-2 intercepted CLI emit a pending-shaped line and still require outer 1 with no ready interpretation. Do not pass a temp/noncanonical `--evidence-dir` in this process harness, do not seed peers/markers, and do not attribute collision/reservation classification or downstream-call counts to it; Task 1's imported real CLI/core and test-only storage adapter own those assertions. Re-run the existing parser/exact-verifier suite so fixed-root rejection remains proven. Parse root `package.json` and assert the exact three names use the closed direct launcher. Temporary harness state may record fixed entry/exit/invocation count only, must use `path.resolve`/OS temp APIs rather than a drive literal, and must never allocate a Phase 13 run, read/write canonical evidence, or touch a provider/browser. Before editing any existing function/class/method, run upstream GitNexus impact and report risk/direct callers/flows; HIGH/CRITICAL emits a blocking warning checkpoint and waits for explicit approval. Immediately before every RED, GREEN, REFACTOR, or task commit, run `gitnexus_detect_changes` across the exact five owned implementation/test paths and compare against only expected launcher/package/test consumers, with no parser/verifier or data-chain business-flow change. Unexpected or HIGH/CRITICAL expansion stops before commit; rerun after any hook/autofix/restage.</action>
  <acceptance_criteria>
    - Actual root `pnpm run smoke:data-chain` and `pnpm run smoke:data-chain:verify` each expose the tested outer mapping 0/1/2 -> 0/1/2 instead of nested-pnpm normalization.
    - Actual root `pnpm run smoke:data-chain:handoff` exposes only 0/1; injected CLI 1 or unexpected 2 produces outer 1 and cannot be accepted as pending.
    - Process tests invoke all three real root script names through one controlled test-process module boundary and prove one dispatch without passing an evidence-root override, creating a run id, or reading/writing the canonical evidence tree.
    - Process tests claim only raw/binary exit propagation; the imported-core six-state matrix independently proves collision/error classifications and downstream counts.
    - Existing runner/verifier argument parsing, fixed evidence root, noncanonical-root rejection, tuple, redaction, and exact-pair contracts retain their post-13-13 semantics and are covered by source-anchor plus existing verifier tests; Plan 13-14 modifies none of their owned symbols.
    - Any required existing-symbol change follows pre-edit impact; HIGH/CRITICAL pauses at a blocking warning checkpoint. Every RED/GREEN/REFACTOR or task commit is immediately preceded by a fresh detect-changes comparison, and unexpected expansion blocks that commit.
    - No dependency/lockfile change, free-form dispatch, schema path, or external action is introduced.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-cli-process.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
    <automated>pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-handoff.json</automated>
    <automated>powershell -NoProfile -Command "$owners = @(@{Path='scripts/data-chain-smoke.ts'; Anchors=@('export const DATA_CHAIN_EVIDENCE_ROOT = path.resolve','export function getDataChainEvidencePaths','export function parseDataChainSmokeArgs')},@{Path='scripts/verify-data-chain-smoke.ts'; Anchors=@('DATA_CHAIN_EVIDENCE_ROOT,','getDataChainEvidencePaths,','parseDataChainSmokeArgs,','if (options.evidenceRoot !== DATA_CHAIN_EVIDENCE_ROOT)')},@{Path='packages/config/src/deployment-target/target-resolver.ts'; Anchors=@('export function isTargetProfileId','export function resolveTargetProfile')}); foreach ($owner in $owners) { $source = Get-Content -Raw -LiteralPath $owner.Path; foreach ($anchor in $owner.Anchors) { if ($source.IndexOf($anchor) -lt 0) { throw ('Missing fixed-owner source anchor in ' + $owner.Path + ': ' + $anchor) } } }; exit 0"</automated>
    <automated>git diff --check -- scripts/data-chain-handoff.ts packages/crawler/scripts/data-chain-cli.mjs package.json packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts packages/config/src/deployment-target/__tests__/data-chain-cli-process.test.ts</automated>
  </verify>
  <done>The actual root scripts independently prove raw runner/verifier and binary handoff exit propagation through a controlled test-only module boundary, without weakening or bypassing the fixed-root production policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Root package command -> package-local TS loader | Dispatch must remain closed and preserve the called CLI's raw status. |
| Untrusted handoff argv -> dedicated parser -> tracked-target resolver | Parser performs no path/filesystem work and accepts no evidence-root flag; CLI resolves and confirms the canonical target before path calls or factory construction. |
| Validated resolution/mode/run -> `deriveDataChainHandoffPaths` | Imported core accepts no raw target/root/bundle; POSIX/Win32 lexical guards plus host strict-descendant and exact-equality checks own the one fixed bundle. |
| Frozen exact path bundle -> storage adapter | Adapter receives readonly logical operations and cannot return replacement paths; first filesystem access uses only the internally derived exact peer/marker names. |
| Exact pair inspection -> persistent mode reservation | Both logical peers must be absent before one zero-byte `local.attempt`/`remote.attempt` marker may be atomically created; pair collision/error reaches no reservation. |
| Persistent mode reservation -> runner/provider mutation | Exclusive marker creation is the ownership linearization point; pre-existing/concurrent-loser/unavailable reservation reaches no runner, verifier, or provider. |
| Owned `remote.attempt` -> official live preflight -> checkpoint/runner | Unmet preflight must persist+verify a redacted checkpoint with zero provider mutation; passed preflight immediately enters one runner without removing its internal gates. |
| Runner result -> verifier -> handoff | Only one eligible runner result and its exact newly persisted pair may authorize browser work. |
| Machine stdout -> executor | Output is untrusted until unique, parsed, and cross-checked with the exact artifact tuple. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-66 | Tampering | nested package status | high | mitigate | Actual root pnpm process tests assert run/verify 0/1/2 propagation and binary handoff normalization. |
| T-13-67 | Spoofing | pending handoff | high | mitigate | One runner, one exact verifier, unique machine JSON, and exact artifact cross-check. |
| T-13-68 | Elevation of Privilege | launcher dispatch, untrusted argv/target identity, and fixed evidence-root policy | critical | mitigate | Dedicated handoff parser accepts no evidence-root input and performs no path work; CLI resolves through `resolveTargetProfile`/`isTargetProfileId` before factory/path calls. Core accepts only the validated resolution and no raw target/root/bundle; runner/verifier parsers remain unchanged. |
| T-13-69 | Information Disclosure | invalid-target, containment, inspection, and reservation refusal output | high | mitigate | Invalid targets emit only `invalid_target`; containment emits only `invalid_evidence_path`; both are non-reflective, and non-ENOENT inspection positions also emit closed allowlisted results. No logical/physical path, resolver/storage error text/code/stack, endpoint, cookie, header, secret, content, or context is representable. |
| T-13-70 | Tampering | canonical-root escape, injected/redirection bundle, pair check-to-write TOCTOU, concurrent execution, and crash re-entry | critical | mitigate | One internal derivation applies foreign-syntax guards, host containment, and exact expected-path equality; frozen bundle storage cannot return paths. After pair validation, atomic `wx` marker ownership preserves EEXIST/error/no-retry behavior and marker-only stays non-success. |
| T-13-102 | Repudiation | remote live-preflight failure without D-05 evidence | critical | mitigate | After reservation, the exact outer official live preflight returns status only. Unmet writes and pair-verifies `pre_ingest/remote_preflight/target_preflight_unmet` with itemId null and zero runner/fixture/D1/API/browser calls; write/verify failure retains marker and cannot fabricate success. |
| T-13-SC | Tampering | package supply chain | low | accept | No install, dependency, package version, or lockfile change occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Real selected-target local-to-production proof. | 13-17 through 13-20 consume this command. | COVERED | Wrapper is an execution gate, not proof. |
| REQ | DATA-01..DATA-06 | Local/remote data and browser surfaces. | 13-17 through 13-20 | COVERED | No external action here. |
| REQ | DATA-07 | Exact repeatable immutable artifacts. | Tasks 1-2, 13-15, 13-17 through 13-20 | COVERED | Canonical JSON/Markdown remains success evidence; persistent zero-byte mode marker is a first-class untracked attempt artifact and never success by itself. |
| REQ | TEST-05 | Stable terminal/checkpoint output. | Tasks 1-2 | COVERED | Imported-core pair/reservation/concurrency/crash/fixed-root classifications and separate actual-root raw/binary exit propagation are tested. |
| RESEARCH | - | Reuse import-safe TS, canonical tracked-target resolver, existing runner/verifier fixed-root owners, native POSIX/Win32/host path primitives, closed official preflight, typed evidence builders, and Node no-clobber primitive; add no second target resolver or dependency. | Tasks 1-2 | COVERED | Dedicated handoff parser fixes ordering, exact internal derivation removes bundle overrides, and tests execute no external action. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05 | One deterministic tuple, restricted fixture boundary, Gateway/local-first order, per-environment identity, and selected-target fail-closed remote execution. | Task 1 plus 13-17 through 13-20 | COVERED | Task 1 makes remote reservation own official preflight; unmet creates verified redacted checkpoint evidence with zero provider mutation, while success alone may enter the still-defensive runner. |
| CONTEXT | D-06 | Machine/evidence are allowlisted, non-secret, and immutable per run. | Task 1 | COVERED | Pair-state gate, atomic persistent per-mode marker, redacted reservation refusal, then exact pair cross-check; marker-only is non-success. |
| CONTEXT | D-07 | Provider proof remains Phase 13. | 13-19/13-20 | COVERED | No simulated success. |
| VERIFICATION | G-1/G-2/G-3 | Local and production chain absent. | 13-17 through 13-20 | COVERED | Repairs precede execution. |
| VERIFICATION | G-4 | Nested pnpm breaks pending wrapper. | Tasks 1-2 | COVERED | Imported core proves handoff decisions; actual root scripts separately prove run/verify raw codes and handoff binary semantics. |

Phase 14 literals/RUNBOOK/final matrix remain excluded. No deferred item is implemented.
</source_audit>

<verification>
- Prove `parseDataChainHandoffArgs` is import-safe/no-path and table-drive the real CLI across missing/duplicate/unknown flags, canonical/noncanonical/equals-form evidence-dir, and invalid/foreign target syntax. Every failure is binary 1, non-reflective, and occurs before path core/factory/storage/reservation/preflight/runner/verifier/provider.
- Require canonical `resolveTargetProfile`/tracked membership before dependency factory and before any core path call. Compile-time tests reject raw-target/evidenceRoot/bundle core calls; forged resolved objects fail at runtime before derivation/storage.
- Verify `deriveDataChainHandoffPaths` alone applies `path.posix`/`path.win32` lexical guards before host resolution and exact fixed-root/target/run/mode equality after strict-descendant checks. Cover POSIX/Windows/UNC/backslash/encoded syntax, equality, parent, absolute, volume/case, alias, same-directory shadows, swapped peers, and wrong-mode marker.
- Assert storage receives one frozen readonly internally derived bundle and cannot return/mutate/repoint it; malicious extra path results and shadow files do not affect inspected/reserved/read/written paths.
- Run valid `starye-org` local/remote through neither/both/JSON-only/Markdown-only/two inspection-error states, both marker names, exclusive creation, EEXIST/non-EEXIST, exactly-one-owner concurrency, crash re-entry, persistence, and marker-only exact-verifier non-success.
- Run remote outer-preflight `unmet`: marker first, official status dependency once, exact redacted `pre_ingest/remote_preflight/target_preflight_unmet` pair with itemId null, read-only pair verification once, binary 1, and zero runner/fixture/D1/API/browser. Inject JSON/Markdown write/parity/verifier failures and require marker persistence, no retry, and no success.
- Run remote outer-preflight `passed`: the next operation is the sole remote runner, whose internal defensive gates remain present; then one artifact-only verifier decides checkpoint versus exact pending. No outer preflight is rerun.
- Confirm actual root runner/verifier scripts expose raw 0/1/2 and handoff maps only 0/1; the process harness passes no root override and claims no pair/preflight classification.
- Record upstream impact for every existing symbol actually edited. Immediately before every RED/GREEN/REFACTOR/task commit run fresh GitNexus detect-changes against the five code/test paths plus the narrow tsconfig and declared parser/path/core/storage/preflight/launcher flows; unexpected or HIGH/CRITICAL expansion blocks commit.
- Do not invoke an actual local/remote smoke, live preflight, browser, provider, service restart, deploy, or mutation while executing this implementation plan.
- Schema/migrations: N/A. No schema file, migration, schema push, or provider resource changes.
</verification>

<success_criteria>
- Actual root script status propagation is correct without changing runner/verifier parser/path policy.
- Handoff has its own no-path parser, accepts no evidence-root flag, and resolves a canonical tracked target before path/factory/storage work.
- Imported core accepts only validated resolution/mode/runId and internally calls one exact fixed-root path owner; caller/test dependencies cannot inject or redirect path bundles.
- POSIX/Win32 foreign syntax and host equality/escape/alias/shadow/swapped/wrong-marker cases fail before filesystem/downstream work, while canonical `starye-org` preserves the six-state pair and atomic reservation/concurrency/crash contract.
- A remote marker owner runs the exact outer official live preflight once. Unmet creates and read-only verifies closed D-05 checkpoint evidence with itemId null and zero provider mutation; any checkpoint/write/verify failure permanently closes the run with marker retained.
- Green outer preflight immediately invokes the sole remote runner without removing its internal gates; exact pending pair plus machine tuple is the only handoff-ready state.
- Every incompatible state fails closed without retry, disclosure, evidence repair, or historical/canonical evidence access.
- Tests, typecheck, exact diff, per-symbol impact, and task-local GitNexus flow comparisons pass with no external action.
</success_criteria>

## Artifacts this phase produces

- **Static import boundary:** `scripts/data-chain-handoff.ts` statically loads only the pure parser/target-resolution prelude and type-only contracts. Node path/fs, runner, verifier, surface, and evidence owners are loaded lazily only after target/profile validation; the test hook records invalid-input zero-load/zero-call and valid-target resolve-before-load order.
- **New production symbols:** `scripts/data-chain-handoff.ts:DataChainHandoffParsedArgs`, `ResolvedDataChainHandoffRequest`, `DataChainHandoffPaths`, `DataChainRemotePreflightStatus`, `DataChainHandoffResult`, `DataChainHandoffStorageAdapter`, `DataChainHandoffDependencies`, `DataChainHandoffCliDependencies`, `parseDataChainHandoffArgs`, `validateResolvedDataChainHandoffRequest`, `assertExactDataChainHandoffPaths`, `deriveDataChainHandoffPaths`, `invokeOfficialDataChainRemotePreflight`, `writeDataChainRemotePreflightCheckpointPair`, `runDataChainHandoffCore`, `createProductionDataChainHandoffDependencies`, and `runDataChainHandoffCli`; `packages/crawler/scripts/data-chain-cli.mjs:dispatchDataChainCli` with closed `run|verify|handoff` entries.
- **New test-only symbol:** `packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts:createTempMappedDataChainHandoffStorage`; it receives the frozen internally derived exact bundle, asserts equality before OS-temp mapping, cannot return replacement paths, exercises real exclusive creation, and is never imported by production.
- **Modified symbols:** none in existing runner/verifier core. Their exported CLIs are consumed directly.
- **CLI contracts:** modified `smoke:data-chain`, modified `smoke:data-chain:verify`, and new `smoke:data-chain:handoff` root scripts, with process-tested outer mappings run/verify 0/1/2 and handoff 0/1 only.
- **Machine fields:** existing verifier fields plus handoff-only `handoffReady`, `itemCode`, `itemId`, closed `preflightStatus: not_applicable|passed|unmet`, and `runnerInvocations: 0|1`; closed checkpoint `target_preflight_unmet` and refusal values `invalid_target`, `invalid_evidence_path`, `evidence_pair_exists`, `evidence_pair_partial`, `evidence_pair_state_unavailable`, `attempt_already_reserved`, and `attempt_reservation_unavailable`. Invalid/preflight output contains no target input/path/error/provider output reflection or free-form field.
- **Dependency seam:** `DataChainHandoffDependencies` exposes exact-bundle logical `inspectPair`, `readPair`, `writePair`, and `reserveAttempt`, status-only `invokeRemotePreflight`, `invokeRunner`, `invokeVerifier`, and `resultSink`. It exposes no resolver/root/path factory or replacement-path return. CLI injects only a post-resolution dependency factory/result sink; production uses `wx`, official preflight, and literal derived paths.
- **Attempt artifacts:** canonical `<target>/<runId>/local.attempt` and `<target>/<runId>/remote.attempt`, each zero-byte, exclusive-created, persistent, untracked, never deleted/reused/backfilled, and ignored as success by the pair-focused verifier.
- **GitNexus scope:** all new parser/request/path/core/storage/preflight/checkpoint/CLI symbols are inventoried and checked after each RED/GREEN/REFACTOR/task delta; every consumed existing parser/path/CLI/resolver/preflight/evidence symbol is impact-checked before any edit, and unexpected or HIGH/CRITICAL flow expansion blocks commit behind an explicit warning checkpoint.
- **Paths:** the six frontmatter paths only. Production root remains `DATA_CHAIN_EVIDENCE_ROOT`; validated target/run and every local/remote JSON/Markdown/marker path are strict-contained before storage. No evidence, source fixture, target profile, environment, schema, migration, or provider path is modified.
- **Schema/migrations:** N/A; no schema push.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-14-SUMMARY.md` after both tasks pass. Record the no-path/no-data-chain static prelude, process/import-hook invalid-input zero-load/zero-call proof, valid-target resolve-before-lazy-load/core proof, all evidence-dir/invalid-target refusals, validated-core boundary, POSIX/Win32 and exact host-path matrix, frozen exact bundle/non-redirection proof, valid `starye-org` six-state and atomic marker matrices, remote outer-preflight unmet checkpoint pair with zero provider calls, write/verify-failure marker persistence, green-preflight immediate runner plus unchanged internal defensive gates, separate actual-root exits, narrow root-script TypeScript result, source-anchor/fixed-owner checks, exact symbol impacts/detect comparisons, and zero canonical/old evidence or external action.
</output>
