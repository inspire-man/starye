---
phase: 13-full-chain-data-smoke
plan: "50"
type: execute
wave: 2
depends_on: [13-49]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-50-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "Only a same-session in-app Browser that visibly shows the signed-in local Dashboard, plus current-source local gates and two accepted Gateway readiness records, can authorize p13-50 allocation."
    - "One allocated p13-50 run has exactly one local handoff, one pending exact verifier, one repository-core Dashboard-to-Viewer observation through the existing in-app Browser session, and one post-observation exact verifier."
    - "The post-observation verifier reports terminal_passed and provesExternalChain true only when the same local target/run/code/itemId tuple has ordered Dashboard then Viewer receipts."
    - "A pre-allocation failure writes no p13-50 run id or evidence; every post-allocation failure permanently fixes that one p13-50 run without another handoff, observer, verifier retry, or replacement identifier."
    - "p13-45, p13-48, and p13-49 retain their immutable stopped states; all historical and new evidence remains untracked and no local result is represented as remote or production success."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-50-RUN-ID.txt
      provides: exactly one allocated p13-50 identifier when every pre-allocation gate succeeds
    - path: .planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md
      provides: non-secret closeout for the terminal local proof or the exact stopping gate
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-50-run-id}/local.attempt
      provides: untracked atomic reservation marker created by the sole local handoff
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-50-run-id}/local.json
      provides: untracked tuple-bound local evidence advanced only by repository-owned observation logic
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-50-run-id}/local.md
      provides: untracked deterministic Markdown rendering of the local evidence JSON
  key_links:
    - from: pnpm check:services
      to: http://localhost:8080/robots.txt, /auth, and /auth/
      via: two fresh starye-gateway-readiness-1 records must report robots, auth, and authSlash accepted before allocation
      pattern: "gateway.*robots.*auth.*authSlash"
    - from: packages/crawler/scripts/data-chain-cli.mjs
      to: scripts/data-chain-handoff.ts
      via: the root launcher removes pnpm's leading delimiter before the closed local handoff receives mode, target, and run id
      pattern: "argv\\[0\\] === '--'"
    - from: local handoff evidence
      to: scripts/data-chain-surface-observation.ts
      via: repository-owned observation accepts only the same unobserved pending p13-50 local tuple and writes Dashboard before Viewer receipts
      pattern: "observeDataChainSurfaces"
    - from: scripts/data-chain-surface-observation.ts
      to: pnpm smoke:data-chain:verify
      via: the sole post-observation exact verifier must read the same persisted local pair as terminal_passed
      pattern: "provesExternalChain"
---

<objective>
Create one new, local-only terminal data-chain proof using the currently visible signed-in Codex in-app Browser session.

Purpose: the one-item Phase 13 gap remains at the authenticated local Dashboard-to-Viewer boundary. p13-45 is immutable invalid_target-before-evidence history, p13-48 is immutable Gateway-readiness-before-allocation history, and p13-49-ff6c8d29fd91408c90b22e8e4935d947 is immutable after its sole repository observer persisted dashboard_auth_unavailable. This plan creates a new p13-50 boundary and never reopens any of those records.

Output: an honest 13-50 Summary, a 13-50 run-id only if allocation occurs, and untracked local evidence for one exact local target/run/code/itemId tuple. This is local proof only.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-41-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-45-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-48-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-49-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md
@.planning/debug/phase13-gateway-auth-readiness.md
@package.json
@packages/crawler/scripts/data-chain-cli.mjs
@scripts/data-chain-handoff.ts
@scripts/data-chain-smoke.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@scripts/gateway-readiness.ts
@scripts/check-services.ps1
</context>

## Execution Selection

The GSD executor must explicitly dispatch only `13-50-PLAN.md`. Do not use a generic phase invocation or any `--wave` selector: the dependency graph normalizes this plan to Wave 2, whose generic selection can include incompatible historical gap plan 13-33. The dependency on completed 13-49 is immutable-history input only; it does not authorize re-execution, observation, verification, or reuse of 13-49 and creates no execution dependency on 13-45, 13-46, 13-47, or 13-48.

## Non-Goals

- Do not retry, observe, verify, reuse, alter, or create evidence for p13-45, p13-48, p13-49, or any other historical run.
- Do not run remote mode, provider commands, separate D1/API/Wrangler commands, production URLs, production Browser work, deployment, migration, 13-46, or 13-47.
- Do not stop `pnpm dev`, terminate any PID, read cookies/local storage/profiles/passwords/session stores, inject authentication material, or serialize authentication material into evidence or the Summary.
- Do not modify source, tests, STATE.md, ROADMAP.md, 13-VERIFICATION.md, existing plans, existing summaries, existing evidence, or unrelated dirty files.

## Source Coverage Audit

| Source | ID | Required item | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 13 | Prove usable Gateway/canonical data chain | Tasks 1-3 produce one bounded local Gateway proof and leave external claims unasserted | COVERED |
| REQ | DATA-01 | Local API/auth/dashboard/content via Gateway | Task 1 requires two accepted 8080 readiness records; Task 3 observes local Dashboard and Viewer only through Gateway | COVERED |
| REQ | DATA-02 | Local D1/minimal-data readiness before ingest | Task 2 uses the existing closed handoff only after projection and local live preflight; its pending receipt set includes local D1 readiness | COVERED |
| REQ | DATA-03 | One deterministic selected-target fixture and identity | Task 2 permits exactly one local handoff for one p13-50 tuple and requires a non-empty local itemId | COVERED |
| REQ | DATA-04 | Same item in D1/API/admin state | Task 2 requires receipt-backed runner surfaces; Task 3 keeps the exact tuple through Dashboard and Viewer receipts | COVERED |
| REQ | DATA-05 | Dashboard validation | Task 1 visibly confirms the existing signed-in Dashboard before allocation; Task 3 records Dashboard before Viewer through repository core | COVERED |
| REQ | DATA-06 | Canonical viewer validation | Task 3 records only `http://localhost:8080/movie/<item-code>` after Dashboard | COVERED |
| REQ | DATA-07 | Repeatable non-secret evidence | Tasks 2-3 preserve untracked JSON/Markdown/marker evidence and allow only run-id plus Summary in execution closeout | COVERED |
| REQ | TEST-05 | Repeatable smoke-script output | Task 1 runs the root launcher regression; Tasks 2-3 require the single pending and single terminal exact verifier outcomes | COVERED |
| RESEARCH | One-item adapter, local-first, Gateway-only, redacted evidence | Reuse the closed handoff, exact verifier, and repository observer; no new crawler/API path or package | COVERED |
| RESEARCH | Dashboard must be real session-backed, not fabricated | Task 1 uses visible in-app Browser state only; Task 3 passes only minimal DOM match results to repository core | COVERED |
| CONTEXT | D-01 | Exactly one deterministic non-R18 item with one player | Task 2 invokes one closed local handoff and accepts no second item or replacement run | COVERED |
| CONTEXT | D-02 | Bounded fixture or targeted adapter only | Task 2 uses the existing local handoff/fixture adapter and never a full crawler corpus | COVERED |
| CONTEXT | D-03 | Local-first Gateway-only proof | Tasks 1-3 require local readiness before allocation and use the 8080 Gateway as the sole canonical browser origin | COVERED |
| CONTEXT | D-04 | Target/run/code/itemId correlation across surfaces | Tasks 2-3 require one exact tuple from handoff through ordered receipts and terminal verification | COVERED |
| CONTEXT | D-05 | Explicit provider-side trust boundary | Non-goals and all tasks exclude provider and remote operations; local proof is not promoted to external proof | COVERED AS BOUNDARY |
| CONTEXT | D-06 | Non-secret repeatable evidence fields | Tasks 2-3 retain only allowlisted tuple/status/hash facts and keep evidence untracked | COVERED |
| CONTEXT | D-07 | Phase 13 owns actual chain evidence | Tasks 2-3 record only this real local attempt in Phase 13 and leave stable documentation owners untouched | COVERED |

<tasks>

<task type="auto">
  <name>Task 1: Prove visible in-app Dashboard authentication and all pre-allocation local gates</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md
    - .planning/debug/phase13-gateway-auth-readiness.md
    - packages/crawler/scripts/data-chain-cli.mjs
    - scripts/gateway-readiness.ts
    - scripts/check-services.ps1
  </read_first>
  <action>
    Before any terminal command and before run allocation, use the same Codex in-app Browser already selected for this work. Navigate only through `http://localhost:8080/dashboard/` and visually confirm the Dashboard shell, a visible signed-in welcome state, and the logout control. This is a UI-state check only: do not inspect browser cookies, local storage, profiles, passwords, session stores, request headers, or authentication tokens; do not inject, copy, or export authentication material. Apply D-05 by treating any redirect to login, missing Dashboard/welcome/logout state, or unavailable in-app Browser as a pre-allocation stop rather than an authentication workaround.

    Only after that visible Browser gate passes, apply D-03 before allocation: preserve the existing current-source `pnpm dev` tree and do not stop a PID. Run the root-launcher process regression, explicit target-managed projection check, local live smoke preflight, and two separate `pnpm check:services` calls. Each readiness record must be schema `starye-gateway-readiness-1` with `robots`, `auth`, and `authSlash` all accepted for the canonical Gateway routes at `http://localhost:8080`.

    Apply D-06 and D-07 by recording only a non-secret boolean-style visible-auth result and the preflight/readiness outcomes in the Summary. Before allocation, require `13-50-RUN-ID.txt` absent and no `evidence/starye-org/p13-50-*` directory. If the browser gate or any command fails, write the honest stopped-before-allocation Summary; do not create a run id, evidence directory, attempt marker, handoff, exact verifier, or observer invocation.
  </action>
  <acceptance_criteria>
    - The existing in-app Browser visibly shows the signed-in local Dashboard and logout control at the canonical 8080 Gateway before a p13-50 id exists.
    - The launcher regression, target projection, local live preflight, and two consecutive service checks pass before allocation.
    - Both service records show `starye-gateway-readiness-1` with all three named Gateway probes accepted.
    - Any pre-allocation failure leaves no p13-50 run-id file, evidence tree, marker, handoff, exact verifier, or observer invocation.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-cli-process.test.ts</automated>
    <automated>pnpm target-profile project-local --target starye-org --check</automated>
    <automated>pnpm target-profile preflight --target starye-org --scope local --command smoke --live --wrangler-profile starye-org</automated>
    <automated>pnpm check:services</automated>
    <automated>pnpm check:services</automated>
  </verify>
  <done>A visibly authenticated in-app Dashboard and all fresh local gates authorize exactly one p13-50 allocation; otherwise only an honest pre-allocation Summary exists.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate one p13-50 local handoff and capture the sole pending verifier result</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-50-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md
    - scripts/data-chain-handoff.ts
    - scripts/data-chain-smoke.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
    Run only after Task 1 completed all gates. Apply D-01 and D-02: generate exactly one identifier as `p13-50-` plus 32 lowercase hexadecimal characters, write it once to 13-50-RUN-ID.txt, and use only the existing closed local handoff/fixture adapter. Immediately require its exact `evidence/starye-org/<run-id>` directory to be absent, then invoke exactly once: `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <run-id>`. Do not invoke `smoke:data-chain` directly, invoke handoff a second time, or allocate a replacement id.

    Apply D-04 by requiring handoff exit 0, `outcome: pending`, `handoffReady: true`, `runnerInvocations: 1`, one deterministic item code, and a non-empty local itemId. Invoke the root exact local verifier once after that handoff; the `<verify>` command below is this task's sole root-level pending-verifier invocation. It must report the same target/run/code/itemId in `resolved_pending_observation`, `pending`, and `provesExternalChain: false`.

    Apply D-06 and D-07 by retaining the new marker and JSON/Markdown evidence untracked, without reading or writing any historical evidence directory. If any allocation, handoff, or pending-verifier condition fails after the run id is written, record that exact non-success in 13-50-SUMMARY.md and freeze the allocated p13-50 run: do not retry handoff or verifier, assign another id, or continue to Task 3.
  </action>
  <acceptance_criteria>
    - 13-50-RUN-ID.txt contains exactly one `p13-50-<32 lowercase hex>` value only after Task 1 gates passed.
    - The new evidence directory was absent before the sole handoff and contains the atomic local marker plus one matching local JSON/Markdown pair after it.
    - The one handoff produces a non-empty local itemId, and the sole pending exact verifier reports the matching pending tuple.
    - Any post-allocation failure fixes this run id and stops downstream work without a second handoff, verifier retry, or replacement run.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-50-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>Exactly one p13-50 pending local pair exists for Task 3, or the allocated p13-50 run is permanently recorded as a truthful stop.</done>
</task>

<task type="auto">
  <name>Task 3: Use the existing in-app Browser session for ordered local receipts and terminal verification</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-50-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-41-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
    Run only when Task 2's sole root-level exact verifier returned the required pending tuple. Apply D-03 and D-04 with the same selected Codex in-app Browser session proven in Task 1. Use an executor-owned in-memory in-app-Browser adapter to call repository-owned `observeDataChainSurfaces` once for mode local, target `starye-org`, and the exact p13-50 run id. The adapter may navigate the visible tab to the canonical local Dashboard `/dashboard/movies` and then the matching Gateway Viewer `/movie/<item-code>`; for each requested surface it may return only `{ status, itemCode, itemId }` after visible DOM confirmation. Repository core retains ownership of pending-pair validation, tuple matching, Dashboard-before-Viewer order, receipt construction, and JSON/Markdown writes.

    The adapter must reuse the existing signed-in in-app Browser session and must not call the default independent Puppeteer profile. It must not inspect or extract cookies, local storage, browser profile data, passwords, session stores, request headers, or tokens, and it must never inject session material. A Dashboard redirect, missing visible tuple, Viewer failure, callback error, tuple mismatch, or repository-core checkpoint is a terminal stop for this p13-50 run. Do not run another observer, handoff, or verifier retry.

    Apply D-06 and D-07 by invoking the root exact local verifier once after the one observer; the `<verify>` command below is this task's sole post-observation verifier. A successful completion requires exit 0, `outcome: terminal_passed`, `ingestState: resolved`, `aggregate: passed`, and `provesExternalChain: true` for the exact p13-50 tuple. Write 13-50-SUMMARY.md with only non-secret gate outcomes, run id, tuple, one-handoff/one-observer counts, Dashboard-to-Viewer order, verifier outcomes, evidence byte counts and SHA-256 values, and scope boundary. Confirm the three evidence artifacts are untracked. Before the execution closeout commit, run GitNexus detect-changes and accept no planned source symbol or execution-flow change; stage only 13-50-RUN-ID.txt when allocated and 13-50-SUMMARY.md. Do not stage evidence or any existing dirty path.
  </action>
  <acceptance_criteria>
    - Repository core records Dashboard before Viewer through `http://localhost:8080` while preserving the exact p13-50 target/run/code/local-itemId tuple.
    - The sole post-observation verifier returns terminal_passed and provesExternalChain true for that same local pair.
    - Observer or verifier non-success fixes the one allocated p13-50 run and records an honest Summary without any replacement action.
    - local.attempt, local.json, and local.md remain untracked; the only allowed execution closeout paths are 13-50-RUN-ID.txt when allocated and 13-50-SUMMARY.md.
    - The Summary explicitly states that p13-45, p13-48, and p13-49 remained unchanged and that no remote or production activity occurred.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-50-RUN-ID.txt -Raw).Trim()</automated>
    <automated>npx --no-install gitnexus detect-changes --repo starye --scope all</automated>
    <automated>powershell -NoProfile -Command "$run = (Get-Content '.planning/phases/13-full-chain-data-smoke/13-50-RUN-ID.txt' -Raw).Trim(); $paths = @('.planning/phases/13-full-chain-data-smoke/evidence/starye-org/' + $run + '/local.attempt', '.planning/phases/13-full-chain-data-smoke/evidence/starye-org/' + $run + '/local.json', '.planning/phases/13-full-chain-data-smoke/evidence/starye-org/' + $run + '/local.md'); foreach ($path in $paths) { git ls-files --error-unmatch -- $path 2>$null; if ($LASTEXITCODE -eq 0) { exit 1 } }; exit 0"</automated>
  </verify>
  <done>The sole p13-50 run has ordered repository-owned local receipts and terminal local proof, or it is permanently frozen at its exact observed failure without external claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| in-app Browser visible UI to local observer adapter | The already-authenticated session proves access only through visible Dashboard/Viewer state; no session material crosses into terminal or evidence. |
| local executor to root handoff launcher | The caller supplies the explicit selected target and one validated run id to the closed handoff parser. |
| local handoff to evidence storage | The handoff owns the atomic reservation and writes only the fixed target/run local pair. |
| local observation to terminal claim | Repository core validates the pending tuple, orders receipts, and the exact verifier derives terminal state. |
| local proof to external-chain claims | A terminal local pair is local evidence and cannot establish remote or production success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-50-01 | Spoofing | in-app Browser authentication state | high | mitigate | Require visible Dashboard, signed-in welcome state, and logout control before allocation; reuse that session only through UI navigation and return no authentication data from the adapter. |
| T-13-50-02 | Tampering | run allocation and evidence reservation | high | mitigate | Require absent p13-50 state, allocate once after all gates, invoke handoff once, retain the atomic marker, and freeze every post-allocation failure. |
| T-13-50-03 | Tampering | historical runs and evidence | high | mitigate | Prohibit every read-write attempt against p13-45, p13-48, p13-49, and all existing evidence; use a separate p13-50 tree only after allocation. |
| T-13-50-04 | Information disclosure | Browser session, evidence, and Summary | high | mitigate | Disallow cookie/profile/storage/header/token inspection or injection; persist only allowlisted tuple/status/path/hash facts and keep evidence untracked. |
| T-13-50-05 | Repudiation | ordered browser proof | medium | mitigate | Repository core, not the browser adapter, validates the pending pair, writes receipts, and enforces Dashboard-before-Viewer before the one post-observation verifier. |
| T-13-50-06 | Elevation of privilege | remote/provider/production operations | high | mitigate | Limit every task to local mode and local Gateway; exclude remote handoff, provider and separate D1/API/Wrangler commands, production URL/browser work, deploy, and migration. |
| T-13-50-07 | Denial of service | current root development tree | low | accept | Reuse the healthy tree, run bounded readiness only, and do not stop a PID or invoke port cleanup. |
| T-13-50-SC | Tampering | package execution | medium | mitigate | Do not install packages; require the existing root-launcher process regression and committed package scripts before the live local sequence. |
</threat_model>

<verification>
Use the GSD executor's explicit single-plan dispatch for 13-50 only, without a generic `--wave` selector. Before allocation, require the visible in-app Dashboard gate, root-launcher regression, projection, local preflight, and two accepted Gateway readiness records. After one handoff, invoke the pending exact verifier once. Let repository-owned observation use the existing in-app Browser session once, then invoke the terminal exact verifier once. Confirm evidence remains untracked and GitNexus reports no plan-owned source-symbol or execution-flow change before the narrowly allowed execution closeout.
</verification>

<success_criteria>
- The GSD executor explicitly dispatches only 13-50 and does not select 13-33 or any other historical plan.
- No p13-50 id or evidence exists unless the same in-app Browser visibly shows an authenticated local Dashboard and all local automated gates pass.
- One allocated p13-50 run has exactly one handoff, one pending exact verifier, one repository-core observation using the existing in-app Browser, and one terminal exact verifier.
- Terminal local success is reported only as `terminal_passed` with `provesExternalChain: true` for the identical target/run/code/itemId tuple after Dashboard then Viewer receipts.
- Every p13-50 failure is durable and truthful; evidence is untracked and no remote, provider, production, historical-run, source, or planning-state mutation occurs.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md` only when the selected attempt reaches terminal local proof or a required gate stops it. Keep evidence untracked; execution closeout may include only `13-50-RUN-ID.txt` when allocated and `13-50-SUMMARY.md`.
</output>
