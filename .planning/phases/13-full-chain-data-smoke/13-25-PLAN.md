---
phase: 13-full-chain-data-smoke
plan: "25"
type: execute
wave: 23
depends_on: [13-13, 13-14, 13-15]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md"
autonomous: true
gap_closure: true
requirements: [DATA-01, DATA-02, DATA-07, TEST-05]
must_haves:
  truths:
    - "D-03: 13-24 is immutable `blocked_after_launch` history. A new retry may start only after one immediate all-free snapshot of 8080, 8787, 5173, 3002, 3003, 3000, and 3001; a listener is a closed no-launch/no-stop blocker."
    - "D-03/D-06: only one root task-owned `pnpm dev` tree may start, through the current root `scripts/local-dev-entry.ts` entry. The Summary records the absolute supervisor command line and current launcher-to-supervisor-to-listener ancestry before canonical Gateway readiness is accepted."
    - "D-03/D-06: a release candidate requires all seven task-owned listeners, accepted Gateway `/robots.txt`, `/auth`, and `/auth/`, `check:services`, and the relevant 13-13 through 13-15 local regression gates."
    - "D-06: only a successful child-before-parent cleanup of the newly launched task-owned tree followed by an all-free post-cleanup snapshot may publish exactly the downstream release contract `runtime_eligibility: released`, `runtime_lifecycle: cleaned`, and `post_cleanup_fixed_ports: all_free`. Every other terminal state is blocked and omits those release fields."
    - "D-01, D-02, D-04, D-05, and D-07 remain unexecuted here: this plan allocates no run id, creates no evidence or fixture, and performs no D1, API, browser, provider, remote, deploy, or migration action."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md"
      provides: "Non-secret fixed-port retry record with launcher/supervisor/listener ancestry, canonical readiness, regression, cleanup, and the mutually exclusive three-field release contract."
      contains: "runtime_eligibility"
  key_links:
    - from: "root `pnpm dev` through scripts/local-dev-entry.ts"
      to: "absolute scripts/local-dev.ts supervisor command line and seven fixed listeners"
      via: "current Win32_Process parent chains recorded from each listener to the task-owned root launcher"
      pattern: "absolute_supervisor_command_line"
    - from: "13-25-SUMMARY.md"
      to: "13-17 through 13-20"
      via: "downstream plans consume only runtime_eligibility, runtime_lifecycle, and post_cleanup_fixed_ports"
      pattern: "runtime_eligibility: released"
---

<objective>
Run one fresh all-free fixed-port retry after the immutable 13-24 `blocked_after_launch` checkpoint, using the corrected root `pnpm dev` entry that launches `scripts/local-dev.ts` by absolute path. Prove the new task-owned tree can become canonically ready and be safely removed without touching an external PID.

Purpose: 13-24 remains historical diagnosis. This replacement retry is local runtime eligibility only and cannot allocate a data-chain attempt or imply Phase 13 completion.

Output: `13-25-SUMMARY.md` with one closed blocked result, or the sole released-and-cleaned three-field runtime contract required by 13-17 through 13-20.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-24-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-24-SUMMARY.md
@.planning/debug/phase13-launcher-early-exit.md
@package.json
@scripts/local-dev-entry.ts
@scripts/local-dev.ts
@scripts/local-dev-authorization.ts
@scripts/check-services.ps1
@scripts/gateway-readiness.ts
@packages/config/src/deployment-target/__tests__/local-dev.test.ts
@packages/config/src/deployment-target/__tests__/local-dev-authorization.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate and start one current-source task-owned retry tree</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-24-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-24-SUMMARY.md
    - .planning/debug/phase13-launcher-early-exit.md
    - package.json
    - scripts/local-dev-entry.ts
    - scripts/local-dev.ts
    - scripts/local-dev-authorization.ts
  </read_first>
  <action>Preserve 13-24, all historical Summaries, and all evidence directories as immutable inputs. Resolve the repository root, `pnpm.cmd`, `scripts/local-dev-entry.ts`, and `scripts/local-dev.ts` to absolute paths. Immediately before any launch, take exactly one fixed-port listener snapshot with `Get-NetTCPConnection -State Listen` for only 8080, 8787, 5173, 3002, 3003, 3000, and 3001. If any selected port has a listener, write a terminal `runtime_eligibility: blocked`, `terminal_branch: blocked_pre_start`, `closed_reason: fixed_ports_not_free`, `launch_attempted: false`, and `cleanup_attempted: false` Summary that records only selected port numbers/counts; stop without `Stop-Process`, `taskkill`, port cleanup, source mutation, run allocation, evidence, smoke, fixture, D1, API, browser, provider, or remote command. Only when all seven ports are free, start exactly one hidden root task-owned `pnpm.cmd dev` process with `Start-Process -PassThru` at the resolved repository root. Record the root PID and its absolute launcher command. Poll boundedly for exactly one descendant Node supervisor whose command line contains the resolved absolute `scripts/local-dev.ts` path produced through `scripts/local-dev-entry.ts`; record its exact absolute command line, PID, and the complete current parent chain to the launcher. A launcher exit, absent/duplicate supervisor, missing absolute script path, or ancestry mismatch writes terminal `runtime_eligibility: blocked`, `terminal_branch: blocked_after_launch`, one allowlisted closed reason, and leaves cleanup to Task 3. Do not reuse or inspect a historical PID as an owned process.</action>
  <acceptance_criteria>
    - The immediate pre-start snapshot covers exactly all seven fixed ports; any listener produces no launch and no process mutation.
    - A started retry has exactly one recorded root launcher, exactly one current-workspace supervisor, an absolute `scripts/local-dev.ts` command line, and a parent chain from supervisor to launcher.
    - No branch allocates a run id, creates evidence, invokes a fixture/smoke/handoff, or accesses D1, API, browser, provider, or remote state.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md'; if (-not (Test-Path -LiteralPath $p)) { throw '13-25 Summary missing' }; $raw=Get-Content -Raw -LiteralPath $p; if ($raw -notmatch '(?m)^runtime_eligibility:\s*(blocked|pending)\s*$') { throw 'Task 1 must persist blocked or pending status' }; if ($raw -match '(?m)^terminal_branch:\s*blocked_pre_start\s*$' -and $raw -notmatch '(?m)^launch_attempted:\s*false\s*$') { throw 'Pre-start blocker launched a process' }; if ($raw -match '(?m)^runtime_eligibility:\s*pending\s*$' -and ($raw -notmatch '(?m)^absolute_supervisor_command_line:\s*.+scripts[\\/]local-dev\.ts.+$' -or $raw -notmatch '(?m)^task_owned_launcher_pid:\s*\d+\s*$')) { throw 'Pending retry lacks absolute supervisor or launcher facts' }"</automated>
  </verify>
  <done>A listener collision stops before mutation, or one new root-owned current-source supervisor is attributable to the task launcher.</done>
</task>

<task type="auto">
  <name>Task 2: Require all listener ancestry, canonical readiness, and local regressions</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md</files>
  <read_first>
    - scripts/local-dev-entry.ts
    - scripts/local-dev.ts
    - scripts/local-dev-authorization.ts
    - scripts/check-services.ps1
    - scripts/gateway-readiness.ts
    - .planning/phases/13-full-chain-data-smoke/13-13-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-14-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-15-PLAN.md
  </read_first>
  <action>Continue only from Task 1's pending task-owned supervisor. Take a fresh seven-port listener snapshot and a current Win32 process snapshot. Require exactly one owner per fixed port and require every listener owner to reach the recorded absolute-command-line supervisor and then the recorded launcher, without a cycle, PID reuse, external ancestor substitution, or early child exit. Persist non-secret sorted port/owner PID/parent-chain facts and the launcher/supervisor chain in the Summary. Any failure writes a closed `runtime_eligibility: blocked` / `terminal_branch: blocked_after_launch` result with one allowlisted ownership reason and skips all data-chain activity. When ancestry is valid, run `pnpm check:services` with a bounded 60-second poll and accept only exit 0 plus one final `starye-local-services-1` machine record with all seven listeners and canonical Gateway `/robots.txt`, `/auth`, and `/auth/` accepted. Direct app ports remain diagnostics only. Then run the relevant 13-13 through 13-15 local regression gates, without any smoke run: `pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts src/deployment-target/__tests__/local-dev-authorization.test.ts src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-cli-process.test.ts src/deployment-target/__tests__/data-chain-smoke-process.test.ts src/deployment-target/__tests__/data-chain-evidence.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts`; `pnpm --filter @starye/config type-check`; `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json`; `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-readiness.json`; `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-handoff.json`; and `pnpm --filter gateway test`. Only every listener, canonical route, check-services record, and regression pass may write `runtime_eligibility: pending`, `terminal_branch: pending_cleanup`, and `local_regressions: passed`; Task 3 alone owns release. A failed readiness/regression gate is terminally blocked and still only Task 3 may attempt cleanup of the proven task-owned tree.</action>
  <acceptance_criteria>
    - Seven listener ownership chains each reach the recorded supervisor and recorded launcher; the Summary retains the absolute supervisor command line and sorted ancestry facts.
    - `check:services` accepts only canonical Gateway routes and every listed local regression gate passes before the release-candidate state.
    - Direct ports, run IDs, evidence paths, fixtures, D1, API, browser, provider, remote commands, and Phase-completion claims are absent from this retry.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md'; $raw=Get-Content -Raw -LiteralPath $p; if ($raw -match '(?m)^runtime_eligibility:\s*released\s*$') { throw 'Task 2 cannot release eligibility' }; if ($raw -match '(?m)^runtime_eligibility:\s*pending\s*$') { foreach($field in 'task_owned_launcher_pid','task_owned_supervisor_pid','absolute_supervisor_command_line','canonical_robots_txt: accepted','canonical_auth: accepted','canonical_auth_slash: accepted','local_regressions: passed') { if ($raw -notmatch [regex]::Escape($field)) { throw \"Pending Summary missing $field\" } } } elseif ($raw -notmatch '(?m)^runtime_eligibility:\s*blocked\s*$') { throw 'Task 2 must leave a pending candidate or blocked result' }"</automated>
  </verify>
  <done>Only an all-seven, task-owned, canonically ready retry with passing local regressions can become a cleanup candidate.</done>
</task>

<task type="auto">
  <name>Task 3: Clean only the new task-owned tree and publish the closed release contract</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-24-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-17-PLAN.md
    - scripts/local-dev-entry.ts
    - scripts/local-dev.ts
  </read_first>
  <action>Close every Task 1 or Task 2 branch. The Summary must contain exactly one `runtime_eligibility` and exactly one `terminal_branch`. A `blocked_pre_start` branch preserves `fixed_ports_not_free`, `launch_attempted: false`, and `cleanup_status: not_required_no_launch`; it never inspects or changes any process after the gate. For a started branch, re-snapshot processes and calculate a child-before-parent stop list only from PIDs that still descend from Task 1's recorded launcher and supervisor. Never expand that set from a port owner, process name, workspace search, old Summary, or newly discovered root. If the recorded tree cannot be proven unchanged and task-owned, write `runtime_eligibility: blocked`, `terminal_branch: blocked_after_launch`, `closed_reason: cleanup_ownership_unproven`, `cleanup_attempted: false`, and `cleanup_status: no_stop_unproven_tree`; target no PID. Otherwise stop only the revalidated descendants child-before-parent, then the supervisor and launcher if still in that tree, and wait boundedly only for those recorded PIDs. A stop failure, surviving task PID, or timeout is `blocked_after_cleanup` with exactly one cleanup reason and `cleanup_status: failed`. A Task 1/2 blocker whose owned cleanup succeeds remains blocked with its original reason and `cleanup_status: cleaned`; do not publish release fields. Only when every Task 1/2 candidate gate passed, every revalidated task-owned PID exited, and a fresh all-seven-port listener snapshot is free, publish exactly once `runtime_eligibility: released`, `runtime_lifecycle: cleaned`, and `post_cleanup_fixed_ports: all_free`, plus non-secret recorded ancestry/readiness/regression facts. Do not add `13_17_eligibility` or any other downstream eligibility field: 13-17 through 13-20 consume only those three release fields. A blocked Summary omits all three release fields and contains one closed reason. No branch creates a run ID/evidence/fixture, invokes smoke/D1/API/browser/provider/remote work, or asserts Phase success. Before a Summary-only commit, run GitNexus detect-changes and require no changed code symbol or execution flow attributable to this plan; preserve unrelated dirty files.</action>
  <acceptance_criteria>
    - Cleanup may stop only the current revalidated descendant set of the newly launched tree, in child-before-parent order; no external PID is ever targeted.
    - Only a fully passed candidate with successful cleanup and a post-cleanup all-free snapshot has all three exact release fields; every blocked branch omits them.
    - The release certifies a cleaned local cold-start capability only, never a live server, run allocation, evidence, smoke, provider result, or Phase 13 completion.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md'; $raw=Get-Content -Raw -LiteralPath $p; function Values([string]$field) { @([regex]::Matches($raw, '(?m)^' + [regex]::Escape($field) + ':\s*(.*?)\s*$') | ForEach-Object { $_.Groups[1].Value.Trim() }) }; $runtime=@(Values 'runtime_eligibility'); $branch=@(Values 'terminal_branch'); if ($runtime.Count -ne 1 -or $branch.Count -ne 1 -or $runtime[0] -notin @('released','blocked')) { throw 'Summary state is not mutually exclusive' }; $releaseFields=@('runtime_eligibility','runtime_lifecycle','post_cleanup_fixed_ports'); if ($runtime[0] -eq 'released') { if ($branch[0] -ne 'released') { throw 'Released branch mismatch' }; if (@(Values 'runtime_lifecycle').Count -ne 1 -or @(Values 'runtime_lifecycle')[0] -ne 'cleaned') { throw 'Released runtime not cleaned' }; if (@(Values 'post_cleanup_fixed_ports').Count -ne 1 -or @(Values 'post_cleanup_fixed_ports')[0] -ne 'all_free') { throw 'Released ports not all free' }; $ports=8080,8787,5173,3002,3003,3000,3001; if (@(Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue | Where-Object { $ports -contains $_.LocalPort }).Count -ne 0) { throw 'Released cleanup left a fixed-port listener' } } else { if (@(Values 'closed_reason').Count -ne 1) { throw 'Blocked Summary needs one closed reason' }; foreach($field in @('runtime_lifecycle','post_cleanup_fixed_ports')) { if (@(Values $field).Count -ne 0) { throw 'Blocked Summary carries release field' } } }"</automated>
  </verify>
  <done>The only successful result is a released, cleaned, all-free three-field contract from a new task-owned tree; every other result is a closed no-external-PID blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical runtime -> new retry | 13-21 through 13-24 PIDs and evidence are untrusted historical inputs and cannot be reused or stopped. |
| Root launcher -> local-dev descendants | Current absolute command line and parent-chain evidence is required before a process is treated as task owned. |
| Listener ports -> readiness | Listener state proves ownership only; canonical Gateway routes decide readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-25-01 | Tampering | Fixed-port retry | high | mitigate | Immediate seven-port all-free gate; collision is no-launch/no-stop. |
| T-13-25-02 | Elevation | Process cleanup | high | mitigate | Revalidate only launcher/supervisor descendants and stop child-before-parent. |
| T-13-25-03 | Denial of Service | Root dev tree | medium | mitigate | Bounded startup/readiness polling and owned-PID exit checks. |
| T-13-25-04 | Repudiation | Runtime release | high | mitigate | Mutually exclusive Summary state and exact three-field downstream contract. |
| T-13-25-05 | Information Disclosure | Summary | low | mitigate | Record only non-secret PIDs, command paths, ancestry, route/status, and regression facts. |
| T-13-25-SC | Tampering | Package execution | low | accept | No dependency installation or package-manager mutation is authorized. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Gateway-first local prerequisite for the full chain | 13-25 then 13-17 through 13-20 | COVERED | 13-25 is eligibility only. |
| REQ | DATA-01 | Canonical local route readiness | 13-25, 13-17, 13-18 | COVERED | `/robots.txt`, `/auth`, `/auth/` through 8080. |
| REQ | DATA-02 | Local D1/minimal data | 13-17 | COVERED | Explicitly excluded from 13-25. |
| REQ | DATA-03 | One deterministic fixture/item | 13-17, 13-19 | COVERED | Explicitly excluded from 13-25. |
| REQ | DATA-04 | D1/API/admin proof | 13-17 through 13-20 | COVERED | No API/D1 action here. |
| REQ | DATA-05 | Dashboard proof | 13-18, 13-20 | COVERED | IAB boundary unchanged. |
| REQ | DATA-06 | Canonical viewer proof | 13-18, 13-20 | COVERED | IAB boundary unchanged. |
| REQ | DATA-07 | Non-secret repeatable artifacts | 13-25, 13-17 through 13-20 | COVERED | 13-25 writes only its Summary. |
| REQ | TEST-05 | Repeatable local smoke/readiness checks | 13-25, 13-17 through 13-20 | COVERED | Local regression suite precedes eligibility. |
| CONTEXT | D-03 | Local-first Gateway order | 13-25 before 13-17 | COVERED | Direct ports remain diagnostic. |
| CONTEXT | D-06 | Non-secret repeatable facts | 13-25 | COVERED | Summary uses allowlisted local facts. |
</source_audit>

<verification>
- Validate the immediate seven-port gate, absolute current-source supervisor command line, launcher/listener ancestry, canonical Gateway readiness, and the listed local regression commands.
- Execute no data-chain, evidence, fixture, D1, API, browser, provider, remote, deployment, migration, or cleanup-of-external-process command.
- Accept a released Summary only after all task-owned PIDs exit and the fresh post-cleanup seven-port snapshot is all free; otherwise keep the closed blocked branch.
</verification>

<success_criteria>
- A listener at the immediate gate prevents every process mutation and produces a closed blocked result.
- A release proves one newly task-owned current-source `pnpm dev` tree, its absolute supervisor command line, all seven listener chains, canonical routes, local regressions, child-before-parent cleanup, and post-cleanup all-free ports.
- Plans 13-17 through 13-20 consume only `13-25-SUMMARY.md` fields `runtime_eligibility: released`, `runtime_lifecycle: cleaned`, and `post_cleanup_fixed_ports: all_free`.
- Phase 13 remains In Progress until the existing local handoff, IAB, selected-target, and canonical verifier plans succeed.
</success_criteria>

## Artifacts This Phase Produces

- `.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md` with the non-secret retry lifecycle and the three-field release contract when, and only when, all gates pass.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-25-SUMMARY.md` only when executed. Do not modify 13-24, historical summaries/evidence, application code, Phase evidence, ROADMAP, STATE, REQUIREMENTS, or 13-VERIFICATION.md during execution.
</output>
