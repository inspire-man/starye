---
phase: 13-full-chain-data-smoke
plan: "28"
type: execute
wave: 30
depends_on: [13-26]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md"
autonomous: true
gap_closure: true
requirements: [DATA-01, TEST-05]
---

<objective>
Perform one basic, fresh local service smoke after the root-loader repair: all ports free, start `pnpm dev`, pass `pnpm check:services`, stop the freshly observed fixed-port listeners, and confirm every fixed port is free.
</objective>

<tasks>
<task type="auto">
  <name>Run the basic local lifecycle smoke</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md</files>
  <action>Require ports 8080, 8787, 5173, 3002, 3003, 3000, and 3001 to be free before launch. Start one root `pnpm.cmd dev` process, wait only until all seven fixed ports are listening, then run `pnpm check:services`. Because the pre-launch fixed-port set was empty, record the seven newly observed listener PIDs as this smoke's cleanup set. Stop only those PIDs, wait briefly, and confirm all seven ports are free. On any failure, record a blocked result without data-chain, remote, browser, or broad test activity.</action>
  <verify>
    <automated>pnpm check:services</automated>
  </verify>
  <done>A passed service check plus cleanup to all-free ports publishes the three lifecycle release fields.</done>
</task>
</tasks>

<success_criteria>
- The canonical Gateway passes `pnpm check:services`.
- Only listener PIDs born after an all-free gate are stopped.
- Success has `runtime_eligibility: released`, `runtime_lifecycle: cleaned`, and `post_cleanup_fixed_ports: all_free`; no data-chain claim is made.
</success_criteria>
