---
phase: 13-full-chain-data-smoke
plan: "08"
type: execute
wave: 7
depends_on:
  - "13-07"
files_modified:
  - .planning/phases/13-full-chain-data-smoke/evidence/
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Local Starye stack and Dashboard session
    why: A real local D1/API/Gateway/browser run requires existing user-managed secrets, running local services, and an authorized Dashboard account.
    env_vars:
      - name: BETTER_AUTH_SECRET
        source: User-managed local API worker secret store
      - name: CRAWLER_SECRET
        source: User-managed local crawler/API service-auth secret store
      - name: ADMIN_GITHUB_ID
        source: User-managed local Dashboard authorization configuration
    dashboard_config:
      - task: Sign in through the local Gateway and confirm the account can access Movies management.
        location: http://localhost:8080/auth/
  - service: Cloudflare selected target and canonical-domain browser session
    why: The remote run requires explicit target credential keys, read-only provider ownership checks, bounded fixture access, and an authorized selected-target Dashboard/browser session.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: Authorized selected-target operator environment
      - name: CLOUDFLARE_ACCOUNT_ID
        source: Authorized selected-target operator environment
      - name: CRAWLER_SECRET
        source: Authorized selected-target operator environment
    dashboard_config:
      - task: Sign in through the selected target canonical Gateway and confirm access to Movies management before the browser observation step.
        location: Selected target canonical Gateway domain
must_haves:
  truths:
    - "A newly executed local smoke creates one terminal, provenance-backed tuple through local D1, Gateway API, authorized Dashboard, and local Gateway viewer; all canonical browser routes use http://localhost:8080."
    - "Only the exact terminal local tuple may start remote smoke; an authorized selected-target run proves provider preflight/ownership, one fixture, D1, canonical API, authorized Dashboard, and canonical viewer with matching provenance receipts."
    - "Any missing local secret/session/browser/provider/role/ownership condition is recorded as a redacted checkpoint and never claimed as local or production success."
  artifacts:
    - .planning/phases/13-full-chain-data-smoke/evidence/
  key_links:
    - "authorized local command -> one-item local runner receipt -> controlled local Dashboard/viewer receipts -> terminal local evidence"
    - "exact terminal local evidence -> explicit selected-target preflight/provider receipts -> controlled canonical Dashboard/viewer receipts -> terminal remote evidence"
---

<objective>
Run the newly hardened local and selected-target remote Phase 13 smoke chain, retaining only real provenance-backed evidence or an honest redacted checkpoint at the unavailable external boundary.

Purpose: G-02 cannot be satisfied by tests or static files. The phase goal requires an actual local Gateway path and an actual selected-target provider/canonical-domain path for the same bounded one-item contract.
Output: a newly created local evidence pair and its exact remote evidence pair, each terminal only when independently captured runner/browser/provider receipts validate.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-03-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-04-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-07-SUMMARY.md
@RUNBOOK.md
@scripts/data-chain-smoke.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@scripts/target-profile.ts
</context>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | 13-07 provenance contract; real local stack/session | Fresh local runner receipt and controlled local browser receipts for one tuple, or checkpoint | First; remote scope is forbidden without this exact terminal pair. |
| 2 | Task 1 exact terminal local pair; authorized selected target/provider/session | Fresh remote provider and canonical browser receipts for the matching tuple, or checkpoint | Second; the remote runner validates the local pair before any provider child. |
</dependency_graph>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute and inspect one provenance-backed local Gateway smoke</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-07-SUMMARY.md
    - scripts/data-chain-smoke.ts
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - RUNBOOK.md
  </read_first>
  <what-built>The preceding plans restore a one-item fixture and require actual runner/browser provenance before an artifact can become terminal. Local mode already owns selected-target projection, local D1, service, Gateway auth, fixture, snapshot, and canonical API ordering.</what-built>
  <how-to-verify>1. Ensure the existing user-managed local secret values are present without adding them to Git or an evidence artifact, start the documented local stack, and sign in through `http://localhost:8080/auth/` with a Dashboard account that can access Movies. 2. Generate a new opaque run id and execute `pnpm smoke:data-chain -- --mode local --target starye-org --run-id <run-id>`. 3. If the runner reports checkpoint, keep its newly written JSON/Markdown pair and stop: do not retry into a claim of success. 4. Only when it produces the exact one-item pending tuple, invoke the hardened observer for Dashboard then viewer; it must control the real local browser, inspect `/dashboard/movies` and `/movie/<item-code>` via `http://localhost:8080`, and append only receipts it captured itself. 5. Validate the completed local pair with `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id <run-id> --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence`. 6. Inspect the evidence summary: it must have one non-empty code/id, count one, runner receipts for local projection/D1/service/Gateway/API, browser receipts for Dashboard/viewer, no direct application origin, and no secret-like fields.</how-to-verify>
  <action>Use only the exact hardened scripts and the local Gateway canonical base. Per D-03, local Gateway/D1/readiness evidence precedes any remote attempt. The operator authenticates in the browser; the agent does not create a cookie, copy credentials, or add user-managed secrets to files. A local checkpoint is a valid truthful outcome and blocks Task 2. Do not use app ports, direct API calls, preexisting evidence, a manually edited artifact, a browser screenshot alone, an operator-supplied success flag, or a different target/run tuple as proof. Keep generated evidence untracked unless the repository evidence policy explicitly permits a reviewed non-secret terminal artifact.</action>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id <run-id> --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
  </verify>
  <acceptance_criteria>
    - A terminal local pair contains the same target/run/code/id across all required receipts, one fixture/snapshot count, and fixed local Gateway paths only.
    - Any unavailable local secret, service, D1, auth/session, Dashboard role, browser, route, or viewer condition becomes a redacted checkpoint; it is not passed to remote mode.
    - The validator rejects old/self-attested evidence and accepts this new artifact only when its captured receipts validate.
  </acceptance_criteria>
  <done>Local Phase 13 proof is either independently evidenced through the local Gateway or explicitly checkpointed without a synthetic pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute and inspect the exact selected-target provider and canonical-domain smoke</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-07-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/evidence/
    - scripts/data-chain-smoke.ts
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - scripts/target-profile.ts
    - RUNBOOK.md
  </read_first>
  <what-built>Task 1 supplies either an exact terminal local pair or a blocking checkpoint. The hardened remote runner is explicit-target, preflight/ownership first, fixed-entry only, and now requires provider/browser receipts before remote evidence can resolve.</what-built>
  <how-to-verify>1. Proceed only if Task 1's newly created local pair is terminal and validates with all one-item local receipts. 2. In an authorized selected-target operator environment, make the credential key values available only to the process and run `pnpm smoke:data-chain -- --mode remote --target starye-org --run-id <the-same-run-id>`. The script must first perform selected-target preflight, credential-key presence, account/resource ownership and read-only live checks. 3. If preflight/provider access fails, retain the checkpoint pair and stop before any claimed remote mutation. 4. If fixed fixture/snapshot/canonical API receipts produce a pending remote tuple, sign in through the selected target's canonical Gateway with a Dashboard account authorized for Movies; run the hardened observer for Dashboard then `/movie/<item-code>` viewer using that canonical base. 5. Validate the exact remote pair with `pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id <the-same-run-id> --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence`. 6. Confirm one matching target/run/code/id across provider, D1, canonical API, Dashboard, and viewer receipts; inspect that no token, cookie, raw provider endpoint, prepared context, or direct application port entered the evidence.</how-to-verify>
  <action>This is an external Cloudflare/provider/browser authorization boundary. Per D-04/D-05/D-06/D-07, use the same item tuple on the selected canonical target, complete target preflight/ownership before the bounded write, preserve only non-secret evidence, and treat real provider execution as Phase 13 scope. Do not assume credentials or a Dashboard session are available. The only permitted remote mutation is the runner's existing explicit-target, preflight-approved, fixed one-item fixture entry; do not issue ad hoc Wrangler, SQL, crawler, deploy, rollback, or Dashboard mutation commands. A failed credential/ownership/provider/browser/session/viewer condition must produce a redacted checkpoint and no remote success claim. Do not use the local browser, arbitrary canonical domain, manually edited JSON/Markdown, a status argument, or schema validation alone as substitute evidence. Keep target profile/preflight boundaries and no-direct-app-port policy intact.</action>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id <run-id> --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
  </verify>
  <acceptance_criteria>
    - A terminal remote pair follows the exact terminal local pair, includes explicit selected-target provider/preflight receipts, and proves the same one-item tuple in remote D1, canonical API, authorized Dashboard, and canonical viewer.
    - A missing credential, provider ownership, browser/session/role, canonical route, or item correlation state remains checkpoint/non-success and cannot be changed into terminal evidence by command arguments or file edits.
    - The final artifact contains only the allowlisted receipt fields and selected-target canonical paths, not secrets or direct service ports.
  </acceptance_criteria>
  <done>Production proof is either a provenance-backed selected-target full chain for the exact local tuple or an honest checkpoint at the external prerequisite boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User-managed local secrets/session -> local smoke | The agent must not materialize user values, impersonate the operator, or treat stale artifacts as a run. |
| Provider credentials/ownership -> remote fixed mutation | Authorization controls the ability to run one selected-target fixture and must fail closed. |
| Authorized browser -> terminal receipt | Browser state is external; only controlled observation of the exact canonical route can certify success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-33 | Spoofing | local/remote session | high | mitigate | Require real Gateway/canonical sign-in and repository-controlled observer; no fabricated cookie or status argument. |
| T-13-34 | Elevation of Privilege | provider mutation | critical | mitigate | Explicit target, required credential keys, ownership/live checks, closed registry, one-item entry, and no ad hoc remote commands. |
| T-13-35 | Information Disclosure | evidence and console output | critical | mitigate | Keep secret values process-only and validate allowlisted receipt/evidence fields before retention. |
| T-13-36 | Repudiation | Phase 13 success claim | high | mitigate | Retain new tuple-bound runner/provider/browser receipts and reject self-attested legacy artifacts. |
| T-13-37 | Tampering | evidence input | high | mitigate | Exact local-to-remote tuple validation, deterministic renderer/validator, and no manual artifact edits. |
</threat_model>

<verification>
- Run the focused automated regression suite from 13-07 before each checkpoint.
- Validate each newly generated pair with the exact mode/target/run wrapper shown in the checkpoint task; record only the wrapper result and allowlisted artifact paths in the summary.
- Before committing any tracked planning/evidence artifact, run GitNexus `detect_changes`; source symbol scope should remain empty for this execution-only plan.
</verification>

<success_criteria>
Phase 13 obtains a fresh, auditable local and remote proof using one bounded item and the canonical Gateway/selected target domain, or truthfully preserves an external checkpoint with no fabricated completion.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md` when done.
</output>
