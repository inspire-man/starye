---
phase: 13-full-chain-data-smoke
plan: "77"
type: execute
wave: 71
depends_on: [13-76]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-77-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Root in-app Browser local Dashboard session
    why: 新 carrier 只能由同一个 root-IAB session 的 readiness 与回执端口共同证明；若本地 Dashboard 未登录，操作者仅在该 Browser 内完成登录。
must_haves:
  truths:
    - "仅当同一个 root-IAB 端口在 Gateway canonical 本地 Dashboard 上就绪并可读取 Plan 13-76 的 code/id DOM tuple 时，才创建一个新的 p13-77 carrier，per D-03、D-04、D-05、D-06。"
    - "该 carrier 只经 repository-owned local handoff 写入一条 deterministic non-R18、one-player fixture，且 local itemId 非空，per D-01、D-02、D-04。"
    - "相同 root-IAB 以 Dashboard 再 Viewer 顺序读取完全相等的 target/run/code/itemId；仅 terminal_passed 与 provesExternalChain true 可进入人工 remote 授权，per D-03、D-04、D-06、D-07。"
    - "任意首个本地非成功会冻结 p13-77，并禁止重试、第二次 handoff、remote、production 或 verifier 后续动作；既有 p13-66、p13-73 与全部历史 carrier 保持不可变。"
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-77-RUN-ID.txt
      provides: 同一 root-IAB 资格与两次 canonical readiness 通过后创建的唯一 p13-77 标识符。
    - path: .planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md
      provides: 不含密钥的资格、一次 local handoff、root-IAB 顺序观察、verifier 分支与冻结结果。
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-77-run-id}/local.json
      provides: 未跟踪的、tuple-bound local terminal 或首个 checkpoint 证据。
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-77-run-id}/local.md
      provides: 与 local JSON 一致的未跟踪 Markdown 证据。
  key_links:
    - from: verifyRootIabObservationReadiness
      to: 13-77-RUN-ID.txt
      via: 同一 root-IAB owner 在 http://localhost:8080/dashboard/movies 的 canonical readiness 与 tuple descriptor 合格后才允许分配。
      pattern: root_iab and ready
    - from: runDataChainHandoffCore
      to: root IAB observation
      via: local pending pair 的 runnerInvocations 为 1、target/run-derived code 精确且 local itemId 非空。
      pattern: handoffReady true
    - from: ordered local receipts and verifier
      to: 13-78 authorization
      via: 唯一可进入 remote 授权的状态是 terminal_passed 和 provesExternalChain true。
      pattern: terminal_passed and provesExternalChain true
---

<objective>
在 Plan 13-76 已提供 root-IAB 可读 DOM tuple 后，创建一个从未使用过的本地 root-IAB carrier，并以严格 Dashboard→Viewer exact tuple 终态或首个不可变 checkpoint 结束。

Purpose: 13-73 是 immutable dashboard_auth_unavailable checkpoint，13-76 没有创建 carrier。当前缺口要求新的 local proof，不能观察、验证、重放或提升任何历史 run。
Output: 条件性 `13-77-RUN-ID.txt`、未跟踪 p13-77 local JSON/Markdown、以及 `13-77-SUMMARY.md`；本计划没有 provider、remote、production Browser 或 Phase 14 动作。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
</context>

<interfaces>
- `verifyRootIabObservationReadiness` 只接受解析后的 local target 与同一 root-IAB port，且在尚未读取证据、分配 run ID 或运行 handoff 前返回 allowlisted readiness。
- `runDataChainHandoffCore` 是唯一 local reservation、one-item fixture、pending-pair 验证所有者；只接受显式 mode、target、run ID。
- `observeDataChainSurfaces` 用同一 in-memory root-IAB port，按 repository-owned descriptor 读取 Plan 13-76 的同元素 code/id，并由 core 保留 canonical path、receipt、顺序与 equality 校验。
- `inspectDataChainSmokeVerification` 只在两张 local receipt 均通过后调用一次；不启动 runner、不修复 checkpoint。
</interfaces>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 13 | fresh local-to-production canonical data-chain proof | 13-77..13-80 | COVERED | 先恢复新的 local terminal carrier，再以分离授权与 remote/prod 步骤收口。 |
| REQ | DATA-01 | Gateway-only local API/auth/Dashboard/content | 13-77 | COVERED | 本地 browser/readiness 仅使用 `http://localhost:8080/...`。 |
| REQ | DATA-02 | local D1/minimal-data readiness before ingest | 13-77 | COVERED | 只通过既有 handoff 的内建 gate 继续。 |
| REQ | DATA-03 | one deterministic controlled fixture | 13-77, 13-79 | COVERED | 每个 mode 仅一个 repository-owned handoff。 |
| REQ | DATA-04 | D1/API/admin tuple correlation | 13-77, 13-79, 13-80 | COVERED | 所有 eligibility 和 receipt 均使用 exact target/run/code/mode-owned itemId。 |
| REQ | DATA-05 | authenticated Dashboard validation | 13-77, 13-80 | COVERED | local 与 production 都使用 root-IAB 真实 session。 |
| REQ | DATA-06 | canonical Viewer validation | 13-77, 13-80 | COVERED | Dashboard 必先于 Viewer；production 仅 selected canonical domain。 |
| REQ | DATA-07 | non-secret repeatable evidence | 13-77..13-80 | COVERED | 每个 run 的 first checkpoint 保留且不被后续伪造结果覆盖。 |
| REQ | TEST-05 | repeatable terminal/checkpoint output | 13-77..13-80 | COVERED | 既有 focused tests 先行，exact verifier 只用于合格 receipt pair。 |
| RESEARCH | R-01 | closed target/handoff/receipt seams and no package addition | 13-77..13-80 | COVERED | 复用既有入口，不安装包、不引入自由 argv。 |
| RESEARCH | R-02 | provider mutation must be explicit-target and preflight-owned | 13-78, 13-79 | COVERED | human authorization 与唯一 handoff 分离。 |
| RESEARCH | R-03 | no cookie/profile/header/provider data in evidence | 13-77..13-80 | COVERED | root-IAB 只交付 status/code/itemId，summary/evidence 不跟踪 secret。 |
| CONTEXT | D-01 | exactly one non-R18, one-player item | 13-77, 13-79 | COVERED | 只接受 count-one handoff 结果。 |
| CONTEXT | D-02 | controlled fixture or adapter only | 13-77, 13-79 | COVERED | 禁止全量 crawler 与平行写入路径。 |
| CONTEXT | D-03 | local-first canonical Gateway ordering | 13-77 | COVERED | local terminal proof 先于 remote authorization。 |
| CONTEXT | D-04 | exact target/run/code plus mode-owned itemId | 13-77..13-80 | COVERED | 任何 partial/mismatch 都是首个 non-success。 |
| CONTEXT | D-05 | selected target/preflight/provider fail-closed | 13-78, 13-79 | COVERED | 未获 run-bound literal authority 前没有 remote 行为。 |
| CONTEXT | D-06 | allowlisted non-secret evidence | 13-77..13-80 | COVERED | 不记录 cookie、token、header、profile、raw endpoint 或 prepared context。 |
| CONTEXT | D-07 | real Phase 13 proof, no Phase 14 substitution | 13-77..13-80 | COVERED | 生产终态与 report refresh 均由 Phase 13 计划拥有。 |

<tasks>

<task type="auto">
  <name>Task 1: 在分配前共同资格化 root-IAB session、descriptor 与 canonical local route</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>由 root agent 取得当前 IAB，并仅从同一个 Browser context 提供 `RootIabSurfaceObserver`。对 local `starye-org` 调用 `verifyRootIabObservationReadiness`；它必须在 `http://localhost:8080/dashboard/movies` 验证 authenticated Movies surface，并确认 Plan 13-76 的 repository-owned code/id descriptor 可由该 port 读取。只在 `13-77-SUMMARY.md` 记录 route class、root ownership、ready/unavailable 与无 secret 的 tuple-capability 事实，按 D-03、D-04、D-05、D-06 执行。

在分配前确认 `13-77-RUN-ID.txt`、`evidence/starye-org/p13-77-*`、local attempt marker 均不存在。root-IAB probe、descriptor 或 path-absence 的首个非成功是 allocation 前冻结：不创建 run-id，且不运行 handoff、observer、verifier、remote、production navigation 或历史 carrier 操作。</action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
  </verify>
  <done>只有同一 root-IAB 在 Gateway canonical local Dashboard 具备 authenticated exact-tuple 能力时，p13-77 才有资格分配；否则该计划在无 carrier 状态结束。</done>
</task>

<task type="auto">
  <name>Task 2: 通过唯一 repository-owned local handoff 分配并建立 count-one pending tuple</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-77-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  </read_first>
  <action>仅在 Task 1 已记录 ready 且同一 root-IAB port 仍驻留内存时继续。运行 `pnpm check:services` 两次，并要求两个结构化记录都接受 Gateway、robots、auth、authSlash。一次性生成并写入唯一 `p13-77-<32 lowercase hex>`，随后仅调用 `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <actual-p13-77-run-id>`。

仅当 pending pair 是 target/run-derived deterministic code、one non-R18/one-player fixture、`runnerInvocations: 1`、`handoffReady: true`、非空 local itemId 时继续。reservation、readiness、handoff、tuple 或内建 inspection 的首个非成功永久冻结 p13-77：只记录 allowlisted category，禁止第二个 run id、第二次 local handoff、local verifier、remote 或 production 动作。该任务逐项实现 D-01、D-02、D-03、D-04、D-06、D-07，且不读取或复用历史 carrier。</action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
  </verify>
  <done>p13-77 只有一个新 local pending tuple 或一个不可变 local checkpoint；任何状态均不泄露或追踪 secret。</done>
</task>

<task type="auto">
  <name>Task 3: 以相同 root-IAB 完成 local Dashboard→Viewer tuple receipt，并仅在双通过后验证</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
  </read_first>
  <action>只允许 Task 1 保留的 root-IAB port 调用 `observeDataChainSurfaces`。repository core 必先从 pending pair 推导 tuple 和 path；IAB 先在 `http://localhost:8080/dashboard/movies` 读取一行同元素 code/id，再在 `http://localhost:8080/movie/<p13-77-item-code>` 读取相同 mode-owned tuple。adapter 只返回 allowlisted status/itemCode/itemId，core 继续拥有 exact equality、receipt 持久化、Dashboard-before-Viewer 顺序及 first-checkpoint freeze，按 D-03、D-04、D-06、D-07 执行。

任何观察 checkpoint 立即冻结 p13-77，不运行 local verifier、任何重试、remote preflight/handoff/verifier、production navigation 或历史 run 操作。只有两张 receipt 都通过，才一次调用 `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id <actual-p13-77-run-id>`；只有其 `terminal_passed` 和 `provesExternalChain: true` 使 Plan 13-78 eligible，其余 verifier 结果同样冻结。</action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <done>p13-77 成为唯一 receipt-backed local terminal carrier，或在第一个 non-success 永久冻结；绝不将 local 结果表述为 production 成功。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Root IAB to observation core | 已认证 Browser 仅传递 canonical route/status/code/itemId，不能注入 selector、session 或 identity claim。 |
| Local handoff to D1/API/Gateway | 一条确定性 fixture 跨越本地状态链，reservation 与 tuple 由 repository core 约束。 |
| Local terminal evidence to remote authorization | 只有 exact receipt-backed terminal proof 可进入下一计划的人类授权边界。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-77-01 | Spoofing | Root-IAB session/tuple | high | mitigate | 同一 port 先通过 canonical Dashboard readiness 与 descriptor capability，再允许 allocation。 |
| T-13-77-02 | Tampering | Run reservation and evidence | high | mitigate | 分配前检查 fresh paths；固定 handoff；exact tuple；首个 non-success 冻结。 |
| T-13-77-03 | Information disclosure | Browser/evidence boundary | high | mitigate | summary/evidence 只记录 allowlisted facts；session、cookie、token、header、profile 不跟踪。 |
| T-13-77-04 | Elevation of privilege | Dashboard access | medium | mitigate | 仅真实 root-IAB Dashboard auth/role 可产生 receipt；不可用即 checkpoint。 |
| T-13-77-05 | Denial of service | Fixture write | medium | mitigate | D-01/D-02 限定一个 fixture、一次 handoff、无 retry。 |
| T-13-77-SC | Tampering | Package installation | low | accept | 本计划不执行 package-manager 安装，沿用既有工具链。 |
</threat_model>

<verification>
- 先运行 focused root-IAB/local handoff/verifier tests，再依赖 live local branch。
- 确认只有一个 p13-77 id、一次 local handoff，以及精确的 Dashboard→Viewer receipt 或第一个 checkpoint。
- 检查所有 canonical local URL 都是 `http://localhost:8080/...`，并确认 summary/evidence 没有 secret-shaped 字段。
</verification>

<success_criteria>
- 新 carrier 从 p13-77 开始，绝不重放 p13-66、p13-73 或任一旧 evidence tree。
- local terminal proof 必须由同一个 root-IAB exact tuple 的 Dashboard→Viewer receipt 支撑；首个失败关闭该 run。
- 远程权限、remote handoff 与 production observation 不在此计划中执行。
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md` when done.
</output>
