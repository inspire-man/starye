---
phase: 13-full-chain-data-smoke
plan: "06"
type: execute
wave: 5
depends_on:
  - "13-05"
files_modified:
  - packages/config/src/deployment-target/mutation-entry.ts
  - packages/config/src/deployment-target/__tests__/mutation-entry.test.ts
  - packages/db/scripts/target-d1-mutation.ts
  - packages/db/scripts/__tests__/target-d1-mutation.test.ts
  - scripts/data-chain-smoke.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - TEST-05
user_setup: []
must_haves:
  truths:
    - "The selected-target prepared fixture and read-only D1 snapshot both require the restored one-item tuple and return a matching code/id/count observation."
    - "Local and remote orchestration refuses a fixture/snapshot result whose count is not one, then persists an honest checkpoint instead of a resolved tuple."
    - "Remote execution keeps explicit target, preflight-first, account/resource ownership, fixed registry entries, and no-child-on-failed-gate behavior."
  artifacts:
    - packages/config/src/deployment-target/mutation-entry.ts
    - packages/db/scripts/target-d1-mutation.ts
    - scripts/data-chain-smoke.ts
  key_links:
    - "one prepared fixture observation -> one read-only D1 snapshot -> matching code/id -> local or remote runner evidence"
---

<objective>
Propagate the restored one-item contract through the prepared D1 snapshot and local/remote runners, so every physical write/read boundary rejects the former ten-item result shape.

Purpose: G-01 is not closed until registry result validation, D1 querying, and both runner modes enforce the same one-item tuple rather than masking a batch behind a primary item.
Output: one-item prepared-result types, one-row read-only D1 snapshot contract, and local/remote runner regressions.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-05-PLAN.md
@packages/config/src/deployment-target/mutation-entry.ts
@packages/db/scripts/target-d1-mutation.ts
@scripts/data-chain-smoke.ts
@scripts/target-remote-entry.ts
</context>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | 13-05 one-item identity/fixture observation | One-row fixed D1 snapshot and prepared result validation | First; runner changes must consume a completed read contract. |
| 2 | Task 1 prepared fixture/snapshot results | Local and remote count-one orchestration gates | Second; both modes share the finished registry/snapshot type. |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constrain prepared registry results and the selected D1 snapshot to one row</name>
  <files>packages/config/src/deployment-target/mutation-entry.ts, packages/config/src/deployment-target/__tests__/mutation-entry.test.ts, packages/db/scripts/target-d1-mutation.ts, packages/db/scripts/__tests__/target-d1-mutation.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-05-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - packages/config/src/deployment-target/mutation-entry.ts
    - packages/config/src/deployment-target/__tests__/mutation-entry.test.ts
    - packages/db/scripts/target-d1-mutation.ts
    - packages/db/scripts/__tests__/target-d1-mutation.test.ts
    - packages/db/MIGRATION.md
    - scripts/target-remote-entry.ts
  </read_first>
  <behavior>
    - The only accepted prepared fixture observation has the fixed crawler operation, successful status, primary code, and count one.
    - The only accepted prepared D1 result is a read-only snapshot of exactly one non-R18 movie with exactly one active player and a non-empty id for the same primary code.
    - A multi-row response, sibling-code response, count mismatch, malformed prepared context, missing declared credential, or failed preflight returns a checkpoint/failure without starting an unauthorized child or yielding an item id.
  </behavior>
  <action>Before editing `prepareTargetMutation`, `runPreparedTargetMutation`, `runTargetD1Mutation`, or snapshot parser symbols, run GitNexus upstream impact analysis for each exact symbol and report direct callers, affected processes, and risk. Warn before editing if HIGH or CRITICAL. Write failing tests first. Per G-01 and the restored 13-05 contract, narrow typed child observations and registry output validation to the literal single-item result only. Update the fixed read-only D1 query/parser to look up precisely the prepared primary code, validate one non-R18 movie plus one active player, and emit only operation/status/code/id/count-one metadata. Keep parameter binding, `shell: false`, prepared-context validation, explicit target identity, allowlisted environment, ownership/preflight requirements, and all no-mutation-on-checkpoint behavior. Do not accept SQL, pattern expansion, D1 name, target, endpoint, output path, or item list from user-controlled arguments. This work is a read contract only: do not apply migrations, change schema, or alter fixture/API write behavior.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/mutation-entry.test.ts && pnpm --filter @starye/db exec vitest run scripts/__tests__/target-d1-mutation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Prepared-entry tests reject a fixture/snapshot observation whose count is not one or whose code is not the prepared primary code.
    - D1 tests accept one matching movie/player row and reject zero, multiple, R18, inactive/multiple-player, sibling-code, malformed JSON, and command failure outputs without supplying an item id.
    - Regression tests prove failed preflight or credential/ownership validation does not materialize prepared context or run a crawler/D1 child.
  </acceptance_criteria>
  <done>The selected-target prepared boundary cannot interpret a batch or pattern query as a valid Phase 13 snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require count-one results in both local and remote smoke orchestration</name>
  <files>scripts/data-chain-smoke.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-05-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - scripts/data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/mutation-entry.ts
    - packages/db/scripts/target-d1-mutation.ts
  </read_first>
  <behavior>
    - Local mode performs its existing projection, local D1, service, and Gateway gates before one fixture/snapshot pair; a non-one count becomes a truthful pre-ingest or resolved checkpoint with no invented id.
    - Remote mode reads the exact local pair, passes the existing selected-target/provider preflight before fixed children, and requires both prepared observations to have the same primary code and count one.
    - Neither mode changes the canonical URL policy, turns checkpoint exit code into success, or attempts browser observations.
  </behavior>
  <action>Before modifying `runDataChainSmoke`, `runLocalDataChainSmoke`, `runRemoteDataChainSmoke`, or their helper symbols, perform GitNexus upstream impact analysis and report blast radius; warn before proceeding on HIGH or CRITICAL. Add RED tests covering a mismatched count, code, or id from the fixture/snapshot dependencies in each mode. Consume the Task 1 one-item types throughout the local direct snapshot and remote prepared-child paths. Preserve D-03 local-first order: local selected-target projection and D1 readiness, service readiness, and Gateway `/auth/` observation remain gates before the one write. Preserve D-05 remote order: exact terminal local pair, explicit selected target, provider preflight/ownership, then fixed fixture and D1 children. If a one-item invariant fails, persist the existing schema-valid checkpoint lifecycle with no fabricated item id or browser pass. Do not change secrets, local env files, target profiles, remote command registry, canonical host derivation, or add a direct app-port URL.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Local and remote happy paths retain one matching code/id/count-one tuple across fixture, D1, and API evidence rows.
    - A count/code/id mismatch in any injected result exits as checkpoint and cannot reach resolved-pending browser state.
    - Tests preserve exact-local-pair remote eligibility, fail-closed provider gates, and local Gateway-only evidence routes.
  </acceptance_criteria>
  <done>Every runner layer agrees that Phase 13 has one item, and no former batch result can produce a usable evidence tuple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Prepared child output -> smoke runner | Child JSON must be an allowlisted one-item observation, not an arbitrary D1/provider result. |
| Selected D1 -> snapshot parser | Provider response is untrusted until one row/code/id/player contract is verified. |
| Exact local pair -> remote runner | Remote work must not use a different or incomplete local run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-24 | Tampering | D1 snapshot output | high | mitigate | Fixed parameterized query and strict one-row/parser validation reject batch or malformed provider data. |
| T-13-25 | Spoofing | prepared child result | high | mitigate | Validate literal operation/status/code/id/count-one result shapes before the root runner consumes them. |
| T-13-26 | Elevation of Privilege | remote prepared execution | critical | mitigate | Preserve explicit target/preflight/ownership, fixed registry children, and no child after a failed gate. |
| T-13-27 | Repudiation | local-to-remote tuple | medium | mitigate | Require an exact terminal local pair and identical target/run/code/id before remote continuation. |
</threat_model>

<verification>
- Run the focused Config/DB runner tests above, then `pnpm --filter @starye/config test --run` and `pnpm --filter @starye/db test --run`.
- Before committing, run GitNexus `detect_changes`; expected symbols are prepared result validation, read-only snapshot parsing, and local/remote smoke orchestration.
- Do not perform a credentialed provider run here. Plan 13-08 contains the authorized execution checkpoints after provenance controls exist.
</verification>

<success_criteria>
The one-item contract is enforced at every remaining execution seam, including remote D1 inspection and local/remote orchestration, while all target/preflight and Gateway boundaries remain fail closed.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-06-SUMMARY.md` when done.
</output>
