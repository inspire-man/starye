---
phase: 13-full-chain-data-smoke
plan: "23"
type: execute
wave: 21
depends_on: [13-22]
files_modified:
  - scripts/local-dev-authorization.ts
  - packages/config/src/deployment-target/__tests__/local-dev-authorization.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-23-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - TEST-05

must_haves:
  truths:
    - "D-03: The read-only PowerShell snapshot generator captures listener owner and Win32 process identifiers without assigning PowerShell's reserved automatic PID variable; the existing evaluator still rejects every malformed, ambiguous, missing-link, and cyclic pre-supervisor tree."
    - "A new exact, read-only seven-port snapshot produces either one hash-bound current-workspace supervisor authorization record or exactly one closed no-mutation blocked record; neither branch starts, stops, cleans, smokes, migrates, observes a browser, contacts a provider, or writes Phase 13 evidence."
    - "Only an explicitly approved Task 2 snapshot that remains byte-identical on immediate recapture can stop its exact child-before-parent PID list; only a subsequent all-free cold start with canonical Gateway readiness may release 13-17 through 13-20."
  artifacts:
    - path: scripts/local-dev-authorization.ts
      provides: "Read-only PowerShell snapshot command built with non-reserved local variable names and unchanged fail-closed supervisor evaluator."
    - path: packages/config/src/deployment-target/__tests__/local-dev-authorization.test.ts
      provides: "RED-GREEN deterministic fixture coverage for snapshot command variable names and existing authorization branches."
    - path: .planning/phases/13-full-chain-data-smoke/13-23-SUMMARY.md
      provides: "The sole current authorization checkpoint or released/blocked runtime lifecycle result for downstream eligibility."
  key_links:
    - from: "PowerShell listener/CIM snapshot command"
      to: evaluateLocalDevAuthorization
      via: "Non-secret listeners/processes JSON with stable pid and parentPid fields"
      pattern: "processId|parentProcessId"
    - from: "13-23-SUMMARY.md authorization_snapshot_sha256"
      to: "exact child-before-parent stop set"
      via: "human-confirmed hash plus identical immediate re-snapshot"
      pattern: "authorization_snapshot_sha256"
    - from: "13-23-SUMMARY.md runtime_eligibility: released"
      to: "13-17 run allocation"
      via: "fresh task-owned seven-port cold start and canonical check-services result"
      pattern: "runtime_eligibility: released"
---

<objective>
Repair the read-only authorization snapshot capture defect, then establish one new exact authorization checkpoint and, only after a hash-bound approval, safely recover the local runtime eligibility gate.

Purpose: Plan 13-22's evaluator remained fail closed, but PowerShell attempted to assign the reserved automatic `$PID` variable while generating its process snapshot. The resulting supervisor-not-found checkpoint cannot authorize a teardown or fresh local proof.
Output: deterministic snapshot-generator regression coverage, one current authorization/recovery Summary, and a released or terminal blocked eligibility result for the unchanged 13-17 runId-bound chain.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md
@.planning/debug/phase13-runtime-ownership.md
@.planning/debug/phase13-legacy-parent-missing.md
@.planning/debug/phase13-supervisor-not-found.md
@scripts/local-dev-authorization.ts
@scripts/local-dev.ts
@packages/config/src/deployment-target/__tests__/local-dev-authorization.test.ts
@packages/config/src/deployment-target/__tests__/local-dev.test.ts
@packages/config/src/deployment-target/__tests__/gateway-readiness.test.ts
</context>

<interfaces>
From `scripts/local-dev-authorization.ts`:
- `evaluateLocalDevAuthorization(input: unknown): LocalDevAuthorizationResult` is the pure evaluator consumed by fixtures. Its closed reason vocabulary and all evaluator decisions remain unchanged in Task 1.
- An authorized result provides `authorizationSnapshotSha256`, `authorizationSupervisorPid`, `authorizationListenerOwnerPids`, `authorizationDescendantPidsChildBeforeParent`, and `authorizedStopPidsChildBeforeParent`; a blocked result provides only `closedReason` and empty PID arrays.
- The executable-only capture currently runs a fixed PowerShell command and passes only `listeners` and `processes` into that evaluator. Task 1 may expose a test-visible pure command-builder seam, but must not add caller input, ambient environment reads, process control, or alternate snapshot sources.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD the non-reserved PowerShell snapshot identifier capture</name>
  <files>scripts/local-dev-authorization.ts, packages/config/src/deployment-target/__tests__/local-dev-authorization.test.ts</files>
  <behavior>
    - RED: A deterministic fixed legacy listener/process fixture and the test-visible snapshot-command seam prove that the command uses distinct non-reserved local names for listener owner, Win32 process id, and parent process id before emitting `ownerPid`, `pid`, and `parentPid` JSON fields.
    - GREEN: That same fixed fixture still authorizes exactly one current-workspace supervisor, produces a stable 64-hex hash, and preserves child-before-parent descendant/stop order.
    - Regression: Existing missing-before-supervisor, ancestry-cycle, malformed/supervisor-not-found, duplicate-owner, and wrong listener-shape failures retain their closed reasons and empty stop arrays.
  </behavior>
  <action>Before editing `captureCurrentWorkspaceSnapshot` or its command-builder seam, run `npx gitnexus analyze` because the current index is stale, then run GitNexus upstream impact analysis for `captureCurrentWorkspaceSnapshot` and `evaluateLocalDevAuthorization`; record direct callers, affected flows, and risk in the Task 1 Summary. If either result is HIGH or CRITICAL, show the root user the exact warning and stop before editing. Write the failing Vitest fixture first. It must exercise only fixed listener/process JSON and the deterministic command text; it must not enumerate real processes, bind a port, run a PowerShell snapshot, start a service, or stop a process. Implement only the capture-generator correction: replace every assignment that targets the PowerShell automatic PID variable with clearly named non-reserved local variables for the listener owner, Win32 process identifier, and parent identifier, while retaining the same JSON object field names consumed by `normalizeInput`. A small exported pure command-builder is permitted only to make that deterministic fixture inspectable; do not change the evaluator's accepted tree, closed reason union, process normalization, listener shape, hash payload, workspace matching, or child-before-parent calculation. Run the focused test once to observe RED, implement the minimum generator-only change, rerun GREEN, then run the narrow TypeScript and config type checks. Run GitNexus detect-changes immediately before each RED, GREEN, and optional REFACTOR commit; expected scope is only this snapshot generator/test pair and their existing authorization execution flow. Stop on any unexpected symbol/flow expansion.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev-authorization.test.ts</automated>
    <automated>pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json</automated>
    <automated>pnpm --filter @starye/config type-check</automated>
  </verify>
  <done>The PowerShell generator no longer targets its reserved automatic PID variable, deterministic fixtures cover the identifier mapping and stable authorized payload, and every existing fail-closed evaluator branch remains mechanically unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture one new exact read-only authorization snapshot</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-23-SUMMARY.md</files>
  <what-built>Task 1 has a tested, generator-only repair while the existing supervisor-root evaluator remains fail closed.</what-built>
  <how-to-verify>Read the Summary. An authorized record must show one 64-hex hash, the current-workspace `scripts/local-dev.ts` supervisor, every listener owner, the child-before-parent descendant and stop lists, and both lifecycle operation flags false. A blocked record must instead show one allowlisted reason and no PID authorization data.</how-to-verify>
  <action>Run the repaired `scripts/local-dev-authorization.ts` executable exactly once from the resolved repository root through its existing `tsx` entry. It may take one fixed, read-only listener/CIM snapshot for only ports 8080, 8787, 5173, 3002, 3003, 3000, and 3001. Do not inspect environment values, secret files, browser state, provider state, or evidence directories. On an authorized result, write exactly one pending record in `13-23-SUMMARY.md`: `runtime_eligibility: blocked`, `legacy_teardown_authorization: pending_human_confirmation`, the 64-hex `authorization_snapshot_sha256`, matched supervisor script/PID, listener-owner list, child-before-parent descendant/stop lists, non-secret external-ancestor context, and `teardown_attempted: false` plus `launch_attempted: false`. On any blocked result, write only `runtime_eligibility: blocked`, `terminal_branch: blocked_pre_teardown`, its one allowlisted closed reason, and both operation flags false; do not publish an approval token, PID set, or resume path. In either branch do not invoke Stop-Process, `pnpm dev`, `pnpm check:services`, clean-port tooling, smoke/handoff/run allocation, browser, provider, schema, migration, deploy, DNS, cleanup, rollback, or evidence commands. Run GitNexus detect-changes before committing only the Summary and stop if it reports any source symbol or execution-flow change.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-23-SUMMARY.md'; $pending = $summary -match '(?m)^legacy_teardown_authorization:\s*pending_human_confirmation\s*$' -and $summary -match '(?m)^authorization_snapshot_sha256:\s*[0-9a-f]{64}\s*$' -and $summary -match '(?m)^authorization_supervisor_script:\s*scripts/local-dev\.ts\s*$' -and $summary -match '(?m)^authorization_supervisor_pid:\s*\d+\s*$' -and $summary -match '(?m)^authorization_descendant_pids_child_before_parent:\s*\d+(,\d+)*\s*$' -and $summary -match '(?m)^authorized_stop_pids_child_before_parent:\s*\d+(,\d+)*\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blocked = $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^terminal_branch:\s*blocked_pre_teardown\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; if (-not ($pending -xor $blocked)) { throw 'Expected exactly one pending authorization or one closed no-mutation blocker.' }"</automated>
  </verify>
  <resume-signal>Type `approved snapshot &lt;authorization_snapshot_sha256&gt;` only after the displayed hash and every ordered PID are confirmed as the intended current-workspace local-dev tree. Any other response leaves the runtime blocked.</resume-signal>
  <done>The only new runtime observation is either an exact, human-reviewable PID/hash checkpoint or a terminal no-mutation blocker; no downstream data-chain surface has been touched.</done>
</task>

<task type="auto">
  <name>Task 3: Execute only the approved exact teardown and cold-start release gate</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-23-SUMMARY.md</files>
  <action>Proceed only after Task 2's resume signal exactly matches the pending `authorization_snapshot_sha256`; any blocked Task 2 outcome, absent/mismatched approval, or changed Summary ends with no process action. Immediately take the same fixed read-only snapshot again and require byte-for-byte equality of the normalized hash, workspace script, supervisor PID, listener-owner set, descendant order, stop order, and external-ancestor context. If the current snapshot is blocked or differs, record one closed `blocked_pre_teardown` branch with both lifecycle flags false and stop. Only on exact equality invoke native PowerShell per-PID Stop-Process operations in the recorded `authorized_stop_pids_child_before_parent` order. Do not use process-name termination, wildcard matching, taskkill, clean:ports, dev:clean, port cleanup, process-tree rediscovery, or any unrecorded PID. Boundedly require every authorized PID to exit and every fixed port to be free; immediately repeat the all-free assertion. Any stop failure, surviving PID, or listener writes `blocked_after_teardown` with teardown attempted true and launch attempted false, then ends. Only after both all-free checks start `pnpm dev` hidden from the resolved repository root, capture the new launcher/supervisor facts, and require the existing evaluator's complete-seven-port mode to prove every listener is a cycle-free descendant of that fresh current-workspace supervisor. Poll `pnpm check:services` for at most 60 seconds and accept only exit 0 plus one healthy `starye-local-services-1` record with seven listener rows and accepted canonical `/robots.txt`, `/auth`, and `/auth/` outcomes. Run the focused local-dev/authorization/readiness/process tests, narrow TypeScript check, config type-check, and `pnpm --filter gateway test`. Any cold-start ownership, readiness, route, or test failure writes `blocked_after_launch` with both lifecycle flags true and forbids all data-chain actions. Only every passing gate may write `runtime_eligibility: released`, `terminal_branch: released`, the approved snapshot/PID facts, fresh supervisor facts, canonical readiness facts, and `next_plan: 13-17`. Before committing the Summary, run GitNexus detect-changes and require no uncommitted source symbol or execution-flow expansion beyond Task 1's committed generator/test fix. Do not run evidence, smoke, handoff, browser, provider, schema, migration, deploy, DNS, cleanup, or rollback actions before this released result is consumed by 13-17.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-23-SUMMARY.md'; $released = $summary -match '(?m)^runtime_eligibility:\s*released\s*$' -and $summary -match '(?m)^terminal_branch:\s*released\s*$' -and $summary -match '(?m)^legacy_teardown_authorization:\s*approved\s*$' -and $summary -match '(?m)^authorization_supervisor_script:\s*scripts/local-dev\.ts\s*$' -and $summary -match '(?m)^pre_cold_start_fixed_ports:\s*all_free\s*$' -and $summary -match '(?m)^fresh_local_dev_supervisor_pid:\s*\d+\s*$'; $blockedPre = $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^terminal_branch:\s*blocked_pre_teardown\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blockedStop = $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^terminal_branch:\s*blocked_after_teardown\s*$' -and $summary -match '(?m)^teardown_attempted:\s*true\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blockedLaunch = $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^terminal_branch:\s*blocked_after_launch\s*$' -and $summary -match '(?m)^teardown_attempted:\s*true\s*$' -and $summary -match '(?m)^launch_attempted:\s*true\s*$'; if ((@($released, $blockedPre, $blockedStop, $blockedLaunch | Where-Object { $_ }).Count) -ne 1) { throw 'Expected exactly one released or closed runtime terminal branch.' }"</automated>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-23-SUMMARY.md'; if ($summary -match '(?m)^runtime_eligibility:\s*blocked\s*$') { exit 0 }; pnpm check:services; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts src/deployment-target/__tests__/local-dev-authorization.test.ts src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-process.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter gateway test; exit $LASTEXITCODE"</automated>
  </verify>
  <done>Only an approved, byte-identical authorization can stop the captured tree. A released branch has two all-free checks, a fresh seven-port supervisor tree, and canonical Gateway readiness; every other branch remains terminally blocked before data-chain work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PowerShell listener/CIM output -> evaluator | Untrusted live process facts must normalize to the existing closed authorization contract. |
| Snapshot hash -> human approval | A PID set cannot become eligible unless the exact current snapshot is reviewed and approved. |
| Approved PID set -> process stop | Only the recorded child-before-parent list may be stopped. |
| All-free ports -> fresh supervisor | A local runtime release needs fresh ownership and canonical Gateway HTTP checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-113 | Tampering | PowerShell snapshot capture | high | mitigate | Deterministic TDD covers non-reserved identifier mapping; evaluator/hash contract remains unchanged. |
| T-13-114 | Spoofing | supervisor ownership | critical | mitigate | Existing evaluator requires one current-workspace supervisor and closed ancestry/listener shape checks. |
| T-13-115 | Elevation of Privilege | teardown authorization | critical | mitigate | Hash-bound human approval plus exact immediate re-snapshot before per-PID child-first stop. |
| T-13-116 | Denial of Service | local process recovery | high | mitigate | No broad cleanup; bounded survivor/all-free checks and task-owned cold start only. |
| T-13-117 | Information Disclosure | runtime Summary | medium | mitigate | Persist only allowlisted PID/hash/port/script/readiness facts; never environment, token, endpoint, or browser/provider state. |
| T-13-SC | Tampering | package supply chain | low | accept | No package-manager install occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Selected target becomes usable only through ordered local and production proof. | 13-23 release gate, then revised 13-17 through 13-20. | COVERED | This plan never substitutes runtime readiness for data proof. |
| REQ | DATA-01 | Canonical local Gateway readiness. | Task 3, then 13-17/18. | COVERED | Released only after `check:services` canonical routes. |
| REQ | DATA-02 | Local D1 readiness before ingest. | 13-17. | COVERED | Forbidden in 13-23. |
| REQ | DATA-03 | One deterministic fixture identity. | 13-17 and 13-19. | COVERED | No fixture/action here. |
| REQ | DATA-04 | D1/API/admin tuple correlation. | 13-17 through 13-20. | COVERED | No data surface here. |
| REQ | DATA-05 | Authorized Dashboard proof. | 13-18 and 13-20. | COVERED | Browser remains downstream. |
| REQ | DATA-06 | Canonical viewer proof. | 13-18 and 13-20. | COVERED | Browser remains downstream. |
| REQ | DATA-07 | Non-secret repeatable artifacts. | Tasks 1-3 and 13-17 through 13-20. | COVERED | PID checkpoint is runtime-only evidence. |
| REQ | TEST-05 | Repeatable runtime/smoke output. | Task 1 regression tests and Task 3 readiness tests. | COVERED | No watch mode or live fixture test. |
| RESEARCH | - | Local-first, explicit target, real provider/browser proof, fail closed. | 13-23 before downstream plans. | COVERED | No provider/browser before release. |
| CONTEXT | D-01 | One target/run/code deterministic item. | 13-17/19 retain the runId binding. | COVERED | 13-23 creates no run ID. |
| CONTEXT | D-02 | Closed fixture/crawler boundary. | 13-17/19. | COVERED | No crawler action here. |
| CONTEXT | D-03 | Gateway-first local proof before production. | Task 3 then 13-17 onward. | COVERED | Current direct runtime gate. |
| CONTEXT | D-04 | Exact D1/API/Dashboard/viewer tuple. | 13-17 through 13-20. | COVERED | No tuple exists here. |
| CONTEXT | D-05 | Explicit provider preflight. | 13-19. | COVERED | Provider remains forbidden in 13-23. |
| CONTEXT | D-06 | Non-secret repeatable artifacts. | Tasks 1-3 and downstream evidence. | COVERED | Snapshot contains only allowlisted facts. |
| CONTEXT | D-07 | Real proof remains Phase 13 work. | 13-19 and 13-20. | COVERED | Runtime repair is not a substitute. |
| VERIFICATION | G-1 | Fresh local path lacks terminal proof. | Task 3 releases only the prerequisite; 13-17/18 produce proof. | COVERED | Release may still block. |
| VERIFICATION | G-2/G-3 | Provider and browser receipts remain absent. | 13-19/20. | COVERED | Strict downstream dependency retained. |
| VERIFICATION | G-4 | Pending wrapper exit mismatch. | Existing 13-14 and later handoff contracts. | COVERED | Not reopened by this snapshot-only repair. |
</source_audit>

<verification>
- Task 1 is RED-GREEN-REFACTOR for only the PowerShell generator defect. It preserves all fail-closed evaluator behavior and runs fresh GitNexus analysis/impact/detect gates.
- Task 2 captures exactly one read-only current snapshot and pauses for a hash-bound human decision; a blocked capture has no teardown path.
- Task 3 requires that decision plus an identical immediate snapshot before exact child-first teardown, two all-free assertions, a new task-owned tree, and canonical readiness.
- No evidence, smoke, handoff, browser, provider, schema, migration, deploy, DNS, cleanup, rollback, or broad port/process action occurs before release.
- Plans 13-17 through 13-20 consume only `13-23-SUMMARY.md` release state and retain their existing unique `run_id` extraction from 13-17-SUMMARY.md.
</verification>

<success_criteria>
- The reserved-variable capture defect is covered by deterministic RED-GREEN fixtures without changing the evaluator's decision surface.
- The next live observation is one non-secret authorized PID/hash checkpoint or one closed no-mutation blocker.
- Only an explicit matching approval and identical re-snapshot can trigger an exact child-first stop.
- Any released branch proves all-free ports, fresh supervisor ownership, and canonical local readiness before the unchanged runId-bound data chain starts.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-23-SUMMARY.md` when executed. Commit code/tests and the Summary separately with GitNexus gates. Never modify historical plans, summaries, or evidence; never stage evidence artifacts.
</output>
