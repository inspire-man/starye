---
phase: 13-full-chain-data-smoke
plan: "27"
type: execute
wave: 29
depends_on: [13-26]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/13-27-SUMMARY.md"
autonomous: true
gap_closure: true
requirements: [DATA-01, TEST-05]
---

<objective>
Run one fresh, task-owned local cold start using the repaired root `dev` command. Prove the Gateway is reachable, then clean only the recorded process tree and return every fixed port to the free state.

This is a lifecycle gate only. It creates no data-chain run, fixture, D1/API/browser/provider evidence, and does not claim Phase 13 completion.
</objective>

<tasks>
<task type="auto">
  <name>Start and verify one task-owned local runtime</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-27-SUMMARY.md</files>
  <action>Before launch, confirm ports 8080, 8787, 5173, 3002, 3003, 3000, and 3001 have no listeners. If any is occupied, write a blocked Summary and stop without targeting a PID. Otherwise start exactly one hidden root `pnpm.cmd dev` process from the repository root. During a bounded startup window, record the first direct Node entry process running `scripts/local-dev-entry.ts`, then record its descendant Node supervisor whose command line contains the resolved absolute `scripts/local-dev.ts` path. Treat the recorded entry process as the task-owned cleanup anchor even if the short-lived pnpm wrapper exits. Require all seven listener owners to descend from the recorded entry process. Run only `pnpm check:services` as the Gateway readiness check; accept exit 0 and its canonical `http://localhost:8080` routes. On any attribution or readiness failure, record a blocked Summary and continue only to owned cleanup.</action>
  <verify>
    <automated>pnpm check:services</automated>
  </verify>
  <done>Either a closed no-external-PID blocker exists, or the recorded entry process owns the supervisor and all fixed listeners and Gateway readiness passes.</done>
</task>

<task type="auto">
  <name>Clean the recorded tree and publish the lifecycle result</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-27-SUMMARY.md</files>
  <action>Revalidate only descendants of the recorded entry process. Stop them child-before-parent, including the recorded supervisor and entry process, and never add a PID based on port, process name, workspace text, or historical Summary. Wait boundedly for those PIDs, then verify all seven fixed ports are free. If startup and readiness passed, cleanup succeeded, and every fixed port is free, publish exactly `runtime_eligibility: released`, `runtime_lifecycle: cleaned`, and `post_cleanup_fixed_ports: all_free`. Otherwise publish one blocked reason and omit all three release fields. Commit the Summary after `git diff --check`; leave user changes and historical evidence untouched.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ports=8080,8787,5173,3002,3003,3000,3001; if (Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue | Where-Object { $ports -contains $_.LocalPort }) { throw 'fixed ports remain occupied' }"</automated>
  </verify>
  <done>A released result proves one clean task-owned lifecycle; any other result is an honest blocked result with no external PID action.</done>
</task>
</tasks>

<success_criteria>
- No pre-existing listener is stopped or reused.
- A release requires one fresh entry Node, its absolute-path supervisor, successful Gateway service check, owned cleanup, and seven free fixed ports.
- No data, remote, browser, provider, migration, or deployment action occurs.
</success_criteria>
