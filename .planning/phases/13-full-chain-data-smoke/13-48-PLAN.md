---
phase: 13-full-chain-data-smoke
plan: "48"
type: execute
wave: 46
depends_on: []
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-48-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "The invalid p13-45 launch is preserved as failed-before-evidence history and is never retried, reused, or given a completion Summary."
    - "One newly allocated p13-48-<32 lowercase hex> run completes fresh local preflight and Gateway readiness, exactly one local handoff, and ordered local Dashboard then Viewer observation."
    - "The one run's post-observation local verifier reports terminal_passed with provesExternalChain true; a local pass is recorded only as a prerequisite for later remote and production work, never as remote or production success."
    - "The new local evidence remains untracked; the execution closeout stages only 13-48-RUN-ID.txt and 13-48-SUMMARY.md."
    - "The plan can be selected independently while 13-45-SUMMARY.md, 13-46-SUMMARY.md, and 13-47-SUMMARY.md do not exist."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-48-RUN-ID.txt
      provides: one allocated p13-48 run identifier for this local-only proof
    - path: .planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md
      provides: non-secret local proof receipt, evidence hashes, scope boundary, and subsequent remote/production prerequisite
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-48-run-id}/local.attempt
      provides: untracked atomic reservation marker for the one local handoff
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-48-run-id}/local.json
      provides: untracked terminal local evidence for the exact target/run/code/item tuple
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-48-run-id}/local.md
      provides: untracked deterministic Markdown rendering of the local evidence JSON
  key_links:
    - from: packages/crawler/scripts/data-chain-cli.mjs
      to: scripts/data-chain-handoff.ts
      via: the root launcher removes only pnpm's one leading delimiter before the local handoff parser receives --mode/--target/--run-id
      pattern: "argv\\[0\\] === '--'"
    - from: local handoff evidence
      to: scripts/data-chain-surface-observation.ts
      via: the same p13-48 tuple must be resolved_pending_observation before Dashboard then Viewer receipts can advance it
      pattern: "observeDataChainSurfaces"
    - from: scripts/data-chain-surface-observation.ts
      to: pnpm smoke:data-chain:verify
      via: ordered local Gateway browser receipts must advance the exact same local pair to terminal_passed
      pattern: "provesExternalChain"
---

<objective>
Create a fresh, local-only terminal proof after the root launcher's pnpm-delimiter repair.

Purpose: p13-45-6c86ba15733e4ff68146d5d316e42401 stopped at the handoff parser's invalid_target launch failure before an attempt marker, local JSON/Markdown pair, or evidence tree existed. Commit ebc500b normalizes the root launcher's one pnpm delimiter and its focused process regression is green. The old run is permanently invalid history: this plan does not retry it, reuse its identifier, create 13-45-SUMMARY.md, or synthesize local/remote evidence for it.

Output: one newly allocated p13-48 run's untracked local evidence pair plus a committed 13-48 run-id and Summary. This is the local prerequisite for a later explicitly planned/selected 13-46 remote handoff and 13-47 production observation; it is not remote or production proof.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-41-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-45-PLAN.md
@packages/crawler/scripts/data-chain-cli.mjs
@packages/config/src/deployment-target/__tests__/data-chain-cli-process.test.ts
@scripts/data-chain-surface-observation.ts
</context>

## Execution Selection

Run this plan alone with `$gsd-execute-phase 13 --gaps-only --wave 46`. Its deliberately unique Wave 46 is the selection boundary: it has no dependency on, and must not wait for, a 13-45/13-46/13-47 Summary. Do not run an unfiltered gaps-only invocation for this recovery path, and do not select Wave 43, which contains the permanently stopped 13-45 launch.

Completing 13-48 supplies the truthful local prerequisite and run id for a later remote/production continuation. The current 13-46 and 13-47 text still names 13-45 output, so do not execute either plan directly from 13-48 or fabricate that missing output; a subsequent narrowly scoped remap must point those later steps at 13-48's actual Summary and run id.

## Artifacts This Plan Produces

- `13-48-RUN-ID.txt` containing exactly one `p13-48-<32 lowercase hex>` value.
- Untracked `local.attempt`, `local.json`, and `local.md` beneath `evidence/starye-org/<that-run-id>/`.
- `13-48-SUMMARY.md` containing only non-secret local facts, exact verifier outcomes, and SHA-256 values for the untracked local pair.

## Non-Goals

- No retry, completion Summary, evidence creation, or status upgrade for p13-45-6c86ba15733e4ff68146d5d316e42401.
- No remote handoff, remote mode, provider/D1/API/Wrangler command, production browser, deploy, migration, or 13-VERIFICATION.md rewrite.
- No reopening of p13-17, p13-37, p13-41, or any other historical evidence tree.
- No staging, reverting, or cleaning unrelated dirty files.

## Source Coverage Audit

| Source | ID | Required item | Coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 13 | Gateway/canonical data-chain usability proof | Task 1 local preflight/handoff and Task 2 canonical local surfaces; production remains the later 13-46/13-47 branch | COVERED |
| REQ | DATA-01 | Local Gateway API/auth/dashboard/content path | Tasks 1-2 use only `http://localhost:8080` for readiness and Dashboard/Viewer | COVERED |
| REQ | DATA-02 | Local D1/minimal-data readiness before ingest | Task 1 requires local preflight and the handoff's receipt-backed local D1 readiness before observation | COVERED |
| REQ | DATA-03 | One deterministic selected-target fixture and recorded identity | Task 1 permits exactly one local handoff for one p13-48 tuple | COVERED |
| REQ | DATA-04 | Same item in D1/API/admin state | Task 1 requires runner D1/API receipts; Task 2 requires exact Dashboard tuple | COVERED |
| REQ | DATA-05 | Dashboard validation | Task 2 records Dashboard before Viewer on the same local tuple | COVERED |
| REQ | DATA-06 | Canonical viewer validation | Task 2 records the Gateway movie route after Dashboard | COVERED |
| REQ | DATA-07 | Repeatable non-secret evidence | Both tasks preserve untracked JSON/Markdown/marker evidence and commit only the run-id/Summary | COVERED |
| REQ | TEST-05 | Repeatable smoke output | Task 1 reruns the root delimiter regression and exact verifier contract | COVERED |
| RESEARCH | Local-first, one-item, redacted evidence, Gateway-only routes | Current local runner and observer are reused without a new crawler/API path | COVERED |
| CONTEXT | D-01, D-02 | One deterministic fixture through the bounded adapter | Task 1 invokes the closed local handoff exactly once | COVERED |
| CONTEXT | D-03, D-04 | Local readiness then Gateway Dashboard/Viewer tuple | Tasks 1-2 enforce fresh local gates and ordered canonical observation | COVERED |
| CONTEXT | D-05 | Provider work remains explicitly gated | Declared out of scope; no provider command appears in this plan; a later remote continuation must consume the actual 13-48 output | COVERED AS OUT OF SCOPE |
| CONTEXT | D-06, D-07 | Redacted evidence and Phase 13 ownership | Tasks 1-2 keep new evidence untracked and record scope truth in 13-48 Summary | COVERED |

<tasks>

<task type="auto">
  <name>Task 1: Allocate one fresh p13-48 run after local Gateway gates</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-48-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-45-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-41-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - packages/crawler/scripts/data-chain-cli.mjs
    - packages/config/src/deployment-target/__tests__/data-chain-cli-process.test.ts
  </read_first>
  <action>
    Apply D-01: accept exactly one deterministic, target/run-derived, non-R18 item with one player and preserve its local mode-owned itemId. Apply D-02: use only the existing closed local handoff/fixture adapter, never a general crawler corpus. Apply D-03: complete the local D1/service/Gateway gates before the one ingest handoff and treat direct implementation ports as diagnostics. Apply D-04: require the same local target/run/code/itemId across D1, API, Dashboard, and Viewer receipts. Apply D-05: exclude every provider-side or production command from this local plan; a later explicitly authorized remote continuation owns that boundary.

    First confirm `ebc500b` is an ancestor of the checked-out source and run the focused root launcher regression `pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-cli-process.test.ts`; require the regression that strips pnpm's single leading delimiter to pass before any live local command. Record in the Summary that p13-45-6c86ba15733e4ff68146d5d316e42401 failed at invalid_target before evidence allocation, and that this task never retries or reuses it.

    Perform a fresh local-only gate sequence per D-01, D-02, D-03, and D-06: inspect the fixed local service state; retain a healthy current root `pnpm dev` tree if it already serves all required services, otherwise start one current-source local tree without stopping any pre-existing PID. Require `pnpm target-profile project-local --target starye-org --check`, the local smoke preflight with `--live --wrangler-profile starye-org`, and `pnpm check:services` to pass. Confirm the canonical Gateway readiness paths `/robots.txt`, `/auth`, and `/auth/` under `http://localhost:8080`; direct implementation ports are ownership diagnostics only and are never evidence URLs.

    Only after the gates pass, allocate once with `p13-48-` plus 32 lowercase hex, write that exact value to 13-48-RUN-ID.txt, and require its complete `evidence/starye-org/<run-id>` directory to be absent. Invoke exactly one local handoff with `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <run-id>`; do not invoke `smoke:data-chain` separately. Require handoff exit 0, pending outcome, handoffReady true, runnerInvocations 1, one deterministic item code, and a non-empty local itemId. Before observation, require the exact local verifier to return its pending checkpoint result and receipt-backed local projection, D1, service, Gateway-auth, D1, and API surfaces. If allocation, handoff, or pre-observation verification does not meet this contract, preserve only the newly created untracked artifacts, write an honest non-success Summary, and stop without a second handoff, replacement run id, or downstream observer.
  </action>
  <acceptance_criteria>
    - 13-48-RUN-ID.txt contains exactly one p13-48-<32 lowercase hex> value and no p13-45 identifier.
    - The p13-48 evidence directory was absent before allocation and contains one zero-byte local.attempt plus a matching local.json/local.md pair after the sole handoff.
    - The focused root-launcher regression and fresh local preflight/Gateway readiness all pass before handoff.
    - The pre-observation exact verifier reports pending, not terminal success, for the same p13-48 tuple with a non-empty itemId.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-cli-process.test.ts</automated>
    <automated>pnpm target-profile project-local --target starye-org --check</automated>
    <automated>pnpm target-profile preflight --target starye-org --scope local --command smoke --live --wrangler-profile starye-org</automated>
    <automated>pnpm check:services</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-48-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>A single new local pending pair exists only when all local gates passed; a failed branch remains permanent for that run and does not produce a replacement attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Observe local Dashboard then Viewer and close out only the local proof</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-48-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-41-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
  </read_first>
  <action>
    Apply D-06: keep the target/run/item/surface/status evidence allowlisted and non-secret, and retain the generated pair as untracked files. Apply D-07: keep the real local data-chain proof in Phase 13 while leaving Phase 14 documentation work and all production proof outside this plan.

    Consume only the pending local pair allocated by Task 1. Re-run its exact local verifier before observation and require the pending tuple. Execute repository-owned `scripts/data-chain-surface-observation.ts` once for mode local, target starye-org, and the exact p13-48 run id. It must observe the signed-in local Gateway Dashboard `/dashboard/movies` before the matching Gateway Viewer `/movie/<item-code>` at `http://localhost:8080`; it must retain the target/run/code/local-itemId tuple and let the repository observer persist the receipts. Do not use production hosts, remote mode, direct ports as evidence, ad hoc browser scripts, or an alternate observer.

    Require the post-observation exact verifier for that same run to exit 0 with outcome terminal_passed and provesExternalChain true. Confirm `git ls-files --error-unmatch` finds none of the three local evidence paths, keep them out of the index, and do not force-add ignored artifacts. Write 13-48-SUMMARY.md with the old p13-45 invalid launch fact, p13-48 run id, local prerequisite results, exactly-one handoff evidence, Dashboard-to-Viewer order, pre/post verifier exits, evidence byte counts and SHA-256 values, and the explicit statement that no remote, provider, production browser, or production success was attempted or proved. Treat this Summary as a prerequisite receipt only; it neither creates 13-45/46/47 Summary files nor changes their plans.

    Before the execution closeout commit, run GitNexus detect-changes and confirm the staged candidate has no changed code symbols or execution flows. Preserve all pre-existing dirty paths. Stage and commit exactly 13-48-RUN-ID.txt and 13-48-SUMMARY.md; do not stage evidence, existing plans, 13-VERIFICATION.md, STATE.md, ROADMAP.md, or unrelated files.
  </action>
  <acceptance_criteria>
    - The same p13-48 local pair moves from pending to terminal_passed only after Dashboard then Viewer receipts in that order.
    - The post-observation verifier reports provesExternalChain true for local mode and the exact target/run/code/local-itemId tuple.
    - local.attempt, local.json, and local.md remain untracked; only 13-48-RUN-ID.txt and 13-48-SUMMARY.md are staged for this execution's final commit.
    - The Summary explicitly distinguishes local terminal proof from remote or production success and identifies later 13-46/13-47 work as out of scope.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx scripts/data-chain-surface-observation.ts --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-48-RUN-ID.txt -Raw).Trim()</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-48-RUN-ID.txt -Raw).Trim()</automated>
    <automated>npx gitnexus detect-changes --repo starye --scope all</automated>
  </verify>
  <done>The sole p13-48 local run has honest terminal local evidence and a narrowly staged Summary/run-id, while all remote and production work remains unexecuted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local executor to handoff CLI | Caller supplies the selected target and run id to the closed root launcher. |
| local handoff to evidence tree | The handoff reserves and writes only the fixed target/run local pair. |
| local browser profile to Gateway | A real local signed-in session crosses the Gateway Dashboard guard; evidence records only allowlisted receipts. |
| local proof to later remote work | A terminal local pair may be consumed later as a prerequisite but must not be interpreted as production proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-48-01 | Tampering | local handoff/evidence reservation | high | mitigate | Require an absent new p13-48 tree, one generated id, one handoff, atomic local.attempt, exact JSON/Markdown verification, and permanent stop on any non-success. |
| T-13-48-02 | Spoofing | Dashboard/Viewer observation | high | mitigate | Use repository-owned observer with the exact local tuple, Gateway origin, Dashboard-before-Viewer order, and verifier-owned terminal transition. |
| T-13-48-03 | Information disclosure | evidence and Summary | high | mitigate | Persist only allowlisted target/run/tuple/status/hash facts; leave evidence untracked and do not serialize credentials, headers, cookies, raw prepared context, or full remote endpoints. |
| T-13-48-04 | Elevation of privilege | remote/provider execution | high | mitigate | Scope excludes remote mode, Wrangler, provider, production API/D1, and production browser commands; later remote work stays behind its own explicit authorization and plan. |
| T-13-48-05 | Repudiation | historic p13-45 failure | medium | mitigate | Record its invalid_target-before-evidence state in 13-48 Summary and retain it without retry, new evidence, or a fabricated completion Summary. |
| T-13-48-SC | Tampering | existing pnpm package execution | medium | mitigate | No dependency installation is permitted; rerun the existing focused launcher process regression before local handoff and preserve the committed lockfile state. |
</threat_model>

<verification>
Run the focused root launcher regression before the live local sequence. For the allocated p13-48 run, capture the pending exact verifier before observation, run the repository observer once, then require the terminal exact verifier. Inspect the evidence files as untracked and run GitNexus detect-changes before staging only the run-id and Summary.
</verification>

<success_criteria>
- `13-48-RUN-ID.txt` names one new p13-48 run and no p13-45 evidence tree or Summary is created.
- Fresh local target/projection/preflight/service/Gateway checks pass before one local handoff.
- The sole local evidence pair reaches terminal_passed with provesExternalChain true after Dashboard then Viewer at `http://localhost:8080`.
- The local pair remains untracked, and the execution closeout commit contains exactly `13-48-RUN-ID.txt` and `13-48-SUMMARY.md` from this plan.
- No remote, provider, production browser, ROADMAP, STATE, verification-report, or historical-evidence mutation occurs.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md` when the selected local run has reached its truthful terminal or stopped state.
</output>
