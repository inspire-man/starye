---
phase: 13-full-chain-data-smoke
plan: "17"
type: execute
wave: 16
depends_on: [13-14, 13-16, 13-21]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-17-run-id>/local.attempt"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-17-run-id>/local.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-17-run-id>/local.md"
  - ".planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md"
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Local Starye operator context
    why: The one-item local runner consumes existing user-managed API/Auth/crawler values without printing or persisting them.
    env_vars:
      - name: BETTER_AUTH_SECRET
        source: Existing local API Worker secret store
      - name: CRAWLER_SECRET
        source: Existing local crawler/API service-auth store
      - name: ADMIN_GITHUB_ID
        source: Existing local Dashboard authorization configuration
must_haves:
  truths:
    - "D-03: 13-21 reports runtime_eligibility: released for one fresh task-owned seven-port supervisor, then all official target/local gates, bounded HTTP readiness, local and remote runner regressions, mutation-entry and handoff regressions, and type-checks pass before any run candidate is generated."
    - "D-01/D-02: after run allocation, exact local JSON/Markdown peers and `local.attempt` are all proven absent immediately before the sole repository-owned local handoff; the handoff atomically creates the zero-byte marker before one deterministic non-R18 fixture upsert and count-one local D1 snapshot."
    - "Machine JSON and the exact local artifact pair, not the outer package code alone, certify one resolved_pending_observation/pending tuple with a non-empty local itemId."
    - "Any pre-existing marker, concurrent reservation loss, gate failure, checkpoint, crash, or attempt failure permanently closes this run without alternate run ID, retry, marker deletion/reuse, append, overwrite, browser action, or remote/provider action."
    - "`local.attempt` is a first-class untracked attempt artifact and never success by itself; exact verifier success remains JSON/Markdown-pair focused."
    - "Every pre-existing evidence file, especially p13-10 and p13-12, remains byte-identical from the pre-run baseline and untracked; the claim is limited to this execution window."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-17-run-id>/local.attempt"
      provides: "Zero-byte exclusive local attempt ownership marker; persistent after pending/checkpoint/failure/crash, untracked, and never verifier success"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-17-run-id>/local.json"
      provides: "Fresh local runner/D1/API pending tuple or immutable checkpoint"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-17-run-id>/local.md"
      provides: "Deterministic Markdown peer"
    - path: ".planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md"
      provides: "Pre-run gates, baseline digest, directory/three-path absence, local reservation-marker state, single invocation, and handoff verdict"
  key_links:
    - from: "13-21 released runtime eligibility Summary"
      to: "p13-17 run allocation"
      via: "historical 13-16 remains a read-only partial-tree diagnosis; only the latest closed 13-21 runtime gate can authorize allocation"
      pattern: "runtime_eligibility: released"
    - from: "pnpm smoke:data-chain:handoff -- --mode local"
      to: "local.attempt then local JSON/Markdown pair"
      via: "pair+marker precheck, atomic exclusive reservation, then one in-process runner and one exact verifier"
      pattern: "handoffReady"
    - from: "local.attempt"
      to: "artifact-only exact verifier"
      via: "marker is retained as attempt ownership history but ignored as proof; verifier still requires the exact pair"
      pattern: "attempt_already_reserved"
    - from: "exact pending local tuple"
      to: "13-18 root IAB"
      via: "matching target/run/code/itemId and no browser receipts"
      pattern: "resolved_pending_observation"
---

<objective>
Allocate one new run only after every deterministic repair and clean-runtime gate passes, require its exact local pair and `local.attempt` marker all absent, then let the handoff atomically reserve local ownership and execute exactly once up to the repository-certified browser handoff boundary (D-01 through D-04, D-06).

Purpose: p13-12 is permanently closed. This plan is the first and only place the repair sequence may consume a new run, and it stops before any browser or provider action.

Output: one persistent zero-byte `local.attempt` plus a `p13-17-*` local JSON/Markdown pair in pending/checkpoint state, a marker-only immutable crash/failure record, or a closed `attempt_reservation_unavailable` refusal with no synthesized marker, plus an accurate tracked Summary. No remote marker or pair is created.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/debug/phase13-gateway-auth-timeout.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-14-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-15-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-smoke.ts
@scripts/verify-data-chain-smoke.ts
</context>

<discovery_evidence>
- Discovery level: 0. This plan consumes committed CLIs, one-item fixture/snapshot children, exact evidence schema, and the fresh local supervisor; it creates no code interface.
- p13-10 and p13-12 are immutable failed runs. Their paths and all other prior evidence are excluded from every write/stage/commit path.
- `smoke:data-chain:handoff` owns the sole runner and verifier calls; do not invoke `smoke:data-chain` separately.
- The handoff's exclusive `local.attempt` creation is the ownership linearization point. Prechecks reduce operator mistakes but do not replace the atomic reservation; any marker-only state is a terminal prior/crashed attempt, not permission to retry.
- Reservation applies only to the newly allocated p13-17 run. Never create or backfill markers for p13-10, p13-12, Attempts A-E, or any historical evidence directory.
- Browser actions are structurally deferred to 13-18 and require the exact pending tuple produced here.
- Plan 13-16 is immutable historical diagnosis. Plan 13-21 is the latest runtime gate: absent, blocked, malformed, stale-supervisor, or non-canonical readiness status forbids allocation; a released status permits this plan's unchanged local handoff protocol.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Order |
|---|---|---|---|
| 1 | Green 13-13..16 repairs/runtime and official local gates | One new persistent local marker plus pending/checkpoint pair, or marker-only immutable failure | Only task; blocks 13-18 on non-pending |
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Gate, allocate, and invoke one fresh local handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-17-run-id&gt;/local.attempt, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-17-run-id&gt;/local.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-17-run-id&gt;/local.md, .planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/STATE.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-12-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-13-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-14-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-15-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md
    - scripts/data-chain-handoff.ts
    - scripts/data-chain-smoke.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>Confirm 13-13 through 13-15 are committed, retain 13-16 as the immutable partial-tree diagnosis, and require 13-21-SUMMARY to report runtime_eligibility: released for one still-live task-owned seven-port supervisor plus accepted canonical check:services routes. The historical 13-16 blocked result must not be read as a permanent refusal; 13-21 is the latest closed runtime gate. Missing, blocked, malformed, stale, or mismatched 13-21 ownership/readiness facts stop before all commands below. Run exactly in order and require exit 0: `pnpm target-profile project-local --target starye-org --check`; `pnpm target-profile validate --target starye-org`; `pnpm target-profile preflight --target starye-org --scope local --command validate --wrangler-profile starye-org`; `pnpm check:services`; `pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-cli-process.test.ts src/deployment-target/__tests__/data-chain-evidence.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts src/deployment-target/__tests__/data-chain-smoke-process.test.ts src/deployment-target/__tests__/mutation-entry.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts`; `pnpm --filter @starye/config type-check`; `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json`; and `pnpm --filter gateway test`. This is the final pre-run suite: it includes task-owned startup, pair/reservation/concurrency/crash/fixed-root CLI tests, both runner modes, mutation entry, and CLI-process coverage, and every result must be recorded before any baseline or run-id allocation. Only after all pass, build an in-memory sorted SHA256 baseline of every pre-existing Phase 13 evidence file and record only its file count and aggregate digest in the Summary; require exact p13-10 and p13-12 paths to remain `??` untracked, and do not create/backfill any `.attempt` file under a historical run. Generate `$runId = 'p13-17-' + [guid]::NewGuid().ToString('N')`, and accept a candidate only after exact `Test-Path -LiteralPath` proves its whole directory absent. A directory collision may discard that unused candidate and repeat only generation/absence checking; after one candidate is accepted, no alternate run ID is allowed. Resolve exactly `<run>/local.json`, `<run>/local.md`, and `<run>/local.attempt`, then immediately before handoff run `Test-Path -LiteralPath` on all three and require all false. If any peer or marker is present, terminally stop this accepted run before handoff/browser/remote/provider work; do not read, delete, repair, overwrite, reuse, rename, retry, or choose another ID. With no intervening command after that three-path precheck, invoke exactly once: `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id &lt;p13-17-run-id&gt;`. Do not invoke `smoke:data-chain` separately. The handoff repeats pair validation and atomically exclusive-creates zero-byte `local.attempt` as its ownership linearization point before its runner; a race loser returns `attempt_already_reserved`, a reservation I/O failure returns `attempt_reservation_unavailable`, and both end the run with zero runner/verifier/provider calls and no retry. For an owner, pre-existing-marker loser, or owner crash/failure, require `local.attempt` to remain present, zero-byte, and `??` untracked; never delete, rewrite, stage, commit, or reuse it. For `attempt_reservation_unavailable`, require no pair/downstream call and do not synthesize or repair a marker. Wrapper exit 0 is eligible only with one unique machine line containing `handoffReady:true`, `resolved_pending_observation/pending`, `provesExternalChain:false`, non-empty itemCode/itemId, and an exact matching JSON/Markdown pair with no browser receipt. Only then run the artifact-only exact verifier in this task's verify block. Marker-only, missing/partial pair, checkpoint, crash, malformed output, reservation-unavailable, or any failed assertion is permanent non-success; exact verifier remains pair-focused and marker presence alone never authorizes 13-18. Inspect any created pair/marker read-only, write an accurate Summary, and end without retry/reopen/append/overwrite. Compare all baseline files after the attempt, keep marker and pair untracked/excluded from commits, run GitNexus detect-changes immediately before the Summary commit to require no code symbol/flow impact, and do not use a browser or remote/provider command.</action>
  <action_addendum>Decision trace for execution translation: D-01 requires exactly one deterministic non-R18 item and one local D1 row; D-02 permits only the closed fixture/prepared boundary; D-03 requires the released 13-21 fresh-runtime gate, every local gate, and canonical Gateway readiness before allocation and keeps remote later; D-04 binds target/run/code and local surfaces; D-05 is not executed in this local task; D-06 requires immutable non-secret pair/marker facts and no raw paths; D-07 keeps real provider proof in the downstream remote plans. A missing or blocked 13-21 runtime eligibility stops before run-id generation and records no local attempt. On a released runtime branch, run `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-readiness.json` and `pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-handoff.json` with the final pre-allocation suite; either failure stops before baseline or run-id generation.</action_addendum>
  <acceptance_criteria>
    - Every official local/test/runtime command exits 0 before any p13-17 string or evidence path exists, including `data-chain-smoke-remote.test.ts`, `mutation-entry.test.ts`, and the handoff/CLI-process focused tests.
    - The accepted run id matches `p13-17-[0-9a-f]{32}`; its directory was absent at allocation, then exact local JSON, Markdown, and `local.attempt` paths were all absent immediately before the only handoff. After acceptance no alternate ID exists.
    - Handoff atomically exclusive-creates zero-byte `local.attempt` before runner/provider/verifier. Marker pre-existence, concurrent loss, reservation failure, or crash permanently closes the run with no retry/reuse/deletion and zero downstream calls for a non-owner.
    - Handoff owns exactly one local runner and one internal exact verifier call; the only additional verifier is the read-only artifact check in this task's verify block, and no runner or handoff rerun occurs.
    - A successful result is exact pending with one local D1-owned itemId and no browser receipt; it is not terminal success.
    - Any failed or marker-only result closes this run and blocks 13-18 through 13-20; marker/checkpoint does not prove pair success, zero side effects, or Phase success, and the pair-focused verifier cannot promote it.
    - All pre-existing evidence stays byte-identical to the time-bounded baseline and untracked; no old marker is created/backfilled and no old artifact is read for replay, copied, staged, or committed. New marker/pair remain `??` and are excluded from the Summary commit.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$output = @(pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id '&lt;p13-17-run-id&gt;'); $code = $LASTEXITCODE; $lines = @($output | Where-Object { $_ -match '^\s*\{.*\}\s*$' }); if ($code -ne 2 -or $lines.Count -ne 1) { throw 'Expected one pending exact-verifier result with raw exit 2.' }; $machine = $lines[0] | ConvertFrom-Json; if ($machine.state -ne 'resolved_pending_observation' -or $machine.aggregate -ne 'pending' -or $machine.outcome -ne 'pending' -or [bool]$machine.provesExternalChain -ne $false) { throw 'Exact local artifact is not pending.' }; exit 0"</automated>
  </verify>
  <done>One exclusively reserved fresh local attempt has an exact pending pair certified for root IAB handoff, or its persistent marker and any partial/checkpoint evidence record one immutable non-success run that ends the execution chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repair/runtime gates -> run allocation | A new immutable attempt is allowed only after every deterministic prerequisite passes. |
| Generated run id -> pair/marker paths | Directory absence at allocation plus exact three-path absence immediately before handoff prevents accidental reuse; neither check replaces atomic ownership. |
| Pair validation -> local runner/provider work | Exclusive zero-byte `local.attempt` creation is the ownership linearization point; any pre-existing/concurrent-loser/unavailable marker state reaches no downstream call. |
| Runner/machine output -> pending tuple | Only exact artifact-cross-checked state may authorize browser work. |
| New evidence -> prior evidence tree | Old untracked attempts are immutable and excluded from writes/staging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-81 | Tampering | pre-run ordering | high | mitigate | All official commands before GUID generation; after allocation require local pair plus marker absent immediately before handoff. |
| T-13-82 | Spoofing | pending tuple | critical | mitigate | One runner/verifier handoff, unique machine JSON, exact peer/tuple validation. |
| T-13-83 | Tampering | historical evidence | high | mitigate | In-memory baseline, exact exclusions, no prior write/stage paths, post-attempt comparison. |
| T-13-84 | Denial of Service | repeated/concurrent/crashed attempt | critical | mitigate | Handoff creates persistent `local.attempt` with `wx`; EEXIST/concurrent loser is downstream-free, marker is never removed/reused, and every failure permanently ends the accepted run. |
| T-13-85 | Information Disclosure | local secrets/evidence | critical | mitigate | Existing process-only secrets and allowlisted artifact fields; no dumps or logs. |
| T-13-SC | Tampering | package supply chain | low | accept | No install or dependency action occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Full local and selected-production chain. | 13-17 local runner; 13-18 local browser; 13-19/20 remote. | COVERED | Pending alone is not goal completion. |
| REQ | DATA-01 | Canonical local API/auth path. | Task 1 and 13-18 | COVERED | 8080-only readiness. |
| REQ | DATA-02 | Local D1 readiness before ingest. | Task 1 | COVERED | Existing runner receipt order. |
| REQ | DATA-03 | One deterministic fixture and identity. | Task 1 and 13-19 | COVERED | One local upsert here. |
| REQ | DATA-04 | D1/API/admin proof. | Task 1 local D1/API; 13-18 admin; 13-19/20 remote | COVERED | Browser pending. |
| REQ | DATA-05/DATA-06 | Dashboard/viewer. | 13-18 and 13-20 | COVERED | No browser here. |
| REQ | DATA-07 | Local/remote artifacts. | Task 1 and 13-18 through 13-20 | COVERED | Exact peers plus persistent untracked mode-attempt marker; marker alone is non-success. |
| REQ | TEST-05 | Repeatable runner output. | 13-13..15 tests and Task 1 handoff | COVERED | Final pre-run suite covers fixed-root CLI, pair matrix, atomic marker, concurrency/crash, local/remote runners, mutation entry, and handoff before run allocation. |
| RESEARCH | - | Local-first, one item, strict evidence. | Task 1 | COVERED | No full crawler. |
| CONTEXT | D-01 | One shared target/run/code; mode-owned itemId. | Task 1 and 13-19 | COVERED | Local id established here. |
| CONTEXT | D-02 | Bounded fixture only. | Task 1 | COVERED | Closed prepared path. |
| CONTEXT | D-03 | Local gates before remote. | Task 1 and 13-18 | COVERED | Remote not invoked. |
| CONTEXT | D-04 | Exact surfaces/canonical routes. | Task 1 and 13-18 through 13-20 | COVERED | API precedes browser. |
| CONTEXT | D-05 | Remote preflight. | 13-19 | COVERED | Not eligible yet. |
| CONTEXT | D-06 | Non-secret evidence. | Task 1 | COVERED | Zero-byte allowlisted `local.attempt`, closed reservation refusals, and exact machine/pair cross-check; no raw path/error. |
| CONTEXT | D-07 | Provider proof in Phase 13. | 13-19/13-20 | COVERED | Remains downstream. |
| VERIFICATION | G-1 | Fresh terminal local prerequisite missing. | Task 1 and 13-18 | COVERED | This plan reaches pending only. |
| VERIFICATION | G-2/G-3 | Production evidence missing. | 13-19/13-20 | COVERED | No remote here. |
| VERIFICATION | G-4 | Wrapper mismatch. | Consumes 13-14 | COVERED | No raw outer-code assumption. |

Phase 14 literals/RUNBOOK/final matrix remain deferred and excluded.
</source_audit>

<verification>
- All official local gates plus fixed-root CLI, pair/reservation/concurrency/crash, local/remote runner, mutation-entry, handoff, and CLI-process regressions precede run allocation.
- After allocation require local JSON, Markdown, and `local.attempt` absent immediately before the one handoff; any marker presence terminally stops the accepted run with no alternate ID.
- Invoke only `smoke:data-chain:handoff` once. Require its persistent zero-byte marker, but let only machine JSON plus the exact pair decide pending eligibility; marker-only exact verification is non-success.
- Compare pre-existing evidence to the in-memory baseline, never backfill historical markers, and keep the new marker plus every evidence file untracked.
- Run GitNexus detect-changes before the Summary commit and require no code symbol/flow impact; exclude evidence from commit.
- Schema/migrations: N/A. No schema file, migration, schema push, provider resource, deploy, cleanup, or rollback action.
</verification>

<success_criteria>
- One p13-17 directory is allocated only after all repair/runtime gates pass, then its exact local pair and marker are absent immediately before handoff.
- One local handoff command atomically reserves `local.attempt` before downstream work and yields an exact pending tuple with count-one D1/API receipts and no browser receipt.
- Any reservation loss, crash, marker-only state, checkpoint, or other failure is immutable and ends the full execution chain without retry/alternate ID/marker deletion.
- Prior evidence remains time-bounded byte-identical and untracked.
</success_criteria>

## Artifacts this phase produces

- **New/modified code symbols:** none.
- **CLI contract consumed:** `smoke:data-chain:handoff --mode local --target starye-org --run-id <exact>` exactly once.
- **Machine fields consumed:** `mode`, `target`, `runId`, `state`, `aggregate`, `outcome`, `provesExternalChain`, `handoffReady`, `itemCode`, `itemId`, optional `checkpoint`.
- **Generated paths:** exact new-run zero-byte `local.attempt`, `local.json`, and `local.md`; all remain untracked. Marker is persistent ownership history and never exact-verifier success.
- **Tracked path:** `13-17-SUMMARY.md` only.
- **Schema/migrations:** N/A; no schema push.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md` after the only accepted-run attempt or terminal precheck. Record exact gates, baseline count/digest, directory and three-path absence checks, `local.attempt` state/zero-byte/untracked status, reservation refusal if any, command/downstream counts, tuple/state, pair-focused verifier result, and whether 13-18 is eligible. Never include secret values, create historical markers, or stage marker/evidence.
</output>
