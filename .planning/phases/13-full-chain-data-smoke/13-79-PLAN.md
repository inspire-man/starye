---
phase: 13-full-chain-data-smoke
plan: "79"
type: execute
wave: 73
depends_on: [13-78]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-79-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "一个 remote handoff 只有在 13-78 保存精确 `授权 remote handoff <p13-77-run-id>` 且 p13-77 local terminal 仍精确匹配时才能开始，per D-03、D-04、D-05。"
    - "唯一 repository-owned `--mode remote --target starye-org --run-id <p13-77-run-id>` handoff 拥有 nested official preflight、reservation 与 one-item fixture；不启动 standalone preflight 或第二次 remote handoff，per D-01、D-02、D-05。"
    - "remote pending eligibility 需要 passed nested preflight、runnerInvocations 1、target/run-derived code、count-one tuple 与非空 remote itemId；任一首个非成功冻结该 remote run，per D-04、D-06、D-07。"
    - "不复用或检查历史 remote carrier；p13-77 remote evidence 只由这次 handoff 建立，且不跟踪 secret。"
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-79-SUMMARY.md
      provides: non-secret authorization equality、唯一 remote handoff 结果与 frozen outcome。
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-77-run-id}/remote.json
      provides: 未跟踪的 selected-target pending tuple 或第一个 redacted remote checkpoint。
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-77-run-id}/remote.md
      provides: 与 remote JSON 对应的未跟踪 Markdown。
  key_links:
    - from: 13-78 exact authorization
      to: runDataChainHandoffCore remote invocation
      via: literal authorization run id 等于 p13-77 persisted run id，且无预先存在的 p13-77 remote pair/marker。
      pattern: 授权 remote handoff
    - from: nested remote preflight and one handoff
      to: Plan 13-80 production observation
      via: 仅 pending pair 的 preflightStatus passed、runnerInvocations 1、handoffReady true 与 non-empty remote itemId 可解锁。
      pattern: preflightStatus passed and handoffReady true
---

<objective>
在单独的人类授权后，对一个 fresh p13-77 carrier 执行一次且仅一次 selected-target remote handoff，并把 pending 或首个 checkpoint 交给 production observation 计划。

Purpose: remote preflight、reservation 与 provider write 必须由 repository-owned handoff 核心原子地拥有；拆开它们或重放历史 tuple 会破坏 D-05 的 fail-closed 边界。
Output: `13-79-SUMMARY.md` 与条件性未跟踪 p13-77 remote JSON/Markdown。该计划没有 production Dashboard/Viewer observation、standalone remote verifier、第二次 preflight 或 retry。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-77-RUN-ID.txt
@.planning/phases/13-full-chain-data-smoke/13-78-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
@packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
</context>

<interfaces>
- `13-78-SUMMARY.md` 必须保存 exact authorization decision；它不是一个泛化 permission，也不能被现有 remote evidence 替代。
- `runDataChainHandoffCore` 以 explicit `remote`、`starye-org`、p13-77 run id 为唯一 mutation path，内部拥有 selected-target preflight、reservation、pending pair inspection 与 fixed fixture runner。
- `DataChainHandoffResult` 中的 `preflightStatus`、`runnerInvocations`、`handoffReady`、code 和 remote itemId 是 Plan 13-80 的唯一 eligibility facts；本计划不单独调用 remote verifier。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 锁定 p13-77 exact authorization 与 fresh remote path，无 remote 读取或执行</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-79-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-77-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-78-SUMMARY.md
    - scripts/data-chain-handoff.ts
  </read_first>
  <action>只读取 13-77/13-78 tracked records，逐字核对 `13-78-SUMMARY.md` 的 literal authorization、`13-77-RUN-ID.txt`、p13-77 terminal local outcome、deterministic code 与 non-empty local itemId。确认 p13-77 的 remote JSON、remote Markdown 与 remote attempt marker 尚不存在，但在这里不读取 remote artifact 内容。将 exact authorization match 与 path-absence result 记录为 non-secret facts。

authorization mismatch、terminal proof 缺失、path collision 或任一不一致是 remote 前冻结：不运行 preflight、handoff、verifier、provider command、production navigation 或另一个 carrier。不得读取、比较、复用或重试 p13-55、p13-57、p13-66、p13-73 或其他历史 remote tree。该任务实现 D-03、D-04、D-05、D-06、D-07。</action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <done>p13-77 是唯一匹配 exact authority 且具有 fresh remote path 的 carrier；否则 remote mode 在零 provider 副作用下冻结。</done>
</task>

<task type="auto">
  <name>Task 2: 运行唯一 nested-preflight remote handoff 并冻结首个 non-success</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-79-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-78-SUMMARY.md
  </read_first>
  <action>仅在 Task 1 完成 exact authorization 和 fresh-path gate 时，调用唯一的 repository-owned command：`pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id <actual-p13-77-run-id>`。它内部拥有唯一 official selected-target nested preflight、attempt reservation、fixed one-item runner 与 pending-pair inspection；不另行运行 `target-profile preflight`、remote verifier、另一个 handoff、browser 或 provider command。

只有 `preflightStatus: passed`、`runnerInvocations: 1`、`handoffReady: true`、一个 deterministic non-R18 one-player code、count-one tuple 与 non-empty remote itemId 可产生 Plan 13-80 pending eligibility。任何 preflight/reservation/handoff/tuple/inspection 的首个 non-success 永久冻结 p13-77 remote mode，summary 只记录 authorization category、target/run/code、itemId presence、status/checkpoint；不得 retry、分配新 id、重写证据、使用历史 carrier 或触及 production route。按 D-01、D-02、D-04、D-05、D-06、D-07 执行。</action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <done>p13-77 具有一个 selected-target remote pending tuple，或具有一个 immutable remote checkpoint；两种结果都不产生 secret-tracked 数据或第二次 remote action。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Exact human authority to remote handoff | 一次 stateful provider operation 仅绑定一个 p13-77 id。 |
| Handoff core to provider | core 先执行 nested read-only preflight，随后才决定是否运行固定 fixture。 |
| Provider result to untracked evidence | core 输出 allowlisted pending/checkpoint facts，而非 raw provider material。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-79-01 | Spoofing | Authorization/run binding | critical | mitigate | literal authorization、run-id file 与 p13-77 terminal facts 三方 exact match。 |
| T-13-79-02 | Tampering | Remote reservation/evidence | high | mitigate | fresh pair/marker gate 与 repository-owned atomic handoff；无手工 evidence 写入。 |
| T-13-79-03 | Elevation of privilege | Provider write | critical | mitigate | 仅 fixed handoff core 运行 nested official preflight 与 closed fixture argv。 |
| T-13-79-04 | Information disclosure | Remote result | high | mitigate | summary/evidence 只包含 allowlisted status/code/itemId presence；不追踪 secret。 |
| T-13-79-05 | Denial of service | Fixture | medium | mitigate | D-01/D-02 强制 single item、single handoff、first checkpoint freeze。 |
| T-13-79-SC | Tampering | Package installation | low | accept | 本计划不执行 package-manager 安装。 |
</threat_model>

<verification>
- 在 live remote action 前运行 focused handoff/remote/verifier tests。
- 确认 remote handoff 的前置状态为 exact authorization、fresh path 和 p13-77 local terminal。
- 记录 remote pending 或首个 checkpoint 后，确认本计划未额外运行 remote verifier、browser、retry 或历史 carrier 操作。
</verification>

<success_criteria>
- 仅一次 remote handoff 与一个 p13-77 run id 绑定，nested preflight 不可拆出或绕过。
- 任何失败都作为该 run 的第一条永久 remote outcome，而不是触发另一个尝试。
- 唯一能进入 production observation 的状态是 fresh exact remote pending tuple。
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-79-SUMMARY.md` when done.
</output>
