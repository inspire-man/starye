---
phase: 13-full-chain-data-smoke
plan: "10"
type: execute
wave: 9
depends_on: [13-09]
files_modified:
  - "apps/api/.dev.vars"
  - "apps/gateway/.dev.vars"
  - ".env.local"
  - "packages/crawler/.env"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/local.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/local.md"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/remote.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/remote.md"
  - ".planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md"
autonomous: false
gap_closure: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Local Starye operator context
    why: The official local projection/preflight and one-item smoke require the existing user-managed local secret keys, running local services, and an authorized Dashboard session; values remain outside Git and evidence.
    env_vars:
      - name: BETTER_AUTH_SECRET
        source: Existing user-managed local API Worker secret store
      - name: CRAWLER_SECRET
        source: Existing user-managed local crawler/API service-auth secret store
      - name: ADMIN_GITHUB_ID
        source: Existing user-managed local Dashboard authorization configuration
    dashboard_config:
      - task: Keep the persistent Codex in-app browser signed in through the local Gateway with Movies management access.
        location: http://localhost:8080/auth/
  - service: Authorized starye-org selected-target context
    why: Remote mutation requires the existing explicit credential-key, account/resource ownership, and read-only live-check boundary plus an authorized canonical Dashboard session.
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: Authorized selected-target operator context
      - name: CLOUDFLARE_ACCOUNT_ID
        source: Authorized selected-target operator context
      - name: CRAWLER_SECRET
        source: Authorized selected-target operator context
    dashboard_config:
      - task: Keep the same persistent Codex in-app browser authorized for Movies access on the repository-resolved selected canonical Gateway.
        location: Repository-resolved selected canonical Gateway
must_haves:
  truths:
    - "D-03: the official starye-org target-local projection check and complete local preflight are green before any p13-10 run id is generated or collision-checked; only the official managed-block writer may restore target-owned values, and user-managed values remain outside its authority."
    - "D-01/D-02/D-03/D-04: one newly proven-absent opaque p13-10 run executes local mode once, establishes one deterministic non-R18 code and one local D1-owned itemId, and reaches terminal local D1/API/authorized Dashboard/viewer proof through http://localhost:8080 only."
    - "D-04/D-05: immediately before remote mutation, the exact official selected-target live preflight passes again; the closed fixture child and count-one remote D1 snapshot reuse targetId/runId/itemCode while independently establishing the remote itemId used by remote API/Dashboard/viewer receipts."
    - "D-04/D-06/D-07: the persistent Codex in-app browser records selected-canonical Dashboard before viewer through repository-owned observation logic, and both exact artifact verifiers return terminal_passed with provesExternalChain true before Phase 13 can be called complete."
    - "Every prior attempt and every failed p13-10 run remains unchanged; a checkpoint proves only its recorded non-success state and missing terminal receipts, never historical immutability or zero provider side effects."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/local.json"
      provides: "Exact fresh local tuple, lifecycle, runner receipts, and ordered browser receipts"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/local.md"
      provides: "Deterministic human-readable rendering of the exact local JSON artifact"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/remote.json"
      provides: "Provider-backed remote D1/API/Dashboard/viewer receipts for the same target/run/code correlation"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/remote.md"
      provides: "Deterministic human-readable rendering of the exact remote JSON artifact"
    - path: ".planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md"
      provides: "Accurate closeout distinguishing projection gate, terminal local proof, remote result, and Phase goal status"
  key_links:
    - from: "scripts/target-profile.ts project-local check/write plus local preflight"
      to: "fresh run allocation"
      via: "all official local gates must exit 0 before run-id generation"
      pattern: "project-local --target starye-org --check"
    - from: "fresh local evidence pair"
      to: "scripts/data-chain-smoke.ts remote eligibility"
      via: "terminal local targetId/runId/itemCode plus all eight passed local surfaces"
      pattern: "terminal_passed.*provesExternalChain"
    - from: "remote D1-established tuple"
      to: "scripts/data-chain-surface-observation.ts"
      via: "repository core derives selected canonical Dashboard then viewer navigation for the persistent Codex in-app browser"
      pattern: "remote_fixture_snapshot.*dashboard.*viewer"
    - from: "local and remote JSON/Markdown pairs"
      to: "scripts/verify-data-chain-smoke.ts"
      via: "artifact-only schema, parity, receipt, lifecycle, and exact tuple verification"
      pattern: "terminal_passed.*provesExternalChain"
  prohibitions:
    - statement: "Attempts A-E, the Plan 13-09 evidence, and all other prior evidence must not be modified, deleted, copied, reused, newly staged, or committed by this plan."
      status: resolved
      verification: judgment
    - statement: "No ad hoc Wrangler, SQL, provider, deploy, crawler, permission, schema, migration, cleanup, or rollback mutation may supplement the repository's official closed commands."
      status: resolved
      verification: judgment
    - statement: "Secrets, cookies, headers, raw provider context, raw endpoints, prepared context, and direct local application ports must not enter evidence, summaries, plans, or execution logs."
      status: resolved
      verification: judgment
    - statement: "A checkpoint or missing success receipt must not be represented as provider success, Phase completion, zero provider side effects, or proof of historical immutability."
      status: resolved
      verification: judgment
    - statement: "No schema file or migration is modified and no schema push is run; schema push is not applicable to this execution-only gap closure."
      status: resolved
      verification: judgment
---

<objective>
Close the three remaining Phase 13 verifier blockers with one ordered execution path: prove or restore the official local projection before allocating a run, obtain a fresh terminal local pair, then obtain provider-backed selected-production D1/API/Dashboard/viewer receipts and terminal exact verification for that same target/run/code correlation (D-01 through D-07).

Purpose: Plan 13-09 closed the official live-preflight prerequisite but spent its immutable run at `local_projection/target_projection_unmet`. Live planning checks now show the official projection check, complete local preflight, and 31 focused projection/preflight tests green, so no source defect or speculative repair is planned. The executor must re-prove those gates in its own session before it can consume a new run.

Output: conditional official managed-block restoration kept outside Git, one new immutable `p13-10-*` local/remote JSON+Markdown pair, and an accurate `13-10-SUMMARY.md`. Phase completion requires terminal local and remote exact-verifier results; any checkpoint leaves the phase open. This plan creates no code symbol, CLI flag, dependency, schema, migration, or provider resource. Schema push is not applicable.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/documentation-ownership.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-01-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-08-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-09-SUMMARY.md
@scripts/target-profile.ts
@packages/config/src/deployment-target/projection-plan.ts
@packages/config/src/deployment-target/env-file-block.ts
@scripts/data-chain-smoke.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
</context>

<discovery_evidence>
- Discovery level: 0. This plan uses established repository CLIs, tests, prepared entries, evidence types, and the accepted persistent-IAB adapter; no dependency or architecture choice is introduced.
- On 2026-07-19, read-only planning verification passed `pnpm target-profile validate --target starye-org`, `pnpm target-profile project-local --target starye-org --check`, and `pnpm target-profile preflight --target starye-org --scope local --command validate --wrangler-profile starye-org`.
- The focused projection/env-block/preflight suite passed 31/31. Live source confirms `--write` changes only target-managed blocks and fails closed on malformed marker topology. Therefore no projection source repair is authorized by this plan; if the official writer cannot restore a later mismatch, execution stops before run allocation and returns a new diagnosed gap.
- GitNexus graph relationships were 10 commits behind HEAD during planning, so its projection/smoke flow results are advisory; live source and executed commands are authoritative.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | Existing target profile, official marker-aware writer, user-managed local key presence | Green official projection check and complete local preflight; conditional managed-block-only local update | First and blocking. No run id, collision check, evidence directory, smoke, provider, or browser action is allowed before this task passes. |
| 2 | Task 1 green in the same execution session; running local stack; persistent authorized Codex IAB | One newly collision-gated local pair, terminal local proof or immutable checkpoint, and a session-only pre-run hash baseline for prior evidence | Second. A local checkpoint ends the run and forbids Task 3. |
| 3 | Task 2 exact terminal local pair; fresh passing official remote live preflight; authorized selected target and persistent IAB | Matching remote pair, both terminal exact-verifier results, plan summary, or an honest immutable remote checkpoint | Third. Only dual terminal proof can close the Phase goal. |
</dependency_graph>

<accepted_execution_override date="2026-07-19" surface="codex-in-app-browser">
Use only the persistent Codex in-app browser for local and remote Dashboard/viewer proof; do not launch an isolated, incognito, or default Puppeteer login browser. Reuse the accepted Plan 13-08 adapter boundary: invoke exported repository core `observeDataChainSurfaces()` with exactly one injected dependency, `observeSurface`. Repository core retains `read`, `write`, `evidenceRoot`, `resolveTarget`, `now`, evidence validation, tuple derivation, canonical base resolution, Dashboard-before-viewer order, receipt construction, deterministic rendering, and persistence.

The adapter receives only repository-derived `BrowserSurfaceObservationInput`, navigates to `new URL(input.path, input.baseUrl)`, requires the exact final origin/path, rejects direct application ports, waits for exactly one `[data-phase13-item-id="<input.itemId>"]` marker, and requires the settled DOM to contain both input itemCode and itemId. It returns the same tuple as `passed` only after those live checks; otherwise it returns `unavailable` so repository core persists a checkpoint. It cannot supply a status, tuple, surface order, receipt, cookie, storage state, screenshot-only claim, or artifact content.
</accepted_execution_override>

<tasks>

<task type="auto">
  <name>Task 1: Re-prove and conditionally restore the official local projection gate</name>
  <files>Conditional managed-block-only updates to apps/api/.dev.vars, apps/gateway/.dev.vars, .env.local, packages/crawler/.env; no tracked source or planning file.</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-01-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-01-SUMMARY.md
    - scripts/target-profile.ts
    - packages/config/src/deployment-target/projection-plan.ts
    - packages/config/src/deployment-target/env-file-block.ts
    - packages/config/src/deployment-target/__tests__/projection-plan.test.ts
    - packages/config/src/deployment-target/__tests__/env-file-block.test.ts
    - packages/config/src/deployment-target/__tests__/preflight.test.ts
  </read_first>
  <action>This is the first execution action. Before generating, hashing, or collision-checking any new run id, run exactly `pnpm target-profile project-local --target starye-org --check`. If it exits 0, do not invoke write mode. If and only if it reports target-managed projection mismatch, invoke the repository's official writer exactly once with `pnpm target-profile project-local --target starye-org --write`, then immediately rerun the exact check. The writer owns only the four typed target-managed blocks; do not inspect, print, copy, delete, replace, or hand-edit user-managed values, and stop on malformed marker topology instead of repairing around it. After the check exits 0, run `pnpm target-profile validate --target starye-org` and the complete local gate `pnpm target-profile preflight --target starye-org --scope local --command validate --wrangler-profile starye-org`. A missing user-managed key, malformed/missing consumer, target mismatch, or any other red gate stops this plan with no run id, collision check, evidence pair, browser action, provider action, or summary claiming execution success. Run the focused existing tests to confirm the writer/check boundary. Current planning evidence shows no source defect, so this task must not modify a function, class, method, test, package script, or target profile. If official write followed by check still cannot recover a reproducible target-owned mismatch, report the non-secret issue category and return for a separately planned source repair; do not improvise a symbol edit.</action>
  <acceptance_criteria>
    - The official `project-local --check` exits 0 in the executor's current session before any p13-10 run id or evidence path exists.
    - The exact local preflight exits 0 and verifies all four consumers plus required user-managed key presence without printing any value.
    - Write mode is skipped when check mode is already green; when used for a mismatch, it is invoked once and only the official managed-block boundary is allowed to change.
    - Focused projection/env-block/preflight tests pass, no code or schema file changes, and no local env file is staged or committed by this task.
  </acceptance_criteria>
  <verify>
    <automated>pnpm target-profile project-local --target starye-org --check</automated>
    <automated>pnpm target-profile preflight --target starye-org --scope local --command validate --wrangler-profile starye-org</automated>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/projection-plan.test.ts src/deployment-target/__tests__/env-file-block.test.ts src/deployment-target/__tests__/preflight.test.ts</automated>
  </verify>
  <done>The current target-local projection and complete local readiness boundary are explicitly green without exposing or taking ownership of user-managed values; otherwise execution has stopped before consuming a run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Create one collision-gated terminal local pair through the persistent IAB</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-10-run-id&gt;/local.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-10-run-id&gt;/local.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-08-PLAN.md
    - .planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-09-SUMMARY.md
    - scripts/data-chain-smoke.ts
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <what-built>The existing runner enforces local projection, D1 readiness, services, Gateway auth, one fixture, one count-one D1 snapshot, and canonical API before it can expose a pending tuple. The repository observer owns the only Dashboard-before-viewer promotion path.</what-built>
  <how_to_verify>After Task 1 is green, capture a session-only SHA256 baseline of every pre-existing evidence file without writing or staging a manifest; this proves only no change during Plan 13-10, not historical immutability. Generate one opaque id as `p13-10-` plus a lowercase 32-hex GUID. Require `Test-Path -LiteralPath` for its exact evidence directory to return `False` before the first smoke command. Invoke local mode exactly once. If it produces `resolved_pending_observation/pending`, use the accepted persistent Codex IAB adapter to let repository core record `http://localhost:8080/dashboard/movies` before `http://localhost:8080/movie/&lt;item-code&gt;`. Run the exact local verifier and require `terminal_passed` plus `provesExternalChain: true` before Task 3.</how_to_verify>
  <action>Only after Task 1 has passed, capture the session-only pre-run SHA256 map of existing evidence files; do not persist, copy, stage, or commit those files or a manifest, and do not describe the baseline as proof about their history before this task. Then generate one opaque `$runId = 'p13-10-' + [guid]::NewGuid().ToString('N')`. Before invoking the runner, require `Test-Path -LiteralPath ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/$runId"` to return `False`; if true, discard only the candidate string, generate another id, and repeat the collision gate without touching that directory. Invoke `pnpm smoke:data-chain -- --mode local --target starye-org --run-id &lt;p13-10-run-id&gt;` exactly once for the accepted id. Exit code 2 is not by itself terminal: inspect only the exact JSON/Markdown pair. A `pre_ingest` or non-pending checkpoint ends this run, remains immutable, blocks Task 3, and must not be retried. Only `resolved_pending_observation/pending` with passed receipt-backed local projection, D1 readiness, service readiness, Gateway auth, count-one D1, and Gateway API may enter browser proof. Use the accepted persistent-IAB adapter and repository core `observeDataChainSurfaces()`; local navigation is exactly Dashboard `/dashboard/movies` then viewer `/movie/&lt;item-code&gt;` at `http://localhost:8080`. Do not use the observer CLI's isolated/default Puppeteer profile, incognito, direct app ports, manual artifact edits, screenshots, caller-supplied status/tuple, or a different browser session. Run `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id &lt;p13-10-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence` against the resulting pair. Proceed only on exit 0 with `terminal_passed` and `provesExternalChain: true`; any code-2 classification is an honest local checkpoint and ends the run.</action>
  <acceptance_criteria>
    - Task 1's exact projection check and local preflight passed before run-id generation, and the accepted run directory was proven absent before the runner's only local invocation.
    - Terminal local evidence contains one deterministic non-R18 code, one local D1-owned itemId, count one, and internally consistent runner/API/Dashboard/viewer receipts using only `http://localhost:8080` canonical routes.
    - Dashboard precedes viewer and both receipts come from repository core plus the persistent Codex IAB live-navigation adapter, with exact final origin/path and code/id DOM markers.
    - The exact local verifier returns `terminal_passed` and `provesExternalChain: true`; otherwise Task 3 remains forbidden and the new local pair is retained unchanged as non-success evidence.
    - The session-only baseline and post-task comparison make no claim before the baseline timestamp; no prior evidence content or index state is changed by this task, and the new pair is not staged or committed.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path -LiteralPath '.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-10-run-id&gt;') { throw 'Evidence run directory already exists.' }"</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id &lt;p13-10-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
  </verify>
  <resume-signal>Continue to Task 3 only when the exact local verifier exits 0 and reports terminal_passed with provesExternalChain true. Any checkpoint closes this run without retry and leaves Phase 13 open.</resume-signal>
  <done>The new p13-10 run has a terminal, tuple-bound local Gateway pair eligible for remote mode, or an immutable honest local checkpoint that blocks every remote action.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Produce the selected-production provider and canonical browser pair</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-10-run-id&gt;/remote.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/&lt;p13-10-run-id&gt;/remote.md, .planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-08-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-09-SUMMARY.md
    - scripts/target-profile.ts
    - scripts/data-chain-smoke.ts
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <what-built>Task 2 supplies the only eligible exact local pair. Remote mode already owns explicit target selection, exact local prerequisite loading, official live checks, closed fixture/snapshot children, selected-canonical API correlation, mode-specific D1 item identity, and fail-closed evidence.</what-built>
  <how_to_verify>Immediately before any remote mutation, rerun exactly `pnpm target-profile preflight --target starye-org --scope remote --command smoke --ci-environment starye-org --live` and require exit 0. Invoke remote mode once with Task 2's exact target/run/code correlation. If it produces pending evidence, use repository core plus the same persistent IAB adapter for selected-canonical Dashboard before viewer. Run both exact artifact verifiers; each must report terminal_passed and provesExternalChain true. Compare all pre-existing evidence hashes to the Task 2 session baseline and confirm only the new run paths were written during this plan.</how_to_verify>
  <action>Proceed only from Task 2's exact terminal local pair. In the authorized selected-target context, immediately rerun `pnpm target-profile preflight --target starye-org --scope remote --command smoke --ci-environment starye-org --live`; any nonzero result stops before remote mutation and creates no synthetic provider evidence. Only after exit 0 invoke `pnpm smoke:data-chain -- --mode remote --target starye-org --run-id &lt;p13-10-run-id&gt;` exactly once. The runner must reload the terminal local pair, preserve targetId/runId/deterministic itemCode, execute only the closed one-item fixture child, then require the count-one `remote_fixture_snapshot` to establish a non-empty remote itemId before selected-canonical API proof. The remote itemId may differ from local but must match every remote D1/API/Dashboard/viewer receipt. A pre-ingest or other non-pending checkpoint ends this run unchanged and is not retried. Only a receipt-backed `resolved_pending_observation/pending` remote pair may enter the persistent-IAB path. Invoke repository core `observeDataChainSurfaces()` with only the accepted `observeSurface` adapter; core resolves the selected canonical base and records Dashboard `/dashboard/movies` before viewer `/movie/&lt;item-code&gt;`. Do not expose the raw origin, copy cookies/storage, use incognito/default Puppeteer, use a direct app port, manually edit evidence, or supply status/tuple/order/receipt content. Run the exact remote verifier, then rerun the exact local verifier. Phase success requires both exit 0 outputs to report `terminal_passed` and `provesExternalChain: true`, each mode internally consistent on its D1-owned itemId and both modes identical on target/run/code. Compare the Task 2 session-only prior-evidence hash map after execution; report only stability since that baseline, never historical immutability. Create `13-10-SUMMARY.md` after Task 2 has executed, accurately distinguishing projection gate, local checkpoint/terminal result, remote checkpoint/terminal result, and Phase goal status. Keep generated evidence untracked under the established policy and do not alter existing index state. Before any orchestrator-only exact-path closeout commit, run GitNexus `detect_changes` on the exact staged closeout set and require no code symbol or execution-flow impact; evidence stays excluded. This task does not authorize a code, package, target-profile, provider-permission, schema, migration, cleanup, deploy, or rollback change.</action>
  <acceptance_criteria>
    - The official remote live preflight exits 0 immediately before the one remote runner invocation and passes mapped D1/R2/KV/API Worker/Gateway Worker read-only checks without exposing values or raw output.
    - The closed remote fixture succeeds once; the count-one D1 snapshot independently establishes the remote itemId, and selected-canonical API plus ordered persistent-IAB Dashboard/viewer receipts match that remote tuple.
    - Both exact verifiers return `terminal_passed` and `provesExternalChain: true`, with shared target/run/code and mode-specific internally consistent itemIds, before Phase success is recorded.
    - Any provider, D1, API, authorization, browser, route, receipt, or verifier failure remains an immutable p13-10 checkpoint with no retry, promotion, zero-side-effect claim, or Phase completion claim.
    - All prior evidence hashes equal the Task 2 baseline, with the conclusion limited to Plan 13-10's execution window; the new evidence remains outside Git, unrelated dirty changes remain untouched, and GitNexus detects no code-symbol flow in any exact-path closeout commit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm target-profile preflight --target starye-org --scope remote --command smoke --ci-environment starye-org --live</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id &lt;p13-10-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode remote --target starye-org --run-id &lt;p13-10-run-id&gt; --evidence-dir .planning/phases/13-full-chain-data-smoke/evidence</automated>
  </verify>
  <resume-signal>Approve Phase 13 completion only after both exact verifiers are terminal and the canonical phase verifier confirms the three gaps closed. Otherwise retain the new checkpoint and report the exact non-secret failed gate.</resume-signal>
  <done>The selected target has provider-backed D1/API/admin and selected-canonical Dashboard/viewer proof for the exact fresh tuple, with both terminal verifiers green, or the run remains an immutable honest checkpoint and Phase 13 stays open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Official projection writer -> operator-owned local env files | Target-managed blocks may change, but user-managed lines and values remain outside the writer's authority and outside evidence/logs. |
| New run id -> evidence writer | A path collision could overwrite evidence; absence must be proven after projection is green and before the runner is invoked. |
| Terminal local pair -> remote runner | Remote mutation may consume only the exact terminal target/run/code correlation; local itemId cannot be copied into selected-production proof. |
| Authorized provider context -> closed remote children | Credentials and ownership authorize one bounded fixture/snapshot path but must not escape into child argv, artifacts, or alternate commands. |
| Persistent Codex IAB -> evidence core | Browser session state is external; only live exact-route and code/id DOM observations may become repository-rendered receipts. |
| Checkpoint -> Phase status | Non-success evidence can be misrepresented as provider completion or zero side effects unless exact verifier and canonical phase verification remain authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-43 | Tampering / Information Disclosure | project-local writer | high | mitigate | Exact check-first, conditional official write, marker-topology fail-close, focused preservation tests, complete local preflight, and no env value output/staging. |
| T-13-44 | Tampering | run-id evidence path | high | mitigate | Generate only after Task 1 passes, require exact `Test-Path -LiteralPath` false, invoke each mode once, and never reopen failed runs. |
| T-13-45 | Spoofing | remote local prerequisite | critical | mitigate | Exact artifact loader plus terminal local verifier and eight passed local surfaces before standalone/live and internal remote gates. |
| T-13-46 | Elevation of Privilege | provider mutation | critical | mitigate | Exact official live preflight immediately before mutation, closed one-item fixture/snapshot registry entries, fixed argv, and prohibition on supplemental commands. |
| T-13-47 | Information Disclosure | credentials/browser/evidence | critical | mitigate | Process-only key values, persistent IAB without cookie export, repository-derived canonical paths, allowlisted receipts, and exact redaction/parity verification. |
| T-13-48 | Spoofing / Repudiation | Dashboard/viewer and Phase result | high | mitigate | Repository core controls tuple/order/receipt persistence; exact local+remote verifiers and canonical Phase verifier are required for success. |
| T-13-49 | Tampering / Repudiation | historical and new evidence | high | mitigate | Session-only pre-run hashes, no access to prior write paths, immutable new checkpoint policy, and explicitly time-bounded stability claims. |
| T-13-SC | Tampering | package supply chain | low | accept | No package install, dependency change, or package-manager resolution occurs; existing locked commands only. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Prove the selected target through the full local-to-production data chain via Gateway/canonical domain. | Tasks 1-3 | COVERED | Dual terminal exact proof is the only Phase-complete outcome. |
| REQ | DATA-01 | Local API/auth/dashboard/content proof uses `http://localhost:8080`. | Existing 13-03/06/07/08 plus Task 2 regression proof | COVERED | Already satisfied; Task 2 prevents current projection drift from invalidating the fresh prerequisite. |
| REQ | DATA-02 | Local D1 schema/readiness precedes ingest/production. | Existing 13-01/03/06/08 plus Task 2 runner gate | COVERED | No schema edit or push is introduced. |
| REQ | DATA-03 | One deterministic bounded fixture records item identity. | Existing 13-01/02/05/06/08 plus Tasks 2-3 | COVERED | One code; each D1 owns its mode's itemId. |
| REQ | DATA-04 | Selected-production D1/API/admin correlation exists. | Task 3 | COVERED | Current verifier blocker 2. |
| REQ | DATA-05 | Authorized selected-production Dashboard proves the item. | Task 3 | COVERED | Current verifier blockers 2-3. |
| REQ | DATA-06 | Selected canonical viewer proves the same remote tuple. | Task 3 | COVERED | Current verifier blocker 3. |
| REQ | DATA-07 | Fresh local and production non-secret terminal evidence exists. | Tasks 2-3 | COVERED | Checkpoint remains non-success. |
| REQ | TEST-05 | Repeatable exact local/remote verification output distinguishes terminal and checkpoint. | Tasks 2-3 | COVERED | Both exact verifiers must be terminal for success. |
| RESEARCH | - | Projection check is a hard first gate; official managed-block writer preserves user values. | Task 1 | COVERED | Live check/preflight/tests are currently green; no source repair is planned. |
| RESEARCH | - | Local-first one-item runner, closed remote entries, Gateway/canonical browser proof, and redacted evidence. | Tasks 2-3 | COVERED | Uses only established stack and accepted adapter. |
| CONTEXT | D-01 | Exactly one deterministic non-R18 fixture with one player and shared target/run/code. | Tasks 2-3 | COVERED | No batch or sibling compatibility. |
| CONTEXT | D-02 | Only the bounded fixture/targeted adapter may write. | Task 3 | COVERED | Closed fixture child only. |
| CONTEXT | D-03 | Local projection/D1/Gateway proof precedes remote. | Tasks 1-2 | COVERED | Run allocation itself is blocked until projection/preflight green. |
| CONTEXT | D-04 | Local/remote share target/run/code; each D1 establishes its internally consistent itemId; canonical routes only. | Tasks 2-3 | COVERED | Local and remote UUID equality is neither required nor accepted as proof. |
| CONTEXT | D-05 | Explicit target, credential, ownership, and read-only live checks precede provider mutation. | Task 3 | COVERED | Official preflight is rerun immediately before remote runner. |
| CONTEXT | D-06 | Evidence is repeatable, allowlisted, non-secret, tuple-bound, and checkpoint-aware. | Tasks 2-3 | COVERED | JSON/Markdown parity plus exact verifier. |
| CONTEXT | D-07 | Real provider/data-chain execution remains Phase 13 scope. | Task 3 | COVERED | Phase 14 work remains excluded. |
| VERIFICATION | G-13-10-01 | Current selected local projection path must produce a fresh terminal local pair. | Tasks 1-2 | COVERED | Closes blocker 1 without spending a run before the gate. |
| VERIFICATION | G-13-10-02 | Provider-backed selected-production D1/API/admin proof is missing. | Task 3 | COVERED | Closed fixture, count-one snapshot, API, Dashboard. |
| VERIFICATION | G-13-10-03 | Selected-canonical Dashboard/viewer receipts and terminal remote verifier are missing. | Task 3 | COVERED | Persistent IAB, ordered receipts, both exact verifiers. |

Deferred Phase 14 source-literal cleanup, RUNBOOK stabilization, and final milestone evidence mapping remain excluded and are not source gaps. All current verifier gaps and every D-01 through D-07 decision are covered; no source item is missing.
</source_audit>

<verification>
- Task 1 is the hard first gate. The exact projection check, local preflight, and focused tests must be green before any p13-10 run id, collision check, evidence path, smoke, browser, or provider action.
- Task 2 must prove path absence and invoke local mode once. The persistent IAB plus repository core is the only allowed browser path; the exact local verifier alone decides remote eligibility.
- Task 3 reruns the exact official remote live preflight immediately before one remote invocation, uses only closed fixture/snapshot children, and requires both exact artifact verifiers to return `terminal_passed` with `provesExternalChain: true`.
- Re-run the canonical Phase verifier after dual terminal proof. Only `13-VERIFICATION.md` changing from `gaps_found` to a passing goal verdict closes Phase 13; a plan Summary or schema-valid checkpoint does not.
- Root `npm run build` is not a relevant gate for this execution-only plan because it requires surface-specific `STARYE_PAGES_BUILD_ENV_PATH`; do not convert that established environment requirement into a source change. Use the exact targeted commands above.
- No proposed file is a Drizzle schema/migration, Prisma model, Payload collection/global, Supabase migration, or TypeORM entity/migration. Schema push detection is negative and schema push is not applicable.
- Before any final execution closeout commit, run GitNexus `detect_changes` on the exact staged closeout paths. It must report no affected code symbols or execution flows; preserve and exclude unrelated dirty/staged files and every generated evidence path.
</verification>

<success_criteria>
- The official local projection check and complete local preflight pass before run allocation, with no user-managed value disclosed, overwritten, staged, or committed.
- One new collision-gated `p13-10-*` run produces terminal local proof through local D1, Gateway API, persistent-IAB Dashboard, and Gateway viewer using one internally consistent local tuple.
- The exact official selected-target live preflight passes immediately before the bounded remote attempt; selected-production D1 independently establishes the remote itemId used by canonical API, authorized Dashboard, and viewer receipts.
- Both exact artifact verifiers report `terminal_passed` and `provesExternalChain: true`, and the canonical Phase verifier closes all three current blockers.
- Any checkpoint preserves an accurate non-success artifact and Summary, consumes no retry, leaves Phase 13 open, and makes no claim about zero provider side effects or pre-baseline historical immutability.
</success_criteria>

## Artifacts this phase produces

- **Code symbols:** none. This is execution/evidence-only; no function, class, method, type, schema, migration, dependency, or provider resource is created or modified.
- **CLI flags:** none. It consumes existing `project-local --check|--write`, local/remote `preflight`, `smoke:data-chain --mode/--target/--run-id`, and exact verifier `--evidence-dir` contracts.
- **Conditional machine-local paths:** `apps/api/.dev.vars`, `apps/gateway/.dev.vars`, `.env.local`, and `packages/crawler/.env` may receive official target-managed-block restoration only; they remain outside Git and user-managed values remain untouched.
- **Generated evidence:** `.planning/phases/13-full-chain-data-smoke/evidence/starye-org/<p13-10-run-id>/{local,remote}.{json,md}`. A local checkpoint creates only the local pair; remote files exist only after terminal local eligibility. Evidence follows the established untracked policy.
- **Tracked closeout:** `.planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md` after Task 2 executes. ROADMAP/STATE/VERIFICATION changes, if any, are orchestrator/verifier-owned exact-path updates, not executor evidence commits.
- **Schema push:** not applicable; no schema-relevant file is proposed.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md` after Task 2 actually executes. If Task 1 fails, create no run id, evidence pair, or execution-success Summary. If Task 2 checkpoints, summarize the local pair and leave Task 3 forbidden. If Task 3 checkpoints, summarize both pairs as non-success. Keep evidence untracked; do not stage or commit any artifact in this plan unless the orchestrator later authorizes an exact-path closeout after GitNexus detect-changes.
</output>
