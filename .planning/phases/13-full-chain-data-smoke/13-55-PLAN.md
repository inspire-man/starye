---
phase: 13-full-chain-data-smoke
plan: "55"
type: execute
wave: 50
depends_on: [13-51, 13-52]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-55-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - TEST-05
must_haves:
  truths:
    - "Only after 13-51 observer repair, 13-52 honest checkpoint, and two accepted Gateway readiness records may a new p13-55 local run be allocated."
    - "Exactly one local handoff produces one unobserved pending pair for a new p13-55 run id; p13-52/p13-50/p13-49/p13-45/p13-41 are never reused."
    - "Ordered Dashboard then Viewer observation uses the signed-in Codex in-app Browser session as the sole observeSurface adapter and reaches terminal_passed with provesExternalChain true, or freezes an honest post-allocation checkpoint without retry."
    - "All p13-55 evidence remains untracked; no remote or production claim is made."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-55-RUN-ID.txt
      provides: sole allocated p13-55 identifier when pre-allocation gates pass
    - path: .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md
      provides: non-secret local terminal or honest checkpoint closeout
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-55-run-id}/local.json
      provides: untracked tuple-bound local evidence
  key_links:
    - from: pnpm check:services
      to: http://localhost:8080/robots.txt,/auth,/auth/
      via: two consecutive accepted readiness records before allocation
      pattern: "starye-gateway-readiness"
    - from: pnpm smoke:data-chain:handoff
      to: observeDataChainSurfaces with IAB observeSurface adapter
      via: one pending local pair observed Dashboard-before-Viewer on signed-in local session
      pattern: "resolved_pending_observation"
---

<objective>
Create one fresh local-only terminal data-chain proof after the 13-52 Dashboard auth checkpoint by reusing the operator-signed Codex in-app Browser session.

Purpose: p13-52 is immutable at dashboard_auth_unavailable. This plan allocates a new p13-55-<32 hex> boundary, runs one local handoff, one ordered Dashboard→Viewer observation through the signed-in IAB tab as a controlled observeSurface adapter, and one post-observation exact verifier.

Output: 13-55-RUN-ID.txt only if allocated, untracked local evidence, and 13-55-SUMMARY.md. Local proof only.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-full-chain-data-smoke/13-51-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-52-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
</context>

## Artifacts this phase produces
- 13-55-RUN-ID.txt (conditional)
- untracked evidence/starye-org/p13-55-*/local.{attempt,json,md}
- 13-55-SUMMARY.md

## Non-goals
- No reuse of p13-52 / p13-50 / p13-49 / p13-45 / p13-41 run ids
- No remote handoff, production browser, provider deploy, or migration
- No second handoff/observer/verifier after a post-allocation non-success
- No cookie/secret logging or tracked session material

<tasks>
<task type="auto">
  <name>Task 1: Confirm 13-51/13-52 gates and dual Gateway readiness</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-51-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-52-SUMMARY.md
  </read_first>
  <action>
Require 13-51-SUMMARY.md observer repair complete and 13-52-SUMMARY.md immutable checkpoint recorded. Confirm 13-55-RUN-ID.txt and evidence/starye-org/p13-55-* are absent. Confirm operator-signed local Dashboard is available in Codex in-app Browser at http://localhost:8080/dashboard. Run two consecutive pnpm check:services with robots/auth/authSlash accepted. If any gate fails, write honest pre-allocation Summary and stop without creating a run id.
  </action>
  <acceptance_criteria>
    - 13-51 and 13-52 summaries exist and authorize a new local attempt.
    - Two service checks accept all three Gateway probes before any p13-55 id exists.
    - Pre-allocation failure leaves no p13-55 run id or evidence tree.
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:services</automated>
  </verify>
  <done>Fresh local gates authorize exactly one p13-55 allocation, or only an honest pre-allocation Summary exists.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate one p13-55 handoff and pending verifier</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-55-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
Generate exactly one p13-55- + 32 lowercase hex id, write once to 13-55-RUN-ID.txt, ensure its evidence directory is absent, then invoke once: pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <run-id>. Require exit 0, outcome pending, handoffReady true, runnerInvocations 1, non-empty itemId. Run exact local verifier once expecting resolved_pending_observation, pending, provesExternalChain false for the same tuple. On any post-allocation failure, freeze this run in Summary; no retry or replacement id.
  </action>
  <acceptance_criteria>
    - One new p13-55 id and one pending local pair exist, or the allocated run is frozen honestly.
    - Historical p13-52 and earlier trees are untouched.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id <p13-55-run-id></automated>
  </verify>
  <done>One unobserved pending local pair is ready for ordered observation, or the run is permanently stopped.</done>
</task>

<task type="auto">
  <name>Task 3: IAB-signed ordered Dashboard→Viewer observation and terminal verify</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-55-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - .planning/phases/13-full-chain-data-smoke/13-55-RUN-ID.txt
  </read_first>
  <action>
Invoke observeDataChainSurfaces exactly once for mode local / target starye-org / the sole p13-55 run id, supplying a controlled observeSurface adapter backed by the already signed-in Codex in-app Browser tab. Adapter may return only status/itemCode/itemId. Navigate Gateway /dashboard/movies then /movie/<itemCode>; require Dashboard before Viewer. On success, run post-observation exact verifier once expecting terminal_passed and provesExternalChain true. On Dashboard/Viewer checkpoint, freeze the run, do not call a second observer or replacement handoff, and record the exact checkpoint code. Keep evidence untracked. Write 13-55-SUMMARY.md with tuple, exits, and hashes. Make no remote/production claim.
  </action>
  <acceptance_criteria>
    - Success path: ordered Dashboard+Viewer passed and post-observation verifier terminal_passed.
    - Failure path: single honest checkpoint, run frozen, no retry.
    - Evidence untracked; no remote commands; no secret logging.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id <p13-55-run-id></automated>
  </verify>
  <done>p13-55 is either local terminal proof authorizing remote planning, or an immutable honest local checkpoint.</done>
</task>
</tasks>
