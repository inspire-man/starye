---
phase: 13-full-chain-data-smoke
plan: "11"
type: execute
wave: 10
depends_on: [13-10]
files_modified:
  - "scripts/target-profile.ts"
  - "scripts/data-chain-smoke.ts"
  - "scripts/verify-data-chain-smoke.ts"
  - "packages/config/src/deployment-target/data-chain-evidence.ts"
  - "packages/config/src/deployment-target/__tests__/preflight.test.ts"
  - "packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts"
  - "packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts"
  - "packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts"
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "D-03: target-profile local preflight and the default local smoke runner use the same exported sanitized runtime-environment builder, so an ambient remote CLOUDFLARE_API_TOKEN cannot make the standalone gate green and the runner gate red."
    - "D-03/D-05: direct runTargetPreflight input that contains CLOUDFLARE_API_TOKEN still fails with local-api-token-shadowing; caller sanitization does not weaken the fail-closed identity policy."
    - "D-06: projection-mismatch and local-api-token-shadowing are separate closed checkpoint codes in JSON, deterministic Markdown, and exact-verifier machine output; arbitrary diagnostic text remains unrepresentable."
    - "D-01/D-02/D-04/D-07: the repair changes no one-item fixture, tuple, prepared child, provider, browser, canonical-route, schema, or migration contract and performs no live provider/browser mutation."
    - "The full evidence validation, local runner, remote runner, surface-observer, and exact-verifier regression set passes before the repair is committed."
  artifacts:
    - path: "scripts/target-profile.ts"
      provides: "Existing pickRuntimeEnvironment exported and parameterized as the single sanitized runtime-environment builder used by both local preflight callers"
    - path: "scripts/data-chain-smoke.ts"
      provides: "Default local runner environment parity plus deterministic mapping of allowlisted preflight issue codes"
    - path: "packages/config/src/deployment-target/data-chain-evidence.ts"
      provides: "Closed checkpoint vocabulary containing exact projection-mismatch and local-api-token-shadowing codes"
    - path: "scripts/verify-data-chain-smoke.ts"
      provides: "Allowlisted checkpoint code in exact-verifier machine output without free-form messages"
    - path: "packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts"
      provides: "Ambient-token default-dependency regression and diagnostic-code differentiation"
  key_links:
    - from: "scripts/target-profile.ts:pickRuntimeEnvironment"
      to: "scripts/target-profile.ts:runPreflight and scripts/data-chain-smoke.ts:runDataChainSmoke"
      via: "both local callers pass a sanitized allowlist rather than ambient process.env"
      pattern: "pickRuntimeEnvironment"
    - from: "runTargetPreflight issues"
      to: "DataChainObservation.checkpoint"
      via: "only exact allowlisted projection-mismatch or local-api-token-shadowing is persisted; all other local projection failures keep target_projection_unmet"
      pattern: "projection-mismatch|local-api-token-shadowing"
    - from: "local JSON checkpoint"
      to: "Markdown and smoke:data-chain:verify output"
      via: "existing strict evidence validation/rendering plus one allowlisted checkpoint field"
      pattern: "checkpoint"
  prohibitions:
    - statement: "The direct local-api-token-shadowing policy in runTargetPreflight must not be removed, bypassed, downgraded, or converted to a warning."
      status: resolved
      verification: judgment
    - statement: "No free-form issue message, token value, environment dump, header, cookie, raw endpoint, or prepared context may enter data-chain evidence or verifier output."
      status: resolved
      verification: judgment
    - statement: "No existing evidence directory, Attempts A-E, Plan 13-09 evidence, or Plan 13-10 evidence may be edited, staged, copied, retried, or committed."
      status: resolved
      verification: judgment
    - statement: "No provider, browser, Wrangler live, D1 mutation, deploy, permission, schema, migration, cleanup, rollback, or schema-push action may run in this code-contract plan."
      status: resolved
      verification: judgment
    - statement: "No any shortcut, untyped diagnostic string, new package, schema file, or migration file may be introduced."
      status: resolved
      verification: judgment
---

<objective>
Repair the diagnosed local-preflight caller-contract mismatch before another run is allocated: reuse the existing sanitized runtime-environment owner in both the target-profile CLI and smoke runner, then preserve the exact non-secret preflight failure code in the existing closed checkpoint channel (D-03, D-05, D-06) without changing the one-item, tuple, provider, browser, or Phase boundary decisions (D-01, D-02, D-04, D-07).

Purpose: Plan 13-10 proved the projection files and standalone local preflight are green, while the runner alone exposed ambient remote credentials and checkpointed. A read-only reproduction isolated the root cause to caller environment construction, and the generic target_projection_unmet artifact hid whether the failure was projection drift or token shadowing.

Output: one shared existing runtime-environment symbol used by both local callers, two exact allowlisted diagnostic checkpoint values, regression coverage for ambient-token parity and direct-policy rejection, and stable JSON/Markdown/exact-verifier observability. This plan performs no smoke run, provider command, browser action, schema change, migration, or package installation.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-10-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-10-SUMMARY.md
@scripts/target-profile.ts
@scripts/data-chain-smoke.ts
@packages/config/src/deployment-target/preflight.ts
@packages/config/src/deployment-target/data-chain-evidence.ts
@scripts/verify-data-chain-smoke.ts
</context>

<discovery_evidence>
- Discovery level: 0. The mismatch is reproduced in established repository code and needs no new dependency, external API choice, or architecture decision.
- Source inspection found `scripts/target-profile.ts:pickRuntimeEnvironment` already owns the platform-specific sanitized runtime allowlist. It is used by `runPreflight` and `runPagesBuild`; exporting and parameterizing this existing symbol is the smallest ownership boundary. Do not create a second environment parser in config, the smoke runner, or a new file.
- `scripts/data-chain-smoke.ts:runDataChainSmoke` ignores its existing `dependencies.environment` in local mode and passes `process.env` directly. Local mode must use the existing injectable environment only as input to `pickRuntimeEnvironment`; remote mode keeps its current unsanitized authorized environment contract.
- The existing `DataChainObservation.checkpoint` field is already strict, cloned, serialized, rendered into Markdown, and verified for parity. Extend that closed vocabulary with exact `projection-mismatch` and `local-api-token-shadowing`; do not add a free-form diagnostic field or edit immutable artifacts.
- GitNexus planning impact: `pickRuntimeEnvironment` LOW (2 direct callers, 2 processes), `runPreflight` LOW, `runDataChainSmoke` LOW, `renderDataChainEvidenceMarkdown` MEDIUM, and `runVerifyDataChainSmokeCli` LOW. `validateDataChainEvidence` is HIGH (2 direct callers, 3 affected processes, 20 impacted symbols): `runDataChainSmoke`, `observeDataChainSurfaces`, and `appendBrowserObservation`. The executor must re-run impact and repeat the HIGH/CRITICAL warning before any evidence-symbol edit; this plan avoids changing the validator body and requires the full three-flow regression set.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | Existing sanitized `pickRuntimeEnvironment`, local CLI fixture, injectable runner dependencies | Shared caller environment semantics plus ambient-token agreement regression | First; diagnostic mapping must consume the corrected caller contract. |
| 2 | Existing strict checkpoint field and evidence renderer | Exact allowlisted projection/token diagnostic checkpoint vocabulary with backward-compatible parity | Second; no runner mapping until the vocabulary is accepted. |
| 3 | Tasks 1-2 | Runner issue mapping, verifier observability, full regression/type proof, reviewed GitNexus scope | Third; all tests and detect-changes must pass before any commit. |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse the existing sanitized runtime environment in both local callers</name>
  <files>scripts/target-profile.ts, scripts/data-chain-smoke.ts, packages/config/src/deployment-target/__tests__/preflight.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - scripts/target-profile.ts
    - scripts/data-chain-smoke.ts
    - packages/config/src/deployment-target/preflight.ts
    - packages/config/src/deployment-target/__tests__/preflight.test.ts
    - packages/config/src/deployment-target/__tests__/identity-boundary.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  </read_first>
  <behavior>
    - A target-profile local preflight fixture with an ambient CLOUDFLARE_API_TOKEN still passes because the CLI supplies only the runtime allowlist and selected account id to runTargetPreflight; the test uses vi.stubEnv and restores the process environment in afterEach.
    - A local runDataChainSmoke call leaves runPreflight uninjected/default, injects collectProjectionIssues as an empty-result test double, injects every later D1/service/auth/fixture/snapshot/API dependency, and reaches the post-preflight pending path with an ambient CLOUDFLARE_API_TOKEN without reading ignored operator env files.
    - A direct runTargetPreflight call that receives the same raw token continues to return local-api-token-shadowing, proving policy remains fail closed.
    - Remote mode continues to receive its authorized unsanitized environment and its credential/account/live checks remain unchanged.
  </behavior>
  <action>Before modifying symbols, re-run GitNexus upstream impact for `pickRuntimeEnvironment` in `scripts/target-profile.ts`, `runPreflight` in the same file, and `runDataChainSmoke` in `scripts/data-chain-smoke.ts`; report direct callers, affected processes, and risk. If any result is HIGH or CRITICAL, explicitly warn again before editing. Keep the existing symbol name `pickRuntimeEnvironment` to avoid a manual rename. Export it and parameterize its source environment while retaining the current Windows/POSIX key allowlist; optionally accept only the selected account id needed by target-profile local preflight. The result must never copy CLOUDFLARE_API_TOKEN, CRAWLER_SECRET, browser credentials, or arbitrary source keys. `runPagesBuild` must retain its existing sanitized build behavior. Change `runPreflight` to call this one symbol with `process.env` and the resolved selected account id. Import the same symbol into `scripts/data-chain-smoke.ts`; in local mode call it with `dependencies.environment ?? process.env` before `runTargetPreflight`. Do not sanitize remote mode. Write the two ambient-token regressions before production changes. In `preflight.test.ts`, call `vi.stubEnv('CLOUDFLARE_API_TOKEN', 'ambient-test-token')` around the existing generated projection fixture, require the target-profile CLI preflight to pass, and call `vi.unstubAllEnvs()` from `afterEach` before/alongside fixture cleanup so no process state leaks to another test. In `data-chain-smoke-local.test.ts`, leave `runPreflight` absent so the real default wiring is exercised, inject `environment` with the ambient token, inject `collectProjectionIssues: async () => []` explicitly, and inject every later `inspectLocalD1`, `checkServices`, `observeGatewayAuth`, `runFixture`, `snapshot`, `fetchGatewayApi`, `write`, `resolveTarget`, and deterministic-time dependency required to reach pending evidence. This test must not call `collectProjectionIssuesDefault`, read `.env.local`, `apps/*/.dev.vars`, `packages/crawler/.env`, or depend on ignored operator files. Preserve the direct raw-token rejection in `identity-boundary.test.ts`; do not weaken it and do not use `any`.</action>
  <acceptance_criteria>
    - `pickRuntimeEnvironment` is the only runtime allowlist builder used by target-profile local preflight and local runDataChainSmoke; no new environment helper or parser exists.
    - The target-profile CLI fixture uses `vi.stubEnv` plus `afterEach` restoration, and the default-preflight runner leaves `runPreflight` uninjected while explicitly stubbing projection collection and every later external dependency.
    - Both ambient-token regressions pass in a clean clone/CI process with no ignored local env files, while direct runTargetPreflight still returns `local-api-token-shadowing` for raw token input.
    - Local smoke uses `dependencies.environment` when supplied, then sanitizes it; remote smoke behavior and credential visibility are unchanged.
    - Pages build behavior, selected account id injection, one-item runner ordering, direct-port ban, and all user-managed key/value boundaries remain unchanged.
    - GitNexus impact was reported before edits and detect-changes is run on the exact task paths before any task commit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/preflight.test.ts src/deployment-target/__tests__/identity-boundary.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
  </verify>
  <done>The standalone CLI and default runner share one sanitized local environment contract, while the underlying local token-shadowing policy remains directly enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the closed checkpoint vocabulary with exact preflight issue codes</name>
  <files>packages/config/src/deployment-target/data-chain-evidence.ts, packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - packages/config/src/deployment-target/preflight.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <behavior>
    - Pre-ingest local_projection checkpoint evidence accepts exact `projection-mismatch` and exact `local-api-token-shadowing` checkpoint codes.
    - JSON serialization and deterministic Markdown render the same exact checkpoint code without any message or secret value.
    - Existing `target_projection_unmet` artifacts remain valid and render identically.
    - Any checkpoint code outside the closed vocabulary is rejected.
  </behavior>
  <action>Before editing the evidence file, re-run GitNexus upstream impact for `dataChainCheckpointValues`, `DataChainCheckpoint`, `validateDataChainEvidence`, `renderDataChainEvidenceMarkdown`, and `appendBrowserObservation`. The known validator result is HIGH; explicitly warn with its three affected processes before continuing, and stop if a new CRITICAL result appears. Add the exact strings `projection-mismatch` and `local-api-token-shadowing` to `dataChainCheckpointValues`, so `DataChainCheckpoint` derives them without a free-form field. Do not modify the body of `validateDataChainEvidence`, clone/serializer/renderer behavior, receipt shape, schema version, lifecycle, required surfaces, browser order, or remote eligibility. Extend evidence tests to prove both new values survive typed creation, JSON serialization, Markdown rendering, and validation; retain tests for `target_projection_unmet`, unknown-code rejection, secret-shaped key rejection, direct-port rejection, and receipt parity. Do not edit any generated evidence file.</action>
  <acceptance_criteria>
    - The only new persisted diagnostic values are exact `projection-mismatch` and `local-api-token-shadowing` members of the closed DataChainCheckpoint union.
    - Existing checkpoint values and version-1 evidence remain backward compatible; Attempts A-E and Plans 13-09/13-10 require no rewrite.
    - JSON and Markdown contain the same allowlisted code, contain no free-form preflight message, and reject arbitrary codes/keys.
    - The validator, browser append, surface order, receipt integrity, and exact pair logic are unchanged in source and remain green in tests.
    - The HIGH impact warning is repeated before the evidence edit and GitNexus detect-changes is run before any task commit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-evidence.test.ts</automated>
  </verify>
  <done>The existing strict checkpoint channel can distinguish projection drift from local token shadowing without widening evidence or changing any old artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist runner diagnostics and expose them through the exact verifier</name>
  <files>scripts/data-chain-smoke.ts, scripts/verify-data-chain-smoke.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - scripts/data-chain-smoke.ts
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/preflight.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
  </read_first>
  <behavior>
    - A local preflight result whose first allowlisted diagnostic is projection-mismatch persists checkpoint `projection-mismatch`.
    - A local preflight result whose first allowlisted diagnostic is local-api-token-shadowing persists checkpoint `local-api-token-shadowing`.
    - Resolver/reader failures and every non-allowlisted preflight issue retain the generic `target_projection_unmet` checkpoint.
    - Exact verifier machine output includes only the persisted checkpoint code when one exists and never includes issue messages or values.
    - Local/remote terminal, pending, checkpoint, observer, receipt, and JSON/Markdown parity regressions remain green.
  </behavior>
  <action>Re-run GitNexus upstream impact for `runDataChainSmoke` and `runVerifyDataChainSmokeCli` before editing and warn on any HIGH/CRITICAL result. In the local `!preflightResult.ok` branch, inspect only `preflightResult.issues[].code` in its existing deterministic order. Persist exact `projection-mismatch` or `local-api-token-shadowing` when present; otherwise preserve `target_projection_unmet`. Do not persist messages, join multiple codes into text, change catch-path semantics, or alter remote checkpoint mapping. Extend `runVerifyDataChainSmokeCli` machine JSON with an optional `checkpoint` property derived only from the exact persisted observation; omit it for terminal/pending rows. Add focused tests for both differentiated failures, generic fallback, default ambient-token success from Task 1, and exact CLI output/redaction. Then run the complete config evidence/local/remote/verifier suite and type-check. Before every commit, run GitNexus detect-changes on the exact owned paths, review all known affected flows (`runDataChainSmoke`, `observeDataChainSurfaces`, `appendBrowserObservation`), and require no unplanned flow or unrelated worktree path. Do not stage generated evidence or external changes.</action>
  <acceptance_criteria>
    - New failures self-identify as projection mismatch versus local token shadowing in JSON, Markdown, and exact-verifier JSON while old generic artifacts still verify.
    - No issue message/value, environment data, endpoint, cookie, header, or raw context enters any serialized or logged result.
    - The full evidence, local runner, remote runner, surface-observer, and exact-verifier suites pass, including all three flows identified by the HIGH impact report.
    - Config type-check and `git diff --check` pass with no `any`, dependency, schema, migration, package-script, CLI-flag, provider, or browser change.
    - GitNexus detect-changes reports only the expected deployment-target evidence and smoke execution scope before each commit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/preflight.test.ts src/deployment-target/__tests__/identity-boundary.test.ts src/deployment-target/__tests__/data-chain-evidence.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
    <automated>pnpm --filter @starye/config type-check</automated>
    <automated>git diff --check -- scripts/target-profile.ts scripts/data-chain-smoke.ts scripts/verify-data-chain-smoke.ts packages/config/src/deployment-target/data-chain-evidence.ts packages/config/src/deployment-target/__tests__/preflight.test.ts packages/config/src/deployment-target/__tests__/data-chain-evidence.test.ts packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <done>The diagnosed caller mismatch is closed, exact non-secret diagnostics are self-contained, and every affected smoke/evidence/browser flow is regression-green without a live execution attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Ambient operator process -> local preflight callers | Remote credentials may be present in the shell, but local Wrangler-profile validation must receive only the canonical runtime allowlist. |
| Preflight result -> evidence checkpoint | Only exact non-secret issue codes may cross; free-form messages and values remain outside the artifact. |
| Evidence vocabulary -> runner/observer/verifier flows | A vocabulary change can affect validation, append, rendering, and remote eligibility even without changing the validator body. |
| Existing untracked evidence -> repaired code | Old immutable artifacts must remain readable and byte-untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-50 | Spoofing / Elevation of Privilege | local preflight environment | high | mitigate | One exported existing allowlist builder feeds both local callers; direct raw-token rejection remains covered. |
| T-13-51 | Information Disclosure | diagnostic persistence | high | mitigate | Persist only two exact closed codes through the existing checkpoint field; no messages, values, or new free-form field. |
| T-13-52 | Tampering / Repudiation | evidence schema and parity | high | mitigate | Repeat HIGH impact warning, preserve schema version, test old/new checkpoints, JSON/Markdown parity, observer append, and exact verifier. |
| T-13-53 | Denial of Service | shared evidence validation flows | medium | mitigate | Full local/remote/observer/verifier regression and type-check before commit; review all three known affected processes in detect-changes. |
| T-13-54 | Tampering | historical evidence | medium | mitigate | No evidence write command in this plan; exact path exclusion and worktree review before commits. |
| T-13-SC | Tampering | package supply chain | low | accept | No package install, lockfile change, or dependency resolution occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Prove selected-target local-to-production data chain through Gateway/canonical domain. | 13-11 enables 13-12; 13-12 executes proof | COVERED | Repair is a hard dependency, not a substitute for live proof. |
| REQ | DATA-01 | Fresh local Gateway path must be runnable through 8080. | Tasks 1/3 plus 13-12 Tasks 1/2 | COVERED | Caller parity removes the current pre-ingest false disagreement. |
| REQ | DATA-02 | Local D1 readiness precedes ingest. | 13-12 Task 1 | COVERED | No schema change in 13-11. |
| REQ | DATA-03 | One deterministic item records identity. | 13-12 Tasks 1/3 | COVERED | Existing one-item contract unchanged. |
| REQ | DATA-04 | D1/API/admin proof exists locally and remotely. | 13-12 Tasks 2/3 | COVERED | Current verifier blocker 2. |
| REQ | DATA-05 | Authorized Dashboard proves exact tuple. | 13-12 Tasks 2/3 | COVERED | Persistent IAB only. |
| REQ | DATA-06 | Canonical viewer proves exact tuple. | 13-12 Tasks 2/3 | COVERED | Dashboard must precede viewer. |
| REQ | DATA-07 | Repeatable local/remote evidence is non-secret and diagnosable. | Tasks 2/3 plus 13-12 | COVERED | Current generic checkpoint deficiency is repaired here. |
| REQ | TEST-05 | Smoke output is repeatable across terminal/checkpoint cases. | Tasks 1-3 plus 13-12 exact verifiers | COVERED | Default-dependency regression is explicit. |
| RESEARCH | - | Reuse selected-target preflight, closed fixture, strict evidence, and no new dependency. | Tasks 1-3 | COVERED | Existing owner and vocabulary are reused. |
| RESEARCH | - | Local-first proof gates all remote mutation. | 13-12 Tasks 1-3 | COVERED | No live action in 13-11. |
| CONTEXT | D-01 | One target/run-derived non-R18 item and per-mode itemId. | Preserved by Tasks 1-3; executed in 13-12 | COVERED | No fixture/tuple edit. |
| CONTEXT | D-02 | Only bounded fixture/targeted adapter writes. | 13-12 Task 3 | COVERED | No write in 13-11. |
| CONTEXT | D-03 | Local projection/D1/Gateway proof precedes remote. | Task 1 and 13-12 Tasks 1/2 | COVERED | Root cause belongs to this order gate. |
| CONTEXT | D-04 | Local/remote share target/run/code with mode-owned itemId and canonical routes. | 13-12 Tasks 2/3 | COVERED | No route or tuple change. |
| CONTEXT | D-05 | Explicit target/live ownership checks precede provider work. | Task 1 preserves policy; 13-12 Task 3 executes it | COVERED | Direct token-shadowing rejection stays green. |
| CONTEXT | D-06 | Allowlisted non-secret JSON/Markdown evidence. | Tasks 2/3 | COVERED | Exact code, no free-form detail. |
| CONTEXT | D-07 | Real provider proof remains Phase 13; Phase 14 stays deferred. | 13-12 Task 3 | COVERED | No documentation-only substitution. |
| VERIFICATION | G-13-11-01 | Standalone and runner local preflight semantics disagree. | Task 1 | COVERED | Root cause repaired at existing owner. |
| VERIFICATION | G-13-11-02 | Evidence hides projection versus token-shadowing failure. | Tasks 2/3 | COVERED | Exact closed checkpoint code is persisted and logged. |
| VERIFICATION | G-13-11-03 | Provider D1/API/admin proof is absent. | 13-12 Task 3 | COVERED | Serial dependency prevents premature execution. |
| VERIFICATION | G-13-11-04 | Production Dashboard/viewer and terminal verifier are absent. | 13-12 Task 3 | COVERED | Persistent IAB handoff is explicit. |

Deferred Phase 14 literal cleanup, RUNBOOK stabilization, recovery documentation, and final requirement-to-evidence mapping remain excluded. The stale 10-item statement in the original RESEARCH.md is superseded by current CONTEXT D-01 and the executed one-item contract; it is not a missing plan item.
</source_audit>

<verification>
- No live provider, browser, D1 mutation, deploy, permission, schema, migration, cleanup, rollback, or schema-push command runs in this plan.
- Before every symbol edit, re-run GitNexus impact. The known evidence validator blast radius is HIGH; repeat the warning, review the three named flows, and do not continue silently if risk remains HIGH/CRITICAL.
- Before every commit, run GitNexus detect-changes on the exact owned paths. Exclude unrelated worktree changes and every evidence path.
- Run the focused six-file Vitest command, config type-check, and exact `git diff --check`. The repair is not ready for 13-12 until all are green.
- Re-run `pnpm target-profile project-local --target starye-org --check`, `pnpm target-profile validate --target starye-org`, and `pnpm target-profile preflight --target starye-org --scope local --command validate --wrangler-profile starye-org` after the code commit as read-only integration proof; do not allocate a run id in this plan.
</verification>

<success_criteria>
- CLI and runner share the existing exported sanitized environment owner and agree under an ambient remote token.
- Direct raw-token local preflight still returns local-api-token-shadowing, preserving the fail-closed policy.
- New checkpoint artifacts can self-identify exact projection mismatch versus token shadowing in JSON, Markdown, and verifier output without carrying a message or value.
- Old evidence remains valid and untouched; every evidence/runner/observer/verifier regression and type-check passes.
- GitNexus impact warnings and detect-changes gates are recorded before edits/commits, with no unplanned symbol or flow impact.
</success_criteria>

## Artifacts this phase produces

- **New symbols:** none. Source inspection selected the existing `pickRuntimeEnvironment` owner rather than inventing another helper.
- **Modified symbols:** `scripts/target-profile.ts:pickRuntimeEnvironment`, `scripts/target-profile.ts:runPreflight`, `scripts/data-chain-smoke.ts:runDataChainSmoke`, `packages/config/src/deployment-target/data-chain-evidence.ts:dataChainCheckpointValues`, derived `DataChainCheckpoint`, and `scripts/verify-data-chain-smoke.ts:runVerifyDataChainSmokeCli`.
- **CLI flags:** none. Existing `target-profile`, `smoke:data-chain`, and `smoke:data-chain:verify` flags are unchanged.
- **Modified paths:** exactly the eight `files_modified` paths in frontmatter; no package, lockfile, target profile, fixture, observer, provider, or planning-state path is changed.
- **Generated evidence:** none. Plan 13-11 must not allocate a run id or write under `.planning/phases/13-full-chain-data-smoke/evidence/`.
- **Schema/migrations:** N/A. No Drizzle schema, D1 migration, Prisma/Payload/Supabase/TypeORM schema, schema push, or provider resource is created or modified.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-11-SUMMARY.md` after all three tasks pass. The Summary must record the repeated HIGH impact warning, exact test/type-check results, detect-changes scope, and that no live run/evidence/provider/browser/schema action occurred.
</output>
