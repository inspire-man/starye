---
phase: 13-full-chain-data-smoke
plan: "51"
type: execute
wave: 46
depends_on: [13-50]
files_modified:
  - scripts/data-chain-surface-observation.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-51-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-05
  - DATA-06
  - TEST-05
must_haves:
  truths:
    - "Local Dashboard observation no longer maps every non-pass into dashboard_auth_unavailable when the surface is authenticated but the smoke tuple is simply not visible."
    - "A controlled observeSurface adapter or authenticated local profile that returns status=passed with the exact itemCode/itemId produces appendBrowserObservation passed receipts for Dashboard then Viewer without secret leakage."
    - "Focused observer/evidence regressions pass; p13-49 and p13-50 evidence trees remain byte-immutable and untracked."
    - "No p13-51 smoke run id, handoff, remote command, or production browser is allocated by this plan."
  artifacts:
    - path: scripts/data-chain-surface-observation.ts
      provides: corrected local observation choke point and clearer non-success mapping
    - path: packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
      provides: regression coverage for auth-miss vs tuple-miss vs passed adapter paths
    - path: .planning/phases/13-full-chain-data-smoke/13-51-SUMMARY.md
      provides: non-secret closeout of the observer contract repair
  key_links:
    - from: observeSurfaceDefault
      to: captureSurface
      via: unavailable or mismatched tuple must not be silently promoted; passed requires exact itemCode/itemId
      pattern: "dashboard_auth_unavailable"
    - from: observeDataChainSurfaces
      to: appendBrowserObservation
      via: Dashboard receipt precedes Viewer; first Dashboard checkpoint freezes the pair
      pattern: "observeDataChainSurfaces"
---

<objective>
Repair the shared local Dashboard observation choke point that caused p13-49 and p13-50 to freeze at `dashboard_auth_unavailable` even after Gateway readiness passed and a human-visible signed-in Dashboard existed.

Purpose: Phase 13 still lacks a reliable local Dashboard→Viewer terminal path after 13-50. Live evidence shows handoff can reach `resolved_pending_observation`, but `scripts/data-chain-surface-observation.ts` collapses non-pass Dashboard results into `dashboard_auth_unavailable`. Default puppeteer uses an isolated `.target-runs/phase13-browser-profile/{targetId}` profile that is not the signed-in in-app session. This plan fixes the observer contract and regressions only.

Output: code/test repair + `13-51-SUMMARY.md`. No new smoke run.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/evidence/starye-org/p13-50-c7d3d017012d4f2186cede0d1bfb2341/local.json
@scripts/data-chain-surface-observation.ts
@packages/config/src/deployment-target/data-chain-evidence.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
</context>

## Artifacts this phase produces
- Updated `observeSurfaceDefault` / `captureSurface` / adapter normalization behavior in `scripts/data-chain-surface-observation.ts`
- Regression assertions in local (and remote if shared) data-chain smoke tests
- `13-51-SUMMARY.md`

## Non-goals
- No `p13-51-*` run allocation, handoff, exact verify smoke, remote, production, D1, or Wrangler
- No rewrite of p13-49 / p13-50 / p13-45 / p13-41 evidence
- No production session work (that is 13-53/13-54)

<tasks>
<task type="auto">
  <name>Task 1: Impact-analyze and diagnose the observer false auth checkpoint</name>
  <files>scripts/data-chain-surface-observation.ts, .planning/phases/13-full-chain-data-smoke/13-51-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - .planning/phases/13-full-chain-data-smoke/13-50-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/p13-50-c7d3d017012d4f2186cede0d1bfb2341/local.json
  </read_first>
  <action>
Before any symbol edit, run GitNexus impact analysis upstream on `observeSurfaceDefault`, `captureSurface`, and `observeDataChainSurfaces`. Report blast radius and risk to the Summary. If HIGH or CRITICAL, stop and alert before editing.

Reconstruct the live failure from p13-50 `local.json`: aggregate `checkpoint`, ingestState `resolved_pending_observation`, Dashboard surface checkpoint `dashboard_auth_unavailable`, no Viewer observation. Compare against source behavior: `observeSurfaceDefault` launches puppeteer with `userDataDir` under `.target-runs/phase13-browser-profile/{targetId}`; pass requires final path `/dashboard/movies` (or viewer path) plus body/html containing both `itemCode` and `itemId`; every non-pass becomes `dashboard_auth_unavailable` for Dashboard. Document in Summary which of these concrete causes applied to 13-49/13-50: (1) isolated profile not signed-in / redirect to `/auth/login`, (2) signed-in but tuple text absent on `/dashboard/movies`, (3) controlled adapter returned `unavailable` or mismatched codes, (4) baseUrl/target resolution failure path that hard-codes `dashboard_auth_unavailable`. Do not modify historical evidence files.
  </action>
  <acceptance_criteria>
    - GitNexus impact results for the three symbols are recorded with risk level before edits.
    - Summary names the concrete choke-point class for p13-49/p13-50 with source identifiers (`observeSurfaceDefault`, `captureSurface`, checkpoint string).
    - No historical evidence file is modified.
  </acceptance_criteria>
  <verify>
    <automated>npx --no-install gitnexus impact --repo starye --target observeDataChainSurfaces --direction upstream</automated>
  </verify>
  <done>Diagnosis and impact are written; safe to implement the shared fix.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the shared observation contract and lock regressions</name>
  <files>scripts/data-chain-surface-observation.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts, packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts, .planning/phases/13-full-chain-data-smoke/13-51-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
  </read_first>
  <action>
Implement the minimal shared fix that makes local authenticated observation honest and usable for the next terminal run:

1. Keep repository core ownership of tuple validation, receipt order, and evidence writes inside `observeDataChainSurfaces` / `captureSurface`.
2. Ensure a dependency-injected `observeSurface` that returns `{ status: 'passed', itemCode, itemId }` for the exact pending tuple yields Dashboard then Viewer `passed` receipts and does not invent `dashboard_auth_unavailable`.
3. For the default puppeteer path, require canonical Gateway base (`LOCAL_GATEWAY_ORIGIN` / target gateway URL only; never direct 5173/300x). If final URL path is an auth login redirect, keep `dashboard_auth_unavailable`. If the page stays on the requested Dashboard/Viewer path but tuple markers are missing, use a distinct non-success checkpoint already allowed by evidence vocabulary when one exists; if the closed vocabulary still requires `dashboard_auth_unavailable`, document that limitation in Summary and still ensure the authenticated-pass adapter path works for executors that supply a signed-in visible UI adapter.
4. Do not log cookies, tokens, storage, or passwords. Adapter results remain limited to `status` / `itemCode` / `itemId`.
5. Extend focused tests in `data-chain-smoke-local.test.ts` (and remote shared cases only if the same helper is covered there) for: passed adapter → terminal-ready pending pair advancement; auth-redirect/unavailable → Dashboard checkpoint without Viewer; mismatched itemCode/itemId → non-pass.

Run the focused vitest targets that cover these files. Write `13-51-SUMMARY.md` with commands, exit codes, and the statement that no p13-51 run was allocated. Per D-03/D-06, only Gateway-canonical local URLs appear in docs/commands.
  </action>
  <acceptance_criteria>
    - Source change makes exact-tuple passed adapter observations produce passed Dashboard/Viewer receipts in unit tests.
    - Auth/unavailable path still fail-closes without writing Viewer success.
    - Focused vitest commands exit 0.
    - `13-51-SUMMARY.md` exists and states no smoke run/handoff/remote was executed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts</automated>
  </verify>
  <done>Observer choke point repaired and regression-locked without allocating a new smoke run.</done>
</task>
</tasks>

## PLANNING COMPLETE marker for this file
gap_closure plan 13-51 ready for execute-phase --gaps-only
