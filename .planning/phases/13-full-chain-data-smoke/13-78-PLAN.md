---
phase: 13-full-chain-data-smoke
plan: "78"
type: execute
wave: 72
depends_on: [13-77]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-78-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "仅当 p13-77 的本地证据为 receipt-backed terminal_passed、provesExternalChain true、deterministic code 与非空 local itemId 时，才展示人工 remote 授权 gate，per D-03、D-04、D-07。"
    - "唯一可接受的授权是绑定已持久化 p13-77 run id 的精确文本 `授权 remote handoff <actual-p13-77-run-id>`；历史授权或泛化继续指令均不拥有 remote 权限，per D-05。"
    - "在授权前，本计划不读取 remote evidence、不执行 remote preflight/handoff/verifier/provider command/production navigation，且不追踪 secret，per D-05、D-06。"
    - "p13-77 的首个 local non-success、p13-66、p13-73 与全部早期 carrier 都是不可变的非授权输入。"
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-78-SUMMARY.md
      provides: non-secret local eligibility 与 run-id-bound 人类授权决策记录。
  key_links:
    - from: p13-77 local terminal evidence
      to: literal remote authorization
      via: persisted run id、terminal_passed、provesExternalChain、exact code 和 non-empty local itemId 必须同时成立。
      pattern: terminal_passed and provesExternalChain true
    - from: literal authorization
      to: Plan 13-79 remote handoff
      via: 授权文本的实际 run id 与 13-77-RUN-ID.txt 精确相等。
      pattern: 授权 remote handoff
---

<objective>
把 provider-side remote mutation 与 fresh local carrier 严格隔离：先核验 p13-77 的持久化 terminal proof，再等待一次绑定真实 run id 的人工授权。

Purpose: local terminal proof 不蕴含 provider 权限。13-79 只能消费本计划记录的精确授权，不能借用 p13-55、p13-66、p13-73 或任一历史 authorization。
Output: `13-78-SUMMARY.md` 的 non-secret eligibility/decision 记录；该计划本身不创建或读取 remote evidence，不执行 remote/prod 动作。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-76-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-77-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-77-RUN-ID.txt
@scripts/verify-data-chain-smoke.ts
</context>

<interfaces>
- `13-77-RUN-ID.txt` 是唯一的授权 identity source；它不允许由聊天文本、历史 summary 或推测生成。
- `inspectDataChainSmokeVerification` 的 persisted local terminal fields 是 eligibility 事实；本计划不调用任何 remote runner 或读取 remote pair。
- `13-78-SUMMARY.md` 只能记录 authorization 是否精确匹配及 non-secret local eligibility，不能写 session、provider 凭据、endpoint 或 raw prepared context。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 只从 p13-77 持久化 local terminal facts 准备授权上下文</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-78-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-77-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-77-RUN-ID.txt
    - scripts/verify-data-chain-smoke.ts
    - .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
  </read_first>
  <action>只读取 p13-77 tracked summary、run-id file 和其已持久化 local verifier result。确认 run id 具有 `p13-77-` 前缀且精确匹配、结果为 `terminal_passed`、`provesExternalChain: true`、deterministic item code 存在并且 local itemId 非空。把这五项 allowlisted 事实或不合格原因写入 `13-78-SUMMARY.md`。任何不匹配都是 p13-77 frozen branch：不展示可授权的 run id，且不执行 remote preflight、remote evidence read、handoff、verifier、provider command 或 production navigation，按 D-03、D-04、D-05、D-06、D-07 执行。

不得以 p13-66、p13-73、13-74/75、任何 earlier RUN-ID、任一未跟踪 evidence tree 或先前 authorization 补足缺失事实；它们只保留为不可变背景。</action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/verify-data-chain-smoke.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
  </verify>
  <done>仅当 p13-77 的 exact local terminal carrier 真实存在时，summary 才显示一个可用于人工授权的值；否则 remote surface 保持未触及。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 获得绑定 p13-77 的一次性 remote handoff 授权</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-78-SUMMARY.md</files>
  <action>在 Task 1 已验证 p13-77 persisted terminal facts 后，展示且仅展示 `13-77-RUN-ID.txt` 中的实际值。仅接受逐字匹配的 `授权 remote handoff <actual-p13-77-run-id>`，把 exact-match authorization 或拒绝/不匹配结果写入 summary。授权到达前不读取 remote evidence，不执行 remote preflight、handoff、verifier、provider command 或 production navigation；历史 authorization、泛化 continuation 与构造的 run id 均不授予权限，按 D-03、D-04、D-05、D-06、D-07 执行。</action>
  <what-built>p13-77 的本地 terminal eligibility 已以无密钥事实呈现，尚未启动任何 remote 或 production 操作。</what-built>
  <how-to-verify>
1. 阅读 `13-78-SUMMARY.md`、`13-77-SUMMARY.md` 与 `13-77-RUN-ID.txt`。
2. 确认 `terminal_passed`、`provesExternalChain: true`、deterministic code 和 non-empty local itemId 均属于同一个 p13-77 run。
3. 如同意仅执行一次该 run 的 provider-side handoff，输入 `授权 remote handoff <actual-p13-77-run-id>`，其中值必须逐字来自 run-id file。
4. 任何其他文本、不匹配 run id 或 local-proof 缺失均保持 remote 路径关闭。
  </how-to-verify>
  <resume-signal>输入 `授权 remote handoff <actual-p13-77-run-id>`，或描述 p13-77 local proof 的不匹配。</resume-signal>
  <verify>
    <automated>if (-not (Test-Path -LiteralPath .planning/phases/13-full-chain-data-smoke/13-77-RUN-ID.txt)) { throw 'p13-77 run id is required before the authorization gate' }</automated>
  </verify>
  <done>Plan 13-79 获得唯一、精确、run-id-bound remote authority，或者远程路径在没有副作用的情况下保持暂停。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local terminal evidence to human decision | 仅 p13-77 receipt-backed terminal facts可提出一个 remote mutation 请求。 |
| Human decision to remote handoff | 人类文本只对一个 exact persisted run id 授权一次。 |
| Plan summary to later executor | summary 传递 authorization decision，不存储 session/provider material。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-78-01 | Spoofing | Remote authorization | critical | mitigate | 只接受 literal phrase 与 13-77-RUN-ID.txt 的 exact equality。 |
| T-13-78-02 | Tampering | Eligibility source | high | mitigate | 只从 p13-77 persisted local terminal fields 读取；历史或 untracked artifact 不可替代。 |
| T-13-78-03 | Information disclosure | Authorization record | high | mitigate | 记录 status/code/itemId presence，不记录 cookie、token、headers、endpoint 或 prepared context。 |
| T-13-78-04 | Elevation of privilege | Provider mutation boundary | critical | mitigate | 无 literal authorization 不触及任何 remote command/evidence；13-79 才可调用 fixed handoff。 |
| T-13-78-SC | Tampering | Package installation | low | accept | 本计划不执行 package-manager 安装，沿用既有测试工具。 |
</threat_model>

<verification>
- 使用 focused verifier/local observer tests 确认 terminal/checkpoint semantics 不漂移。
- 确认 remote 资格仅来自一个 p13-77 persisted local proof。
- 在 literal authorization 到达前确认没有 remote evidence read、provider command 或 production navigation。
</verification>

<success_criteria>
- local proof 与 provider authority 被两个独立计划和清晰的人类 gate 分开。
- 授权文本必须逐字绑定 p13-77 run id；泛化 continuation 不拥有 remote 权限。
- 无 local terminal 或无 exact authority 时没有 remote 状态变化。
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-78-SUMMARY.md` when done.
</output>
