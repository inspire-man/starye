---
phase: 13-full-chain-data-smoke
plan: "49"
type: execute
wave: 47
depends_on: []
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-49-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
must_haves:
  truths:
    - "A fresh p13-49 run is allocated only after current-source local projection, live preflight, and two consecutive canonical Gateway readiness checks pass with robots, auth, and authSlash accepted."
    - "The one allocated run has exactly one local handoff, reaches resolved_pending_observation before a repository-owned ordered local Dashboard then Viewer observation, and proves terminal_passed with provesExternalChain true only after those receipts."
    - "A pre-allocation failure produces no p13-49 run id or evidence; a post-allocation gate, handoff, observer, or verifier failure fixes that exact p13-49 run as the sole attempt and stops all downstream actions."
    - "The local evidence remains untracked; closeout commits only the p13-49 run-id when allocated and the honest Summary, and explicitly does not represent remote or production success."
    - "p13-45 and p13-48 retain their immutable pre-allocation stop states; this plan does not remap or execute 13-46 or 13-47."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-49-RUN-ID.txt
      provides: one allocated p13-49 identifier only after every pre-allocation gate passes
    - path: .planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md
      provides: non-secret truthful closeout for either the terminal local proof or the exact stop point
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-49-run-id}/local.attempt
      provides: untracked atomic reservation marker for the one local handoff when a run is allocated
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-49-run-id}/local.json
      provides: untracked tuple-bound local evidence that can advance only through the repository observer
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-49-run-id}/local.md
      provides: untracked deterministic Markdown rendering of the local JSON evidence
  key_links:
    - from: pnpm check:services
      to: http://localhost:8080/robots.txt, /auth, and /auth/
      via: two consecutive starye-gateway-readiness-1 records with robots, auth, and authSlash accepted before run allocation
      pattern: "gateway.*robots.*auth.*authSlash"
    - from: packages/crawler/scripts/data-chain-cli.mjs
      to: scripts/data-chain-handoff.ts
      via: root launcher removes exactly pnpm's one leading delimiter before the fixed local handoff parser receives mode, target, and run id
      pattern: "argv\\[0\\] === '--'"
    - from: local handoff evidence
      to: scripts/data-chain-surface-observation.ts
      via: the exact p13-49 local pair must be resolved_pending_observation with receipt-backed prerequisites before Dashboard then Viewer receipts are appended
      pattern: "observeDataChainSurfaces"
    - from: scripts/data-chain-surface-observation.ts
      to: pnpm smoke:data-chain:verify
      via: the same target/run/code/itemId pair reaches terminal_passed only after the ordered canonical local receipts
      pattern: "provesExternalChain"
---

<objective>
Create one new, local-only, terminal data-chain proof on p13-49 after the current root development tree demonstrated stable canonical Gateway readiness.

Purpose: 13-48 stopped before allocation because its then-live `/auth/` readiness observation was rejected. The current root development tree has since passed `pnpm check:services` three times with all Gateway probes accepted, and the repository's current source plus regression prove that `/auth/` redirect handling is correct. This plan converts that repaired runtime observation into one independently selectable, fail-closed local proof without reopening historical attempts.

Output: one honest `13-49-SUMMARY.md`, an allocated `13-49-RUN-ID.txt` only after all pre-allocation gates pass, and an untracked local evidence pair only when the sole handoff owns the run.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-41-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-44-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-45-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-48-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md
@.planning/debug/phase13-gateway-auth-readiness.md
@packages/crawler/scripts/data-chain-cli.mjs
@packages/config/src/deployment-target/__tests__/data-chain-cli-process.test.ts
@scripts/data-chain-surface-observation.ts
</context>

## Execution Selection

Run this plan alone with `$gsd-execute-phase 13 --gaps-only --wave 47`. Wave 47 has no dependency on 13-45, 13-46, 13-47, or 13-48 output and is the only selection boundary for p13-49. Do not use an unfiltered gaps-only invocation for this local proof.

## Artifacts This Plan Produces

- `13-49-SUMMARY.md` for the terminal local proof or its exact fail-closed stop state.
- `13-49-RUN-ID.txt` containing one `p13-49-<32 lowercase hex>` identifier only after Task 1 completes every pre-allocation gate.
- Untracked `local.attempt`, `local.json`, and `local.md` below `evidence/starye-org/<p13-49-run-id>/` only after the one permitted local handoff.

## Non-Goals

- Do not retry, add a completion Summary to, or synthesize evidence for p13-45; its `invalid_target` parser stop remains before evidence allocation.
- Do not retry or allocate a p13-48 run; its Gateway-readiness stop remains before evidence allocation.
- Do not execute remote mode, provider/D1/API/Wrangler commands, production URLs, production Browser observation, deployment, migration, 13-46, or 13-47.
- Do not modify source, tests, STATE.md, ROADMAP.md, 13-VERIFICATION.md, existing plans, existing summaries, historical evidence, or unrelated dirty files.

## Source Coverage Audit

| Source | ID | Required item | Coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 13 gap closure | Current-source local Gateway-to-viewer proof after the 13-48 pre-allocation stop | Tasks 1-2 establish the local prerequisite, one handoff, ordered surfaces, and exact terminal verifier | COVERED |
| REQ | DATA-01 | Local API/auth/dashboard/content via Gateway | Task 1 requires two accepted 8080 readiness records; Task 2 observes Dashboard and Viewer only through the local Gateway | COVERED |
| REQ | DATA-02 | Local D1 schema and minimal data before ingest | Task 1 requires `project-local --check`, local live smoke preflight, and receipt-backed local D1 readiness before handoff | COVERED |
| REQ | DATA-03 | One selected-target deterministic fixture with identity | Task 1 permits exactly one local handoff for one p13-49 tuple and requires its non-empty local itemId | COVERED |
| REQ | DATA-04 | Same item in D1, API, and admin state | Task 1 requires receipt-backed D1/API runner surfaces; Task 2 validates the exact tuple through Dashboard | COVERED |
| REQ | DATA-05 | Dashboard validation | Task 2 records the signed-in local Gateway Dashboard before Viewer | COVERED |
| REQ | DATA-06 | Canonical viewer validation | Task 2 uses only `http://localhost:8080/movie/<item-code>` after Dashboard | COVERED |
| REQ | DATA-07 | Repeatable non-secret evidence | Both tasks retain allowlisted untracked evidence, run exact verification, and commit only run-id when present plus Summary | COVERED |
| REQ | TEST-05 | Repeatable smoke-script output | Task 1 reruns the root-launcher process regression and requires the exact pending verifier before observation; Task 2 requires the exact terminal verifier | COVERED |
| RESEARCH | Local-first, one-item, Gateway-only, redacted evidence | Current root lifecycle, closed local handoff, repository observer, and evidence writer are reused without a new crawler/API path | COVERED |
| CONTEXT | D-01, D-02 | One deterministic non-R18 fixture through the bounded adapter | Task 1 invokes the closed local handoff exactly once and rejects a replacement identifier | COVERED |
| CONTEXT | D-03, D-04 | Local readiness then Gateway-correlated Dashboard/Viewer tuple | Tasks 1-2 enforce local-first ordering, canonical Gateway routes, and target/run/code/itemId correlation | COVERED |
| CONTEXT | D-05 | Provider work remains explicit-target and preflight-controlled | This plan invokes no provider-side command, preserving D-05's trust boundary without representing local proof as provider proof | COVERED CONSTRAINT |
| CONTEXT | D-06, D-07 | Redacted evidence and Phase 13 evidence ownership | Tasks 1-2 retain untracked allowlisted evidence and state the local-only result in the Summary | COVERED |

<tasks>

<task type="auto">
  <name>Task 1: Gate current local source and allocate exactly one p13-49 local handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-49-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md
    - .planning/debug/phase13-gateway-auth-readiness.md
    - .planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
    - packages/crawler/scripts/data-chain-cli.mjs
    - packages/config/src/deployment-target/__tests__/data-chain-cli-process.test.ts
  </read_first>
  <action>
    Apply D-01: accept one deterministic target/run-derived non-R18 item with one player. Apply D-02: use only the existing closed local handoff rather than a full crawler corpus. Apply D-03: complete local readiness before ingest and treat `http://localhost:8080` as the only canonical browser evidence origin. Apply D-04: preserve one local target/run/code/itemId tuple through runner receipts and later browser receipts. Apply D-05: exclude provider-side commands from this local plan. Apply D-06: retain only allowlisted non-secret evidence facts. Apply D-07: keep the resulting local data-chain evidence in Phase 13 without rewriting Phase 14 documentation owners.

    First make the one-attempt boundary explicit. Confirm `13-49-RUN-ID.txt` does not exist and no `evidence/starye-org/p13-49-*` directory exists. If either exists, do not create, replace, or reuse an identifier; write an honest 13-49 Summary naming the pre-existing state and stop. Confirm `ebc500b` is an ancestor of the checked-out source, then run `pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-cli-process.test.ts`; require the root delimiter regression to pass before a live local command.

    Use the stable current root development tree when it serves the checked-out current source; otherwise start a current-source root tree with the repository local lifecycle. Do not stop or alter an existing unrelated PID. Before allocation, require `pnpm target-profile project-local --target starye-org --check`, `pnpm target-profile preflight --target starye-org --scope local --command smoke --live --wrangler-profile starye-org`, and two consecutive fresh `pnpm check:services` executions to pass. Each readiness record must use schema `starye-gateway-readiness-1` and report `robots`, `auth`, and `authSlash` as `accepted` for the canonical Gateway paths `/robots.txt`, `/auth`, and `/auth/` under `http://localhost:8080`; a direct implementation port may establish process ownership but is not a readiness or browser-evidence URL.

    If any gate fails before allocation, write 13-49-SUMMARY.md with the command, non-secret gate result, current-source lifecycle state, and `stopped-before-allocation`; do not create 13-49-RUN-ID.txt, an evidence directory, attempt marker, handoff, verifier, or observer. Keep the Summary as the only closeout artifact and stop all remaining tasks.

    Only after every preceding gate passes, generate one identifier using `p13-49-` plus 32 lowercase hexadecimal characters, write it once to 13-49-RUN-ID.txt, and require the exact `evidence/starye-org/<run-id>` directory to be absent immediately before handoff. Invoke exactly once: `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <run-id>`. Require exit 0, `outcome: pending`, `handoffReady: true`, `runnerInvocations: 1`, one deterministic item code, and a non-empty local itemId. Run the exact local verifier once before observation and require its pending result, `resolved_pending_observation`, and `provesExternalChain: false` for the same target/run/code/itemId tuple. A failure after allocation fixes that exact p13-49 identifier: write an honest Summary, do not allocate a replacement, do not invoke handoff again, and do not continue to Task 2.
  </action>
  <acceptance_criteria>
    - Before allocation, two consecutive `pnpm check:services` runs report all three canonical Gateway probes accepted; the task does not treat a direct port as evidence.
    - A pre-allocation failure leaves no 13-49 run-id file, evidence tree, marker, handoff, verifier, or observer invocation and writes an honest Summary.
    - A successful allocation writes exactly one `p13-49-<32 lowercase hex>` value, finds its evidence tree absent before handoff, and invokes the local handoff once.
    - The sole handoff produces a non-empty local itemId and the pre-observation exact verifier reports the pending lifecycle state for the same tuple.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/data-chain-cli-process.test.ts</automated>
    <automated>pnpm target-profile project-local --target starye-org --check</automated>
    <automated>pnpm target-profile preflight --target starye-org --scope local --command smoke --live --wrangler-profile starye-org</automated>
    <automated>pnpm check:services</automated>
    <automated>pnpm check:services</automated>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-49-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>Either an honest pre-allocation stop Summary exists with no p13-49 run or exactly one immutable p13-49 pending local pair exists for Task 2; neither branch authorizes a replacement attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the one pending p13-49 pair and close out only local terminal proof</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-49-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-41-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-48-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
  </read_first>
  <action>
    Run this task only when Task 1 allocated one p13-49 run and its exact local verifier reported the pending lifecycle state. Apply D-03: observe canonical local Gateway routes only. Apply D-04: use that same local target/run/code/itemId tuple, never an old p13-45, p13-48, or other run. Apply D-06: retain only non-secret target/run/tuple/surface/status/hash facts. Apply D-07: record the real local proof in Phase 13 without widening its claim.

    Run the repository-owned observer once with `pnpm exec tsx scripts/data-chain-surface-observation.ts --mode local --target starye-org --run-id <p13-49-run-id>`. The observer must consume the unobserved pending pair and navigate the signed-in local Gateway Dashboard `/dashboard/movies` before the matching Gateway Viewer `/movie/<item-code>` at `http://localhost:8080`; do not use an ad hoc browser script, remote mode, production host, direct implementation port, or alternate evidence root. It must retain the exact target/run/code/local-itemId tuple while it appends the repository-owned receipts.

    Run the exact local verifier for the same run once after the observer. Require exit 0, `outcome: terminal_passed`, `ingestState: resolved`, `aggregate: passed`, and `provesExternalChain: true`. If the observer or verifier reports an error, checkpoint, mismatched tuple, or non-terminal outcome, preserve the current p13-49 run as the only attempt, write the observed non-success in 13-49-SUMMARY.md, and stop without another observer call, another handoff, or another run id.

    For a terminal result, write 13-49-SUMMARY.md with the current-source lifecycle choice, both `check:services` results, pre/post verifier exits, one handoff count, Dashboard-to-Viewer order, the exact local tuple, evidence byte counts and SHA-256 values, and a statement that this is local proof only. State that remote handoff, provider/D1/API/Wrangler activity, production Browser/URL checks, and 13-46/13-47 execution were not performed and that p13-45 and p13-48 remain unchanged stop records. Confirm local.attempt, local.json, and local.md are untracked and unstaged. Run GitNexus detect-changes before closeout; stage and commit only 13-49-RUN-ID.txt when allocated and 13-49-SUMMARY.md. Never stage the evidence pair or any pre-existing dirty file.
  </action>
  <acceptance_criteria>
    - Dashboard is observed before Viewer through `http://localhost:8080`, and both receipts carry the same p13-49 target/run/code/local-itemId tuple.
    - The post-observation exact verifier reaches terminal_passed with provesExternalChain true only for the sole local p13-49 pair.
    - An observer or verifier failure leaves the current p13-49 identifier fixed and records an honest Summary without a second observer, handoff, or identifier.
    - `local.attempt`, `local.json`, and `local.md` remain untracked; the closeout commit contains only the p13-49 run-id when allocated and 13-49 Summary.
    - The Summary explicitly distinguishes local terminal proof from remote or production success and leaves p13-45, p13-48, 13-46, and 13-47 unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-49-RUN-ID.txt -Raw).Trim()</automated>
    <automated>npx --no-install gitnexus detect-changes --repo starye --scope all</automated>
    <automated>powershell -NoProfile -Command "$run = (Get-Content '.planning/phases/13-full-chain-data-smoke/13-49-RUN-ID.txt' -Raw).Trim(); $paths = @('.planning/phases/13-full-chain-data-smoke/evidence/starye-org/' + $run + '/local.attempt', '.planning/phases/13-full-chain-data-smoke/evidence/starye-org/' + $run + '/local.json', '.planning/phases/13-full-chain-data-smoke/evidence/starye-org/' + $run + '/local.md'); foreach ($path in $paths) { git ls-files --error-unmatch -- $path 2>$null; if ($LASTEXITCODE -eq 0) { exit 1 } }; exit 0"</automated>
  </verify>
  <done>The single p13-49 run either has terminal local evidence after the required ordered Gateway receipts or has one truthful terminal stop Summary; neither outcome represents remote or production success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local operator to root data-chain launcher | Caller supplies the explicit local target and newly allocated run id to the closed launcher. |
| root development tree to Gateway readiness | A current-source runtime must prove the three canonical 8080 probes before it can create smoke data. |
| local handoff to evidence storage | The handoff owns one exact target/run directory and atomically creates the mode marker before runner work. |
| local Browser profile to Gateway Dashboard | The repository observer crosses the authenticated Dashboard guard and persists only tuple-bound receipts. |
| local proof to external-chain claims | A terminal local pair is a prerequisite receipt and cannot establish provider or production success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-49-01 | Tampering | run allocation and local evidence reservation | high | mitigate | Require absent run-id/evidence state, allocate once after all gates, handoff once, retain the atomic marker, and stop the exact run on every post-allocation non-success. |
| T-13-49-02 | Spoofing | Gateway readiness and browser receipts | high | mitigate | Require two current-source `check:services` records with three accepted 8080 probes, then use only the repository observer and exact tuple verifier. |
| T-13-49-03 | Information disclosure | evidence and Summary | high | mitigate | Persist only target/run/tuple/status/path/hash facts, keep evidence untracked, and omit cookies, headers, tokens, raw prepared context, and remote endpoints. |
| T-13-49-04 | Repudiation | failed pre-allocation or observer branch | medium | mitigate | Require an honest Summary for every terminal branch and prohibit replacement identifiers, repeated handoff, and repeated observer execution. |
| T-13-49-05 | Elevation of privilege | provider and production operations | high | mitigate | The command set is local-only; no remote mode, provider/D1/API/Wrangler command, production Browser, deployment, or migration appears in either task. |
| T-13-49-06 | Denial of service | existing local development tree | low | accept | Reuse a healthy current-source tree when available and do not stop unrelated PIDs; readiness bounds are owned by the existing service command. |
| T-13-49-SC | Tampering | package execution | medium | mitigate | No package installation is permitted; use the committed launcher regression and current lockfile before the live local sequence. |
</threat_model>

<verification>
Before allocation, run the focused root-launcher process regression, explicit project-local check, local live preflight, and two consecutive canonical service checks. For an allocated p13-49 run, require the pending exact verifier, one repository observer, then the terminal exact verifier. Confirm the evidence pair is untracked and use GitNexus detect-changes before staging the narrowly allowed closeout files.
</verification>

<success_criteria>
- `$gsd-execute-phase 13 --gaps-only --wave 47` selects this plan without selecting historical stopped attempts or remote plans.
- No p13-49 identifier or evidence exists unless all pre-allocation gates pass, including two accepted Gateway readiness records.
- An allocated p13-49 run has exactly one local handoff and progresses from pending to terminal_passed only through ordered local Dashboard then Viewer receipts at `http://localhost:8080`.
- Any failure is truthful and terminal for its branch: pre-allocation leaves no run/evidence, while post-allocation leaves the sole run fixed with no retry or replacement.
- Evidence stays untracked and closeout commits only 13-49 run-id when allocated plus Summary; this local proof is never reported as remote or production success.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-49-SUMMARY.md` when the selected local run reaches terminal proof or a required gate stops it before or after allocation.
</output>
