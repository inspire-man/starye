---
phase: 13-full-chain-data-smoke
plan: "73"
type: execute
wave: 67
depends_on: [13-72]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-73-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Root in-app Browser local Dashboard session
    why: A new carrier requires the root-owned canonical local Movies surface and the Plan 13-72 root-IAB observer port to be live in the same browser context.
    dashboard_config:
      - task: Sign in interactively only when the canonical local Dashboard is not already authenticated; keep all session material inside the Browser.
must_haves:
  truths:
    - "A p13-73 identifier is allocated only after the same root IAB context passes the canonical local Dashboard signed-session probe and owns the receipt callback, per D-03, D-05, and D-06."
    - "One repository-owned local handoff creates exactly one deterministic non-R18 one-player tuple with a non-empty local itemId, per D-01, D-02, and D-04."
    - "Root IAB observes the exact fresh tuple in local Dashboard then Viewer order; only receipt-backed terminal_passed with provesExternalChain true can make remote authorization eligible, per D-03, D-04, D-06, and D-07."
    - "Any first local non-success freezes p13-73 and prohibits Viewer continuation, standalone local verification, another handoff, remote preflight, remote handoff, remote verification, and production navigation."
    - "p13-69 and every older carrier/evidence tree remain immutable inputs to diagnosis only and are never read as current execution evidence, retried, verified, or modified."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-73-RUN-ID.txt
      provides: The sole fresh p13-73 identifier, created after root-IAB ownership and two canonical local readiness records pass.
    - path: .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
      provides: Non-secret root-IAB gate, local handoff, ordered observation, verifier branch, and first-checkpoint facts.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-73-run-id}/local.json
      provides: Untracked local one-item evidence correlated by target, run, code, and mode-local itemId.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-73-run-id}/local.md
      provides: Untracked canonical Markdown rendering of the same local evidence.
  key_links:
    - from: Plan 13-72 root-IAB adapter probe
      to: p13-73 allocation
      via: The root-owned canonical Dashboard readiness result and root callback must both be available before the run-id file may exist.
      pattern: root_iab and ready
    - from: repository local handoff
      to: root IAB observation core
      via: A pending pair has runnerInvocations one, the target/run-derived code, and a non-empty local itemId.
      pattern: resolved_pending_observation
    - from: ordered root-IAB receipts and exact verifier
      to: Plan 13-74 eligibility
      via: Only terminal_passed plus provesExternalChain true permits the exact remote authorization checkpoint.
      pattern: terminal_passed and provesExternalChain true
---

<objective>
Create one fresh local carrier only after Plan 13-72 proves that the root IAB session and receipt observer share ownership, then retain either a terminal canonical Dashboard-to-Viewer proof or the first immutable p13-73 checkpoint.

Purpose: p13-69 is frozen at its first Dashboard checkpoint and cannot receive another browser action. The next carrier must establish both the local signed session and the actual receipt-producing observer before it writes any identifier or fixture, rather than treating a visible root tab as evidence for a separate browser profile.

Output: Conditional `13-73-RUN-ID.txt`, untracked p13-73 local JSON/Markdown evidence, and `13-73-SUMMARY.md`. This plan has no remote, provider, production-browser, or Phase 14 operation.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-72-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
@packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
</context>

<interfaces>
- `verifyRootIabObservationReadiness` proves root-owned canonical Dashboard availability without loading evidence, allocating a run id, or exposing session material.
- `runDataChainHandoffCore` remains the only owner of local reservation, one-item fixture execution, and pending-pair validation; it accepts only explicit mode, target, and run id.
- `observeDataChainSurfaces` receives the Plan 13-72 root-IAB port in memory, derives the exact tuple from pending evidence, and persists Dashboard before Viewer receipts.
- `inspectDataChainSmokeVerification` runs once only after both root-IAB observations pass; it never starts a runner or repairs a checkpoint.
</interfaces>

## Artifacts This Phase Produces

| Artifact | Owner | Produced state |
| --- | --- | --- |
| `13-73-RUN-ID.txt` | Phase 13 | One identifier after root-IAB ownership and local Gateway readiness pass. |
| `13-73-SUMMARY.md` | Phase 13 | Non-secret qualification, handoff, observation, verifier, and freeze outcome. |
| `evidence/starye-org/{p13-73-run-id}/local.json` and `local.md` | Untracked execution evidence | One tuple-bound local terminal pair or first immutable checkpoint. |

<tasks>

<task type="auto">
  <name>Task 1: Qualify the root IAB session and observation owner before allocation</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-72-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>
The root agent claims the existing IAB and supplies the Plan 13-72 `RootIabSurfaceObserver` from that same browser context. Invoke `verifyRootIabObservationReadiness` for local `starye-org`; its probe must inspect only `http://localhost:8080/dashboard/movies` and establish the canonical route, authenticated state, and Movies collection surface. Record only route class, ready/unavailable category, and root ownership in `13-73-SUMMARY.md`. Keep the adapter in memory for Task 3 and retain no cookie, storage, token, header, profile, or DOM dump.

Before any allocation, confirm `13-73-RUN-ID.txt` and every `evidence/starye-org/p13-73-*` directory are absent. A failed root probe or unavailable same-owner adapter is a pre-allocation stop: leave the run-id file absent and end this plan without handoff, observation, verifier, remote action, or production route. This task implements D-03, D-05, and D-06.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Root session readiness and the callback that will write receipts are proved from the same IAB context before a p13-73 identifier exists.
    - The summary carries only non-secret readiness facts.
    - A non-ready probe has no p13-73 evidence directory and no downstream command.
  </acceptance_criteria>
  <done>The fresh carrier is eligible for allocation only with a callable same-owner root-IAB adapter; otherwise execution ends before allocation.</done>
</task>

<task type="auto">
  <name>Task 2: Reserve one local pending tuple through the repository handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-73-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  </read_first>
  <action>
Proceed only after Task 1 records a ready root-IAB probe and retained same-owner adapter. Run `pnpm check:services` twice and require both structured records to accept Gateway plus `robots`, `auth`, and `authSlash`. Allocate exactly one `p13-73-<32 lowercase hex>` identifier and write it once. Invoke only `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <actual-p13-73-run-id>`.

Accept a pending pair only when it has one deterministic non-R18, one-player item, `runnerInvocations: 1`, `handoffReady: true`, and a non-empty local itemId. The handoff core owns its internal pending-pair inspection, so this task issues no standalone local verifier. Any reservation, readiness, handoff, tuple, or internal inspection non-success permanently freezes p13-73: record its non-secret category and stop without another handoff, observer, local verifier, remote action, or production navigation. This task implements D-01, D-02, D-03, D-04, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one run id and one repository-owned local handoff exist only after root-IAB qualification and dual canonical readiness records.
    - The pending tuple has the exact target/run-derived code, one fixture invocation, and a non-empty local itemId.
    - A first non-success has no replacement local or remote action.
  </acceptance_criteria>
  <done>p13-73 has one unobserved local pending tuple or one permanent local checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Observe p13-73 through the qualified root IAB and run the terminal verifier branch</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>
Only the root agent holding the Task 1 adapter may call `observeDataChainSurfaces` programmatically with that in-memory port. The root IAB must first navigate the canonical local Dashboard Movies route and confirm the exact p13-73 code plus local itemId in the row, then navigate `http://localhost:8080/movie/<p13-73-item-code>` and confirm the identical tuple through allowed non-secret metadata. The repository core retains route derivation, receipt construction, evidence writes, and Dashboard-before-Viewer validation.

When observation returns checkpoint exit 2, record the first result and stop without a local verifier, another observer, another handoff, remote preflight, remote handoff, remote verifier, or production navigation. Only when both receipts pass may the root execute `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id <actual-p13-73-run-id>` once. Plan 13-74 is eligible only for `terminal_passed` with `provesExternalChain: true`; every other verifier result freezes p13-73. This task implements D-03, D-04, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The actual root-IAB Dashboard receipt precedes the Viewer receipt, and both contain the exact p13-73 code/itemId tuple.
    - A checkpoint stops before the external verifier; a passed pair permits exactly one local verifier.
    - Terminal local proof has receipt-backed `terminal_passed` and `provesExternalChain: true`, while all other outcomes are frozen.
  </acceptance_criteria>
  <done>p13-73 becomes a fresh terminal local carrier for the separate remote authorization gate or remains an immutable local checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Root IAB to local observer port | A real authenticated Browser yields only canonical tuple/status facts through the Plan 13-72 port. |
| Local handoff to D1/API/Gateway | A single deterministic fixture crosses the controlled local stateful chain. |
| Local terminal evidence to remote eligibility | Only exact receipt-backed terminal local proof may reach the distinct human authorization plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-73-01 | Spoofing | Root IAB readiness | high | mitigate | Require the same root port to pass a canonical Dashboard probe before allocation and to supply later receipts. |
| T-13-73-02 | Tampering | Run reservation and local evidence | high | mitigate | Check fresh paths, use the fixed-root handoff, enforce exact tuple checks, and freeze at the first non-success. |
| T-13-73-03 | Information disclosure | Browser/evidence boundary | high | mitigate | Retain only route/status/code/itemId facts; keep all session material inside the root Browser. |
| T-13-73-04 | Elevation of privilege | Dashboard observation | medium | mitigate | Preserve the actual Dashboard session and role guard; classify unavailable or tuple mismatch as a checkpoint. |
| T-13-73-05 | Denial of service | Fixture execution | medium | mitigate | Enforce D-01/D-02 through one deterministic item, one handoff, and no retry. |
</threat_model>

<verification>
- Confirm Task 1 has no p13-73 run id or evidence before its same-owner root-IAB probe succeeds.
- Run focused local handoff, root observer, and exact-verifier tests before relying on the live local branch.
- Confirm the summary contains no session/provider material and that every first non-success branch has no follow-up carrier or remote operation.
</verification>

<success_criteria>
- A p13-73 run is allocated only after shared root-IAB session and observer ownership is established.
- Its one-item local flow either has receipt-backed Dashboard-to-Viewer terminal proof or one immutable checkpoint.
- No historical carrier, direct app port, provider operation, or local result-as-production claim enters the new local branch.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md` when done.
</output>
