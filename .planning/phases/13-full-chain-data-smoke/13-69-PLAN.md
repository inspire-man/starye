---
phase: 13-full-chain-data-smoke
plan: "69"
type: execute
wave: 63
depends_on: [13-68]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-69-RUN-ID.txt
  - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Root in-app Browser local Dashboard session
    why: A new carrier may be allocated only after the root IAB shows the authenticated local Movies surface on the canonical Gateway route.
    dashboard_config:
      - task: Sign in interactively in the root IAB when its existing local Dashboard session is absent; keep session material inside the browser.
must_haves:
  truths:
    - "A root-owned IAB check proves an authenticated local Movies surface on http://localhost:8080/dashboard/movies before any p13-69 identifier is allocated, per D-03 and D-06."
    - "Exactly one new p13-69-<32 lowercase hex> local carrier writes one deterministic non-R18, one-player fixture with one non-empty local itemId through the repository-owned handoff, per D-01 and D-02."
    - "The root IAB observes the new tuple only in canonical local Dashboard then Viewer order; terminal local proof requires the exact code/itemId pair plus terminal_passed and provesExternalChain true, per D-03, D-04, D-06, and D-07."
    - "Any first local non-success permanently freezes p13-69 before remote work; p13-66, p13-55, p13-57, p13-60, p13-63, and all earlier carrier/evidence trees remain unobserved, unverified, unretried, and unmodified."
    - "No cookie, profile, token, storage, raw provider response, direct app port, or production operation is used or recorded by this local-carrier plan, per D-05 and D-06."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-69-RUN-ID.txt
      provides: The sole new local carrier identifier, created only after the root IAB gate and two accepted Gateway readiness records.
    - path: .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
      provides: Non-secret local session-gate, handoff, ordered-observation, verifier-branch, and frozen-checkpoint facts.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-69-run-id}/local.json
      provides: Untracked, tuple-bound one-item local evidence with code and mode-local itemId.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-69-run-id}/local.md
      provides: Untracked Markdown rendering that must match the local JSON evidence exactly.
  key_links:
    - from: root IAB authenticated local Dashboard gate
      to: p13-69 run allocation
      via: Canonical Movies surface is observed before the run-id file and local evidence directory can exist.
      pattern: root_iab_local_ready
    - from: repository-owned local handoff
      to: root IAB observation core
      via: A pending one-item tuple has runnerInvocations 1, the exact target/run-derived code, and a non-empty local itemId.
      pattern: resolved_pending_observation
    - from: ordered local Dashboard and Viewer receipts
      to: remote eligibility
      via: The post-observation verifier is invoked once only after both observations pass and reports terminal_passed with provesExternalChain true.
      pattern: terminal_passed and provesExternalChain true
---

<objective>
Create one fresh local terminal carrier from a root IAB authenticated Dashboard gate, retaining either a full canonical local Dashboard-to-Viewer proof or the first immutable p13-69 checkpoint.

Purpose: `13-VERIFICATION.md` proves that p13-66 stopped permanently at its first production Viewer checkpoint. Its local success and any earlier carrier are evidence history, not reusable work. The remaining live gap requires a new carrier that independently re-establishes the local-first contract before any remote authorization can be requested.

Output: Conditional `13-69-RUN-ID.txt`, untracked p13-69 local JSON/Markdown evidence, and `13-69-SUMMARY.md`. No remote preflight, handoff, remote verifier, production browser route, or Phase 14 artifact is operated by this plan.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-66-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-67-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-67-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-68-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
@scripts/data-chain-handoff.ts
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
@packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
</context>

<interfaces>
- `runDataChainHandoffCli` accepts only `--mode`, `--target`, and `--run-id`; its core atomically reserves the exact fixed-root evidence pair and reports a pending tuple only when `runnerInvocations` is one and the mode-local itemId is non-empty.
- `observeDataChainSurfaces` derives the item tuple from pending evidence, resolves local mode to `http://localhost:8080`, writes Dashboard before Viewer receipts, and returns exit 0 only for a terminal pair or exit 2 for a checkpoint.
- `inspectDataChainSmokeVerification` reads the persisted pair without starting a runner; it returns terminal proof only for receipt-backed resolved/passed evidence.
</interfaces>

## Artifacts This Phase Produces

| Artifact | Owner | Produced state |
| --- | --- | --- |
| `13-69-RUN-ID.txt` | Phase 13 tracked planning artifact | One p13-69 identifier after the root IAB and Gateway gates pass. |
| `13-69-SUMMARY.md` | Phase 13 tracked planning artifact | Local readiness, single handoff, observation branch, and immutable outcome without sensitive data. |
| `evidence/starye-org/{p13-69-run-id}/local.json` and `local.md` | Untracked execution evidence | One code/itemId-correlated local evidence pair, either terminal or frozen checkpoint. |

## Source Coverage Audit

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 13 | Fresh local-to-selected-production canonical data-chain proof | 13-69, 13-70, 13-71 | COVERED | Starts with the required fresh local carrier and ends only on a terminal remote Viewer receipt. |
| REQ | DATA-01 | Local canonical Gateway routes | 13-69 | COVERED | Local Dashboard and Viewer use only `http://localhost:8080`. |
| REQ | DATA-02 | Local schema/minimal-data readiness | 13-69 | COVERED | Repository handoff runs only after local readiness gates. |
| REQ | DATA-03 | One deterministic targeted fixture | 13-69, 13-70 | COVERED | One local and, after authorization, one remote repository-owned handoff. |
| REQ | DATA-04 | D1/API/admin tuple correlation | 13-70, 13-71 | COVERED | Remote pending evidence must have a non-empty itemId before Dashboard observation. |
| REQ | DATA-05 | Authenticated Dashboard validation | 13-69, 13-71 | COVERED | Root IAB checks the Movies surface before each corresponding Viewer step. |
| REQ | DATA-06 | Canonical Viewer validation | 13-69, 13-71 | COVERED | Dashboard-first Viewer branch is terminal only with exact tuple correlation. |
| REQ | DATA-07 | Non-secret local and production evidence | 13-69, 13-70, 13-71 | COVERED | Fixed evidence paths, redacted summaries, and frozen non-success branches. |
| REQ | TEST-05 | Repeatable terminal/checkpoint smoke output | 13-69, 13-70, 13-71 | COVERED | Existing handoff, surface, evidence, and verifier contracts are re-run as focused tests. |
| RESEARCH | R-01 | Existing closed handoff, target, receipt, and Gateway-first seams only | 13-69, 13-70, 13-71 | COVERED | No new package, profile parser, direct DB client, browser bypass, or remote command surface. |
| RESEARCH | R-02 | First checkpoint blocks downstream mutation and browser work for that carrier | 13-69, 13-70, 13-71 | COVERED | Each plan has a one-carrier freeze branch. |
| CONTEXT | D-01 | One deterministic non-R18 item with one player | 13-69, 13-70 | COVERED | Every handoff remains count-one and target/run-derived. |
| CONTEXT | D-02 | Only controlled fixture or targeted crawler adapter | 13-69, 13-70 | COVERED | Uses the repository-owned handoff rather than broad crawl or raw transport. |
| CONTEXT | D-03 | Local-first and canonical Gateway rule | 13-69 | COVERED | The local branch precedes all remote authorization. |
| CONTEXT | D-04 | Exact target/run/code plus mode-owned itemId | 13-69, 13-70, 13-71 | COVERED | Each observation and remote eligibility check uses the exact tuple. |
| CONTEXT | D-05 | Explicit target, preflight, and provider fail-closed boundary | 13-70, 13-71 | COVERED | The only nested preflight is inside the one authorized remote handoff. |
| CONTEXT | D-06 | Allowlisted non-secret evidence | 13-69, 13-70, 13-71 | COVERED | Browser adapters return tuple/status facts only. |
| CONTEXT | D-07 | Real execution remains Phase 13; Phase 14 stays deferred | 13-71 | COVERED | Verification is refreshed only from Phase 13 fresh-carrier facts. |

<tasks>
<task type="auto">
  <name>Task 1: Root-gate a new local carrier on the authenticated Dashboard surface</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-66-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-68-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
    - scripts/data-chain-surface-observation.ts
  </read_first>
  <action>
The root agent claims the existing application IAB and inspects only `http://localhost:8080/dashboard/movies`. Before any allocation, record in `13-69-SUMMARY.md` only the canonical-route class, signed-in result, and Movies-surface availability. Do not read, print, copy, export, persist, or transfer cookies, browser profile files, storage, headers, tokens, or session values. Treat missing authenticated Movies-surface proof as a pre-allocation checkpoint: leave `13-69-RUN-ID.txt` absent and stop. A child binding limitation does not establish a root-IAB outage. This task implements D-03, D-05, and D-06 and applies the UI session boundary.
  </action>
  <verify>
    <automated>if (-not (Test-Path -LiteralPath .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md)) { throw 'p13-69 root-IAB gate summary is required' }</automated>
  </verify>
  <acceptance_criteria>
    - The readiness record contains only non-secret route and surface categories.
    - A non-ready root IAB leaves no p13-69 identifier or evidence directory.
    - No frozen carrier, remote operation, production route, or default observer is touched.
  </acceptance_criteria>
  <done>The root-owned local Dashboard gate is proved for a new carrier or p13-69 stops before allocation.</done>
</task>

<task type="auto">
  <name>Task 2: Reserve exactly one local pending tuple through the existing handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-69-RUN-ID.txt, .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
  </read_first>
  <action>
Proceed only after Task 1 has recorded root IAB readiness. Confirm that `13-69-RUN-ID.txt` and every `evidence/starye-org/p13-69-*` path are absent. Run `pnpm check:services` twice and require both structured records to accept Gateway plus `robots`, `auth`, and `authSlash`. Allocate one `p13-69-<32 lowercase hex>` identifier and write it once. Invoke only `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id <actual-p13-69-run-id>`. Accept the handoff result only when it reports one deterministic non-R18 one-player item, `runnerInvocations: 1`, `handoffReady: true`, and a non-empty local itemId. The handoff core owns its pending-pair inspection; do not start a standalone pre-observation verifier. Any first non-pending result is p13-69's terminal checkpoint, recorded without another handoff, verifier, observer, remote command, or allocation. This task implements D-01, D-02, D-03, D-04, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - One new run exists only after the root IAB and both canonical Gateway readiness records pass.
    - Exactly one local handoff owns the p13-69 reservation and produces a pending tuple with a non-empty itemId, or freezes the carrier at its first non-success.
    - The plan has produced no remote evidence, provider call, or production assertion.
  </acceptance_criteria>
  <done>p13-69 has one unobserved local pending pair or a permanent local checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Root-observe the p13-69 local Dashboard then Viewer branch</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-local.test.ts
    - packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-UI-SPEC.md
  </read_first>
  <action>
Only the root agent with the authenticated local IAB may invoke `observeDataChainSurfaces` with an in-memory root-IAB `observeSurface` adapter. The adapter returns only actual navigation `status`, `itemCode`, and `itemId`; the repository core retains tuple derivation, receipt creation, Dashboard-before-Viewer order, validation, and evidence writes. Observe `http://localhost:8080/dashboard/movies` first and confirm the exact p13-69 code plus local itemId in the Dashboard row, then observe `http://localhost:8080/movie/<p13-69-item-code>` and confirm the same code/itemId through allowed non-secret metadata. Never invoke the default observer or inspect session material. If the observation core returns checkpoint exit 2, record the first checkpoint and stop without any local verifier, replacement observer, handoff, remote operation, or production navigation. Only if both receipts pass may the root run `pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id <actual-p13-69-run-id>` exactly once. It unlocks Plan 13-70 only for `terminal_passed` with `provesExternalChain: true`; every other result freezes p13-69. This task implements D-03, D-04, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard is the only first local browser receipt; Viewer follows only after the exact Dashboard tuple passes.
    - Terminal local proof has the exact code/itemId tuple and both terminal verifier fields; a checkpoint has no follow-up action.
    - The summary and evidence contain no sensitive browser or provider material and no direct application-port canonical URL.
  </acceptance_criteria>
  <done>p13-69 is a fresh terminal local carrier for the independent remote-authorization gate, or it is permanently frozen.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Root IAB to local Dashboard | A real signed browser session contributes only non-sensitive tuple/status observations. |
| Local handoff to D1/API/Gateway | The fixed one-item fixture crosses into stateful local data surfaces. |
| Local evidence to remote eligibility | A terminal local record decides whether a separate human authorization can ever be requested. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-69-01 | Spoofing | Root IAB session gate | high | mitigate | Require a root-owned authenticated Movies surface on the canonical local route before run allocation. |
| T-13-69-02 | Tampering | Run reservation and local evidence | high | mitigate | Require absent p13-69 evidence before allocation, the fixed-root handoff marker, exact tuple validation, and a permanent first-checkpoint freeze. |
| T-13-69-03 | Information disclosure | Browser and evidence boundary | high | mitigate | Allow the adapter and summary to retain only route class, status, code, itemId presence, and verifier category. |
| T-13-69-04 | Elevation of privilege | Dashboard observation | medium | mitigate | Preserve the real Dashboard session/role guard and treat auth or tuple failure as a checkpoint. |
| T-13-69-05 | Denial of service | Fixture execution | medium | mitigate | Enforce D-01 and D-02 through one deterministic non-R18 item, one local handoff, and no retry. |
</threat_model>

<verification>
- Confirm the p13-69 run-id file is absent for a root-IAB readiness checkpoint and has the required p13-69 format only after allocation.
- Use the existing focused handoff, local observation, and evidence-verifier tests to confirm the fixed reservation, Dashboard-first ordering, terminal, and checkpoint contracts.
- Confirm execution output names only p13-69 and contains no remote/provider action, historical-carrier operation, session material, or direct-port canonical evidence.
</verification>

<success_criteria>
- A new local carrier is created only from the root IAB authenticated Dashboard gate and canonical Gateway readiness.
- It has either a receipt-backed local Dashboard-to-Viewer terminal pair with `provesExternalChain: true` or one immutable checkpoint that blocks remote work.
- p13-66 and every historical evidence tree remain immutable inputs, never execution targets.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md` when done.
</output>
