---
phase: 13-full-chain-data-smoke
plan: "74"
type: execute
wave: 68
depends_on: [13-73]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-74-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-07
  - TEST-05
user_setup:
  - service: Selected-production remote handoff authorization
    why: The sole provider-side mutation requires a literal authorization tied to the actual new p13-73 terminal local run.
    dashboard_config:
      - task: Provide exactly `授权 remote handoff <actual-new-run-id>` after this plan displays the persisted p13-73 terminal facts.
must_haves:
  truths:
    - "Remote work is eligible only when p13-73 records terminal_passed, provesExternalChain true, a deterministic code, and a non-empty local itemId, per D-03 and D-04."
    - "The plan accepts only the literal `授权 remote handoff <actual-new-run-id>` where the value equals the persisted p13-73 run id; before it, no remote preflight, handoff, verifier, provider command, production navigation, or evidence inspection runs, per D-05."
    - "One authorized repository-owned `starye-org` remote handoff owns its nested official selected-target preflight and yields either a pending tuple with one remote itemId or the first immutable remote checkpoint, per D-01, D-02, D-05, D-06, and D-07."
    - "p13-69 and all older carrier trees remain frozen and are not authorization sources, live inputs, or execution targets."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-74-SUMMARY.md
      provides: Non-secret p13-73 eligibility, exact authorization decision, sole remote handoff result, and frozen remote state.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-73-run-id}/remote.json
      provides: Untracked selected-target pending or checkpoint evidence written only by the authorized handoff.
    - path: .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{p13-73-run-id}/remote.md
      provides: Untracked canonical Markdown rendering of the remote JSON evidence.
  key_links:
    - from: p13-73 terminal local evidence
      to: literal authorization gate
      via: Persisted run id plus terminal verifier fields are checked before the authorization can be accepted.
      pattern: terminal_passed and provesExternalChain true
    - from: authorized repository handoff
      to: Plan 13-75 production observation
      via: Passed nested preflight, one runner invocation, exact code, and a non-empty remote itemId produce the only eligible remote pending pair.
      pattern: preflightStatus passed and handoffReady true
---

<objective>
Hold the selected-production mutation behind a fresh exact authorization for p13-73, then perform one repository-owned nested-preflight handoff or persist its first checkpoint.

Purpose: A local terminal result proves no provider-side outcome. Remote execution must have new authority that binds one actual p13-73 run, rather than reusing any p13-55, p13-57, p13-66, p13-69, or generic workflow approval.

Output: `13-74-SUMMARY.md` and conditional untracked p13-73 remote evidence. This plan contains no production Dashboard or Viewer observation and no standalone remote preflight or verifier.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-PATTERNS.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-73-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-73-RUN-ID.txt
@scripts/data-chain-handoff.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
@packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
@packages/config/src/deployment-target/__tests__/verify-data-chain-smoke.test.ts
</context>

<interfaces>
- `DataChainHandoffResult` is the sole remote result consumed here: `handoffReady`, `preflightStatus`, `runnerInvocations`, deterministic code, and non-empty remote itemId decide Plan 13-75 eligibility.
- `runDataChainHandoffCore` owns target selection, nested remote preflight, reservation, fixture execution, and pending evidence validation; no sibling preflight or remote verifier may be issued.
- `inspectDataChainSmokeVerification` is only the handoff core's existing pending-pair inspector in this plan; it is never called as a separate remote action.
</interfaces>

## Artifacts This Phase Produces

| Artifact | Owner | Produced state |
| --- | --- | --- |
| `13-74-SUMMARY.md` | Phase 13 | Exact authorization decision and one remote handoff outcome. |
| `evidence/starye-org/{p13-73-run-id}/remote.json` and `remote.md` | Untracked execution evidence | One selected-target pending tuple or first redacted checkpoint. |
| `remote.attempt` under the p13-73 evidence root | Repository handoff | Atomic one-owner marker; never recreated or removed manually. |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify p13-73 local terminal evidence and obtain exact remote authority</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-74-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-73-RUN-ID.txt
    - .planning/phases/13-full-chain-data-smoke/13-69-SUMMARY.md
    - scripts/verify-data-chain-smoke.ts
  </read_first>
  <action>
Read only p13-73 tracked local facts and its exact persisted verifier result. Confirm `terminal_passed`, `provesExternalChain: true`, deterministic item code, and non-empty local itemId before presenting this checkpoint. Record the authorization decision without session or provider material. Until an exact authorization arrives, run no remote preflight, handoff, verifier, provider command, production browser navigation, or remote evidence read.

The only accepted decision is `授权 remote handoff <actual-new-run-id>`, with the placeholder replaced by the run id from `13-73-RUN-ID.txt`. Treat every other message, a generic continuation request, any historical authorization, and any constructed identifier as not authorized. This task implements D-03, D-04, D-05, and D-06.
  </action>
  <what-built>Plan 13-73 has either supplied one eligible fresh local carrier or has already ended at its first local non-success.</what-built>
  <how-to-verify>
1. Read `13-73-SUMMARY.md` and `13-73-RUN-ID.txt`.
2. Confirm `terminal_passed`, `provesExternalChain: true`, a deterministic code, and a non-empty local itemId.
3. To authorize the sole provider-side handoff, enter exactly `授权 remote handoff <actual-new-run-id>` with the persisted p13-73 value.
4. Any mismatch or missing local terminal fact leaves the remote path untouched.
  </how-to-verify>
  <resume-signal>Type `授权 remote handoff <actual-new-run-id>` for the terminal p13-73 run, or describe the local-proof mismatch.</resume-signal>
  <verify>
    <automated>if (-not (Test-Path -LiteralPath .planning/phases/13-full-chain-data-smoke/13-73-RUN-ID.txt)) { throw 'p13-73 run id is required before the remote authorization gate' }</automated>
  </verify>
  <acceptance_criteria>
    - The gate accepts exactly one literal authorization bound to the persisted p13-73 run.
    - Before that literal authorization, there is no remote operation, remote evidence inspection, or production navigation.
    - Historical carriers are neither proof nor authority for this gate.
  </acceptance_criteria>
  <done>The plan is paused at the exact authorization boundary or has the sole authority for one p13-73 remote handoff.</done>
</task>

<task type="auto">
  <name>Task 2: Run the single authorized nested-preflight remote handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-74-SUMMARY.md</files>
  <read_first>
    - scripts/data-chain-handoff.ts
    - packages/config/src/deployment-target/__tests__/data-chain-handoff.test.ts
    - packages/config/src/deployment-target/__tests__/data-chain-smoke-remote.test.ts
    - .planning/phases/13-full-chain-data-smoke/13-73-SUMMARY.md
  </read_first>
  <action>
Proceed only after Task 1 records the literal authorization bound to the existing p13-73 run id. Confirm the exact p13-73 remote JSON, Markdown, and attempt marker are absent. Invoke one repository-owned command: `pnpm smoke:data-chain:handoff -- --mode remote --target starye-org --run-id <actual-p13-73-run-id>`. The handoff core owns the nested official selected-target preflight, so issue no separate target-profile preflight. It also owns the pending-pair inspection, so issue no standalone remote verifier.

Only a passed nested preflight, `runnerInvocations: 1`, `handoffReady: true`, the deterministic count-one non-R18 tuple, and a non-empty remote itemId make Plan 13-75 eligible. Any authorization, reservation, preflight, handoff, tuple, or internal inspection non-success permanently freezes p13-73 remote mode without another preflight, handoff, verifier, browser observation, allocation, or historical-run use. Record only authorization category, status category, code, itemId presence, and first checkpoint in the summary. This task implements D-01, D-02, D-04, D-05, D-06, and D-07.
  </action>
  <verify>
    <automated>pnpm --filter @starye/config test --run src/deployment-target/__tests__/data-chain-handoff.test.ts src/deployment-target/__tests__/data-chain-smoke-remote.test.ts src/deployment-target/__tests__/verify-data-chain-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - One explicit `starye-org` handoff bound to p13-73 owns the only nested preflight and write attempt.
    - A usable pending remote tuple has passed preflight, one fixture invocation, and a non-empty remote itemId; every other first result is frozen evidence.
    - This plan has no production Dashboard/Viewer route and no extra remote verifier.
  </acceptance_criteria>
  <done>p13-73 has one remote pending tuple for Plan 13-75 or one immutable remote checkpoint that blocks it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Human authorization to remote handoff | A specific user decision permits one stateful selected-target operation for one exact fresh local carrier. |
| Local terminal evidence to provider operation | The handoff consumes the fresh tuple and owns nested preflight before fixture work. |
| Provider command to untracked evidence | Remote facts return only through allowlisted status/code/itemId evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-13-74-01 | Spoofing | Remote authorization | critical | mitigate | Require the literal authorization phrase bound to the persisted p13-73 run after terminal local facts are confirmed. |
| T-13-74-02 | Tampering | Target/run reservation | high | mitigate | Use explicit `starye-org`, fixed-root evidence paths, absent-pair checks, and the repository-owned attempt marker. |
| T-13-74-03 | Elevation of privilege | Provider mutation | critical | mitigate | Permit only the handoff core to invoke its nested selected-target preflight and fixed fixture runner. |
| T-13-74-04 | Information disclosure | Remote output | high | mitigate | Persist only authorization/status categories, deterministic code, and itemId presence. |
| T-13-74-05 | Denial of service | Remote fixture | medium | mitigate | Restrict work to the D-01/D-02 one-item fixture and prohibit retries. |
</threat_model>

<verification>
- Confirm terminal p13-73 local facts before presenting the authorization checkpoint.
- Use focused handoff, remote smoke, and artifact-verification tests without starting an additional provider command.
- Confirm Plan 13-75 eligibility only from the sole handoff's passed nested preflight, one runner invocation, and non-empty remote itemId.
</verification>

<success_criteria>
- No remote work begins before the exact authorization phrase for the actual p13-73 run.
- One authorized handoff creates a tuple-bound remote pending pair or freezes at its first remote non-success.
- A fresh pending pair, never an older artifact or local result, is the sole production-observation input.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-74-SUMMARY.md` when done.
</output>
