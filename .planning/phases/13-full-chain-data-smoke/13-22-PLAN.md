---
phase: 13-full-chain-data-smoke
plan: "22"
type: execute
wave: 16
depends_on: [13-16, 13-21]
files_modified:
  - scripts/local-dev-authorization.ts
  - scripts/tsconfig.phase13-local-dev.json
  - packages/config/src/deployment-target/__tests__/local-dev-authorization.test.ts
  - .planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md
autonomous: false
gap_closure: true
requirements: [DATA-01, DATA-07, TEST-05]
must_haves:
  truths:
    - "D-03/D-06: A legacy listener owner is teardown-eligible only when every fixed-port owner reaches one current-workspace scripts/local-dev.ts supervisor through a complete cycle-free chain; a missing process or cycle before that supervisor is a no-mutation failure."
    - "A missing or unavailable parent above the matched scripts/local-dev.ts supervisor is recorded only as non-secret external-ancestor context and does not invalidate otherwise complete owner-to-supervisor proof."
    - "The authorization record contains the exact normalized snapshot hash, all fixed listener owners, every descendant below the supervisor in child-before-parent order, and the ordered stop set; a human approves only that exact record."
    - "D-03: A second identical snapshot is required before stopping the approved set; only child-before-parent stops, two all-free assertions, a fresh task-owned seven-port tree, and accepted canonical check-services routes may publish runtime_eligibility: released."
    - "Every rejected authorization, teardown, cold-start, ancestry, or canonical readiness result ends with runtime_eligibility: blocked before D-01/D-02 fixture work, D-04/D-05/D-06 browser or provider proof, and D-07 evidence/run allocation."
  artifacts:
    - path: scripts/local-dev-authorization.ts
      provides: Import-safe, testable Windows listener/CIM snapshot evaluator and read-only CLI that anchors authorization at the resolved current-workspace local-dev supervisor.
    - path: scripts/tsconfig.phase13-local-dev.json
      provides: Narrow no-emit TypeScript coverage for the local supervisor and authorization evaluator.
    - path: packages/config/src/deployment-target/__tests__/local-dev-authorization.test.ts
      provides: Deterministic supervisor-root, missing-before-supervisor, ancestry-cycle, descendant-order, and external-ancestor-context tests.
    - path: .planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md
      provides: Non-secret pending or terminal authorization snapshot, exact PID sets, external context, runtime branch, and latest eligibility result.
  key_links:
    - from: fixed listener owner PIDs
      to: current-workspace scripts/local-dev.ts supervisor
      via: one complete cycle-free parent chain per owner that terminates at the matched supervisor rather than its launcher or an unrelated ancestor
      pattern: supervisor_root
    - from: supervisor-root snapshot
      to: child-before-parent stop set
      via: all current descendants below the supervisor are derived from the same immutable process map, sorted deepest-first, then the supervisor is appended last
      pattern: authorized_stop_pids_child_before_parent
    - from: 13-22-SUMMARY.md
      to: Plans 13-17 through 13-20
      via: only runtime_eligibility: released plus the fresh canonical readiness facts authorizes the downstream chain; 13-16 and 13-21 remain historical diagnoses
      pattern: runtime_eligibility
---

<objective>
Close the 13-21 legacy-ancestry gap by making the exact current-workspace `scripts/local-dev.ts` supervisor the authorization ancestor endpoint, then use a PID-snapshot human checkpoint and exact snapshot equality to either perform the bounded legacy-tree replacement or record a closed runtime blocker.

Purpose: 13-21 correctly refused teardown because a parent above the matching local-dev supervisor was absent. That absence does not weaken the ownership evidence below the supervisor; this plan distinguishes it from the unsafe cases that occur before the supervisor or create an ancestry cycle.

Output: a tested non-secret authorization evaluator, one pending-or-blocked PID snapshot record, and one released-or-blocked runtime eligibility result. This is not a smoke, browser, provider, schema, migration, or Phase-pass claim.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-RESEARCH.md
@.planning/phases/13-full-chain-data-smoke/13-VALIDATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-16-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md
@.planning/debug/phase13-runtime-ownership.md
@.planning/debug/phase13-legacy-parent-missing.md
@scripts/local-dev.ts
@scripts/check-services.ps1
@scripts/tsconfig.phase13-local-dev.json
@packages/config/src/deployment-target/__tests__/local-dev.test.ts
</context>

<discovery_evidence>
- Commit 86fc107 preserves the no-mutation 13-21 checkpoint: PID 39560 was missing above a listener-owner chain, so no PID set, human approval, teardown, launch, or downstream eligibility was published.
- The earlier runtime diagnosis establishes that `pnpm dev` starts the current-workspace `scripts/local-dev.ts` supervisor and that it owns the seven labeled service contract. Plan 13-21 already repaired and tested atomic startup; this plan does not revise its source or summary.
- The required ownership boundary is therefore the live Node process whose command line resolves to this worktree's `scripts/local-dev.ts`. Every listener owner must reach that process without a missing record or a cycle. The parent of that matched process is outside the authorization tree; its absence may be recorded without expanding or blocking the approved descendant set.
- No package installation, provider request, process-environment inspection, browser action, schema/migration, evidence-run allocation, or remote command is needed for Task 1 or the read-only checkpoint.
</discovery_evidence>

<dependency_graph>
| Task | Needs | Creates | Order |
|---|---|---|---|
| 1 | Immutable 13-16/13-21 diagnoses and current local-dev source contract | Tested supervisor-root snapshot evaluator and narrow type coverage | First; no live process action |
| 2 | Task 1 committed plus one fresh read-only listener/CIM snapshot | Pending human PID authorization or no-mutation pre-teardown blocker | Second; no stop or launch |
| 3 | Exact Task 2 approval and an identical recomputed snapshot | Child-before-parent legacy disposition, all-free cold start, canonical release or closed blocker | Last; only released output unlocks 13-17 |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD a supervisor-root authorization snapshot evaluator</name>
  <files>scripts/local-dev-authorization.ts, scripts/tsconfig.phase13-local-dev.json, packages/config/src/deployment-target/__tests__/local-dev-authorization.test.ts</files>
  <behavior>
    - A six-listener legacy snapshot whose owner chains all reach one Node process executing the resolved current-workspace `scripts/local-dev.ts` authorizes that supervisor even when the supervisor's own parent record is missing; that missing parent appears only in external-ancestor context.
    - A missing process record before an owner reaches the selected supervisor returns a closed pre-teardown refusal with no descendant or stop PID set.
    - A repeated PID in an owner-to-supervisor chain returns a closed cycle refusal with no descendant or stop PID set.
    - The accepted snapshot includes every process whose cycle-free ancestry reaches the matched supervisor, includes every listener owner, emits descendants deepest-first with deterministic PID tie-breaking, and appends the supervisor only after all descendants in the stop set.
  </behavior>
  <action>Before any existing symbol edit, call GitNexus upstream impact for `runLocalDevSupervisor`, `localDevServiceSpecs`, and every additional existing symbol selected; report direct callers and execution flows. A HIGH or CRITICAL result is a blocking warning checkpoint: notify the root user, compare the reported blast radius with this task's local-runtime boundary, and do not edit if it expands outside that boundary. Start RED with deterministic Vitest fixtures for normalized listener and Win32 process records; tests must not enumerate real processes, bind ports, start services, or stop a process. Add an import-safe `scripts/local-dev-authorization.ts` that runs a fixed read-only PowerShell/CIM listener snapshot only when its executable entry is invoked, accepts no caller-supplied workspace, port list, command, PID set, or secret-bearing input, and exports a pure evaluator for tests. Resolve the current repository root and the exact `scripts/local-dev.ts` path internally. The evaluator must select exactly one live Node supervisor whose command line identifies that resolved script in the current workspace; for the legacy branch require 8080 absent and exactly one owner on each of 8787, 5173, 3002, 3003, 3000, and 3001. Walk each listener owner upward only until the selected supervisor. Missing process, ambiguous supervisor, wrong workspace/script, duplicate listener owner, or cycle before that endpoint is blocking. Do not require, traverse, or authorize any parent above the selected supervisor; when that external parent is unavailable, emit only deterministic non-secret `external_ancestor_context`. Derive the full descendant set below the supervisor from the same captured process map, require it to contain every listener owner and no process outside the supervisor tree, sort it child-before-parent, and append the supervisor as the last member of the exact stop set. Serialize deterministic port/PID/parent/executable/script/start-time/descendant facts, then hash only that normalized non-secret payload. Extend the existing narrow TypeScript project with this new root script. Run RED, GREEN, and any REFACTOR verification with fresh GitNexus detect-changes before each commit; expected changes are the new authorization evaluator/test and the explicit TypeScript file list only. Any unexpected symbol or execution-flow impact stops the task.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts src/deployment-target/__tests__/local-dev-authorization.test.ts src/deployment-target/__tests__/gateway-readiness.test.ts</automated>
    <automated>pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json</automated>
    <automated>pnpm --filter @starye/config type-check</automated>
  </verify>
  <done>The new evaluator proves that supervisor-root convergence is sufficient despite an unavailable ancestor above the supervisor, while any missing or cyclic link before that endpoint yields no authorization or stop set; no live runtime action occurs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture and confirm the exact supervisor-root legacy PID snapshot</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md</files>
  <what-built>Task 1 provides the bounded read-only evaluator. This checkpoint captures one fresh authorization record rooted at the current-workspace local-dev supervisor before any process mutation.</what-built>
  <how-to-verify>Inspect the Summary's one 64-hex snapshot hash, matched supervisor PID, listener-owner list, descendant and stop PID order, and external-ancestor context. Confirm that every displayed PID belongs to the intended legacy local-dev tree and that neither teardown nor launch was attempted.</how-to-verify>
  <action>Run only the Task 1 fixed read-only snapshot CLI from the resolved repository root. It may inspect listener/CIM process records for exactly 8080, 8787, 5173, 3002, 3003, 3000, and 3001, but must not inspect environment values, secret files, browser state, or provider state. On authorization success, write exactly one pending record in 13-22-SUMMARY.md with `runtime_eligibility: blocked`, `legacy_teardown_authorization: pending_human_confirmation`, `authorization_snapshot_sha256`, `authorization_supervisor_script: scripts/local-dev.ts`, `authorization_supervisor_pid`, `authorization_listener_owner_pids`, `authorization_descendant_pids_child_before_parent`, `authorized_stop_pids_child_before_parent`, `authorization_external_ancestor_context`, `teardown_attempted: false`, and `launch_attempted: false`. The descendant list must include every listener owner and the stop list must equal descendants followed by the supervisor. A missing supervisor parent is recorded only in external context and does not make this record ineligible. If a listener owner has a missing link before the supervisor, a cycle, duplicate listener, wrong workspace script, ambiguous root, or malformed payload, write only the closed `blocked_pre_teardown` branch with one allowlisted reason and both operation flags false; do not publish a PID authorization record or offer approval. Do not call Stop-Process, `pnpm dev`, `pnpm check:services`, clean-port scripts, smoke, run allocation, schema/migration, browser, provider, or evidence commands. Before committing only this Summary, run GitNexus detect-changes and require no source symbol or execution-flow change.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md'; $pending = $summary -match '(?m)^legacy_teardown_authorization:\s*pending_human_confirmation\s*$' -and $summary -match '(?m)^authorization_snapshot_sha256:\s*[0-9a-f]{64}\s*$' -and $summary -match '(?m)^authorization_supervisor_script:\s*scripts/local-dev\.ts\s*$' -and $summary -match '(?m)^authorization_supervisor_pid:\s*\d+\s*$' -and $summary -match '(?m)^authorization_descendant_pids_child_before_parent:\s*\d+(,\d+)*\s*$' -and $summary -match '(?m)^authorized_stop_pids_child_before_parent:\s*\d+(,\d+)*\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blocked = $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^terminal_branch:\s*blocked_pre_teardown\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; if (-not ($pending -xor $blocked)) { throw 'Expected one pending supervisor-root authorization or one no-mutation pre-teardown blocker.' }; exit 0"</automated>
  </verify>
  <resume-signal>Type `approved legacy-tree PID set &lt;authorization_snapshot_sha256&gt;` only after the displayed hash and every ordered PID are the intended legacy tree. Any other response leaves the tree untouched.</resume-signal>
  <done>A human sees one exact supervisor-root PID authorization record with no process mutation, or a closed no-mutation blocker proves that no unsafe record can be approved.</done>
</task>

<task type="auto">
  <name>Task 3: Stop only the identically authorized tree and publish the latest runtime eligibility</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md</files>
  <action>Proceed only after the Task 2 resume signal exactly matches the pending `authorization_snapshot_sha256`. Reload the Summary and recompute the same fixed read-only supervisor-root snapshot immediately before a stop. Require exact equality of the normalized hash, selected supervisor PID/script/workspace, listener-owner set, descendant order, stop order, external-ancestor context, and the legacy six-listener/missing-8080 state. A parent missing above the supervisor remains non-blocking only when the recomputed context is identical; any missing or cyclic link before the supervisor, changed PID, changed listener, changed descendant, changed context, malformed record, or absent approval writes `runtime_eligibility: blocked`, `terminal_branch: blocked_pre_teardown`, one closed reason, `teardown_attempted: false`, and `launch_attempted: false`, then ends before process mutation. Only on exact equality stop exactly `authorized_stop_pids_child_before_parent` with native PowerShell per-PID APIs in recorded order. Never use broad image-name termination, wildcard matching, clean:ports, dev:clean, taskkill, port cleanup, process-tree rediscovery, or an unrecorded PID. Wait boundedly for every approved PID to exit and every fixed port to become free. A stop error, surviving authorized PID, or remaining listener writes `blocked_after_teardown` with teardown attempted true and launch attempted false, then ends. After a second immediate all-free assertion, start `pnpm dev` hidden with the resolved repository root as working directory, capture the fresh launcher and its unique current-workspace local-dev supervisor, and use the same evaluator in complete-seven-port mode to require each listener owner to converge cycle-free to that fresh supervisor. Poll `pnpm check:services` for at most 60 seconds and accept only exit 0 plus exactly one `starye-local-services-1` machine record with seven listener rows true and accepted canonical `/robots.txt`, `/auth`, and `/auth/` outcomes. Run Task 1's authorization/local-dev/readiness tests, the narrow type-check, config type-check, and `pnpm --filter gateway test`. Any post-launch ownership, readiness, route, or test failure writes `runtime_eligibility: blocked`, `terminal_branch: blocked_after_launch`, `teardown_attempted: true`, and `launch_attempted: true`, then forbids every data-chain action. Only all passing gates may write `runtime_eligibility: released`, `terminal_branch: released`, the approved hash and PID sets, fresh supervisor facts, accepted check-services facts, and `next_plan: 13-17`. This release is only the runtime prerequisite for downstream plans. Before the Summary commit, run GitNexus detect-changes and require no uncommitted code symbol or execution-flow expansion beyond Task 1's committed evaluator artifacts.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md'; $released = $summary -match '(?m)^runtime_eligibility:\s*released\s*$' -and $summary -match '(?m)^terminal_branch:\s*released\s*$' -and $summary -match '(?m)^legacy_teardown_authorization:\s*approved\s*$' -and $summary -match '(?m)^authorization_supervisor_script:\s*scripts/local-dev\.ts\s*$' -and $summary -match '(?m)^pre_cold_start_fixed_ports:\s*all_free\s*$' -and $summary -match '(?m)^fresh_local_dev_supervisor_pid:\s*\d+\s*$'; $blockedPre = $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^terminal_branch:\s*blocked_pre_teardown\s*$' -and $summary -match '(?m)^teardown_attempted:\s*false\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blockedStop = $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^terminal_branch:\s*blocked_after_teardown\s*$' -and $summary -match '(?m)^teardown_attempted:\s*true\s*$' -and $summary -match '(?m)^launch_attempted:\s*false\s*$'; $blockedLaunch = $summary -match '(?m)^runtime_eligibility:\s*blocked\s*$' -and $summary -match '(?m)^terminal_branch:\s*blocked_after_launch\s*$' -and $summary -match '(?m)^teardown_attempted:\s*true\s*$' -and $summary -match '(?m)^launch_attempted:\s*true\s*$'; if ((@($released, $blockedPre, $blockedStop, $blockedLaunch | Where-Object { $_ }).Count) -ne 1) { throw 'Expected exactly one released or closed runtime terminal branch.' }; exit 0"</automated>
    <automated>powershell -NoProfile -Command "$summary = Get-Content -Raw -LiteralPath '.planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md'; if ($summary -match '(?m)^runtime_eligibility:\s*blocked\s*$') { exit 0 }; pnpm check:services; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/local-dev.test.ts src/deployment-target/__tests__/local-dev-authorization.test.ts src/deployment-target/__tests__/gateway-readiness.test.ts src/deployment-target/__tests__/data-chain-smoke-local.test.ts src/deployment-target/__tests__/data-chain-smoke-process.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm exec tsc --noEmit -p scripts/tsconfig.phase13-local-dev.json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm --filter gateway test; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The original tree is untouched unless a human-approved snapshot remains byte-identical; the only released branch has an all-free cold start, fresh cycle-free listener ownership, and canonical Gateway readiness, while every other branch remains closed before downstream work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| listener owner -> local-dev supervisor | A listener PID is eligible only when a complete cycle-free chain reaches the exact current-workspace supervisor. |
| local-dev supervisor -> external parent | The supervisor's launcher is not part of the authorized descendant tree; unavailable external ancestry is retained as context only. |
| pending snapshot -> process stop | Human approval and a byte-identical recomputed snapshot are both required before stopping any PID. |
| all-free ports -> fresh local runtime | A fresh task-owned supervisor plus canonical Gateway HTTP readiness is the only release condition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-108 | Tampering | legacy PID authorization | critical | mitigate | Require a current-workspace local-dev supervisor root, all listener chains complete before that root, deterministic descendant/stop sets, exact human-approved hash, and exact re-snapshot equality. |
| T-13-109 | Spoofing | ancestry attribution | critical | mitigate | Reject ambiguous roots, wrong script/workspace, duplicate owners, missing-before-supervisor links, and cycles; parent state above the matched root cannot expand the set. |
| T-13-110 | Denial of Service | local process teardown | high | mitigate | Stop only the captured child-before-parent set; no broad cleanup; block on survivor or non-free ports before launch. |
| T-13-111 | Elevation of Privilege | data-chain eligibility | critical | mitigate | Release only after a fresh all-seven ownership proof and canonical check-services output; all blocked branches forbid every downstream action. |
| T-13-112 | Information Disclosure | process evidence | medium | mitigate | Snapshot and Summary persist only PID, parent, executable, script/workspace match, start fact, port, ordered set, hash, and closed context; never environment values or secret files. |
| T-13-SC | Tampering | package supply chain | low | accept | No package-manager install occurs. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Prove selected target through local Gateway then production chain. | 13-22 runtime prerequisite; 13-17 through 13-20 retain full chain. | COVERED | 13-22 does not claim data-chain success. |
| REQ | DATA-01 | Canonical local Gateway/API/auth/content readiness. | Task 3 then 13-17/18. | COVERED | Release requires accepted canonical Gateway checks. |
| REQ | DATA-02 | Local D1 readiness before ingest. | 13-17. | COVERED | Forbidden in 13-22. |
| REQ | DATA-03 | One deterministic fixture. | 13-17 and 13-19. | COVERED | No fixture operation here. |
| REQ | DATA-04 | D1/API/admin correlation. | 13-17 through 13-20. | COVERED | No data-surface action here. |
| REQ | DATA-05 | Dashboard proof. | 13-18 and 13-20. | COVERED | Browser remains downstream. |
| REQ | DATA-06 | Canonical viewer proof. | 13-18 and 13-20. | COVERED | Browser remains downstream. |
| REQ | DATA-07 | Non-secret repeatable artifacts. | Tasks 1-3 Summary plus 13-17 through 13-20 pairs. | COVERED | PID snapshot is runtime evidence only. |
| REQ | TEST-05 | Repeatable smoke/runtime output. | Task 1 tests and Task 3 checks. | COVERED | No dependency additions. |
| RESEARCH | - | Explicit target, Gateway-first, one-item, fail-closed local-first ordering. | Task 3 and downstream plans. | COVERED | Runtime release precedes local smoke only. |
| RESEARCH | - | Do not expose secrets or use direct ports as canonical proof. | Tasks 1-3. | COVERED | Process snapshot is non-secret; 8080 checks remain canonical. |
| CONTEXT | D-01 | One deterministic item. | 13-17 and 13-19. | COVERED | No item allocation in this plan. |
| CONTEXT | D-02 | Closed fixture/crawler boundary. | 13-17 and 13-19. | COVERED | No crawler operation in this plan. |
| CONTEXT | D-03 | Local-first through localhost:8080 before production. | Tasks 1-3 then 13-17 onward. | COVERED | The direct runtime release gate. |
| CONTEXT | D-04 | Exact tuple across D1/API/Dashboard/viewer. | 13-17 through 13-20. | COVERED | No tuple exists here. |
| CONTEXT | D-05 | Explicit provider preflight. | 13-19. | COVERED | Provider remains forbidden here. |
| CONTEXT | D-06 | Non-secret repeatable artifacts. | Tasks 1-3 and downstream evidence. | COVERED | No secret/process-environment persistence. |
| CONTEXT | D-07 | Real provider proof remains in Phase 13. | 13-19 and 13-20. | COVERED | Runtime repair does not substitute for it. |
| VERIFICATION | G-1 | Fresh local runtime was blocked before a new terminal local pair. | Tasks 1-3, 13-17, and 13-18. | COVERED | Root cause is authorization scope above supervisor, not a relaxation of readiness. |
| VERIFICATION | G-2/G-3 | Provider and production browser proof remain missing. | 13-19 and 13-20. | COVERED | Strict downstream dependency retained. |
| VERIFICATION | G-4 | Pending-wrapper exit mismatch. | 13-14 and 13-17 onward. | COVERED | This plan does not reopen that contract. |
</source_audit>

<verification>
- Task 1 is RED-GREEN-REFACTOR: deterministic tests must cover matched supervisor with missing external parent, missing-before-supervisor, cycle, and exact descendant order before implementation can pass.
- Task 2 is the only human authorization gate. It writes one hash-bound pending record or a no-mutation blocker; external ancestry is context only after the supervisor root is reached.
- Task 3 requires exact snapshot equality before the captured per-PID child-before-parent stop. It verifies all-free twice, fresh ownership, and canonical check-services before release.
- Every source symbol edit has a fresh GitNexus impact gate; HIGH/CRITICAL requires a root-user warning and stop. Every task commit has fresh GitNexus detect-changes and expected symbol/flow comparison.
- No broad kill, port clean, source replay, browser/provider command, evidence allocation, schema, migration, or remote command is permitted before a released result is consumed by 13-17.
</verification>

<success_criteria>
- A complete owner-to-supervisor chain is accepted even if the matched supervisor's external parent has disappeared, while missing-before-supervisor and cycle cases are mechanically rejected.
- The pending authorization is hash- and PID-set-specific, human-confirmed, and rechecked byte-for-byte before any process stop.
- Stops are limited to the exact child-before-parent set; all seven ports must be free twice before a fresh cold start.
- Only a fresh seven-port supervisor with accepted canonical Gateway routes writes `runtime_eligibility: released`; every other result writes a closed blocked branch and no downstream smoke action occurs.
</success_criteria>

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-22-SUMMARY.md` when done. It must record only non-secret supervisor-root snapshot facts, exact approved PID sets, any non-blocking external-ancestor context, terminal eligibility, and whether 13-17 is eligible. It must never modify Plans, Summaries, or evidence for 13-01 through 13-21.
</output>
