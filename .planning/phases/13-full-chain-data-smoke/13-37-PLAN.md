---
phase: 13-full-chain-data-smoke
plan: "37"
type: execute
wave: 35
depends_on: [13-36]
files_modified:
  - .planning/phases/13-full-chain-data-smoke/13-37-SUMMARY.md
  - .planning/phases/13-full-chain-data-smoke/13-37-RUN-ID.txt
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - TEST-05
must_haves:
  truths:
    - "A brand-new run_id (never p13-17) completes local lifecycle gates, one local handoff pending pair with non-empty itemId, and ordered local Dashboard→Viewer terminal_passed provesExternalChain true."
    - "Evidence stays under evidence/starye-org/<new-run-id>/ and untracked."
    - "p13-17 and historical blocked trees remain untouched."
  artifacts:
    - path: .planning/phases/13-full-chain-data-smoke/13-37-RUN-ID.txt
      provides: single allocated run id for Waves 36-37
    - path: .planning/phases/13-full-chain-data-smoke/13-37-SUMMARY.md
      provides: local terminal proof authorizing remote handoff
---

<objective>
Produce one fresh local terminal data-chain proof on a NEW run_id after the local-dev fix. Supersedes incomplete thin drafts 13-32/13-33 for execution. Does not perform remote handoff.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-full-chain-data-smoke/13-36-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
</context>

## Artifacts this phase produces
- `13-37-RUN-ID.txt` with one `p13-37-<32hex>` value
- Untracked local.attempt/local.json/local.md under the new run
- `13-37-SUMMARY.md`

## Non-goals
- No remote handoff / no production IAB
- Do not reuse p13-17-b807d44deee04e1f85b42fd02ed8cf26
- Do not delete or rewrite historical evidence

<tasks>
<task type="auto">
  <name>Task 1: Fresh local lifecycle + one local handoff</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-37-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-36-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-28-SUMMARY.md
    - .planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
  </read_first>
  <action>
Confirm fixed ports 8080,8787,5173,3002,3003,3000,3001 free. Start root `pnpm dev`. Pass `pnpm target-profile project-local --target starye-org --check`, `pnpm target-profile preflight --target starye-org --scope local --command validate --wrangler-profile starye-org`, and `pnpm check:services` with Gateway robots/auth/authSlash via http://localhost:8080. Allocate once: run_id = `p13-37-` + 32 hex; write to 13-37-RUN-ID.txt; require evidence directory absent. Invoke exactly once: `pnpm smoke:data-chain:handoff -- --mode local --target starye-org --run-id $runId`. Require outcome pending, handoffReady true, non-empty local itemId, aggregate pending / resolved_pending_observation. Do not invoke smoke:data-chain separately.
  </action>
  <acceptance_criteria>
    - 13-37-RUN-ID.txt contains p13-37-[0-9a-f]{32}
    - local.json exists for that run with non-empty itemId and aggregate pending
    - local exact verify for that run returns exit 2 pending path (or exit 0 if already browser-complete later in Task 2 only)
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-37-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
</task>
<task type="auto">
  <name>Task 2: Local ordered Dashboard then Viewer</name>
  <files>.planning/phases/13-full-chain-data-smoke/13-37-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
  </read_first>
  <action>
Using the same run_id only: pre-browser local exact verify should be pending (exit 2). Observe signed-in local Gateway Dashboard then matching /movie/&lt;item-code&gt; Viewer through http://localhost:8080 only (never direct 3000/3001/5173 as canonical). Prefer repository observe path (`pnpm smoke:data-chain-observe` or observeDataChainSurfaces equivalent used in 13-18). Post-browser local exact verify must be exit 0, terminal_passed, provesExternalChain true. Write 13-37-SUMMARY.md with ports, run_id, handoff once, browser order, verifier exits, SHA-256 of untracked local pair, cleanup of task-owned listeners, scope boundary (no remote). Keep evidence untracked.
  </action>
  <acceptance_criteria>
    - post-browser local verify: exit 0, terminal_passed, provesExternalChain true
    - 13-37-SUMMARY.md records that remote was not invoked
  </acceptance_criteria>
  <verify>
    <automated>pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $(Get-Content .planning/phases/13-full-chain-data-smoke/13-37-RUN-ID.txt -Raw).Trim()</automated>
  </verify>
  <done>Fresh local terminal pair authorizes Wave 36 remote handoff only.</done>
</task>
</tasks>
