---
phase: 13-full-chain-data-smoke
plan: "18"
type: execute
wave: 17
depends_on: [13-17, 13-21]
files_modified:
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.json"
  - ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.md"
  - ".planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md"
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - TEST-05
user_setup:
  - service: Root Codex persistent in-app browser
    why: Only the existing signed-in root browser may provide live local Dashboard and viewer observations for the exact pending tuple.
    dashboard_config:
      - task: Preserve the existing authorized Movies session; do not export cookies or storage.
        location: http://localhost:8080/auth/
must_haves:
  truths:
    - "D-03: No browser action occurs unless one exact `run_id` is extracted from 13-17-SUMMARY.md, 13-21 remains the released fresh-runtime gate, and the paths/commands derived from that run verify the p13-17 local pair as resolved_pending_observation/pending with raw verifier exit 2 and the same target/run/code/itemId."
    - "The existing zero-byte `local.attempt` is read-only, persistent, and untracked throughout browser observation; it proves reservation history only and the pair-focused verifier never treats it as pending or terminal success."
    - "D-03/D-04: repository core directs the root persistent Codex IAB to local Dashboard first and viewer second through http://localhost:8080; each surface has exactly one marker whose data-phase13-item-id attribute equals itemId and whose settled visible text contains itemCode, without requiring visible itemId text."
    - "The browser adapter supplies navigation outcome only; repository core owns tuple derivation, route order, receipt construction, validation, JSON/Markdown rendering, and writes."
    - "Chrome, incognito/isolated contexts, Puppeteer, Playwright, screenshots, cookie/storage export, manual artifact edits, and user attestation cannot produce a receipt."
    - "Any auth/route/DOM/tuple/write/verifier failure persists an allowlisted checkpoint, permanently closes the run, and blocks all remote work."
  artifacts:
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.attempt"
      provides: "Read-only zero-byte local attempt ownership history; never modified, staged, committed, or accepted as verifier success"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.json"
      provides: "Ordered local browser receipts for the exact runner tuple or immutable browser checkpoint"
    - path: ".planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.md"
      provides: "Exact deterministic peer"
    - path: ".planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md"
      provides: "Read-only local attempt-marker integrity, root-IAB handoff, and terminal pair-verifier closeout"
  key_links:
    - from: "13-17 exact pending pair"
      to: "root persistent Codex IAB"
      via: "artifact-only exact verifier and repository-derived pending tuple"
      pattern: "resolved_pending_observation"
    - from: "local.attempt"
      to: "13-18 browser gate"
      via: "read-only presence/zero-byte/untracked integrity check; eligibility still comes only from the exact pair verifier"
      pattern: "local.attempt"
    - from: "observeDataChainSurfaces"
      to: "local JSON/Markdown pair"
      via: "injected observeSurface only; Dashboard before viewer"
      pattern: "browser_observer"
    - from: "terminal local pair"
      to: "13-19 remote eligibility"
      via: "exact verifier terminal_passed and provesExternalChain true"
      pattern: "terminal_passed"
    - from: "13-17-SUMMARY.md run_id"
      to: "local evidence paths and root-IAB command"
      via: "PowerShell requires exactly one canonical p13-17 UUID before it derives all LiteralPaths and verifier arguments"
      pattern: "^run_id:\\s*p13-17-[0-9a-f]{32}$"
---

<objective>
Turn the exact p13-17 pending local tuple into receipt-backed terminal local proof using only the root persistent Codex in-app browser and repository-owned observation core (D-03, D-04, D-06).

Purpose: local runner/D1/API evidence is not enough for DATA-05/06. The same exact item must be authorized in Dashboard and visible in the Gateway viewer before remote mutation becomes eligible.

Output: a terminal local pair and Summary, or an immutable browser checkpoint that ends the run and Phase execution.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-17-PLAN.md
@.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
@.planning/phases/13-full-chain-data-smoke/13-21-SUMMARY.md
@scripts/data-chain-surface-observation.ts
@scripts/verify-data-chain-smoke.ts
@packages/config/src/deployment-target/data-chain-evidence.ts
</context>

<browser_execution_contract>
Only the root `$gsd-execute-phase 13 --gaps-only` orchestrator may control the persistent Codex in-app browser. If an executor subagent reaches this plan without that surface, it must hand off mode, target, run id, exact evidence paths, and repository-derived pending tuple to the root and wait. It must not launch or substitute another browser.

The root calls exported repository core `observeDataChainSurfaces()` with exactly one injected dependency, `observeSurface`. Core retains exact reads/writes, fixed evidence root, target resolution, canonical base, tuple validation, Dashboard-before-viewer order, receipt construction, deterministic rendering, and persistence. The adapter navigates to `new URL(input.path, input.baseUrl)`, requires exact final origin/path, waits for exactly one element selected by `[data-phase13-item-id="<input.itemId>"]`, requires `getAttribute('data-phase13-item-id') === input.itemId`, and separately requires that element's settled visible `textContent` contains `input.itemCode`. Visible text is never required to contain `input.itemId`. It returns passed only after those checks; otherwise unavailable so core persists a checkpoint.
</browser_execution_contract>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Let root repository core record local Dashboard then viewer</name>
  <files>.planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.json, .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.md, .planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/13-full-chain-data-smoke/13-CONTEXT.md
    - .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
    - .planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md
    - scripts/data-chain-surface-observation.ts
    - scripts/verify-data-chain-smoke.ts
    - packages/config/src/deployment-target/data-chain-evidence.ts
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.json
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.md
    - .planning/phases/13-full-chain-data-smoke/evidence/starye-org/{run_id from 13-17-SUMMARY.md}/local.attempt
  </read_first>
  <what-built>Plan 13-17 produced the only eligible pending local tuple after all repaired gates. This task supplies live browser navigation only; repository core owns every persisted fact.</what-built>
  <how-to-verify>Verify the pending pair artifact-only, use the same signed-in root Codex IAB to observe `http://localhost:8080/dashboard/movies` then `http://localhost:8080/movie/&lt;item-code&gt;`, and require the exact terminal verifier result.</how-to-verify>
  <action>Enter only when 13-21-SUMMARY still reports runtime_eligibility: released for the recorded fresh task-owned supervisor and 13-17-SUMMARY records handoffReady true. First run the Summary-to-PowerShell binding: read 13-17-SUMMARY.md once, require exactly one standalone `run_id: p13-17-[0-9a-f]{32}` match, reject zero/multiple/malformed matches, assign that captured value to `$runId`, and derive `$runDir`, local JSON, Markdown, and `local.attempt` LiteralPaths only from the fixed evidence root, target `starye-org`, and `$runId`. Never accept an inline UUID, a placeholder, an environment value, or a manually typed evidence path. The immutable 13-16 partial-tree checkpoint is diagnostic history, not a browser gate. Before any browser navigation, require the derived exact `local.attempt` to exist, remain zero-byte and `??` untracked, and treat it as read-only ownership history; do not delete, rewrite, repair, stage, commit, or use it as success evidence. Invoke the direct canonical exact verifier once for local mode with `--run-id $runId` and require raw exit 2 plus one machine line whose runId matches `$runId` and whose tuple is pending; the verifier remains JSON/Markdown-pair focused and no runner/handoff rerun occurs. The root orchestrator then calls repository core under `browser_execution_contract` with that same repository-derived tuple. Use only the existing persistent Codex IAB session, local base exactly `http://localhost:8080`, Dashboard `/dashboard/movies` before viewer `/movie/&lt;item-code&gt;`, and exact final routes. On each route require exactly one `[data-phase13-item-id="&lt;item-id&gt;"]` element, require its `getAttribute('data-phase13-item-id')` to equal the repository-provided itemId, and require its settled visible `textContent` to contain the repository-provided itemCode; never require itemId to appear in visible text. Do not expose or persist cookies, storage, HTML, screenshots, headers, direct ports, or caller-generated receipts. After repository core writes only the pair, invoke the exact local verifier once with `$runId` and continue only on raw exit 0, `terminal_passed`, and `provesExternalChain:true`; recheck that the derived `local.attempt` is byte-identical/untracked. Any invalid binding/auth/route/DOM/tuple/write/verifier/marker-integrity result closes this run without retry and blocks 13-19/13-20. Compare the pre-13-17 evidence baseline digest, keep marker/evidence untracked, and run GitNexus detect-changes before committing only the Summary.</action>
  <acceptance_criteria>
    - Pre-browser exact verifier confirms the same pending tuple with raw exit 2; no runner/handoff rerun occurs.
    - `local.attempt` exists as the same zero-byte untracked file before and after browser work, is never modified/staged/committed, and never substitutes for pair verification.
    - Root persistent Codex IAB is the only browser, and repository core receives only one injected observeSurface adapter.
    - Dashboard precedes viewer; on both exact 8080 routes the unique marker attribute equals the same itemId and settled visible text contains itemCode, while visible itemId text is not required.
    - Resulting pair is resolved/passed with ordered tuple-bound browser receipts, and exact local verifier returns terminal_passed/provesExternalChain true.
    - Any failure is an immutable checkpoint, prevents remote work, and is not described as success or zero-side-effect proof.
    - Prior evidence remains byte-identical to the recorded execution baseline and untracked; only Summary is committed.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$summaryPath = '.planning/phases/13-full-chain-data-smoke/13-17-SUMMARY.md'; $matches = @([regex]::Matches((Get-Content -Raw -LiteralPath $summaryPath), '(?m)^run_id:\s*(p13-17-[0-9a-f]{32})\s*$')); if ($matches.Count -ne 1) { throw 'Expected exactly one canonical run_id in 13-17 Summary.' }; $runId = $matches[0].Groups[1].Value; pnpm smoke:data-chain:verify -- --mode local --target starye-org --run-id $runId; exit $LASTEXITCODE"</automated>
  </verify>
  <resume-signal>Root resumes only with repository-written local receipts and exact verifier terminal_passed/provesExternalChain true. Plain approval is insufficient.</resume-signal>
  <done>The exact p13-17 local pair is terminal and remote-eligible, or the run ends at an immutable browser checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Pending artifact -> browser permission | Browser navigation is forbidden until exact artifact-only verification. |
| Persistent local attempt marker -> browser permission | Marker presence records ownership history only; it remains read-only and cannot replace pair-focused pending verification. |
| Persistent browser -> repository core | Browser session is external; it may report navigation/DOM only, never construct receipts. |
| Browser observation -> terminal local pair | Exact order, tuple, routes, and DOM markers are required before pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-86 | Spoofing | browser receipt | critical | mitigate | Root IAB live route/DOM checks; core derives and writes receipts. |
| T-13-87 | Elevation of Privilege | Dashboard session | critical | mitigate | Existing authorized session only; no cookie/storage fabrication or export. |
| T-13-88 | Information Disclosure | browser state | critical | mitigate | No cookies, storage, HTML, screenshots, headers, or endpoints persisted. |
| T-13-89 | Tampering | surface order/tuple | high | mitigate | Core fixes Dashboard-before-viewer; adapter validates itemId by exact attribute and itemCode by settled visible text. |
| T-13-90 | Repudiation | human approval or marker-only claim | high | mitigate | Plain approval and `local.attempt` presence cannot resume/promote; exact pair verifier is required and marker integrity remains read-only/untracked. |
| T-13-SC | Tampering | package supply chain | low | accept | No install/dependency action. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan-set coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Full local/production Gateway proof. | Task 1 local browser; 13-19/20 remote. | COVERED | Remote remains. |
| REQ | DATA-01 | Local canonical routes. | Task 1 | COVERED | 8080 only. |
| REQ | DATA-02/DATA-03 | Local D1/fixture. | 13-17 prerequisite | COVERED | Exact tuple consumed. |
| REQ | DATA-04 | Local D1/API/admin correlation. | 13-17 + Task 1 | COVERED | Dashboard closes local admin. |
| REQ | DATA-05 | Authorized Dashboard. | Task 1 | COVERED | Root IAB. |
| REQ | DATA-06 | Canonical viewer. | Task 1 | COVERED | Ordered after Dashboard. |
| REQ | DATA-07/TEST-05 | Exact artifact/output. | Task 1 | COVERED | Dual verifier states. |
| RESEARCH | - | Real auth session and Gateway-first browser. | Task 1 | COVERED | No mock browser. |
| CONTEXT | D-01..D-04 | One exact local tuple and canonical surfaces. | Task 1 | COVERED | Mode-owned local id. |
| CONTEXT | D-05 | Remote live preflight. | 13-19 | COVERED | Not run here. |
| CONTEXT | D-06 | Non-secret receipt evidence. | Task 1 | COVERED | Core owns pair receipts; zero-byte `local.attempt` stays read-only/untracked and is never success evidence. |
| CONTEXT | D-07 | Provider proof in Phase 13. | 13-19/20 | COVERED | Remains downstream. |
| VERIFICATION | G-1 | Fresh terminal local pair missing. | Task 1 | COVERED | Terminal exact verifier required. |
| VERIFICATION | G-2/G-3 | Production proof missing. | 13-19/20 | COVERED | Strict dependency. |
| VERIFICATION | G-4 | Wrapper mismatch. | 13-14 prerequisite | COVERED | Direct verifier raw 2 works. |

Phase 14 deferred work remains excluded.
</source_audit>

<verification>
- Verify pending artifact before browser and terminal artifact after browser; never rerun runner/handoff.
- Treat `local.attempt` as read-only attempt history and confirm it remains zero-byte/untracked; exact verifier success remains pair-only.
- Root persistent Codex IAB and repository core are the only allowed observation path.
- Any browser/verifier failure closes the run and blocks all remote work.
- Run GitNexus detect-changes before Summary commit; evidence remains untracked/excluded.
- Schema/migrations: N/A; no schema push, D1 command, provider action, or code change.
</verification>

<success_criteria>
- Exact local Dashboard and viewer receipts are ordered and tuple-bound through 8080, with itemId proven by the marker attribute and itemCode by settled visible text.
- Local exact verifier is terminal_passed with provesExternalChain true.
- `local.attempt` remains the same zero-byte untracked file and is never modified or interpreted as verifier success.
- No alternate browser/session export/manual receipt or evidence commit occurs.
- Otherwise the run is immutable and remote execution remains forbidden.
</success_criteria>

## Artifacts this phase produces

- **New/modified code symbols:** none.
- **CLI consumed:** artifact-only `smoke:data-chain:verify --mode local` before and after root IAB; no runner/handoff call.
- **Machine fields consumed:** exact verifier `state`, `aggregate`, `outcome`, `provesExternalChain`, and tuple.
- **Evidence paths:** existing new-run local JSON/Markdown advance only through repository core; remain untracked.
- **Read-only attempt artifact:** existing new-run `local.attempt` remains zero-byte/untracked and is neither modified by repository core nor interpreted by the exact verifier.
- **Tracked path:** `13-18-SUMMARY.md` only.
- **Schema/migrations:** N/A; no schema push.

<output>
Create `.planning/phases/13-full-chain-data-smoke/13-18-SUMMARY.md` with pending verification, root-IAB/core result, exact terminal verifier, baseline comparison, and remote eligibility. Do not include browser secrets/state or commit evidence.
</output>
