---
phase: 08-cost-guardrails
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - packages/crawler/src/lib/image-processor.ts
  - packages/crawler/src/core/optimized-crawler.ts
  - packages/crawler/src/crawlers/comic-crawler.ts
  - packages/crawler/src/crawlers/actor-crawler.ts
  - packages/crawler/src/crawlers/publisher-crawler.ts
  - packages/crawler/scripts/backfill-covers.ts
  - packages/crawler/scripts/test-single-movie.ts
  - packages/crawler/test/image-processor-purpose-policy.test.ts
  - packages/crawler/src/crawlers/__tests__/comic-crawler.chapter-flow.test.ts
autonomous: true
requirements:
  - COST-02
must_haves:
  truths:
    - Crawler and script uploads use the same Phase 8 purpose vocabulary introduced in Plan 01, and every `ImageProcessor.process()` caller must declare `cover`, `avatar`, or `logo` explicitly.
    - `comic_chapter_page` is a first-class rejected intent: chapter-like prefixes such as `comics/<slug>/<chapter>` or any future page-mirror call path are refused at the upload boundary instead of relying on caller discipline.
    - Existing allowed flows remain intact: movie/comic covers, actor avatars, publisher logos, and historical backfill cover uploads still resolve to preview URLs under approved prefixes.
  artifacts:
    - `packages/crawler/src/lib/image-processor.ts` owns the crawler-side purpose + namespace guard.
    - `packages/crawler/test/image-processor-purpose-policy.test.ts` locks the allowed and rejected combinations.
    - `packages/crawler/src/crawlers/__tests__/comic-crawler.chapter-flow.test.ts` continues to protect the “chapter pages never re-enter R2” rule after the new guard lands.
  key_links:
    - Plan 01 defines the shared purpose vocabulary; Plan 02 must import and reuse it rather than inventing crawler-only synonyms.
    - `comic-crawler.ts::storeComicCoverIfEnabled()` is already the explicit comic cover seam; Phase 8 should strengthen it, not replace it.
---

<objective>
Apply the Phase 8 purpose policy to crawler and legacy script upload boundaries for COST-02.

Purpose: Ensure that no future crawler or script change can quietly reintroduce chapter-page uploads or generic image writes into R2.
Output: guarded `ImageProcessor` contract + updated allowed call sites + crawler regression tests
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-cost-guardrails/08-CONTEXT.md
@.planning/phases/08-cost-guardrails/08-RESEARCH.md
@.planning/phases/08-cost-guardrails/08-PATTERNS.md
@.planning/phases/08-cost-guardrails/08-01-PLAN.md
@packages/api-types/src/storage-purpose-policy.ts
@packages/crawler/src/lib/image-processor.ts
@packages/crawler/src/core/optimized-crawler.ts
@packages/crawler/src/crawlers/comic-crawler.ts
@packages/crawler/src/crawlers/actor-crawler.ts
@packages/crawler/src/crawlers/publisher-crawler.ts
@packages/crawler/scripts/backfill-covers.ts
</context>

## Artifacts this phase produces

- `packages/crawler/src/lib/image-processor.ts::assertAllowedCrawlerImageTarget`
- `packages/crawler/src/lib/image-processor.ts::buildApprovedCrawlerPrefix`
- `packages/crawler/test/image-processor-purpose-policy.test.ts`
- `packages/crawler/src/crawlers/__tests__/comic-crawler.chapter-flow.test.ts`
- `.planning/phases/08-cost-guardrails/08-02-SUMMARY.md`
- `.planning/phases/08-cost-guardrails/08-VERIFICATION.md`

<tasks>

<task type="auto">
  <name>Task 1: 把 purpose + namespace guard 挂到 `ImageProcessor` 边界</name>
  <read_first>
- packages/crawler/src/lib/image-processor.ts
- packages/api-types/src/storage-purpose-policy.ts
- packages/crawler/src/core/optimized-crawler.ts
- packages/crawler/src/crawlers/comic-crawler.ts
- packages/crawler/src/crawlers/actor-crawler.ts
- packages/crawler/src/crawlers/publisher-crawler.ts
- .planning/phases/08-cost-guardrails/08-RESEARCH.md
  </read_first>
  <files>
packages/crawler/src/lib/image-processor.ts
packages/crawler/src/core/optimized-crawler.ts
packages/crawler/src/crawlers/comic-crawler.ts
packages/crawler/src/crawlers/actor-crawler.ts
packages/crawler/src/crawlers/publisher-crawler.ts
packages/crawler/test/image-processor-purpose-policy.test.ts
  </files>
  <action>将 `ImageProcessor.process()` 改成显式接收 purpose-aware 输入，而不是纯 `(imageUrl, keyPrefix, filename)`。最小可执行方案是新增一个入参对象，至少包含 `imageUrl`、`purpose`、`keyNamespace`、`filename`、`refererUrl`。在 `image-processor.ts` 内实现一个 narrow guard：`purpose='cover'` 只允许 `movies/<code>` 或 `comics/<slug>`，`purpose='avatar'` 只允许 `actors/<id>`，`purpose='logo'` 只允许 `publishers/<id>`；任何额外 chapter segment、`images/`、或显式 `comic_chapter_page` 意图都要抛出同步错误。然后把 `optimized-crawler.ts`、`comic-crawler.ts::storeComicCoverIfEnabled()`、`actor-crawler.ts`、`publisher-crawler.ts` 的现有 call site 全部改成显式 purpose 调用。新增 `packages/crawler/test/image-processor-purpose-policy.test.ts`，覆盖 allowed combinations 与 rejected combinations，特别是 `comics/<slug>/<chapter>` 被拒绝。</action>
  <acceptance_criteria>
- `ImageProcessor.process()` 的公共签名里存在显式 `purpose`，而不是只靠调用方拼 prefix。
- `cover` 仅允许 `movies/<code>` 与 `comics/<slug>`，`avatar` 仅允许 `actors/<id>`，`logo` 仅允许 `publishers/<id>`。
- `comics/<slug>/<chapter>`、generic `images/`、以及显式 chapter-page intent 会在上传前被拒绝。
- `packages/crawler/test/image-processor-purpose-policy.test.ts` 覆盖至少 1 组 allowed `cover`、1 组 allowed `avatar`、1 组 allowed `logo`、以及 2 组 rejected chapter/generic combinations。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/crawler exec vitest run test/image-processor-purpose-policy.test.ts</automated>
    <automated>pnpm --filter @starye/crawler exec tsc --noEmit</automated>
  </verify>
  <done>crawler 主上传边界具备与 manual upload 同级别的 purpose policy，chapter-page intent 不能再凭误调用绕过。</done>
</task>

<task type="auto">
  <name>Task 2: 把 legacy scripts 和章节回归测试对齐到新 guard</name>
  <read_first>
- packages/crawler/scripts/backfill-covers.ts
- packages/crawler/scripts/test-single-movie.ts
- packages/crawler/src/crawlers/__tests__/comic-crawler.chapter-flow.test.ts
- .planning/phases/08-cost-guardrails/08-PATTERNS.md
  </read_first>
  <files>
packages/crawler/scripts/backfill-covers.ts
packages/crawler/scripts/test-single-movie.ts
packages/crawler/src/crawlers/__tests__/comic-crawler.chapter-flow.test.ts
  </files>
  <action>把仍然会写 R2 的历史 script surfaces 对齐到新的 `ImageProcessor` purpose contract。`backfill-covers.ts` 必须显式传 `purpose='cover'`，并且 namespace 只能是 `movies/<code>`；`test-single-movie.ts` 如调用了真实上传，也要同步改签名。与此同时，扩充 `comic-crawler.chapter-flow.test.ts`：除保留“章节正文图不会调用 `ImageProcessor.process()`”之外，再加入一个 guard regression 场景，证明即便未来有人错误地尝试把 chapter-level namespace 喂给 image processor，也会被拒绝而不是悄悄产出 R2 URL。</action>
  <acceptance_criteria>
- `backfill-covers.ts` 和任何仍会触发真实封面上传的测试脚本都显式传入 `purpose='cover'`。
- `comic-crawler.chapter-flow.test.ts` 继续保护“章节正文图不上传”，并额外覆盖 chapter-like target reject。
- 新 guard 不会破坏 `storeComicCoverIfEnabled()` 的 cover preview URL 回写语义。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/crawler exec vitest run src/crawlers/__tests__/comic-crawler.chapter-flow.test.ts test/image-processor-purpose-policy.test.ts</automated>
    <automated>rg -n "purpose: 'cover'|purpose: 'avatar'|purpose: 'logo'" packages/crawler/src packages/crawler/scripts</automated>
  </verify>
  <done>主链路与历史脚本都已经切到同一个 allowed-purpose contract，后续新增 chapter-page upload 更难静默回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler / scripts -> `ImageProcessor` | 一旦这里没有显式 purpose guard，未来任何脚本都能把错误对象写进 R2 |
| `ImageProcessor` -> R2 | 错误 namespace 会把“允许的 cover/avatar/logo 上传”退化成“任意 prefix 都可写” |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Tampering | `ImageProcessor.process()` | high | mitigate | 在 boundary 级别增加 purpose + namespace allowlist，拒绝 chapter-like targets |
| T-08-05 | Elevation / Policy bypass | legacy `backfill-covers.ts` | medium | mitigate | 把历史脚本也收进同一 contract，避免“主链路安全、脚本侧门不安全” |
| T-08-06 | Regression | comic external image flow | high | mitigate | 保留并扩展 `comic-crawler.chapter-flow.test.ts`，确保 Phase 7 的章节正文图约束不被 Phase 8 误伤 |
</threat_model>

<verification>
- `pnpm --filter @starye/crawler exec vitest run test/image-processor-purpose-policy.test.ts src/crawlers/__tests__/comic-crawler.chapter-flow.test.ts`
- `pnpm --filter @starye/crawler exec tsc --noEmit`
- `rg -n "comics/\\$\\{.*chapter|purpose: 'cover'|purpose: 'avatar'|purpose: 'logo'" packages/crawler/src packages/crawler/scripts`
</verification>

<success_criteria>
- crawler / script uploads都显式带 purpose。
- chapter-page target 会在上传边界被拒绝。
- movie/comic cover、actor avatar、publisher logo 的现有 allowed flows 不被破坏。
</success_criteria>

<output>
Create `.planning/phases/08-cost-guardrails/08-02-SUMMARY.md` when done
</output>
