---
phase: 08-cost-guardrails
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - packages/crawler/scripts/audit-r2-storage.ts
  - packages/crawler/test/audit-r2-storage.test.ts
  - RUNBOOK.md
autonomous: true
requirements:
  - COST-03
  - COST-04
  - COST-05
must_haves:
  truths:
    - `D-06:` `tmp/` keeps a 3-day retention window; `D-07:` `crawler-debug/` keeps a 3-day retention window; `D-08:` `import-staging/` keeps a 7-day retention window.
    - `D-09:` `mappings/backups/` keeps a 14-day retention window, and `D-10:` each mapping series retains only its latest 20 backups as an executable audit rule instead of a doc-only suggestion.
    - `D-12:` `images/` / `comics/<slug>/<chapter>` growth, expired short-term objects, and over-retained backups surface as blocking findings; `D-13:` `system/` and `ops/d1-backups/` remain audit-only inventory and are not auto-failed by short-term lifecycle rules.
    - `D-14:` RUNBOOK records the `$1` warning and `$3` escalation Budget Alert thresholds; `D-15:` it states explicitly that alerts notify only and do not stop billing automatically.
    - `D-16:` `audit-r2-storage` remains a manual-first audit entrypoint, but storage policy changes, cleanup, migrations, and bulk imports must run it first; `D-17:` incomplete DB reference evidence blocks cleanup/lifecycle execution.
  artifacts:
    - `packages/crawler/scripts/audit-r2-storage.ts::guardrailPolicies`
    - `packages/crawler/scripts/audit-r2-storage.ts::evaluateGuardrailBreaches`
    - `packages/crawler/test/audit-r2-storage.test.ts` coverage for age/count/blocked-cleanup logic
    - `RUNBOOK.md` R2 cost guardrail and Budget Alerts chapter
  key_links:
    - Plan 01 defines which new uploads are allowed; Plan 03 defines how to audit the bucket for anything outside that contract.
    - `06-STORAGE-POLICY.md` remains the prefix vocabulary source of truth; this plan adds operational windows and failure handling on top of it.
---

<objective>
Encode lifecycle/audit/budget operations into the audit script and RUNBOOK for COST-03 through COST-05.

Purpose: Give the owner a repeatable, evidence-first way to detect storage-cost drift before any cleanup, lifecycle, migration, or bulk import action.
Output: stronger `audit-r2-storage.ts` guardrails + contract tests + RUNBOOK cost operations chapter
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-cost-guardrails/08-CONTEXT.md
@.planning/phases/08-cost-guardrails/08-RESEARCH.md
@.planning/phases/08-cost-guardrails/08-PATTERNS.md
@.planning/phases/06-storage-policy-audit/06-STORAGE-POLICY.md
@.planning/phases/06-storage-policy-audit/06-R2-AUDIT-DRY-RUN.md
@packages/crawler/scripts/audit-r2-storage.ts
@packages/crawler/test/audit-r2-storage.test.ts
@RUNBOOK.md
</context>

## Artifacts this phase produces

- `packages/crawler/scripts/audit-r2-storage.ts::guardrailPolicies`
- `packages/crawler/scripts/audit-r2-storage.ts::evaluateGuardrailBreaches`
- `packages/crawler/scripts/audit-r2-storage.ts::cleanupBlockedReason`
- `RUNBOOK.md::R2 Cost Guardrails`
- `RUNBOOK.md::Cloudflare Budget Alerts`
- `.planning/phases/08-cost-guardrails/08-03-SUMMARY.md`
- `.planning/phases/08-cost-guardrails/08-VERIFICATION.md`

<tasks>

<task type="auto">
  <name>Task 1: 把 Phase 8 的 age/count/hard-failure 规则编码进 `audit-r2-storage.ts`</name>
  <read_first>
- packages/crawler/scripts/audit-r2-storage.ts
- packages/crawler/test/audit-r2-storage.test.ts
- .planning/phases/06-storage-policy-audit/06-STORAGE-POLICY.md
- .planning/phases/06-storage-policy-audit/06-R2-AUDIT-DRY-RUN.md
- .planning/phases/08-cost-guardrails/08-RESEARCH.md
  </read_first>
  <files>
packages/crawler/scripts/audit-r2-storage.ts
packages/crawler/test/audit-r2-storage.test.ts
  </files>
  <action>在 `audit-r2-storage.ts` 中新增一个显式 guardrail policy 表，并按 Phase 8 决议编码固定阈值：`tmp/` 3 天、`crawler-debug/` 3 天、`import-staging/` 7 天、`mappings/backups/` 14 天，另加“每类 mapping series 仅保留最近 20 份”规则。实现一个纯 helper（例如 `evaluateGuardrailBreaches`），它基于列举结果计算：过期对象数量、超量备份 series、`images/` 仍存在对象、`comics/<slug>/<chapter>` 仍存在对象、以及 `db_reference_status` 为 `missing_credentials` / `partial` / `missing_query_context` 时的 cleanup-blocked 状态。报告 contract 必须继续保留现有 prefix matrix，并新增可 machine-check 的 guardrail 结论字段（例如 `guardrail_status`、`guardrail_findings`、`cleanup_blocked`），让 Markdown/JSON/CSV 都能表达“哪些情况是 hard failure / block cleanup”。补充 `audit-r2-storage.test.ts`，覆盖短期前缀超龄、mapping backups 超过最近 20 份、`system/` 与 `ops/d1-backups/` 不误判 hard failure、以及不完整 DB reference 证据阻断 cleanup recommendation。</action>
  <acceptance_criteria>
- `audit-r2-storage.ts` 中存在一处集中定义的 lifecycle/count guardrail 配置，而不是散落硬编码。
- `tmp/`、`crawler-debug/`、`import-staging/`、`mappings/backups/` 的阈值与 Context 决议完全一致。
- `images/` 与 `comics/<slug>/<chapter>` 一旦有对象，会在报告里被标为 blocking/hard-failure，而不是普通提醒。
- `db_reference_status` 为 `missing_credentials`、`partial`、`missing_query_context` 时，报告会明确阻断 cleanup / lifecycle recommendation。
- `packages/crawler/test/audit-r2-storage.test.ts` 覆盖 age breach、backup count breach、audit-only prefixes、以及 cleanup blocked 场景。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/crawler exec vitest run test/audit-r2-storage.test.ts</automated>
    <automated>rg -n "tmp/|crawler-debug/|import-staging/|mappings/backups/|20|14|cleanup_blocked|guardrail_" packages/crawler/scripts/audit-r2-storage.ts</automated>
  </verify>
  <done>audit 脚本能用精确的 Phase 8 阈值判断 bucket 是否仍处在可接受的成本边界内，并在证据不完整时阻断 cleanup 决策。</done>
</task>

<task type="auto">
  <name>Task 2: 在 RUNBOOK 中正式落地 R2 成本审计、Budget Alerts 和误上传补救流程</name>
  <read_first>
- RUNBOOK.md
- .planning/phases/08-cost-guardrails/08-RESEARCH.md
- .planning/phases/06-storage-policy-audit/06-STORAGE-POLICY.md
- .planning/phases/06-storage-policy-audit/06-VERIFICATION.md
  </read_first>
  <files>RUNBOOK.md</files>
  <action>在 `RUNBOOK.md` 中新增一个 owner-facing 的 R2 成本操作章节，至少包含四块内容。第一块是 lifecycle guidance table：逐项写出 `tmp/`、`crawler-debug/`、`import-staging/`、`mappings/backups/` 的保留窗口和 `mappings/backups/` 的“每类最近 20 份”规则，并明确 `system/` 与 `ops/d1-backups/` 仅做审计记录。第二块是 repeatable audit procedure：给出标准命令 `pnpm --filter @starye/crawler exec tsx scripts/audit-r2-storage.ts --dry-run --strict-env --md-out ... --json-out ... --csv-out ...`，并写明在 storage policy change、cleanup、migration、bulk import 前必须先跑；同时列出 hard failure 条件：`images/` 仍有对象、`comics/<slug>/<chapter>` 仍有对象、过期短期前缀、超龄或超量 mapping backups、以及不完整 DB reference 状态。第三块是 Budget Alerts：写明 Cloudflare Billing -> Budget Alerts 设置的 `$1` 预警和 `$3` 升级提醒阈值，并明确“alerts notify only, they do not stop billing automatically”。第四块是 accidental upload remediation：说明如果 audit 发现新的 `images/` 或 forbidden prefix，先冻结对应 writer / route，保留审计报告，再进入后续 cleanup 评估，而不是直接删除对象。</action>
  <acceptance_criteria>
- `RUNBOOK.md` 中存在单独的 R2 成本 guardrail 章节，而不是零散的阈值句子。
- 文档明确写出 `tmp/`、`crawler-debug/`、`import-staging/`、`mappings/backups/` 的 retention windows 与 `mappings/backups/` 的 recent-20 规则。
- 文档明确写出 `$1` 与 `$3` Budget Alerts 阈值，并包含“notify only / do not stop billing automatically”。
- 文档明确规定：审计结果若为 `missing_credentials`、`partial` 或 `missing_query_context`，cleanup / lifecycle 变更不能继续。
- 文档包含 accidental upload remediation 顺序，而不是建议直接删除 forbidden objects。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Budget Alerts|\\$1|\\$3|notify only|tmp/|crawler-debug/|import-staging/|mappings/backups/|audit-r2-storage.ts --dry-run --strict-env|accidental upload" RUNBOOK.md</automated>
  </verify>
  <done>owner 获得一份可执行的 R2 成本 runbook：知道何时必须先审计、哪些结果会阻断 cleanup，以及 Budget Alerts 只提醒不封顶。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit report -> cleanup / lifecycle action | 如果审计结论不够硬，后续 cleanup 可能在缺少完整证据时误删对象 |
| owner -> Cloudflare Billing UI | Budget Alerts 是人工配置能力；文档若说不清楚“只提醒不封顶”，就会造成错误风险预期 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Repudiation / Unsafe cleanup | audit report contract | high | mitigate | 把 hard failure 与 cleanup-blocked 条件写成 machine-check 字段和 RUNBOOK 步骤 |
| T-08-08 | Information gap | DB reference verification | high | mitigate | 对 `missing_credentials` / `partial` / `missing_query_context` 明确 fail-closed |
| T-08-09 | Misconfiguration | Budget Alerts | medium | mitigate | RUNBOOK 明确 `$1` / `$3` 阈值与“notify only”语义，避免把通知误当作成本封顶 |
</threat_model>

<verification>
- `pnpm --filter @starye/crawler exec vitest run test/audit-r2-storage.test.ts`
- `rg -n "guardrail_|cleanup_blocked|tmp/|crawler-debug/|import-staging/|mappings/backups/" packages/crawler/scripts/audit-r2-storage.ts`
- `rg -n "Budget Alerts|\\$1|\\$3|notify only|audit-r2-storage.ts --dry-run --strict-env|accidental upload" RUNBOOK.md`
</verification>

<success_criteria>
- audit script 能按 Phase 8 的精确阈值报告 hard failure / blocked cleanup。
- RUNBOOK 提供可复制的成本审计命令与误上传处理顺序。
- Budget Alerts 阈值和“只提醒不封顶”边界被正式写入运维手册。
</success_criteria>

<output>
Create `.planning/phases/08-cost-guardrails/08-03-SUMMARY.md` when done
</output>
