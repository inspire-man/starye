---
phase: 26-video-source-and-magnet-availability
plan: 10
type: execute
wave: 5
depends_on: [26-05]
files_modified: [apps/api/src/routes/public/movies/index.ts, apps/api/src/routes/movies/handlers/movies.handler.ts, apps/api/src/routes/movies/services/movie.service.ts, apps/api/src/schemas/movie.ts, apps/api/src/routes/public/movies/__tests__/readiness.test.ts, apps/api/src/routes/movies/__tests__/handlers/movies.handler.test.ts]
autonomous: true
requirements: [VID-01, VID-04, VID-05]
must_haves:
  truths:
    - "D-01..D-04: GET /api/public/movies/:code returns metadata, direct, magnet/TorrServer and playback as four independent current layers with bounded history."
    - "D-14/D-15: the public detail response uses appendStatement -> createServerReadinessProjection authoritative same-revision readback; older revisions are history-only."
    - "metadata.persisted comes from the authoritative revision-bound projection input rather than a fixed true value."
  artifacts:
    - {path: apps/api/src/routes/movies/services/movie.service.ts, provides: "Movie-scoped authoritative current/history readback"}
    - {path: apps/api/src/routes/movies/handlers/movies.handler.ts, provides: "Shared public detail response handler"}
    - {path: apps/api/src/routes/public/movies/index.ts, provides: "Typed GET /api/public/movies/:code route"}
    - {path: apps/api/src/schemas/movie.ts, provides: "Four-layer current/history response schema"}
  key_links:
    - {from: "GET /api/public/movies/:code", to: getMovieDetail, via: "public route registers shared handler and response schema"}
    - {from: getMovieByIdentifier, to: createServerReadinessProjection, via: "authoritative repository current/history for the movie and source revision"}
    - {from: "pnpm --filter api type-check", to: "pnpm --filter @starye/api-types run build", via: "refresh AppType declarations before 26-06 movie-app validation"}
---
<objective>Carry the Phase 26 authoritative four-layer projection through the public movie detail route that MovieDetail actually calls. Purpose: close the server readback chain without substituting the admin crawler-task API. Output: typed same-revision current/history detail response and focused tests.</objective>
<execution_context>@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.agents/skills/starye-hono-rpc/SKILL.md
@.planning/phases/26-video-source-and-magnet-availability/26-CONTEXT.md
@.planning/phases/26-video-source-and-magnet-availability/26-05-SUMMARY.md
@apps/api/src/routes/public/movies/index.ts
@apps/api/src/routes/movies/services/movie.service.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Read authoritative movie-scoped current and history</name><files>apps/api/src/routes/movies/services/movie.service.ts, apps/api/src/routes/movies/handlers/movies.handler.ts, apps/api/src/routes/movies/__tests__/handlers/movies.handler.test.ts</files><behavior>- service resolves the movie then loads its bounded authoritative current/history projection for the current source revision.
- current contains four independent layers; prior revision successes remain labeled history-only.
- absent metadata stays persisted=false and receipt/provider/task success cannot synthesize current.</behavior><action>Run GitNexus upstream impact for `getMovieByIdentifier`, `getMovieDetail` and every changed existing symbol; report callers/flows/risk and warn before HIGH/CRITICAL edits. Extend the movie service to read the Plan 26-05 authoritative movie-scoped current plus bounded history and pass that explicit same-revision data to `createServerReadinessProjection`; remove the existing inference that `movie.updatedAt` means metadata persisted. Update the shared handler to support the public `code` parameter while preserving existing identifier/R18 behavior and to return the service projection unchanged. Test metadata/direct/magnet/playback independence, empty/single/many/tied source aggregation, stale determinate history, revision mismatch history-only, absent metadata, and bounded/redacted output.</action><verify><automated>pnpm --filter api exec vitest run src/routes/movies/__tests__/handlers/movies.handler.test.ts</automated></verify><done>The service/handler returns only authoritative same-revision current plus bounded old-revision history for one movie.</done></task>
<task type="auto" tdd="true"><name>Task 2: Expose the projection on the typed public movie detail route</name><files>apps/api/src/routes/public/movies/index.ts, apps/api/src/schemas/movie.ts, apps/api/src/routes/public/movies/__tests__/readiness.test.ts</files><behavior>- GET /api/public/movies/:code response schema includes Phase 26 current/history and all four current layers.
- public route delegates to the shared detail handler/service chain instead of rebuilding readiness from movie/source rows.
- schema rejects raw URL, credential, signed material, cookie, provider config and unbounded evidence fields.</behavior><action>Run GitNexus upstream impact for `publicMoviesRoutes`, `MovieDetailSchema`, `ReadinessProjectionSchema` and changed route/schema symbols, recording blast radius and warning before HIGH/CRITICAL edits. Replace the public route's duplicate detail projection logic with the shared `getMovieDetail` handler while retaining Hono method chaining, OpenAPI validation and typed `AppType`. Expand the movie response schema for the bounded four-layer current/history DTO created in 26-05. Extend the focused public readiness suite to prove `GET /api/public/movies/:code` returns current only for the current revision, retains old revision in history, exposes explicit metadata absence, and never hardcodes `metadata.persisted: true`. After the focused tests, run API type-check and then build `@starye/api-types` so the exported declaration consumed by 26-06 contains this public response contract before any movie-app validation.</action><verify><automated>pnpm --filter api exec vitest run src/routes/public/movies/__tests__/readiness.test.ts src/routes/movies/__tests__/handlers/movies.handler.test.ts &amp;&amp; pnpm --filter api type-check &amp;&amp; pnpm --filter @starye/api-types run build</automated></verify><done>The exact public endpoint consumed by MovieDetail carries the authoritative same-revision Phase 26 current/history response type, and API type-check followed by api-types build publishes its refreshed AppType declaration for downstream movie-app tests/type-check.</done></task>
</tasks>
<threat_model>ASVS L1. T-26-23 Tampering/high/mitigate: service reads current owner/revision/policy tuple and keeps mismatches history-only. T-26-24 Disclosure/high/mitigate: response schema allowlists bounded projection fields. T-26-25 Elevation/medium/mitigate: shared handler preserves current R18 visibility checks.</threat_model>
<source_coverage_audit>GOAL/VID-01/VID-04/VID-05, D-01..D-04/D-14..D-16, public movie detail readback and explicit metadata authority COVERED. No deferred items.</source_coverage_audit>
<verification>Run focused handler/public-route tests, API type-check, git diff --check and GitNexus detect-changes.</verification>
<success_criteria>MovieDetail's real public endpoint returns one typed, bounded, authoritative four-layer current/history projection.</success_criteria>
<output>Create 26-10-SUMMARY.md.</output>
