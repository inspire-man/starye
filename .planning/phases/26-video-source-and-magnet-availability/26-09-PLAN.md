---
phase: 26-video-source-and-magnet-availability
plan: 09
type: execute
wave: 4
depends_on: [26-03, 26-04]
files_modified: [scripts/local-task-runner.ts, scripts/local-task-runner.e2e.ts, packages/crawler/scripts/target-crawl-mutation.ts, packages/crawler/src/task-runner/__tests__/runner-wiring.test.ts, packages/crawler/src/task-runner/__tests__/production-adapter.test.ts]
autonomous: true
requirements: [VID-02, VID-03, VID-04]
must_haves:
  truths:
    - "D-05..D-12: both real runner entrypoints construct direct and magnet adapters from server-owned configuration after adapter implementation."
    - "createTemplateAdapterRegistry remains selection-only; local and production entrypoints explicitly supply both adapter instances."
    - "Local and production paths select the matching input kind and reject direct/magnet cross-kind snapshots before provider I/O."
  artifacts:
    - {path: scripts/local-task-runner.ts, provides: "Real local direct/magnet construction"}
    - {path: packages/crawler/scripts/target-crawl-mutation.ts, provides: "Real production direct/magnet construction"}
    - {path: packages/crawler/src/task-runner/__tests__/runner-wiring.test.ts, provides: "Entrypoint selection and rejection proof"}
  key_links:
    - {from: "server-owned local/production provider config", to: "direct and magnet adapter factories", via: "explicit entrypoint construction before createTemplateAdapterRegistry"}
    - {from: "signed candidate snapshot", to: "matching adapter execute", via: "real registry selection with cross-kind rejection"}
---
<objective>Wire the completed direct and magnet adapters into both actual execution entrypoints. Purpose: prove Phase 26 operations are executable outside isolated registry tests. Output: tested local and production runner construction paths.</objective>
<execution_context>@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/26-video-source-and-magnet-availability/26-CONTEXT.md
@.planning/phases/26-video-source-and-magnet-availability/26-03-SUMMARY.md
@.planning/phases/26-video-source-and-magnet-availability/26-04-SUMMARY.md
@scripts/local-task-runner.ts
@packages/crawler/scripts/target-crawl-mutation.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Construct Phase 26 adapters in the real local runner</name><files>scripts/local-task-runner.ts, scripts/local-task-runner.e2e.ts, packages/crawler/src/task-runner/__tests__/runner-wiring.test.ts</files><behavior>- local runner config constructs both direct and magnet adapters and then passes them to createTemplateAdapterRegistry.
- direct snapshots select only the direct adapter; magnet snapshots select only the magnet adapter.
- cross-kind snapshots and missing provider configuration fail with bounded reason-specific results before network/provider I/O.</behavior><action>Run GitNexus upstream impact for `loadLocalRunnerConfig`, the local adapter-list construction, and every other changed symbol; record direct callers, flows and risk, and warn before edits for HIGH/CRITICAL results. Extend the ignored local config contract with an explicit server-owned Phase 26 provider section, construct the Plan 26-04 direct adapter and Plan 26-03 magnet adapter from that section, and include both instances in the real `createTemplateAdapterRegistry` list while retaining existing movie/manga/repair/local-proof adapters. Update `local-task-runner.e2e.ts` proof factories to use the same construction contract and to exercise local direct/magnet selection, cross-kind rejection, missing-provider `provider_unavailable`, and redacted evidence. Keep browser-local TorrServer settings outside this contract per D-12/D-16.</action><verify><automated>pnpm --filter @starye/crawler exec vitest run src/task-runner/__tests__/runner-wiring.test.ts</automated></verify><done>The shipped local entrypoint and its E2E proof factory explicitly construct both Phase 26 adapters from server-owned config and reject the wrong input kind.</done></task>
<task type="auto" tdd="true"><name>Task 2: Construct Phase 26 adapters in the production mutation runner</name><files>packages/crawler/scripts/target-crawl-mutation.ts, packages/crawler/src/task-runner/__tests__/runner-wiring.test.ts, packages/crawler/src/task-runner/__tests__/production-adapter.test.ts</files><behavior>- production prepared environment/config constructs direct and magnet adapters after signed candidate claim.
- production selection reaches the matching adapter for each Phase 26 operation and preserves all legacy production adapters.
- direct/magnet cross-kind, client-supplied provider fields and unbound config are rejected.</behavior><action>Run GitNexus upstream impact for `runTargetCrawlerMutation`, `runClaimedProductionCrawlerMutation`, `TargetCrawlerMutationDependencies`, production operation selection and every changed factory; record blast radius and warn before HIGH/CRITICAL edits. Extend the fixed prepared production binding and injected test dependencies with a server-owned Phase 26 provider config factory, construct both adapter factories in the actual registry list after claim/binding validation, and retain existing crawler/repair behavior. The signed snapshot chooses kind; no command body, browser setting or callback supplies provider URL/auth. Test real production selection for direct and magnet, legacy preservation, cross-kind rejection, missing config, and redaction.</action><verify><automated>pnpm --filter @starye/crawler exec vitest run src/task-runner/__tests__/runner-wiring.test.ts src/task-runner/__tests__/production-adapter.test.ts &amp;&amp; pnpm --filter @starye/crawler type-check</automated></verify><done>The shipped production mutation entrypoint executes both Phase 26 adapter kinds through its real construction list with server-owned provider authority.</done></task>
</tasks>
<threat_model>ASVS L1. T-26-20 Spoofing/high/mitigate: signed snapshot kind and fixed prepared binding select adapters. T-26-21 Elevation/high/mitigate: provider URL/auth/config originate only from server-owned local file or production environment. T-26-22 Disclosure/high/mitigate: config and evidence tests enforce bounded redaction.</threat_model>
<source_coverage_audit>GOAL execution reachability, VID-02/VID-03/VID-04, D-05..D-12/D-14..D-16, both real runner entrypoints and selector-only registry semantics COVERED. No deferred items.</source_coverage_audit>
<verification>Run focused local/production runner wiring Vitest, crawler type-check, git diff --check, and GitNexus detect-changes after implementation.</verification>
<success_criteria>Both shipped runner entrypoints construct and select direct/magnet adapters with server-owned configuration and tested cross-kind rejection.</success_criteria>
<output>Create 26-09-SUMMARY.md.</output>
