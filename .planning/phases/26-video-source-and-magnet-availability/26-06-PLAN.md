---
phase: 26-video-source-and-magnet-availability
plan: 06
type: execute
wave: 6
depends_on: [26-05, 26-10]
files_modified: [apps/dashboard/src/lib/api.ts, apps/dashboard/src/views/Crawlers.vue, apps/dashboard/src/views/__test__/Crawlers.test.ts, apps/movie-app/src/types.ts, apps/movie-app/src/lib/api-client.ts, apps/movie-app/src/lib/__tests__/api-client.test.ts, apps/movie-app/src/views/MovieDetail.vue, apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts]
autonomous: true
requirements: [VID-01, VID-05]
must_haves:
  truths:
    - "D-01..D-04: Dashboard and MovieDetail show four stable current rows, exact counts/details and bounded old-revision history."
    - "D-13/D-14: every abnormal finding shows its reason-specific revision-bound action and queued/running/readback receipt progress."
    - "MovieDetail's typed client consumes GET /api/public/movies/:code; it does not read the admin crawler-task endpoint as its detail source."
  artifacts:
    - {path: apps/dashboard/src/views/Crawlers.vue, provides: "Task-result four-layer findings"}
    - {path: apps/movie-app/src/lib/api-client.ts, provides: "Typed public detail projection client"}
    - {path: apps/movie-app/src/views/MovieDetail.vue, provides: "Movie-scoped current/history findings"}
  key_links:
    - {from: apps/dashboard/src/lib/api.ts, to: "26-05 admin command/readback", via: "typed crawler control-plane client"}
    - {from: apps/movie-app/src/lib/api-client.ts, to: "GET /api/public/movies/:code from 26-10", via: "AppType-derived Hono RPC response after @starye/api-types build"}
    - {from: MovieDetail.vue, to: "response.data availability current/history", via: "movieApi.getMovieDetail without raw fetch or optimistic projection"}
---
<objective>Render the same truthful Phase 26 facts and actions on Dashboard and MovieDetail, with each surface using its real typed API chain.</objective>
<execution_context>@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.agents/skills/starye-hono-rpc/SKILL.md
@.agents/skills/starye-ui-components/SKILL.md
@.planning/phases/26-video-source-and-magnet-availability/26-05-SUMMARY.md
@.planning/phases/26-video-source-and-magnet-availability/26-10-SUMMARY.md
@apps/dashboard/src/views/Crawlers.vue
@apps/movie-app/src/views/MovieDetail.vue</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Render authoritative Dashboard task findings and actions</name><files>apps/dashboard/src/lib/api.ts, apps/dashboard/src/views/Crawlers.vue, apps/dashboard/src/views/__test__/Crawlers.test.ts</files><behavior>- task results preserve four layers, reasons, current/history revision, receipt and authoritative stage.
- no_source, source_failed, stale and every degraded reason render exactly their Plan 01 action/guidance.
- queued/running/readback stays inline and receipt/provider success never optimistically changes current.</behavior><action>Run GitNexus upstream impact for the Dashboard API wrapper and `Crawlers` component symbols; record callers/flows/risk and warn before HIGH/CRITICAL edits. Extend the existing typed admin client and render four unframed rows with existing tokens/icons/components, best/available/abnormal counts, bounded detail, current/history revision, freshness and receipt. Drive actions only from the closed D-13 mapping with movie/source revision and render current only from authoritative readback. Add component tests for all reason/action pairs, empty/single/many/tie, stale/history, progress stages, redaction, text fit and accessibility.</action><verify><automated>pnpm --filter dashboard exec vitest run src/views/__test__/Crawlers.test.ts &amp;&amp; pnpm --filter dashboard type-check</automated></verify><done>Dashboard presents actionable task findings without aggregate masking or optimistic current state.</done></task>
<task type="auto" tdd="true"><name>Task 2: Consume and render the typed public MovieDetail projection</name><files>apps/movie-app/src/types.ts, apps/movie-app/src/lib/api-client.ts, apps/movie-app/src/lib/__tests__/api-client.test.ts, apps/movie-app/src/views/MovieDetail.vue, apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts</files><behavior>- movieApi.getMovieDetail uses client.api.public.movies[':code'].$get, preserves four-layer current/history typing, and its exact success path is cast-free rather than using `as unknown as MovieDetail`.
- MovieDetail renders no_source/source_failed/stale/all degraded revision-bound actions and old revision never replaces current.
- compatibility rendering applies only when the Phase 26 projection is absent.</behavior><action>Run GitNexus upstream impact for `movieApi.getMovieDetail`, the local `MovieDetail`/readiness contracts, `MovieDetail` component symbols and all changed existing functions; record blast radius and warn before HIGH/CRITICAL edits. Build `@starye/api-types` before movie-app validation, keep the existing Hono RPC call to `GET /api/public/movies/:code`, and consume the AppType-derived 26-10 response directly. Update `apps/movie-app/src/types.ts` so its local `MovieDetail` and readiness contract is structurally compatible with that response; preserve additional UI-domain fields only through a narrow explicit compatible type, with no broad cast at the client boundary. Add an API-client regression that proves this exact route supplies current/history and that the `movieApi.getMovieDetail` success path contains no broad double cast. Render the same four rows/counts/detail/action/progress semantics as Dashboard from the returned movie detail; never issue a raw availability fetch or derive `metadata.persisted` from movie presence. Test old/current revision, absent metadata, stale history, every reason-specific action, D-16 redaction, responsive fit and accessibility.</action><verify><automated>pnpm --filter @starye/api-types run build &amp;&amp; pnpm --filter @starye/movie-app exec vitest run src/lib/__tests__/api-client.test.ts src/views/__tests__/MovieDetail.dom-contract.test.ts &amp;&amp; pnpm --filter @starye/movie-app type-check</automated></verify><done>The api-types build precedes movie-app validation; MovieDetail consumes the real AppType-derived 26-10 response through a structurally compatible local contract, and the exact getMovieDetail success path is proven cast-free.</done></task>
</tasks>
<threat_model>ASVS L1. T-26-26 Tampering/high/mitigate: both surfaces render authoritative response current only. T-26-27 Disclosure/high/mitigate: clients and components allowlist bounded fields. T-26-28 Repudiation/medium/mitigate: receipt/revision/history remain visible.</threat_model>
<source_coverage_audit>GOAL/VID-01/VID-05 and D-01..D-04/D-13..D-16 UI coverage COMPLETE; Dashboard admin and MovieDetail public route chains are explicit. No deferred items.</source_coverage_audit>
<verification>Run focused Dashboard, movie API-client and MovieDetail tests, both type-checks, git diff --check and GitNexus detect-changes.</verification>
<success_criteria>Dashboard and MovieDetail expose identical honest facts/actions while using their correct typed backend chains.</success_criteria>
<output>Create 26-06-SUMMARY.md.</output>
