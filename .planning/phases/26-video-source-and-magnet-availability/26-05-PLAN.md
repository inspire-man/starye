---
phase: 26-video-source-and-magnet-availability
plan: 05
type: execute
wave: 4
depends_on: [26-03, 26-04]
files_modified: [apps/api/src/domain/crawler-tasks/availability-repository.ts, apps/api/src/domain/movies/source-contract.ts, apps/api/src/domain/video-availability/__tests__/repository-readback.test.ts, apps/api/src/schemas/crawler-tasks.ts, apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/phase26-video-availability.test.ts]
autonomous: true
requirements: [VID-01, VID-04, VID-05]
must_haves:
  truths: ["Authoritative metadata, direct, magnet/TorrServer and playback facts are appended and projected with the same movie/source revision while history retains older revisions.", "D-14: appendStatement precedes createServerReadinessProjection and current promotion.", "D-15: absent, stale or revision-mismatched facts remain explicit/history-only and never promote current.", "createServerReadinessProjection consumes authoritative current/history input and never hardcodes metadata.persisted=true."]
  artifacts:
    - {path: apps/api/src/domain/crawler-tasks/availability-repository.ts, provides: "Authoritative append-first source"}
    - {path: apps/api/src/domain/movies/source-contract.ts, provides: "Server readiness projection"}
    - {path: apps/api/src/routes/admin/crawler-tasks/index.ts, provides: "Typed readback/actions"}
  key_links:
    - {from: appendStatement, to: createServerReadinessProjection, via: "same movie/source revision authoritative readback"}
    - {from: createServerReadinessProjection, to: "admin and public response consumers", via: "one bounded current/history four-layer DTO"}
---
<objective>Persist and expose the authoritative four-layer projection, including metadata persisted, through the existing Phase 25 repository.</objective>
<execution_context>@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/26-video-source-and-magnet-availability/26-CONTEXT.md
@.planning/phases/26-video-source-and-magnet-availability/26-03-SUMMARY.md
@.planning/phases/26-video-source-and-magnet-availability/26-04-SUMMARY.md
@apps/api/src/domain/crawler-tasks/availability-repository.ts
@apps/api/src/domain/movies/source-contract.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Wire authoritative four-layer persistence and current/history projection</name><files>apps/api/src/domain/crawler-tasks/availability-repository.ts, apps/api/src/domain/movies/source-contract.ts, apps/api/src/domain/video-availability/__tests__/repository-readback.test.ts</files><behavior>- success: metadata, direct, magnet/TorrServer and playback current facts share exact movie/source revision, policy and current owner.
- absence: no persisted metadata fact remains explicit and cannot be inferred from task success.
- stale: prior determinate metadata fact is retained stale.
- revision mismatch: append remains history-only and projection current is unchanged.</behavior><action>Run GitNexus upstream impact for `appendStatement`, `createServerReadinessProjection` and every other changed existing symbol; record callers/flows/risk and warn before edits if any result is HIGH/CRITICAL. Reuse bounded existing observation/history JSON and receipt rows; add no table/index. Append metadata/direct/magnet/playback observations through `appendStatement`, read current plus bounded history by authoritative owner/revision/policy/tuple, then pass that explicit readback to `createServerReadinessProjection`. Expand its input/output contract so metadata persisted is taken from the same-revision authoritative fact instead of being fixed true, each of the four layers has current and history semantics, and an old revision can only appear in history. Test success, absent metadata, stale prior determinate fact, mixed layer results, same-revision current promotion and revision mismatch.</action><verify><automated>pnpm --filter api exec vitest run src/domain/video-availability/__tests__/repository-readback.test.ts</automated></verify><done>`appendStatement` followed by same-revision `createServerReadinessProjection` is the single authoritative current/history readback, including an explicit metadata persisted fact.</done></task>
<task type="auto" tdd="true"><name>Task 2: Add typed current/history and reason-specific commands</name><files>apps/api/src/schemas/crawler-tasks.ts, apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/phase26-video-availability.test.ts</files><behavior>- no_source/source_failed/stale/all degraded reasons accept only their Plan 01 action.
- duplicate identity returns existing task; exhausted limit creates none.
- receipt/provider success alone cannot change current or invalidate cache.</behavior><action>Run GitNexus upstream impact for the crawler task schema/route symbols, record blast radius, and warn before HIGH/CRITICAL edits. Define schemas then authenticated Hono handlers. Accept identity/reason/revision/policy/idempotency only, derive execution server-side, append observations before same-revision authoritative current/history readback, and invalidate cache only after matching promotion. Test D-13..D-16, metadata/direct/magnet/playback separation, bounded history, redaction, old revision and all reason-specific actions.</action><verify><automated>pnpm --filter api exec vitest run src/routes/admin/crawler-tasks/phase26-video-availability.test.ts &amp;&amp; pnpm --filter api type-check</automated></verify><done>The admin command/readback boundary exposes the same authoritative four-layer current/history contract consumed later by the public movie detail chain.</done></task>
</tasks>
<threat_model>ASVS L1. T-26-11 Tampering/high/mitigate: append-first CAS and owner/revision checks. T-26-12 Elevation/high/mitigate: auth and closed commands. T-26-13 Disclosure/high/mitigate: response redaction.</threat_model>
<source_coverage_audit>VID-01/04/05, D-13..D-16, metadata authoritative source and bounded existing persistence COVERED.</source_coverage_audit>
<verification>Repository/route tests, API type-check, GitNexus detect-changes.</verification><success_criteria>All current facts are produced by same-revision authoritative readback.</success_criteria><output>Create 26-05-SUMMARY.md.</output>
