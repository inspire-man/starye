---
phase: 14-test-and-operations-hardening
plan: "03"
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - .github/workflows/deploy-auth.yml
  - .github/workflows/deploy-blog.yml
  - .github/workflows/deploy-dashboard.yml
  - .github/workflows/deploy-movie.yml
  - .github/workflows/deploy-comic.yml
  - packages/config/src/deployment-target/__tests__/workflow-contract.test.ts
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "D-02: All five Pages workflows consume the prepared selected-target redirect input during their closed build handoff."
    - "D-03: CI uses only `steps.prepare.outputs` paths and retains `if: always()` cleanup; no workflow reintroduces a static default-domain redirect."
    - "D-04: The workflow inventory test fails for any Pages surface that omits redirect handoff or carries public environment values outside preparation."
  artifacts:
    - path: packages/config/src/deployment-target/__tests__/workflow-contract.test.ts
      provides: Inventory-wide selected redirect-input workflow contract
  key_links:
    - from: .github/workflows/deploy-auth.yml
      to: scripts/target-profile.ts
      via: Prepared redirect-input path passed to closed run-pages-build invocation
      pattern: pages_redirect_input_path
  flagged_assumptions:
    - "FLAGGED unresolved edge TEST-01/unclassified: workflow contents are validated as tracked active configuration, while the later literal audit makes any non-allowlisted default-domain text terminal."
  prohibitions:
    - "provider execution"
    - "inline canonical domain"
    - "public environment outside prepare"
---

<objective>
Complete the CI handoff for selected-target redirect inputs across the five Pages deployment workflows and encode it in the existing inventory-driven workflow contract test.

Purpose: Ensure local and CI Pages builds use the same target-derived redirect artifact rather than letting CI fall back to removed static sources.
Output: Five fixed Pages workflow command lines and one complete contract test.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-test-and-operations-hardening/14-CONTEXT.md
@.planning/phases/14-test-and-operations-hardening/14-RESEARCH.md
@.planning/phases/14-test-and-operations-hardening/14-PATTERNS.md
@.planning/phases/14-test-and-operations-hardening/14-02-SUMMARY.md
@packages/config/src/deployment-target/__tests__/workflow-contract.test.ts
@.github/workflows/deploy-auth.yml
@.github/workflows/deploy-blog.yml
@.github/workflows/deploy-dashboard.yml
@.github/workflows/deploy-movie.yml
@.github/workflows/deploy-comic.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass prepared redirect input through Auth and Blog deployments</name>
  <files>.github/workflows/deploy-auth.yml, .github/workflows/deploy-blog.yml</files>
  <read_first>.github/workflows/deploy-auth.yml; .github/workflows/deploy-blog.yml; packages/config/src/deployment-target/mutation-entry.ts; scripts/target-profile.ts</read_first>
  <action>Update the existing Auth and Blog Pages build steps to pass the exact prepared redirect-input output to `target-profile run-pages-build`, per D-02/D-03. Preserve their explicit target resolution, mapped GitHub Environment, single prepare step, fixed project output, and `if: always()` cleanup. Do not add a direct Pages command, a literal domain, a raw `VITE_`/`NUXT_PUBLIC_` value, credential output, or any provider execution to the workflow.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/workflow-contract.test.ts</automated></verify>
  <acceptance_criteria>Auth and Blog builds receive only their prepared selected-target redirect input and preserve all existing resolver/cleanup boundaries.</acceptance_criteria>
  <done>The two Nuxt Pages workflows cannot depend on a tracked static redirect file.</done>
</task>

<task type="auto">
  <name>Task 2: Pass prepared redirect input through Dashboard, Movie, and Comic deployments</name>
  <files>.github/workflows/deploy-dashboard.yml, .github/workflows/deploy-movie.yml, .github/workflows/deploy-comic.yml</files>
  <read_first>.github/workflows/deploy-dashboard.yml; .github/workflows/deploy-movie.yml; .github/workflows/deploy-comic.yml; packages/config/src/deployment-target/mutation-entry.ts; scripts/target-profile.ts</read_first>
  <action>Apply the same exact prepared redirect-input handoff to Dashboard, Movie, and Comic Pages builds, per D-02/D-03. Keep each closed surface selector and existing feature-input boundary intact, preserve prepared-output-only build paths and final cleanup, and leave remote Actions unexecuted. Do not introduce a caller-supplied host, a project-name-derived origin, inline default domain, or a second mutation preparation step.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/workflow-contract.test.ts</automated></verify>
  <acceptance_criteria>Each Vite Pages workflow uses its selected surface and one prepared redirect input, with no public runtime settings outside preparation.</acceptance_criteria>
  <done>All five Pages deployment workflows have uniform selected-target redirect handoff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce the all-five-workflow redirect handoff contract</name>
  <files>packages/config/src/deployment-target/__tests__/workflow-contract.test.ts</files>
  <read_first>packages/config/src/deployment-target/__tests__/workflow-contract.test.ts; .github/workflows/deploy-auth.yml; .github/workflows/deploy-blog.yml; .github/workflows/deploy-dashboard.yml; .github/workflows/deploy-movie.yml; .github/workflows/deploy-comic.yml</read_first>
  <behavior>
    - Every inventory Pages workflow references the exact redirect-input output in its `run-pages-build` call.
    - The argument remains absent from Worker, prepared-entry, and rollback paths that do not build Pages output.
    - Any Pages workflow that uses an inline domain, omits cleanup, duplicates preparation, or exposes public environment data outside prepare fails deterministically.
  </behavior>
  <action>Before editing the existing workflow-contract test symbol, run GitNexus impact analysis for its test suite entry and report HIGH or CRITICAL output before editing. Add the failing all-five-surface assertion, then make it pass against the workflow changes from Tasks 1 and 2. Keep the existing resolver, secret, and output allowlist assertions; extend them narrowly for the redirect-input output rather than using broad text matching. Before the plan commit, run `npx gitnexus detect-changes --repo starye --scope all` and confirm only workflow-contract and Pages workflow flows changed.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/workflow-contract.test.ts</automated></verify>
  <acceptance_criteria>The inventory test independently proves that all five Pages workflows consume a prepared redirect input and no other workflow type does.</acceptance_criteria>
  <done>CI cannot silently regress to static or default-domain redirect behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI preparation output -> Pages workflow | A generated filesystem path is passed between workflow steps. |
| workflow YAML -> build CLI | Workflow text controls the selected surface and build arguments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03-01 | Tampering | CI Pages build arguments | high | mitigate | Tasks 1 and 2 pass only the prepared output for a closed Pages surface. |
| T-14-03-02 | Information disclosure | workflow environment | medium | mitigate | Tasks preserve public-value confinement to prepare and do not print credentials or outputs. |
| T-14-03-03 | Elevation of privilege | workflow preparation | high | mitigate | Task 3 asserts one resolver-gated prepare step and rejects direct or caller-derived paths. |
| T-14-03-04 | Repudiation | workflow drift | medium | mitigate | Task 3 keeps the explicit inventory and deterministic test failure for an omitted surface. |
</threat_model>

<verification>
Run the workflow-contract Vitest file and `git diff --check`. Before commit, run `npx gitnexus detect-changes --repo starye --scope all` as required by AGENTS.md.
</verification>

<success_criteria>
- Five Pages workflows use the same prepared redirect artifact contract as local builds.
- Workflow static tests reject domain literals, missing path handoff, and unsafe environment placement.
- No workflow is dispatched and no provider service is contacted.
</success_criteria>

<output>
Create `.planning/phases/14-test-and-operations-hardening/14-03-SUMMARY.md` when done.
</output>
