---
phase: 14-test-and-operations-hardening
plan: "07"
type: execute
wave: 6
depends_on: [14-05, 14-06]
files_modified:
  - packages/config/src/deployment-target/requirement-evidence-matrix.ts
  - packages/config/src/deployment-target/__tests__/requirement-evidence-matrix.test.ts
  - scripts/verify-v12-evidence-matrix.ts
  - .planning/phases/14-test-and-operations-hardening/14-EVIDENCE-MATRIX.json
  - .planning/phases/14-test-and-operations-hardening/14-EVIDENCE-MATRIX.md
autonomous: true
requirements: [TEST-07]
must_haves:
  truths:
    - "D-09/D-11: Matrix status comes from canonical verifier evidence, not REQUIREMENTS checkboxes, and keeps DATA-01..06 blocked plus DATA-07/TEST-05 partial."
    - "D-10/D-12: Exactly 30 v1.2 requirements appear once in REQUIREMENTS order, each with source phase, status, local evidence path/anchor, and a Markdown rendering derived from JSON."
    - "Every partial, blocked, or deferred row contains checkpoint/missing-artifact, recovery prerequisite, and next operator command; the validator is read-only."
  artifacts:
    - path: packages/config/src/deployment-target/requirement-evidence-matrix.ts
      provides: Pure schema, canonical-report reconciliation, path/anchor, order, duplicate, and status validation
    - path: scripts/verify-v12-evidence-matrix.ts
      provides: Read-only final matrix CLI
    - path: .planning/phases/14-test-and-operations-hardening/14-EVIDENCE-MATRIX.json
      provides: Canonical 30-row Phase 14 evidence matrix
    - path: .planning/phases/14-test-and-operations-hardening/14-EVIDENCE-MATRIX.md
      provides: Derived review/archive rendering of the canonical JSON
  key_links:
    - from: .planning/phases/14-test-and-operations-hardening/14-EVIDENCE-MATRIX.json
      to: .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
      via: Canonical status and recovery details for DATA-01 through DATA-07 and TEST-05
      pattern: "Requirements Coverage"
    - from: scripts/verify-v12-evidence-matrix.ts
      to: .planning/REQUIREMENTS.md
      via: Parse and compare the ordered v1.2 requirement set without using checkbox state as proof
      pattern: "PROF-01"
  flagged_assumptions:
    - "FLAGGED unresolved edge TEST-07/adjacency: adjacent requirement IDs remain separate rows; group coverage cannot merge or satisfy neighboring IDs."
    - "FLAGGED unresolved edge TEST-07/empty: empty, null, and single-incomplete matrix inputs are invalid rather than vacuously complete."
    - "FLAGGED unresolved edge TEST-07/ordering: output order is the exact v1.2 requirement order parsed from REQUIREMENTS; equal or duplicate IDs are rejected rather than reordered."
  prohibitions:
    - "credentialed commands"
    - "provider commands"
    - "evidence status promotion"
    - "remote artifact paths"
---

<objective>
Produce the canonical 30-row requirement-to-evidence JSON matrix, its deterministic Markdown projection, and a read-only final validator that reconciles all prior phase status honestly.

Purpose: Fulfill TEST-07 with mechanically checkable traceability while preserving the Phase 13 verifier's blocked and partial truth.
Output: Pure validator, static CLI, canonical JSON, and derived Markdown report.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-test-and-operations-hardening/14-CONTEXT.md
@.planning/phases/14-test-and-operations-hardening/14-RESEARCH.md
@.planning/phases/14-test-and-operations-hardening/14-PATTERNS.md
@.planning/phases/14-test-and-operations-hardening/14-05-SUMMARY.md
@.planning/phases/14-test-and-operations-hardening/14-06-SUMMARY.md
@.planning/phases/11-deployment-target-foundation/11-VERIFICATION.md
@.planning/phases/12-cloudflare-config-switching/12-VERIFICATION.md
@.planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md
@scripts/verify-data-chain-smoke.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the strict evidence matrix schema and reconciliation rules</name>
  <files>packages/config/src/deployment-target/requirement-evidence-matrix.ts, packages/config/src/deployment-target/__tests__/requirement-evidence-matrix.test.ts</files>
  <read_first>.planning/REQUIREMENTS.md; .planning/phases/11-deployment-target-foundation/11-VERIFICATION.md; .planning/phases/12-cloudflare-config-switching/12-VERIFICATION.md; .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md; scripts/verify-data-chain-smoke.ts</read_first>
  <behavior>
    - Parser requires all 30 v1.2 IDs exactly once, in source order, with no empty/null rows or silent group merging.
    - Evidence paths are repository-relative existing local files, contain no traversal/absolute/remote locator, and each anchor is present in the referenced file.
    - Phase 13 reconciliation keeps DATA-01 through DATA-06 blocked and DATA-07 plus TEST-05 partial; a checkbox, plan, or passing unit test cannot promote them.
    - Every partial/blocked/deferred row requires checkpoint-or-missing-artifact, recovery prerequisite, and next operator command.
  </behavior>
  <action>Before editing existing deployment-target symbols, run GitNexus impact analysis for any reused verifier helper and report HIGH or CRITICAL output before edit. Write failing pure-validator tests first for complete success, missing/extra/duplicate/out-of-order ID, adjacent-but-distinct IDs, empty/null/single-incomplete rows, invalid status, absolute/traversal/missing path, absent anchor, non-identical Markdown rendering, missing recovery fields, and Phase 13 status inflation. Implement the typed JSON schema, REQUIREMENTS ordered-ID parser, stable renderer, local path/anchor verifier, and canonical Phase 11/12/13 reconciliation. Keep all dependencies injected for tests. Do not spawn a command, open credentials, inspect a provider, normalize duplicates, or infer status from checkboxes.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/requirement-evidence-matrix.test.ts</automated></verify>
  <acceptance_criteria>The model rejects every invalid structural, ordering, local-reference, or status-reconciliation input and preserves canonical blocked/partial status.</acceptance_criteria>
  <done>A pure and comprehensive TEST-07 validator contract exists before artifact population.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the read-only final matrix CLI</name>
  <files>scripts/verify-v12-evidence-matrix.ts, packages/config/src/deployment-target/__tests__/requirement-evidence-matrix.test.ts</files>
  <read_first>scripts/verify-data-chain-smoke.ts; packages/config/src/deployment-target/requirement-evidence-matrix.ts; packages/config/src/deployment-target/__tests__/requirement-evidence-matrix.test.ts; .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md</read_first>
  <behavior>
    - CLI reads the fixed Phase 14 JSON/Markdown paths and local reports, emits deterministic validation output, and returns nonzero for any issue.
    - `--final` requires exact JSON/Markdown agreement and no pending Phase 14 TEST rows, while it allows honest upstream blocked/partial/deferred rows with required recovery data.
    - CLI has no child-process spawn, no credentials, and no deploy/migrate/crawl/smoke/rollback/provider branch.
  </behavior>
  <action>Before modifying existing verifier-style symbols, run GitNexus impact analysis for `inspectDataChainSmokeVerification` if importing or adapting its contract, and report HIGH or CRITICAL output before edit. Mirror only the import-safe CLI shape, deterministic JSON logging, main guard, and nonzero error behavior from the existing verifier. Point the new CLI at fixed local Phase 14 matrix/report paths and the pure validator; add CLI dependency-injection tests for read-only behavior and final-mode reconciliation. It must validate references and render agreement only, per D-09/D-11, and must not call the commands it describes.</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/requirement-evidence-matrix.test.ts</automated></verify>
  <acceptance_criteria>The CLI can prove matrix consistency locally but lacks any code path for provider, credential, or operational execution.</acceptance_criteria>
  <done>TEST-07 has a deterministic read-only final gate implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Populate the 30-row canonical matrix and derived Markdown</name>
  <files>.planning/phases/14-test-and-operations-hardening/14-EVIDENCE-MATRIX.json, .planning/phases/14-test-and-operations-hardening/14-EVIDENCE-MATRIX.md</files>
  <read_first>.planning/REQUIREMENTS.md; .planning/phases/11-deployment-target-foundation/11-VERIFICATION.md; .planning/phases/12-cloudflare-config-switching/12-VERIFICATION.md; .planning/phases/13-full-chain-data-smoke/13-VERIFICATION.md; packages/config/src/deployment-target/requirement-evidence-matrix.ts; scripts/verify-v12-evidence-matrix.ts</read_first>
  <action>Create `14-EVIDENCE-MATRIX.json` as the only canonical data source and render `14-EVIDENCE-MATRIX.md` byte-for-byte from it. Populate exactly the 30 IDs in REQUIREMENTS order with source phase, status, evidence kind/path/anchor, limitations, and for every partial/blocked/deferred row all three recovery fields. Preserve the canonical Phase 13 truth exactly: DATA-01..DATA-06 are blocked, DATA-07 and TEST-05 are partial, and their recovery route is `$gsd-plan-phase 13 --gaps` with the verifier's checkpoint/missing-artifact details; do not schedule or execute a remote command. Record TEST-01, TEST-06, and TEST-07 only after their static contract artifacts/tests are present. Keep every path repo-relative and local. Before commit run `npx gitnexus detect-changes --repo starye --scope all` and confirm changes remain evidence-validator, test, and Phase 14 artifact scope.</action>
  <verify><automated>pnpm --filter @starye/crawler exec node --import tsx ../../scripts/verify-v12-evidence-matrix.ts --final</automated></verify>
  <acceptance_criteria>The final command validates exactly 30 ordered unique rows, derived Markdown identity, local paths/anchors, status reconciliation, and all required recovery fields without invoking an external operation.</acceptance_criteria>
  <done>TEST-07 is fulfilled by a canonical, honest, machine-verifiable matrix that retains all upstream limitations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical reports -> matrix status | Historical verifier data determines claimed requirement state. |
| JSON evidence reference -> local filesystem | Matrix paths and anchors select files and text for validation/rendering. |
| matrix CLI -> final decision | Validator output can influence milestone verification but must not run operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07-01 | Tampering | evidence status reconciliation | high | mitigate | Task 1 parses canonical reports and rejects checkbox/test/plan-based status promotion; Task 3 preserves Phase 13 states. |
| T-14-07-02 | Tampering | JSON paths and anchors | high | mitigate | Task 1 rejects absolute/traversal/missing/remote paths and absent anchors before render. |
| T-14-07-03 | Information disclosure | evidence validator output | medium | mitigate | Tasks restrict output to local paths/anchors/status descriptions and include no credential or artifact-content dump. |
| T-14-07-04 | Repudiation | derived Markdown | medium | mitigate | Tasks require deterministic JSON-to-Markdown identity so review cannot diverge from canonical data. |
| T-14-07-05 | Elevation of privilege | final CLI | high | mitigate | Task 2 has read-only dependencies and explicit tests proving no child/provider/credential operation path. |
</threat_model>

<verification>
Run the requirement-evidence-matrix Vitest suite, `pnpm --filter @starye/crawler exec node --import tsx ../../scripts/verify-v12-evidence-matrix.ts --final`, config type-check, and `git diff --check`. Before commit run `npx gitnexus detect-changes --repo starye --scope all`.
</verification>

<success_criteria>
- TEST-07 maps all and only 30 v1.2 requirements to verifiable local evidence.
- JSON is canonical, Markdown is derived, and all references/statuses are mechanically reconciled.
- Phase 13 blocked/partial rows remain honest and no provider command runs during matrix validation.
</success_criteria>

<output>
Create `.planning/phases/14-test-and-operations-hardening/14-07-SUMMARY.md` when done.
</output>
