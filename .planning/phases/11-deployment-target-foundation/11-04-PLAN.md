---
phase: 11-deployment-target-foundation
plan: "04"
type: execute
gap_closure: true
wave: 4
depends_on:
  - "11-01"
  - "11-02"
  - "11-03"
files_modified:
  - packages/config/src/deployment-target/env-file-block.ts
  - packages/config/src/deployment-target/projection-plan.ts
  - packages/config/src/deployment-target/preflight.ts
  - packages/config/src/deployment-target/live-checks.ts
  - packages/config/src/deployment-target/__tests__/env-file-block.test.ts
  - packages/config/src/deployment-target/__tests__/preflight.test.ts
  - packages/config/src/deployment-target/__tests__/live-checks.test.ts
  - scripts/target-profile.ts
autonomous: true
requirements:
  - PROF-02
  - PROF-03
  - ENV-02
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "D-08: marker updates accept exactly one line-exact, ordered managed block and preserve every operator-managed byte outside it; malformed, duplicate, nested, substring, and isolated markers fail closed."
    - "D-09/D-10/ENV-02: `target-profile preflight` reads all four projection consumers from an explicit `--env-root` or an explicitly documented default scope, then blocks on missing files, marker errors, target-managed mismatch, and missing user-managed secret keys."
    - "D-09: live resource-check failures name the selected non-secret target/resource identity while never including token values."
  artifacts:
    - packages/config/src/deployment-target/env-file-block.ts
    - packages/config/src/deployment-target/preflight.ts
    - scripts/target-profile.ts
    - packages/config/src/deployment-target/live-checks.ts
  key_links:
    - "line-exact marker parser -> safe managed-block update and projection validation"
    - "CLI --env-root -> four final consumer files -> validateProjectedEnv -> runTargetPreflight"
    - "resolved target resource identity -> live-check issue message -> non-secret CLI stderr"
---

<objective>
Close the Phase 11 verification gaps without expanding into Phase 12 runtime, Wrangler, Pages, or GitHub-workflow consumption: make managed env markers unambiguous and fail closed, make local CLI preflight consume complete projection evidence from the four locked files, and make live-check failures operationally identifiable without secret leakage.

Purpose: fulfill D-08, D-09, D-10, ENV-02, and TEST-02 at the existing Phase 11 local validation boundary.
Output: strict marker recognition, complete explicit-scope preflight projection validation, resource-identifying live-check failures, and focused regression tests.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-deployment-target-foundation/11-CONTEXT.md
@.planning/phases/11-deployment-target-foundation/11-VERIFICATION.md
@.planning/phases/11-deployment-target-foundation/11-REVIEW.md
@.planning/phases/11-deployment-target-foundation/11-01-SUMMARY.md
@.planning/phases/11-deployment-target-foundation/11-02-SUMMARY.md
@.planning/phases/11-deployment-target-foundation/11-03-SUMMARY.md
@packages/config/src/deployment-target/env-file-block.ts
@packages/config/src/deployment-target/projection-plan.ts
@packages/config/src/deployment-target/preflight.ts
@packages/config/src/deployment-target/live-checks.ts
@scripts/target-profile.ts
</context>

<source_audit>
## Gap-closure coverage audit

| Source | Explicit gap | Planned coverage |
|---|---|---|
| 11-VERIFICATION.md | Marker substring, duplicate/nested, or isolated marker handling can delete operator-managed values | Task 1 line-exact state parser and preservation regressions |
| 11-VERIFICATION.md | CLI `preflight` does not read four projection consumer files or validate required user secrets | Task 2 explicit `--env-root`/scope reader, complete `validateProjectedEnv` wiring, and CLI tests |
| 11-REVIEW.md | Live-check failure lacks target/resource identity | Task 3 non-secret selected-target/resource error messages and token-redaction regressions |
| CONTEXT D-08 | Only named target-managed keys are changed; all user-managed values remain byte-for-byte | Task 1 fail-closed parser before any cleanup/write path |
| CONTEXT D-09/D-10 | Fail closed on incomplete or stale selected-target local projection | Task 2 passes all validation issue kinds into preflight |
| Requirements | ENV-02, PROF-02, PROF-03, TEST-02 | Tasks 1-3 and focused Vitest plus CLI checks |

No Phase 12 runtime/Wrangler/Pages/GitHub workflow consumption, remote mutation, new dependency, or deploy command is in scope.
</source_audit>

<dependency_graph>
| Task | Needs | Creates | Execution order |
|---|---|---|---|
| 1 | Current block writer and projection key ownership | Strict marker classification that protects all later readers/writers | First |
| 2 | Task 1 marker result plus existing four-file projection plan | CLI-provided complete projection issues for local preflight | Second; shares projection/preflight tests with Task 1 |
| 3 | Existing resolved live-check metadata and preflight issue pass-through | Non-secret target/resource-identifying live-check errors | Third; final regression pass across preflight surface |
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make target-managed block recognition line-exact, unique, and fail closed</name>
  <files>packages/config/src/deployment-target/env-file-block.ts, packages/config/src/deployment-target/__tests__/env-file-block.test.ts</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/phases/11-deployment-target-foundation/11-CONTEXT.md`
    - `.planning/phases/11-deployment-target-foundation/11-VERIFICATION.md`
    - `packages/config/src/deployment-target/env-file-block.ts`
    - `packages/config/src/deployment-target/projection-plan.ts`
    - `packages/config/src/deployment-target/__tests__/env-file-block.test.ts`
  </read_first>
  <behavior>
    - Only a full physical line equal to `targetManagedBlockStart` and one later full physical line equal to `targetManagedBlockEnd` form a managed block.
    - Exactly one non-nested, correctly ordered start/end pair is valid. Marker substrings in comments or values, duplicate markers, nested starts, end-before-start, and isolated start/end markers are malformed and throw before stale-key removal or content reconstruction.
    - On every malformed-marker path, the original content is not transformed, so operator-managed secrets and unrelated text remain byte-for-byte intact.
  </behavior>
  <action>Before changing the existing `applyTargetManagedEnvBlock` symbol, run the AGENTS.md-required GitNexus upstream impact analysis and report the blast radius; warn before continuing if it is HIGH or CRITICAL. Write failing Vitest cases first for marker substrings, duplicate complete blocks, nested/duplicate start markers, duplicate end markers, end-before-start, and each isolated marker. Replace index-based substring detection in `env-file-block.ts` with a line-aware parser/state machine that records exact marker-line offsets and rejects every invalid topology before calling `removeStaleTargetManagedKeys`. Preserve the existing valid-block replacement, CRLF behavior, named-key stale cleanup, and D-08 byte-for-byte outside-block preservation. Do not make marker-like text in operator comments or secret values authoritative.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/env-file-block.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove `# note ${targetManagedBlockStart}`, `VALUE=${targetManagedBlockEnd}`, and marker text embedded in an operator-managed secret do not create a block or trigger cleanup.
    - Each malformed topology throws before returning an update, and the test fixture containing operator-managed secret text remains exactly equal to its original content.
    - A valid single ordered marker pair still replaces only its own body and preserves all outside bytes including CRLF line endings.
  </acceptance_criteria>
  <done>D-08 marker ownership is unambiguous; malformed marker data cannot cause deletion or rewriting of operator-managed values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed complete four-file projection validation into explicit-scope CLI preflight</name>
  <files>packages/config/src/deployment-target/projection-plan.ts, packages/config/src/deployment-target/preflight.ts, packages/config/src/deployment-target/__tests__/preflight.test.ts, scripts/target-profile.ts</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/phases/11-deployment-target-foundation/11-CONTEXT.md`
    - `.planning/phases/11-deployment-target-foundation/11-VERIFICATION.md`
    - `packages/config/src/deployment-target/projection-plan.ts`
    - `packages/config/src/deployment-target/preflight.ts`
    - `packages/config/src/deployment-target/__tests__/preflight.test.ts`
    - `scripts/target-profile.ts`
  </read_first>
  <behavior>
    - `target-profile preflight` obtains its projection evidence from all and only `apps/api/.dev.vars`, `apps/gateway/.dev.vars`, `.env.local`, and `packages/crawler/.env` under an explicit `--env-root`; absent `--env-root` uses the documented repository-root scope only.
    - The preflight builds the selected target projection plan and passes the complete `validateProjectedEnv` result, including `missing-user-managed-secret`, to `runTargetPreflight`; it does not filter away secret-key presence failures.
    - Missing projected files, malformed markers, stale/wrong target-managed values, and absent user-managed secret keys block the CLI with non-secret file/key diagnostics. No local projection file is written by `preflight`.
  </behavior>
  <action>Before changing existing exported symbols in `projection-plan.ts`, `preflight.ts`, or `scripts/target-profile.ts`, run the required GitNexus upstream impact analysis for each affected symbol and report blast radius; halt for an explicit HIGH/CRITICAL warning before edits. Add failing CLI-level Vitest coverage first in `preflight.test.ts`: use `mkdtemp`, `mkdir`, and `writeFile` to create an explicit `--env-root` with all four locked paths, call exported `runTargetProfileCli`, and use a `console.error` spy to assert a valid local preflight succeeds while each missing file, malformed marker, wrong target-managed value, and missing user-managed secret fails without printing the fixture secret. Factor a reusable async read/validate helper if needed so `project-local --check` and `preflight` share the exact four-file reader and `validateProjectedEnv` semantics. Make file absence and strict marker parse failures become typed/non-secret projection issues rather than silently treating content as empty. For `preflight`, resolve the target, build its plan, read every consumer under the selected root, pass every issue to `runTargetPreflight`, then print only issue code plus file/key/expected non-secret identity. Preserve D-09/D-10 fail-closed behavior, keep `--env-root` explicit for test isolation, and document the repository-root default in CLI help. Do not inspect other env files, invent secret values, or add runtime consumer/workflow changes.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/preflight.test.ts</automated>
    <automated>CLI-level Vitest uses `mkdtemp`/`writeFile` plus `runTargetProfileCli` and a `console.error` spy to assert the valid fixture succeeds, while missing file, malformed marker, wrong managed value, and missing user secret all reject without writing a fixture secret to diagnostics.</automated>
    <automated><![CDATA[
$ErrorActionPreference = 'Stop'
$FixtureSecret = 'fixture-secret-never-print'
$BlockStart = '# >>> STARYE TARGET-MANAGED BLOCK >>>'
$BlockEnd = '# <<< STARYE TARGET-MANAGED BLOCK <<<'
$FixtureRoot = Join-Path ([System.IO.Path]::GetTempPath()) ("starye-target-profile-" + [guid]::NewGuid())

function Set-FixtureFile([string]$RelativePath, [string[]]$Lines) {
  $Path = Join-Path $FixtureRoot $RelativePath
  New-Item -ItemType Directory -Force -Path (Split-Path -Parent $Path) | Out-Null
  [System.IO.File]::WriteAllText($Path, ($Lines -join "`n"))
}

function Reset-Fixture {
  Set-FixtureFile 'apps/api/.dev.vars' @(
    $BlockStart, 'STARYE_TARGET_ID=starye-org', 'STARYE_TARGET_DOMAIN=starye.org', 'STARYE_TARGET_ACCOUNT_ID=27c162f54c8f59fff74224775a59937e',
    'WEB_URL=https://starye.org', 'ADMIN_URL=https://dashboard.starye.org', 'BETTER_AUTH_URL=https://starye-auth.pages.dev', 'R2_BUCKET_NAME=starye-media', 'R2_PUBLIC_URL=https://cdn.starye.org', $BlockEnd,
    "BETTER_AUTH_SECRET=$FixtureSecret", "GITHUB_CLIENT_SECRET=$FixtureSecret", "CRAWLER_SECRET=$FixtureSecret", "ADMIN_GITHUB_ID=$FixtureSecret"
  )
  Set-FixtureFile 'apps/gateway/.dev.vars' @(
    $BlockStart, 'STARYE_TARGET_ID=starye-org', 'STARYE_TARGET_DOMAIN=starye.org', 'STARYE_TARGET_ACCOUNT_ID=27c162f54c8f59fff74224775a59937e',
    'API_ORIGIN=https://api.starye.org', 'AUTH_ORIGIN=https://starye-auth.pages.dev', 'DASHBOARD_ORIGIN=https://dashboard.starye.org', 'BLOG_ORIGIN=https://blog.starye.org', 'MOVIE_ORIGIN=https://starye-movie.pages.dev', 'COMIC_ORIGIN=https://starye-comic.pages.dev', 'TAVERN_ORIGIN=https://starye-tavern.pages.dev', $BlockEnd,
    "ADMIN_GITHUB_ID=$FixtureSecret", "SENTRY_DSN=$FixtureSecret"
  )
  Set-FixtureFile '.env.local' @(
    $BlockStart, 'STARYE_TARGET_ID=starye-org', 'STARYE_TARGET_DOMAIN=starye.org', 'STARYE_TARGET_ACCOUNT_ID=27c162f54c8f59fff74224775a59937e',
    'VITE_API_URL=http://localhost:8080', 'NUXT_PUBLIC_API_URL=http://localhost:8080', $BlockEnd, "SENTRY_DSN=$FixtureSecret"
  )
  Set-FixtureFile 'packages/crawler/.env' @(
    $BlockStart, 'STARYE_TARGET_ID=starye-org', 'STARYE_TARGET_DOMAIN=starye.org', 'STARYE_TARGET_ACCOUNT_ID=27c162f54c8f59fff74224775a59937e',
    'CLOUDFLARE_ACCOUNT_ID=27c162f54c8f59fff74224775a59937e', 'R2_BUCKET_NAME=starye-media', 'R2_PUBLIC_URL=https://cdn.starye.org', 'API_URL=https://api.starye.org', $BlockEnd,
    "CRAWLER_SECRET=$FixtureSecret", "R2_ACCESS_KEY_ID=$FixtureSecret", "R2_SECRET_ACCESS_KEY=$FixtureSecret"
  )
}

function Invoke-ExpectedFailure([string]$Name) {
  $Output = & pnpm run target-profile -- preflight --target starye-org --scope local --command validate --wrangler-profile starye-org --env-root $FixtureRoot 2>&1 | Out-String
  if ($LASTEXITCODE -eq 0) { throw "$Name unexpectedly succeeded" }
  if ($Output.Contains($FixtureSecret)) { throw "$Name leaked fixture secret" }
}

try {
  Reset-Fixture
  & pnpm run target-profile -- preflight --target starye-org --scope local --command validate --wrangler-profile starye-org --env-root $FixtureRoot
  if ($LASTEXITCODE -ne 0) { throw 'complete fixture preflight failed' }

  Reset-Fixture
  Remove-Item -LiteralPath (Join-Path $FixtureRoot '.env.local')
  Invoke-ExpectedFailure 'missing projection file'

  Reset-Fixture
  Add-Content -LiteralPath (Join-Path $FixtureRoot 'apps/api/.dev.vars') -Value $BlockStart
  Invoke-ExpectedFailure 'malformed marker'

  Reset-Fixture
  $ApiFile = Join-Path $FixtureRoot 'apps/api/.dev.vars'
  [System.IO.File]::WriteAllText($ApiFile, ([System.IO.File]::ReadAllText($ApiFile).Replace('STARYE_TARGET_ID=starye-org', 'STARYE_TARGET_ID=wrong-target')))
  Invoke-ExpectedFailure 'wrong target-managed value'

  Reset-Fixture
  $CrawlerFile = Join-Path $FixtureRoot 'packages/crawler/.env'
  [System.IO.File]::WriteAllText($CrawlerFile, ([System.IO.File]::ReadAllText($CrawlerFile).Replace("CRAWLER_SECRET=$FixtureSecret", '')))
  Invoke-ExpectedFailure 'missing user-managed secret'
}
finally {
  Remove-Item -LiteralPath $FixtureRoot -Recurse -Force -ErrorAction SilentlyContinue
}
]]></automated>
  </verify>
  <acceptance_criteria>
    - CLI-level Vitest creates its own four-file root with `mkdtemp`/`writeFile`, calls `runTargetProfileCli`, and proves `preflight` fails closed when any one of the four files is absent, has an invalid marker topology, has a wrong/missing managed value, or lacks a required user-managed secret key.
    - The self-contained PowerShell fixture runs a passing local preflight and separately asserts `$LASTEXITCODE -ne 0` for missing file, malformed marker, wrong managed value, and missing user secret; every failure output excludes `fixture-secret-never-print`.
    - Tests prove a complete fixture passes local preflight only with `--wrangler-profile starye-org`, and diagnostics name keys/files without showing fixture secret values.
    - `preflight` never invokes `applyTargetManagedEnvBlock` or writes a local consumer file.
  </acceptance_criteria>
  <done>D-09/D-10 and ENV-02 are enforced by the actual `preflight` command against the complete selected local projection, not by optional caller-supplied issues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Identify failing live resources without exposing credentials</name>
  <files>packages/config/src/deployment-target/live-checks.ts, packages/config/src/deployment-target/__tests__/live-checks.test.ts</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/phases/11-deployment-target-foundation/11-REVIEW.md`
    - `.planning/phases/11-deployment-target-foundation/11-CONTEXT.md`
    - `packages/config/src/deployment-target/live-checks.ts`
    - `packages/config/src/deployment-target/preflight.ts`
    - `packages/config/src/deployment-target/__tests__/live-checks.test.ts`
    - `packages/config/src/deployment-target/__tests__/identity-boundary.test.ts`
  </read_first>
  <behavior>
    - A D1/R2/KV read-check failure identifies the selected target id and the non-secret resource kind plus configured name or namespace id needed by the operator to remediate it.
    - Failure messages never include `CLOUDFLARE_API_TOKEN`, command environment values, injected executor output, or any token-like input.
    - Checks remain argv-only and read-only; this task neither executes remote checks in tests nor adds deploy/mutation behavior.
  </behavior>
  <action>Before changing `runLiveResourceChecks` or related existing symbols, run the AGENTS.md-required GitNexus upstream impact analysis and report the blast radius; issue the required explicit warning before any HIGH/CRITICAL edit. Add failing Vitest coverage for executor-throw, non-zero exit, and missing KV output. Update `live-checks.ts` issue construction so each error includes `resolution.id`, resource kind, and the selected non-secret resource identity (D1/R2 name or KV namespace id), while retaining stable issue codes consumed by `preflight`. Add regression assertions using deliberately secret-looking executor output and environment-style text to prove neither can enter error messages. Keep the executor interface argv-only and do not include argv output verbatim in diagnostics.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/live-checks.test.ts src/deployment-target/__tests__/identity-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each failed D1/R2/KV check message includes `starye-org` plus the relevant non-secret configured resource identity.
    - Tests assert error text excludes test token values, `CLOUDFLARE_API_TOKEN`, and injected executor stdout/stderr-like strings.
    - Existing successful argv-only remote check test remains green without a real Wrangler call.
  </acceptance_criteria>
  <done>Operators can identify which selected target resource failed, while Phase 11 keeps credential material and remote mutation outside its output and scope.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Operator-managed env content -> managed-block parser | Arbitrary local text must not be mistaken for generator authority. |
| Local env files -> CLI preflight | Existing files provide evidence for a selected target and may contain secrets. |
| Wrangler executor result -> preflight diagnostics | Read-check operational output is untrusted and may contain sensitive material. |

## STRIDE register

| Threat ID | Category | Severity | Disposition | Mitigation |
|---|---|---|---|---|
| T-11-04-1 | Tampering | critical | mitigate | Line-exact parser rejects substring, duplicate, nested, unordered, and isolated markers before D-08 cleanup/rewrite. |
| T-11-04-2 | Spoofing | high | mitigate | D-09/D-10 CLI preflight reads all four explicit consumer paths and blocks target/marker/secret-key mismatch. |
| T-11-04-3 | Information Disclosure | high | mitigate | Validation diagnostics expose only file paths, key names, selected target/resource metadata, and stable issue codes; tests assert token/output redaction. |
| T-11-04-4 | Elevation of Privilege | medium | accept | This closure retains Phase 11 argv-only read checks and performs no deploy, remote mutation, or workflow consumption; those capabilities remain Phase 12 scope. |
| T-11-04-5 | Repudiation | low | mitigate | Typed blocking issues and focused CLI/Vitest checks leave reproducible evidence for every local preflight failure. |
</threat_model>

<verification>
Run after all tasks:

- `pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/env-file-block.test.ts src/deployment-target/__tests__/preflight.test.ts src/deployment-target/__tests__/live-checks.test.ts src/deployment-target/__tests__/identity-boundary.test.ts`
- `pnpm --filter @starye/config test --run`
- `pnpm --filter @starye/config exec tsc --noEmit`
- Run the self-contained PowerShell fixture command in Task 2, which creates four fixed consumer files under a fresh temporary `--env-root`, asserts the valid preflight succeeds, then asserts `$LASTEXITCODE -ne 0` and secret redaction for missing file, malformed marker, wrong target-managed value, and missing user secret before cleanup.
- Before any commit, run `npx gitnexus detect-changes --repo starye --scope all` and confirm only the planned deployment-target validation symbols/execution flows changed.
</verification>

<success_criteria>
Phase 11's verifier can demonstrate that malformed markers never authorize deletion, local preflight validates the complete four-file projection including user-secret key presence, and live resource failures identify the non-secret target/resource without leaking credentials. The plan deliberately does not alter Phase 12 consumer surfaces or perform remote mutation.
</success_criteria>

<output>
Create `.planning/phases/11-deployment-target-foundation/11-04-SUMMARY.md` when execution completes.
</output>
