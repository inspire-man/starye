---
phase: 11-deployment-target-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/config/package.json
  - packages/config/tsconfig.json
  - packages/config/vitest.config.ts
  - packages/config/src/index.ts
  - packages/config/src/deployment-target/index.ts
  - packages/config/src/deployment-target/target-profile.schema.ts
  - packages/config/src/deployment-target/target-profiles.ts
  - packages/config/src/deployment-target/target-resolver.ts
  - packages/config/src/deployment-target/__tests__/target-profile.schema.test.ts
  - packages/config/src/deployment-target/__tests__/target-resolver.test.ts
  - pnpm-lock.yaml
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "D-01: TargetProfile models exactly one deployable target; no account/domain/resource overlay composition is accepted."
    - "D-02: TargetProfile stores explicit stable identity for account, domain, Workers, Pages, D1, R2, KV, routes, canonical URLs, and required-secret metadata."
    - "D-03: profile.urls explicitly contains gateway, api, dashboard, auth, blog, movie, comic, and tavern canonical URLs."
    - "D-04: required-secret metadata records names, required flags, and consumers only; no real secret values are allowed in tracked profiles."
    - "D-14/D-15: the tracked `starye-org` profile declares `local.wranglerProfile = starye-org` and `ci.githubEnvironment = starye-org`."
    - "D-16: target resolution requires an explicit selected target id and rejects implicit defaults or legacy aliases."
  artifacts:
    - packages/config/src/deployment-target/target-profile.schema.ts
    - packages/config/src/deployment-target/target-profiles.ts
    - packages/config/src/deployment-target/target-resolver.ts
  key_links:
    - "packages/config package exports -> deployment-target modules"
    - "current singleton Wrangler/env surfaces -> starye-org non-secret profile fixture"
    - "resolver errors -> later projection and preflight commands"
---

<objective>
Create the Phase 11 target profile contract in `@starye/config`: a strict, typed, non-secret model; one tracked current-target profile; and an explicit resolver that fails closed without `--target`.

Purpose: give all Phase 11 projection/preflight work one source of target identity before any deploy/runtime consumer is changed.
Output: package setup, schema, current profile registry, resolver, and focused tests for PROF-01, PROF-02, PROF-03, TEST-02.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-deployment-target-foundation/11-CONTEXT.md
@.planning/phases/11-deployment-target-foundation/11-RESEARCH.md
@.planning/phases/11-deployment-target-foundation/11-PATTERNS.md
@.planning/phases/11-deployment-target-foundation/11-VALIDATION.md
@packages/config/package.json
@packages/config/vitest-base.ts
@packages/api-types/package.json
@apps/api/wrangler.toml
@apps/gateway/wrangler.toml
@apps/dashboard/wrangler.toml
@.env.example
</context>

## Artifacts this phase produces

- Files: `packages/config/tsconfig.json`, `packages/config/vitest.config.ts`, `packages/config/src/index.ts`, `packages/config/src/deployment-target/index.ts`, `target-profile.schema.ts`, `target-profiles.ts`, `target-resolver.ts`.
- Types: `TargetProfile`, `TargetProfileId`, `TargetUrlSurface`, `TargetWorkerSurface`, `TargetPagesSurface`, `RequiredSecretMetadata`, `SecretConsumer`, `TargetResourceBinding`, `TargetResolution`.
- Constants: `targetUrlSurfaceValues`, `requiredTargetUrlSurfaces`, `trackedTargetProfiles`, `trackedTargetProfileMap`.
- Functions: `parseTargetProfile`, `assertTargetProfile`, `formatTargetProfileIssues`, `resolveTargetProfile`, `listTargetProfiles`, `isTargetProfileId`.
- Fields: `id`, `account.id`, `account.name`, `domain.root`, `domain.zoneName`, `local.wranglerProfile` with current value `starye-org`, `ci.githubEnvironment` with current value `starye-org`, `urls.gateway`, `urls.api`, `urls.dashboard`, `urls.auth`, `urls.blog`, `urls.movie`, `urls.comic`, `urls.tavern`, `workers.api`, `workers.gateway`, `pages.dashboard`, `pages.auth`, `pages.blog`, `pages.movie`, `pages.comic`, `pages.tavern`, `resources.d1`, `resources.r2`, `resources.kv`, `requiredSecrets`.

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Single non-secret target profile model and local env/preflight foundation | Covered by plans 11-01, 11-02, 11-03 |
| REQ | PROF-01 | Covered by 11-01 schema/profile registry |
| REQ | PROF-02 | Covered by 11-01 resolver and 11-03 preflight command |
| REQ | PROF-03 | Covered by 11-01 schema errors and 11-03 fail-closed checks |
| REQ | PROF-04 | Covered by 11-03 identity boundary, with D-13/D-14/D-15 metadata from 11-01 |
| REQ | ENV-01 | Covered by 11-02 projection plan and env block writer |
| REQ | ENV-02 | Covered by 11-02 projection validation and 11-03 local preflight |
| REQ | TEST-02 | Covered by all three plans with targeted Vitest suites |
| RESEARCH | `packages/config/src/deployment-target/` schema/registry/resolver | Covered by 11-01 |
| RESEARCH | Marker-aware projection for `apps/api/.dev.vars`, `apps/gateway/.dev.vars`, `.env.local`, `packages/crawler/.env` | Covered by 11-02 |
| RESEARCH | Fail-closed CLI/preflight and local-vs-CI identity validation without workflow consumption changes | Covered by 11-03 |
| RESEARCH | No new external package family; reuse existing Valibot/tsx versions only | Covered by 11-01 and 11-03 package metadata actions |
| VALIDATION | Wave 0 test/config map and CLI smoke checks | Covered by 11-01 package/test harness and all plan verification sections |
| CONTEXT | D-01..D-04 | Covered by 11-01 |
| CONTEXT | D-05..D-08 | Covered by 11-02 |
| CONTEXT | D-09..D-12 | Covered by 11-03 |
| CONTEXT | D-13..D-16 | Covered by 11-01 and 11-03 |

No source items are intentionally omitted. Out-of-scope source items are not planned as implementation work.
</source_audit>

<risk_notes>
Plan 11-01 intentionally keeps package setup, schema, current profile registry, and explicit resolver together because 11-02 and 11-03 both depend on the same `@starye/config` export boundary. The risk is constrained by three focused tasks, no Worker/Pages runtime config consumption changes, targeted Vitest checks, package type-checking, and the phase-level GitNexus detect-changes gate.
</risk_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prepare @starye/config as a tested deployment-target package</name>
  <files>packages/config/package.json, packages/config/tsconfig.json, packages/config/vitest.config.ts, packages/config/src/index.ts, packages/config/src/deployment-target/index.ts, pnpm-lock.yaml</files>
  <read_first>
    - `packages/config/package.json`
    - `packages/config/vitest-base.ts`
    - `packages/api-types/package.json`
    - `packages/crawler/vitest.config.ts`
    - `.planning/phases/11-deployment-target-foundation/11-PATTERNS.md`
  </read_first>
  <behavior>
    - Package scripts expose `test`, `type-check`, and `build` so targeted Phase 11 checks can run under `@starye/config`.
    - Package exports expose `.` and `./deployment-target` without side effects.
    - Dependency metadata reuses existing repo versions for `valibot`; update only importer/lock metadata required for the package to resolve it.
  </behavior>
  <action>Add `type: module`, `test`, `type-check`, `build`, `.` export, and `./deployment-target` export to `packages/config/package.json`. Create `packages/config/tsconfig.json` extending root `tsconfig.json` with `include: ["src/**/*"]`, declaration output, and composite build settings matching `packages/api-types/tsconfig.json` without references to app packages. Create `packages/config/vitest.config.ts` by merging `baseVitestConfig` with Node environment. Create shallow barrels in `packages/config/src/index.ts` and `packages/config/src/deployment-target/index.ts`. If package dependency metadata requires `valibot`, declare the existing repo version `^1.3.1` only and update `pnpm-lock.yaml` without adding new package names or version upgrades.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `packages/config/package.json` contains `test`, `type-check`, `build`, `.`, `./deployment-target`, and existing `./vitest-base` export entries.
    - `packages/config/vitest.config.ts` imports `baseVitestConfig` from `./vitest-base` and does not set a browser DOM environment.
    - `pnpm-lock.yaml` changes, if any, only add/sync importer metadata for existing dependency versions needed by `@starye/config`.
  </acceptance_criteria>
  <done>`@starye/config` can run TypeScript and Vitest commands for new deployment-target source files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define strict TargetProfile schema and current target registry</name>
  <files>packages/config/src/deployment-target/target-profile.schema.ts, packages/config/src/deployment-target/target-profiles.ts, packages/config/src/deployment-target/__tests__/target-profile.schema.test.ts</files>
  <read_first>
    - `.planning/phases/11-deployment-target-foundation/11-CONTEXT.md`
    - `.planning/phases/11-deployment-target-foundation/11-RESEARCH.md`
    - `apps/api/wrangler.toml`
    - `apps/gateway/wrangler.toml`
    - `apps/dashboard/wrangler.toml`
    - `.env.example`
    - `packages/crawler/src/lib/image-processor.ts`
    - `packages/api-types/src/storage-purpose-policy.ts`
  </read_first>
  <behavior>
    - Valid `starye-org` profile includes explicit account/domain/Workers/Pages/D1/R2/KV/routes/URLs/required-secret metadata per D-01, D-02, D-03, D-04.
    - Profile parsing rejects missing URL surfaces, missing resource IDs/names, missing required-secret metadata, and secret-like value fields.
    - The tracked profile id is `starye-org`, with `local.wranglerProfile` set to `starye-org` per D-14 and `ci.githubEnvironment` set to `starye-org` per D-15.
  </behavior>
  <action>Create strict Valibot-backed profile parsing in `target-profile.schema.ts` with literal unions for URL surfaces, Worker surfaces, Pages surfaces, secret consumers, and resource kinds. Add cross-field checks that every required URL surface exists, every Worker/Pages/resource entry has stable identity fields, and `requiredSecrets` entries contain only metadata fields such as name, required, consumers, local file, and CI environment. Create `target-profiles.ts` with one tracked non-secret current-target fixture named `starye-org` reverse-mapped from the current Wrangler/env files, including concrete identity metadata `local.wranglerProfile: "starye-org"` and `ci.githubEnvironment: "starye-org"` per D-14/D-15. Do not store `CLOUDFLARE_API_TOKEN`, OAuth client secrets, R2 secret access keys, Better Auth secret values, or any other real secret value in profile data.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/target-profile.schema.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert `profile.urls` has exactly the required gateway/api/dashboard/auth/blog/movie/comic/tavern surfaces and no root-domain inference path.
    - Tests assert `requiredSecrets` contains metadata names and consumers but rejects objects containing value-like secret fields.
    - Tests assert the tracked `starye-org` profile includes D1 database `starye-db`, R2 bucket `starye-media`, KV binding `CACHE`, API Worker `starye-api`, Gateway Worker `starye-gateway`, and dashboard Pages project `starye-dashboard`.
    - Tests assert the tracked `starye-org` profile has `local.wranglerProfile === "starye-org"` and `ci.githubEnvironment === "starye-org"`.
  </acceptance_criteria>
  <done>Target profile shape and current-target fixture satisfy PROF-01 and the D-01..D-04 non-secret contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add explicit selected-target resolver</name>
  <files>packages/config/src/deployment-target/target-resolver.ts, packages/config/src/deployment-target/__tests__/target-resolver.test.ts, packages/config/src/deployment-target/index.ts</files>
  <read_first>
    - `.planning/phases/11-deployment-target-foundation/11-CONTEXT.md`
    - `packages/api-types/src/storage-purpose-policy.ts`
    - `packages/config/src/deployment-target/target-profile.schema.ts`
    - `packages/config/src/deployment-target/target-profiles.ts`
  </read_first>
  <behavior>
    - Missing target id throws a typed error that tells the operator to pass `--target <id>` per D-16.
    - Unknown target id throws without falling back to `starye-org` or accepting aliases per D-01 and D-11.
    - Valid target id returns a parsed `TargetResolution` with normalized id and profile data.
  </behavior>
  <action>Implement `resolveTargetProfile(targetId, profiles)` and related error types in `target-resolver.ts`. Use `trackedTargetProfileMap` as the default registry, but keep resolver input injectable for tests. Add `listTargetProfiles()` and `isTargetProfileId()` helpers only if they keep resolver behavior explicit. Export resolver symbols through `deployment-target/index.ts`. Do not introduce an implicit default target, account overlay, domain overlay, legacy alias map, or fallback resolver path.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/target-resolver.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Test for `undefined`, empty string, and whitespace target id all fail with the same missing-selected-target category.
    - Test for old alias strings such as `production`, `prod`, `default`, and `starye.org` fails as unknown target.
    - Test for `starye-org` returns a profile already accepted by `parseTargetProfile`.
  </acceptance_criteria>
  <done>Operators and later commands cannot resolve target identity without an explicit valid target id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator profile data -> config package | Unknown profile data is parsed into trusted target metadata. |
| Tracked profile metadata -> local/CI commands | Non-secret target identity influences later mutation commands. |
| package metadata -> install graph | Existing dependency versions must not drift while enabling validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `target-profiles.ts` | high | mitigate | Schema and tests reject secret value fields; tracked profile stores required-secret metadata only per D-04. |
| T-11-02 | Spoofing | `resolveTargetProfile` | high | mitigate | Resolver requires explicit selected target and rejects defaults/aliases per D-16 and D-11. |
| T-11-03 | Tampering | `TargetProfile` resource fields | medium | mitigate | Strict schema requires stable account/domain/resource identifiers and canonical URL surfaces per D-02/D-03. |
| T-11-SC | Tampering | npm package metadata | high | mitigate | Reuse existing `valibot` lockfile version only; do not add new package families or version upgrades in this plan. |
</threat_model>

<verification>
Overall checks for this plan:
- `pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/target-profile.schema.test.ts src/deployment-target/__tests__/target-resolver.test.ts -x`
- `pnpm --filter @starye/config exec tsc --noEmit`
- Before changing existing exported functions, classes, or methods, follow the AGENTS.md GitNexus impact-analysis gate and surface HIGH/CRITICAL blast radius before continuing.
- Before phase commit or closeout: `npx gitnexus detect-changes --repo starye --scope all`
</verification>

<success_criteria>
PROF-01 profile definition exists, PROF-02/PROF-03 resolver/schema failures are automated, TEST-02 has concrete validation tests, and D-01..D-04/D-14..D-16 are explicitly implemented.
</success_criteria>

<output>
Create `.planning/phases/11-deployment-target-foundation/11-01-SUMMARY.md` when done.
</output>
