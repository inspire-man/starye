---
phase: 11-deployment-target-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - packages/config/src/deployment-target/index.ts
  - packages/config/src/deployment-target/projection-plan.ts
  - packages/config/src/deployment-target/env-file-block.ts
  - packages/config/src/deployment-target/__tests__/projection-plan.test.ts
  - packages/config/src/deployment-target/__tests__/env-file-block.test.ts
autonomous: true
requirements:
  - PROF-02
  - PROF-03
  - ENV-01
  - ENV-02
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "D-05: project-local behavior writes the final existing consumer env files directly, not an intermediate config layer."
    - "D-06: projection coverage is exactly `apps/api/.dev.vars`, `apps/gateway/.dev.vars`, `.env.local`, and `packages/crawler/.env`."
    - "D-07: generator-managed keys are separated from user-managed local secrets; missing user-managed secret keys are reported by key name only."
    - "D-08: switching target updates only generator-managed keys, removes old managed residue, and preserves user-managed secrets/non-target values byte-for-byte."
    - "D-10: projection validation compares selected profile output against existing local env files so a correct profile cannot mask stale files."
    - "D-16: projection helpers require an explicit resolved target; they do not read an implicit default."
  artifacts:
    - packages/config/src/deployment-target/projection-plan.ts
    - packages/config/src/deployment-target/env-file-block.ts
  key_links:
    - "TargetResolution -> LocalEnvProjectionPlan"
    - "LocalEnvProjectionPlan -> marker-aware env block updater"
    - "env block updater -> projection validation and preflight inputs"
---

<objective>
Implement marker-aware local env projection for the four existing runtime consumer files, preserving operator-managed secrets while making target-managed identity deterministic and checkable.

Purpose: satisfy ENV-01/ENV-02 without rewriting app runtime config or GitHub workflows.
Output: projection plan helpers, env block updater, preservation tests, stale-managed-key tests, and exports for the preflight plan.
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-deployment-target-foundation/11-CONTEXT.md
@.planning/phases/11-deployment-target-foundation/11-RESEARCH.md
@.planning/phases/11-deployment-target-foundation/11-PATTERNS.md
@.planning/phases/11-deployment-target-foundation/11-VALIDATION.md
@packages/config/src/deployment-target/target-profile.schema.ts
@packages/config/src/deployment-target/target-profiles.ts
@packages/config/src/deployment-target/target-resolver.ts
@.env.example
@packages/crawler/scripts/check-config.ts
</context>

## Artifacts this phase produces

- Files: `projection-plan.ts`, `env-file-block.ts`, `projection-plan.test.ts`, `env-file-block.test.ts`.
- Types: `LocalEnvTargetFile`, `TargetManagedEnvKey`, `UserManagedSecretKey`, `LocalEnvProjectionEntry`, `LocalEnvProjectionPlan`, `ProjectionValidationIssue`, `EnvFileBlockUpdate`.
- Constants: `targetManagedBlockStart`, `targetManagedBlockEnd`, `localEnvTargetFiles`, `targetManagedEnvKeysByFile`, `userManagedSecretKeysByFile`.
- Functions: `buildLocalEnvProjectionPlan`, `assertProjectionPlanComplete`, `readProjectedEnvValues`, `validateProjectedEnv`, `applyTargetManagedEnvBlock`, `removeStaleTargetManagedKeys`.
- Target files modeled by code: `apps/api/.dev.vars`, `apps/gateway/.dev.vars`, `.env.local`, `packages/crawler/.env`.
- Managed marker fields: `STARYE_TARGET_ID`, `STARYE_TARGET_DOMAIN`, `STARYE_TARGET_ACCOUNT_ID` in each generated target-managed block.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the local env projection plan</name>
  <files>packages/config/src/deployment-target/projection-plan.ts, packages/config/src/deployment-target/__tests__/projection-plan.test.ts, packages/config/src/deployment-target/index.ts</files>
  <read_first>
    - `.planning/phases/11-deployment-target-foundation/11-CONTEXT.md`
    - `.planning/phases/11-deployment-target-foundation/11-PATTERNS.md`
    - `packages/config/src/deployment-target/target-profile.schema.ts`
    - `packages/config/src/deployment-target/target-resolver.ts`
    - `.env.example`
    - `packages/crawler/scripts/check-config.ts`
  </read_first>
  <behavior>
    - Projection plan covers exactly the four D-06 target files and maps each to target-managed keys.
    - Public browser env values use `http://localhost:8080` as the local canonical gateway entry.
    - User-managed secret keys are listed for validation by key name and are never populated from `TargetProfile`.
  </behavior>
  <action>Create `projection-plan.ts` that accepts `TargetResolution` and returns a `LocalEnvProjectionPlan` for `apps/api/.dev.vars`, `apps/gateway/.dev.vars`, `.env.local`, and `packages/crawler/.env` per D-05 and D-06. Include target identity markers `STARYE_TARGET_ID`, `STARYE_TARGET_DOMAIN`, and `STARYE_TARGET_ACCOUNT_ID` in each managed block. Map resource/public keys such as `WEB_URL`, `ADMIN_URL`, `BETTER_AUTH_URL`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL`, `API_ORIGIN`, `AUTH_ORIGIN`, `DASHBOARD_ORIGIN`, `BLOG_ORIGIN`, `MOVIE_ORIGIN`, `COMIC_ORIGIN`, `TAVERN_ORIGIN`, `VITE_API_URL`, `NUXT_PUBLIC_API_URL`, `API_URL`, and `CLOUDFLARE_ACCOUNT_ID` from the resolved profile. Keep secret keys such as `BETTER_AUTH_SECRET`, `GITHUB_CLIENT_SECRET`, `CRAWLER_SECRET`, `R2_ACCESS_KEY_ID`, and `R2_SECRET_ACCESS_KEY` in a user-managed key list only. Export projection symbols through `deployment-target/index.ts`.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/projection-plan.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts the plan contains exactly four target files: `apps/api/.dev.vars`, `apps/gateway/.dev.vars`, `.env.local`, `packages/crawler/.env`.
    - Test asserts root public env entries resolve through `http://localhost:8080`, not direct app ports.
    - Test asserts known user-managed secret keys are not assigned concrete values by the projection plan.
    - Test asserts projection validation reports missing user-managed secret keys by key name only.
  </acceptance_criteria>
  <done>ENV-01 has a deterministic projection plan and ENV-02 has checkable local env completeness inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add marker-aware env file block updater</name>
  <files>packages/config/src/deployment-target/env-file-block.ts, packages/config/src/deployment-target/__tests__/env-file-block.test.ts, packages/config/src/deployment-target/index.ts</files>
  <read_first>
    - `.planning/phases/11-deployment-target-foundation/11-CONTEXT.md`
    - `.planning/phases/11-deployment-target-foundation/11-PATTERNS.md`
    - `packages/config/src/deployment-target/projection-plan.ts`
    - `packages/crawler/test/audit-r2-storage.test.ts`
    - `packages/crawler/scripts/audit-r2-storage.ts`
  </read_first>
  <behavior>
    - Applying a managed block preserves every line outside the block byte-for-byte.
    - Applying a new target removes the prior managed block and known stale managed keys.
    - Applying a block never deletes user-managed secret keys or unrelated local values.
  </behavior>
  <action>Implement pure string helpers in `env-file-block.ts` before any file I/O: `applyTargetManagedEnvBlock`, `removeStaleTargetManagedKeys`, and `serializeTargetManagedEntries`. Use explicit start/end marker constants so preflight can find the target-managed region. Only remove keys listed in `targetManagedEnvKeysByFile`; never remove keys listed in `userManagedSecretKeysByFile`. Keep write-capable wrappers small and injectable so tests use in-memory strings. Do not rewrite whole env files from scratch.</action>
  <verify>
    <automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/env-file-block.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Test fixture with user-managed secrets before and after the block remains byte-for-byte identical outside marker boundaries.
    - Test fixture with an old `STARYE_TARGET_ID` block is replaced by the new selected target block.
    - Test fixture with known stale managed keys outside markers removes only those keys and preserves unrelated keys.
    - Test fixture with missing file content produces a managed block without claiming user-managed secrets are present.
  </acceptance_criteria>
  <done>D-07 and D-08 are enforced by pure projection writer behavior with preservation tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Target profile -> ignored env files | Non-secret target metadata is written into local runtime env files. |
| Generator-managed block -> operator-managed lines | Automated updates must not alter local secrets or unrelated values. |
| Env file content -> preflight validation | Parsed local files become evidence for target consistency checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Information Disclosure | `projection-plan.ts` | high | mitigate | Projection never fills user-managed secret values and reports missing secrets by key name only per D-07. |
| T-11-05 | Tampering | `env-file-block.ts` | high | mitigate | Marker-aware updater preserves all non-managed lines and removes only known target-managed residue per D-08. |
| T-11-06 | Spoofing | local env markers | medium | mitigate | Each managed block includes target id/domain/account markers validated against the selected profile per D-10. |
| T-11-SC | Tampering | npm package installs | high | mitigate | No package additions in this plan; use existing workspace tooling from 11-01. |
</threat_model>

<verification>
Overall checks for this plan:
- `pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/projection-plan.test.ts src/deployment-target/__tests__/env-file-block.test.ts -x`
- `pnpm --filter @starye/config exec tsc --noEmit`
- Before changing existing exported functions, classes, or methods, follow the AGENTS.md GitNexus impact-analysis gate and surface HIGH/CRITICAL blast radius before continuing.
- Before phase commit or closeout: `npx gitnexus detect-changes --repo starye --scope all`
</verification>

<success_criteria>
ENV-01 projection and ENV-02 completeness validation are implemented without modifying app runtime consumers or CI workflow behavior, and D-05..D-08/D-10/D-16 are explicitly enforced.
</success_criteria>

<output>
Create `.planning/phases/11-deployment-target-foundation/11-02-SUMMARY.md` when done.
</output>
