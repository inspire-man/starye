---
phase: 02-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/gateway/src/dashboard-guard.ts
  - apps/gateway/src/index.ts
  - apps/gateway/wrangler.toml
  - apps/gateway/vitest.config.ts
  - apps/gateway/src/__tests__/dashboard-guard.test.ts
autonomous: true
requirements:
  - ACCESS-01
  - ACCESS-02
  - PUBSEC-01
  - PUBSEC-02

must_haves:
  truths:
    - "匿名访问 /dashboard/* 被 302 重定向到 /auth/login?next=<encoded_path>"
    - "非白名单账号访问 /dashboard/* 被 302 重定向到 /auth/login?error=not_admin&next=<encoded_path>"
    - "白名单账号访问 /dashboard/* 正常 proxy 到 Pages"
    - "GET /robots.txt 返回包含 Disallow: /dashboard 的纯文本"
    - "/dashboard/* 响应包含 X-Robots-Tag: noindex, nofollow 响应头"
    - "/api/admin/* 响应包含 X-Robots-Tag: noindex, nofollow 响应头"
  artifacts:
    - path: "apps/gateway/src/dashboard-guard.ts"
      provides: "checkDashboardAuth 函数 + isInAdminWhitelist 函数 + L1 session 缓存"
      exports: ["checkDashboardAuth", "isInAdminWhitelist", "DashboardAuthResult"]
    - path: "apps/gateway/src/index.ts"
      provides: "/robots.txt 路由 + /dashboard/* 前置拦截 + X-Robots-Tag 注入"
      contains: "checkDashboardAuth"
    - path: "apps/gateway/wrangler.toml"
      provides: "ADMIN_GITHUB_ID secret 声明注释"
      contains: "ADMIN_GITHUB_ID"
    - path: "apps/gateway/vitest.config.ts"
      provides: "Gateway vitest 配置"
      min_lines: 10
    - path: "apps/gateway/src/__tests__/dashboard-guard.test.ts"
      provides: "ACCESS-01/02/PUBSEC-01/02 自动化测试"
      min_lines: 60
  key_links:
    - from: "apps/gateway/src/index.ts"
      to: "apps/gateway/src/dashboard-guard.ts"
      via: "import checkDashboardAuth"
      pattern: "checkDashboardAuth"
    - from: "apps/gateway/src/dashboard-guard.ts"
      to: "API_ORIGIN/api/auth/get-session"
      via: "fetch 获取 session"
      pattern: "fetch.*api/auth/get-session"
---

<objective>
新建 Gateway `dashboard-guard.ts` 模块实现 `/dashboard/*` 前置鉴权拦截，在 `apps/gateway/src/index.ts` 中接入 `/robots.txt` 路由和 `X-Robots-Tag` 响应头注入，并建立 Gateway vitest 测试基础设施。

Purpose: D-01 要求 Gateway Worker 层前置拦截 `/dashboard/*`，通过 fetch `/api/auth/get-session` 获取 session 后判断 `user.githubId` 是否在 `ADMIN_GITHUB_ID` 白名单中。D-02 要求 L1 session 缓存 30s 避免每次静态资源请求都打 API。D-15 要求 Gateway 统一提供 `/robots.txt` 并注入 `X-Robots-Tag`。本 plan 实现 Gateway 侧的完整访问控制和 SEO 防护。

Output: `dashboard-guard.ts` 导出 `checkDashboardAuth` 和 `isInAdminWhitelist`，`index.ts` 接入拦截逻辑，Gateway vitest 配置建立，测试覆盖 ACCESS-01/02/PUBSEC-01/02。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard/02-CONTEXT.md
@.planning/phases/02-dashboard/02-RESEARCH.md
@.planning/phases/02-dashboard/02-PATTERNS.md
@.planning/phases/02-dashboard/02-VALIDATION.md
@.planning/phases/02-dashboard/02-01-SUMMARY.md
</context>

<interfaces>
<!-- 从现有 Gateway 代码提取的关键接口 -->

From apps/gateway/src/index.ts（当前路由分支结构，line 11-108）:
```typescript
interface Env {
  API_ORIGIN?: string
  DASHBOARD_ORIGIN?: string
  AUTH_ORIGIN?: string
  BLOG_ORIGIN?: string
  MOVIE_ORIGIN?: string
  COMIC_ORIGIN?: string
  TAVERN_ORIGIN?: string
  CACHE: KVNamespace
  // ADMIN_GITHUB_ID 需要新增
}

// 路由分支结构（line 39-107）
// /dashboard/* 分支已存在，当前直接 cachedProxy，需要在前面加鉴权
if (path.startsWith('/dashboard')) {
  // 当前：直接 proxy，无鉴权
  return cachedProxy(request, target, pathRewrite, { bypassCache: true, executionCtx: ctx })
}
```

From apps/gateway/src/cache-middleware.ts（decorateResponse 模式，line 339-394）:
```typescript
// decorateResponse 已有响应头注入位
// X-Robots-Tag 可以在 proxy() 函数内注入
function decorateResponse(response: Response, policy: CachePolicy, url: URL): Response {
  const headers = new Headers(response.headers)
  // ... 现有头注入 ...
  return new Response(response.body, { status: response.status, statusText: response.statusText, headers })
}
```

From apps/api/src/lib/auth.ts（cookie 名称已确认）:
```typescript
// cookiePrefix: 'starye' → session cookie 名为 starye.session_token
advanced: {
  cookiePrefix: 'starye',
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 dashboard-guard.ts + Gateway vitest 配置</name>
  <files>apps/gateway/src/dashboard-guard.ts, apps/gateway/vitest.config.ts</files>

  <read_first>
    - apps/gateway/src/cache-middleware.ts（参考模块顶层常量 + 纯函数导出模式，line 1-57）
    - apps/api/vitest.config.ts（参考 vitest 配置结构）
    - apps/gateway/package.json（确认包名 @starye/gateway，确认是否有 test script）
    - .planning/phases/02-dashboard/02-PATTERNS.md（lines 92-167，dashboard-guard.ts 完整模式）
  </read_first>

  <behavior>
    - isInAdminWhitelist('12345', '12345') 返回 true
    - isInAdminWhitelist('67890', '12345,67890') 返回 true（逗号分隔）
    - isInAdminWhitelist('99999', '12345') 返回 false
    - isInAdminWhitelist('12345', undefined) 返回 false
    - checkDashboardAuth 无 cookie 返回 { allowed: false, reason: 'no_session' }
    - checkDashboardAuth session API 返回无 user 时返回 { allowed: false, reason: 'no_session' }
    - checkDashboardAuth githubId 在白名单中返回 { allowed: true }
    - checkDashboardAuth githubId 不在白名单中返回 { allowed: false, reason: 'not_admin' }
    - L1 缓存命中时不发 fetch 请求（30s TTL）
  </behavior>

  <action>
**新建 apps/gateway/src/dashboard-guard.ts：**

参考 PATTERNS.md line 92-167 的完整模式：

```typescript
/**
 * Gateway dashboard 鉴权守卫
 * 覆盖：ACCESS-01（未登录 302）、ACCESS-02（白名单判断）
 */

// 模块顶层：Worker 实例生命周期内有效的 L1 缓存（D-02）
interface CachedSession {
  githubId: string
  expiresAt: number
}
const sessionCache = new Map<string, CachedSession>()
const SESSION_CACHE_TTL = 30_000 // 30s，固定不配置化

export interface DashboardAuthResult {
  allowed: boolean
  reason?: 'no_session' | 'not_admin'
}

/**
 * 判断 githubId 是否在 ADMIN_GITHUB_ID 白名单中（D-03）
 * 支持单个 ID（"12345678"）或逗号分隔多个（"12345,67890"）
 */
export function isInAdminWhitelist(githubId: string, adminEnv?: string): boolean {
  if (!adminEnv) return false
  return adminEnv.split(',').map(s => s.trim()).includes(String(githubId))
}

/**
 * 检查 dashboard 访问权限（D-01）
 * 1. 从 cookie 提取 session token
 * 2. 查 L1 缓存（30s TTL）
 * 3. 缓存 miss 时 fetch API_ORIGIN/api/auth/get-session
 * 4. 判断 user.githubId 是否在 ADMIN_GITHUB_ID 白名单中
 */
export async function checkDashboardAuth(
  request: Request,
  env: { API_ORIGIN?: string, ADMIN_GITHUB_ID?: string },
): Promise<DashboardAuthResult> {
  const cookie = request.headers.get('cookie') || ''
  // cookie 名称：starye.session_token（来自 auth.ts cookiePrefix: 'starye'）
  const tokenMatch = cookie.match(/starye\.session_token=([^;]+)/)
  if (!tokenMatch) return { allowed: false, reason: 'no_session' }

  const token = decodeURIComponent(tokenMatch[1])
  const now = Date.now()

  // L1 缓存查询（best-effort 加速，不是安全边界）
  const cached = sessionCache.get(token)
  if (cached && cached.expiresAt > now) {
    return isInAdminWhitelist(cached.githubId, env.ADMIN_GITHUB_ID)
      ? { allowed: true }
      : { allowed: false, reason: 'not_admin' }
  }

  // 缓存 miss：fetch API 获取 session
  const apiOrigin = env.API_ORIGIN || 'http://127.0.0.1:8787'
  let data: any
  try {
    const resp = await fetch(`${apiOrigin}/api/auth/get-session`, {
      headers: { cookie },
    })
    if (!resp.ok) return { allowed: false, reason: 'no_session' }
    data = await resp.json()
  }
  catch {
    // fetch 失败（API 不可达）时 fail-closed，拒绝访问
    return { allowed: false, reason: 'no_session' }
  }

  if (!data?.user) return { allowed: false, reason: 'no_session' }

  // githubId 来自 Better Auth additionalFields（Plan 01 已注入）
  const githubId = String(data.user.githubId || '')
  if (!githubId) return { allowed: false, reason: 'not_admin' }

  // 写入 L1 缓存
  sessionCache.set(token, { githubId, expiresAt: now + SESSION_CACHE_TTL })

  return isInAdminWhitelist(githubId, env.ADMIN_GITHUB_ID)
    ? { allowed: true }
    : { allowed: false, reason: 'not_admin' }
}
```

**新建 apps/gateway/vitest.config.ts：**

参考 `apps/api/vitest.config.ts` 结构：

```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
    globals: false,
  },
})
```

**注意：** Gateway 目前无 vitest 配置，需要确认 `apps/gateway/package.json` 是否有 `vitest` devDependency。如果没有，需要在 `apps/gateway/package.json` 的 `devDependencies` 中添加 `"vitest": "^4.1.4"` 并在 `scripts` 中添加 `"test": "vitest"`。
  </action>

  <verify>
    <automated>pnpm --filter @starye/gateway test --run</automated>
  </verify>

  <done>
- `apps/gateway/src/dashboard-guard.ts` 存在，导出 `checkDashboardAuth`、`isInAdminWhitelist`、`DashboardAuthResult`
- `apps/gateway/vitest.config.ts` 存在
- `sessionCache` 是模块级 Map，TTL 30s
- cookie 正则 `/starye\.session_token=([^;]+)/` 正确
- fetch 失败时 fail-closed（返回 no_session）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gateway index.ts 接入拦截逻辑 + 测试文件</name>
  <files>apps/gateway/src/index.ts, apps/gateway/wrangler.toml, apps/gateway/src/__tests__/dashboard-guard.test.ts</files>

  <read_first>
    - apps/gateway/src/index.ts（完整读取，确认路由分支结构、Env 接口、proxy 函数位置）
    - apps/gateway/wrangler.toml（确认 [vars] 块结构）
    - .planning/phases/02-dashboard/02-PATTERNS.md（lines 31-90，index.ts 修改模式；lines 706-771，测试文件模式）
  </read_first>

  <behavior>
    - /robots.txt 路由在所有 proxy 分支之前 match，返回纯文本
    - /dashboard/* 分支在 cachedProxy 调用之前执行 checkDashboardAuth
    - 未登录时 302 到 /auth/login?next=<encoded_path>
    - 非白名单时 302 到 /auth/login?error=not_admin&next=<encoded_path>
    - /dashboard 无尾斜杠时 301 到 /dashboard/
    - proxy 函数对 /dashboard/* 和 /api/admin/* 响应注入 X-Robots-Tag: noindex, nofollow
  </behavior>

  <action>
**修改 apps/gateway/src/index.ts：**

1. **在 `Env` 接口中新增 `ADMIN_GITHUB_ID`**（line 11-20 附近）：
```typescript
interface Env {
  API_ORIGIN?: string
  DASHBOARD_ORIGIN?: string
  // ... 现有字段保留 ...
  ADMIN_GITHUB_ID?: string  // 新增：逗号分隔的 GitHub ID 白名单（D-03）
}
```

2. **在文件顶部 import `checkDashboardAuth`**：
```typescript
import { checkDashboardAuth } from './dashboard-guard'
```

3. **在所有 proxy 分支之前添加 `/robots.txt` 路由**（参考 PATTERNS.md line 47-53，放在 `if (path.startsWith('/api'))` 等分支之前）：
```typescript
// D-15：robots.txt 在所有 proxy 分支之前 match
if (path === '/robots.txt') {
  return new Response(
    'User-agent: *\nDisallow: /dashboard\nDisallow: /auth\nDisallow: /api\n',
    { headers: { 'Content-Type': 'text/plain; charset=utf-8' } },
  )
}
```

4. **修改 `/dashboard` 分支，在 `cachedProxy` 调用之前加鉴权**（参考 PATTERNS.md line 55-69）：
```typescript
if (path.startsWith('/dashboard')) {
  if (path === '/dashboard') {
    return Response.redirect(`${url.origin}/dashboard/`, 301)
  }
  // D-01：Gateway 前置鉴权，在 cachedProxy 之前执行
  const authResult = await checkDashboardAuth(request, env)
  if (!authResult.allowed) {
    const next = encodeURIComponent(url.pathname + url.search)
    const errorParam = authResult.reason === 'not_admin' ? '&error=not_admin' : ''
    return Response.redirect(`${url.origin}/auth/login?next=${next}${errorParam}`, 302)
  }
  const target = isLocal ? 'http://localhost:5173' : (env.DASHBOARD_ORIGIN || 'http://localhost:5173')
  const pathRewrite = isLocal ? undefined : (p: string) => p.replace(/^\/dashboard/, '') || '/'
  return cachedProxy(request, target, pathRewrite, { bypassCache: true, executionCtx: ctx })
}
```

5. **在 `proxy()` 函数内注入 `X-Robots-Tag`**（找到 proxy 函数中 `fetch` 返回后的位置）：

新建辅助函数 `injectRobotsTag`（放在 `index.ts` 文件顶层，`fetch` handler 之外），然后在 proxy 函数的 fetch 返回后调用：

```typescript
// apps/gateway/src/index.ts — 文件顶层辅助函数（D-15）
function injectRobotsTag(response: Response, pathname: string): Response {
  if (pathname.startsWith('/dashboard') || pathname.startsWith('/api/admin')) {
    const headers = new Headers(response.headers)
    headers.set('X-Robots-Tag', 'noindex, nofollow')
    return new Response(response.body, { status: response.status, statusText: response.statusText, headers })
  }
  return response
}
```

在 proxy 函数内，`fetch` 返回后调用：

```typescript
// 在 proxy 函数内，fetch 返回后注入响应头（D-15）
const response = await fetch(newRequest)
return injectRobotsTag(response, url.pathname)
```

**修改 apps/gateway/wrangler.toml：**

在 `[vars]` 块末尾追加注释：
```toml
# ADMIN_GITHUB_ID 通过 wrangler secret put 注入，不在此明文配置
# 本地开发：在 apps/gateway/.dev.vars 中设置 ADMIN_GITHUB_ID=<your_github_id>
# 格式：单个 ID（"12345678"）或逗号分隔多个（"12345,67890"）
```

**新建 apps/gateway/src/__tests__/dashboard-guard.test.ts：**

参考 PATTERNS.md line 706-771 的完整测试模式：

```typescript
/**
 * dashboard-guard.ts 单元测试
 * 覆盖：ACCESS-01（未登录 302）、ACCESS-02（白名单判断）、PUBSEC-01（robots.txt）、PUBSEC-02（X-Robots-Tag）
 */
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { checkDashboardAuth, isInAdminWhitelist } from '../dashboard-guard'

describe('isInAdminWhitelist', () => {
  it('单个 ID 命中返回 true', () => {
    expect(isInAdminWhitelist('12345', '12345')).toBe(true)
  })
  it('逗号分隔多个 ID 命中返回 true', () => {
    expect(isInAdminWhitelist('67890', '12345,67890')).toBe(true)
  })
  it('ID 含空格时仍能命中（trim 处理）', () => {
    expect(isInAdminWhitelist('67890', '12345, 67890')).toBe(true)
  })
  it('未命中返回 false', () => {
    expect(isInAdminWhitelist('99999', '12345,67890')).toBe(false)
  })
  it('env 为空返回 false', () => {
    expect(isInAdminWhitelist('12345', undefined)).toBe(false)
  })
})

describe('checkDashboardAuth', () => {
  beforeEach(() => {
    vi.restoreAllMocks()
  })

  it('无 cookie 返回 no_session', async () => {
    const req = new Request('https://starye.org/dashboard/')
    const result = await checkDashboardAuth(req, { ADMIN_GITHUB_ID: '12345' })
    expect(result).toEqual({ allowed: false, reason: 'no_session' })
  })

  it('session API 返回无 user 时返回 no_session', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(
      new Response(JSON.stringify({}), { status: 200 }),
    ))
    const req = new Request('https://starye.org/dashboard/', {
      headers: { cookie: 'starye.session_token=abc123' },
    })
    const result = await checkDashboardAuth(req, {
      API_ORIGIN: 'http://127.0.0.1:8787',
      ADMIN_GITHUB_ID: '12345',
    })
    expect(result).toEqual({ allowed: false, reason: 'no_session' })
  })

  it('githubId 在白名单中返回 allowed', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(
      new Response(JSON.stringify({ user: { githubId: '12345' } }), { status: 200 }),
    ))
    const req = new Request('https://starye.org/dashboard/', {
      headers: { cookie: 'starye.session_token=abc123' },
    })
    const result = await checkDashboardAuth(req, {
      API_ORIGIN: 'http://127.0.0.1:8787',
      ADMIN_GITHUB_ID: '12345',
    })
    expect(result).toEqual({ allowed: true })
  })

  it('githubId 不在白名单中返回 not_admin', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(
      new Response(JSON.stringify({ user: { githubId: '99999' } }), { status: 200 }),
    ))
    const req = new Request('https://starye.org/dashboard/', {
      headers: { cookie: 'starye.session_token=abc123' },
    })
    const result = await checkDashboardAuth(req, {
      API_ORIGIN: 'http://127.0.0.1:8787',
      ADMIN_GITHUB_ID: '12345',
    })
    expect(result).toEqual({ allowed: false, reason: 'not_admin' })
  })

  it('fetch 失败时 fail-closed 返回 no_session', async () => {
    vi.stubGlobal('fetch', vi.fn().mockRejectedValue(new Error('network error')))
    const req = new Request('https://starye.org/dashboard/', {
      headers: { cookie: 'starye.session_token=abc123' },
    })
    const result = await checkDashboardAuth(req, {
      API_ORIGIN: 'http://127.0.0.1:8787',
      ADMIN_GITHUB_ID: '12345',
    })
    expect(result).toEqual({ allowed: false, reason: 'no_session' })
  })
})
```

**注意：** L1 缓存（模块级 Map）在测试间可能有状态污染。由于 `sessionCache` 是模块级变量，不同测试用例使用不同的 token 值（`abc123` 等）可以避免污染，或在 `beforeEach` 中使用 `vi.resetModules()` 重置模块状态。
  </action>

  <verify>
    <automated>pnpm --filter @starye/gateway test --run</automated>
  </verify>

  <done>
- `apps/gateway/src/index.ts` 的 `Env` 接口包含 `ADMIN_GITHUB_ID?: string`
- `/robots.txt` 路由在所有 proxy 分支之前，返回正确的 disallow 规则
- `/dashboard/*` 分支在 `cachedProxy` 之前调用 `checkDashboardAuth`
- 未登录时 302 到 `/auth/login?next=<encoded_path>`
- 非白名单时 302 到 `/auth/login?error=not_admin&next=<encoded_path>`
- proxy 函数对 `/dashboard/*` 和 `/api/admin/*` 注入 `X-Robots-Tag: noindex, nofollow`
- `apps/gateway/wrangler.toml` 包含 ADMIN_GITHUB_ID 注释
- `apps/gateway/src/__tests__/dashboard-guard.test.ts` 测试全部通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Gateway | 用户请求携带 session cookie，Gateway 在 proxy 之前验证 |
| Gateway → API | Gateway fetch `/api/auth/get-session`，直连 API_ORIGIN 绕过缓存 |
| L1 Cache | Worker 实例内存 Map，best-effort 加速，不是安全边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | L1 session 缓存 key 设计 | mitigate | Map key 是完整 sessionToken（不是 userId 或 IP），不同用户 token 不同，不会混淆；TTL 30s 确保过期 session 不长期驻留 |
| T-02-02 | Information Disclosure | Dashboard 静态资源泄漏 | mitigate | `checkDashboardAuth` 在 `cachedProxy` 之前执行，未授权请求不会到达 Pages；fail-closed 设计（fetch 失败时拒绝访问） |
| T-02-03 | Spoofing | Open Redirect via next 参数 | accept | Gateway 侧 302 的 next 参数由 Gateway 自己构造（`url.pathname + url.search`），不接受用户输入；open redirect 防护在 auth 侧（Plan 05）实现 |
| T-02-05 | Security Feature Bypass | *.pages.dev 绕过 Gateway | transfer | 由 Plan 06 的 `_redirects` 301 规则处理，本 plan 不负责 |
| T-02-11 | Denial of Service | L1 缓存无限增长 | accept | 单用户场景，Worker 实例内 session 数量极少（通常 1 个）；Cloudflare Workers 内存限制（128MB）作为自然上限 |

</threat_model>

<verification>
## 整体验证

**Gateway 测试：**
```bash
pnpm --filter @starye/gateway test --run
```

**手动验证（可选）：**
1. 启动 Gateway：`pnpm --filter @starye/gateway dev`
2. 验证 robots.txt：
```bash
curl http://localhost:8080/robots.txt
# 期望：User-agent: *\nDisallow: /dashboard\nDisallow: /auth\nDisallow: /api
```
3. 验证未登录 dashboard 重定向：
```bash
curl -I http://localhost:8080/dashboard/
# 期望：HTTP/1.1 302 Found，Location: http://localhost:8080/auth/login?next=%2Fdashboard%2F
```
4. 验证 X-Robots-Tag：
```bash
curl -I http://localhost:8080/dashboard/ -H "Cookie: starye.session_token=valid_admin_token"
# 期望：X-Robots-Tag: noindex, nofollow
```

</verification>

<success_criteria>
1. `apps/gateway/src/dashboard-guard.ts` 导出 `checkDashboardAuth`、`isInAdminWhitelist`、`DashboardAuthResult`
2. `sessionCache` 是模块级 Map，TTL 30s，key 为完整 sessionToken
3. `apps/gateway/src/index.ts` 的 `/robots.txt` 路由在所有 proxy 分支之前
4. `/dashboard/*` 分支在 `cachedProxy` 之前调用 `checkDashboardAuth`
5. proxy 函数对 `/dashboard/*` 和 `/api/admin/*` 注入 `X-Robots-Tag: noindex, nofollow`
6. `apps/gateway/vitest.config.ts` 存在
7. `apps/gateway/src/__tests__/dashboard-guard.test.ts` 所有测试通过（5 个 case）
8. `apps/gateway/wrangler.toml` 包含 ADMIN_GITHUB_ID 注释说明
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard/02-03-SUMMARY.md`
</output>
