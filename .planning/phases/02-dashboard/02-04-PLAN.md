---
phase: 02-dashboard
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/services/adult-filter.ts
  - apps/api/src/routes/public/movies/index.ts
  - apps/api/src/routes/public/comics/index.ts
  - apps/api/src/routes/public/search/index.ts
  - apps/api/src/services/__tests__/adult-filter.test.ts
autonomous: true
requirements:
  - ACCESS-04
  - ACCESS-06
  - ACCESS-07

must_haves:
  truths:
    - "匿名用户的 /api/public/movies list 响应不含 isR18=true 记录"
    - "匿名用户的 /api/public/movies/recommended 响应不含 isR18=true 记录"
    - "匿名用户的 /api/public/comics list 响应不含 isR18=true 记录"
    - "匿名用户的 /api/search 响应不含 isR18=true 记录（WHERE 过滤，非应用层 filter）"
    - "isR18Verified=true 的用户可以看到 isR18=true 记录"
    - "admin/super_admin 角色用户可以看到 isR18=true 记录"
  artifacts:
    - path: "apps/api/src/services/adult-filter.ts"
      provides: "buildAdultVisibilityCondition 函数"
      exports: ["buildAdultVisibilityCondition"]
    - path: "apps/api/src/routes/public/movies/index.ts"
      provides: "list/recommended handler 统一调用 adult-filter"
      contains: "buildAdultVisibilityCondition"
    - path: "apps/api/src/routes/public/comics/index.ts"
      provides: "list handler 统一调用 adult-filter"
      contains: "buildAdultVisibilityCondition"
    - path: "apps/api/src/routes/public/search/index.ts"
      provides: "search handler WHERE 过滤 bug 修复"
      contains: "buildAdultVisibilityCondition"
    - path: "apps/api/src/services/__tests__/adult-filter.test.ts"
      provides: "ACCESS-04/07 自动化测试"
      min_lines: 40
  key_links:
    - from: "apps/api/src/routes/public/movies/index.ts"
      to: "apps/api/src/services/adult-filter.ts"
      via: "import buildAdultVisibilityCondition"
      pattern: "buildAdultVisibilityCondition"
    - from: "apps/api/src/routes/public/search/index.ts"
      to: "apps/api/src/services/adult-filter.ts"
      via: "WHERE 条件注入（修复 bug）"
      pattern: "and.*adultCond.*searchCond"
---

<objective>
新建 `apps/api/src/services/adult-filter.ts` 导出 `buildAdultVisibilityCondition`，并将所有 `/api/public/*` 的 list/search/recommend handler 统一调用此函数，同时修复 `public/search/index.ts` 的应用层过滤 bug。

Purpose: D-07/D-08 要求统一的成人内容可见性条件构造函数，注入所有 public handler 的 WHERE 条件数组。当前 `public/search/index.ts` 存在 bug（先查全量再应用层 `.filter(m => !m.isR18)`），不符合 ACCESS-07 "服务端 WHERE 过滤"要求，且导致 limit 语义错误。本 plan 新建 service 并修复所有 public routes。

Output: `adult-filter.ts` 导出 `buildAdultVisibilityCondition`，movies/comics/search 三个 public handler 统一调用，search bug 修复，单元测试覆盖 ACCESS-04/07。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard/02-CONTEXT.md
@.planning/phases/02-dashboard/02-RESEARCH.md
@.planning/phases/02-dashboard/02-PATTERNS.md
@.planning/phases/02-dashboard/02-01-SUMMARY.md
</context>

<interfaces>
<!-- 从现有代码提取的关键接口 -->

From apps/api/src/routes/actors/services/auth.service.ts（语义参考）:
```typescript
import type { SessionUser } from '../../../types'

export function checkUserAdultStatus(user?: SessionUser): boolean {
  if (!user) return false
  return !!(user.isAdult || user.isR18Verified || user.role === 'admin')
}
```

From apps/api/src/routes/public/movies/index.ts（现有正确模式，line 52-55）:
```typescript
// 已有正确实现，buildAdultVisibilityCondition 应与此对齐
const conditions: SQL[] = []
if (!user?.isR18Verified) {
  conditions.push(eq(movies.isR18, false))
}
```

From apps/api/src/routes/public/search/index.ts（有 bug 的实现，line 33-63）:
```typescript
// BUG：r18Filter 构造正确，但 WHERE 子句没有使用它
const r18Filter = user?.isR18Verified ? undefined : eq(movies.isR18, false)
results.movies = await db.select({...}).from(movies)
  .where(
    r18Filter
      ? or(eq(movies.code, keyword), like(movies.title, keyword))
      : or(eq(movies.code, keyword), like(movies.title, keyword)),
    // 两个分支完全相同，r18Filter 根本没有被 AND 进去
  )
  .limit(limitNum)
// 然后又在应用层过滤（line 61-63）
if (r18Filter && results.movies) {
  results.movies = results.movies.filter(m => !m.isR18)
}
```

From packages/db/src/schema.ts（isR18 字段）:
```typescript
// movies 表
isR18: integer('isR18', { mode: 'boolean' }).notNull().default(true)
// comics 表
isR18: integer('isR18', { mode: 'boolean' }).notNull().default(true)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 adult-filter.ts + 单元测试</name>
  <files>apps/api/src/services/adult-filter.ts, apps/api/src/services/__tests__/adult-filter.test.ts</files>

  <read_first>
    - apps/api/src/routes/actors/services/auth.service.ts（参考纯函数 service 模式和 checkUserAdultStatus 语义）
    - apps/api/src/types.ts（确认 SessionUser 类型，特别是 isR18Verified、role 字段）
    - packages/db/src/schema.ts（确认 movies、comics、actors、publishers 表的 isR18 字段类型）
    - .planning/phases/02-dashboard/02-PATTERNS.md（lines 293-346，adult-filter.ts 完整模式）
  </read_first>

  <behavior>
    - 未登录用户（user === undefined）返回 eq(table.isR18, false)
    - isR18Verified=false 的用户返回 eq(table.isR18, false)
    - isR18Verified=true 的用户返回 undefined（无过滤）
    - admin 角色用户返回 undefined（与 checkUserAdultStatus 语义对齐，D-07）
    - super_admin 角色用户返回 undefined
    - 返回的条件可以直接 push 进 conditions[] 数组
  </behavior>

  <action>
**新建 apps/api/src/services/adult-filter.ts：**

参考 PATTERNS.md line 293-346 的完整模式，与 `checkUserAdultStatus` 语义对齐（admin 角色也返回 undefined）：

```typescript
import type { SessionUser } from '../types'
import { eq } from 'drizzle-orm'

// 支持所有含 isR18 列的表（movies / comics / actors / publishers）
// 使用结构类型而非 union，避免导入所有表类型
type TableWithR18 = { isR18: any }

/**
 * 构造成人内容可见性 WHERE 条件。
 * 与 checkUserAdultStatus 语义对齐：admin/super_admin 角色或 isR18Verified=true 可见全部。
 *
 * @param user 当前用户（undefined 表示匿名）
 * @param table 含 isR18 列的 Drizzle 表对象
 * @returns Drizzle SQL 条件（push 进 conditions[]），或 undefined（无需过滤）
 */
export function buildAdultVisibilityCondition(
  user: SessionUser | undefined,
  table: TableWithR18,
) {
  // 对齐 checkUserAdultStatus：admin/super_admin 角色也可见 R18（D-07）
  if (user?.isR18Verified || user?.role === 'admin' || user?.role === 'super_admin') {
    return undefined
  }
  return eq(table.isR18, false)
}
```

**新建 apps/api/src/services/__tests__/adult-filter.test.ts：**

参考 PATTERNS.md line 775-825 的测试模式：

```typescript
/**
 * adult-filter.ts 单元测试
 * 覆盖：ACCESS-04（匿名用户可访问公开目录）、ACCESS-07（匿名用户 list 不含 isR18=true）
 */
import { describe, expect, it } from 'vitest'
import { buildAdultVisibilityCondition } from '../adult-filter'

// 模拟含 isR18 列的表对象（与 Drizzle 表结构兼容）
const mockTable = { isR18: { name: 'isR18' } as any }

describe('buildAdultVisibilityCondition', () => {
  it('未登录用户（undefined）返回过滤条件', () => {
    const cond = buildAdultVisibilityCondition(undefined, mockTable)
    expect(cond).toBeDefined()
  })

  it('isR18Verified=false 的用户返回过滤条件', () => {
    const user = { isR18Verified: false, role: 'user' } as any
    const cond = buildAdultVisibilityCondition(user, mockTable)
    expect(cond).toBeDefined()
  })

  it('isR18Verified=true 的用户返回 undefined（无过滤）', () => {
    const user = { isR18Verified: true, role: 'user' } as any
    const cond = buildAdultVisibilityCondition(user, mockTable)
    expect(cond).toBeUndefined()
  })

  it('admin 角色返回 undefined（无过滤，与 checkUserAdultStatus 对齐）', () => {
    const user = { isR18Verified: false, role: 'admin' } as any
    const cond = buildAdultVisibilityCondition(user, mockTable)
    expect(cond).toBeUndefined()
  })

  it('super_admin 角色返回 undefined（无过滤）', () => {
    const user = { isR18Verified: false, role: 'super_admin' } as any
    const cond = buildAdultVisibilityCondition(user, mockTable)
    expect(cond).toBeUndefined()
  })

  it('返回的条件可以 push 进 conditions 数组', () => {
    const conditions: any[] = []
    const cond = buildAdultVisibilityCondition(undefined, mockTable)
    if (cond) conditions.push(cond)
    expect(conditions).toHaveLength(1)
  })
})
```

**注意：** 测试放 `apps/api/src/services/__tests__/` 目录（需新建 `__tests__` 子目录）。纯函数测试，无需 mock，直接 import 调用。
  </action>

  <verify>
    <automated>pnpm --filter @starye/api test --run src/services/__tests__/adult-filter.test.ts</automated>
  </verify>

  <done>
- `apps/api/src/services/adult-filter.ts` 存在，导出 `buildAdultVisibilityCondition`
- 函数对 admin/super_admin 角色返回 undefined（与 checkUserAdultStatus 语义对齐）
- `apps/api/src/services/__tests__/adult-filter.test.ts` 所有测试通过（6 个 case）
- 不使用 `sql.raw()`，只用 Drizzle `eq()`
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 public routes 统一调用 adult-filter + 修复 search bug</name>
  <files>apps/api/src/routes/public/movies/index.ts, apps/api/src/routes/public/comics/index.ts, apps/api/src/routes/public/search/index.ts</files>

  <read_first>
    - apps/api/src/routes/public/movies/index.ts（完整读取，找到所有 isR18 过滤位置：list handler line 52、recommended handler、detail handler 的 related movies 查询）
    - apps/api/src/routes/public/comics/index.ts（完整读取，找到 isR18 过滤位置）
    - apps/api/src/routes/public/search/index.ts（完整读取，确认 bug 位置 line 33-63，找到应用层 filter 代码）
    - .planning/phases/02-dashboard/02-PATTERNS.md（lines 350-416，三个文件的修改模式）
  </read_first>

  <action>
**修改 apps/api/src/routes/public/movies/index.ts：**

参考 PATTERNS.md line 350-374 的修改模式：

1. 在文件顶部 import 中添加：
```typescript
import { buildAdultVisibilityCondition } from '../../../services/adult-filter'
```

2. 找到所有内联 `!user?.isR18Verified` 判断（PATTERNS.md 指出有 6 处），统一替换为：
```typescript
// 替换前：
if (!user?.isR18Verified) {
  conditions.push(eq(movies.isR18, false))
}

// 替换后：
const adultCond = buildAdultVisibilityCondition(user, movies)
if (adultCond) conditions.push(adultCond)
```

**注意：** PATTERNS.md line 372 指出 `genres handler`（line 200）使用 sql 模板字符串的 r18Filter，保持现有模式不改（sql 模板不走 sql.raw，无注入风险）。只改 `conditions[]` 数组模式的位置。

**修改 apps/api/src/routes/public/comics/index.ts：**

与 movies 相同的模式：
1. 添加 import `buildAdultVisibilityCondition`
2. 找到所有内联 `!user?.isR18Verified` 判断，替换为 `buildAdultVisibilityCondition` 调用

**修改 apps/api/src/routes/public/search/index.ts（修复 bug）：**

参考 PATTERNS.md line 378-416 的 bug 修复模式：

1. 添加 import：
```typescript
import { buildAdultVisibilityCondition } from '../../../services/adult-filter'
import { and, or, eq, like } from 'drizzle-orm'
```

2. 修复 movies 搜索查询（line 33-63 附近）：
```typescript
// 修复前（有 bug）：
const r18Filter = user?.isR18Verified ? undefined : eq(movies.isR18, false)
results.movies = await db.select({...}).from(movies)
  .where(
    r18Filter
      ? or(eq(movies.code, keyword), like(movies.title, keyword))
      : or(eq(movies.code, keyword), like(movies.title, keyword)),
  )
  .limit(limitNum)
if (r18Filter && results.movies) {
  results.movies = results.movies.filter(m => !m.isR18)  // 删除此行
}

// 修复后（正确）：
const adultCond = buildAdultVisibilityCondition(user, movies)
const searchCond = or(eq(movies.code, keyword), like(movies.title, keyword))
results.movies = await db.select({...}).from(movies)
  .where(adultCond ? and(adultCond, searchCond) : searchCond)
  .limit(limitNum)
// 删除应用层 filter（lines 61-63）
```

3. 如果 search handler 也查询 comics，同样修复 comics 的搜索查询。

**关键验证点：**
- `public/search/index.ts` 中不再有 `.filter(m => !m.isR18)` 应用层过滤
- 所有 public routes 的 isR18 过滤都通过 `buildAdultVisibilityCondition` 实现
- 不使用 `sql.raw()`
  </action>

  <verify>
    <automated>pnpm --filter @starye/api test --run && pnpm --filter @starye/api exec tsc --noEmit</automated>
  </verify>

  <done>
- `apps/api/src/routes/public/movies/index.ts` 所有 conditions 数组模式的 isR18 过滤统一调用 `buildAdultVisibilityCondition`
- `apps/api/src/routes/public/comics/index.ts` 同上
- `apps/api/src/routes/public/search/index.ts` 的 WHERE 子句正确使用 `and(adultCond, searchCond)`，删除应用层 `.filter(m => !m.isR18)`
- TypeScript 编译通过
- 所有 API 测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | 匿名用户请求 public routes，API 在 WHERE 层过滤 R18 内容 |
| API → D1 | Drizzle ORM 参数化查询，不使用 sql.raw() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | R18 内容通过 API 直接访问 | mitigate | `buildAdultVisibilityCondition` 在 WHERE 层过滤，不依赖前端隐藏；使用 Drizzle `eq()` 参数化查询，无 SQL 注入风险（CONCERNS.md sql.raw 风险规避） |
| T-02-12 | Information Disclosure | search handler 应用层过滤绕过 | mitigate | 修复 search bug，改为 WHERE 条件过滤；删除应用层 `.filter(m => !m.isR18)` 代码 |
| T-02-13 | Elevation of Privilege | admin 角色绕过 R18 过滤 | accept | admin/super_admin 角色可见全部 R18 内容，与 `checkUserAdultStatus` 语义一致；单用户场景下作者即 admin，此行为符合预期 |

</threat_model>

<verification>
## 整体验证

**单元测试：**
```bash
pnpm --filter @starye/api test --run
```

**类型检查：**
```bash
pnpm --filter @starye/api exec tsc --noEmit
```

**手动验证（可选）：**
1. 启动 API Worker：`pnpm --filter @starye/api dev`
2. 匿名访问 movies list：
```bash
curl http://localhost:8787/api/public/movies | jq '[.data[] | select(.isR18 == true)] | length'
# 期望：0（匿名用户看不到 R18 内容）
```
3. 匿名搜索：
```bash
curl "http://localhost:8787/api/search?q=test" | jq '[.movies[] | select(.isR18 == true)] | length'
# 期望：0
```

**验证 search bug 已修复：**
```bash
grep -n "filter.*isR18" apps/api/src/routes/public/search/index.ts
# 期望：无输出（应用层 filter 已删除）
```

</verification>

<success_criteria>
1. `apps/api/src/services/adult-filter.ts` 导出 `buildAdultVisibilityCondition`，对 admin/super_admin 返回 undefined
2. `apps/api/src/services/__tests__/adult-filter.test.ts` 所有测试通过（6 个 case）
3. `apps/api/src/routes/public/movies/index.ts` 所有 conditions 数组模式统一调用 `buildAdultVisibilityCondition`
4. `apps/api/src/routes/public/comics/index.ts` 同上
5. `apps/api/src/routes/public/search/index.ts` WHERE 子句使用 `and(adultCond, searchCond)`，无应用层 `.filter(m => !m.isR18)`
6. TypeScript 编译通过，所有 API 测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard/02-04-SUMMARY.md`
</output>
