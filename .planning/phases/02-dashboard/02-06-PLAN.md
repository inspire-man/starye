---
phase: 02-dashboard
plan: 06
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - apps/movie-app/public/_redirects
  - apps/comic-app/public/_redirects
  - apps/dashboard/public/_redirects
  - apps/auth/public/_redirects
  - apps/blog/public/_redirects
  - RUNBOOK.md
autonomous: false
requirements:
  - PUBSEC-03
  - PUBSEC-05

user_setup:
  - service: cloudflare-waf
    why: "PUBSEC-03：/api/auth/sign-in 限速 10 req/min/IP"
    dashboard_config:
      - task: "创建 WAF Rate Limiting 规则"
        location: "Cloudflare Dashboard → Security → WAF → Rate Limiting Rules"
        details: "规则名: starye-signin-ratelimit，匹配条件: URI Path equals /api/auth/sign-in，阈值: 10 req/min/IP，动作: Block（或 Challenge）"

must_haves:
  truths:
    - "所有 5 个已存在的 Pages 应用的 *.pages.dev 直链被 301 重定向到 starye.org/<app>/（apps/tavern 尚未创建，跳过）"
    - "301 规则在 SPA fallback 之前（_redirects 文件顺序正确）"
    - "RUNBOOK.md 包含 WAF 手配记录段落"
    - "D-16: 6 app _redirects 统一加 301 规则; D-18: WAF 手配 + RUNBOOK 段落"
  artifacts:
    - path: "apps/movie-app/public/_redirects"
      provides: "*.pages.dev → starye.org/movie/ 301 规则"
      contains: "starye-movie.pages.dev"
    - path: "apps/comic-app/public/_redirects"
      provides: "*.pages.dev → starye.org/comic/ 301 规则"
      contains: "starye-comic.pages.dev"
    - path: "apps/dashboard/public/_redirects"
      provides: "*.pages.dev → starye.org/dashboard/ 301 规则"
      contains: "starye-dashboard.pages.dev"
    - path: "apps/auth/public/_redirects"
      provides: "*.pages.dev → starye.org/auth/ 301 规则（新建文件）"
      contains: "starye-auth.pages.dev"
    - path: "apps/blog/public/_redirects"
      provides: "*.pages.dev → starye.org/blog/ 301 规则"
      contains: "starye-blog.pages.dev"
    - path: "RUNBOOK.md"
      provides: "WAF 手配记录段落"
      contains: "WAF Rate Limiting"
  key_links:
    - from: "apps/*/public/_redirects"
      to: "starye.org/<app>/"
      via: "Cloudflare Pages _redirects 301! 规则"
      pattern: "301!"
---

<objective>
在所有 6 个 Pages 应用的 `public/_redirects` 文件中统一添加 `*.pages.dev` → `starye.org/<app>/` 的 301 重定向规则，并在 `RUNBOOK.md` 中新增 WAF 手配记录段落。

Purpose: D-16 要求 6 个 Pages 应用的 `*.pages.dev` 直链在一个 commit 内统一加完，防止用户绕过 Gateway 直接访问 Pages 原始域名。D-18 要求 PUBSEC-03 的 WAF 限速手配记录写入 RUNBOOK.md（实际 WAF 配置在 Cloudflare Dashboard 手动完成）。

Output: 6 个 `_redirects` 文件包含 301 规则，`RUNBOOK.md` 包含 WAF 手配记录，人工 checkpoint 确认 WAF 规则已在 Cloudflare Dashboard 配置。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard/02-CONTEXT.md
@.planning/phases/02-dashboard/02-RESEARCH.md
@.planning/phases/02-dashboard/02-PATTERNS.md
@.planning/phases/02-dashboard/02-03-SUMMARY.md
</context>

<interfaces>
<!-- 从现有 _redirects 文件提取的模式 -->

From apps/movie-app/public/_redirects（现有 SPA fallback）:
```
# SPA fallback - 所有非文件请求都返回 index.html
/* /index.html 200
```

From apps/auth/public/_redirects（不存在，需新建）:
```
# 文件不存在，需新建
```

Cloudflare Pages _redirects 语法（D-16）:
```
# 跨域 301 规则（必须在 SPA fallback 之前）
https://source.pages.dev/* https://target.org/path/:splat 301!

# SPA fallback（保留原有规则）
/* /index.html 200
```
注意：`301!` 末尾的 `!` 表示强制重定向，优先级高于 SPA fallback。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 统一更新 6 个 Pages 应用的 _redirects 文件</name>
  <files>apps/movie-app/public/_redirects, apps/comic-app/public/_redirects, apps/dashboard/public/_redirects, apps/auth/public/_redirects, apps/blog/public/_redirects</files>

  <read_first>
    - apps/movie-app/public/_redirects（确认现有内容，是否只有 SPA fallback）
    - apps/comic-app/public/_redirects（同上）
    - apps/dashboard/public/_redirects（同上）
    - apps/blog/public/_redirects（同上）
    - apps/auth/public/_redirects（确认文件是否存在；PATTERNS.md 指出此文件不存在，需新建）
    - apps/tavern/public/_redirects（确认文件是否存在；PATTERNS.md 指出需确认 tavern app 是否存在）
    - apps/gateway/wrangler.toml（确认 TAVERN_ORIGIN 是否声明，以确认 tavern app 存在）
    - .planning/phases/02-dashboard/02-PATTERNS.md（lines 615-666，各 app 的具体规则）
  </read_first>

  <action>
**对每个 Pages 应用，在 `_redirects` 文件的 SPA fallback 规则之前插入 301 规则。**

参考 PATTERNS.md line 625-659 的各 app 具体规则：

**apps/movie-app/public/_redirects（追加到文件开头）：**
```
# *.pages.dev 直链 301 回 starye.org（必须在 SPA fallback 之前）（D-16）
https://starye-movie.pages.dev/* https://starye.org/movie/:splat 301!

# SPA fallback（保留原有规则）
/* /index.html 200
```

**apps/comic-app/public/_redirects（追加到文件开头）：**
```
# *.pages.dev 直链 301 回 starye.org（必须在 SPA fallback 之前）（D-16）
https://starye-comic.pages.dev/* https://starye.org/comic/:splat 301!

# SPA fallback（保留原有规则）
/* /index.html 200
```

**apps/dashboard/public/_redirects（追加到文件开头）：**
```
# *.pages.dev 直链 301 回 starye.org（必须在 SPA fallback 之前）（D-16）
https://starye-dashboard.pages.dev/* https://starye.org/dashboard/:splat 301!

# SPA fallback（保留原有规则）
/* /index.html 200
```

**apps/auth/public/_redirects（新建文件）：**

auth 是 Nuxt 4 应用，`public/` 目录内容会被复制到 `.output/public/`，Cloudflare Pages 会识别 `_redirects`。

```
# *.pages.dev 直链 301 回 starye.org（必须在 SPA fallback 之前）（D-16）
https://starye-auth.pages.dev/* https://starye.org/auth/:splat 301!

# SPA fallback
/* /index.html 200
```

**apps/blog/public/_redirects（追加到文件开头）：**

blog 走 `blog.starye.org` 自定义域，但保留兜底规则防止直链 `starye-blog.pages.dev`。

```
# *.pages.dev 直链 301 回 starye.org（必须在 SPA fallback 之前）（D-16）
https://starye-blog.pages.dev/* https://starye.org/blog/:splat 301!

# SPA fallback（保留原有规则）
/* /index.html 200
```

**apps/tavern/public/_redirects（若文件存在则追加，若不存在则新建）：**

先确认 `apps/tavern/` 目录是否存在（gateway wrangler.toml 中有 `TAVERN_ORIGIN` 声明）。若存在：

```
# *.pages.dev 直链 301 回 starye.org（必须在 SPA fallback 之前）（D-16）
https://starye-tavern.pages.dev/* https://starye.org/tavern/:splat 301!

# SPA fallback（保留原有规则）
/* /index.html 200
```

**关键注意事项：**
- 301 规则必须在 `/* /index.html 200` 之前（Cloudflare Pages 按顺序匹配）
- `301!` 末尾的 `!` 表示强制重定向，不可省略
- 如果现有文件有注释，保留注释，只在文件开头插入 301 规则
- 6 个文件在同一 commit 内完成（D-16）
  </action>

  <verify>
    <automated>grep -l "301!" apps/movie-app/public/_redirects apps/comic-app/public/_redirects apps/dashboard/public/_redirects apps/auth/public/_redirects apps/blog/public/_redirects | wc -l
    <!-- 期望输出: 5 --></automated>
  </verify>

  <done>
- `apps/movie-app/public/_redirects` 包含 `starye-movie.pages.dev` 301 规则，且在 SPA fallback 之前
- `apps/comic-app/public/_redirects` 包含 `starye-comic.pages.dev` 301 规则
- `apps/dashboard/public/_redirects` 包含 `starye-dashboard.pages.dev` 301 规则
- `apps/auth/public/_redirects` 文件存在，包含 `starye-auth.pages.dev` 301 规则
- `apps/blog/public/_redirects` 包含 `starye-blog.pages.dev` 301 规则
- `apps/tavern/public/_redirects` 包含 `starye-tavern.pages.dev` 301 规则（若 tavern app 存在）
- 所有文件的 301 规则在 `/* /index.html 200` 之前
  </done>
</task>

<task type="auto">
  <name>Task 2: 新建或更新 RUNBOOK.md，添加 WAF 手配记录段落</name>
  <files>RUNBOOK.md</files>

  <read_first>
    - RUNBOOK.md（确认文件是否存在；若存在则读取现有内容，找到合适的插入位置）
    - .planning/phases/02-dashboard/02-CONTEXT.md（D-18 的 RUNBOOK 要求）
  </read_first>

  <action>
**若 RUNBOOK.md 不存在，新建文件；若存在，在合适位置追加以下段落：**

```markdown
## WAF Rate Limiting 手配记录

**需求：** PUBSEC-03 — `/api/auth/sign-in` 限制 10 req/min/IP

**配置位置：** Cloudflare Dashboard → Security → WAF → Rate Limiting Rules

**规则配置：**

| 字段 | 值 |
|------|-----|
| 规则名 | `starye-signin-ratelimit` |
| 匹配条件 | URI Path equals `/api/auth/sign-in` AND Request Method equals `POST` |
| 阈值 | 10 requests per 1 minute per IP |
| 动作 | Block（返回 429 Too Many Requests） |
| 响应码 | 429 |
| 适用范围 | starye.org（主域名） |

**配置步骤：**

1. 登录 Cloudflare Dashboard → 选择 starye.org 域名
2. 进入 Security → WAF → Rate Limiting Rules
3. 点击 "Create rule"
4. 填写规则名：`starye-signin-ratelimit`
5. 在 "When incoming requests match..." 设置：
   - Field: URI Path，Operator: equals，Value: `/api/auth/sign-in`
   - 点击 "And"，Field: Request Method，Operator: equals，Value: `POST`
6. 在 "Rate limit" 设置：Requests: 10，Period: 1 minute，Characteristics: IP
7. 在 "Then take action..." 选择：Block
8. 保存规则

**验证方式：**

```bash
# 连续发送 11 次 POST 请求，第 11 次应返回 429
for i in $(seq 1 11); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    -X POST https://starye.org/api/auth/sign-in \
    -H "Content-Type: application/json" \
    -d '{"email":"test@test.com","password":"wrong"}'
done
# 期望：前 10 次返回 401（密码错误），第 11 次返回 429（限速）
```

**注意：** 免费 plan 的 Rate Limiting 规则有数量限制（通常 1 条）。如果已有其他规则，需要评估是否需要升级 plan 或合并规则。

**配置日期：** _（配置完成后填写）_

**配置人：** _（填写）_
```
  </action>

  <verify>
    <automated>grep -c "WAF Rate Limiting" RUNBOOK.md</automated>
  </verify>

  <done>
- `RUNBOOK.md` 存在（新建或已有）
- 包含 "WAF Rate Limiting 手配记录" 段落
- 段落包含规则名、匹配条件、阈值、动作、配置步骤
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 06 Task 1 已在所有 6 个 Pages 应用的 `public/_redirects` 文件中添加 `*.pages.dev` → `starye.org/<app>/` 的 301 重定向规则。Task 2 已在 `RUNBOOK.md` 中添加 WAF 手配记录段落。
  </what-built>
  <how-to-verify>
    **验证 _redirects 文件（自动）：**
    ```bash
    # 确认所有文件包含 301! 规则
    grep -l "301!" apps/movie-app/public/_redirects apps/comic-app/public/_redirects apps/dashboard/public/_redirects apps/auth/public/_redirects apps/blog/public/_redirects
    # 期望：列出所有 5 个文件路径

    # 确认 301 规则在 SPA fallback 之前
    head -3 apps/movie-app/public/_redirects
    # 期望：第一行是 301 规则，不是 /* /index.html 200
    ```

    **验证 RUNBOOK.md（自动）：**
    ```bash
    grep "WAF Rate Limiting" RUNBOOK.md
    # 期望：找到匹配行
    ```

    **WAF 手配（手动，需要 Cloudflare Dashboard 访问权限）：**
    1. 登录 Cloudflare Dashboard → starye.org → Security → WAF → Rate Limiting Rules
    2. 按照 RUNBOOK.md 中的步骤创建 `starye-signin-ratelimit` 规则
    3. 确认规则状态为 "Active"
    4. 可选验证：连续发送 11 次 POST 到 `/api/auth/sign-in`，第 11 次应返回 429

    **注意：** WAF 配置是手动步骤，无法自动化。如果暂时不配置，可以跳过此步骤，但需要在 RUNBOOK.md 中标注"待配置"。
  </how-to-verify>
  <resume-signal>
    输入 "approved" 确认 _redirects 文件正确且 WAF 规则已配置（或标注为待配置）。
    如有问题，描述具体问题。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *.pages.dev → starye.org | 用户直接访问 Pages 原始域名，需要 301 重定向到 Gateway |
| /api/auth/sign-in | 暴力破解入口，需要 WAF 限速保护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Security Feature Bypass | *.pages.dev 绕过 Gateway | mitigate | `_redirects` 301! 强制重定向，`!` 确保优先级高于 SPA fallback；6 个 app 统一一次加完避免遗漏（D-16） |
| T-02-07 | Denial of Service | /api/auth/sign-in 暴力破解 | mitigate | Cloudflare WAF Rate Limiting 10 req/min/IP（D-18）；手动配置 + RUNBOOK 记录 |
| T-02-16 | Security Feature Bypass | _redirects 规则顺序错误 | mitigate | 301 规则必须在 `/* /index.html 200` 之前；executor 需验证文件顺序（Pitfall 4） |

</threat_model>

<verification>
## 整体验证

**自动验证：**
```bash
# 确认所有 _redirects 文件包含 301! 规则
grep -rn "301!" apps/*/public/_redirects

# 确认 RUNBOOK.md 包含 WAF 段落
grep "WAF Rate Limiting" RUNBOOK.md

# 确认 301 规则在 SPA fallback 之前（movie-app 示例）
head -5 apps/movie-app/public/_redirects
```

**手动验证（部署后）：**
1. 部署 movie-app 到 Cloudflare Pages
2. 访问 `https://starye-movie.pages.dev/`
3. 期望：301 重定向到 `https://starye.org/movie/`
4. 验证 WAF 规则：Cloudflare Dashboard → Security → WAF → Rate Limiting Rules 确认规则存在且 Active

</verification>

<success_criteria>
1. `apps/movie-app/public/_redirects` 包含 `https://starye-movie.pages.dev/* https://starye.org/movie/:splat 301!`，且在 `/* /index.html 200` 之前
2. `apps/comic-app/public/_redirects` 包含 `https://starye-comic.pages.dev/*` 301 规则
3. `apps/dashboard/public/_redirects` 包含 `https://starye-dashboard.pages.dev/*` 301 规则
4. `apps/auth/public/_redirects` 文件存在，包含 `https://starye-auth.pages.dev/*` 301 规则
5. `apps/blog/public/_redirects` 包含 `https://starye-blog.pages.dev/*` 301 规则
6. `RUNBOOK.md` 包含 "WAF Rate Limiting 手配记录" 段落，含规则名、匹配条件、阈值、配置步骤
7. 人工 checkpoint 通过（_redirects 文件验证 + WAF 规则配置确认）
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard/02-06-SUMMARY.md`
</output>
