---
phase: 02-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/middleware/guard.ts
  - apps/api/src/index.ts
  - apps/api/wrangler.toml
  - apps/api/src/__tests__/docs-auth.test.ts
autonomous: true
requirements:
  - ACCESS-02
  - ACCESS-03
  - PUBSEC-04

must_haves:
  truths:
    - "ADMIN_GITHUB_ID 白名单命中的用户可通过所有 requireAuth 检查"
    - "非白名单用户仍受 role 检查约束"
    - "匿名用户访问 /api/docs 返回 401"
    - "白名单用户访问 /api/docs 返回 200"
  artifacts:
    - path: "apps/api/src/middleware/guard.ts"
      provides: "requireAuth 白名单短路分支"
      contains: "ADMIN_GITHUB_ID"
    - path: "apps/api/src/index.ts"
      provides: "/api/docs 和 /api/openapi.json 路由加 requireAuth 保护"
      contains: "requireAuth(['admin', 'super_admin'])"
    - path: "apps/api/wrangler.toml"
      provides: "ADMIN_GITHUB_ID secret 声明注释"
      contains: "ADMIN_GITHUB_ID"
    - path: "apps/api/src/__tests__/docs-auth.test.ts"
      provides: "PUBSEC-04 自动化测试"
      min_lines: 30
  key_links:
    - from: "apps/api/src/middleware/guard.ts"
      to: "c.env.ADMIN_GITHUB_ID"
      via: "白名单短路分支"
      pattern: "ADMIN_GITHUB_ID.*split.*includes"
    - from: "apps/api/src/index.ts"
      to: "requireAuth"
      via: "/api/docs 路由中间件"
      pattern: "requireAuth.*admin.*super_admin"
---

<objective>
在 API Worker 的 `requireAuth` 中间件中加入 `ADMIN_GITHUB_ID` 白名单短路分支，并为 `/api/docs` 和 `/api/openapi.json` 路由添加鉴权保护。

Purpose: D-04 要求 `ADMIN_GITHUB_ID` 命中即视为 `super_admin`，不依赖 D1 的 `user.role`。D-17 要求 `/api/docs` 和 `/api/openapi.json` 在生产环境需要鉴权才能访问（匿名 401）。本 plan 实现 API 侧的双重保护：白名单短路让作者账号自动通过所有 admin 路由检查，requireAuth 保护让 OpenAPI 文档不对匿名用户暴露。

Output: `requireAuth` 包含白名单短路逻辑，`/api/docs` 和 `/api/openapi.json` 路由受 `requireAuth(['admin', 'super_admin'])` 保护，`apps/api/wrangler.toml` 包含 `ADMIN_GITHUB_ID` secret 声明注释，单元测试验证 PUBSEC-04。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard/02-CONTEXT.md
@.planning/phases/02-dashboard/02-RESEARCH.md
@.planning/phases/02-dashboard/02-PATTERNS.md
@.planning/phases/02-dashboard/02-01-SUMMARY.md
</context>

<interfaces>
<!-- 从现有代码提取的关键接口 -->

From apps/api/src/middleware/guard.ts（当前完整实现）:
```typescript
import type { AppEnv, SessionUser } from '../types'
import { createMiddleware } from 'hono/factory'
import { HTTPException } from 'hono/http-exception'

export function requireAuth(requiredRole?: string | string[]) {
  return createMiddleware<AppEnv>(async (c, next) => {
    const auth = c.get('auth')
    const session = await auth.api.getSession({ headers: c.req.raw.headers })

    if (!session || !session.user) {
      throw new HTTPException(401, { message: 'Unauthorized: Please login first' })
    }

    const user = session.user as SessionUser

    if (requiredRole) {
      const roles = Array.isArray(requiredRole) ? requiredRole : [requiredRole]
      // Super admin always passes
      if (user.role === 'super_admin' || user.role === 'admin') {
        await next()
        return
      }
      if (!roles.includes(user.role)) {
        throw new HTTPException(403, { message: `Forbidden: Requires one of [${roles.join(', ')}] role` })
      }
    }

    await next()
  })
}
```

From apps/api/src/types.ts（Plan 01 修改后）:
```typescript
export interface SessionUser {
  id: string
  email: string
  name: string
  role: string
  isAdult: boolean
  isR18Verified: boolean
  githubId?: string | null  // Plan 01 新增
}

// AppEnv 包含 c.env 类型
export interface AppEnv {
  Bindings: Env
  Variables: {
    db: Database
    auth: Auth
    user: SessionUser | undefined
    requestId: string
  }
}
```

From apps/api/src/index.ts（/api/docs 挂载位置，line 170 附近）:
```typescript
// 当前无鉴权
.get('/api/openapi.json', openAPIRouteHandler(app, { documentation: {...} }))
.get('/api/docs', Scalar({...}))
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: requireAuth 加入 ADMIN_GITHUB_ID 白名单短路分支</name>
  <files>apps/api/src/middleware/guard.ts, apps/api/wrangler.toml</files>

  <read_first>
    - apps/api/src/middleware/guard.ts（完整读取，确认当前 requireAuth 实现）
    - apps/api/src/types.ts（确认 AppEnv 和 SessionUser 类型，特别是 githubId 字段）
    - apps/api/wrangler.toml（确认 [vars] 块结构，找到追加注释的位置）
    - .planning/phases/02-dashboard/02-PATTERNS.md（lines 170-224，guard.ts 修改模式）
  </read_first>

  <behavior>
    - ADMIN_GITHUB_ID 白名单命中时，无论 requiredRole 是什么，直接 next()
    - 白名单短路在 role 检查之前执行（D-04：白名单覆盖 DB role）
    - 白名单未命中时，原有 role 检查逻辑不变
    - ADMIN_GITHUB_ID 未配置时（undefined），白名单短路不生效，走原有逻辑
    - user.githubId 为 null 或 undefined 时，白名单短路不生效
  </behavior>

  <action>
**修改 apps/api/src/middleware/guard.ts：**

在 `if (requiredRole)` 块内、`user.role === 'super_admin'` 判断之前，插入白名单短路分支（参考 PATTERNS.md line 208-217）：

```typescript
import type { AppEnv, SessionUser } from '../types'
import { createMiddleware } from 'hono/factory'
import { HTTPException } from 'hono/http-exception'

export function requireAuth(requiredRole?: string | string[]) {
  return createMiddleware<AppEnv>(async (c, next) => {
    const auth = c.get('auth')
    const session = await auth.api.getSession({ headers: c.req.raw.headers })

    if (!session || !session.user) {
      throw new HTTPException(401, { message: 'Unauthorized: Please login first' })
    }

    const user = session.user as SessionUser

    if (requiredRole) {
      const roles = Array.isArray(requiredRole) ? requiredRole : [requiredRole]

      // D-04：ADMIN_GITHUB_ID 白名单命中即视为 super_admin，覆盖 DB role
      if (user.githubId) {
        const adminIds = c.env.ADMIN_GITHUB_ID
        if (adminIds && adminIds.split(',').map((s: string) => s.trim()).includes(String(user.githubId))) {
          await next()
          return
        }
      }

      // Super admin always passes
      if (user.role === 'super_admin' || user.role === 'admin') {
        await next()
        return
      }
      if (!roles.includes(user.role)) {
        throw new HTTPException(403, { message: `Forbidden: Requires one of [${roles.join(', ')}] role` })
      }
    }

    await next()
  })
}
```

**修改 apps/api/wrangler.toml：**

在现有 `[vars]` 块末尾追加注释（不写明文值，实际值走 `wrangler secret put`）：

```toml
# ADMIN_GITHUB_ID 通过 wrangler secret put 注入，不在此明文配置
# 本地开发：在 apps/api/.dev.vars 中设置 ADMIN_GITHUB_ID=<your_github_id>
# 格式：单个 ID（"12345678"）或逗号分隔多个（"12345,67890"）
```

**注意：** `c.env.ADMIN_GITHUB_ID` 需要 `AppEnv.Bindings`（即 `Env` 接口）中已声明 `ADMIN_GITHUB_ID?: string`，这在 Plan 01 的 `apps/api/src/lib/auth.ts` 中已完成。如果 `Env` 接口在 `apps/api/src/types.ts` 中单独定义，也需要在那里添加。
  </action>

  <verify>
    <automated>pnpm --filter @starye/api exec tsc --noEmit</automated>
  </verify>

  <done>
- `apps/api/src/middleware/guard.ts` 包含 `ADMIN_GITHUB_ID` 白名单短路分支
- 白名单短路在 `user.role === 'super_admin'` 判断之前
- `apps/api/wrangler.toml` 包含 `ADMIN_GITHUB_ID` 注释说明
- TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/docs 和 /api/openapi.json 加 requireAuth 保护 + 新建 docs-auth.test.ts</name>
  <files>apps/api/src/index.ts, apps/api/src/__tests__/docs-auth.test.ts</files>

  <read_first>
    - apps/api/src/index.ts（完整读取，找到 /api/docs 和 /api/openapi.json 挂载位置，确认 requireAuth import 是否已存在）
    - apps/api/src/middleware/__tests__/error-handler.test.ts（参考测试文件结构）
    - .planning/phases/02-dashboard/02-PATTERNS.md（lines 420-444，index.ts 修改模式）
    - .planning/phases/02-dashboard/02-VALIDATION.md（PUBSEC-04 测试要求）
  </read_first>

  <behavior>
    - 匿名用户（无 session）访问 /api/docs 返回 401
    - 匿名用户（无 session）访问 /api/openapi.json 返回 401
    - 白名单用户（ADMIN_GITHUB_ID 命中）访问 /api/docs 返回 200
    - 本地开发和生产环境都保护（D-17：不用 env 开关）
  </behavior>

  <action>
**修改 apps/api/src/index.ts：**

找到 `/api/openapi.json` 和 `/api/docs` 路由挂载位置（line 170 附近），在两个路由的 handler 前插入 `requireAuth(['admin', 'super_admin'])`（参考 PATTERNS.md line 434-437）：

```typescript
// 修改前（无鉴权）：
.get('/api/openapi.json', openAPIRouteHandler(app, { documentation: {...} }))
.get('/api/docs', Scalar({...}))

// 修改后（加 requireAuth）：
.get('/api/openapi.json', requireAuth(['admin', 'super_admin']), openAPIRouteHandler(app, { documentation: {...} }))
.get('/api/docs', requireAuth(['admin', 'super_admin']), Scalar({...}))
```

确认 `requireAuth` 已在文件顶部 import（`import { requireAuth } from './middleware/guard'`），若未 import 则添加。

**新建 apps/api/src/__tests__/docs-auth.test.ts：**

参考 VALIDATION.md 的 PUBSEC-04 测试要求：

```typescript
/**
 * /api/docs 和 /api/openapi.json 鉴权测试
 * 覆盖：PUBSEC-04（生产环境 /api/docs 需鉴权才能访问）
 */
import { describe, expect, it, vi, beforeEach } from 'vitest'
import app from '../index'

describe('PUBSEC-04: /api/docs 鉴权保护', () => {
  it('匿名访问 /api/docs 返回 401', async () => {
    const req = new Request('http://localhost:8787/api/docs')
    const res = await app.fetch(req, {
      DB: {} as any,
      CACHE: {} as any,
      BETTER_AUTH_SECRET: 'test-secret',
      BETTER_AUTH_URL: 'http://localhost:8787',
      WEB_URL: 'http://localhost:8080',
      ADMIN_URL: 'http://localhost:5173',
      GITHUB_CLIENT_ID: 'test-id',
      GITHUB_CLIENT_SECRET: 'test-secret',
    } as any)
    expect(res.status).toBe(401)
  })

  it('匿名访问 /api/openapi.json 返回 401', async () => {
    const req = new Request('http://localhost:8787/api/openapi.json')
    const res = await app.fetch(req, {
      DB: {} as any,
      CACHE: {} as any,
      BETTER_AUTH_SECRET: 'test-secret',
      BETTER_AUTH_URL: 'http://localhost:8787',
      WEB_URL: 'http://localhost:8080',
      ADMIN_URL: 'http://localhost:5173',
      GITHUB_CLIENT_ID: 'test-id',
      GITHUB_CLIENT_SECRET: 'test-secret',
    } as any)
    expect(res.status).toBe(401)
  })
})
```

**注意：** 如果 `app` 不是默认导出，需要调整 import 方式。如果 `app.fetch` 需要 `ExecutionContext`，传入 `{} as any`。测试中 mock 的 env 对象需要包含所有必需的 Bindings 字段，否则 Better Auth 初始化会失败。
  </action>

  <verify>
    <automated>pnpm --filter @starye/api test --run src/__tests__/docs-auth.test.ts</automated>
  </verify>

  <done>
- `apps/api/src/index.ts` 的 `/api/docs` 路由包含 `requireAuth(['admin', 'super_admin'])` 中间件
- `apps/api/src/index.ts` 的 `/api/openapi.json` 路由包含 `requireAuth(['admin', 'super_admin'])` 中间件
- `apps/api/src/__tests__/docs-auth.test.ts` 存在且测试通过
- 匿名访问 /api/docs 和 /api/openapi.json 均返回 401
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | 用户请求携带 session cookie，API 验证 session 并检查白名单 |
| ADMIN_GITHUB_ID env var | 白名单数据来源，通过 wrangler secret 注入，不在代码中硬编码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | requireAuth 白名单短路 | mitigate | 白名单判断使用 `String()` 转换确保类型一致；`split(',').map(s => s.trim())` 处理空格；`ADMIN_GITHUB_ID` 未配置时短路不生效，回退到 role 检查 |
| T-02-06 | Information Disclosure | /api/docs 暴露 API 结构 | mitigate | `requireAuth(['admin', 'super_admin'])` 保护，匿名访问返回 401；白名单用户通过 D-04 短路自动通过 |
| T-02-10 | Tampering | ADMIN_GITHUB_ID 注入攻击 | mitigate | env var 通过 `wrangler secret put` 注入，不在代码或 wrangler.toml 明文存储；Workers 运行时 env 不可被请求修改 |

</threat_model>

<verification>
## 整体验证

**类型检查：**
```bash
pnpm --filter @starye/api exec tsc --noEmit
```

**单元测试：**
```bash
pnpm --filter @starye/api test --run
```

**手动验证（可选）：**
1. 启动 API Worker：`pnpm --filter @starye/api dev`
2. 匿名访问 /api/docs：
```bash
curl -I http://localhost:8787/api/docs
# 期望：HTTP/1.1 401 Unauthorized
```
3. 带有效 session 访问 /api/docs：
```bash
curl -I http://localhost:8787/api/docs \
  -H "Cookie: starye.session_token=<valid_token>"
# 期望：HTTP/1.1 200 OK（白名单用户）
```

</verification>

<success_criteria>
1. `apps/api/src/middleware/guard.ts` 包含 ADMIN_GITHUB_ID 白名单短路分支，位于 `user.role === 'super_admin'` 判断之前
2. 白名单短路逻辑：`adminIds.split(',').map(s => s.trim()).includes(String(user.githubId))`
3. `apps/api/src/index.ts` 的 `/api/docs` 和 `/api/openapi.json` 路由均包含 `requireAuth(['admin', 'super_admin'])` 中间件
4. `apps/api/wrangler.toml` 包含 ADMIN_GITHUB_ID 注释说明（不含明文值）
5. `apps/api/src/__tests__/docs-auth.test.ts` 测试通过，验证匿名访问 401
6. TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard/02-02-SUMMARY.md`
</output>
