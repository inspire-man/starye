---
phase: 21-source-health-and-local-repair-players-vertical-slice
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified:
  - apps/api/src/routes/admin/crawler-tasks/index.ts
  - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
  - apps/api/src/routes/internal/crawler-runs/index.ts
  - apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts
autonomous: true
requirements: [SRC-02, REP-01]
must_haves:
  truths:
    - "Dashboard mutation 经过 authenticated session/resource permission、current movie reread、二次确认后才创建固定 repair_players task。"
    - "POST /api/admin/crawler-tasks/repair-players 的请求只表达 movie identity、当前 reason 和 restore_playable_sources；server 负责 operation/template/target/workflow/secrets。"
    - "signed internal runner route 能提交 bounded source observation、readback 和 terminal repair receipt，并按 runId/attempt/sequence/eventId/sourceRevision 处理 replay/conflict。"
  artifacts:
    - path: "apps/api/src/routes/admin/crawler-tasks/index.ts"
      provides: "authenticated repair-players command route and bounded task/readback projection"
      contains: "/repair-players"
    - path: "apps/api/src/routes/internal/crawler-runs/index.ts"
      provides: "signed operation-aware observation and event callback boundary"
      contains: "source-observation"
    - path: "apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts"
      provides: "admin auth/input/current-disposition/confirmation contract tests"
    - path: "apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts"
      provides: "signed observation, receipt, replay and stale conflict tests"
  key_links:
    - from: "admin/crawler-tasks/index.ts"
      to: "crawler-tasks/repository.ts"
      via: "route creates only the validated server-owned repair snapshot"
    - from: "internal/crawler-runs/index.ts"
      to: "source-reconciliation.ts"
      via: "signed source observation enters persistence/readback service"
    - from: "internal/crawler-runs/index.ts"
      to: "receipt-validation.ts"
      via: "terminal event accepts only repair-specific validated receipt"
---

<objective>
把 repair_players 契约发布为专用 admin mutation 与 signed internal runner boundary，形成 Dashboard -> task/run/attempt -> observation/readback 的可调用 API。

Purpose: D-05 到 D-08 固定用户入口与输入边界，D-11 到 D-15 固定 runner callback、receipt 和 stale/replay 保护。
Output: admin repair route、safe task projection、signed observation callback、route regression tests。
</objective>

## Artifacts this phase produces

- POST /api/admin/crawler-tasks/repair-players、bounded admin task/readback DTO and route tests。
- Signed internal source-observation callback、operation/sourceRevision replay checks and internal route tests。

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-CONTEXT.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-RESEARCH.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-PATTERNS.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-03-PLAN.md
@.planning/phases/20-source-contract-receipt-boundary-and-sun-064/20-03-SUMMARY.md
</context>

<assumption_delta_decision>
采用 add-alongside：增加 repair-specific admin/internal route，与既有 generic crawler route 和 signed event route 并存；同一 movie.id、sourceRevision 和 operation snapshot 贯穿两条 route。理由是路由级显式分流能阻止 repair input 借用普通 crawler contract，也让 permission、signature、readback 各自保持现有责任边界。接受的债务是 task detail/read model 需要同时兼容 operation 和旧 template rows。
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 发布 authenticated repair-players admin route 与安全 task projection</name>
  <files>apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</files>
  <read_first>
    - apps/api/src/routes/admin/crawler-tasks/index.ts
    - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
    - apps/api/src/schemas/crawler-tasks.ts
    - apps/api/src/domain/crawler-tasks/repository.ts
    - apps/api/src/domain/crawler-tasks/template-registry.ts
  </read_first>
  <depends_on>21-03 的 operation registry、receipt validator、repository/state-machine 已完成。</depends_on>
  <action>在 admin crawler task router 增加 POST /api/admin/crawler-tasks/repair-players：复用 requireSessionUser、crawler resource/template access 和 movie lookup，先重新读取当前 movie readiness/disposition/sourceRevision，再验证 reason 只匹配 no_source 或 source_failed，targetIntent 只接受 restore_playable_sources，最后调用 repository 创建单电影 repair snapshot，落实 D-06、D-07、D-09。route response 与 task detail 只返回 bounded operation/status/attempt/observedAt/reason/allowed next action；adapter、workflow、target、URL、command、secret 与 runner raw output 均由 server registry/snapshot 保留，落实 D-03、D-05、D-08、D-16。测试 session/resource denial、movie not found、reason/revision drift、extra executable fields、fixed intent、single movie、second-confirmation token、safe projection 和 cache/readback linkage。</action>
  <acceptance_criteria>
    - exact path POST /api/admin/crawler-tasks/repair-players 存在并受 session/resource guard 保护。
    - route 在 task creation 前重新读取 current disposition/revision，并拒绝 stale reason 或不允许的 intent。
    - response/task detail 没有 raw URL、command、workflow、secret、page、exception 或 signature fields。
    - accepted command 创建 operation=repair_players 且只绑定一个 movieId。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter api exec vitest run src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</automated>
  </verify>
  <done>D-03、D-05、D-06、D-07、D-08、D-09、D-16 的 admin command 和 safe projection 已可被 Dashboard typed client 调用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 发布 signed source-observation 与 operation-aware runner callback route</name>
  <files>apps/api/src/routes/internal/crawler-runs/index.ts, apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</files>
  <read_first>
    - apps/api/src/routes/internal/crawler-runs/index.ts
    - apps/api/src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts
    - apps/api/src/domain/movies/source-reconciliation.ts
    - apps/api/src/domain/crawler-tasks/repository.ts
    - apps/api/src/domain/crawler-tasks/receipt-validation.ts
  </read_first>
  <depends_on>Task 1 的 admin-created snapshot path 与 21-03 的 signed event/repository validation 已完成。</depends_on>
  <action>在 internal crawler run router 增加 operation-aware signed source-observation callback（沿用现有 /api/internal/crawler-runs/:runId path，新增 source-observation action）：复用 runner-event-auth/event-signer 校验 timestamp、path/body hash、callback key、runId、attempt、sequence 和 eventId，再确认 snapshot.operation=repair_players 与 sourceRevision，调用 acceptRepairSourceObservation 并返回 bounded authoritative readback/repair receipt candidate，落实 D-10、D-11、D-12、D-13、D-15。普通 crawler callback 保持原分支。测试签名失效、operation mismatch、sourceRevision mismatch、duplicate exact replay、conflicting replay、old attempt、out-of-order sequence、successful readback 与 bounded failure response，并确认 raw runner/source material 永不进入 response。</action>
  <acceptance_criteria>
    - signed callback 只有在 operation、run/attempt/sequence/eventId/sourceRevision 全部匹配时才写 observation。
    - identical event replay 返回幂等受控结果；conflict/stale/old event 不能改变 current source health。
    - callback success 返回同一 movie/sourceRevision 的 bounded readback/receipt candidate，进程退出与普通 receipt 不足以构成 success。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter api exec vitest run src/routes/internal/crawler-runs/__tests__/crawler-runs.route.test.ts</automated>
  </verify>
  <done>D-10、D-11、D-12、D-13、D-15 的 signed observation/receipt boundary 已连接 repository 与 reconciliation service。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> admin route | authenticated session 与 resource permission 保护 mutation。 |
| Runner -> internal route | signed callback envelope 保护 observation/event 写入。 |
| Route -> repository/service | route 只传 typed bounded identity，业务事实由 server readback 产生。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-13 | Elevation | repair-players admin route | high | mitigate | reuse session/resource guards, canonical movie lookup and disposition gate。 |
| T-21-14 | Tampering | internal source-observation route | high | mitigate | reuse signed runner auth/body hashing and validate run/attempt/sequence/eventId/sourceRevision。 |
| T-21-15 | Information Disclosure | task/readback response | high | mitigate | allowlisted DTO projection plus route tests with raw source/request/signature sentinels。 |
| T-21-16 | Repudiation | duplicate/conflicting callback | high | mitigate | repository event identity and controlled duplicate/conflict result are returned and audited。 |
</threat_model>

<verification>
运行 admin/internal route focused tests、API type-check，并检查 route mount/export 使 Gateway-relative paths可达。
</verification>

<success_criteria>
浏览器 command 与 signed runner observation 各自经过正确 trust boundary，返回的 task/readback/receipt projection 只含 bounded state。
</success_criteria>

<output>
Create .planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-04-SUMMARY.md when done.
</output>
