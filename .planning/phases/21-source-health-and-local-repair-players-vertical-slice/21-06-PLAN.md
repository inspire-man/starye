---
phase: 21-source-health-and-local-repair-players-vertical-slice
plan: 06
type: execute
wave: 5
depends_on:
  - 21-04
files_modified:
  - apps/dashboard/src/lib/api.ts
  - apps/dashboard/src/views/Crawlers.vue
  - apps/dashboard/src/views/__test__/Crawlers.test.ts
  - apps/movie-app/src/views/MovieDetail.vue
  - apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
autonomous: true
requirements: [SRC-02, REP-01]
must_haves:
  truths:
    - "Dashboard source rows 显示 direct/magnet/TorrServer、inactive/unverified/failed、observedAt/bounded reason 和 eligibility；inactive 可见但不会成为 repair/play candidate。"
    - "Dashboard repair command 经二次确认后只发送 canonical movie identity、当前 reason 和 restore_playable_sources，并刷新 task/readback 状态。"
    - "MovieDetail 保持 informational boundary：展示 source health/readiness 与 Dashboard repair/task handoff，页面自身没有 admin mutation 调用。"
    - "task/source health UI 只显示 bounded status、attempt、observedAt、bounded failure reason 和 allowed next action，raw runner/source/request/exception/signature 不进入 DOM。"
  artifacts:
    - path: "apps/dashboard/src/lib/api.ts"
      provides: "typed repair command, source health rows, operation/receipt/readback DTOs"
    - path: "apps/dashboard/src/views/Crawlers.vue"
      provides: "confirmation-gated repair entry, polling and bounded task/source status"
    - path: "apps/movie-app/src/views/MovieDetail.vue"
      provides: "informational source health/readiness and Dashboard handoff"
    - path: "apps/dashboard/src/views/__test__/Crawlers.test.ts"
      provides: "confirmation, payload redaction, bounded UI and polling tests"
  key_links:
    - from: "apps/dashboard/src/views/Crawlers.vue"
      to: "apps/dashboard/src/lib/api.ts"
      via: "confirmed UI action calls typed repair-players wrapper with fixed intent"
    - from: "apps/movie-app/src/views/MovieDetail.vue"
      to: "Dashboard repair/task handoff"
      via: "informational link/action carries same movie identity without admin mutation"
    - from: "apps/dashboard/src/lib/api.ts"
      to: "apps/api/src/routes/admin/crawler-tasks/index.ts"
      via: "Gateway-relative typed request/response shapes match the dedicated route"
---

<objective>
把 API 的 bounded source health 与 repair task projection 接入 Dashboard，并让 MovieDetail 仅提供状态与 Dashboard handoff。

Purpose: D-05、D-08、D-16 固定 UI mutation/confirmation/redaction 边界，SRC-02 需要 per-source health 真实可见。
Output: typed Dashboard API wrapper、confirmation/polling UI、MovieDetail informational handoff 与 DOM regression tests。
</objective>

## Artifacts this phase produces

- Typed Dashboard repair/source-health/readback API wrappers, confirmation dialog flow, bounded polling/status UI and tests。
- MovieDetail source-health/readiness informational projection, Dashboard repair/task handoff and DOM redaction tests。

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-CONTEXT.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-RESEARCH.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-PATTERNS.md
@.planning/phases/21-source-contract-receipt-boundary-and-sun-064/20-03-SUMMARY.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-04-PLAN.md
</context>

<assumption_delta_decision>
采用 add-alongside：在 Crawlers.vue 增加 repair/source-health focal surface，与既有 task detail/status projection 并存；MovieDetail 只复用 movie.id/readiness DTO 并导航到 Dashboard 状态入口。理由是 D-05 把 mutation ownership 固定在 Dashboard，且现有 Phase 20 readiness UI 可继续消费同形状 projection。接受的债务是跨 app 的 task handoff 依赖同一 Gateway cache/readback freshness contract。
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 完成 Dashboard typed repair command、source health 与 confirmation/polling UI</name>
  <files>apps/dashboard/src/lib/api.ts, apps/dashboard/src/views/Crawlers.vue, apps/dashboard/src/views/__test__/Crawlers.test.ts</files>
  <read_first>
    - apps/dashboard/src/lib/api.ts
    - apps/dashboard/src/views/Crawlers.vue
    - apps/dashboard/src/views/__test__/Crawlers.test.ts
    - apps/api/src/routes/admin/crawler-tasks/index.ts
    - apps/api/src/domain/movies/source-contract.ts
  </read_first>
  <depends_on>21-04 的 admin repair route、safe task projection 和 source observation readback route 已完成。</depends_on>
  <action>扩展 dashboard api.ts 的 typed source-health rows、repair command/result、operation/receipt/readback unions，所有请求使用 Gateway-relative admin path 与 encoded IDs。修改 Crawlers.vue：在 bounded readiness/source-health 行提供单电影 repair entry，保留 direct/magnet/TorrServer、inactive/unverified/failed、observedAt、bounded reason、attempt、allowed next action；使用现有 ConfirmDialog 做二次确认，确认摘要只显示影片名称、当前 no_source/source_failed reason 和恢复可播放源意图，确认完成后才 POST fixed targetIntent 并启动可见 task polling，落实 D-01、D-02、D-04、D-06、D-07、D-08、D-16。请求 wrapper 与 UI state 只持有 movie identity/reason/intent，server-owned target/workflow/secret 和 runner raw output 从类型、请求、DOM 及测试 fixture 排除。沿用现有 resource guard、info/success feedback、design tokens 与 visible-only polling。测试 confirm-before-submit、exact payload、inactive display/eligibility、bounded failure/receipt、attempt/observedAt polling、raw sentinel absence。</action>
  <acceptance_criteria>
    - 未确认时没有 repair POST；确认摘要只包含影片名称、当前受控原因和恢复可播放源意图。
    - POST payload 只包含 canonical movie identity、current reason 和 fixed restore_playable_sources。
    - source rows 显示三类 sourceType、三类 health、observedAt/reason 与 eligibility；inactive 保留展示且不成为 eligible action。
    - task UI 只显示 bounded status/attempt/time/reason/next action，raw runner/source/request/signature sentinel 不出现在 DOM。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter dashboard exec vitest run src/views/__test__/Crawlers.test.ts</automated>
  </verify>
  <done>D-01、D-02、D-04、D-06、D-07、D-08、D-16 的 Dashboard command/source-health/readback UX 与 focused UI tests 已完成。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 保持 MovieDetail informational 并连接 Dashboard repair/task handoff</name>
  <files>apps/movie-app/src/views/MovieDetail.vue, apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts</files>
  <read_first>
    - apps/movie-app/src/views/MovieDetail.vue
    - apps/movie-app/src/views/__tests__/MovieDetail.dom-contract.test.ts
    - apps/movie-app/src/types.ts
    - apps/dashboard/src/views/Crawlers.vue
    - .planning/phases/20-source-contract-receipt-boundary-and-sun-064/20-03-SUMMARY.md
  </read_first>
  <depends_on>Task 1 的 Dashboard route/handoff shape 已完成；Phase 20 MovieDetail readiness projection 保持可用。</depends_on>
  <action>在 MovieDetail 中消费同一 movie.id 的 source health/readiness projection，展示 source type、health、observedAt、bounded reason、eligibility 与受控下一步动作；repairable/no_source/source_failed 状态的入口导航到 Dashboard repair/task 状态，并保留 ready、receipt 和 browser playback proof 的独立语义，落实 D-01、D-03、D-04、D-05、D-16。页面保持 informational：只读或导航，不调用 admin repair mutation；raw source URL、page、exception、runner output、signature material 不渲染。保留现有 DOM roles/data attributes，并增加测试证明 Dashboard handoff 使用同一 movie identity、MovieDetail 没有直接 repair POST、bounded fields 可见且 redacted fields 不在 DOM。</action>
  <acceptance_criteria>
    - MovieDetail 能显示 per-source bounded health 与 readiness，不将 source health 或 ready 误报为 browser playback proof。
    - repairable 状态的操作进入 Dashboard/task handoff，MovieDetail 自身没有 admin mutation request。
    - raw source/request/page/exception/runner/signature sentinel 不进入 DOM。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/movie-app exec vitest run src/views/__tests__/MovieDetail.dom-contract.test.ts</automated>
  </verify>
  <done>D-01、D-03、D-04、D-05、D-16 的 MovieDetail informational boundary 已由 DOM contract tests 证明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard UI -> admin API | confirmation 后才跨入 mutation route，payload 受 typed wrapper 限制。 |
| API projection -> DOM | validated bounded DTO 进入 task/source health surfaces。 |
| MovieDetail -> Dashboard handoff | informational navigation 保持 movie identity，不拥有 admin mutation。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-21 | Elevation | Dashboard repair action | high | mitigate | ConfirmDialog、resource guard、current reason/identity DTO 和 fixed intent wrapper。 |
| T-21-22 | Information Disclosure | Dashboard/MovieDetail DOM | high | mitigate | bounded DTO types、redacted fixtures、raw sentinel DOM assertions。 |
| T-21-23 | Tampering | client payload | high | mitigate | strict client type plus server route validation; client never selects target/workflow/secret。 |
| T-21-24 | Tampering | stale UI read model | medium | mitigate | accepted task triggers polling and cache/readback refresh for same movie identity。 |
</threat_model>

<verification>
运行 Dashboard 与 MovieDetail focused tests、各 app type-check，并确认 UI 使用 Gateway-relative API wrapper 与现有 design tokens。
</verification>

<success_criteria>
用户在 Dashboard 可确认并发起固定 repair_players command，在 task/source rows 看到 bounded lifecycle；MovieDetail 只显示状态并导航到 Dashboard。
</success_criteria>

<output>
Create .planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-06-SUMMARY.md when done.
</output>
