---
phase: 21-source-health-and-local-repair-players-vertical-slice
plan: 05
type: execute
wave: 5
depends_on:
  - 21-04
files_modified:
  - packages/crawler/src/task-runner/repair-adapter.ts
  - packages/crawler/src/task-runner/template-adapters.ts
  - packages/crawler/src/task-runner/local-runner.ts
  - packages/crawler/src/task-runner/__tests__/local-runner.test.ts
  - packages/crawler/src/task-runner/__tests__/template-adapters.test.ts
  - packages/crawler/src/task-runner/runner-client.ts
  - packages/crawler/src/task-runner/__tests__/runner-client.test.ts
autonomous: true
requirements: [SRC-02, REP-01]
must_haves:
  truths:
    - "local runner 根据 snapshot.operation 选择专用 repair_players adapter；普通 movie adapter 继续使用原选择，缺少/错配 operation 时 fail closed。"
    - "repair adapter 通过 signed controlled API 提交 source observation，等待 authoritative readback/repair receipt，再把 bounded terminal result 交给 runner client。"
    - "runner envelope 绑定 task/run/attempt/sequence/eventId/sourceRevision，取消、readback failure 和 bounded retry 结果沿用既有 lifecycle，raw source/page/exception/signature 不进入 terminal payload。"
  artifacts:
    - path: "packages/crawler/src/task-runner/repair-adapter.ts"
      provides: "dedicated repair_players adapter with injected checkpoint/fetch fixture"
    - path: "packages/crawler/src/task-runner/template-adapters.ts"
      provides: "operation-first adapter registry selection"
    - path: "packages/crawler/src/task-runner/runner-client.ts"
      provides: "signed repair observation/receipt event envelope"
  key_links:
    - from: "packages/crawler/src/task-runner/template-adapters.ts"
      to: "packages/crawler/src/task-runner/repair-adapter.ts"
      via: "operation=repair_players selects only the dedicated adapter"
    - from: "packages/crawler/src/task-runner/repair-adapter.ts"
      to: "apps/api/src/routes/internal/crawler-runs/index.ts"
      via: "runner client sends controlled signed source-observation request"
    - from: "packages/crawler/src/task-runner/local-runner.ts"
      to: "packages/crawler/src/task-runner/runner-client.ts"
      via: "validated repair receipt/readback controls terminal event"
---

<objective>
把本地 poll/claim/heartbeat runner 接入专用 repair_players adapter 和 signed receipt envelope，完成 API route 之后的实际 local execution seam。

Purpose: D-10、D-11、D-12 要求 repair adapter 与普通 crawler 显式分离，并把服务端 readback 置于 succeeded 之前。
Output: dedicated adapter、operation-aware selection、runner client envelope、local runner regressions。
</objective>

## Artifacts this phase produces

- packages/crawler/src/task-runner/repair-adapter.ts、repairPlayersAdapter registry branch and local-runner receipt/cancellation integration。
- Signed repair observation/receipt runner envelope and adapter/runner-client focused tests。

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-CONTEXT.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-RESEARCH.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-PATTERNS.md
@.planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-04-PLAN.md
</context>

<assumption_delta_decision>
采用 add-alongside：repairPlayersAdapter 作为 template adapter registry 的 operation branch，与 movieAdapter 并列；runner-client 保留普通 terminal envelope，并增加 repair observation/receipt payload discriminator。理由是显式 adapter 选择可让普通 crawler 行为稳定，同时对 repair 的 server readback prerequisite 做类型约束。接受的债务是本地 runner 仍共享 poll/claim/heartbeat outer loop，operation-specific behavior 由 adapter/route contract 负责。
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 repair_players adapter 与 operation-first registry selection</name>
  <files>packages/crawler/src/task-runner/repair-adapter.ts, packages/crawler/src/task-runner/template-adapters.ts, packages/crawler/src/task-runner/local-runner.ts, packages/crawler/src/task-runner/__tests__/local-runner.test.ts, packages/crawler/src/task-runner/__tests__/template-adapters.test.ts</files>
  <read_first>
    - packages/crawler/src/task-runner/movie-adapter.ts
    - packages/crawler/src/task-runner/controlled-adapter.ts
    - packages/crawler/src/task-runner/template-adapters.ts
    - packages/crawler/src/task-runner/local-runner.ts
    - packages/crawler/src/task-runner/__tests__/local-runner.test.ts
    - packages/crawler/src/task-runner/__tests__/template-adapters.test.ts
  </read_first>
  <depends_on>21-04 的 signed source-observation callback 与 repair receipt candidate response 已完成。</depends_on>
  <action>新增 repair-adapter.ts，沿用 movie/controlled adapter 的 checkpoint、injected fetch 和 cooperative cancellation 形状；adapter 解析并校验 operation=repair_players、single movieId、sourceRevision、server-owned target，调用 runner-client 的 signed source-observation API，等待 bounded authoritative readback/repair receipt 后返回 terminal result，任何 raw URL/page/exception 只留在服务端处理，落实 D-10、D-11、D-12、D-13、D-16。修改 template-adapters.ts 使 operation 先于 template fallback：repair snapshot 只能选 repairPlayersAdapter，ordinary movie/manga 仍走原 registry，missing/mismatch operation fail closed，落实 D-10。接入 local-runner.ts 的 adapter result/receipt gate：adapter readback failure 进入 controlled failure/retry path，cancel_requested 保持未成功，receipt missing 不进入 success。测试 selection、missing operation、operation/template mismatch、controlled API request、receipt handoff、readback failure、cancellation 和 raw-field exclusion。</action>
  <acceptance_criteria>
    - operation=repair_players 永远选择 repairPlayersAdapter；ordinary movie/manga 流程 focused tests 继续通过。
    - adapter 通过受控 signed API 获得 readback/receipt 后才返回 success candidate。
    - missing operation、mismatch、cancel_requested、receipt missing 和 readback failure 都进入受控 lifecycle result。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/crawler exec vitest run src/task-runner/__tests__/local-runner.test.ts src/task-runner/__tests__/template-adapters.test.ts</automated>
  </verify>
  <done>D-10、D-11、D-12、D-13、D-16 的 dedicated adapter、fail-closed registry 和 terminal receipt gate 已通过 runner tests。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 runner-client 的 signed repair envelope</name>
  <files>packages/crawler/src/task-runner/runner-client.ts, packages/crawler/src/task-runner/__tests__/runner-client.test.ts</files>
  <read_first>
    - packages/crawler/src/task-runner/runner-client.ts
    - packages/crawler/src/task-runner/__tests__/runner-client.test.ts
    - apps/api/src/routes/internal/crawler-runs/index.ts
    - apps/api/src/domain/crawler-tasks/types.ts
  </read_first>
  <depends_on>Task 1 的 repair adapter 调用契约已确定；21-04 的 internal callback path 已完成。</depends_on>
  <action>扩展 runner-client 的 candidate/snapshot parsing 和 serialized signed body，增加 repair observation request 与 dedicated repair receipt/event envelope；每次请求携带 task/run/attempt/sequence/eventId/sourceRevision identity，沿用现有 API base URL、timeout、callback key id 和 body signing，callback secret 只存在 runner config，落实 D-11、D-13、D-15。保持普通 runner event envelope shape 与已有 tests；新增 assertions 检查 repair payload discriminator、operation/movie identity、readback fields，以及 raw source URL/page/exception/signature material 不出现在 terminal envelope。</action>
  <acceptance_criteria>
    - signed repair body 可被 internal route 解析并绑定同一 run/attempt/sequence/eventId/sourceRevision。
    - ordinary runner event behavior 不变，repair receipt 与 ordinary contentIds/templateKey receipt 可区分。
    - terminal payload 只含 bounded repair summary 和状态 identity，callback secret 与 raw runner/source material永不进入 payload。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @starye/crawler exec vitest run src/task-runner/__tests__/runner-client.test.ts</automated>
  </verify>
  <done>D-11、D-13、D-15 的 signed repair envelope 与普通 runner client 已完成 typed regression coverage。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local runner -> internal API | adapter 发出的 observation request 经过签名与 operation identity 验证。 |
| Runner config -> envelope | callback secret 参与签名但只留在 runner process/config。 |
| Adapter result -> terminal event | readback/receipt gate 决定 terminal success。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-17 | Tampering | operation adapter selection | high | mitigate | operation-first registry 与 fail-closed mismatch handling。 |
| T-21-18 | Spoofing | runner-client callback | high | mitigate | reuse event signer/body hash/key id/time validation，保持 secret server-side/runner-side。 |
| T-21-19 | Information Disclosure | repair terminal envelope | high | mitigate | dedicated bounded receipt payload 与 raw-field sentinel tests。 |
| T-21-20 | Denial of Service | local runner retry/cancel | medium | mitigate | reuse one active run、heartbeat cancellation 和 repository bounded retry classification。 |
</threat_model>

<verification>
运行 crawler focused tests 与 type-check，确认 repair adapter、ordinary adapter、signed envelope 和 cancel/readback paths 都有结果断言。
</verification>

<success_criteria>
本地 runner 能从 operation snapshot 选择 repair adapter，经 signed observation/readback 获得专用 receipt，再将 bounded terminal event 交回 control plane。
</success_criteria>

<output>
Create .planning/phases/21-source-health-and-local-repair-players-vertical-slice/21-05-SUMMARY.md when done.
</output>
