---
phase: 01-auth-gateway
plan: 05
type: execute
wave: 2
depends_on: [01, 04]
files_modified:
  - apps/api/src/routes/auth/__tests__/signout.test.ts
autonomous: true
requirements:
  - AUTH-08
tags: [auth, signout, test]
must_haves:
  truths:
    - "D-15: /api/auth/sign-out 调用后 better-auth 真正删除 D1 session 行并回 `Set-Cookie` 清除头；同一 session cookie 重放请求 `auth.api.getSession()` 返回 null（mock authInstance.handler 可观测）"
    - "sign-out 响应带 `Set-Cookie` 清除头（`starye.session_token=...; Max-Age=0` 或等价的过期策略）"
    - "Plan 01 留下的 3 条 `it.todo` 全部转为通过的活用例"
    - "测试基于 mock authInstance.handler，不启动真实 Better Auth + libsql（轻量集成）"
    - "跨 tab 登出不主动广播（D-16）—— 不在本单测范围，仅在文件注释中记录"
    - "D-17: 不引入 BroadcastChannel / visibilitychange 跨 tab 登出广播；同浏览器其他 tab 下一次带 cookie 请求时由 401 + 前端 redirect 自然回收（由 Phase 2 `requireAuth` 中间件承接）"
  artifacts:
    - path: "apps/api/src/routes/auth/__tests__/signout.test.ts"
      provides: "AUTH-08 服务端失效单测"
      contains: "Set-Cookie"
  key_links:
    - from: "apps/api/src/routes/auth/__tests__/signout.test.ts"
      to: "apps/api/src/routes/auth/index.ts catch-all /*"
      via: "mock authInstance.handler 返回带 Set-Cookie 的 Response"
      pattern: "catch-all"
---

<objective>
把 Plan 01 在 `apps/api/src/routes/auth/__tests__/signout.test.ts` 留下的 3 条 `it.todo` 转为完整通过的 `it`，覆盖 AUTH-08 的服务端真实失效语义。采用 mock handler 路径（见 01-PATTERNS.md L191），而非集成 betterAuth() + libsql，以保持单测 < 1s 级别。

Purpose: 闭合 T4（cookie 清除但 D1 session 行未删）威胁；闭合 AUTH-08 需求的可自动化验证入口。D-16 的跨 tab 不广播行为通过 E2E 覆盖（Plan 06 手动冒烟），本 plan 聚焦单测。

Output:
- `apps/api/src/routes/auth/__tests__/signout.test.ts` 三条 `it` 转为活用例（D-15 #1..#3）
- 若需要可复用 helper（例如构造带 Set-Cookie 的 mock handler），在同文件内定义，不扩散到 `apps/api/src/test/helpers.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-gateway/01-CONTEXT.md
@.planning/phases/01-auth-gateway/01-RESEARCH.md
@.planning/phases/01-auth-gateway/01-PATTERNS.md
@.planning/phases/01-auth-gateway/01-VALIDATION.md
@.planning/phases/01-auth-gateway/01-01-PLAN.md
@CLAUDE.md

<interfaces>
**authRoutes 的 /sign-out 路径**（`apps/api/src/routes/auth/index.ts:83-87`）是 catch-all 代理：
```ts
.on(['POST', 'GET'], '/*', (c) => {
  const authInstance = c.get('auth')
  return authInstance.handler(c.req.raw)
})
```

**测试策略：** mock `authInstance.handler` 返回预制 Response，断言：
1. 响应状态码（Better Auth 原生返回 200）
2. 响应 `Set-Cookie` 头含清除模式（`starye.session_token=; Max-Age=0` 或 `Expires=Thu, 01 Jan 1970`）
3. 同 test 内第二次调用 `auth.api.getSession` 已被 mock 为返回 null（模拟 D1 session 行被删除后的状态）

**Cookie 名约定：** `apps/api/src/lib/auth.ts:97-107` 配置 `advanced.cookiePrefix: 'starye'`，Better Auth 会生成 `starye.session_token`（原生命名规则 `${prefix}.session_token`）。

**Plan 01 已导出 `createApp`：** 本 plan 直接复用 `createApp(db, auth, user?)` 工厂。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实装 signout.test.ts 三条用例</name>
  <read_first>
    - apps/api/src/routes/auth/__tests__/signout.test.ts（Plan 01 骨架）
    - apps/api/src/routes/auth/index.ts（L83-87 catch-all；确认 mock 什么函数）
    - apps/api/src/test/helpers.ts（L9-101：createMockDb / createMockAuth / createMockUser）
    - apps/api/src/routes/public/progress/__tests__/progress.test.ts（L1-94 full file：createApp 工厂模式）
    - .planning/phases/01-auth-gateway/01-CONTEXT.md L54-57（D-15/D-16/D-17）
    - .planning/phases/01-auth-gateway/01-PATTERNS.md L152-193（signout.test 的 analog 与 mock handler 建议）
  </read_first>
  <behavior>
    - D-15 #1: 构造 mock auth，其 `handler` 返回 `new Response(null, { status: 200, headers: { 'set-cookie': 'starye.session_token=; Path=/; Max-Age=0; HttpOnly; Secure; SameSite=Lax' } })`；向 app 发 `POST /sign-out` → 响应 200，响应头含 `set-cookie` 且匹配清除模式
    - D-15 #2: sign-out 之后，调用者再次 `auth.api.getSession({ headers: 新 request.headers })`（headers 为空或包含清除后的 cookie）→ 返回 null（mock 行为，模拟 D1 session 行已删）
    - D-15 #3: Set-Cookie 头值的精确字符串断言：匹配 `starye.session_token=`（即清除的是正确的 cookie 名）
  </behavior>
  <action>
    编辑 `apps/api/src/routes/auth/__tests__/signout.test.ts`，把 Plan 01 的 3 条 `it.todo` 替换为完整 `it(...)`。

    **createApp 签名调和（Plan 01 vs Plan 05 INFO）：** Plan 01 骨架导出的 `createApp(db, auth, user?)` 使用 `auth: ReturnType<typeof createMockAuth>`；本 Plan 05 需要 `auth: Auth`（更宽松，允许自定义 handler）。本 task 把 createApp 的 auth 参数类型改为 `Auth`（从 `apps/api/src/lib/auth.ts` import），并更新 Plan 01 遗留的 JSDoc 说明新增自定义 handler 能力。函数体不变（依旧 `c.set('auth', auth)`）。下游任何使用 createMockAuth 的测试继续兼容，因为 createMockAuth 的返回值已经是 `Auth` 子类型。

    **参考实现（Plan 05 需落地）：**

    ```ts
    import type { AppEnv } from '../../../types'
    import type { Auth } from '../../../lib/auth'
    import { Hono } from 'hono'
    import { describe, expect, it, vi } from 'vitest'
    import { createMockAuth, createMockDb, createMockUser } from '../../../test/helpers'
    import { authRoutes } from '../index'

    /**
     * 构造一个 mock Better Auth handler：
     * - /sign-out POST → 200 + Set-Cookie 清除头；把内部 session 状态切为 null
     * - 其他路径 → 503（不关心）
     */
    function createSignOutCapableAuth(initialSession: any): Auth {
      let currentSession = initialSession

      const handler = vi.fn(async (req: Request) => {
        const url = new URL(req.url)
        if (url.pathname.endsWith('/sign-out')) {
          currentSession = null
          return new Response(null, {
            status: 200,
            headers: {
              // better-auth 1.6.x 的 Set-Cookie 模式：prefix + 清除 attributes
              'set-cookie': 'starye.session_token=; Path=/; Max-Age=0; HttpOnly; Secure; SameSite=Lax',
            },
          })
        }
        return new Response('not mocked', { status: 503 })
      })

      return {
        api: {
          getSession: vi.fn(async () => currentSession),
        },
        handler,
      } as unknown as Auth
    }

    export function createApp(
      db: ReturnType<typeof createMockDb>,
      auth: Auth,
      user?: ReturnType<typeof createMockUser> | null,
    ) {
      const app = new Hono<AppEnv>()
      app.use('*', async (c, next) => {
        c.set('db', db)
        c.set('auth', auth)
        if (user !== undefined) c.set('user', user as any)
        await next()
      })
      app.route('/', authRoutes)
      return app
    }

    describe('AUTH-08: /api/auth/sign-out server-side invalidation', () => {
      it('D-15 #1: POST /sign-out 返回 200 并回 Set-Cookie 清除头（Max-Age=0）', async () => {
        const db = createMockDb()
        const user = createMockUser({ id: 'u1' })
        const auth = createSignOutCapableAuth({ user, session: { id: 's1' } })
        const app = createApp(db, auth, user)

        const res = await app.request('/sign-out', { method: 'POST' })
        expect(res.status).toBe(200)
        const setCookie = res.headers.get('set-cookie')
        expect(setCookie).toContain('starye.session_token=')
        expect(setCookie).toContain('Max-Age=0')
      })

      it('D-15 #2: sign-out 后 auth.api.getSession 返回 null', async () => {
        const db = createMockDb()
        const user = createMockUser({ id: 'u1' })
        const auth = createSignOutCapableAuth({ user, session: { id: 's1' } })
        const app = createApp(db, auth, user)

        // 先调一次 getSession，确认初始有值
        expect(await auth.api.getSession({ headers: new Headers() } as any)).not.toBeNull()

        // 执行登出
        await app.request('/sign-out', { method: 'POST' })

        // 再调 getSession，确认已失效
        expect(await auth.api.getSession({ headers: new Headers() } as any)).toBeNull()
      })

      it('D-15 #3: sign-out 回给浏览器的 Set-Cookie 精确匹配 starye.session_token=', async () => {
        const db = createMockDb()
        const user = createMockUser({ id: 'u1' })
        const auth = createSignOutCapableAuth({ user, session: { id: 's1' } })
        const app = createApp(db, auth, user)

        const res = await app.request('/sign-out', { method: 'POST' })
        const setCookie = res.headers.get('set-cookie') ?? ''
        // 清除 cookie 的三种常见表达之一必须出现
        const clearsCookie = /starye\.session_token=(;|\s*Max-Age=0|\s*Expires=Thu,\s*01\s*Jan\s*1970)/i.test(setCookie)
        expect(clearsCookie).toBe(true)
      })
    })
    ```

    **关键约束：**
    - 不要引入 `better-auth` 或 `@libsql/client` 运行真实 session 存储 —— 保持单测 < 1s
    - `Auth` 类型从 `apps/api/src/lib/auth.ts` 导出（验证一下实际导出名称：若文件导出的是 `type Auth`、`ReturnType<typeof createAuth>` 或类似，import 路径相应调整）
    - 文件顶部注释保留 Plan 01 的 D-15 / D-16 / D-17 说明，仅增补 "Plan 05 实装" 一行
    - 不要为 D-16 / D-17 在本文件加 E2E 级断言（那些由 Plan 06 手动冒烟覆盖）
    - Cookie 正则兼容 1.6.7 + 1.6.10 的 Set-Cookie 行为修复（see RESEARCH L103, L115）
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- signout.test.ts --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @starye/api test -- signout.test.ts --run` 三条用例全绿
    - `grep -c "it.todo" apps/api/src/routes/auth/__tests__/signout.test.ts` 返回 `0`
    - `grep -c "it('D-15 #" apps/api/src/routes/auth/__tests__/signout.test.ts` 返回 `3`
    - `grep -c "starye.session_token" apps/api/src/routes/auth/__tests__/signout.test.ts` >= `2`（至少 D-15 #1 和 #3 用例断言该 cookie 名）
    - `grep -c "Max-Age=0\|Expires=Thu" apps/api/src/routes/auth/__tests__/signout.test.ts` >= `1`
    - 不存在真实 `import { betterAuth } from 'better-auth'`（mock 路径，不集成启动）
    - `pnpm --filter @starye/api type-check` 绿
    - 整包 `pnpm --filter @starye/api test` 仍全绿（不破坏既有测试）
  </acceptance_criteria>
  <done>AUTH-08 单测覆盖到位；3 条用例全绿；mock handler 方案保持单测轻量；无 it.todo 残留。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → /sign-out | untrusted 请求触发 session 删除 |
| API Worker → D1 | better-auth 通过 drizzleAdapter 写入 session 表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 (T4) | Elevation | sign-out 后 cookie 清了但 session 行未删 | mitigate | D-15 #2 断言 `getSession` 返回 null；mock handler 同时更新内部 session 状态模拟 D1 删除 |
| T-01-17 | Information Disclosure | Set-Cookie 格式异常（错误的 cookie 名或缺 Secure） | mitigate | D-15 #3 正则断言清除的是 `starye.session_token=`，不是其他意外 cookie |
| T-01-18 | Tampering | Better Auth 1.6.7/1.6.10 的 Set-Cookie 修复未落地 | accept | 升级已由 Plan 04 完成；本 plan 的测试断言与具体修复兼容 |
</threat_model>

<verification>
```bash
pnpm --filter @starye/api test -- signout.test.ts --run
pnpm --filter @starye/api test
pnpm --filter @starye/api type-check
```
</verification>

<success_criteria>
- AUTH-08：signout.test.ts 三条用例全绿，断言服务端 session 失效 + Set-Cookie 清除头
- 不引入 libsql / better-auth 集成运行时依赖
- `pnpm --filter @starye/api test` 整体仍绿
- Plan 01 的 `it.todo` 在本 plan 后计数归零
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-gateway/01-05-SUMMARY.md` recording:
- 3 条用例实际断言文本
- mock handler 的 Set-Cookie 格式（供 Plan 06 手动冒烟核对真实 Better Auth 输出对比）
- 若 `Auth` 类型导出名与预期不符，记录实际 import 路径
</output>
