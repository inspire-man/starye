---
phase: 01-auth-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gateway/src/__tests__/cache-middleware.test.ts
  - apps/blog/e2e/session.spec.ts
  - apps/dashboard/e2e/auth-crosspath.spec.ts
  - apps/api/src/routes/auth/__tests__/signout.test.ts
autonomous: true
requirements:
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - AUTH-01
  - AUTH-02
tags: [auth, cache, test-scaffold, nyquist]
must_haves:
  truths:
    - "Wave 0 完成后，phase 1 新增的 4 个测试入口文件全部存在且被 Vitest/Playwright 可发现"
    - "所有骨架用例以 skip / todo 状态存在，运行时不假阳性通过（不存在空断言的 it）"
    - "骨架明确声明每条用例对应的 AUTH-XX 需求与 D-11 矩阵编号，后续 plan 只需把断言填进去"
  artifacts:
    - path: "apps/gateway/src/__tests__/cache-middleware.test.ts"
      provides: "D-11 四条 BYPASS 用例骨架（AUTH-06 / AUTH-07）"
      contains: "it.todo('D-11 #2"
    - path: "apps/blog/e2e/session.spec.ts"
      provides: "Nuxt blog SSR session hydrate 骨架（AUTH-02）"
      contains: "test.skip"
    - path: "apps/dashboard/e2e/auth-crosspath.spec.ts"
      provides: "登录后跨子路径 session 共享骨架（AUTH-01）"
      contains: "test.skip"
    - path: "apps/api/src/routes/auth/__tests__/signout.test.ts"
      provides: "sign-out 服务端失效骨架（AUTH-08）"
      contains: "it.todo"
  key_links:
    - from: "apps/gateway/src/__tests__/cache-middleware.test.ts"
      to: "D-11 矩阵"
      via: "四条 it.todo 的 title 精确对应 D-11 表格 #1..#4"
    - from: "apps/api/src/routes/auth/__tests__/signout.test.ts"
      to: "apps/api/src/test/helpers.ts"
      via: "import createMockDb / createMockAuth / createMockUser（骨架留 import，用例留 todo）"
---

<objective>
为 Phase 1 建立 Nyquist 采样骨架：在每个"有要验证的不变量"的位置先创建失败/待办的测试文件，让后续实现 plan（02/03/05/06）能逐条把骨架转绿，而不是边实现边补测试。

Purpose: 锁死测试入口先于实现存在（Nyquist 采样定理：先有采样点，才有可度量的信号）。后续 plan 改代码时立即能知道是否把某条需求写断了。

Output:
- `apps/gateway/src/__tests__/cache-middleware.test.ts` 新增 D-11 #1..#4 四条 `it.todo`（AUTH-06/07）
- `apps/blog/e2e/session.spec.ts`（新建，`test.skip` 骨架覆盖 AUTH-02 + SSR 3s 超时降级）
- `apps/dashboard/e2e/auth-crosspath.spec.ts`（新建，`test.skip` 骨架覆盖 AUTH-01）
- `apps/api/src/routes/auth/__tests__/signout.test.ts`（新建，`__tests__` 目录一并建，`it.todo` 骨架覆盖 AUTH-08）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-gateway/01-CONTEXT.md
@.planning/phases/01-auth-gateway/01-RESEARCH.md
@.planning/phases/01-auth-gateway/01-PATTERNS.md
@.planning/phases/01-auth-gateway/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- 后续 plan 需要感知的骨架约定 -->

D-11 矩阵（来源：01-VALIDATION.md L57-66 + 01-RESEARCH.md §Gateway Cache Bypass Implementation）：
| # | Scenario | Headers | Expected |
|---|----------|---------|----------|
| 1 | 无头 + public group | 无 | 首次 MISS，二次 HIT |
| 2 | 带 session cookie + public group | `cookie: starye.session_token=xxx` | `X-Cache-Status: BYPASS`, `X-Cache-Reason: auth-headers`, 不写 KV |
| 3 | 带 Authorization + public group | `authorization: Bearer xxx` | 同上 |
| 4 | `/api/auth/*` 双 case | `/api/auth/get-session` ± cookie | 两 case 都 BYPASS（NO_STORE_PREFIXES 命中） |

既有测试工具（不要重复定义）：
- `apps/api/src/test/helpers.ts` 导出 `createMockDb()`, `createMockAuth(sessionData?)`, `createMockUser(overrides?)`（见行号 L9-101）
- `apps/dashboard/e2e/auth-flow.spec.ts:14-32` 提供 `mockSession(page, session)` / `interceptExternalRedirects(page)` 两个帮助函数，本 plan 的新 spec 应该照抄同款
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 cache-middleware.test.ts 加入 D-11 四条 it.todo</name>
  <read_first>
    - apps/gateway/src/__tests__/cache-middleware.test.ts（整文件，了解现有 createMockKv() 与 cachedProxy 模式）
    - apps/gateway/src/cache-middleware.ts（L12-13, L49, L179-196, L339-371 —— 理解将被修改的 API 形状）
    - .planning/phases/01-auth-gateway/01-VALIDATION.md L57-66（D-11 矩阵原文）
    - .planning/phases/01-auth-gateway/01-PATTERNS.md L89-148（cache-middleware.test.ts 的测试骨架模板）
  </read_first>
  <action>
    在 `apps/gateway/src/__tests__/cache-middleware.test.ts` 第 160 行（`it('invalidates cache entries by prefix', ...)` 块）之后、`})` 关闭 describe 之前，插入 D-11 四条 `it.todo`。骨架要求：

    1. 保留既有 `it('isolates favorites cache per user cookie', ...)` 块不改（L68-106）—— Plan 02 会在清理 private scope 时删它，**本任务不动**
    2. 在 describe 末尾追加以下 4 条 `it.todo`（title 精确到 D-11 # 号，不要编号漂移）：

    ```ts
    // D-11 #1: baseline regression — 无头 public group 仍走 MISS→HIT（Plan 02 实现后激活）
    it.todo('D-11 #1: caches /api/movies on public group when no auth headers present (MISS then HIT)')

    // D-11 #2: AUTH-07 — 带 session cookie → BYPASS + X-Cache-Reason
    it.todo('D-11 #2: bypasses KV on /api/movies when request has Cookie header (BYPASS, X-Cache-Reason=auth-headers)')

    // D-11 #3: AUTH-07 — 带 Authorization → BYPASS + X-Cache-Reason
    it.todo('D-11 #3: bypasses KV on /api/movies when request has Authorization header (BYPASS, X-Cache-Reason=auth-headers)')

    // D-11 #4: AUTH-06 — /api/auth/* 无论头都 BYPASS（NO_STORE_PREFIXES 独立命中）
    it.todo('D-11 #4: bypasses /api/auth/get-session regardless of request headers')
    ```

    不要删除已存在的 it；不要改 createMockKv 工厂。`it.todo` 是 Vitest 原生 API，Plan 02 把 `.todo` 删除、同名改为完整 `it('D-11 #N: ...', async () => { ... })` 即激活。
  </action>
  <verify>
    <automated>cd apps/gateway && pnpm test -- cache-middleware.test.ts --reporter=verbose 2>&1 | grep -c "D-11 #"</automated>
  </verify>
  <acceptance_criteria>
    - 运行上述 grep 命令必须输出 `4`（4 条 D-11 骨架都被 Vitest 识别）
    - `grep -c "it.todo('D-11" apps/gateway/src/__tests__/cache-middleware.test.ts` 返回 `4`
    - `grep -c "isolates favorites cache per user cookie" apps/gateway/src/__tests__/cache-middleware.test.ts` 仍返回 `1`（Plan 02 之前保留）
    - `pnpm --filter @starye/gateway test` 整体绿（todo 不算失败）
  </acceptance_criteria>
  <done>D-11 四条 it.todo 存在于 cache-middleware.test.ts；Vitest 运行时显示 4 个 `- todo` 标记；未破坏既有 4 条 it。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 apps/blog/e2e/session.spec.ts 与 apps/dashboard/e2e/auth-crosspath.spec.ts 骨架</name>
  <read_first>
    - apps/blog/e2e/blog-features.spec.ts（L1-119，了解 blog e2e 基线：`page.route` mock + `page.goto` + `networkidle`）
    - apps/dashboard/e2e/auth-flow.spec.ts（L10-52，了解 mockSession / interceptExternalRedirects 两个帮助函数）
    - apps/blog/playwright.config.ts 和 apps/dashboard/playwright.config.ts（确认 baseURL）
    - .planning/phases/01-auth-gateway/01-PATTERNS.md L319-413（两个 e2e 文件的 analog 说明）
    - .planning/phases/01-auth-gateway/01-CONTEXT.md L32-34（D-01/D-02/D-03：SSR 通道、走 gateway、3s 超时降级）
  </read_first>
  <action>
    创建两个文件。

    **文件 A：`apps/blog/e2e/session.spec.ts`**（新建）

    ```ts
    /**
     * Phase 1 / AUTH-02: Nuxt blog SSR session hydrate 骨架
     *
     * 验证：
     * - D-01: SSR 预取 /api/auth/get-session → 注入 useState('session')
     * - D-03: SSR fetch 3s 超时/5xx → 降级匿名不阻塞渲染
     * - 客户端 hydrate 后 authClient.useSession() 无闪烁
     *
     * 此骨架由 Plan 03（Nuxt SSR 通道）填充断言。当前以 test.skip 占位，
     * 确保 Plan 03 不需新建文件即可转绿。
     */

    import { expect, test } from '@playwright/test'

    // 与 apps/dashboard/e2e/auth-flow.spec.ts:14-22 同款 mockSession
    async function mockSession(page: any, session: any) {
      await page.route('**/api/auth/get-session', (route: any) =>
        route.fulfill({
          status: 200,
          contentType: 'application/json',
          body: JSON.stringify(session),
        }))
    }

    test.describe('AUTH-02: Nuxt blog SSR session hydrate', () => {
      test.skip('D-01: SSR HTML 预取 session 后 useState 含用户标记', async ({ page }) => {
        await mockSession(page, {
          user: { id: 'user-1', email: 'author@starye.org', role: 'admin' },
          session: { id: 'session-1' },
        })
        await page.goto('/')
        const html = await page.content()
        expect(html).toContain('user-1') // 占位；Plan 03 根据实际 DOM 选择器调整
      })

      test.skip('D-03: /api/auth/get-session 3s 超时时 SSR 降级为匿名不抛错', async ({ page }) => {
        // 模拟超时：route.abort('timedout') 或延迟 > 3s
        await page.route('**/api/auth/get-session', async (route) => {
          await new Promise(resolve => setTimeout(resolve, 5000))
          await route.abort('timedout')
        })
        const response = await page.goto('/')
        expect(response?.status()).toBe(200) // SSR 不因 session fetch 失败而 500
      })

      test.skip('AUTH-02 hydrate: 客户端 useSession() 复用 useState 不二次拉取', async ({ page }) => {
        // Plan 03 填：断言客户端 hydrate 期间只有 1 次 /api/auth/get-session 请求
      })
    })
    ```

    **文件 B：`apps/dashboard/e2e/auth-crosspath.spec.ts`**（新建）

    ```ts
    /**
     * Phase 1 / AUTH-01: 登录后跨子路径 session 共享骨架
     *
     * 验证：
     * - 同 cookie 下，/movie → /comic → /blog → /dashboard 四处均读到同一 user.id
     * - 手动冒烟（D-19 step 4）的自动化对照
     *
     * 此骨架由 Plan 06（冒烟 + 跨路径 spec）填充断言。
     */

    import { expect, test } from '@playwright/test'

    // 与 apps/dashboard/e2e/auth-flow.spec.ts:14-22 同款
    async function mockSession(page: any, session: any) {
      await page.route('**/api/auth/get-session', (route: any) =>
        route.fulfill({
          status: 200,
          contentType: 'application/json',
          body: JSON.stringify(session),
        }))
    }

    async function interceptExternalRedirects(page: any) {
      await page.route('**/auth/**', (route: any) =>
        route.fulfill({
          status: 200,
          contentType: 'text/html',
          body: '<html><body><div id="auth-page">Auth Page</div></body></html>',
        }))
    }

    test.describe('AUTH-01: 登录后跨子路径 session 共享', () => {
      test.skip('D-19 step 4: 同 cookie 跨 /movie /comic /blog /dashboard 读到同一 user.id', async ({ page }) => {
        await mockSession(page, {
          user: { id: 'user-1', email: 'author@starye.org', role: 'admin' },
          session: { id: 'session-1' },
        })
        await interceptExternalRedirects(page)
        await page.goto('/')
        // Plan 06 填：切路径 + 断言 window.__session__?.user?.id === 'user-1'
        expect(true).toBe(true)
      })
    })
    ```

    **注意事项：**
    - `apps/blog/e2e/` 目录已存在（仅 `blog-features.spec.ts`）—— 直接新建文件
    - `apps/dashboard/e2e/` 目录已存在（有 `auth-flow.spec.ts`）—— 直接新建文件
    - `test.skip` 是 Playwright 原生 API，`pnpm test:e2e` 会显示 `- skipped`，不算失败
    - 两个文件都复用 `mockSession` 帮助函数：内部重复定义而不是跨 spec import，避免 Playwright config 共享上下文问题
  </action>
  <verify>
    <automated>test -f apps/blog/e2e/session.spec.ts && test -f apps/dashboard/e2e/auth-crosspath.spec.ts && grep -c "test.skip" apps/blog/e2e/session.spec.ts && grep -c "test.skip" apps/dashboard/e2e/auth-crosspath.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 两个文件都存在
    - `grep -c "test.skip" apps/blog/e2e/session.spec.ts` = `3`（三条 skip 骨架）
    - `grep -c "test.skip" apps/dashboard/e2e/auth-crosspath.spec.ts` = `1`
    - `grep -c "mockSession" apps/blog/e2e/session.spec.ts` >= `2`（定义 + 调用各一次）
    - `grep -c "mockSession" apps/dashboard/e2e/auth-crosspath.spec.ts` >= `2`
    - 不存在 `test.only` 或 `test.fixme`（骨架阶段禁止独占/待修复）
  </acceptance_criteria>
  <done>两个 e2e spec 文件按模板创建；Playwright 发现三 + 一共四个 skipped test；下游 plan 移除 `.skip` 即可激活。</done>
</task>

<task type="auto">
  <name>Task 3: 新建 apps/api/src/routes/auth/__tests__/signout.test.ts 骨架</name>
  <read_first>
    - apps/api/src/routes/auth/index.ts（L1-89，特别是 L83-87 catch-all 代理到 authInstance.handler）
    - apps/api/src/routes/public/progress/__tests__/progress.test.ts（L1-94，确认 createApp 工厂 + Hono<AppEnv> mock 模式）
    - apps/api/src/test/helpers.ts（L9-101，createMockDb / createMockAuth / createMockUser）
    - .planning/phases/01-auth-gateway/01-PATTERNS.md L152-193（signout.test 的 analog 说明与 app factory 模板）
    - .planning/phases/01-auth-gateway/01-CONTEXT.md L54-57（D-15/16/17：服务端删 D1 row + 清 cookie，不广播）
  </read_first>
  <action>
    新建目录 `apps/api/src/routes/auth/__tests__/` 并在其下创建 `signout.test.ts`。

    ```ts
    /**
     * Phase 1 / AUTH-08: signOut 服务端真实失效测试骨架
     *
     * 验证：
     * - D-15: /api/auth/sign-out 返回 200 + Set-Cookie 清除头（Max-Age=0 或等价）
     * - D-15: auth.api.getSession 在 sign-out 之后返回 null（D1 session 行已删）
     * - D-16: 跨 tab 登出不主动广播 —— 不在本单测范围（需 E2E 双上下文），仅记录 comment
     *
     * 此骨架由 Plan 05 填充断言。当前 it.todo 占位。
     * 路由路径：authRoutes 的 /sign-out 走 catch-all /* 代理到 authInstance.handler
     * （apps/api/src/routes/auth/index.ts:83-87），Plan 05 可以 mock authInstance.handler
     * 返回带 Set-Cookie 的 Response，比起集成 betterAuth() + libsql 更轻量。
     */

    import type { AppEnv } from '../../../types'
    import { Hono } from 'hono'
    import { describe, expect, it } from 'vitest'
    import { createMockAuth, createMockDb, createMockUser } from '../../../test/helpers'
    import { authRoutes } from '../index'

    describe('AUTH-08: /api/auth/sign-out server-side invalidation', () => {
      it.todo('D-15 #1: POST /sign-out 返回 200 并回 Set-Cookie 清除头（Max-Age=0）')
      it.todo('D-15 #2: sign-out 后 auth.api.getSession 返回 null')
      it.todo('D-15 #3: sign-out 回给浏览器的 Set-Cookie 精确匹配 starye.session_token=')
    })

    // 以下 helper 作为 Plan 05 的起点保留（避免 Plan 05 重复造轮子）
    // 参考 apps/api/src/routes/public/progress/__tests__/progress.test.ts:83-94
    export function createApp(
      db: ReturnType<typeof createMockDb>,
      auth: ReturnType<typeof createMockAuth>,
      user?: ReturnType<typeof createMockUser> | null,
    ) {
      const app = new Hono<AppEnv>()
      app.use('*', async (c, next) => {
        c.set('db', db)
        c.set('auth', auth)
        if (user !== undefined) c.set('user', user as any)
        await next()
      })
      app.route('/', authRoutes)
      return app
    }
    ```

    **注意事项：**
    - `__tests__` 目录**不存在**（`apps/api/src/routes/auth/` 当前只有 `index.ts`），task 需一并创建
    - 导入路径从 `__tests__/` 回上三级到 `apps/api/src/`：`../../../test/helpers`、`../../../types`、`../index`（见 progress.test.ts L1-10 对照）
    - `createApp` 导出是**故意**的：Plan 05 会从 `./signout.test.ts` 或同目录复用；不要在本 task 把 helper 拆到 test/helpers.ts（那会影响其他测试）
    - **不要**在骨架里启动真实 better-auth — PATTERNS.md L191 明确建议 mock handler 路径
  </action>
  <verify>
    <automated>test -d apps/api/src/routes/auth/__tests__ && test -f apps/api/src/routes/auth/__tests__/signout.test.ts && cd apps/api && pnpm test -- signout.test.ts --reporter=verbose 2>&1 | grep -c "D-15 #"</automated>
  </verify>
  <acceptance_criteria>
    - 目录 `apps/api/src/routes/auth/__tests__/` 存在
    - 文件 `apps/api/src/routes/auth/__tests__/signout.test.ts` 存在
    - `grep -c "it.todo('D-15 #" apps/api/src/routes/auth/__tests__/signout.test.ts` 返回 `3`
    - `grep -c "export function createApp" apps/api/src/routes/auth/__tests__/signout.test.ts` 返回 `1`
    - `pnpm --filter @starye/api test -- signout.test.ts` 整体绿（todo 不算失败；import resolution 必须成功，否则文件路径写错）
    - `pnpm --filter @starye/api type-check` 绿（`authRoutes` 与 `AppEnv` 的 import 必须解析）
  </acceptance_criteria>
  <done>signout.test.ts 与 __tests__ 目录创建；3 条 it.todo 被 Vitest 识别；createApp helper 导出；type-check 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试执行边界 | 骨架只涉及本地 Vitest/Playwright 进程，不跨网络 |
| CI 可见性 | 本 wave 的骨架决定了后续 wave 能否被 CI 及时发现回归 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | apps/gateway/src/__tests__/cache-middleware.test.ts（T1 缓存投毒的测试入口） | mitigate | 预先留 D-11 #2/#3 两条 BYPASS 用例 todo；Plan 02 实现后即可判断是否出现跨用户泄漏 |
| T-01-02 | Tampering | apps/api/src/routes/auth/__tests__/signout.test.ts（T4 sign-out 行为回归） | mitigate | 预先留 D-15 #1/#2 用例 todo；Plan 05 实现后即可检测到 "cookie 清了但 session 行留着" 的漂移 |
| T-01-03 | Information Disclosure | apps/blog/e2e/session.spec.ts（T3 SSR 转发头泄漏） | accept（本 plan 仅骨架，断言落在 Plan 03 task "middleware 仅 pick cookie 头"） | 在 spec 注释中写清 D-03 降级不得抛出 upstream 请求；Plan 03 task 2 会加断言 |
| T-01-04 | Tampering | apps/dashboard/e2e/auth-crosspath.spec.ts（T2 better-auth 升级后 session 行为一致性） | mitigate | 骨架覆盖 D-19 step 4；Plan 06 填断言，升级后自动跑 |
</threat_model>

<verification>
整体骨架验证：
```bash
# 1. 所有四个测试文件存在且 Vitest/Playwright 可发现
find apps/gateway/src/__tests__/cache-middleware.test.ts apps/blog/e2e/session.spec.ts apps/dashboard/e2e/auth-crosspath.spec.ts apps/api/src/routes/auth/__tests__/signout.test.ts

# 2. Todo/skip 计数精确
grep -c "it.todo('D-11" apps/gateway/src/__tests__/cache-middleware.test.ts   # 期望 4
grep -c "test.skip" apps/blog/e2e/session.spec.ts                              # 期望 3
grep -c "test.skip" apps/dashboard/e2e/auth-crosspath.spec.ts                  # 期望 1
grep -c "it.todo('D-15" apps/api/src/routes/auth/__tests__/signout.test.ts    # 期望 3

# 3. 运行所有单测包，确保 import 不断
pnpm --filter @starye/gateway test
pnpm --filter @starye/api test
```
</verification>

<success_criteria>
- 4 个测试文件全部存在，在对应 test runner 下可见
- 11 个骨架用例（4 todo + 3 skip + 1 skip + 3 todo）被识别，零运行失败
- 下游 plan（02/03/05/06）不需要新建任何测试文件，只需把 `.todo` / `.skip` 替换为完整断言
- `pnpm --filter @starye/gateway test` 与 `pnpm --filter @starye/api test` 都绿
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-gateway/01-01-SUMMARY.md` recording:
- 新建/修改文件清单
- 11 条骨架的 D-XX 编号 → 覆盖 plan 映射（02/03/05/06）
- Plan 02/03/05/06 接骨架时需注意的导入路径
</output>
