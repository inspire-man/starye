---
phase: 01-auth-gateway
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/auth/package.json
  - apps/blog/package.json
  - apps/dashboard/package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
tags: [better-auth, upgrade, security]
must_haves:
  truths:
    - "D-18: 四个 package.json（api / auth / blog / dashboard）的 `better-auth` 依赖版本从 `^1.6.2` 升至 `^1.6.10`，与 pnpm-lock.yaml 在同一 commit 内更新（防跨 commit CI 拉漂移）"
    - "升级后 `apps/api/src/lib/auth.ts` 的 cookieDomain / sameSite / secure / path 行为未变（D-05 / D-06）—— 通过跑 api 既有单测回归"
    - "升级后四个应用 `pnpm build` / `pnpm type-check` / `pnpm test` 全绿"
    - "D-20: 1.6.3..1.6.10 的 changelog 无 breaking change 影响本项目（RESEARCH.md §Changelog 已审），故升级按 LOW 风险执行；若执行阶段发现未知 breaking → 停止并回 CONTEXT 讨论"
  artifacts:
    - path: "apps/api/package.json"
      provides: "api 包的 better-auth ^1.6.10 声明"
      contains: "1.6.10"
    - path: "apps/auth/package.json"
      provides: "auth 包的 better-auth ^1.6.10 声明"
      contains: "1.6.10"
    - path: "apps/blog/package.json"
      provides: "blog 包的 better-auth ^1.6.10 声明"
      contains: "1.6.10"
    - path: "apps/dashboard/package.json"
      provides: "dashboard 包的 better-auth ^1.6.10 声明"
      contains: "1.6.10"
    - path: "pnpm-lock.yaml"
      provides: "锁定四个包的 better-auth 到 1.6.10 的同一具体解析版本"
      contains: "better-auth@1.6.10"
  key_links:
    - from: "apps/api/package.json better-auth"
      to: "apps/{auth,blog,dashboard}/package.json better-auth"
      via: "同 commit 同版本"
      pattern: "\"better-auth\": \"\\^1\\.6\\.10\""
---

<objective>
把 Better Auth 从 `^1.6.2` 升级到 `^1.6.10`（D-18），四个 package.json + pnpm-lock.yaml 同 commit 更新。升级后跑 build / type-check / test 回归，确认 cookieDomain / sameSite / secure 行为未漂移。

Purpose: 满足 AUTH-04，同时附带 1.6.7 + 1.6.10 的 `Set-Cookie` 稳定性修复（对登录/登出路径直接受益）。AUTH-03 的 cookie 属性验证通过既有单测 + 手动 curl 冒烟覆盖，本 plan 保证升级"无回归"。

Output:
- `apps/{api,auth,blog,dashboard}/package.json` 四个文件 better-auth 版本号更新
- `pnpm-lock.yaml` 同步刷新
- 所有包的 `pnpm install --frozen-lockfile && pnpm -r build && pnpm -r test` 通过
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-gateway/01-CONTEXT.md
@.planning/phases/01-auth-gateway/01-RESEARCH.md
@.planning/phases/01-auth-gateway/01-PATTERNS.md
@CLAUDE.md

<interfaces>
四个文件精确行号（grep 确认）：
- `apps/api/package.json:23`: `"better-auth": "^1.6.2"` → `"better-auth": "^1.6.10"`
- `apps/auth/package.json:18`: 同样
- `apps/blog/package.json:22`: 同样
- `apps/dashboard/package.json:29`: 同样

CHANGELOG 摘要（RESEARCH.md §Better Auth 1.6.3..1.6.10）：
- **Breaking change：0 项**
- 1.6.4 声明 peer `drizzle-orm@^0.45.2` —— 本仓 `packages/db` pin 正是 0.45.2，满足
- 1.6.7 保留 `APIError` 时的 `Set-Cookie`（sign-out 稳定性提升）
- 1.6.10 消除 OAuth 回调重复 `Set-Cookie`

**包管理器：** pnpm 10.33.0（`package.json packageManager` 已锁）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 四 package.json 同步升级 + lockfile 刷新</name>
  <read_first>
    - apps/api/package.json（L23 确认当前版本）
    - apps/auth/package.json（L18 确认当前版本）
    - apps/blog/package.json（L22 确认当前版本）
    - apps/dashboard/package.json（L29 确认当前版本）
    - pnpm-workspace.yaml（确认 workspace 配置）
    - .planning/phases/01-auth-gateway/01-RESEARCH.md L79-129（1.6.3..1.6.10 逐版本审计）
    - .planning/phases/01-auth-gateway/01-CONTEXT.md L58-60（D-18 同 commit 要求）
  </read_first>
  <action>
    **方式 A（推荐）：命令行升级**
    ```bash
    pnpm up -r better-auth@^1.6.10
    ```
    `-r` 让 pnpm 对 workspace 所有引用 better-auth 的 package.json 统一升版，同步刷新根 `pnpm-lock.yaml`。

    **方式 B（fallback，若方式 A 命令失败）：手工改 4 个 package.json 后 install**
    1. Edit `apps/api/package.json:23` → `"better-auth": "^1.6.10",`
    2. Edit `apps/auth/package.json:18` → 同样
    3. Edit `apps/blog/package.json:22` → 同样
    4. Edit `apps/dashboard/package.json:29` → 同样
    5. 根目录执行 `pnpm install`（自动更新 lockfile）

    **执行后立即验证：**
    - `pnpm install --frozen-lockfile` 不报错（lockfile 与 package.json 一致）
    - 所有工作区解析到 `better-auth@1.6.10`（或 1.6.11+ 若已发布）

    **关键约束：**
    - 四个 package.json + lockfile **必须同一 commit**（D-18）；若 orchestrator 把本 task 拆成多步，执行时仍要等全部改完再 git add
    - 不得降级其他依赖：`pnpm up` 只跟 better-auth；如果 pnpm 试图顺带更新 drizzle-orm 等其他包，改走 Edit 手工路径
    - 不得修改 `.npmrc` 或 `packageManager` 字段
    - 如果升级过程中 pnpm 报 peer dependency warning（例如 drizzle-orm 相关），先核对 `packages/db/package.json` 的 `drizzle-orm` 版本 —— 当前为 `^0.45.2`，应满足 better-auth 1.6.4 起的 peer 要求；若确有不兼容，停止并升级到 checkpoint 询问
  </action>
  <verify>
    <automated>grep -c '"better-auth": "\^1.6.10"' apps/api/package.json apps/auth/package.json apps/blog/package.json apps/dashboard/package.json | grep -v ':0' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 上面 grep + wc 命令输出精确等于 `4`（四个 package.json 全部含 `^1.6.10`）
    - `grep "\"better-auth\": \"\\^1.6.2\"" apps/api/package.json apps/auth/package.json apps/blog/package.json apps/dashboard/package.json` 返回空（旧版本全部清零）
    - `pnpm install --frozen-lockfile` 在根目录执行成功（lockfile 与 package.json 一致）
    - `pnpm-lock.yaml` 包含 `better-auth@1.6.10`（grep `better-auth@1.6.10` 命中 >= 1 次；若 pnpm 写为 `/better-auth/1.6.10` 格式则 grep `1.6.10` 在 better-auth 上下文内命中）
  </acceptance_criteria>
  <done>四个 package.json + lockfile 一致升到 ^1.6.10；frozen-lockfile 安装通过。</done>
</task>

<task type="auto">
  <name>Task 2: 升级后全仓回归 build / type-check / test</name>
  <read_first>
    - turbo.json（确认 build / type-check / test 任务管道）
    - apps/api/src/lib/auth.ts（L34-107，确认升级前 cookieDomain / sameSite / secure / cookiePrefix 配置，升级后行为必须等价）
    - apps/api/src/middleware/auth.ts（L5-23，authMiddleware 对 getSession 的调用，升级后返回类型不变）
    - .planning/phases/01-auth-gateway/01-RESEARCH.md L79-129（changelog 已审；本 task 只是确认无动态漂移）
  </read_first>
  <action>
    按顺序执行全仓回归：

    1. `pnpm install --frozen-lockfile`（前置确认）
    2. `pnpm -r type-check`（所有包 tsc 零错误）
    3. `pnpm -r lint`（eslint 零错误 / 零 warning-as-error）
    4. `pnpm -r test`（Vitest 所有单测绿；不含 e2e）
    5. `pnpm -r build`（Turbo 并行 build 所有包；必须全部产出）

    **若任一步失败：**
    - type-check 报 better-auth 相关类型错：对照 changelog 判断是否 1.6.x API 签名变更；若是 deprecated 但未移除，加 `// @deprecated` 注释并在 SUMMARY 记录；若是硬 breaking，回滚本 plan 并升级到 checkpoint
    - 测试失败：定位失败用例，判断是行为变更还是既有断言依赖内部实现（例如按 `Set-Cookie` 具体字符串断言）；若是后者，更新测试断言以匹配 1.6.10 的新行为；若是前者，停止并 checkpoint
    - build 失败：通常是 bundler 的 peer 警告升级成错；对照 changelog 1.6.4 的 drizzle-orm peer 要求

    **关键约束：**
    - 本 task 不修改 `apps/api/src/lib/auth.ts`（D-05 / D-06 明确不改）；如果回归跑失败必须改该文件，优先判断是否 changelog 有未审到的行为变更 —— 若确有，停止并 checkpoint
    - 不要 `pnpm install` 无 `--frozen-lockfile` 触发 lockfile 意外漂移
    - 不要因单测失败就 `pnpm test -- --run` 然后忽略错误；必须全绿才算完成
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm -r type-check && pnpm -r test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install --frozen-lockfile` 零错
    - `pnpm -r type-check` 零错
    - `pnpm -r test` 全绿（各包单测通过；不包含 e2e）
    - `pnpm -r build` 全绿（所有 apps 产出 `dist/` 或 `.output/`）
    - `apps/api/src/lib/auth.ts` 未被修改（`git diff apps/api/src/lib/auth.ts` 为空）
    - `apps/api/src/middleware/auth.ts` 未被修改
  </acceptance_criteria>
  <done>升级无回归；四个应用的 build / type-check / test 全绿；auth.ts 未动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | 升级从公开 registry 拉 better-auth 新版本 |
| lockfile → 运行时 | 版本变动影响 cookie/session 行为的权威源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 (T2) | Elevation / Tampering | better-auth 升级可能引入 session 行为不一致（cookie 属性 / 有效期 / signOut 语义） | mitigate | RESEARCH.md 全量审计 1.6.3..1.6.10 零 breaking change；Task 2 跑全仓 test 回归；auth.ts 字节不动可复核 `git diff` 空 |
| T-01-14 | Supply Chain | 恶意 typo-squat 或被篡改的 better-auth 包 | accept | 从 npm 官方源拉 `better-auth`（非 typosquat）；lockfile integrity hash 由 pnpm 强制校验 |
| T-01-15 | Tampering | 跨 commit 版本漂移（D-18 不守时） | mitigate | acceptance_criteria 要求 `pnpm install --frozen-lockfile` 绿 —— 四 package.json 与 lockfile 不一致会立即失败 |
</threat_model>

<verification>
```bash
# 版本一致性
grep -l "\"better-auth\": \"\\^1.6.10\"" apps/{api,auth,blog,dashboard}/package.json
grep -c "\"better-auth\": \"\\^1.6.2\"" apps/{api,auth,blog,dashboard}/package.json  # 每个都 0

# 全仓回归
pnpm install --frozen-lockfile
pnpm -r type-check
pnpm -r lint
pnpm -r test
pnpm -r build

# auth.ts 未动
git diff --exit-code apps/api/src/lib/auth.ts apps/api/src/middleware/auth.ts
```
</verification>

<success_criteria>
- AUTH-04：四个 package.json + lockfile 同 commit 升到 `^1.6.10`，全仓 install / type-check / test / build 全绿
- AUTH-03（回归）：`apps/api/src/lib/auth.ts` 未被修改，cookieDomain / sameSite / secure / path 行为等价于升级前
- Plan 06 的手动冒烟（D-19）在升级 PR merge 后执行，作为 phase gate 的最后签字
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-gateway/01-04-SUMMARY.md` recording:
- 升级后 `better-auth` 具体解析版本（lockfile 里的 `better-auth@1.6.xx`）
- 回归命令输出摘要（type-check / test / build 各通过）
- 若触发 drizzle-orm 或其他包 peer warning，记录警告文本与是否安全
- 升级 PR 与 Gateway 重构 PR（Plan 02）建议独立 —— 两次独立回滚位点
</output>
