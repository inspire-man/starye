---
phase: 18-github-actions-production-orchestration
plan: 05
type: execute
wave: 3
depends_on:
  - 18-03
  - 18-04
files_modified:
  - packages/crawler/scripts/target-crawl-mutation.ts
  - packages/crawler/src/task-runner/movie-adapter.ts
  - packages/crawler/src/task-runner/manga-adapter.ts
  - packages/crawler/src/task-runner/__tests__/production-adapter.test.ts
  - packages/config/src/deployment-target/mutation-entry.ts
  - packages/config/src/deployment-target/__tests__/mutation-entry.test.ts
  - packages/config/src/deployment-target/__tests__/crawler-source-entry-contract.test.ts
autonomous: true
requirements:
  - PROD-02
  - PROD-03
must_haves:
  truths:
    - "GitHub-hosted crawler 只通过 registry-owned prepared entry 执行 movie/manga production adapter，Worker 继续只承担控制面。"
    - "adapter 在启动、心跳/安全检查点、进度/日志、取消和终态 receipt 处发送 signed Actions events，并携带同一 run/attempt/provider binding。"
    - "validated receipt 包含非空且匹配的内容标识与入库摘要；部分入库和审计摘要保持可查询。"
  artifacts:
    - path: packages/crawler/scripts/target-crawl-mutation.ts
      provides: "closed production operation dispatch boundary"
    - path: packages/crawler/src/task-runner/movie-adapter.ts
      provides: "movie production callback/checkpoint adapter"
    - path: packages/crawler/src/task-runner/manga-adapter.ts
      provides: "manga production callback/checkpoint adapter"
    - path: packages/config/src/deployment-target/mutation-entry.ts
      provides: "registry-owned production mutation entries"
  key_links:
    - from: packages/crawler/scripts/target-crawl-mutation.ts
      to: packages/crawler/src/task-runner/actions-event-client.ts
      via: "signed callback and cancellation checkpoint"
    - from: packages/crawler/src/task-runner/movie-adapter.ts
      to: packages/crawler/src/task-runner/runner-client.ts
      via: "existing crawler transport and validated receipt flow"
    - from: packages/config/src/deployment-target/mutation-entry.ts
      to: packages/crawler/scripts/target-crawl-mutation.ts
      via: "prepared context operation allowlist"
---

<objective>
把现有 smoke-only mutation boundary 扩展为两个 registry-owned production adapter，复用 target-profile/prepared context、crawler transport、取消检查点和 validated receipt。

Purpose: 落实 D-03、D-08、D-11、D-12、D-15，保证生产 Actions 数据面只运行受控 movie/manga 入口。
Output: production adapter、mutation registry、receipt/cancel contract tests；不把 Puppeteer 或 crawler transport 放入 Worker。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-github-actions-production-orchestration/18-CONTEXT.md
@.planning/phases/18-github-actions-production-orchestration/18-RESEARCH.md
@.planning/phases/18-github-actions-production-orchestration/18-PATTERNS.md
@packages/crawler/scripts/target-crawl-mutation.ts
@packages/crawler/src/task-runner/movie-adapter.ts
@packages/crawler/src/task-runner/manga-adapter.ts
@packages/config/src/deployment-target/mutation-entry.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 加入 registry-owned movie/manga production operations</name>
  <files>packages/crawler/scripts/target-crawl-mutation.ts, packages/crawler/src/task-runner/movie-adapter.ts, packages/crawler/src/task-runner/manga-adapter.ts</files>
  <action>保留 absolute prepared context/config path、declared secret、process.argv guard 和现有 smoke/check-config 分支；新增明确的 movie-production 与 manga-production operation，只能由对应 mutation entry 选择。adapter 复用现有 site crawler、ApiClient sync、runner transport 和 receipt validation，注入 ActionsEventClient 发送 provider_started、heartbeat/progress/log、terminal receipt。每个心跳/安全检查点读取 cancel_requested，先停止后续 crawler 工作再签名 cancelled；已核验成功 receipt 在取消生效前到达时记录 cancel_not_effective。所有 content id/count/template/attempt/provider id 都通过 schema 检查。</action>
  <verify><automated>pnpm --filter crawler exec vitest run src/task-runner/__tests__/production-adapter.test.ts</automated></verify>
  <done>movie/manga production operation 都能在 stubbed runner/API 下生成绑定事件和 validated receipt；任意自由 child operation、URL、命令、target 或 secret 进入 fail-closed 分支。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 prepared mutation registry 与来源契约</name>
  <files>packages/config/src/deployment-target/mutation-entry.ts, packages/config/src/deployment-target/__tests__/mutation-entry.test.ts, packages/config/src/deployment-target/__tests__/crawler-source-entry-contract.test.ts</files>
  <action>沿用 crawlerEntry(id, childOperation, mode, allowedOptions, requiredSecretKeys) 的闭合 shape，为 movie/manga 生产操作声明固定 entry、固定 required secrets、允许 options 与 prepared context 输出。target/profile/Environment 映射保持 starye-org；workflow 侧只调用生成的 prepared entry，source contract test 枚举 production entry、secret allowlist、路径边界和 cleanup 约束。测试对遗留 target_url、caller-supplied command、unlisted entry 和 cross-template operation 给出确定的 4xx/validation result。</action>
  <verify><automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/mutation-entry.test.ts src/deployment-target/__tests__/crawler-source-entry-contract.test.ts</automated></verify>
  <done>prepared mutation registry 仅暴露两个生产 adapter，target-profile/environment、secret allowlist 和 cleanup contract 与 Phase 11/17 规则一致。</done>
</task>

<task type="auto">
  <name>Task 3: 验证 receipt、部分入库和取消竞态契约</name>
  <files>packages/crawler/src/task-runner/__tests__/production-adapter.test.ts</files>
  <action>扩展 adapter contract tests 覆盖空 receipt、template/attempt/provider mismatch、内容标识不匹配、部分入库后失败、late cancelled、cancel_not_effective 和重试新 attempt。断言 secret-shaped payload、source body、Authorization header 和 raw crawler error 不进入 event details；断言通过 Gateway 的本地 contract fixture 与真实 provider proof 分离，生产凭据由目标环境执行阶段提供。</action>
  <verify><automated>pnpm --filter crawler exec vitest run src/task-runner/__tests__/production-adapter.test.ts</automated></verify>
  <done>adapter 只有在 provider binding、signed terminal event 和 validated non-empty receipt 同时满足时报告成功，部分入库/审计摘要在失败和取消分支仍保留。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| prepared context → crawler process | generated production entry crosses into Node/Puppeteer execution |
| crawler → API callback | runtime event and receipt cross back through signed client |
| crawler result → content store | validated receipt gates durable content handoff |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-13 | Elevation | mutation registry | critical | mitigate | fixed entry/operation/options/secret allowlist and prepared context checks |
| T-18-14 | Tampering | receipt adapter | high | mitigate | exact run/attempt/provider/template binding and API-side receipt validation |
| T-18-15 | Information Disclosure | crawler events | high | mitigate | redaction tests for token/header/source-body/error-shaped fields |
</threat_model>

<verification>
<automated>pnpm --filter crawler exec vitest run src/task-runner/__tests__/production-adapter.test.ts</automated>
<automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__</automated>
<automated>pnpm --filter crawler type-check</automated>
</verification>

<success_criteria>
Actions runner 能执行两个 registry-owned production adapter 并回写完整签名生命周期；任何未绑定、未校验或空 receipt 分支都保留失败/取消事实而非成功摘要。
</success_criteria>

<output>
Create `.planning/phases/18-github-actions-production-orchestration/18-05-SUMMARY.md` when done
</output>
