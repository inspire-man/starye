---
phase: 18-github-actions-production-orchestration
plan: 06
type: execute
wave: 4
depends_on:
  - 18-04
  - 18-05
files_modified:
  - apps/api/src/domain/crawler-tasks/__tests__/production-orchestration.integration.test.ts
  - apps/api/src/routes/internal/crawler-runs/__tests__/production-events.integration.test.ts
  - packages/crawler/src/task-runner/__tests__/production-workflow.integration.test.ts
  - packages/config/src/deployment-target/__tests__/production-workflow.integration.test.ts
  - .planning/phases/18-github-actions-production-orchestration/COVERAGE.md
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
must_haves:
  truths:
    - "stubbed provider contract 可以从手动创建或 schedule 注册走到 provider_started、poll compensation、cancel/retry 和 validated receipt。"
    - "每个 PROD requirement 都有自动化验证映射；credentialed remote proof 明确保持为目标环境/Phase 19 handoff。"
    - "本地 evidence 使用 Gateway canonical boundary，未将 local runner 结果表述为 GitHub production completion。"
  artifacts:
    - path: apps/api/src/domain/crawler-tasks/__tests__/production-orchestration.integration.test.ts
      provides: "API/D1/provider lifecycle integration fixture"
    - path: packages/crawler/src/task-runner/__tests__/production-workflow.integration.test.ts
      provides: "signed Actions workflow and receipt contract fixture"
    - path: .planning/phases/18-github-actions-production-orchestration/COVERAGE.md
      provides: "external API capability decisions"
  key_links:
    - from: apps/api/src/domain/crawler-tasks/__tests__/production-orchestration.integration.test.ts
      to: apps/api/src/domain/crawler-tasks/reconciliation.ts
      via: "stubbed GitHub status/cancel fixtures"
    - from: packages/crawler/src/task-runner/__tests__/production-workflow.integration.test.ts
      to: .github/workflows/daily-movie-crawl.yml
      via: "workflow static contract and callback sequence"
    - from: packages/config/src/deployment-target/__tests__/production-workflow.integration.test.ts
      to: packages/config/src/deployment-target/mutation-entry.ts
      via: "target-profile/environment/prepared entry boundary"
---

<objective>
用一组跨 API、D1、签名事件、Actions workflow 和 target-profile 的 stubbed integration fixtures 收口 Phase 18 验证，并留下真实 provider proof 的清晰 handoff。

Purpose: 以 goal-backward 方式证明 PROD-01、PROD-02、PROD-03 的正向链路和关键负向分支，避免把 dispatch accepted 或 local proof 当作生产成功。
Output: integration tests、coverage gate 复核和 Phase 19 可消费的 provider evidence contract。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-github-actions-production-orchestration/18-CONTEXT.md
@.planning/phases/18-github-actions-production-orchestration/18-RESEARCH.md
@.planning/phases/18-github-actions-production-orchestration/18-PATTERNS.md
@.planning/phases/18-github-actions-production-orchestration/COVERAGE.md
</context>

<tasks>
<task type="tdd" tdd="true">
  <name>Task 1: 验证 API/D1/provider lifecycle vertical contract</name>
  <files>apps/api/src/domain/crawler-tasks/__tests__/production-orchestration.integration.test.ts, apps/api/src/routes/internal/crawler-runs/__tests__/production-events.integration.test.ts</files>
  <behavior>
    - manual create dispatches fixed movie/manga workflow after D1 run creation and stores provider snapshot.
    - schedule registration is bucket-idempotent; duplicate event reuses the same run.
    - callback/poll success needs three-way evidence; mismatch/lost/cancel/retry paths preserve audited facts and attempt boundaries.
  </behavior>
  <action>使用内存 LibSQL/D1 migration、deterministic clock/id、stubbed GitHub App/Actions fetch 和 signed event fixtures，串起 admin create → dispatch accepted → provider_started → progress/log → poll → receipt。覆盖 provider mismatch/reconciliation window/provider_lost、cancel_requested/cancel_not_effective、late event、new attempt 和 secret redaction。断言 D1 rows/events/receipt summaries，而不是只断言 HTTP status。</action>
  <verify><automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__/production-orchestration.integration.test.ts src/routes/internal/crawler-runs/__tests__/production-events.integration.test.ts</automated></verify>
  <done>PROD-01/02/03 的核心控制面事实在纯本地 fixture 中可重放，所有成功断言包含 provider、签名终态和 validated receipt 三重一致。</done>
</task>

<task type="auto">
  <name>Task 2: 验证 workflow、adapter 与 target boundary</name>
  <files>packages/crawler/src/task-runner/__tests__/production-workflow.integration.test.ts, packages/config/src/deployment-target/__tests__/production-workflow.integration.test.ts</files>
  <action>读取两份 workflow 和 prepared mutation registry，验证 manual inputs、schedule_register、provider_started、target-profile validation、Environment、production adapter、always cleanup 和 signed terminal receipt 的顺序。fixture 只使用 Gateway/local contract boundary；测试中把 local proof 与 credentialed provider proof 分成不同标签和输出文件，避免证据语义漂移。</action>
  <verify><automated>pnpm --filter crawler exec vitest run src/task-runner/__tests__/production-workflow.integration.test.ts; pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/production-workflow.integration.test.ts</automated></verify>
  <done>workflow 静态/运行 contract 证明两个模板的入口、Environment、target 和 cleanup 都受控，adapter receipt 进入正确的 API callback 顺序。</done>
</task>

<task type="auto">
  <name>Task 3: 复核 API coverage 与生产 proof handoff</name>
  <files>.planning/phases/18-github-actions-production-orchestration/COVERAGE.md</files>
  <action>重新运行 API coverage gate，确认所有 external capability 行仍有 decision；在 phase summary 中记录本地 stubbed evidence 的命令、结果和限制，并为 Phase 19 handoff 标出需要真实 GitHub App metadata、repository、Environment secrets、provider run URL 和 validated receipt 的目标 tuple。保持 credentialed remote proof 作为独立目标环境操作。</action>
  <verify><automated>node C:/Users/11407/.codex/gsd-core/bin/gsd-tools.cjs query check api-coverage.verify-pre .planning/phases/18-github-actions-production-orchestration</automated></verify>
  <done>coverage gate 通过，Phase 18 的 local contract evidence 与 Phase 19 的 remote sign-off 输入可区分、可追踪。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| test fixture → lifecycle code | synthetic provider evidence exercises production state transitions |
| workflow file → contract test | static inputs and cleanup are checked as deploy-time boundaries |
| local evidence → release handoff | local proof must retain its scope and target tuple metadata |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-16 | Tampering | integration fixtures | high | mitigate | signed event fixtures, exact provider snapshot and receipt assertions |
| T-18-17 | Information Disclosure | evidence artifacts | high | mitigate | redact token/private-key/header-shaped values and keep target proof metadata-only |
| T-18-18 | Misrepresentation | local/remote evidence handoff | medium | mitigate | separate labels, commands, target tuple and explicit Phase 19 remote gate |
</threat_model>

<verification>
<automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__/production-orchestration.integration.test.ts src/routes/internal/crawler-runs/__tests__/production-events.integration.test.ts</automated>
<automated>pnpm --filter crawler exec vitest run src/task-runner/__tests__/production-workflow.integration.test.ts</automated>
<automated>pnpm --filter @starye/config exec vitest run src/deployment-target/__tests__/production-workflow.integration.test.ts</automated>
<automated>node C:/Users/11407/.codex/gsd-core/bin/gsd-tools.cjs query check api-coverage.verify-pre .planning/phases/18-github-actions-production-orchestration</automated>
</verification>

<success_criteria>
Phase 18 的本地自动化验证覆盖三个 PROD requirement 及全部锁定 lifecycle 分支，并把 credentialed production proof 以明确 tuple 交接给 Phase 19。
</success_criteria>

<output>
Create `.planning/phases/18-github-actions-production-orchestration/18-06-SUMMARY.md` when done
</output>
