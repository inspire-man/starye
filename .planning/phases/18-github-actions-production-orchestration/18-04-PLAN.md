---
phase: 18-github-actions-production-orchestration
plan: 04
type: execute
wave: 3
depends_on:
  - 18-02
  - 18-03
files_modified:
  - apps/api/src/domain/crawler-tasks/state-machine.ts
  - apps/api/src/domain/crawler-tasks/repository.ts
  - apps/api/src/domain/crawler-tasks/reconciliation.ts
  - apps/api/src/routes/admin/crawler-tasks/index.ts
  - apps/api/src/index.ts
  - apps/api/src/domain/crawler-tasks/__tests__/state-machine.test.ts
  - apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts
  - apps/api/src/domain/crawler-tasks/__tests__/reconciliation.test.ts
  - apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts
autonomous: true
requirements:
  - PROD-01
  - PROD-03
must_haves:
  truths:
    - "手动创建先在 D1 产生 run/attempt/provider snapshot，再 dispatch 固定 workflow；dispatch 受理保持 dispatching 过程状态。"
    - "provider callback 与 GitHub poll 共享 repository/state-machine CAS，provider mismatch 进入有限 reconciliation window，窗口结束记录 provider_lost。"
    - "cancel_requested 先写入 D1 再调用 provider cancel；成功 receipt 在取消生效前完成核验时以 cancel_not_effective 记录成功优先。"
    - "管理员重试创建递增新 attempt 和新 workflow run，旧 attempt 的日志、receipt、部分入库和审计摘要继续可查询。"
  artifacts:
    - path: apps/api/src/domain/crawler-tasks/reconciliation.ts
      provides: "bounded provider poll/mismatch/lost/cancel compensation"
    - path: apps/api/src/domain/crawler-tasks/repository.ts
      provides: "D1 CAS, schedule idempotency and provider association writes"
    - path: apps/api/src/routes/admin/crawler-tasks/index.ts
      provides: "permissioned manual dispatch/cancel/retry commands"
  key_links:
    - from: apps/api/src/routes/admin/crawler-tasks/index.ts
      to: apps/api/src/lib/github-app/github-actions-client.ts
      via: "server-owned provider snapshot dispatch/cancel/poll"
    - from: apps/api/src/domain/crawler-tasks/reconciliation.ts
      to: apps/api/src/domain/crawler-tasks/repository.ts
      via: "CAS transitions and redacted compensation events"
    - from: apps/api/src/index.ts
      to: apps/api/src/domain/crawler-tasks/reconciliation.ts
      via: "scheduled waitUntil reconciliation alongside existing cleanup"
---

<objective>
把 provider association 接入 crawler task 状态机、repository、管理员命令和固定窗口 reconciliation，形成 production lifecycle 的唯一 D1 事实源。

Purpose: 落实 D-04、D-05、D-09、D-10、D-11、D-12、D-13、D-14、D-15 与 PROD-03。
Output: CAS repository/state-machine 扩展、provider polling/reconciliation、admin dispatch/cancel/retry wiring 和内存 D1/route 测试。
</objective>

<execution_context>
@C:/Users/11407/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/11407/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-github-actions-production-orchestration/18-CONTEXT.md
@.planning/phases/18-github-actions-production-orchestration/18-RESEARCH.md
@.planning/phases/18-github-actions-production-orchestration/18-PATTERNS.md
@apps/api/src/domain/crawler-tasks/state-machine.ts
@apps/api/src/domain/crawler-tasks/repository.ts
@apps/api/src/routes/admin/crawler-tasks/index.ts
@apps/api/src/index.ts
</context>

<tasks>
<task type="tdd" tdd="true">
  <name>Task 1: 扩展 repository/state-machine 的 provider CAS 与幂等</name>
  <files>apps/api/src/domain/crawler-tasks/state-machine.ts, apps/api/src/domain/crawler-tasks/repository.ts, apps/api/src/domain/crawler-tasks/__tests__/state-machine.test.ts, apps/api/src/domain/crawler-tasks/__tests__/repository.test.ts</files>
  <behavior>
    - schedule bucket 使用 template+target+workflow+scheduled_at 唯一键，重复注册返回已有 run。
    - provider_started 只接受与 dispatch snapshot 精确一致的 provider ID/attempt/workflow/ref/sha/Environment。
    - terminal success 同时检查 provider success、同一 app run/attempt/provider binding 和 validated receipt；dispatch accepted 或 provider success 单独不足以成功。
  </behavior>
  <action>沿用现有 repository batch/CAS、event idempotency、lease 和 receipt validation；加入 provider association 查询/写入、schedule register、provider binding、provider status compensation 入口。状态机保留 queued cancel→cancelled、dispatching/running cancel→cancel_requested、validated receipt 优先以及 late/out-of-sequence audit。业务 retry 产生新 attempt/new workflow run，GitHub rerun 只作为 provider infrastructure fact。为每个 CAS 结果返回结构化 decision，避免 route 依据 provider HTTP 状态猜测业务成功。</action>
  <verify><automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__/state-machine.test.ts src/domain/crawler-tasks/__tests__/repository.test.ts</automated></verify>
  <done>D1 transition/evidence history 对 provider association、schedule idempotency、receipt gate、cancel race 和 retry chain 都是可重复测试的。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 provider reconciliation 与 scheduled poll</name>
  <files>apps/api/src/domain/crawler-tasks/reconciliation.ts, apps/api/src/index.ts, apps/api/src/domain/crawler-tasks/__tests__/reconciliation.test.ts</files>
  <action>新增注入式 reconciliation service，按固定 cadence 查询已绑定 provider run，不扫描 latest/unrelated run；对 callback 缺失、status 停滞、终态不一致写入安全补偿事件。provider mismatch 进入 named reconciliation window，窗口结束走 provider_lost；timeout/5xx 采用有限退避，身份/权限/模板/target 错误立即落 terminal reason。cancel 请求先通过 repository 写 cancel_requested，再调用 Actions cancel；provider cancel response 仅作为过程证据，workflow signed cancelled 或轮询终态完成收口。将 reconciliation 与现有 scheduled cleanup 合并到 waitUntil，不重建第二套 state machine，自动业务 retry 保持关闭。</action>
  <verify><automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__/reconciliation.test.ts</automated></verify>
  <done>轮询、mismatch window、provider_lost、cancel race、provider 5xx/身份错误及 no-auto-retry 都有 clock/fetch stub 覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: 串接管理员创建、取消、重试和权限边界</name>
  <files>apps/api/src/routes/admin/crawler-tasks/index.ts, apps/api/src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</files>
  <action>保留 Better Auth session 与 movie/comic resource 权限检查；创建请求先走 repository 建 run/attempt/provider snapshot，再调用 GitHub Actions client dispatch。返回 D1 decision、provider accepted/status 的安全投影，不把 dispatch 204 映射成 success。取消先迁移 cancel_requested，再调用 provider cancel；重试必须管理员确认语义下创建新 attempt/new workflow run，旧 attempt 事实只读保留。route tests 覆盖权限、template registry、provider mismatch、cancel_not_effective、retry chain 和 token-shaped log redaction。</action>
  <verify><automated>pnpm --filter api exec vitest run src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</automated></verify>
  <done>管理员命令与 D1/Actions/reconciliation 连接，所有输入和结果投影遵守闭合资源权限与生产 provider 事实门槛。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| admin session → lifecycle command | user authorization crosses into run mutation |
| provider poll/callback → D1 CAS | external evidence crosses into business state |
| cancel/retry command → provider API | asynchronous provider mutation races with local state |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-10 | Tampering | repository/provider binding | critical | mitigate | immutable snapshot, status/version/sequence CAS and provider mismatch audit |
| T-18-11 | Denial of Service | reconciliation loop | high | mitigate | fixed cadence/window, bounded retries and provider_lost terminal fact |
| T-18-12 | Inconsistent State | cancel/retry race | high | mitigate | cancel_requested first, validated receipt precedence, new attempt history |
</threat_model>

<verification>
<automated>pnpm --filter api exec vitest run src/domain/crawler-tasks/__tests__</automated>
<automated>pnpm --filter api exec vitest run src/routes/admin/crawler-tasks/__tests__/crawler-tasks.route.test.ts</automated>
<automated>pnpm --filter api type-check</automated>
</verification>

<success_criteria>
API lifecycle、provider evidence、reconciliation 和管理员命令共享同一个 D1 CAS 状态机；late/mismatch/dispatch-only 事实均保持可审计且不形成伪成功。
</success_criteria>

<output>
Create `.planning/phases/18-github-actions-production-orchestration/18-04-SUMMARY.md` when done
</output>
