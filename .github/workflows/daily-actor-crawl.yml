name: Retired Daily Actor Crawl

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Report retired workflow
        run: |
          echo "Actor crawl is retired; the production registry supports only Manga and Movie templates."
