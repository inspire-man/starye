name: Retired Daily Publisher Crawl

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Report retired workflow
        run: |
          echo "Publisher crawl is retired; the production registry supports only Manga and Movie templates."
