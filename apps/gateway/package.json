{
  "name": "gateway",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "pnpm --workspace-root target-deploy -- --app gateway",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260417.1",
    "@sentry/cloudflare": "^10.53.1",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4",
    "wrangler": "^4.90.0"
  }
}
